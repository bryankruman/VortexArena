############################################################################################
FINDING 1 [MAJOR] conf=high  AREA: INTEGRATION (the 9 cross-task seams in GameWorld.cs / Commands.cs / DamageSystem.cs / NetGame.cs / Shell.cs)
TITLE: Seated vehicle pilot/gunner fires their PERSONAL hand weapon while firing the vehicle weapon (port lacks QC's pre-W_WeaponFrame button clear)
FILE: src/Rebirth.Server/GameWorld.cs :: OnClientMove seated-gate (lines 788-795) -> OnPlayerPostThink (817-847) -> WeaponThink (856-863) -> WeaponFireDriver.Frame; cf. Racer.cs Frame end (lacks player-button clear); WeaponFireDriver.cs WeaponUseForbidden=>false (316) / WeaponLocked (328, no vehicle gate)
PROBLEM: The T37 SEAM A gate stashes the input copy onto p.VehInput / p.Vehicle.VehInput, skips Movement.Move, then calls OnPlayerPostThink(p). OnPlayerPostThink unconditionally runs WeaponThink(p) for any alive non-observer, and WeaponThink re-reads InputProvider(p) (the LIVE input, with ButtonAttack1/2 still held by the pilot to fire the vehicle cannon). The seated player still has their real ActiveWeaponId/OwnedWeapons (EnterVehicle never strips weapons, and the port has no temp_wepent swap), and neither WeaponUseForbidden (always false) nor WeaponLocked (only checks Frozen) gates on player.Vehicle. So holding +attack to fire the Racer/Raptor/Spiderbot cannon (or the Bumblebee gun, GunnerFrame reads gunner.VehInput.ButtonAttack1) ALSO fires the pilot's/gunner's hand weapon every tick the refire gate allows: phantom projectiles spawn from the player origin, the pilot's ammo is consumed, and the personal refire timers churn. Worse, the order is inverted vs QC: WeaponFireDriver runs in tick step 2 (OnClientMove) while the vehicle Frame runs in step 3 (vehicle Think, SimulationLoop.cs:176-182), so the personal weapon actually fires BEFORE the vehicle frame runs at all, and the vehicle frame only clears the discarded VehInput copy (Racer.cs:385) — never the player's input source.
BASE: QC SV_PlayerPhysics is documented `// called by engine before it calls PlayerPreThink()` (common/physics/player.qc:859); it runs the seated player's PlayerPhysplug = veh.PlayerPhysplug (set in vehicles_enter, sv_vehicles.qc:1024) i.e. the vehicle *_frame, which CLEARS the player's buttons IN PLACE at its end: racer_frame `PHYS_INPUT_BUTTON_ATCK(player) = PHYS_INPUT_BUTTON_ATCK2(player) = false;` (racer.qc:181 and :385; raptor/spiderbot/bumblebee likewise). THEN PlayerPreThink->PlayerThink->W_WeaponFrame (client.qc:2466) reads `PHYS_INPUT_BUTTON_ATCK(actor)` which is now 0 (weaponsystem.qc:482-483) -> the personal weapon does not fire. (vehicles_enter additionally swaps each weaponentities[] to a temp_wepent on the VEHICLE, sv_vehicles.qc:1016-1020.) The port reproduces neither the in-place player-button clear nor the temp_wepent swap, and inverts the frame order, so the suppression QC relies on is absent.
FIX: Gate the personal weapon driver off for a seated player. Minimal/faithful: in GameWorld.OnPlayerPostThink (or WeaponThink) skip WeaponThink when p.Vehicle is not null; OR early-out at the top of WeaponFireDriver.Frame when player.Vehicle is not null (after the Health<1 guard). Note QC still runs W_WeaponFrame's switch state machine while seated (only the FIRE is suppressed via cleared buttons), so the most faithful shape is to zero the attack buttons fed to WeaponFireDriver while seated (mirror racer_frame's clear) rather than skipping the whole frame — but a simple skip is acceptable since the port doesn't model weapon-switch-while-seated. This also fixes the Bumblebee gunner (same root cause).
############################################################################################
FINDING 2 [MAJOR] conf=high  AREA: T28 — Menu skin/theme loader (skinvalues.txt → SKIN* table) + Localization (gettext .po engine, CTX, languages.txt, Tr seam, menu_restart wiring)
TITLE: Localization feature is inert at runtime — no .po files ship in the content repo
FILE: game/menu/framework/Localization.cs :: SetLanguage() lines 71-102 (ReadIfExists "common.<lang>.po" / "menu.dat.<lang>.po")
PROBLEM: The PO engine, Tr seam, CtxTr, language picker, prvm_language/menu_restart wiring are all complete and correctly wired, but the runtime asset repo Rebirth.Assets/data/xonotic-data.pk3dir/ ships ZERO .po files (verified: `ls *.po` = 0 loose, and the two pk3 archives xonotic-20230620-{maps,nexcompat}.pk3 contain none). MEMORY confirms Base/ is NOT read at runtime. So at runtime SetLanguage("de", vfs) → vfs.Exists("common.de.po")==false → PoCatalog.Empty → every UI string stays English even after the user selects a non-English language and the menu restarts. The whole translation feature does nothing live. Build+tests miss this because PoCatalogTests.Real_German_Common_Po_Slice_Parses / LanguagesTxtTests probe Rebirth.Assets FIRST then fall through to the Base reference checkout (which DOES have the 60 common.*.po), so the tests pass against Base data the shipped game can't see.
BASE: darkplaces/prvm_edict.c:2633-2636 PRVM_PO_Load("%s.%s.po" realfilename prvm_language, "common.%s.po" prvm_language) — the real game ships common.<lang>.po (60 files present in Base/data/xonotic-data.pk3dir) so translation is functional in DP. The port reproduces the load path faithfully but the data files are absent from the runtime gamedir.
FIX: Add the common.*.po files (at minimum the languages listed in languages.txt) to Rebirth.Assets/data/xonotic-data.pk3dir/ so SetLanguage finds them via the VFS; OR if localization is intentionally deferred, document the limitation (and ideally have the picker note that text language is not yet applied) so it isn't presented as a working feature. The code itself needs no change.
############################################################################################
FINDING 3 [MAJOR] conf=high  AREA: T37 — Vehicle runtime seam (board / input / impulse / return)
TITLE: Seated pilot/gunner still fires their on-foot handheld weapon every tick (live input attack buttons are never zeroed)
FILE: src/Rebirth.Server/GameWorld.cs :: OnClientMove seated-gate (lines 788-795) -> OnPlayerPostThink -> WeaponThink (856-863) -> WeaponFireDriver.Frame
PROBLEM: The T37 seated-gate stashes the input on VehInput and then calls OnPlayerPostThink(p), which runs WeaponThink(p) -> WeaponFireDriver.Frame(p, InputProvider(p)) with the LIVE per-tick input. That input still has ButtonAttack1/ButtonAttack2 set (the pilot holds fire to shoot the vehicle cannon), and WeaponFireDriver has no `p.Vehicle != null` guard, so the player's currently-equipped handheld weapon (blaster/shotgun/etc.) fires from inside the cockpit, dealing damage and draining the player's ammo, simultaneously with the vehicle weapon. This is invisible to the unit tests (they never give the boarded player a weapon nor run the GameWorld weapon frame).
BASE: Every QC vehicle *_frame ENDS by zeroing the live input attack buttons: spiderbot.qc:304 / raptor.qc:432 / racer / bumblebee all do `PHYS_INPUT_BUTTON_ATCK(this) = PHYS_INPUT_BUTTON_ATCK2(this) = false;`. Those frames are the player's PlayerPhysplug and run during the player's physics step BEFORE PlayerThink's weapon loop (client.qc:2461-2467 `W_WeaponFrame(this, weaponentity)`), so by the time W_WeaponFrame runs the buttons are false and the handheld weapon does not fire. (vehicles_enter also swaps veh.(weaponentity) to temp_wepent, but the suppression that actually matters is the per-frame button-zero.) The port reads a MovementInput struct COPY off VehInput and runs the vehicle Frame from the vehicle's own Think (step 3, AFTER the weapon frame in step 2), so it can never zero the live buttons.
FIX: Do not run the on-foot weapon driver for a seated player. Simplest: in the seated-gate, skip the weapon frame — e.g. have WeaponThink/OnPlayerPostThink early-out of WeaponFireDriver.Frame when p.Vehicle != null (QC's temp_wepent makes the player's W_WeaponFrame a no-op fire). Keep the rest of OnPlayerPostThink (drown/regen) running as the recon intends.
############################################################################################
FINDING 4 [MAJOR] conf=high  AREA: T37 — Vehicle runtime seam (board / input / impulse / return)
TITLE: Bumblebee auto-seats a same-team player on CONTACT in the default use-key config (missing the g_vehicles_enter gate)
FILE: src/Rebirth.Common/Gameplay/Vehicles/Bumblebee.cs :: Touch(Entity vehicle, Entity toucher) — lines 151-164
PROBLEM: Bumblebee.Touch is installed as the vehicle's persistent .Touch (Spawn line 122, Enter line 146) and a moving vehicle DOES dual-dispatch .Touch via the engine (PhysicsToss -> PushEntity(doTouch:true) -> Impact -> vehicle.Touch, FlyMove.cs:86-90; Bumblebee is SOLID_BBox and uses MOVETYPE_TOSS/BOUNCEMISSILE which route through PhysicsToss, MoveTypePhysics.cs:71-79). Bumblebee.Touch has NO `g_vehicles_enter` gate, so whenever a piloted/landing bumblebee touches a valid same-team on-foot player it calls GunnerEnter and yanks them into a gunner seat WITHOUT them pressing +use. In the shipped default (g_vehicles_enter=1, use-key) boarding must require +use; a teammate should never be auto-boarded by getting bumped, and this can hijack a teammate mid-fight.
BASE: QC bumblebee_touch (common/vehicles/vehicle/bumblebee.qc) opens with `if (autocvar_g_vehicles_enter) return;` — touch-based gunner entry only happens in TOUCH mode (g_vehicles_enter==0). g_vehicles_enter defaults to 1 (sv_vehicles.qh), so in the default config bumblebee_touch is a no-op (it then only falls to vehicles_touch for crush/impact). The port's recon (recon-T37.md lines 459-460, 277-279) explicitly states touch-mode is the non-default partial and use-key (+use) is the parity path.
FIX: Mirror QC: at the top of Bumblebee.Touch add `if (Cvar("g_vehicles_enter", 1f) != 0f) return;` (the same shipped-default cvar read VehicleBoarding uses). Gunner boarding in the default config already works correctly via VehicleBoarding.Enter's MULTISLOT branch on +use, so gating Touch loses nothing in the default and restores parity.
############################################################################################
FINDING 5 [MAJOR] conf=high  AREA: T51 — remaining mutators (doublejump/hook/bugrigs/campcheck/damagetext/itemstime + P3 stubs), the 5 Overkill weapons, doublejump fix, DamageTextLayer + the PlayerDamaged fire-site
TITLE: OkHmg superweapon gate is bypassed — the HMG fires with no Superweapon status
FILE: Rebirth/src/Rebirth.Common/Gameplay/Weapons/OkHmg.cs :: WrThink (line 91-100) -> AttackAuto (line 116-147); the gate lives only in CheckAmmoPrimary (line 150-151) and the reschedule lambda (line 142)
PROBLEM: In QC the superweapon requirement is enforced INSIDE the fire function W_OverkillHeavyMachineGun_Attack_Auto (okhmg.qc:22-28: `(!wr_checkammo1 && !IT_UNLIMITED_AMMO) || (!StatusEffects_active(STATUSEFFECT_Superweapon) && !IT_UNLIMITED_SUPERWEAPONS)` -> W_SwitchWeapon_Force + w_ready, firing NOTHING). The port instead bundled the superweapon test into wr_checkammo1 (OkHmg.CheckAmmoPrimary) and left AttackAuto with no entry guard. wr_checkammo1 is reached via PrepareAttack -> CheckAmmoWithAutoSwitch, but that method short-circuits `if (actor.UnlimitedAmmo || (actor.Items & ItUnlimitedAmmo) != 0) return true;` (WeaponFireGate.cs:136) BEFORE calling WrCheckAmmo — and the Overkill loadout always grants UNLIMITED_AMMO (OverkillMutator.OnSetStartItems adds "UNLIMITED_AMMO"). So the superweapon check is never consulted on the firing path. Each tick the driver calls WrThink(Primary) -> PrepareAttack(attackTime:0f) -> AttackAuto fires one bullet with NO superweapon check; the only superweapon test (the reschedule lambda) just gates its own re-fire, but the driver re-enters via WrThink the next time the slot is Ready, so the HMG fires continuously (one bullet per refire) without ever holding the Superweapon status. QC fires zero and switches away.
BASE: okhmg.qc:14-28 (W_OverkillHeavyMachineGun_Attack_Auto gates on `!StatusEffects_active(STATUSEFFECT_Superweapon, actor) && !(actor.items & IT_UNLIMITED_SUPERWEAPONS)` -> switch+ready, no shot); okhmg.qc:116-122 (wr_checkammo1 is AMMO ONLY — it does NOT include the superweapon test).
FIX: Move the superweapon gate out of CheckAmmoPrimary and into AttackAuto's entry (and the WrThink primary branch), matching QC: at the top of AttackAuto (and before the first AttackAuto call), if `!HasSuperweapon(actor)` then SwitchToOtherWeapon + return without firing. Keep CheckAmmoPrimary as ammo-only (bullets >= ammo, plus the clip term) to mirror okhmg.qc:116-122.
############################################################################################
FINDING 6 [MAJOR] conf=high  AREA: T51 — remaining mutators (doublejump/hook/bugrigs/campcheck/damagetext/itemstime + P3 stubs), the 5 Overkill weapons, doublejump fix, DamageTextLayer + the PlayerDamaged fire-site
TITLE: All 5 Overkill weapons are missing from the WeaponAmmo.Check dispatcher -> wr_checkammo never runs for them
FILE: Rebirth/src/Rebirth.Common/Gameplay/Weapons/WeaponFireGate.cs :: WeaponAmmo.Check switch (lines 478-500); the base Weapon.WrCheckAmmo (line 186-187) dispatches every weapon's ammo check through it
PROBLEM: The central ammo dispatcher `WeaponAmmo.Check(Weapon w, ...)` is a type switch with explicit cases per weapon and a `_ => true` fallback. None of OkMachinegun/OkShotgun/OkNex/OkHmg/OkRpc is listed, so they fall through to `_ => true` — meaning the base WrCheckAmmo always reports 'has ammo' for them and their CheckAmmoPrimary/CheckAmmoSecondary (each a faithful wr_checkammo1/2 port reading GetResource >= ammo) is dead code on the firing path. Consequences: (a) the OK weapons never dry-fire and never auto-switch-away when truly empty (W_SwitchToOtherWeapon is never triggered); (b) this is the mechanism that makes the OkHmg superweapon gate (finding above) dead even before the unlimited-ammo short-circuit. Largely masked in stock Overkill play by the always-granted UNLIMITED_AMMO, but it is a latent correctness bug for any config without unlimited ammo (e.g. a custom ruleset, or the okrpc/okhmg superweapon cases) and breaks the wr_checkammo contract the port relies on everywhere else.
BASE: okmachinegun.qc:115-126, okshotgun.qc:72-82, oknex.qc:261-279, okhmg.qc:116-130, okrpc.qc:194-206 all define real wr_checkammo1/wr_checkammo2 (GetResource(actor, ammo_type) >= ammo, plus the clip term) — i.e. QC DOES gate the OK weapons on ammo; the port silently treats them as infinite via `_ => true`.
FIX: Add the 5 Ok* cases to WeaponAmmo.Check, mirroring the existing pattern: `OkMachinegun m => m.CheckAmmoPrimary(actor), OkShotgun s => secondary ? s.CheckAmmoSecondary(actor) : s.CheckAmmoPrimary(actor), OkNex n => n.CheckAmmoPrimary(actor), OkHmg h => secondary ? h.CheckAmmoSecondary(actor) : h.CheckAmmoPrimary(actor), OkRpc r => secondary ? r.CheckAmmoSecondary(actor) : r.CheckAmmoPrimary(actor),`. (Note: keep the OkHmg superweapon gate OUT of CheckAmmoPrimary per the other finding.)
############################################################################################
FINDING 7 [MAJOR] conf=medium  AREA: T51 — remaining mutators (doublejump/hook/bugrigs/campcheck/damagetext/itemstime + P3 stubs), the 5 Overkill weapons, doublejump fix, DamageTextLayer + the PlayerDamaged fire-site
TITLE: bugrigs body-pitch align uses FixedVecToAngles2 where QC uses plain vectoangles2 (inverted car pitch on slopes)
FILE: Rebirth/src/Rebirth.Common/Gameplay/Mutators/BugrigsMutator.cs :: RaceCarPhysics, the down-4u surface-align block, line 317 (`self.Angles = QMath.FixedVecToAngles2(aimVec, pn);`)
PROBLEM: QC sets `this.angles = vectoangles2(aimVec, trace_plane_normal)` (bugrigs.qc:271-278) and then immediately feeds it into `makevectors(this.angles)` at the start of the angle-smoothing block (bugrigs.qc:294). QMath.VecToAngles2 is the faithful numeric equivalent of QC vectoangles2 (it already applies the flippitch the builtin uses — QMath.cs:135-160), so to match QC this line must use VecToAngles2, NOT FixedVecToAngles2. FixedVecToAngles2 negates pitch (QMath.cs:163-168), so AngleVectors(self.Angles) in the smoothing block produces forward.Z of the OPPOSITE sign vs QC, inverting the car's body pitch when it rests on / drives over a slope (the down-4u trace hits a surface). Because this.angles also seeds next frame's drive basis (PM_Physics restores player.angles = bugrigs_prevangles, then RaceCarPhysics' top makevectors), the inverted pitch also perturbs slope driving, not just the visual tilt. Tellingly, the FINAL smoothing output on line 341 correctly uses plain VecToAngles2 (+ explicit `-smoothangles.X`), so the file is internally inconsistent about the same QC `vectoangles2` call — a strong signal line 317 is the error. (Flat ground takes the racecar_angle else-branch and is unaffected; line 287's Fixed in the planar block is harmless because that angles value is overwritten before use.)
BASE: bugrigs.qc:271-278 `this.angles = vectoangles2(...)` then bugrigs.qc:292-305 `makevectors(this.angles); ...; smoothangles = vectoangles2(vf1,vu1); this.angles_x = -smoothangles.x; this.angles_z = smoothangles.z;` — QC uses plain vectoangles2 at BOTH spots; the port matched the second (line 341) but not the first (line 317).
FIX: Change line 317 to `self.Angles = QMath.VecToAngles2(aimVec, pn);` to match QC's plain vectoangles2-into-makevectors sequence (consistent with the correct line 341).
############################################################################################
FINDING 8 [MAJOR] conf=medium  AREA: T56 — Generic + client + reply commands (+ engine defer / RPN VM)
TITLE: suggestmap ignores g_maplist_votable_suggestions disable + skips all map validation (accepts nonexistent/recent/wrong-gametype maps)
FILE: C:/Users/Bryan/Projects/Xonotic/Rebirth/src/Rebirth.Server/Commands.cs :: CmdSuggestMap, lines 1320-1338
PROBLEM: The port's CmdSuggestMap only dedups against prior suggestions then accepts the map. QC MapVote_Suggest (mapvoting.qc:130) performs SIX gates the port omits: (1) if(!autocvar_g_maplist_votable_suggestions) return 'Suggestions are not accepted on this server.' — this cvar ships as 2 (xonotic-server.cfg:379) and 0 disables suggestions, but the port never reads it so an admin's disable is ignored; (2) if(mapvote_initialized && !gametypevote) 'voting already in progress'; (3) GameTypeVote_MapInfo_FixName(m) — REJECTS a map that doesn't exist on the server ('not available on this server'); (4) Map_IsRecent recent-map rejection; (5) MapInfo_CheckMap gametype-support rejection. The port therefore accepts arbitrary/garbage strings (e.g. 'suggestmap doesnotexist' succeeds) and honors none of the server cvars. Also the reply text diverges: QC says 'Suggestion of <m> accepted.' / 'This map was already suggested.'; the port prepends ^2/^1 and adds quotes + extra wording.
BASE: server/mapvoting.qc:130-166 MapVote_Suggest — the m==''/g_maplist_votable_suggestions/mapvote_initialized/GameTypeVote_MapInfo_FixName(map-exists)/Map_IsRecent/MapInfo_CheckMap gate cascade before the dedup+accept; xonotic-server.cfg:379 set g_maplist_votable_suggestions 2 (0=disabled). cmd.qc:907-929 ClientCommand_suggestmap → sprint(MapVote_Suggest(...)).
FIX: At minimum honor the disable switch — early-return 'Suggestions are not accepted on this server.' when Cvars.Float("g_maplist_votable_suggestions")==0 (the cvar is already in Cvars.cs:307). Ideally also reject maps not in the catalog (wire a MapExists/MapRotation check like the console-side MapExists hook) and match QC's accept/dup message strings. Note the downstream ballot integration is itself a deferred seam, so the most defensible immediate fix is the disable-cvar gate + map-existence check.
############################################################################################
FINDING 9 [MINOR] conf=medium  AREA: T28 — Menu skin/theme loader (skinvalues.txt → SKIN* table) + Localization (gettext .po engine, CTX, languages.txt, Tr seam, menu_restart wiring)
TITLE: skinvalues value-substring diverges from tokenize_console on an inline trailing // comment
FILE: src/Rebirth.Common/Localization/SkinValues.cs :: SplitKeyValue() lines 151-173 (value runs to last non-ws char of the whole line)
PROBLEM: menu.qc uses substring(s, argv_start_index(1), argv_end_index(-1)-argv_start_index(1)) where argv_* come from tokenize_console, which STOPS at a // comment mid-line. So for a line `FONTSIZE_NORMAL 12 // note`, QC's value is "12" (atof→12). The port's SplitKeyValue takes the verbatim remainder to the last non-ws char → "12 // note", and Stof("12 // note") fails float.TryParse → 0. A vector with a trailing comment would likewise yield a partly-zeroed vector. This is a genuine behavioral divergence from Base, but is currently LATENT: verified none of the four shipped skins (luma/luminos/wickedx/xaw) has any `KEY value // comment` line — all comments are full-line `// ...`, and the Perl generator emits `%-31s %s` with no trailing comment. So no shipped/regenerated skin file triggers it; only a hand-edited skin using DP's inline-comment convention would.
BASE: menu.qc:199 Skin_ApplySetting(argv(0), substring(s, argv_start_index(1), argv_end_index(-1) - argv_start_index(1))) — tokenize_console (COM_ParseToken_Console) treats // as a line comment, so argv_end_index(-1) is the end of the last REAL token before the comment.
FIX: In SplitKeyValue, truncate the value at an unquoted `//` (mirroring tokenize_console) before trimming trailing whitespace; or document it as an accepted limitation given no shipped file uses inline comments. Low priority.
############################################################################################
FINDING 10 [MINOR] conf=high  AREA: T30 — golden-trace loop + TraceResult.DpHitTextureName population
TITLE: verify-against-dp.md §2.1 mis-reasons the WallFriction default: claims the port honors sv_wallfriction=1 and that Defaults.WallFriction=0 is the error — the truth is the opposite (the QC body is commented out, so wall friction must NEVER run; 0 is the correct value, and the port already reads 0 today)
FILE: Rebirth/tools/movement-ref/verify-against-dp.md (lines 153-167, §2.1); latent code at Rebirth/src/Rebirth.Common/Physics/PlayerPhysics.cs:1136-1138,1181-1195 :: verify-against-dp.md §2.1 "Latent note" para (lines 161-167); PlayerPhysics.WallFriction (1181) + the gate at FlyMove line 1137
PROBLEM: The T30 deliverable-1 doc frames the WallFriction situation backwards. It asserts (a) live DP has sv_wallfriction=1, (b) "real gameplay honors wallfriction=1" via MovementParameters.FromCvars, and (c) MovementParameters.Defaults.WallFriction=0 (MovementParameters.cs:197) is therefore "a real default-doc error worth a follow-up." All three framings are wrong about the SHIPPING behavior: the QC _Movetype_WallFriction BODY is entirely commented out (movetypes.qc:104-116), so stock Xonotic applies wall friction ZERO times regardless of the cvar's value. Therefore the faithful behavior is to never run wall friction, which is exactly what WallFriction=0 achieves — so Defaults.WallFriction=0 is CORRECT, not an error. Separately, claim (b) is factually false for the port as it stands: the port never registers sv_wallfriction (no .cfg/Register sets it; EngineServices.GetFloat returns 0f for any unregistered cvar, EngineServices.cs:289), so CvarRaw("sv_wallfriction",0) reads 0 and PlayerPhysics.cs:1137 (`mp.WallFriction != 0`) never fires the body — i.e. the port does NOT currently honor 1. The genuine latent risk the doc should have flagged is the reverse: PlayerPhysics.cs:1181-1195 ports the commented-out QC body as a LIVE method gated only on the cvar, so IF any future host ever registers sv_wallfriction to DP's engine default 1 (plausible in a DP-defaults import pass), the port would start applying wall friction that stock Xonotic never does — diverging on wall-strafe/surf velocity. MoveTypePhysics.cs:404 already handles this correctly by making the body a hard no-op ("#if 0 in DP"); PlayerPhysics does not.
BASE: qcsrc/common/physics/movetypes/movetypes.qc:102-117 — `void _Movetype_WallFriction(entity this, vector stepnormal) { /* ...entire body commented out... */ }`. qcsrc/common/stats.qh:422 declares `float autocvar_sv_wallfriction;` with NO `=default` (unlike stats.qh:396 `...q2airaccelerate = 1;`), and REGISTER_STAT(MOVEVARS_WALLFRICTION,...) only networks it; the value is irrelevant because the body is empty.
FIX: Two parts. (1) Correct verify-against-dp.md §2.1: state that stock Xonotic NEVER applies wall friction because the QC body is commented out, so WallFriction=0 is the faithful value (not a doc error), and the live-engine cvar reading 1 is moot. (2) Make PlayerPhysics.WallFriction (PlayerPhysics.cs:1181) a no-op that matches the commented-out QC — mirror MoveTypePhysics.cs:404's `#if 0` handling — OR at minimum remove the misleading "follow-up" framing and keep the gate at Defaults=0 with a comment that the cvar must stay unregistered/0. This is a PlayerPhysics file (not T30-owned), so T30's correct action is to fix the doc claim and flag the code mismatch, not edit PlayerPhysics here.
############################################################################################
FINDING 11 [MINOR] conf=high  AREA: T36 — Untracked-mode map-objective spawnfuncs (Assault / Nexball / Invasion / CTS, + KeyHunt note)
TITLE: func_assault_wall provides no collision (should be an initially-solid SOLID_BSP gate)
FILE: src/Rebirth.Server/GameWorld.cs :: WireObjectiveSpawns Assault arm, case "func_assault_wall" (lines 1164-1168) then falls through to RetirePlaceholder (1177)
PROBLEM: QC spawnfunc(func_assault_wall) (sv_assault.qc:364-374) makes a SOLID_BSP brush whose model/solid TOGGLES with its .enemy objective's health (assault_wall_think): while the objective is INACTIVE (health=ASSAULT_VALUE_INACTIVE>=0) the wall is SOLID and BLOCKS players; it only goes SOLID_NOT once the objective is destroyed (health<0). The port's arm is a pure no-op: it neither resolves the brush bounds (no SetModel, unlike the destructible arm at 1148-1149) nor marks it solid, then retires the placeholder. So a func_assault_wall has ZERO collision from match start: a gate that should block the route to an objective is open immediately, letting players bypass/skip the gated objective.
BASE: sv_assault.qc:364-374 spawnfunc(func_assault_wall){ ... this.solid = SOLID_BSP; setthink(this, assault_wall_think); ... } and :178-192 assault_wall_think toggles model/solid on GetResource(this.enemy,RES_HEALTH)<0 (solid until the objective is destroyed).
FIX: In the func_assault_wall case keep the edict as a solid wall: resolve its brush model (if(!string.IsNullOrEmpty(e.Model)) Api.Entities.SetModel(e,e.Model);), set e.Solid=Solid.Bsp, register it against its objective by name (e.Target) via the deferred graph, and flip it Solid.Not once that objective is destroyed; do NOT retire it. If the toggle is out of P0 scope, at minimum make it a permanent SOLID_BSP wall (resolve model + Solid.Bsp + return, skipping RetirePlaceholder) so the blocking geometry exists.
############################################################################################
FINDING 12 [MINOR] conf=high  AREA: T36 — Untracked-mode map-objective spawnfuncs (Assault / Nexball / Invasion / CTS, + KeyHunt note)
TITLE: Invasion STAGE round-end trigger is zero-sized so it never fires via the live engine touch grid
FILE: src/Rebirth.Server/GameWorld.cs :: WireObjectiveSpawns Invasion arm, case "target_invasion_roundend" (lines 1207-1214)
PROBLEM: The arm reuses the placeholder edict as the end trigger: it sets e.Touch + e.Solid=Solid.Trigger and returns WITHOUT setting a bbox. SpawnMapEntities/ApplyDictFields never call SetModel/SetSize on the placeholder, so its mins/maxs are zero; a zero-size trigger never overlaps a player in the engine touch grid, so TriggerRoundEnd never fires through any live path in a STAGE match. (The CTS/Nexball arms instead call gametype Spawn* -> GametypeEntities.SpawnObjective -> SetSize, getting a real box.) The unit test passes only because it invokes roundend.Touch directly. Also the port wires e.Touch (touch-fired) whereas QC's target_invasion_roundend is .use-fired by a separate targeting trigger, so even a QC-style SUB_UseTargets chain wouldn't reach the port's touch handler.
BASE: sv_invasion.qc:45-56 spawnfunc(target_invasion_roundend) sets this.use=target_invasion_roundend_use and IL_PUSHes it; :25-43 the use handler requires IS_PLAYER(actor) and counts 70% of players. The entity is fired through a .use chain over its real (brush) volume.
FIX: Give the kept roundend trigger a real volume: resolve its brush model (Api.Entities.SetModel(e,e.Model)) when present, else SetSize a default box, so the touch grid can fire it; OR (closer to QC) install e.Use (not e.Touch) so a map's targeting trigger reaches it via SUB_UseTargets. STAGE is a minority invasion type (recon-deferred), so low-urgency, but as written STAGE invasion has no functioning win trigger in-game.
############################################################################################
FINDING 13 [MINOR] conf=high  AREA: T43 — Monster damage → pain/death/reset seam + global counters (common/monsters/sv_monsters.qc)
TITLE: Global monster counters (MonstersTotal/MonstersKilled) leak across maps/restarts — ResetCounters() never wired to map-load/match-reset
FILE: Rebirth/src/Rebirth.Common/Gameplay/Monsters/MonsterAI.cs (ResetCounters, line 1504) + Rebirth/src/Rebirth.Server/GameWorld.cs (ResetMap, line 1827) :: MonsterAI.ResetCounters() — only caller is Commands.cs:1612 (the mobkill/butcher command). GameWorld.ResetMap (1827) and Invasion.Activate (1200-area) do NOT call it.
PROBLEM: MonstersTotal/MonstersKilled are process-global statics. In QC `monsters_total`/`monsters_killed` are server globals cleared when a new map spawns. The port only zeroes them on the `editmob butcher` command, never on map change or ResetMap (ready-restart). So the scoreboard map-stats feed (NetGame.FeedScoreboardHeader:1128-1129 reads these statics) shows CUMULATIVE 'Monsters killed: K/T' across every Invasion map played in one process run — map 2's spawns add on top of map 1's totals.
BASE: sv_monsters.qc:1427 (++monsters_total) and :1047 (++monsters_killed) are server globals (sv_monsters.qh); worldspawn/map reset zeroes them with the global state. Commands.cs:1611 comment itself says 'QC: monsters_total = monsters_killed = totalspawned = 0'. Recon-T43 §6 step 5 explicitly required: 'Call ResetCounters() from the map/gametype reset path (world boot / gametype OnInit)' — this wire-up was not done.
FIX: Call MonsterAI.ResetCounters() from the map-load / match-restart path: either in GameWorld.ResetMap (when !fakeRoundStart, alongside Scores.ClearAll) or in Invasion.Activate (next to its own `MonstersKilled = 0`). Mirrors QC clearing the globals on map spawn.
############################################################################################
FINDING 14 [MINOR] conf=high  AREA: T43 — Monster damage → pain/death/reset seam + global counters (common/monsters/sv_monsters.qc)
TITLE: Monster default armor never seeded — armor block is 0 instead of QC's bound(0.2, 0.5*skillmod, 0.9), so damage numbers are off
FILE: Rebirth/src/Rebirth.Common/Gameplay/Monsters/MonsterAI.cs (Setup, lines 198-297) :: MonsterAI.Setup — seeds health/speeds/ranges/respawn/shield but never assigns e.ArmorValue. MonsterEventDamage:1415 then computes armorBlock = self.ArmorValue/100f = 0/100 = 0.
PROBLEM: QC Monster_Spawn_Setup (sv_monsters.qc:1327-1328) gives EVERY monster a default armor `if (!RES_ARMOR) SetResourceExplicit(RES_ARMOR, bound(0.2, 0.5*MONSTER_SKILLMOD(this), 0.9))` (~0.295 at skill 1). The armor split in Monster_Damage uses RES_ARMOR/100 as the block fraction (~0.00295), so QC monsters absorb a small slice of each hit. The port never seeds ArmorValue, so armorBlock=0 and monsters take the FULL raw damage. A 100-dmg hit takes ~99.7 in QC vs exactly 100 in the port.
BASE: sv_monsters.qc:1327-1328 default-armor seed (runs for every monster in mr_setup) + :1091 healtharmor_applydamage(100, GetResource(this,RES_ARMOR)/100, ...). Magnitude is sub-1% (recon §1.10 calls monster armor 'near-cosmetic numerically'), but the value is non-zero in QC and zero in the port.
FIX: In Setup, after resolving skill, seed the default like QC: `if (e.ArmorValue <= 0f) e.ArmorValue = QMath.Bound(0.2f, 0.5f * SkillMod(st), 0.9f);` (matching sv_monsters.qc:1328). Note the test Damage_LiveMonster...NotPlayerCorpseSemantics asserts exactly startHp-50 and would need updating to startHp-50*(1-armorblock).
############################################################################################
FINDING 15 [MINOR] conf=medium  AREA: T43 — Monster damage → pain/death/reset seam + global counters (common/monsters/sv_monsters.qc)
TITLE: INVINCIBLE monster wrongly soaks lava/slime/void/swamp damage (ITEM_DAMAGE_NEEDKILL exemption missing)
FILE: Rebirth/src/Rebirth.Common/Gameplay/Monsters/MonsterAI.cs (MarkPain, lines 1268-1273) :: MarkPain — `if (!isKill) { if ((self.SpawnFlags & MonsterFlag_Invincible) != 0) return 0f; ... }`
PROBLEM: QC Monster_Damage:1085 invuln gate is `(spawnflags & MONSTERFLAG_INVINCIBLE) && deathtype != DEATH_KILL && !ITEM_DAMAGE_NEEDKILL(deathtype)` — an INVINCIBLE monster STILL takes hurttrigger/void, slime, lava, and swamp damage. The port blocks ALL non-kill damage for an INVINCIBLE monster, so an INVINCIBLE monster standing in lava or a kill-volume is immortal in the port but dies in QC.
BASE: sv_monsters.qc:1085 + server/items/items.qh:123 `#define ITEM_DAMAGE_NEEDKILL(dt) ((dt)==DEATH_HURTTRIGGER || DEATH_SLIME || DEATH_LAVA || DEATH_SWAMP)`. The port's DeathTypes has Void/Slime/Lava/Swamp constants but MarkPain ignores them in the invuln gate.
FIX: Add an ITEM_DAMAGE_NEEDKILL helper to DeathTypes (BaseOf in {Void, Slime, Lava, Swamp}) and change the gate to `if ((self.SpawnFlags & MonsterFlag_Invincible) != 0 && !DeathTypes.NeedsKill(deathType)) return 0f;`. The SpawnShield branch correctly stays unconditional (QC only exempts KILL there).
############################################################################################
FINDING 16 [MINOR] conf=medium  AREA: T43 — Monster damage → pain/death/reset seam + global counters (common/monsters/sv_monsters.qc)
TITLE: Per-monster mr_death/mr_pain server-side effects dropped (wyvern death-toss velocity, zombie death armor restore, per-monster pain_finished timing)
FILE: Rebirth/src/Rebirth.Common/Gameplay/Monsters/MonsterAI.cs (MarkPain:1285 PainFinished, MarkDead:1325-1376) + the 5 monster descriptors (no Pain/Death hook) :: MarkPain hardcodes st.PainFinished = Now + 0.34f for ALL monsters; MarkDead never invokes a per-monster mr_death equivalent (no Monster.Pain/Death virtual exists).
PROBLEM: Recon §3.5 justified omitting per-monster mr_pain/mr_death as 'only sets the death anim (client-render)'. That is inaccurate for 2 of the 5 monsters' SERVER logic: (a) Wyvern mr_death (wyvern.qc:151-153) assigns a random toss velocity `(±200, ±200, +100..200)` — a flyer (FL_FLY), so MarkDead's `if (!Fly&&!Swim) Velocity=Zero` SKIPS it and the wyvern corpse just keeps its last velocity instead of the death toss. (b) Zombie mr_death (zombie.qc:120) does `SetResourceExplicit(RES_ARMOR, g_monsters_armor_blockpercent)` (0.6) — a server resource write, not anim. Also pain_finished: zombie 0.34 (matches), but golem/wyvern use 0.5 and spider uses animstate_endtime — the port forces 0.34 for all, slightly mistiming the (server) anim-phase gate for non-zombies.
BASE: wyvern.qc:147-155 (mr_death velocity toss), zombie.qc:117-124 (mr_death RES_ARMOR=blockpercent), golem.qc:240 (pain_finished=time+0.5), wyvern.qc:142 (0.5), spider.qc:196 (animstate_endtime). Monster_Dead:1080 `mon.mr_death(mon, this)` and Monster_Damage:1096 `take = mon.mr_pain(...)` invoke these per-type.
FIX: Add an optional per-descriptor death/pain hook (e.g. Monster.OnDeath(Entity)/OnPain) and call it from MarkDead/MarkPain; or special-case the wyvern toss + zombie/non-zombie pain_finished inline. All 5 mr_pain return damage_take unchanged so no damage math is affected — only corpse velocity/armor and anim timing. Document the residual as intentional if not implemented.
############################################################################################
FINDING 17 [MINOR] conf=high  AREA: T43 — Monster damage → pain/death/reset seam + global counters (common/monsters/sv_monsters.qc)
TITLE: g_monsters_score_kill never awarded to the attacker on a monster kill
FILE: Rebirth/src/Rebirth.Common/Gameplay/Monsters/MonsterAI.cs (MarkDead, lines 1325-1376) :: MarkDead — fires Combat.Death.Call (1347) + MonstersKilled++ (1343) but has no GameRules_scoring_add(SCORE) equivalent.
PROBLEM: QC Monster_Dead:1049-1051 awards `GameRules_scoring_add(attacker, SCORE, +autocvar_g_monsters_score_kill)` when `IS_PLAYER(attacker) && (g_monsters_score_spawned || natural)`. The port's MarkDead never applies g_monsters_score_kill. Shipped default is 0 so there is no effect out-of-the-box, but a server that sets g_monsters_score_kill>0 (to reward monster hunting) gets no score, and g_monsters_score_spawned (lets spawned-monster kills score) is unmodeled.
BASE: sv_monsters.qc:1049-1051 + monsters.cfg defaults g_monsters_score_kill 0 / g_monsters_score_spawned 0. Recon-T43 §2 table flags this row: 'NOT applied in MarkDead ... default 0 hides it but non-default servers lose it'.
FIX: In MarkDead, when the attacker is a player and (Cvar('g_monsters_score_spawned')!=0 || natural), add Cvar('g_monsters_score_kill',0) to the attacker's SCORE via the scoring API (Scoring.GameScores.AddToPlayer / GameRules_scoring_add equivalent). Guarded by the same natural/spawned gate as the counter.
############################################################################################
FINDING 18 [MINOR] conf=medium  AREA: T51 — remaining mutators (doublejump/hook/bugrigs/campcheck/damagetext/itemstime + P3 stubs), the 5 Overkill weapons, doublejump fix, DamageTextLayer + the PlayerDamaged fire-site
TITLE: Overkill weapons consume the ammo resource pool directly and never run their clip/reload cycle
FILE: Rebirth/src/Rebirth.Common/Gameplay/Weapons/OkMachinegun.cs :: AttackAuto line 111 `actor.TakeResource(AmmoType, Cvars.Ammo)` (same pattern in OkShotgun.Attack:104, OkNex.Attack:148, OkHmg.AttackAuto:119, OkRpc.Attack:120); forced-reload check WrThink `st.ClipLoad < Cvars.Ammo` (OkMachinegun:85)
PROBLEM: All 5 OK weapons carry WEP_FLAG_RELOADABLE and in QC consume ammo via W_DecreaseAmmo, which for a reloadable weapon decrements the CLIP (clip_load) — the forced-reload branch `clip_load < ammo` then drives the reload cycle. The port instead calls actor.TakeResource(AmmoType, ammo) directly (drains the resource pool, not the clip), so st.ClipLoad (seeded full on raise in WeaponFireDriver.BeginRaise) is never decremented and the forced-reload `st.ClipLoad < Cvars.Ammo` never trips — the OK weapons never reload. In stock Overkill (UNLIMITED_AMMO) this is mostly invisible (ammo never runs dry, clip auto-refills), but it diverges from QC's per-magazine reload pause/animation and would mis-meter ammo for any non-unlimited config.
BASE: okmachinegun.qc:21 `W_DecreaseAmmo(WEP_OVERKILL_MACHINEGUN, actor, ammo, weaponentity)` + okmachinegun.qc:91-95 forced reload on `clip_load < ammo`; the port's Weapon.DecreaseAmmo (WeaponFireGate.cs:324-343) is the correct clip-aware helper but the OK weapons bypass it with TakeResource.
FIX: Replace the direct `actor.TakeResource(AmmoType, Cvars.Ammo)` calls in the 5 OK weapons with `DecreaseAmmo(actor, slot, Cvars.Ammo)` (the clip-aware W_DecreaseAmmo port), so reloadable OK weapons drain the clip and the forced-reload branch engages, matching QC.
############################################################################################
FINDING 19 [MINOR] conf=high  AREA: T56 — Generic + client + reply commands (+ engine defer / RPN VM)
TITLE: RPN crc16 uses wrong initial value (0x0000 instead of DP's 0xFFFF) → every crc16 result is wrong
FILE: C:/Users/Bryan/Projects/Xonotic/Rebirth/src/Rebirth.Engine/Console/Rpn.cs :: Crc16(string), lines 492-502 (init at 494)
PROBLEM: DP's crc16 builtin (VM_crc16 → CRC_Block, com_crc16.c) is a non-reflected CRC-16/CCITT with CRC_INIT_VALUE 0xFFFF and CRC_XOR_VALUE 0x0000. The port initializes `ushort crc = 0x0000;` (XMODEM-style), so it computes a different checksum. Verified numerically: crc16('test') port=39686 but DP=8134; crc16('')=0 vs DP=65535; 'abc'=40406 vs 20810; 'hello'=50018 vs 53870. The bit-by-bit poly-0x1021 loop is otherwise correct (equivalent to DP's table for the same init); only the init seed is wrong. The class comment even asserts 'init 0x0000' — a documented-but-incorrect assumption.
BASE: Base/darkplaces/com_crc16.c:32-37 ("non-reflected CRC using the polynomial 0x1021", #define CRC_INIT_VALUE 0xffff, #define CRC_XOR_VALUE 0x0000) + CRC_Block (line 75-81: crc=CRC_INIT_VALUE; crc=(crc<<8)^crctable[(crc>>8)^*data]; return crc^CRC_XOR_VALUE). rpn.qc:268-269 calls crc16(false, rpn_get()) (the case-sensitive CRC_Block path).
FIX: Change the seed to 0xFFFF: `ushort crc = 0xFFFF;` (final XOR is 0x0000 so no other change needed). Then crc16('test') returns 8134 matching DP.
############################################################################################
FINDING 20 [MINOR] conf=high  AREA: T56 — Generic + client + reply commands (+ engine defer / RPN VM)
TITLE: RPN bound uses Min(Max()) instead of DP's bound macro → wrong result for reversed (min>max) bounds; should reuse QMath.Bound
FILE: C:/Users/Bryan/Projects/Xonotic/Rebirth/src/Rebirth.Engine/Console/Rpn.cs :: BoundQc(min,num,max) line 426 (called from the 'bound' op, lines 225-235)
PROBLEM: The port implements bound as `MathF.Min(MathF.Max(min, num), max)`. DP's bound (mathlib.h:34, used by VM_bound prvm_cmds.c:1832) is `(num >= min) ? ((num < max) ? num : max) : min`. These agree only when min<=max; for reversed bounds they diverge: bound(10,5,0) → port returns 0 but DP returns 10; bound(5,3,1) → port=1, DP=5 (verified end-to-end through Rpn.Run). The divergence occurs when min>max and num<min: DP returns min, port returns max. The port already ships a CORRECT helper QMath.Bound(lo,v,hi)=Clamp(v,lo,hi) (QMath.cs:170-173, the `v<lo?lo:(v>hi?hi:v)` form) which exactly matches DP's macro and is reachable from Rebirth.Engine — Rpn reimplemented it incorrectly instead.
BASE: Base/darkplaces/prvm_cmds.c:1829-1833 VM_bound → bound(PARM0,PARM1,PARM2); Base/darkplaces/mathlib.h:34 #define bound(min,num,max) ((num) >= (min) ? ((num) < (max) ? (num) : (max)) : (min)). rpn.qc:235-239 bound: f=popf;f2=popf;f3=getf; rpn_setf(bound(f3,f2,f)).
FIX: Replace BoundQc's body with `return Rebirth.Common.Math.QMath.Bound(min, num, max);` (already correct + matches DP on reversed bounds), or inline DP's macro `num >= min ? (num < max ? num : max) : min`.
############################################################################################
FINDING 21 [MINOR] conf=high  AREA: T56 — Generic + client + reply commands (+ engine defer / RPN VM)
TITLE: editmob spawn missing the in-vehicle gate (and AllowMobSpawning hook) — lets a seated player spawn monsters
FILE: C:/Users/Bryan/Projects/Xonotic/Rebirth/src/Rebirth.Server/Commands.cs :: CmdEditMob, case "spawn", gate block lines 1505-1518
PROBLEM: QC CommonCommand_editmob spawn runs these gates in order: g_monsters, max<=0||perplayer<=0, IS_PLAYER, MUTATOR_CALLHOOK(AllowMobSpawning), caller.vehicle ('You can't spawn monsters while driving a vehicle'), IS_DEAD, count>=max, count>=perplayer. The port omits the `caller.vehicle` check entirely (Player.Vehicle exists and is used at Commands.cs:977), so a player seated in a vehicle can spawn a monster where QC rejects it. It also omits the AllowMobSpawning mutator hook (which has no port analog — no-op in stock play, so lower impact).
BASE: server/command/common.qc:368-372 — if (MUTATOR_CALLHOOK(AllowMobSpawning, caller)) {...} / if (caller.vehicle) { print_to(caller, "You can't spawn monsters while driving a vehicle"); return; } — placed between the IS_PLAYER check and the IS_DEAD check.
FIX: Add, right after the FragsSpectator/IsObserver check (~line 1510): `if (caller.Vehicle is not null) { outCtx.Print("You can't spawn monsters while driving a vehicle"); return true; }`. (The AllowMobSpawning hook can be skipped until a monster-spawn mutator exists, but note the omission.)
############################################################################################
FINDING 22 [MINOR] conf=medium  AREA: T56 — Generic + client + reply commands (+ engine defer / RPN VM)
TITLE: RPN 'time' op (and put/get/db*/digest/localtime/eval) silently read a same-named cvar; breaks demoseeking.cfg which depends on 'time'
FILE: C:/Users/Bryan/Projects/Xonotic/Rebirth/src/Rebirth.Engine/Console/Rpn.cs :: Step(), the deferred-ops case lines 314-324
PROBLEM: These ops route to the fallback `st.Push(st.Cvars.GetString(cmd))` (developer>0 warns). For most of the DB family this is the recon-sanctioned fallback (R3). But 'time' specifically is used by a shipped cfg: demoseeking.cfg (lines 123/126/145/152) does e.g. `rpn /_seekdemo_target time "$1" add def`, expecting the VM time pushed. The port pushes cvar_string("time")="" → the expression computes 0+$1 instead of now+$1, so client demo seeking computes wrong target times (a functional break of that feature, not a crash). DP's `time` pushes the VM clock (rpn.qc:547-548 rpn_pushf(time)).
BASE: rpn.qc:547-548 (} else if(rpncmd == "time") { rpn_pushf(time); }); demoseeking.cfg:123 `cl_cmd rpn /_seekdemo_target time "$1" add def`, :152 `rpn _seekdemo_target time sub ... def`.
FIX: Implement 'time' to push the VM/sim clock (e.g. Format9g of Api.Clock.Time when Api.Services is available, else 0) rather than the cvar fallback. The rest of the DB/digest/eval family may remain documented omissions, but 'time' has a concrete stock-cfg consumer (client demo seeking).
############################################################################################
FINDING 23 [NIT] conf=low  AREA: T28 — Menu skin/theme loader (skinvalues.txt → SKIN* table) + Localization (gettext .po engine, CTX, languages.txt, Tr seam, menu_restart wiring)
TITLE: Octal \NNN decode emits one UTF-16 char per byte, mangling a multi-byte UTF-8 octal sequence
FILE: src/Rebirth.Common/Localization/PoCatalog.cs :: ParseString() lines 217-236 (sb.Append((char)(val & 0xFF)))
PROBLEM: The engine works on UTF-8 BYTES, so `\303\244` becomes the two bytes 0xC3 0xA4 = the UTF-8 encoding of 'ä'. The port appends two separate UTF-16 chars U+00C3 U+00A4, i.e. the .NET string "Ã¤" (mojibake) rather than 'ä'. Divergence is purely theoretical: real Xonotic .po files declare charset=UTF-8 / 8bit and store non-ASCII as RAW UTF-8 (verified in common.de.po: Content-Transfer-Encoding: 8bit, German text is raw UTF-8), NOT octal-escaped — octal is used only for single-byte control chars, which round-trip correctly (and that's all the octal test covers). VFS.ReadText decodes raw UTF-8 correctly, so this path is never exercised by shipped data.
BASE: darkplaces/prvm_edict.c:1767-1789 octal case builds a single byte via (*out<<3)|(*in-'0') and advances one out byte — the engine's whole string model is UTF-8 bytes; multi-byte chars arrive as consecutive bytes.
FIX: None needed for shipped files. If strict byte-fidelity is ever wanted, accumulate raw bytes through ParseString and UTF-8-decode the whole msgid/msgstr at the end instead of char-casting per byte. Lowest priority.
############################################################################################
FINDING 24 [NIT] conf=medium  AREA: T36 — Untracked-mode map-objective spawnfuncs (Assault / Nexball / Invasion / CTS, + KeyHunt note)
TITLE: invasion_wave with empty .spawnmob is dropped, diverging from QC's override-to-random fallback
FILE: src/Rebirth.Common/Gameplay/GameTypes/Invasion.cs :: AddWave(int waveNumber, string spawnmob) lines 99-104 (early return on empty spawnmob)
PROBLEM: AddWave does `if (string.IsNullOrWhiteSpace(spawnmob)) return;`, so a wave entity carrying a .cnt but no .spawnmob is NOT recorded. QC's spawnfunc(invasion_wave) IL_PUSHes EVERY wave entity unconditionally; invasion_GetWaveEntity(wavenum) then finds it by it.cnt==wavenum and uses its (empty) spawnmob -> random pick. So a map that deliberately places an empty-spawnmob wave at round N to reset that round to random monsters diverges: QC round N = random, but the port (having dropped wave N) falls back via GetWaveMonsters to the last lower-numbered wave's monster LIST, spawning the wrong set.
BASE: sv_invasion.qc:58-63 spawnfunc(invasion_wave){ ... IL_PUSH(g_invasion_waves, this); } is unconditional; :156-172 invasion_GetWaveEntity matches it.cnt==wavenum first, only then falls back to the last it.cnt<=wavenum.
FIX: Record the wave even with an empty list (store empty string[] keyed by waveNumber) so GetWaveMonsters returns the exact (empty=>random) match for that round, then have PickMonster treat an empty exact-match list as random rather than falling through to a lower wave.
############################################################################################
FINDING 25 [NIT] conf=high  AREA: T37 — Vehicle runtime seam (board / input / impulse / return)
TITLE: VehicleEnter/VehicleExit hooks for a gunner pass the BODY as slot1 where QC passes the gun SLOT (and Exit fires after the link is already dropped)
FILE: src/Rebirth.Common/Gameplay/Vehicles/VehicleBoarding.cs :: Enter gunner branch (lines 191-192) and Exit gunner branch (lines 259-260)
PROBLEM: For the multi-seat gunner path the port fires MutatorHooks.VehicleEnter/VehicleExit with `vehicle`/`body` (the bumblebee body) as slot1, whereas QC fires MUTATOR_CALLHOOK(VehicleEnter/VehicleExit, player, gunner) with the gun SLOT entity. Additionally the port's gunner VehicleExit fires from VehicleBoarding.Exit AFTER GunnerExit already set player.Vehicle=null, so a handler reading the player's seat would see null; QC sets player.vehicle=NULL after the hook. No stock mutator subscribes to these chains in this batch, so impact is currently nil.
BASE: bumblebee.qc bumblebee_gunner_enter: `MUTATOR_CALLHOOK(VehicleEnter, player, gunner)`; bumblebee_gunner_exit: `MUTATOR_CALLHOOK(VehicleExit, player, gunner)` then `player.vehicle = NULL;` (gunner = the gun slot, VHF_PLAYERSLOT).
FIX: Pass the gun slot (the player's actual seat) as slot1 of the gunner-path VehicleEnter/VehicleExit args, and fire the gunner VehicleExit before the slot link is cleared, to match QC's slot1=gunner contract for any future mutator that inspects the seat.
