unit: mutator-dodging
category: mutator
base_ref_rev: "v0.8.6-1779-g863cd3e84"
last_audited: "2026-06-22"
summary:
  feature_count: 10
  worst_gaps:
    - "STILL the headline gap after Wave-2: dodging never triggers on the live path because the input layer still never writes Entity.MovementForward/MovementRight (verified post-change: zero `MovementForward =`/`MovementRight =` assignments anywhere in src/**; only reads in Dodging/Multijump/Bugrigs). Live input still flows only through IMovementInput.MoveValues into PlayerPhysics.Move and is never copied onto the Entity fields, and DodgeButtonHeld is still hardcoded false. So the double-tap detector's four direction branches are always false and no dodge is ever detected — even though the OnPlayerPhysics hook is now genuinely dispatched (PlayerPhysics.cs:196 MutatorHooks.PlayerPhysics.Call)."
    - "MakePlayerObserver reset is still unreachable: MutatorHooks still has NO MakePlayerObserver hook member (verified: only comment references across the codebase). A player going to spectate mid-dodge keeps stale dodging_* fields."
    - "Per-client REPLICATE still missing: sv_dodging_clientselect logic is now implemented but cl_dodging / cl_dodging_timeout are still read from the SERVER-side cvar (no client→server replication). The ClientDodgingProvider seam exists but defaults to the server cvar; no real per-client opt-in is wired."
    - "Frozen-dodge logic is now ported in DodgingMutator, but PlayerPhysics.cs:169 still zeroes a frozen player's wishmove ('dodging-frozen sub-case omitted'), and (like everything else) the trigger is dead, so frozen dodging cannot actually fire on the live path."
features:
  - id: mutator-dodging.core.enable
    name: "Mutator registration + g_dodging enable gate"
    layer: [authority]
    base_refs:
      - "common/mutators/mutator/dodging/sv_dodging.qc:67 REGISTER_MUTATOR(dodging, cvar(g_dodging))"
    port_refs:
      - "src/XonoticGodot.Common/Gameplay/Mutators/DodgingMutator.cs:89 IsEnabled"
      - "src/XonoticGodot.Common/Gameplay/Mutators/MutatorActivation.cs:Apply"
      - "src/XonoticGodot.Server/GameWorld.cs (MutatorActivation.Apply on the live game-setup path)"
    constants:
      - { name: "g_dodging", base: "0", port: "0", match: true }
    status:
      logic: faithful
      values: faithful
      timing: na
      presentation: na
      audio: na
      liveness: live
    intended_divergence: false
    divergence_rationale: null
    gaps: []
    verification:
      - { kind: code, ref: "MutatorActivation.Apply() called on the live game-setup path; IsEnabled reads g_dodging cvar (verified live). Overkill ruleset sets g_dodging 1. Unchanged by Wave-2.", result: pass }
    confidence: high
    notes: "Registration/enable/activation remains the one fully-faithful, live piece. Wave-2 did NOT change this row. The mechanic it gates is still dead (see input.double_tap)."

  - id: mutator-dodging.input.double_tap
    name: "Double-tap detection (PM_dodging_checkpressedkeys)"
    layer: [authority, shared]
    base_refs:
      - "common/mutators/mutator/dodging/sv_dodging.qc:151 PM_dodging_checkpressedkeys"
      - "common/mutators/mutator/dodging/sv_dodging.qc:278 PM_dodging_GetPressedKeys"
    port_refs:
      - "src/XonoticGodot.Common/Gameplay/Mutators/DodgingMutator.cs:148 CheckPressedKeys"
      - "src/XonoticGodot.Common/Physics/PlayerPhysics.cs:196 (MutatorHooks.PlayerPhysics.Call — now live)"
    constants:
      - { name: "cl_dodging_timeout", base: "0.2", port: "0.2 (cl_dodging_timeout cvar; server-side, NOT per-client)", match: true }
    status:
      logic: partial
      values: faithful
      timing: unknown
      presentation: na
      audio: na
      liveness: dead
    intended_divergence: false
    divergence_rationale: null
    gaps:
      - "ROOT CAUSE UNCHANGED by Wave-2: reads player.MovementForward / player.MovementRight, but those Entity fields are STILL never written anywhere in src/** (verified post-change: zero `MovementForward =` / `MovementRight =` assignments; only reads in Dodging/Multijump/Bugrigs). Live input still flows through IMovementInput.MoveValues → PlayerPhysics.Move and is never copied onto the Entity fields. They stay 0, so all four direction branches are false and no dodge is ever detected."
      - "Entity.PressedKeys is still only ever written by this mutator itself (DodgingMutator.cs:221), never seeded by the input layer, so the previous-frame state-change test is fed stale self-authored bits."
      - "+dodge button path still unwired: DodgeButtonHeld() is still hardcoded false (DodgingMutator.cs:226), so even a +dodge bind cannot trigger via this path."
    verification:
      - { kind: code, ref: "repo-wide grep post-change: no writes to Entity.MovementForward/MovementRight in src/**; PressedKeys written only at DodgingMutator.cs:221; DodgeButtonHeld hardcoded false at :226. The PlayerPhysics hook IS now dispatched (PlayerPhysics.cs:196) but is fed all-zero movement.", result: fail }
    confidence: high
    notes: "FIXES vs baseline: (1) the frozen_no_doubletap single-tap branch is now present in all four direction tests (matches Base X-macro `|| frozen_no_doubletap`); (2) the spurious crouch-exclusion on the +dodge button was REMOVED — dodgeButton is now checked unconditionally, matching Base PHYS_INPUT_BUTTON_DODGE. So the detection SHAPE is now fully faithful. STILL DEAD: its input source fields are never populated. Multijump and bugrigs read the same dead MovementForward/Right fields — one input-bridge fix would revive all three."

  - id: mutator-dodging.input.delay_state_gate
    name: "Delay cooldown + ground/wall/air state gating"
    layer: [authority]
    base_refs:
      - "common/mutators/mutator/dodging/sv_dodging.qc:186 (delay gate) + :192 (state gates)"
    port_refs:
      - "src/XonoticGodot.Common/Gameplay/Mutators/DodgingMutator.cs:200 (delay) :204-206 (gates)"
      - "src/XonoticGodot.Common/Gameplay/Mutators/DodgingMutator.cs:264 IsCloseToGround"
      - "src/XonoticGodot.Common/Gameplay/Mutators/DodgingMutator.cs:271 IsCloseToWall"
      - "src/XonoticGodot.Common/Gameplay/Mutators/DodgingMutator.cs:278 TraceHitsNear"
    constants:
      - { name: "sv_dodging_delay", base: "0.6", port: "0.6 (cvar; hardcoded fallback NOW 0.6)", match: true }
      - { name: "sv_dodging_height_threshold", base: "10", port: "10", match: true }
      - { name: "sv_dodging_wall_distance_threshold", base: "10", port: "10", match: true }
      - { name: "sv_dodging_maxspeed", base: "450", port: "450", match: true }
      - { name: "sv_dodging_air_maxspeed", base: "450", port: "450", match: true }
      - { name: "sv_dodging_wall_dodging", base: "0", port: "0", match: true }
      - { name: "sv_dodging_air_dodging", base: "0", port: "0", match: true }
    status:
      logic: faithful
      values: faithful
      timing: unknown
      presentation: na
      audio: na
      liveness: dead
    intended_divergence: false
    divergence_rationale: null
    gaps:
      - "The `if (cvar != 0) field = cvar` guards still mean explicitly setting one of the *_threshold/delay cvars to 0 is ignored and the hardcoded fallback used instead. NOTE: MaxSpeed/AirMaxSpeed are still read WITHOUT the !=0 guard (DodgingMutator.cs:117-118), so a 0 there is honoured correctly (= no cap, matching Base)."
      - "Unreachable: gated behind the dead double_tap detector, so the delay/state gates never actually run on the live path."
    verification:
      - { kind: code, ref: "delay gate checked after key scan (matches Base ordering :186); three OR'd state gates (ground/wall/air) mirror :192-195; traces honour Q3SURFACEFLAG_SKY exclusion; is_close_to_ground = OnGround||down-trace; gate vectors built from body-yaw (Angles) not v_angle, matching Base :190.", result: pass }
    confidence: high
    notes: "FIX vs baseline: the hardcoded C# fallback default for sv_dodging_delay is now 0.6 (was 0.5), matching Base — values upgraded partial→faithful. Gate ordering, the three OR'd state gates, and the trace logic still mirror Base. Only residual hazard is the if-nonzero fallback guard on the *_threshold/delay cvars (harmless with the shipped cfg)."

  - id: mutator-dodging.force.scaling
    name: "Speed-to-force scaling (determine_force / map_bound_ranges)"
    layer: [authority]
    base_refs:
      - "common/mutators/mutator/dodging/sv_dodging.qc:142 determine_force"
      - "lib/math.qh:map_bound_ranges"
    port_refs:
      - "src/XonoticGodot.Common/Gameplay/Mutators/DodgingMutator.cs:241 DetermineForce"
      - "src/XonoticGodot.Common/Gameplay/Mutators/DodgingMutator.cs:251 MapBoundRanges"
    constants:
      - { name: "sv_dodging_horiz_speed_min", base: "200", port: "200 (cvar; hardcoded fallback NOW 200)", match: true }
      - { name: "sv_dodging_horiz_speed_max", base: "1000", port: "1000", match: true }
      - { name: "sv_dodging_horiz_force_slowest", base: "400", port: "400 (cvar; hardcoded fallback NOW 400)", match: true }
      - { name: "sv_dodging_horiz_force_fastest", base: "400", port: "400", match: true }
      - { name: "sv_dodging_horiz_force_frozen", base: "200", port: "200", match: true }
    status:
      logic: faithful
      values: faithful
      timing: na
      presentation: na
      audio: na
      liveness: dead
    intended_divergence: false
    divergence_rationale: null
    gaps:
      - "Unreachable on the live path (gated behind the dead detector)."
    verification:
      - { kind: code, ref: "DetermineForce now returns HorizForceFrozen (200) when PhysFrozen, else MapBoundRanges over 2D horizontal speed — matches Base determine_force exactly (PHYS_FROZEN early return + vlen(vec2(velocity))). MapBoundRanges clamps then lerps with the fromMax<=fromMin guard (matches map_bound_ranges).", result: pass }
    confidence: high
    notes: "FIXES vs baseline: (1) the PHYS_FROZEN → horiz_force_frozen (200) branch is now present in DetermineForce — logic upgraded partial→faithful; (2) the swapped hardcoded fallbacks for speed_min (now 200) and force_slowest (now 400) are corrected — values upgraded partial→faithful. With stock cvars force_slowest==force_fastest==400, so the mapped force is a flat 400 at all speeds. Still dead (gated behind the dead detector)."

  - id: mutator-dodging.move.ramp_impulse
    name: "Per-frame ramp + one-shot up-impulse (PM_dodging)"
    layer: [authority, shared]
    base_refs:
      - "common/mutators/mutator/dodging/sv_dodging.qc:217 PM_dodging"
    port_refs:
      - "src/XonoticGodot.Common/Gameplay/Mutators/DodgingMutator.cs:322 PMDodging"
      - "src/XonoticGodot.Common/Gameplay/Mutators/DodgingMutator.cs:306 BeginDodge"
      - "src/XonoticGodot.Common/Physics/PlayerPhysics.cs:196"
    constants:
      - { name: "sv_dodging_ramp_time", base: "0.1", port: "0.1", match: true }
      - { name: "sv_dodging_up_speed", base: "200", port: "200", match: true }
    status:
      logic: faithful
      values: faithful
      timing: unknown
      presentation: na
      audio: na
      liveness: dead
    intended_divergence: false
    divergence_rationale: null
    gaps:
      - "Never receives DodgingAction != 0 (the detector that would set it is dead), so the ramp/up-impulse early-returns every frame: no hop, no horizontal lunge ever applied on the live path."
    verification:
      - { kind: code, ref: "ramp = min(frametime/ramp_time * force_total, remaining); UNSET_ONGROUND + up_speed*up one-shot; v_angle (ViewAnglesOf) when AirDodging else body Angles; completion reset when remaining<=0 — all match Base. Abort branch now `WaterLevel>=2 || dead || (ClientSelect && !ClientDodgingEnabled && !frozenDodging)` — exactly Base's `waterlevel>=SWIMMING || IS_DEAD || (CLIENTSELECT && !ENABLED && !frozen_dodging)`.", result: pass }
    confidence: high
    notes: "FIX vs baseline: the swimming/dead abort branch now includes BOTH the clientselect term AND the frozen_dodging carve-out, matching Base (sv_dodging.qc:223-224) — logic upgraded partial→faithful. Ramp math, the min cap, the one-shot up flag, the air-dodge v_angle choice, and the completion reset remain faithful. Correct but still unreachable because the trigger is dead."

  - id: mutator-dodging.audio.jump_sound
    name: "Dodge jump sound (sv_dodging_sound)"
    layer: [authority, presentation]
    base_refs:
      - "common/mutators/mutator/dodging/sv_dodging.qc:259 (PlayerSound playersound_jump)"
    port_refs:
      - "src/XonoticGodot.Common/Gameplay/Mutators/DodgingMutator.cs:356"
    constants:
      - { name: "sv_dodging_sound", base: "1", port: "1 (cvar)", match: true }
    status:
      logic: faithful
      values: faithful
      timing: na
      presentation: na
      audio: faithful
      liveness: dead
    intended_divergence: false
    divergence_rationale: null
    gaps:
      - "Never plays because the dodge never triggers (dead path)."
    verification:
      - { kind: code, ref: "Api.Sound.Play(player, SoundChannel.Body, player/jump.wav) when sv_dodging_sound != 0 — same call as the regular jump. Unchanged by Wave-2.", result: pass }
    confidence: medium
    notes: "Faithful in intent and API (matches the port's jump-sound convention). CH_PLAYER → SoundChannel.Body is the established port mapping. Unreachable on the live path. No change from baseline."

  - id: mutator-dodging.anim.jump_action
    name: "Dodge plays the jump animation (ANIMACTION_JUMP)"
    layer: [authority, presentation]
    base_refs:
      - "common/mutators/mutator/dodging/sv_dodging.qc:262 (animdecide_setaction ANIMACTION_JUMP)"
    port_refs: []
    constants: []
    status:
      logic: missing
      values: na
      timing: na
      presentation: missing
      audio: na
      liveness: na
    intended_divergence: false
    divergence_rationale: null
    gaps:
      - "Base drives ANIMACTION_JUMP on the dodge hop (unconditionally, OUTSIDE the sv_dodging_sound guard); the port still plays only the sound, no animation action, so a dodge would show no jump pose. STILL MISSING after Wave-2 — the change only added a clarifying comment (DodgingMutator.cs:358-360) documenting that no animdecide/anim-action seam exists yet."
    verification:
      - { kind: code, ref: "PMDodging plays the sound but does not call any animdecide/anim-action equivalent; the new comment explicitly states the animdecide seam is absent (DodgingMutator.cs:358-360).", result: fail }
    confidence: high
    notes: "No change from baseline. Base calls animdecide_setaction OUTSIDE the sv_dodging_sound guard (always), whereas the port only does anything inside that guard. Doubly moot while the dodge is dead, but the animation drive is absent independent of liveness."

  - id: mutator-dodging.lifecycle.reset
    name: "Dodge state reset on spawn + observer (dodging_ResetPlayer)"
    layer: [authority]
    base_refs:
      - "common/mutators/mutator/dodging/sv_dodging.qc:309 dodging_ResetPlayer"
      - "common/mutators/mutator/dodging/sv_dodging.qc:322 PlayerSpawn"
      - "common/mutators/mutator/dodging/sv_dodging.qc:328 MakePlayerObserver"
    port_refs:
      - "src/XonoticGodot.Common/Gameplay/Mutators/DodgingMutator.cs:372 ResetPlayer"
      - "src/XonoticGodot.Common/Gameplay/Mutators/DodgingMutator.cs:286 OnPlayerSpawn"
    constants: []
    status:
      logic: partial
      values: faithful
      timing: na
      presentation: na
      audio: na
      liveness: partial
    intended_divergence: false
    divergence_rationale: null
    gaps:
      - "STILL only PlayerSpawn is hooked; the MakePlayerObserver reset is still unreachable because MutatorHooks STILL has NO MakePlayerObserver hook member at all (verified post-change: no MakePlayerObserver hook in MutatorHooks.cs; only comment references across the codebase). A player going to spectate mid-dodge keeps stale dodging_* fields. Wave-2 did not add this hook."
    verification:
      - { kind: code, ref: "Hook() subscribes only MutatorHooks.PlayerSpawn (live via ClientManager.cs:570 PlayerSpawn.Call); ResetPlayer zeroes the same six fields as Base. No MakePlayerObserver hook exists to subscribe to.", result: fail }
    confidence: high
    notes: "No change from baseline. ResetPlayer is faithful and the PlayerSpawn reset is live; liveness is 'partial' (one of two Base reset triggers is wired). Low practical impact while the dodge is dead, but a real omission — missing-hook-infrastructure gap."

  - id: mutator-dodging.clientselect.opt_in
    name: "Client opt-in (sv_dodging_clientselect + cl_dodging + REPLICATE)"
    layer: [authority, presentation]
    base_refs:
      - "common/mutators/mutator/dodging/sv_dodging.qc:224 (clientselect abort branch)"
      - "common/mutators/mutator/dodging/sv_dodging.qc:57-58 REPLICATE(cvar_cl_dodging / cvar_cl_dodging_timeout)"
      - "common/mutators/mutator/dodging/cl_dodging.qc:3-4 REPLICATE"
    port_refs:
      - "src/XonoticGodot.Common/Gameplay/Mutators/DodgingMutator.cs:76 ClientSelect"
      - "src/XonoticGodot.Common/Gameplay/Mutators/DodgingMutator.cs:84 ClientDodgingProvider"
      - "src/XonoticGodot.Common/Gameplay/Mutators/DodgingMutator.cs:233 ClientDodgingEnabled"
      - "src/XonoticGodot.Common/Gameplay/Mutators/DodgingMutator.cs:328-330 (PMDodging abort branch)"
    constants:
      - { name: "sv_dodging_clientselect", base: "0", port: "0 (cvar now read)", match: true }
      - { name: "cl_dodging", base: "0", port: "0 (read SERVER-side; not per-client replicated)", match: true }
    status:
      logic: faithful
      values: faithful
      timing: na
      presentation: partial
      audio: na
      liveness: dead
    intended_divergence: false
    divergence_rationale: null
    gaps:
      - "REPLICATE still missing: cl_dodging / cl_dodging_timeout are NOT replicated client→server. ClientDodgingEnabled() defaults to reading the SERVER-side cl_dodging cvar (a ClientDodgingProvider seam exists for a future replication layer but is unset). So on a real multi-client clientselect server every client shares the one server cvar value rather than each opting in individually."
      - "The clientselect gate lives only in PMDodging, which is gated behind the dead detector, so it never actually runs on the live path."
    verification:
      - { kind: code, ref: "ClientSelect read from sv_dodging_clientselect (DodgingMutator.cs:122); abort branch consults `ClientSelect && !ClientDodgingEnabled(player) && !frozenDodging` (matches Base); ClientDodgingEnabled uses ClientDodgingProvider ?? server cvar. No REPLICATE / per-client carrier wired.", result: partial }
    confidence: high
    notes: "FIX vs baseline: the clientselect gate is now IMPLEMENTED (cvar read + ClientDodgingEnabled + the PMDodging abort term) — logic upgraded missing→faithful, values missing→faithful. Residual: the per-client REPLICATE of cl_dodging/cl_dodging_timeout is still absent (provider seam defaults to the server cvar), so presentation is 'partial', and the whole thing is unreachable while the detector is dead."

  - id: mutator-dodging.frozen.dodge
    name: "Frozen dodging (sv_dodging_frozen / _frozen_doubletap / horiz_force_frozen)"
    layer: [authority]
    base_refs:
      - "common/mutators/mutator/dodging/sv_dodging.qc:153-154 (frozen_dodging / frozen_no_doubletap)"
      - "common/mutators/mutator/dodging/sv_dodging.qc:143 (determine_force PHYS_FROZEN branch)"
      - "common/mutators/mutator/dodging/sv_dodging.qc:223-224 (PM_dodging frozen_dodging carve-out)"
    port_refs:
      - "src/XonoticGodot.Common/Gameplay/Mutators/DodgingMutator.cs:155-156 (frozenDodging / frozenNoDoubletap)"
      - "src/XonoticGodot.Common/Gameplay/Mutators/DodgingMutator.cs:243 (DetermineForce PhysFrozen branch)"
      - "src/XonoticGodot.Common/Gameplay/Mutators/DodgingMutator.cs:328-330 (PMDodging frozen carve-out)"
    constants:
      - { name: "sv_dodging_frozen", base: "0", port: "0 (cvar now read)", match: true }
      - { name: "sv_dodging_frozen_doubletap", base: "0", port: "0 (cvar now read)", match: true }
      - { name: "sv_dodging_horiz_force_frozen", base: "200", port: "200", match: true }
    status:
      logic: faithful
      values: faithful
      timing: na
      presentation: na
      audio: na
      liveness: dead
    intended_divergence: false
    divergence_rationale: null
    gaps:
      - "Still unreachable on the live path: gated behind the dead double_tap detector (MovementForward/Right never written). Even with the frozen single-tap branch present, mvF/mvR are always 0 so the detector loop body never runs."
      - "PlayerPhysics.cs:169 STILL zeroes a frozen player's wishmove (`move = Vector3.Zero`, 'dodging-frozen sub-case omitted'). This is a separate carve-out Base preserves so a frozen dodger can still be moved by the dodge; the port omits it. (Note: it zeroes the physics `move` local, not the never-written Entity.MovementForward, so it is not the reason the detector is dead — but it remains a real omission for the frozen-dodge motion path.)"
    verification:
      - { kind: code, ref: "All three Base sites now ported: (1) frozen_no_doubletap single-tap branch in all four detector directions (DodgingMutator.cs:171-194); (2) DetermineForce PhysFrozen→HorizForceFrozen (200); (3) PMDodging abort frozen carve-out. Cvars sv_dodging_frozen/_frozen_doubletap/_horiz_force_frozen all read in Hook(). Constants match Base (0/0/200). PlayerPhysics.cs:169 still comments the frozen sub-case omitted.", result: pass }
    confidence: high
    notes: "FIX vs baseline: the entire frozen-dodging mechanic is now ported across all three Base sites with all three cvars read — logic/values upgraded missing→faithful. Defaults off (sv_dodging_frozen 0) so low practical impact. Two residual gaps keep liveness dead: the detector is dead (no input bridge) and PlayerPhysics.cs:169 still zeroes a frozen player's wishmove."
