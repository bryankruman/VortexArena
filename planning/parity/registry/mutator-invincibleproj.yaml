unit: mutator-invincibleproj
category: mutator
base_ref_rev: "v0.8.6-1779-g863cd3e84"
last_audited: "2026-06-25"
summary:
  feature_count: 5
  worst_gaps:
    - "BuildMutatorsPrettyString is now PORTED (override + chain method) but DEAD: nothing calls MutatorActivation.BuildMutatorsPrettyString anywhere in the port (0 callers), so the scoreboard/server-browser ', Invincible Projectiles' display label is still never assembled."
    - "Client-side active-mutator detection (MUT_INVINCIBLE_PROJECTILES, util.qc:300 / util.qh:60) still not ported — the port has no active_mutators / mut_set_active surface at all (zero hits), so the menu/server-browser 'active mutators' list can never light up this mutator."
features:
  - id: mutator-invincibleproj.core.register
    name: "Mutator registration + enable predicate (g_invincible_projectiles)"
    layer: [authority]
    base_refs:
      - "common/mutators/mutator/invincibleproj/sv_invincibleproj.qc:4 REGISTER_MUTATOR(invincibleprojectiles, expr_evaluate(autocvar_g_invincible_projectiles))"
      - "mutators.cfg:116"
    port_refs:
      - "src/XonoticGodot.Common/Gameplay/Mutators/InvincibleProjectilesMutator.cs:13 (class) / :18 (IsEnabled)"
      - "src/XonoticGodot.Server/GameWorld.cs:563 (MutatorActivation.Apply)"
      - "src/XonoticGodot.Common/Gameplay/Mutators/MutatorActivation.cs:80 Apply / :49 Add (subscribes Hook() for enabled mutators)"
    constants:
      - { name: "g_invincible_projectiles (default)", base: "0", port: "0", match: true }
    status:
      logic: faithful
      values: faithful
      timing: na
      presentation: na
      audio: na
      liveness: live
    intended_divergence: false
    divergence_rationale: null
    gaps: []
    verification:
      - { kind: code, ref: "InvincibleProjectilesMutator [Mutator] -> Mutators.All; NetName='invincibleprojectiles'; MutatorActivation.Apply() (GameWorld.cs:563, was :535) iterates Mutators.All and Add()s every IsEnabled mutator, which calls mut.Hook() subscribing OnEditProjectile to the EditProjectile chain. Live server boot path.", result: pass }
      - { kind: value, ref: "assets/data/xonotic-data.pk3dir/mutators.cfg:116 'set g_invincible_projectiles 0 \"disable any damage to projectiles ...\"' == Base mutators.cfg:116 byte-identical incl. description", result: pass }
    confidence: high
    notes: "Registered + activated on the live server boot path; IsEnabled reads the cvar via the facade (GetFloat(\"g_invincible_projectiles\") != 0). QC's string-cvar expr_evaluate reduced to a float-nonzero test — equivalent for the default and the menu 0/1 values (a non-numeric truthy string like 'on' would differ, but the menu/cfg never sets one). Activation call site is GameWorld.cs:563 (was :535/:511); still the same live boot path."

  - id: mutator-invincibleproj.core.editprojectile_zero_health
    name: "EditProjectile: zero a fired projectile's health so it can't be shot down"
    layer: [authority]
    base_refs:
      - "common/mutators/mutator/invincibleproj/sv_invincibleproj.qc:6-15 MUTATOR_HOOKFUNCTION(invincibleprojectiles, EditProjectile)"
    port_refs:
      - "src/XonoticGodot.Common/Gameplay/Mutators/InvincibleProjectilesMutator.cs:35 OnEditProjectile"
      - "src/XonoticGodot.Common/Gameplay/Mutators/MutatorHooks.cs:389-395 EditProjectileArgs + EditProjectile chain"
      - "src/XonoticGodot.Common/Gameplay/Items/Resources.cs:42-70 GetResource/SetResourceExplicit (Health -> Entity.Health)"
      - "src/XonoticGodot.Common/Gameplay/Weapons/Projectiles.cs:80 MakeShootable / :92 ShootDown (the now-live shoot-down path)"
      - "src/XonoticGodot.Common/Gameplay/Damage/DamageSystem.cs:331-335 EventDamage -> GtEventDamage dispatch"
    constants:
      - { name: "projectile RES_HEALTH set value", base: "0", port: "0", match: true }
      - { name: "guard: only projectiles with health", base: "if(GetResource(proj,RES_HEALTH))", port: "if(proj.GetResource(Health) != 0f)", match: true }
    status:
      logic: faithful
      values: faithful
      timing: faithful
      presentation: na
      audio: na
      liveness: live
    intended_divergence: false
    divergence_rationale: null
    gaps: []
    verification:
      - { kind: code, ref: "OnEditProjectile: if (proj.GetResource(Health) != 0f) proj.SetResourceExplicit(Health, 0f) == QC if(GetResource(proj,RES_HEALTH)) SetResourceExplicit(proj,RES_HEALTH,0). GetResource(Health)/SetResourceExplicit map to Entity.Health (Resources.cs:44,60) — the same field weapons seed AND the same field ShootDown reads.", result: pass }
      - { kind: code, ref: "Projectiles.MakeShootable (Projectiles.cs:80) installs e.GtEventDamage = ShootDown. DamageSystem.EventDamage (DamageSystem.cs:331-335) routes a non-player victim with GtEventDamage!=null through that closure on the live damage path. ShootDown's first line is `if (self.Health <= 0f) return;` (Projectiles.cs:96) — so a projectile whose Health the mutator zeroed never reaches the g_projectiles_damage gate or detonation. Enabling g_invincible_projectiles produces the real observable effect (projectiles cannot be shot down).", result: pass }
      - { kind: code, ref: "Live callers of MakeShootable confirmed on weapon-fire (Devastator/Electro/Hagar/Mortar/Minelayer/Seeker/Arc projectile-spawn paths); EditProjectile.Call fires per-projectile in the same spawn paths. Ordering of MakeShootable vs EditProjectile is irrelevant: ShootDown reads self.Health lazily at hit time, after EditProjectile has zeroed it.", result: pass }
    confidence: high
    notes: "Mutator handler is faithful AND fully live. With Health=0, ShootDown's recursion guard short-circuits before the damage subtract/detonate, matching QC's W_*_Damage first-line GetResource(this,RES_HEALTH)<=0 return."

  - id: mutator-invincibleproj.string.machine
    name: "BuildMutatorsString — append ':InvincibleProjectiles' to active-mutators token string"
    layer: [authority]
    base_refs:
      - "common/mutators/mutator/invincibleproj/sv_invincibleproj.qc:17-20 MUTATOR_HOOKFUNCTION(invincibleprojectiles, BuildMutatorsString)"
    port_refs:
      - "src/XonoticGodot.Common/Gameplay/Mutators/InvincibleProjectilesMutator.cs:45 BuildMutatorsString override"
      - "src/XonoticGodot.Common/Gameplay/Mutators/MutatorActivation.cs:108 BuildMutatorsString chain (iterates Added mutators)"
      - "src/XonoticGodot.Server/GameWorld.cs:876 MutatorActivation.BuildMutatorsString(\"\") (gamelog init; gated by sv_eventlog)"
    constants:
      - { name: "token", base: ":InvincibleProjectiles", port: ":InvincibleProjectiles", match: true }
    status:
      logic: faithful
      values: faithful
      timing: na
      presentation: na
      audio: na
      liveness: live
    intended_divergence: false
    divergence_rationale: null
    gaps: []
    verification:
      - { kind: code, ref: "InvincibleProjectilesMutator.BuildMutatorsString(s) => s + \":InvincibleProjectiles\" == QC strcat(s, \":InvincibleProjectiles\"). MutatorBase.BuildMutatorsString virtual (GameplayBases.cs:213) defaults to identity; only Added mutators contribute (MutatorActivation.cs:110-112), mirroring QC where the hook only fires for subscribed mutators.", result: pass }
      - { kind: code, ref: "LIVE caller confirmed: GameWorld.cs:876 calls MutatorActivation.BuildMutatorsString(\"\") inside the sv_eventlog branch of the server boot path (QC server/gamelog.qc:50 GameLogInit MUTATOR_CALLHOOK(BuildMutatorsString,...)). Split on ':' feeds GameLog.Init mutator tokens. sv_eventlog-gated, same as Base's GameLogInit gating.", result: pass }
    confidence: high
    notes: "FIX vs baseline (was logic/values/presentation:missing, liveness:na, port_refs:[]). The active-mutators machine-token surface is now ported AND live: per-mutator override + the MutatorActivation.BuildMutatorsString chain + a live caller at GameWorld gamelog init. presentation set na (this is the colon-delimited MACHINE token for gamelog/server-info, not a user-facing display string — that is string.pretty). Liveness is live but sv_eventlog-gated, matching QC's GameLogInit which also only runs under sv_eventlog."

  - id: mutator-invincibleproj.string.pretty
    name: "BuildMutatorsPrettyString — append ', Invincible Projectiles' to display string"
    layer: [authority, presentation]
    base_refs:
      - "common/mutators/mutator/invincibleproj/sv_invincibleproj.qc:22-25 MUTATOR_HOOKFUNCTION(invincibleprojectiles, BuildMutatorsPrettyString)"
    port_refs:
      - "src/XonoticGodot.Common/Gameplay/Mutators/InvincibleProjectilesMutator.cs:48 BuildMutatorsPrettyString override"
      - "src/XonoticGodot.Common/Gameplay/Mutators/MutatorActivation.cs:121 BuildMutatorsPrettyString chain (NO live caller)"
    constants:
      - { name: "label", base: ", Invincible Projectiles", port: ", Invincible Projectiles", match: true }
    status:
      logic: faithful
      values: faithful
      timing: na
      presentation: faithful
      audio: na
      liveness: dead
    intended_divergence: false
    divergence_rationale: null
    gaps:
      - "DEAD: the MutatorActivation.BuildMutatorsPrettyString chain method (MutatorActivation.cs:121) has ZERO callers anywhere in the port (grep MutatorActivation.BuildMutatorsPrettyString across src+game = 0 invocations). The override emits the correct ', Invincible Projectiles' token, but no consumer assembles the pretty active-mutators string, so the scoreboard/HUD/server-browser display label is still never produced. QC calls this from server/client.qc:1107 (G_GetMutators / scoreboard mutator line) — that consumer is unported."
    verification:
      - { kind: code, ref: "InvincibleProjectilesMutator.BuildMutatorsPrettyString(s) => s + \", Invincible Projectiles\" == QC strcat(s, \", Invincible Projectiles\"). Code dimension faithful.", result: pass }
      - { kind: code, ref: "grep 'MutatorActivation.BuildMutatorsPrettyString' across src+game -> only the definition at MutatorActivation.cs:121, no callers. The chain (and thus this override) is never invoked on any live path.", result: fail }
    confidence: high
    notes: "PARTIAL FIX vs baseline (was all-missing): the override + the static chain method are now PRESENT and correct (logic/values/presentation missing->faithful), but the chain has no caller so liveness is DEAD (na->dead). The create-game menu checkbox input side still exists at game/menu/dialogs/DialogMutators.cs:181; what's missing is the server-side pretty-string CONSUMER (QC server/client.qc:1107) that would call MutatorActivation.BuildMutatorsPrettyString. Contrast string.machine, which has its consumer wired (gamelog)."

  - id: mutator-invincibleproj.client.active_mut_flag
    name: "Client active-mutators detection flag (MUT_INVINCIBLE_PROJECTILES)"
    layer: [presentation]
    base_refs:
      - "common/util.qc:300 X(\"Invincible Projectiles\", _(\"Invincible Projectiles\"), MUT_INVINCIBLE_PROJECTILES, cvar(\"g_invincible_projectiles\"))"
      - "common/util.qh:60 const int MUT_INVINCIBLE_PROJECTILES = 5"
    port_refs: []
    constants:
      - { name: "MUT_INVINCIBLE_PROJECTILES", base: "5", port: "none", match: false }
    status:
      logic: missing
      values: missing
      timing: na
      presentation: missing
      audio: na
      liveness: na
    intended_divergence: false
    divergence_rationale: null
    gaps:
      - "The Base GetActiveMutators-style scan (util.qc:280-330) walks the active_mutators cvar (or, when empty, the live cvars) and calls mut_set_active(MUT_INVINCIBLE_PROJECTILES) + appends the translated name when g_invincible_projectiles is set, so the menu / server-browser 'active mutators' list and any MUT_ bitmask consumer can light up this mutator. The port still has NO active_mutators / mut_set_active / MUT_ flag surface (grep across src+game = 0 hits)."
    verification:
      - { kind: code, ref: "grep active_mutators / mut_set_active / MUT_INVINCIBLE / GetActiveMutators across src+game -> 0 hits", result: fail }
    confidence: high
    notes: "Unchanged. CLIENT-side cvar-driven detection (distinct from the two server-side BuildMutators* hooks above), part of a port-wide-missing active-mutators bitmask surface shared by every mutator. Base symbols re-confirmed at util.qc:300 / util.qh:60."
