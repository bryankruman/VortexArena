unit: mutator-sandbox
category: mutator
base_ref_rev: "v0.8.6-1779-g863cd3e84"
last_audited: "2026-06-22"
summary:
  feature_count: 14
  worst_gaps:
    - "Wave-2 added a faithful, self-contained SandboxMutator.cs (all object/edit/attach/storage/think/touch logic + every cvar default), and the mutator IS activated on the live path (MutatorActivation.Apply at GameWorld.cs:535) with its SvStartFrame think-tick pumped (MutatorHooks.FireStartFrame in the GameWorld loop) — BUT the entire command-dispatch surface is dead: HandleCommand has zero callers, `g_sandbox`/`sandbox` is not in ClientCommandRegistry, and there is no SV_ParseClientCommand mutator-hook seam."
    - "Every host seam is unwired (all null): Trace (no crosshair selection; spawns land at player origin), Store (no save/load/autoload, clipboard inert), CryptoIdfpProvider/NetNameProvider/ViewYawProvider (no UID/owner/view-yaw), SetRealClients (never called → owner-UID resync no-ops on an empty roster), OnMaterialEffect/PrintTo."
    - "Net result in a real match: with g_sandbox 1 NO object can be spawned, edited, dragged, claimed, or persisted — the logic exists but is unreachable from the player. Code went missing→faithful, but liveness stays dead for everything that needs the command surface or a host seam."
    - "Drag/grab (+button8) integration is still entirely absent (no Drag/Drag_Begin/Drag_Finish; .grab is computed by the think tick but nothing consumes it)."
    - "The three Sandbox_* mutator hooks (DragAllowed/SaveAllowed/EditAllowed) have no port hook-chain; gating is approximated on g_sandbox_readonly only (TODOs in source)."
features:
  - id: mutator-sandbox.core.registration
    name: "Mutator activation via g_sandbox + onadd autoload"
    layer: [authority]
    base_refs:
      - "common/mutators/mutator/sandbox/sv_sandbox.qc:REGISTER_MUTATOR"
      - "common/mutators/mutator/sandbox/sv_sandbox.qc:MUTATOR_ONADD"
    port_refs:
      - "src/XonoticGodot.Common/Gameplay/Mutators/SandboxMutator.cs:IsEnabled"
      - "src/XonoticGodot.Common/Gameplay/Mutators/SandboxMutator.cs:Hook"
      - "src/XonoticGodot.Server/GameWorld.cs:535 (MutatorActivation.Apply)"
    constants:
      - { name: "g_sandbox", base: "0", port: "0", match: true }
      - { name: "g_sandbox_storage_autoload", base: "1", port: "1", match: true }
      - { name: "g_sandbox_storage_autosave", base: "5", port: "5", match: true }
    status:
      logic: faithful
      values: faithful
      timing: faithful
      presentation: na
      audio: na
      liveness: live
    intended_divergence: false
    divergence_rationale: null
    gaps:
      - "MUTATOR_ONADD autoload calls DatabaseLoad(), but Store is null on the live path, so the autoload is inert (returns immediately) — no storage file is ever read in a real match."
    verification:
      - { kind: code, ref: "SandboxMutator is [Mutator]-attributed; MutatorActivation.Apply() (live at GameWorld.cs:535) reconciles IsEnabled→Hook(). IsEnabled = g_sandbox != 0. Hook() seeds autosave_time, clears object list, subscribes SvStartFrame.", result: pass }
      - { kind: code, ref: "all 15 g_sandbox* + cl_sandbox_clipboard defaults in port assets/.../mutators.cfg byte-identical to Base mutators.cfg:95-110", result: pass }
      - { kind: code, ref: "DatabaseLoad() bails when Store is null; no host wires Store (grep: zero `.Store =` callers)", result: fail }
    confidence: high
    notes: "FIXED vs baseline (missing→faithful, na→live): the mutator now genuinely registers and activates on g_sandbox 1 via MutatorActivation.Apply (live). Cvar reads are real. Residual: the onadd autoload is wired but inert because the file-IO Store seam is null."

  - id: mutator-sandbox.object.spawn
    name: "object_spawn — create object entity from a model in front of player"
    layer: [authority]
    base_refs:
      - "common/mutators/mutator/sandbox/sv_sandbox.qc:sandbox_ObjectSpawn"
      - "common/mutators/mutator/sandbox/sv_sandbox.qc:SV_ParseClientCommand(object_spawn)"
    port_refs:
      - "src/XonoticGodot.Common/Gameplay/Mutators/SandboxMutator.cs:ObjectSpawn"
      - "src/XonoticGodot.Common/Gameplay/Mutators/SandboxMutator.cs:HandleCommand(object_spawn)"
    constants:
      - { name: "g_sandbox_editor_distance_spawn", base: "200", port: "200", match: true }
      - { name: "g_sandbox_editor_flood (s)", base: "1", port: "1", match: true }
      - { name: "g_sandbox_editor_maxobjects", base: "1000", port: "1000", match: true }
      - { name: "spawn_solid", base: "SOLID_BBOX", port: "Solid.BBox", match: true }
      - { name: "spawn_movetype", base: "MOVETYPE_TOSS", port: "MoveType.Toss", match: true }
      - { name: "spawn_takedamage", base: "DAMAGE_AIM", port: "DamageMode.Aim", match: true }
      - { name: "damageforcescale", base: "1", port: "1", match: true }
    status:
      logic: faithful
      values: faithful
      timing: faithful
      presentation: missing
      audio: na
      liveness: dead
    intended_divergence: false
    divergence_rationale: null
    gaps:
      - "DEAD: HandleCommand has zero callers — `g_sandbox`/`sandbox` is not in ClientCommandRegistry and no SV_ParseClientCommand hook routes the client command, so object_spawn can never run in a match."
      - "Trace seam (crosshair WarpZone_TraceLine forward 200) is null on the live path, so even if reached, spawns land at the player origin, not the forward trace endpoint."
      - "ModelExists() is permissive (TODO: no IModelService.fexists), so the Base non-existent-model rejection is not enforced."
      - "presentation: CSQCMODEL_AUTOINIT (object→client networking) has no port seam — a spawned object would not render on clients."
    verification:
      - { kind: code, ref: "ObjectSpawn + HandleCommand(object_spawn) implement flood timer, maxobjects cap, model-arg check, forward-trace placement, view-yaw angles, owner-UID/info setup — logic/values match Base line-by-line", result: pass }
      - { kind: grep, ref: "grep HandleCommand outside SandboxMutator.cs = none; grep g_sandbox in ClientCommandRegistry = none; no SvParseClientCommand seam in MutatorHooks", result: fail }
    confidence: high
    notes: "FIXED on logic/values/timing (missing→faithful). STILL OPEN on liveness (dead): no command path reaches it. Trace + CSQC seams unwired."

  - id: mutator-sandbox.object.think_grab
    name: "Per-object think: grab-class assignment + owner UID resync + CSQC update"
    layer: [authority]
    base_refs:
      - "common/mutators/mutator/sandbox/sv_sandbox.qc:sandbox_ObjectFunction_Think"
    port_refs:
      - "src/XonoticGodot.Common/Gameplay/Mutators/SandboxMutator.cs:ObjectThink"
      - "src/XonoticGodot.Common/Gameplay/Mutators/SandboxMutator.cs:OnStartFrame"
    constants:
      - { name: "g_sandbox_editor_free", base: "1", port: "1", match: true }
      - { name: "grab_values", base: "0 none / 1 owner / 3 anyone", port: "0/1/3", match: true }
    status:
      logic: partial
      values: faithful
      timing: faithful
      presentation: missing
      audio: na
      liveness: partial
    intended_divergence: false
    divergence_rationale: null
    gaps:
      - "Think tick IS pumped live (OnStartFrame iterates _objects each frame), but _objects is always empty because nothing can spawn — so it never does meaningful work."
      - "Owner-UID resync depends on SetRealClients() + CryptoIdfpProvider, both unwired (roster is always empty, provider null) → realowner never resolves."
      - "Sandbox_DragAllowed mutator-hook veto has no port chain; grab is gated on g_sandbox_readonly only (TODO in source)."
      - "presentation: CSQCMODEL_AUTOUPDATE has no port seam."
    verification:
      - { kind: code, ref: "ObjectThink computes grab (readonly→0, free<2 && uid→1, else 3) and resyncs realowner from UID — faithful; OnStartFrame is subscribed to MutatorHooks.SvStartFrame which IS fired in GameWorld loop", result: pass }
      - { kind: grep, ref: "no `.SetRealClients(` / `.CryptoIdfpProvider =` callers anywhere; _realClients defaults to Array.Empty", result: fail }
    confidence: high
    notes: "FIXED partially (missing→partial logic, na→partial liveness): the think loop is genuinely on the live per-frame path, but operates on an empty set and its UID-resync inputs are unwired. DragAllowed hook still absent."

  - id: mutator-sandbox.object.touch_material_fx
    name: "Object touch: material impact particles + impact sounds (velocity-scaled)"
    layer: [authority, presentation]
    base_refs:
      - "common/mutators/mutator/sandbox/sv_sandbox.qc:sandbox_ObjectFunction_Touch"
    port_refs:
      - "src/XonoticGodot.Common/Gameplay/Mutators/SandboxMutator.cs:ObjectTouch"
    constants:
      - { name: "g_sandbox_object_material_velocity_min", base: "100", port: "100", match: true }
      - { name: "g_sandbox_object_material_velocity_factor", base: "0.002", port: "0.002", match: true }
      - { name: "touch_timer_rate_s", base: "0.1", port: "0.1", match: true }
      - { name: "impact_sound_variants", base: "5 (impact_<mat>_1..5.wav)", port: "ceil(random()*5)", match: true }
      - { name: "effect_count_range", base: "1..10 (ceil(intensity*10))", port: "ceil(intensity*10)", match: true }
      - { name: "sound_channel/atten", base: "CH_TRIGGER / VOL_BASE*intensity / ATTEN_NORM", port: "TriggerAuto / 1*intensity / 1", match: true }
    status:
      logic: faithful
      values: faithful
      timing: faithful
      presentation: missing
      audio: partial
      liveness: dead
    intended_divergence: false
    divergence_rationale: null
    gaps:
      - "DEAD: no touch handler is wired to the engine collision callback — ObjectTouch has no live caller (objects never spawn, and there is no settouch seam routing world touches into it)."
      - "presentation: the impact_<material> particle effect is not registered in the port EffectSystem; ObjectTouch falls back to an OnMaterialEffect delegate which is null/unwired."
      - "audio: sound playback uses the right cue (object/impact_<mat>_N.wav, channel/atten faithful) but is unreachable; precache of the 5 variants is deferred to lazy Play (intentional)."
    verification:
      - { kind: code, ref: "ObjectTouch: intensity=avg(|vel|), >=min gate, offset+factor clamp, 0.1s rate-limit, ceil(random*5) variant, count=ceil(intensity*10) — all faithful to Base", result: pass }
      - { kind: grep, ref: "no settouch/collision callback routes into ObjectTouch; OnMaterialEffect has no `=` assignment anywhere", result: fail }
    confidence: high
    notes: "FIXED on logic/values/timing/audio-cue (missing→faithful). STILL OPEN: dead (no touch wiring), particle effect unregistered."

  - id: mutator-sandbox.object.edit_get
    name: "Edit-target trace + ownership permission check"
    layer: [authority]
    base_refs:
      - "common/mutators/mutator/sandbox/sv_sandbox.qc:sandbox_ObjectEdit_Get"
    port_refs:
      - "src/XonoticGodot.Common/Gameplay/Mutators/SandboxMutator.cs:ObjectEditGet"
    constants:
      - { name: "g_sandbox_editor_distance_edit", base: "300", port: "300", match: true }
    status:
      logic: faithful
      values: faithful
      timing: na
      presentation: na
      audio: na
      liveness: dead
    intended_divergence: false
    divergence_rationale: null
    gaps:
      - "DEAD: depends on the Trace seam (null on live path) and is only reachable from HandleCommand (no callers); ObjectEditGet returns null when Trace is null, so nothing is ever selectable."
    verification:
      - { kind: code, ref: "ObjectEditGet: range + classname-via-hit + per-owner gate (unowned OR free>=2 OR mine) — faithful to Base permission ladder", result: pass }
      - { kind: grep, ref: "Trace property has no `.Trace =` assignment; ObjectEditGet returns null when Trace==null", result: fail }
    confidence: high
    notes: "FIXED logic/values (missing→faithful). STILL OPEN: dead (Trace seam unwired + no command caller)."

  - id: mutator-sandbox.object.edit_properties
    name: "object_edit — skin/alpha/color_main/color_glow/frame/scale/solidity/physics/force/material"
    layer: [authority]
    base_refs:
      - "common/mutators/mutator/sandbox/sv_sandbox.qc:SV_ParseClientCommand(object_edit)"
    port_refs:
      - "src/XonoticGodot.Common/Gameplay/Mutators/SandboxMutator.cs:HandleCommand(object_edit)"
    constants:
      - { name: "solidity_map", base: "0->SOLID_TRIGGER / 1->SOLID_BBOX", port: "0->Trigger / 1->BBox", match: true }
      - { name: "physics_map", base: "0->MOVETYPE_NONE / 1->MOVETYPE_TOSS / 2->MOVETYPE_PHYSICS", port: "0->None / 1->Toss / 2->Push", match: false }
      - { name: "force_field", base: "damageforcescale", port: "DamageForceScale", match: true }
    status:
      logic: faithful
      values: partial
      timing: na
      presentation: missing
      audio: na
      liveness: dead
    intended_divergence: false
    divergence_rationale: null
    gaps:
      - "DEAD: object_edit lives in HandleCommand (no callers)."
      - "values: physics 2 maps to MoveType.Push (engine value 7), but Base MOVETYPE_PHYSICS is a distinct higher value — the numeric movetype differs (no real physics-engine behavior backs it either way, so behaviorally moot, but the stored/serialized movetype byte diverges)."
      - "The faithful Base solidity→physics fall-through (missing break;) IS preserved (goto case \"physics\")."
    verification:
      - { kind: code, ref: "all property cases present; solidity-falls-into-physics quirk reproduced via goto case; material precache deferred (intentional)", result: pass }
      - { kind: code, ref: "EntityEnums.cs: MoveType.Push = 7; Base MOVETYPE_PHYSICS != 7 → physics-2 movetype value diverges; serialized as (float)physics in save/load so round-trips internally consistent but not Base-byte-identical", result: fail }
    confidence: high
    notes: "FIXED logic (missing→faithful, incl. the break-fallthrough quirk). values:partial — physics-2 movetype enum mismatch. STILL OPEN: dead (no command caller)."

  - id: mutator-sandbox.object.scale
    name: "Object scale clamp + mins/maxs resize"
    layer: [authority]
    base_refs:
      - "common/mutators/mutator/sandbox/sv_sandbox.qc:sandbox_ObjectEdit_Scale"
    port_refs:
      - "src/XonoticGodot.Common/Gameplay/Mutators/SandboxMutator.cs:ObjectEditScale"
    constants:
      - { name: "g_sandbox_object_scale_min", base: "0.1", port: "0.1", match: true }
      - { name: "g_sandbox_object_scale_max", base: "2", port: "2", match: true }
    status:
      logic: faithful
      values: faithful
      timing: na
      presentation: missing
      audio: na
      liveness: dead
    intended_divergence: false
    divergence_rationale: null
    gaps:
      - "DEAD: only reached from object_edit/ObjectPortLoad, both unreachable on the live path."
      - "Model re-set + mins/maxs resize depend on the entity service (Edict); headless/unwired path skips them."
    verification:
      - { kind: code, ref: "ObjectEditScale: clamp(0.1,f,2), SetModel reset, RoundPerfectVector(mins*scale)/(maxs*scale) (#2742) via Round-to-1/8 — faithful", result: pass }
      - { kind: grep, ref: "callers are ObjectEditScale<-object_edit/ObjectPortLoad, all gated behind dead command/store paths", result: fail }
    confidence: high
    notes: "FIXED logic/values (missing→faithful), RoundPerfectVector reproduced. STILL OPEN: dead."

  - id: mutator-sandbox.object.attach
    name: "object_attach get/set/remove — child object attachment to tag/bone"
    layer: [authority]
    base_refs:
      - "common/mutators/mutator/sandbox/sv_sandbox.qc:sandbox_ObjectAttach_Set"
      - "common/mutators/mutator/sandbox/sv_sandbox.qc:sandbox_ObjectAttach_Remove"
      - "common/mutators/mutator/sandbox/sv_sandbox.qc:SV_ParseClientCommand(object_attach)"
    port_refs:
      - "src/XonoticGodot.Common/Gameplay/Mutators/SandboxMutator.cs:ObjectAttachSet"
      - "src/XonoticGodot.Common/Gameplay/Mutators/SandboxMutator.cs:ObjectAttachRemove"
      - "src/XonoticGodot.Common/Gameplay/Mutators/SandboxMutator.cs:HandleCommand(object_attach)"
    constants:
      - { name: "attach_movetype", base: "MOVETYPE_FOLLOW", port: "MoveType.Follow", match: true }
      - { name: "attach_solid", base: "SOLID_NOT", port: "Solid.Not", match: true }
      - { name: "MAX_STORAGE_ATTACHMENTS", base: "16", port: "16", match: true }
    status:
      logic: faithful
      values: faithful
      timing: na
      presentation: missing
      audio: na
      liveness: dead
    intended_divergence: false
    divergence_rationale: null
    gaps:
      - "DEAD: object_attach get/set/remove live in HandleCommand (no callers)."
      - "presentation: setattachment (engine tag-follow) has no port seam — attached children store the tag and old_solid/old_movetype faithfully but are not actually tag-followed (TODO in source)."
    verification:
      - { kind: code, ref: "Set persists OldSolid/OldMoveType, sets Follow/Not/takedamage=false; Remove restores per-child state, freezes angles, clears spin — faithful; 16-slot cap honored in save", result: pass }
      - { kind: grep, ref: "HandleCommand(object_attach) has no caller; setattachment TODO not implemented", result: fail }
    confidence: high
    notes: "FIXED logic/values (missing→faithful). STILL OPEN: dead + no real tag-follow."

  - id: mutator-sandbox.object.remove
    name: "object_remove — delete object + detach children + clear selections"
    layer: [authority]
    base_refs:
      - "common/mutators/mutator/sandbox/sv_sandbox.qc:sandbox_ObjectRemove"
      - "common/mutators/mutator/sandbox/sv_sandbox.qc:SV_ParseClientCommand(object_remove)"
    port_refs:
      - "src/XonoticGodot.Common/Gameplay/Mutators/SandboxMutator.cs:ObjectRemove"
      - "src/XonoticGodot.Common/Gameplay/Mutators/SandboxMutator.cs:HandleCommand(object_remove)"
    constants: []
    status:
      logic: faithful
      values: na
      timing: na
      presentation: missing
      audio: na
      liveness: dead
    intended_divergence: false
    divergence_rationale: null
    gaps:
      - "DEAD: object_remove lives in HandleCommand (no callers)."
    verification:
      - { kind: code, ref: "ObjectRemove: detach children (ObjectAttachRemove), clear stale _attachSelection entries pointing at e, delete edict, --object_count — faithful to Base", result: pass }
      - { kind: grep, ref: "HandleCommand(object_remove) has no caller", result: fail }
    confidence: high
    notes: "FIXED logic (missing→faithful). STILL OPEN: dead."

  - id: mutator-sandbox.object.duplicate
    name: "object_duplicate copy/paste via clipboard cvar"
    layer: [authority]
    base_refs:
      - "common/mutators/mutator/sandbox/sv_sandbox.qc:SV_ParseClientCommand(object_duplicate)"
      - "common/mutators/mutator/sandbox/sv_sandbox.qc:sandbox_ObjectPort_Save"
      - "common/mutators/mutator/sandbox/sv_sandbox.qc:sandbox_ObjectPort_Load"
    port_refs:
      - "src/XonoticGodot.Common/Gameplay/Mutators/SandboxMutator.cs:HandleCommand(object_duplicate)"
      - "src/XonoticGodot.Common/Gameplay/Mutators/SandboxMutator.cs:ObjectPortSave"
      - "src/XonoticGodot.Common/Gameplay/Mutators/SandboxMutator.cs:ObjectPortLoad"
    constants:
      - { name: "clipboard_cvar", base: "cl_sandbox_clipboard", port: "cl_sandbox_clipboard", match: true }
    status:
      logic: faithful
      values: faithful
      timing: faithful
      presentation: na
      audio: na
      liveness: dead
    intended_divergence: false
    divergence_rationale: null
    gaps:
      - "DEAD: copy/paste live in HandleCommand (no callers)."
      - "Copy pushes the serialized object to the client cvar via Store.SetClipboard (stuffcmd equivalent), but Store is null on the live path → clipboard write is inert."
      - "Paste shares the flood + maxobjects gates with object_spawn (faithful)."
    verification:
      - { kind: code, ref: "copy: ObjectPortSave(e,false) + escape quotes + SetClipboard; paste: flood/empty/cap checks then ObjectPortLoad — faithful; ObjectPortSave/Load serializer reproduces the slot layout, %.9v vectors, ftos, db columns", result: pass }
      - { kind: grep, ref: "no caller for HandleCommand; Store has no `.Store =` assignment (SetClipboard inert)", result: fail }
    confidence: high
    notes: "FIXED logic/values/timing (missing→faithful), incl. a faithful textual serializer. STILL OPEN: dead + clipboard store unwired."

  - id: mutator-sandbox.object.claim_info
    name: "object_claim + object_info object/mesh/attachments"
    layer: [authority]
    base_refs:
      - "common/mutators/mutator/sandbox/sv_sandbox.qc:SV_ParseClientCommand(object_claim)"
      - "common/mutators/mutator/sandbox/sv_sandbox.qc:SV_ParseClientCommand(object_info)"
    port_refs:
      - "src/XonoticGodot.Common/Gameplay/Mutators/SandboxMutator.cs:HandleCommand(object_claim)"
      - "src/XonoticGodot.Common/Gameplay/Mutators/SandboxMutator.cs:HandleCommand(object_info)"
    constants: []
    status:
      logic: faithful
      values: na
      timing: na
      presentation: missing
      audio: na
      liveness: dead
    intended_divergence: false
    divergence_rationale: null
    gaps:
      - "DEAD: claim/info live in HandleCommand (no callers); also need CryptoIdfpProvider/NetNameProvider/PrintTo (all null)."
      - "object_info mesh: tag enumeration (FOR_EACH_TAG) needs the model-skeleton service — not implemented (TODO); mesh subcommand prints the header but no tag list."
    verification:
      - { kind: code, ref: "claim: UID check, owner-name update, already-mine short-circuit, crypto_idfp set — faithful; info object/attachments faithful; info mesh stubs the tag loop", result: pass }
      - { kind: grep, ref: "no HandleCommand caller; CryptoIdfpProvider/NetNameProvider/PrintTo unassigned", result: fail }
    confidence: high
    notes: "FIXED logic (missing→faithful) except the mesh tag-enumeration TODO. STILL OPEN: dead + UID/print seams unwired."

  - id: mutator-sandbox.storage.persist
    name: "Per-map object storage: save/load + 5s auto-save + autoload at map start"
    layer: [authority]
    base_refs:
      - "common/mutators/mutator/sandbox/sv_sandbox.qc:sandbox_Database_Save"
      - "common/mutators/mutator/sandbox/sv_sandbox.qc:sandbox_Database_Load"
      - "common/mutators/mutator/sandbox/sv_sandbox.qc:MUTATOR_HOOKFUNCTION(sandbox, SV_StartFrame)"
    port_refs:
      - "src/XonoticGodot.Common/Gameplay/Mutators/SandboxMutator.cs:DatabaseSave"
      - "src/XonoticGodot.Common/Gameplay/Mutators/SandboxMutator.cs:DatabaseLoad"
      - "src/XonoticGodot.Common/Gameplay/Mutators/SandboxMutator.cs:OnStartFrame"
    constants:
      - { name: "g_sandbox_storage_name", base: "default", port: "default", match: true }
      - { name: "g_sandbox_storage_autosave", base: "5", port: "5", match: true }
      - { name: "g_sandbox_storage_autoload", base: "1", port: "1", match: true }
      - { name: "storage_path", base: "sandbox/storage_<name>_<map>.txt", port: "delegated to IObjectStore (host builds path)", match: false }
    status:
      logic: faithful
      values: faithful
      timing: faithful
      presentation: na
      audio: na
      liveness: partial
    intended_divergence: false
    divergence_rationale: null
    gaps:
      - "The 5s auto-save TICK is genuinely live (OnStartFrame is on the fired MutatorHooks.SvStartFrame chain, autosave_time seeded correctly), but DatabaseSave bails when Store is null → no file is ever written; likewise DatabaseLoad bails → no autoload."
      - "No host wires IObjectStore (file IO + path = sandbox/storage_<name>_<map>.txt), so per-map persistence is inert in a real match."
      - "Sandbox_SaveAllowed mutator-hook veto has no port chain (TODO)."
    verification:
      - { kind: code, ref: "OnStartFrame: gate on autosave==0, time<autosave_time, reschedule, DatabaseSave; header line + per-parent ObjectPortSave(true) — faithful. DatabaseLoad parses lines, skips //#, ObjectPortLoad(true). Subscribed to live SvStartFrame.", result: pass }
      - { kind: grep, ref: "no `.Store =` assignment; DatabaseSave/Load early-return on null Store", result: fail }
    confidence: high
    notes: "FIXED on logic/values/timing and the autosave TICK is live (na→partial). STILL OPEN: actual persistence dead because the IObjectStore file-IO seam is unwired; SaveAllowed hook absent."

  - id: mutator-sandbox.drag.grab
    name: "Drag/grab integration: carry objects with +button8 gated by .grab"
    layer: [authority]
    base_refs:
      - "server/cheats.qc:Drag (grab switch on e.grab)"
      - "server/cheats.qc:Drag_Begin"
      - "server/cheats.qc:Drag_Finish"
    port_refs: []
    constants:
      - { name: "g_grab_range", base: "200", port: "none", match: false }
      - { name: "drag_movetype", base: "MOVETYPE_WALK (gravity ~0.00001)", port: "none", match: false }
    status:
      logic: missing
      values: missing
      timing: missing
      presentation: missing
      audio: na
      liveness: na
    intended_divergence: false
    divergence_rationale: null
    gaps:
      - "STILL MISSING: no Drag/Drag_Begin/Drag_Finish system. The think tick computes .grab (e.Grab) but nothing consumes it; +button8 does nothing; objects cannot be physically carried."
    verification:
      - { kind: grep, ref: "no Drag_Begin/Drag_Finish/+button8 grab consumer in src/ or game/; e.Grab is written by ObjectThink but never read", result: fail }
    confidence: high
    notes: "UNCHANGED from baseline (still missing). The shared generic drag system (also used by g_monsters_edit) was not ported. Only the .grab classification half exists (and even that is on an empty object set)."

  - id: mutator-sandbox.hooks.gating
    name: "readonly mode + Sandbox_DragAllowed/SaveAllowed/EditAllowed mutator hooks"
    layer: [authority]
    base_refs:
      - "common/mutators/mutator/sandbox/sv_sandbox.qc:SV_ParseClientCommand (readonly/EditAllowed gate)"
      - "server/mutators/events.qh:Sandbox_DragAllowed"
      - "server/mutators/events.qh:Sandbox_SaveAllowed"
      - "server/mutators/events.qh:Sandbox_EditAllowed"
    port_refs:
      - "src/XonoticGodot.Common/Gameplay/Mutators/SandboxMutator.cs:HandleCommand (ReadOnly gate)"
    constants:
      - { name: "g_sandbox_readonly", base: "0", port: "0", match: true }
      - { name: "g_sandbox_info", base: "1", port: "1", match: true }
    status:
      logic: partial
      values: faithful
      timing: na
      presentation: na
      audio: na
      liveness: dead
    intended_divergence: false
    divergence_rationale: null
    gaps:
      - "g_sandbox_readonly IS read and gates HandleCommand + grab class, but HandleCommand is dead (no caller), so the readonly gate is unreachable."
      - "The three Sandbox_* veto hooks (DragAllowed/SaveAllowed/EditAllowed) have NO port hook-chain — no other mutator can gate sandbox. Source has explicit TODOs noting the chain is missing; each call site is approximated as 'readonly only'."
      - "g_sandbox_info IS read and gates the (currently no-op) server LOG verbosity faithfully."
    verification:
      - { kind: code, ref: "ReadOnly/Info cvars read; HandleCommand prints the readonly message and bails; ObjectThink readonly→grab 0 — faithful where reachable", result: pass }
      - { kind: grep, ref: "no Sandbox_DragAllowed/SaveAllowed/EditAllowed hook chain in src/; TODO comments confirm gating is readonly-only", result: fail }
    confidence: high
    notes: "FIXED partially: readonly + info cvars now read (missing→partial). STILL OPEN: the three Sandbox_* hooks absent; readonly gate dead because HandleCommand has no caller."

  - id: mutator-sandbox.menu.tools_dialog
    name: "Sandbox Tools menu dialog (cvar inputs + command buttons)"
    layer: [presentation]
    base_refs:
      - "menu/xonotic/dialog_sandboxtools.qc:XonoticSandboxToolsDialog_fill"
    port_refs:
      - "game/menu/dialogs/DialogSandboxTools.cs:BuildUi"
      - "game/Shell.cs:188 (\"sandbox\" => new DialogSandboxTools())"
    constants:
      - { name: "slider_skin_frame", base: "0..99 step 1", port: "0..99 step 1", match: true }
      - { name: "slider_alpha", base: "0.1..1 step 0.05", port: "0.1..1 step 0.05", match: true }
      - { name: "slider_scale", base: "0.25..2 step 0.05", port: "0.25..2 step 0.05", match: true }
      - { name: "slider_force", base: "0..10 step 0.5", port: "0..10 step 0.5", match: true }
      - { name: "clipboard_cvar", base: "cl_sandbox_clipboard", port: "cl_sandbox_clipboard", match: true }
    status:
      logic: faithful
      values: faithful
      timing: na
      presentation: faithful
      audio: na
      liveness: dead
    intended_divergence: false
    divergence_rationale: null
    gaps:
      - "Dialog opens and controls move, but every action button is a no-op: the `sandbox …` command strings still route to MenuCommand and are logged inert. Wave-2 added a server-side SandboxMutator backend, but the menu/console commands are NOT routed to its HandleCommand (no SV_ParseClientCommand seam, `g_sandbox` not in ClientCommandRegistry), so the buttons remain dead end-to-end."
      - "No in-game entry point: the Shell `\"sandbox\"` factory key is reached ONLY via the dev/CI `--menu-screen sandbox` flag. No menu button or keybind opens it in a normal match (Base uses the `menu_showsandboxtools` keybind, no port equivalent)."
    verification:
      - { kind: code, ref: "DialogSandboxTools.cs: all cvar names, slider ranges (skin/frame 0..99/1, alpha 0.1..1/0.05, scale 0.25..2/0.05, force 0..10/0.5), command strings match Base exactly", result: pass }
      - { kind: code, ref: "DialogSandboxTools.cs doc-comment still says buttons drive a backend the port 'does not have yet', logged inert via MenuCommand; grep confirms `sandbox` commands reach no server handler", result: fail }
      - { kind: code, ref: "Shell.cs:188 is the only reference; no button/keybind pushes it (dev --menu-screen only)", result: fail }
    confidence: high
    notes: "UI unchanged from baseline (faithful, dead). The Wave-2 SandboxMutator did NOT close the dialog's deadness: the command-routing seam between the menu/console and HandleCommand is still missing, and there is still no normal-match entry point. NOT a regression — same status, with the gap description updated to reflect that a backend now exists but is unrouted."
