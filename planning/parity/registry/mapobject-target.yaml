unit: mapobject-target
category: mapobject
base_ref_rev: "v0.8.6-1779-g863cd3e84"
last_audited: "2026-06-25"
summary:
  feature_count: 15
  worst_gaps:
    - "target_spawn is STILL a documented no-op: the $-templating reflection engine is cut AND SpawnEntityHandler is never assigned (grep 2026-06-25: only the declaration at TargetUtilities.cs:538; zero assignments incl. tests), so on-use and ON_MAPLOAD both invoke a null seam — nothing is created or edited."
    - "target_music / trigger_music override is still LISTEN-SERVER ONLY (TE_CSQC_TARGET_MUSIC / ENT_CLIENT_TRIGGER_MUSIC net paths not ported); a remote/dedicated client receives no override. target_music_use is still a GLOBAL override (not QC per-activator). The per-round reset plumbing is LIVE (ResetMapObjects fires e.Reset on every non-client entity)."
    - "target_speaker ACTIVATOR (BIT3) has a dedicated IsRealClient-gated handler on the correct CH_TRIGGER channel, but with no MSG_ONE per-client sound facade it still plays to ALL clients; the LOOPED_OFF-untargeted objerror is not modelled (port silently leaves it inert)."
    - "FIXED THIS PASS: target_spawnpoint's forced-spawn output is now LIVE (SpawnSystem.SelectSpawnPoint reads forPlayer.SpawnPointTarg); target_location's %l HUD/chat reader is now confirmed live (Chat.NearestLocation walks MapObjectsState.Locations); target_speaker now installs SpeakerReset on LOOPED_ON/OFF (round-restart re-arm) AND attempts the *-prefixed per-player voice-sample via the wired ModelSoundResolver (no longer always-silent)."

features:
  - id: mapobject-target.kill.use
    name: "target_kill — deal 1000 void damage to the activator"
    layer: [authority]
    base_refs:
      - "common/mapobjects/target/kill.qc:target_kill_use"
      - "common/mapobjects/target/kill.qc:spawnfunc(target_kill)"
    port_refs:
      - "src/XonoticGodot.Common/Gameplay/MapObjects/TargetUtilities.cs:KillUse"
      - "src/XonoticGodot.Common/Gameplay/MapObjects/TargetUtilities.cs:KillSetup"
    constants:
      - { name: "damage", base: "1000", port: "1000", match: true }
      - { name: "deathtype", base: "DEATH_HURTTRIGGER", port: "DeathTypes.Void", match: true }
      - { name: "default_message", base: "was in the wrong place", port: "was in the wrong place", match: true }
      - { name: "default_message2", base: "was thrown into a world of hurt by", port: "was thrown into a world of hurt by", match: true }
    status:
      logic: faithful
      values: faithful
      timing: na
      presentation: na
      audio: na
      liveness: live
    intended_divergence: false
    divergence_rationale: null
    gaps:
      - "QC gates on actor.iscreature || actor.damagedbytriggers; the port has no damagedbytriggers field and substitutes IsPushable (projectiles/loot/bodies) — same intent, slightly different membership."
    verification:
      - { kind: test, ref: "tests/XonoticGodot.Tests/TargetUtilitiesTests.cs:TargetKill_DamagesCreatureActorToDeath", result: pass }
      - { kind: code, ref: "KillUse gate + Combat.Damage(...,1000,DeathTypes.Void); registered MapObjectsRegistry.cs; reached via MapObjectsCommon.UseTargets (t.Use(t,actor))", result: pass }
    confidence: high
    notes: "Re-verified 2026-06-25 unchanged. Reached on the live path via SUB_UseTargets from a trigger/button."

  - id: mapobject-target.speed.use
    name: "target_speed — reproject the activator's velocity from speed + axis mask"
    layer: [authority]
    base_refs:
      - "common/mapobjects/target/speed.qc:target_speed_use"
      - "common/mapobjects/target/speed.qc:target_speed_calculatevelocity"
    port_refs:
      - "src/XonoticGodot.Common/Gameplay/MapObjects/TargetUtilities.cs:SpeedUse"
      - "src/XonoticGodot.Common/Gameplay/MapObjects/TargetUtilities.cs:CalculateVelocity"
    constants:
      - { name: "default_speed", base: "100", port: "100", match: true }
      - { name: "SPEED_PERCENTAGE/ADD/POS_X/NEG_X/POS_Y/NEG_Y/POS_Z/NEG_Z/LAUNCHER", base: "1/2/4/8/16/32/64/128/256", port: "1/2/4/8/16/32/64/128/256", match: true }
    status:
      logic: faithful
      values: faithful
      timing: na
      presentation: na
      audio: na
      liveness: live
    intended_divergence: true
    divergence_rationale: "The Q3COMPAT launcher branch (which accumulates launcherspeed inside the per-axis loop — a deliberately-preserved Q3 bug) is not taken because the port has no Q3COMPAT flag; the faithful non-Q3 path is always used. Also the CSQC ENT_CLIENT_TARGET_SPEED net copy (for client prediction) is not ported — the server-side velocity edit is authoritative."
    gaps:
      - "On a Q3-compat map a launcher target_speed would apply a different (larger) speed in Base; the port always uses the non-Q3 magnitude."
      - "No CSQC-side velocity application, so a target_speed mid-air boost may visibly correct on the next server update rather than predict instantly."
    verification:
      - { kind: test, ref: "tests/XonoticGodot.Tests/TargetUtilitiesTests.cs:TargetSpeed_SetsAxisVelocityToSpeed", result: pass }
      - { kind: test, ref: "tests/XonoticGodot.Tests/TargetUtilitiesTests.cs:TargetSpeed_Percentage_ScalesCurrentSpeed", result: pass }
      - { kind: code, ref: "spawnflag bits verified against speed.qh (all 9 match exactly)", result: pass }
    confidence: high
    notes: "Re-verified 2026-06-25 unchanged. All 9 spawnflag bits verified."

  - id: mapobject-target.spawnpoint.use
    name: "target_spawnpoint — force the activator's next spawn point"
    layer: [authority]
    base_refs:
      - "common/mapobjects/target/spawnpoint.qc:target_spawnpoint_use"
    port_refs:
      - "src/XonoticGodot.Common/Gameplay/MapObjects/TargetUtilities.cs:SpawnPointUse"
      - "src/XonoticGodot.Common/Gameplay/Player/SpawnSystem.cs:SelectSpawnPoint"
    constants: []
    status:
      logic: faithful
      values: na
      timing: na
      presentation: na
      audio: na
      liveness: live
    intended_divergence: false
    divergence_rationale: null
    gaps:
      - "The .reset (target_spawnpoint_reset, re-asserts ACTIVE_ACTIVE) is a no-op trivially-satisfied invariant; no port .Reset hook installed but immaterial (Active is already ActiveActive)."
    verification:
      - { kind: test, ref: "tests/XonoticGodot.Tests/TargetUtilitiesTests.cs:TargetSpawnpoint_SetsActorForcedSpawn", result: pass }
      - { kind: code, ref: "SpawnPointUse sets actor.SpawnPointTarg = self; registered MapObjectsRegistry.cs", result: pass }
      - { kind: code, ref: "SpawnSystem.SelectSpawnPoint (SpawnSystem.cs:344) NOW reads `if (forPlayer.SpawnPointTarg is { IsFreed:false } forced) return ToSpawnPoint(forced);` — short-circuits gather/score/team-filter exactly like QC `if(this.spawnpoint_targ) return this.spawnpoint_targ;`. One-shot: cleared by PutPlayerInServer (SpawnSystem.cs:678). Race also writes SpawnPointTarg for checkpoint respawns (Race.cs:1021), so the field has multiple live producers and a live consumer", result: pass }
    confidence: high
    notes: "FIXED since 2026-06-22: liveness partial->live. The prior audit recorded SpawnPointTarg as written-but-never-read; the spawnsystem unit has since wired SelectSpawnPoint to honor it (and clear it on spawn), so the forced next-spawn now actually takes effect on the live spawn path. Sets actor.SpawnPointTarg = this, matching QC."

  - id: mapobject-target.location.spawn
    name: "target_location / info_location — HUD location-name volumes"
    layer: [authority, presentation]
    base_refs:
      - "common/mapobjects/target/location.qc:spawnfunc(target_location)"
      - "common/mapobjects/target/location.qc:spawnfunc(info_location)"
    port_refs:
      - "src/XonoticGodot.Common/Gameplay/MapObjects/TargetUtilities.cs:LocationSetup"
      - "src/XonoticGodot.Common/Gameplay/MapObjects/TargetUtilities.cs:InfoLocationSetup"
      - "src/XonoticGodot.Server/Chat.cs:NearestLocation"
    constants: []
    status:
      logic: faithful
      values: na
      timing: na
      presentation: faithful
      audio: na
      liveness: live
    intended_divergence: false
    divergence_rationale: null
    gaps:
      - "QC NearestLocation falls back to the nearest g_items entity (findnearest checkitems=true) when no target_location wins; the port's reader only walks the location volumes (item fallback out of scope — documented at Chat.cs:648)."
    verification:
      - { kind: test, ref: "tests/XonoticGodot.Tests/TargetUtilitiesTests.cs:TargetLocation_RegistersIntoLocationsListAsPassivePoint", result: pass }
      - { kind: test, ref: "tests/XonoticGodot.Tests/TargetUtilitiesTests.cs:InfoLocation_CopiesNetnameToMessage", result: pass }
      - { kind: code, ref: "LocationSetup pushes to MapObjectsState.Locations; both classes registered MapObjectsRegistry.cs", result: pass }
      - { kind: code, ref: "Chat.NearestLocation (Chat.cs:651) walks MapObjectsState.Locations and returns the nearest label; reached from the %l/%y/%d chat formatmessage token expansion (Chat.cs:584-586) — the live chat path. So the location volumes ARE consumed (the prior audit left this untraced)", result: pass }
    confidence: high
    notes: "RECLASSIFIED 2026-06-25: presentation unknown->faithful, liveness unknown->live. Passive SOLID_NOT point pushed to MapObjectsState.Locations (matches target_push_init + IL_PUSH(g_locations)); the %l/%y/%d reader (Chat.NearestLocation) now confirmed to consume that list and pick the nearest name, reached on the live chat token-expansion path. info_location copies netname->message then re-sets classname."

  - id: mapobject-target.changelevel.use
    name: "target_changelevel — end / switch the level (multiplayer fraction)"
    layer: [authority]
    base_refs:
      - "common/mapobjects/target/changelevel.qc:target_changelevel_use"
      - "common/mapobjects/target/changelevel.qc:spawnfunc(target_changelevel)"
      - "common/mapobjects/target/changelevel.qh:CHANGELEVEL_MULTIPLAYER"
    port_refs:
      - "src/XonoticGodot.Common/Gameplay/MapObjects/TargetUtilities.cs:ChangeLevelUse"
      - "src/XonoticGodot.Common/Gameplay/MapObjects/TargetUtilities.cs:ChangeLevelSetup"
      - "src/XonoticGodot.Server/GameWorld.cs:650"
    constants:
      - { name: "count_default_fraction", base: "0.7", port: "0.7 (when count==0)", match: true }
      - { name: "CHANGELEVEL_MULTIPLAYER", base: "BIT(1)=2", port: "1<<1=2 (TargetUtilities.cs:272)", match: true }
    status:
      logic: faithful
      values: faithful
      timing: na
      presentation: na
      audio: na
      liveness: live
    intended_divergence: false
    divergence_rationale: null
    gaps:
      - "The .gametype next-map gametype switch is wired (SwitchGameTypeHandler -> Cvars.Set(\"gametype\", gt), GameWorld.cs:675), but it only sets the cvar for the NEXT changelevel boot — it does NOT live-switch MapInfo like QC's MapInfo_SwitchGameType. Faithful enough on the named-chmap path; a bare end-match (.chmap empty) carrying a .gametype changes the cvar but the immediate intermission/next-map still uses the current gametype context."
      - "Listen-server / authoritative: the CSQC side of the changelevel is not networked (no remote vote UI), but the rules-side fraction + map switch are server-authoritative and faithful."
    verification:
      - { kind: test, ref: "tests/XonoticGodot.Tests/TargetUtilitiesTests.cs:TargetChangelevel_EmptyChmap_CallsNextLevel", result: pass }
      - { kind: code, ref: "changelevel.qh CHANGELEVEL_MULTIPLAYER=BIT(1); port TargetUtilities.cs:272 `1<<1` (=2) — matches", result: pass }
      - { kind: code, ref: "ALL SIX host seams assigned in GameWorld.Boot (GameWorld.cs:628/640/650/659/662/675/677): GiveItemHandler/IsCampaign/NextLevelHandler/ChangeLevelHandler/RealPlayerVoteCount/SwitchGameTypeHandler/CampaignLevelWarpHandler — re-wired every Boot so they never go stale (re-confirmed LIVE 2026-06-25)", result: pass }
      - { kind: code, ref: ".chmap / .gametype BSP keys bound (MapObjectFieldsExtra.cs:142-143) on the live entity-field path; .count bound as FLOAT onto ParticleCount so a fractional 0.5 survives (ChangeLevelSetup seeds MoverCnt from ParticleCount)", result: pass }
    confidence: high
    notes: "Re-confirmed LIVE 2026-06-25. Registered via SpawnFuncs, reached by UseTargets, all host seams wired in Boot, chmap/gametype keys bound, fractional count survives. CHANGELEVEL_MULTIPLAYER bit faithful (1<<1=BIT(1)=2). Remaining minor divergence: gametype switch is cvar-for-next-boot, not live MapInfo_SwitchGameType."

  - id: mapobject-target.levelwarp.use
    name: "target_levelwarp — campaign level warp"
    layer: [authority]
    base_refs:
      - "common/mapobjects/target/levelwarp.qc:target_levelwarp_use"
    port_refs:
      - "src/XonoticGodot.Common/Gameplay/MapObjects/TargetUtilities.cs:LevelWarpUse"
      - "src/XonoticGodot.Server/GameWorld.cs:677"
    constants: []
    status:
      logic: faithful
      values: na
      timing: na
      presentation: na
      audio: na
      liveness: live
    intended_divergence: false
    divergence_rationale: null
    gaps:
      - "Live only inside a campaign (g_campaign): outside a campaign the .use early-returns (matching QC). Campaign.LevelWarp itself is a real Unload/Load/Setup implementation — only reachable on the single-player campaign path."
    verification:
      - { kind: test, ref: "tests/XonoticGodot.Tests/TargetUtilitiesTests.cs:TargetLevelwarp_OnlyInCampaign", result: pass }
      - { kind: code, ref: "CampaignLevelWarpHandler = n => Campaign.LevelWarp(n) and IsCampaign = () => Cvars.Bool(\"g_campaign\") both assigned in GameWorld.Boot (GameWorld.cs:677/640), re-wired every Boot — re-confirmed LIVE 2026-06-25. Campaign.LevelWarp is a real Unload/Load(n,1)/Setup implementation", result: pass }
      - { kind: code, ref: "LevelWarpUse cnt-1 specific-level / 0->next (-1) logic faithful; registered MapObjectsRegistry.cs; reached via UseTargets", result: pass }
    confidence: high
    notes: "Re-confirmed LIVE 2026-06-25. CampaignLevelWarpHandler + IsCampaign wired in Boot and Campaign.LevelWarp is a real implementation, so a campaign level-warp trigger now warps. cnt-1/0->next logic faithful."

  - id: mapobject-target.give.use
    name: "target_give — give the activator the items pointed to"
    layer: [authority]
    base_refs:
      - "common/mapobjects/target/give.qc:target_give_use"
    port_refs:
      - "src/XonoticGodot.Common/Gameplay/MapObjects/TargetUtilities.cs:GiveUse"
      - "src/XonoticGodot.Server/GameWorld.cs:628"
    constants: []
    status:
      logic: faithful
      values: faithful
      timing: na
      presentation: na
      audio: faithful
      liveness: live
    intended_divergence: false
    divergence_rationale: null
    gaps:
      - "QC iterates the g_items IL filtered by targetname; the port resolves by targetname over the whole entity index then filters to Item-flagged / item_* classnames — equivalent for placed items but would miss a non-standard item membership."
      - "The buff-item branch (GiveBuff + SND_SHIELD_RESPAWN) is folded into the GiveItemHandler pickup path; not separately verified."
      - "QC chooses CH_TRIGGER_SINGLE for powerups vs CH_TRIGGER for others; the port delegates the sound entirely to GiveItemHandler's pickup path (channel choice not re-verified)."
    verification:
      - { kind: code, ref: "GameWorld.cs:628 wires TargetUtilities.GiveItemHandler -> ItemGiveTo + PlayPickupSound (re-confirmed LIVE 2026-06-25)", result: pass }
    confidence: medium
    notes: "Re-verified 2026-06-25 unchanged: GiveItemHandler wired on server boot — genuinely live on the match path. GiveUse itself unchanged. The SUB_UseTargets tail runs."

  - id: mapobject-target.items.use
    name: "target_items — set/add items + resources + powerups via a token string"
    layer: [authority]
    base_refs:
      - "common/mapobjects/target/items.qc:target_items_use"
      - "common/mapobjects/target/items.qc:spawnfunc(target_items)"
    port_refs:
      - "src/XonoticGodot.Common/Gameplay/MapObjects/TargetUtilities.cs:ItemsUse"
      - "src/XonoticGodot.Common/Gameplay/MapObjects/TargetUtilities.cs:ApplyGiveTokens"
    constants:
      - { name: "powerup_durations", base: "g_balance_powerup_{strength,invincible,speed,invisibility}_time + g_balance_superweapons_time", port: "same cvars, fallback 30", match: true }
    status:
      logic: partial
      values: faithful
      timing: na
      presentation: faithful
      audio: na
      liveness: live
    intended_divergence: false
    divergence_rationale: null
    gaps:
      - "The spawnfunc's normalized re-serialization of item tokens into a max/min/minus-prefixed give string (driven by spawnflags 0/1/2/4, then re-tokenized) is NOT reproduced; the port keeps the raw netname and parses it directly via GiveItems.Apply — equivalent for the common (give-string) case, but spawnflag-prefixed (max/min/minus) target_items semantics and the GetResource-driven resource serialization are not honored."
      - "The loot-toucher delete branch (ITEM_IS_LOOT actor -> EXACTTRIGGER_TOUCH + delete, and IL_EACH delete of loot enemies) is not modelled (target_items is .use-only here; the port early-returns on an Item-flagged actor instead of deleting it)."
    verification:
      - { kind: test, ref: "tests/XonoticGodot.Tests/TargetUtilitiesTests.cs:TargetItems_GivesResourcesAndItemFlags", result: pass }
      - { kind: test, ref: "tests/XonoticGodot.Tests/TargetUtilitiesTests.cs:TargetItems_GrantsPowerupStatusEffect", result: pass }
      - { kind: code, ref: "ItemsUse calls MapMover.Centerprint(actor, self.Message) (TargetUtilities.cs:510); routes through GiveItems.Apply; powerup-duration seeding from 5 balance cvars", result: pass }
      - { kind: code, ref: "MapObjectsCommon.Centerprint calls NotificationSystem.SendCenterRaw(NotifBroadcast.OneOnly, actor, message) DIRECTLY (CenterRaw->Sink.Dispatch->CenterPrintPanel) — no longer depends on the unassigned CenterprintHandler delegate, so the message text reaches the activator on the live path", result: pass }
    confidence: high
    notes: "Re-verified 2026-06-25 unchanged. Centerprint message displays for the activator via the CenterRaw notification channel. Audio faithfully silent (matches items.qc — give has no sound). The give-token apply + powerup seeding remain faithful + live. Logic still partial only for the spawnflag re-serialization + loot-toucher delete branch."

  - id: mapobject-target.spawn.use
    name: "target_spawn — data-driven entity creator/editor ($-templating)"
    layer: [authority]
    base_refs:
      - "common/mapobjects/target/spawn.qc:target_spawn_use"
      - "common/mapobjects/target/spawn.qc:target_spawn_edit_entity"
      - "common/mapobjects/target/spawn.qc:target_spawn_cancreate"
    port_refs:
      - "src/XonoticGodot.Common/Gameplay/MapObjects/TargetUtilities.cs:SpawnSetup"
      - "src/XonoticGodot.Common/Gameplay/MapObjects/TargetUtilities.cs:SpawnUse"
    constants:
      - { name: "ON_MAPLOAD spawnflag", base: "BIT(1)", port: "MapMover.SpawnAllEntities (BIT(1))", match: true }
    status:
      logic: stub
      values: na
      timing: na
      presentation: na
      audio: na
      liveness: dead
    intended_divergence: true
    divergence_rationale: "The full $-field reflection templating (numentityfields / putentityfieldstring / the field DB, deferred replacement, self/activator/other/pusher/target/target2-4/killtarget/time substitutions, +offset/+random, helper spawnfuncs setmodel/setsize, and the .count create-cap) has no port analogue and is intentionally cut (documented in the file header). The minimal SpawnSetup keeps the ON_MAPLOAD hook + a SpawnEntityHandler seam."
    gaps:
      - "STILL DEAD even on the minimal path: SpawnEntityHandler is never assigned (grep 2026-06-25: only the declaration at TargetUtilities.cs:538; zero assignments incl. tests), so both .use and ON_MAPLOAD invoke a null seam — nothing is created or edited."
      - "Maps relying on target_spawn to spawn/edit entities (mapper scripting, *activator edits, FOREACH_ENTITY_STRING(target) edits) get no effect."
    verification:
      - { kind: grep, ref: "no TargetUtilities.SpawnEntityHandler assignment anywhere (src/game/tests all empty; only decl at TargetUtilities.cs:538; re-confirmed 2026-06-25)", result: fail }
      - { kind: code, ref: "SpawnUse delegates to a null SpawnEntityHandler (TargetUtilities.cs:564); ON_MAPLOAD think wired but harmless; registered MapObjectsRegistry.cs", result: pass }
    confidence: high
    notes: "Re-verified 2026-06-25 unchanged — STILL DEAD. count create-cap, the $-mini-language, target_spawn_cancreate, and the activator/other/pusher/target replacements are all absent. ON_MAPLOAD deferred-think hook correctly gated on BIT(1). Unlike the changelevel/levelwarp/give seams (Boot-assigned), SpawnEntityHandler was NOT wired in any wave."

  - id: mapobject-target.speaker.ambient_triggered
    name: "target_speaker — ambient / looped / one-shot / activator sound emitter"
    layer: [authority]
    base_refs:
      - "common/mapobjects/target/speaker.qc:spawnfunc(target_speaker)"
      - "common/mapobjects/target/speaker.qc:target_speaker_use_on/_off/_activator"
      - "common/mapobjects/target/speaker.qc:target_speaker_reset"
      - "common/mapobjects/target/speaker.qh:SPEAKER_LOOPED_ON/OFF/GLOBAL/ACTIVATOR"
    port_refs:
      - "src/XonoticGodot.Common/Gameplay/MapObjects/TargetSpeaker.cs:SpeakerSetup"
      - "src/XonoticGodot.Common/Gameplay/MapObjects/TargetSpeaker.cs:SpeakerUseActivator"
      - "src/XonoticGodot.Common/Gameplay/MapObjects/TargetSpeaker.cs:SpeakerReset"
    constants:
      - { name: "default_volume", base: "1", port: "1", match: true }
      - { name: "atten default (targeted/untargeted)", base: "ATTEN_NORM / ATTEN_STATIC", port: "AttenNorm / AttenStatic", match: true }
      - { name: "GLOBAL/negative atten", base: "ATTEN_NONE (play everywhere)", port: "AttenNone", match: true }
      - { name: "channel (CH_TRIGGER_SINGLE / CH_TRIGGER)", base: "3 / -3", port: "SoundChannel.Item=3 / SoundChannel.TriggerAuto=-3", match: true }
      - { name: "LOOPED_ON/LOOPED_OFF/GLOBAL/ACTIVATOR", base: "BIT0/1/2/3 = 1/2/4/8", port: "1/2/4/8", match: true }
    status:
      logic: partial
      values: faithful
      timing: na
      presentation: na
      audio: partial
      liveness: live
    intended_divergence: false
    divergence_rationale: null
    gaps:
      - "ACTIVATOR (BIT3) still plays to ALL players, not just the triggering client — SpeakerUseActivator gates on IsRealClient (a bot/world activator produces nothing, matching QC's early-out) and emits on the stacking CH_TRIGGER channel, but there is no MSG_ONE / per-client sound facade, so every client hears it (documented TODO at TargetSpeaker.cs:211)."
      - "The *-prefixed per-player voice-message sample now RESOLVES (ResolveNoise routes a *-name through Sounds.PlayerSoundSample + the wired ModelSoundResolver, which handles argv(1)-style variant-count randomization) — but it requires a real-client activator with a model; an ambient/null/no-model invocation still falls back to silence (matching QC's SND(Null) not-found). Plays to all clients (no MSG_ONE), not just the triggerer."
      - "QC objerrors a LOOPED_OFF untargeted speaker; the port silently leaves it inert."
    verification:
      - { kind: code, ref: "channel CH_TRIGGER_SINGLE->Item(3)/CH_TRIGGER->TriggerAuto(-3); ACTIVATOR(BIT3) checked FIRST per spawnfunc order with IsRealClient gate (TargetSpeaker.cs:101); *-noise -> ResolveNoise(noise, activator) via ModelSoundResolver, silence fallback on no-model/null", result: pass }
      - { kind: code, ref: "atten/volume resolution + on/off toggle match speaker.qc; spawnflag bits verified against speaker.qh; reached via UseTargets; registered MapObjectsRegistry.cs", result: pass }
      - { kind: code, ref: "SpeakerReset NOW installed on LOOPED_ON (TargetSpeaker.cs:110) and LOOPED_OFF (TargetSpeaker.cs:118) -> this_.Reset; re-arms the loop state per spawnflag on round restart (LOOPED_ON restarts if toggled off, LOOPED_OFF silences if toggled on), matching QC target_speaker_reset. The central .Reset pump is live (GameWorld.ResetMapObjects fires e.Reset on every non-client entity), so this is now LIVE on a round restart", result: pass }
      - { kind: code, ref: "ModelSoundResolver wired on the host path (game/loaders/PlayerSoundResolver.cs:47), so the *-voice-sample path is not dead", result: pass }
    confidence: high
    notes: "FIXED since 2026-06-22: the LOOPED_ON/OFF reset hook is now installed (prior audit recorded 'no this_.Reset = / SpeakerReset' — that is now stale) and re-driven by the live ResetMapObjects pump; and the *-prefixed per-player voice-sample now actually resolves (was always-silent) for a real-client activator with a model. STILL OPEN: MSG_ONE per-client targeting (plays to all), the LOOPED_OFF-untargeted objerror, and the silent fallback when no model/activator. Channel byte-faithful; ACTIVATOR(BIT3) checked before looped flags per QC spawn order."

  - id: mapobject-target.music.target_music
    name: "target_music — triggered background-music override / map default"
    layer: [authority, presentation]
    base_refs:
      - "common/mapobjects/target/music.qc:target_music_use"
      - "common/mapobjects/target/music.qc:Net_TargetMusic / TargetMusic_Advance"
      - "common/mapobjects/target/music.qc:target_music_reset / target_music_kill / TargetMusic_RestoreGame"
    port_refs:
      - "src/XonoticGodot.Common/Gameplay/MapObjects/TargetMusic.cs:TargetMusicSetup"
      - "src/XonoticGodot.Common/Gameplay/MapObjects/TargetMusic.cs:TargetMusicUse"
      - "src/XonoticGodot.Common/Gameplay/MapObjects/TargetMusic.cs:TargetMusicReset"
      - "game/client/MusicPlayer.cs:EvaluateMusicSources"
    constants:
      - { name: "default_volume", base: "1", port: "1", match: true }
      - { name: "lifetime/fade_time/fade_rate", base: "seconds (fade 0 = instant)", port: "seconds (fade 0 -> 2s default)", match: false }
    status:
      logic: partial
      values: partial
      timing: partial
      presentation: faithful
      audio: faithful
      liveness: partial
    intended_divergence: true
    divergence_rationale: "The whole CSQC TargetMusic_Advance + TE_CSQC_TARGET_MUSIC net path is replaced by MusicPlayer.cs reading the live server entity state directly. On the listen-server (the primary play path) this is byte-for-byte the same audible result without a net round-trip; the priority stack (trigger > targeted target_music within lifetime > lifetime-0 default-slot claim > untargeted default > cdtrack) and lifetime/default-slot semantics are preserved. Fade default keeps the port's gentle 2s crossfade where QC's 0 means instant (no shipped map carries the fade keys)."
    gaps:
      - "Listen-server ONLY: a remote/dedicated client receives no target_music override (the TE_CSQC_TARGET_MUSIC net message is not ported)."
      - "target_music_use is still a GLOBAL override, not QC's per-activator (IS_REAL_CLIENT actor) + spectator (IS_SPEC && enemy==actor) override — every client hears the override. TargetMusicUse DOES guard `activator is null` + non-client, so a null/non-client relay no longer activates it, but the override is still applied globally rather than per-activator."
      - "QC fade 0 = instant; the port substitutes a 2s default crossfade."
      - "target_music_kill and TargetMusic_RestoreGame still have no analogue."
    verification:
      - { kind: code, ref: "TargetMusicUse guards `activator is null` + non-client (matches QC `if(!actor) return;`); TargetMusicReset hook assigned to e.Reset", result: pass }
      - { kind: code, ref: "MusicPlayer.cs priority eval (trigger/lifetime-target/default-slot); MusicLifetime/fade fields bound (MapObjectFieldsExtra.cs:134-136); MusicActivationTime stamped in TargetMusicUse", result: pass }
      - { kind: code, ref: "GameWorld.ResetMapObjects iterates every non-client entity and fires e.Reset?.Invoke(e); reached via ResetMap, called on Rounds.OnRoundReset + Assault round-2. So TargetMusicReset is LIVE on a round restart", result: pass }
      - { kind: test, ref: "tests/XonoticGodot.Tests/MusicSystemTests.cs", result: pass }
    confidence: medium
    notes: "Re-verified 2026-06-25: the QC null/client trigger gate is present + live; the target_music_reset hook is LIVE (ResetMapObjects dispatches e.Reset). Core priority/lifetime/default-slot eval faithful. Liveness stays `partial` only because the override remains listen-server-only / global (no net path, no per-activator routing). Registered MapObjectsRegistry.cs."

  - id: mapobject-target.music.trigger_music
    name: "trigger_music — brush-volume music override (highest priority)"
    layer: [authority, presentation]
    base_refs:
      - "common/mapobjects/target/music.qc:trigger_music_SendEntity / Ent_TriggerMusic_Think"
      - "common/mapobjects/target/music.qc:trigger_music_reset"
      - "common/mapobjects/defs.qh:START_DISABLED"
    port_refs:
      - "src/XonoticGodot.Common/Gameplay/MapObjects/TargetMusic.cs:TriggerMusicSetup"
      - "src/XonoticGodot.Common/Gameplay/MapObjects/TargetMusic.cs:TriggerMusicReset"
      - "game/client/MusicPlayer.cs:EvaluateMusicSources"
    constants:
      - { name: "default_volume", base: "1", port: "1", match: true }
      - { name: "START_DISABLED spawnflag", base: "BIT(0)=1", port: "BIT(0)=1", match: true }
      - { name: "touch_freshness_window", base: "per-frame Ent_TriggerMusic_Think while inside", port: "TouchFreshnessWindow PushLTime window", match: false }
    status:
      logic: partial
      values: faithful
      timing: partial
      presentation: faithful
      audio: faithful
      liveness: partial
    intended_divergence: true
    divergence_rationale: "Like target_music, the CSQC brush-overlap test (Ent_TriggerMusic_Think against the local view box) is replaced by a server-side Touch that stamps PushLTime, read by MusicPlayer with a freshness window. Equivalent on the listen-server."
    gaps:
      - "Listen-server ONLY (the ENT_CLIENT_TRIGGER_MUSIC net link to a remote client is not ported)."
      - "On multiple overlapping trigger_music volumes the port takes the FIRST fresh one; QC's Ent_TriggerMusic_Think uses the LAST-touched. Could pick a different track where volumes overlap (code comment acknowledges this)."
    verification:
      - { kind: code, ref: "use handler installed unconditionally (matches QC generic_netlinked_legacy_use); TriggerMusicReset assigned to e.Reset AND called at spawn for the initial START_DISABLED state; START_DISABLED bit = BIT(0)=1 per defs.qh", result: pass }
      - { kind: code, ref: "MusicPlayer.cs trigger_music branch + TriggerMusicTouch PushLTime stamp; registered MapObjectsRegistry.cs", result: pass }
      - { kind: code, ref: "GameWorld.ResetMapObjects fires e.Reset on every non-client entity, reached via ResetMap on the live round-restart path -> the round-restart TriggerMusicReset (re-apply START_DISABLED) is LIVE", result: pass }
    confidence: medium
    notes: "Re-verified 2026-06-25: unconditional use+reset install (matches QC spawnfunc), reset called at spawn to seed the active state AND re-driven on a round restart by the live ResetMapObjects e.Reset pump. Brush-overlap via touch; 'first vs last' overlap pick is a known minor divergence. Liveness stays `partial` only for the listen-server-only net limitation."

  - id: mapobject-target.voicescript.sequence
    name: "target_voicescript — scripted per-player voice-line sequence"
    layer: [authority]
    base_refs:
      - "common/mapobjects/target/voicescript.qc:target_voicescript_use"
      - "common/mapobjects/target/voicescript.qc:target_voicescript_next"
      - "common/mapobjects/target/voicescript.qc:spawnfunc(target_voicescript)"
    port_refs:
      - "src/XonoticGodot.Common/Gameplay/MapObjects/VoiceScript.cs:VoiceScriptUse"
      - "src/XonoticGodot.Common/Gameplay/MapObjects/VoiceScript.cs:Next"
      - "src/XonoticGodot.Server/GameWorld.cs:1156"
    constants:
      - { name: "nextthink_jitter", base: "voiceend + wait*(0.5+random())", port: "voiceend + Wait*(0.5+Prandom.Float())", match: true }
      - { name: "negative_dur (no extra delay)", base: "voiceend = time - dt; nextthink = voiceend", port: "same", match: true }
    status:
      logic: faithful
      values: faithful
      timing: faithful
      presentation: na
      audio: faithful
      liveness: live
    intended_divergence: false
    divergence_rationale: null
    gaps:
      - "play2 (per-player audible) maps to the shared sound facade on the Voice channel rather than a true per-client message — acceptable on the listen server but a remote client may hear another player's script."
    verification:
      - { kind: test, ref: "tests/XonoticGodot.Tests/MapObjectLongTailTests.cs (voicescript latch/advance/clear)", result: pass }
      - { kind: code, ref: "GameWorld.cs:1156 pumps VoiceScript.Next every per-client PreThink", result: pass }
    confidence: high
    notes: "Re-verified 2026-06-25 unchanged. Ordered-prefix-then-looping-random-pool token math, the cnt split on '*', and the schedule are all faithful. Live: pumped each tick."

  - id: mapobject-target.teleport_dest.spawn
    name: "info_teleport_destination / misc_teleporter_dest — teleport endpoint"
    layer: [authority]
    base_refs:
      - "common/mapobjects/misc/teleport_dest.qc:spawnfunc(info_teleport_destination)"
      - "common/mapobjects/misc/teleport_dest.qc:spawnfunc(misc_teleporter_dest)"
    port_refs:
      - "src/XonoticGodot.Common/Gameplay/MapObjects/Teleporters.cs:TeleportDestSetup"
    constants:
      - { name: "dest_z_nudge", base: "1 - mins.z - 24", port: "1 - mins.z - 24 (DestZNudge)", match: true }
    status:
      logic: faithful
      values: faithful
      timing: na
      presentation: na
      audio: na
      liveness: live
    intended_divergence: false
    divergence_rationale: null
    gaps:
      - "QC objerrors (refuses to link) a destination with no targetname; the port just registers it (a teleporter would fail to find it anyway)."
      - "The ENT_CLIENT_TELEPORT_DEST net link (so CSQC can predict the teleport) is not ported; consumed only server-side."
    verification:
      - { kind: test, ref: "tests/XonoticGodot.Tests/TeleportersTests.cs", result: pass }
      - { kind: code, ref: "both classnames registered MapObjectsRegistry.cs; mangle=angles, angles cleared", result: pass }
    confidence: high
    notes: "Re-verified 2026-06-25 unchanged. .mangle = .angles; angles cleared. Consumed by Simple_TeleportPlayer (teleporters unit)."

  - id: mapobject-target.position.spawn
    name: "target_position / info_notnull — passive jumppad/impulse destination marker"
    layer: [authority]
    base_refs:
      - "common/mapobjects/trigger/jumppads.qc:spawnfunc(target_position)"
      - "common/mapobjects/trigger/jumppads.qc:spawnfunc(info_notnull)"
      - "common/mapobjects/trigger/jumppads.qc:target_push_init"
    port_refs:
      - "src/XonoticGodot.Common/Gameplay/MapObjects/Jumppads.cs:TargetPushSetup"
    constants: []
    status:
      logic: faithful
      values: na
      timing: na
      presentation: na
      audio: na
      liveness: live
    intended_divergence: false
    divergence_rationale: null
    gaps:
      - "Behavioral detail (target_push_init: SOLID_NOT passive point carrying origin/angles) is owned by the jumppad/trigger_push unit; this row only records that both classnames are registered and reached as arc destinations. Not independently re-traced for arc consumption here."
    verification:
      - { kind: code, ref: "both classnames registered MapObjectsRegistry.cs -> Jumppads.TargetPushSetup; consumed by trigger_push/trigger_impulse (jumppad unit)", result: pass }
    confidence: medium
    notes: "Re-verified 2026-06-25 unchanged. Full arc-destination behavior belongs to the jumppad/trigger unit; recorded here as a registered passive marker."
