unit: turret-plasma
category: turret
base_ref_rev: "v0.8.6-1779-g863cd3e84"
last_audited: "2026-06-22"
summary:
  feature_count: 13
  worst_gaps:
    - "No turret presentation: head never visibly aims/spins, no team color, no waypoint sprite/healthbar, no damage smoke/sparks, no death gibs — renders as a static base.md3 block (no head sub-entity, no CSQC turret net layer)."
    - "TFL_FIRECHECK_AFF (don't fire when the predicted impact hits a teammate) is still not modeled in the fire gate."
    - "TFL_DMG_HEADSHAKE jitter is not applied (no rendered/networked head bone to perturb)."
    - "Unit balance is still baked into C# consts rather than read from g_turrets_unit_plasma_* — the values MATCH the cfg, but server cvar overrides / turret_scale_* / g_turrets_reloadcvars still have no effect (the framework cvars targetscan/aimidle/nofire ARE read)."
features:
  - id: turret-plasma.spawn.mapplacement
    name: "Map placement spawnfunc + initialize (turret_plasma / turret_plasma_dual)"
    layer: [authority]
    base_refs:
      - "common/turrets/turret/plasma.qc:spawnfunc(turret_plasma)"
      - "common/turrets/sv_turrets.qc:turret_initialize"
    port_refs:
      - "src/XonoticGodot.Common/Gameplay/Turrets/TurretSpawnFuncs.cs:Plasma"
      - "src/XonoticGodot.Common/Gameplay/Turrets/TurretSpawn.cs:Init"
      - "src/XonoticGodot.Common/Gameplay/MapObjects/MapObjectsRegistry.cs:206"
      - "src/XonoticGodot.Server/GameWorld.cs:2158"
    constants:
      - { name: "g_turrets master switch default", base: "1", port: "1 (MasterSwitchEnabled: unset→on)", match: true }
      - { name: "team when no teamplay", base: "FLOAT_MAX", port: "float.MaxValue", match: true }
    status:
      logic: faithful
      values: faithful
      timing: faithful
      presentation: na
      audio: na
      liveness: live
    intended_divergence: false
    divergence_rationale: null
    gaps: []
    verification:
      - { kind: code, ref: "SpawnFuncs.Register(turret_plasma) -> GameWorld.SpawnMapEntities SpawnFuncs.TrySpawn at GameWorld.cs:2158; TurretSpawnFuncs.Spawn gates on MasterSwitchEnabled(g_turrets) and arms e.Think=def.Think each frame", result: pass }
    confidence: high
    notes: "Hand-placed map turret; not in any default gametype. g_turrets 0 deletes the edict (MasterSwitchEnabled distinguishes absent from present-and-0), matching QC. Unchanged by the fix-up pass."

  - id: turret-plasma.identity.model_hitbox
    name: "Identity, hitbox, model, health, weapon binding"
    layer: [authority, presentation]
    base_refs:
      - "common/turrets/turret/plasma.qh:PlasmaTurret"
      - "common/turrets/turret/plasma_dual.qh:DualPlasmaTurret"
    port_refs:
      - "src/XonoticGodot.Common/Gameplay/Turrets/PlasmaTurret.cs:PlasmaTurret"
      - "src/XonoticGodot.Common/Gameplay/Turrets/PlasmaDualTurret.cs:PlasmaDualTurret"
    constants:
      - { name: "mins/maxs", base: "'-32 -32 0' / '32 32 64'", port: "(-32,-32,0)/(32,32,64)", match: true }
      - { name: "body model", base: "models/turrets/base.md3", port: "models/turrets/base.md3", match: true }
      - { name: "head_model", base: "plasma.md3 / plasmad.md3", port: "none (no head entity)", match: false }
      - { name: "health", base: "500", port: "500", match: true }
      - { name: "spawnflags", base: "SPLASH|MEDPROJ|PLAYER", port: "modeled via aimSplash + Select players", match: true }
    status:
      logic: faithful
      values: faithful
      timing: na
      presentation: partial
      audio: na
      liveness: live
    intended_divergence: false
    divergence_rationale: null
    gaps:
      - "head_model (plasma.md3/plasmad.md3) is never loaded or rendered — there is no tur_head sub-entity, only the base body."
    verification:
      - { kind: value, ref: "plasma.qh ATTRIBs vs PlasmaTurret ctor/Spawn", result: pass }
    confidence: high
    notes: "Dual is a PlasmaTurret subclass per QC CLASS(DualPlasmaTurret, PlasmaTurret). Unchanged by the fix-up pass."

  - id: turret-plasma.balance.cvars
    name: "Balance constants (damage/refire/range/ammo/aim) from g_turrets_unit_plasma_*"
    layer: [authority]
    base_refs:
      - "turrets.cfg:g_turrets_unit_plasma_*"
      - "common/turrets/sv_turrets.qc:load_unit_settings"
      - "common/turrets/sv_turrets.qc:turret_initparams"
    port_refs:
      - "src/XonoticGodot.Common/Gameplay/Turrets/PlasmaTurret.cs:ShotDamage"
      - "src/XonoticGodot.Common/Gameplay/Turrets/PlasmaTurret.cs:MakeParams"
      - "src/XonoticGodot.Common/Gameplay/Turrets/PlasmaDualTurret.cs:Think"
    constants:
      - { name: "shot_dmg", base: "80", port: "80", match: true }
      - { name: "shot_refire (plasma/dual)", base: "0.6 / 0.35", port: "0.6 / 0.35", match: true }
      - { name: "shot_radius", base: "150", port: "150", match: true }
      - { name: "shot_speed", base: "2000", port: "2000", match: true }
      - { name: "shot_spread (plasma/dual)", base: "0.015 / 0.015", port: "0.015 / 0.015", match: true }
      - { name: "shot_force", base: "100", port: "100", match: true }
      - { name: "target_range (plasma/dual)", base: "3500 / 3000", port: "3500 / 3000", match: true }
      - { name: "target_range_min (plasma/dual)", base: "200 / 80", port: "200 / 80", match: true }
      - { name: "target_range_optimal (plasma/dual)", base: "500 / 1000", port: "500 / 1000", match: true }
      - { name: "ammo_max / ammo_recharge", base: "640 / 40", port: "640 / 40", match: true }
      - { name: "aim_speed (plasma/dual)", base: "200 / 100", port: "200 / 100", match: true }
      - { name: "aim_maxpitch / aim_maxrot", base: "30 / 360", port: "30 / 360", match: true }
      - { name: "aim_firetolerance_dist (plasma/dual)", base: "120 / 200", port: "120 / 200", match: true }
      - { name: "select biases range/same/angle (plasma)", base: "0.5/0.01/0.25", port: "0.5/0.01/0.25", match: true }
      - { name: "select biases range/same/angle (dual)", base: "0.2/0.4/0.4", port: "0.2/0.4/0.4", match: true }
      - { name: "track accel_pitch/accel_rot/blendrate (plasma=dual)", base: "0.5/0.7/0.2", port: "0.5/0.7/0.2", match: true }
    status:
      logic: faithful
      values: faithful
      timing: na
      presentation: na
      audio: na
      liveness: live
    intended_divergence: false
    divergence_rationale: null
    gaps:
      - "Unit balance is still hardcoded C# consts, not read from g_turrets_unit_plasma_* — the values now match the cfg, but server cvar overrides / turret_scale_* map controls / g_turrets_reloadcvars have no effect."
    verification:
      - { kind: value, ref: "turrets.cfg g_turrets_unit_plasma[_dual]_* (lines 323-388) vs PlasmaTurret/PlasmaDualTurret consts + MakeParams/Think defaults — all 16 verified equal", result: pass }
    confidence: high
    notes: "Values are bit-faithful to turrets.cfg (verified line-by-line incl. dual's 0.2/0.4/0.4 biases and the shared 0.5/0.7/0.2 track tuning). Residual: per-unit values are baked C# consts, not cvar-driven. Unchanged by the fix-up pass."

  - id: turret-plasma.setup.flags
    name: "tr_setup flag configuration (ammo/damage/firecheck/aim flags)"
    layer: [authority]
    base_refs:
      - "common/turrets/turret/plasma.qc:PlasmaTurret.tr_setup"
    port_refs:
      - "src/XonoticGodot.Common/Gameplay/Turrets/PlasmaTurret.cs:MakeParams"
      - "src/XonoticGodot.Common/Gameplay/Turrets/PlasmaTurret.cs:Select"
    constants:
      - { name: "aim_flags", base: "LEAD | SHOTTIMECOMPENSATE | SPLASH", port: "lead+shotTimeCompensate+aimSplash (zPredict:false)", match: true }
      - { name: "ammo_flags", base: "ENERGY|RECHARGE|RECIEVE", port: "ammo regen always on (no flag model)", match: true }
      - { name: "firecheck_flags AFF", base: "TFL_FIRECHECK_AFF (avoid FF impact)", port: "not modeled (no impactent FF pre-check)", match: false }
      - { name: "damage_flags HEADSHAKE", base: "TFL_DMG_HEADSHAKE", port: "not modeled (no server head bone; jitter omitted in EventDamage)", match: false }
    status:
      logic: faithful
      values: partial
      timing: na
      presentation: na
      audio: na
      liveness: live
    intended_divergence: false
    divergence_rationale: null
    gaps:
      - "TFL_FIRECHECK_AFF (don't fire when the predicted impact would hit a teammate) is still not modeled in the fire gate."
      - "TFL_DMG_HEADSHAKE jitter is not applied (TurretAI.EventDamage omits the head-angle shake; the port has no rendered head bone to perturb)."
    verification:
      - { kind: code, ref: "plasma.qc tr_setup aim_flags (LEAD|SHOTTIMECOMPENSATE|SPLASH) vs MakeParams(lead:true, shotTimeCompensate:true, aimSplash:true, zPredict:false) — ZPredict matches Base (does NOT set TFL_AIM_ZPREDICT)", result: pass }
      - { kind: code, ref: "no TFL_FIRECHECK_AFF in RunCombat fire gate; no HEADSHAKE jitter in EventDamage (sv_turrets.qc:227-233 has it; port omits)", result: fail }
    confidence: high
    notes: "ZPredict matches Base aim_flags. AFF firecheck and HEADSHAKE jitter remain unmodeled. Unchanged by the fix-up pass."

  - id: turret-plasma.think.pipeline
    name: "Per-frame think: ammo regen, acquire/scan delays, aim, track, fire gate"
    layer: [authority]
    base_refs:
      - "common/turrets/sv_turrets.qc:turret_think"
      - "common/turrets/sv_turrets.qc:turret_firecheck"
    port_refs:
      - "src/XonoticGodot.Common/Gameplay/Turrets/PlasmaTurret.cs:Think"
      - "src/XonoticGodot.Common/Gameplay/Turrets/TurretAI.cs:RunCombat"
      - "src/XonoticGodot.Common/Gameplay/Turrets/TurretAI.cs:Cvar"
      - "src/XonoticGodot.Common/Gameplay/Turrets/TurretSpawnFuncs.cs:46"
    constants:
      - { name: "targetscan_mindelay", base: "0.1", port: "Cvar g_turrets_targetscan_mindelay (default 0.1)", match: true }
      - { name: "targetscan_maxdelay", base: "1", port: "Cvar g_turrets_targetscan_maxdelay (default 1)", match: true }
      - { name: "aimidle_delay", base: "5", port: "Cvar g_turrets_aimidle_delay (default 5)", match: true }
      - { name: "g_turrets_nofire master fire-disable", base: "0", port: "Cvar g_turrets_nofire (default 0, honored in Fire)", match: true }
      - { name: "ammo regen", base: "ammo += ammo_recharge*frametime (cap)", port: "same", match: true }
    status:
      logic: faithful
      values: faithful
      timing: faithful
      presentation: na
      audio: na
      liveness: live
    intended_divergence: false
    divergence_rationale: null
    gaps: []
    verification:
      - { kind: code, ref: "RunCombat reads minDelay=Cvar(g_turrets_targetscan_mindelay,0.1)/maxDelay=Cvar(g_turrets_targetscan_maxdelay,1); Cvar() falls back to Base default when cvar unseeded (empty GetString), so the effective value matches Base either way", result: pass }
      - { kind: code, ref: "Fire honors g_turrets_nofire (master fire-disable short-circuit); RunCombat reads aimidle via Cvar; e.Think wired (TurretSpawnFuncs:46) driven by SimulationLoop SV_RunThink", result: pass }
    confidence: high
    notes: "Cvar() helper falls back to the Base default when the cvar is unseeded, so the effective value matches Base whether or not turrets.cfg is loaded. Unchanged by the fix-up pass."

  - id: turret-plasma.target.select_score
    name: "Target validation + bias-weighted scoring + selection"
    layer: [authority]
    base_refs:
      - "common/turrets/sv_turrets.qc:turret_validate_target"
      - "common/turrets/sv_turrets.qc:turret_targetscore_generic"
      - "common/turrets/sv_turrets.qc:turret_select_target"
    port_refs:
      - "src/XonoticGodot.Common/Gameplay/Turrets/TurretAI.cs:ValidTarget"
      - "src/XonoticGodot.Common/Gameplay/Turrets/TurretAI.cs:ScoreTarget"
      - "src/XonoticGodot.Common/Gameplay/Turrets/TurretAI.cs:SelectTarget"
    constants:
      - { name: "alpha cloak cull", base: "alpha != 0 && alpha <= 0.3", port: "same", match: true }
      - { name: "out-of-range score collapse", base: "score *= 0.001", port: "score *= 0.001f", match: true }
      - { name: "grapplinghook reject", base: "yes", port: "yes", match: true }
      - { name: "aim_firetolerance_dist in ValidTarget LOS gate", base: "aim_firetolerance_dist", port: "p.FireToleranceDist (was hardcoded 64)", match: true }
    status:
      logic: faithful
      values: faithful
      timing: na
      presentation: na
      audio: na
      liveness: live
    intended_divergence: false
    divergence_rationale: null
    gaps: []
    verification:
      - { kind: test, ref: "tests/XonoticGodot.Tests/TurretTargetingTests.cs (ValidTarget gates, bias formula, sticky enemy, range/LOS)", result: pass }
      - { kind: code, ref: "ScoreTarget formula matches turret_targetscore_generic (dScore*rangeBias + aScore*angleBias + mScore*missileBias + pScore*playerBias, *0.001 OOR collapse); per-unit biases fed in; ValidTarget LOS gate uses p.FireToleranceDist", result: pass }
    confidence: high
    notes: "Best-covered turret feature; formula faithful and fed the real per-unit biases. ValidTarget threads aim_firetolerance_dist into the LOS firetolerance test. Unchanged by the fix-up pass."

  - id: turret-plasma.aim.lead_splash
    name: "Aim prediction: lead + shot-traveltime compensate + ground splash"
    layer: [authority]
    base_refs:
      - "common/turrets/sv_turrets.qc:turret_aim_generic"
    port_refs:
      - "src/XonoticGodot.Common/Gameplay/Turrets/TurretAI.cs:AimPoint"
    constants:
      - { name: "lead time", base: "max(attack_finished-time,0)+sys_frametime", port: "max(AttackFinished-now,0)+frameTime", match: true }
      - { name: "impact_time", base: "vlen(target-shotorg)/shot_speed", port: "same", match: true }
      - { name: "splash downtrace", base: "+0 0 32 to -0 0 64, MOVE_WORLDONLY", port: "same", match: true }
      - { name: "z-predict (plasma)", base: "off (no TFL_AIM_ZPREDICT)", port: "off (zPredict:false)", match: true }
    status:
      logic: faithful
      values: faithful
      timing: faithful
      presentation: na
      audio: na
      liveness: live
    intended_divergence: false
    divergence_rationale: null
    gaps: []
    verification:
      - { kind: test, ref: "tests/XonoticGodot.Tests/TurretAimTrackTests.cs (aim/track)", result: pass }
      - { kind: code, ref: "AimPoint z-predict branch gated by p.ZPredict, which plasma sets false; lead + traveltime + splash downtrace faithful", result: pass }
    confidence: high
    notes: "Lead + traveltime + splash + (no) z-predict all faithful. Unchanged by the fix-up pass."

  - id: turret-plasma.track.fluid_inertia
    name: "Head track motor (fluid-inertia) with per-axis pitch/rot clamps"
    layer: [authority, presentation]
    base_refs:
      - "common/turrets/sv_turrets.qc:turret_track"
    port_refs:
      - "src/XonoticGodot.Common/Gameplay/Turrets/TurretAI.cs:Track"
    constants:
      - { name: "track_type", base: "3 (FLUIDINERTIA)", port: "TrackFluidInertia (3)", match: true }
      - { name: "track_accel_pitch/rot", base: "0.5 / 0.7", port: "0.5 / 0.7 (passed via MakeParams/Think)", match: true }
      - { name: "track_blendrate", base: "0.2", port: "0.2 (passed)", match: true }
    status:
      logic: faithful
      values: faithful
      timing: faithful
      presentation: missing
      audio: na
      liveness: live
    intended_divergence: false
    divergence_rationale: null
    gaps:
      - "Head angles live only in server TurretState and are never networked/rendered — the head never visibly aims (presentation)."
    verification:
      - { kind: value, ref: "turrets.cfg track_accel_rot 0.7 / track_blendrate 0.2 vs MakeParams defaults (TrackAccelRot 0.7, TrackBlendRate 0.2) consumed in TurretAI.Track lines 497-499", result: pass }
    confidence: high
    notes: "Motor tuning faithful and consumed in Track. The head remains invisible client-side (presentation still missing). Unchanged by the fix-up pass."

  - id: turret-plasma.attack.plasma_ball
    name: "tr_attack: plasma ball projectile (PlasmaAttack.wr_think) + radius damage"
    layer: [authority]
    base_refs:
      - "common/turrets/turret/plasma.qc:PlasmaTurret.tr_attack"
      - "common/turrets/turret/plasma_weapon.qc:PlasmaAttack.wr_think"
      - "common/turrets/sv_turrets.qc:turret_projectile"
    port_refs:
      - "src/XonoticGodot.Common/Gameplay/Turrets/PlasmaTurret.cs:Attack"
      - "src/XonoticGodot.Common/Gameplay/Turrets/PlasmaDualTurret.cs:Attack"
      - "src/XonoticGodot.Common/Gameplay/Turrets/TurretSpawn.cs:Projectile"
    constants:
      - { name: "projectile movetype", base: "MOVETYPE_FLYMISSILE", port: "MoveType.FlyMissile", match: true }
      - { name: "lifetime", base: "nextthink = time + 9", port: "+9", match: true }
      - { name: "radius damage", base: "shot_dmg 80, edge 0, radius 150, force 100, DEATH_TURRET_PLASMA", port: "same", match: true }
      - { name: "spread cone (plasma/dual)", base: "0.015 / 0.015", port: "0.015 / 0.015 (DualShotSpread 0.015)", match: true }
    status:
      logic: faithful
      values: faithful
      timing: faithful
      presentation: missing
      audio: faithful
      liveness: live
    intended_divergence: false
    divergence_rationale: null
    gaps:
      - "Client-render only: PROJECTILE_ELECTRO_BEAM CSQC trail + EFFECT_BLASTER_MUZZLEFLASH are not emitted (NOTE comment)."
    verification:
      - { kind: code, ref: "PlasmaTurret.Attack -> TurretSpawn.Projectile vs turret_projectile + RadiusDamage; dual re-emits its own ball with DualShotSpread 0.015 (own Attack override, not base.Attack)", result: pass }
    confidence: high
    notes: "Fire sound weapons/hagar_fire.wav matches Base SND hagar_fire. Both single and dual use 0.015 spread. Two-barrel alternation + head-frame are presentation-only. Unchanged by the fix-up pass."

  - id: turret-plasma.attack.instagib_railgun
    name: "tr_attack instagib override: instant railgun beam"
    layer: [authority]
    base_refs:
      - "common/turrets/turret/plasma.qc:PlasmaTurret.tr_attack (MUTATOR_IS_ENABLED mutator_instagib)"
    port_refs:
      - "src/XonoticGodot.Common/Gameplay/Turrets/PlasmaTurret.cs:Attack (InstagibEnabled branch)"
      - "src/XonoticGodot.Common/Gameplay/Turrets/TurretCombat.cs:FireBullet"
    constants:
      - { name: "rail damage", base: "10000000000 (1e10)", port: "10000000000 (InstagibRailDamage)", match: true }
      - { name: "rail force", base: "800", port: "800 (InstagibRailForce)", match: true }
      - { name: "headshot_notify", base: "false", port: "n/a (FireBullet no headshot arg)", match: true }
      - { name: "deathtype", base: "DEATH_TURRET_PLASMA", port: "DeathTypes.TurretPlasma", match: true }
      - { name: "instagib gate", base: "MUTATOR_IS_ENABLED(mutator_instagib)", port: "g_instagib != 0", match: true }
      - { name: "fire sound", base: "none (FireRailgunBullet passes empty snd_fire)", port: "none", match: true }
    status:
      logic: faithful
      values: faithful
      timing: faithful
      presentation: missing
      audio: faithful
      liveness: live
    intended_divergence: false
    divergence_rationale: null
    gaps:
      - "Client-render only: EFFECT_VORTEX_MUZZLEFLASH + the team-coloured EFFECT_VAPORIZER_BEAM hit beam are not emitted."
    verification:
      - { kind: code, ref: "plasma.qc tr_attack FireRailgunBullet(it,...,10000000000,false,800,...,DEATH_TURRET_PLASMA) vs Attack -> FireBullet(damage:InstagibRailDamage=1e10, force:InstagibRailForce=800, DeathTypes.TurretPlasma); no fire sound", result: pass }
    confidence: high
    notes: "Damage 1e10 (instakill) / force 800 / no fire sound, matching Base. Beam/muzzle FX remain client-render gaps. Unchanged by the fix-up pass."

  - id: turret-plasma.headspin.frame_anim
    name: "Head spin frame animation after each shot (tr_think frame 1->5)"
    layer: [authority, presentation]
    base_refs:
      - "common/turrets/turret/plasma.qc:PlasmaTurret.tr_think"
      - "common/turrets/turret/plasma.qc:PlasmaTurret.tr_attack (frame=1 trigger)"
    port_refs: []
    constants:
      - { name: "frame cycle", base: "if frame!=0 ++frame; if frame>5 frame=0; attack sets frame=1", port: "none", match: false }
    status:
      logic: missing
      values: missing
      timing: missing
      presentation: missing
      audio: na
      liveness: na
    intended_divergence: false
    divergence_rationale: null
    gaps:
      - "No turret head/spin presentation: there is no server head bone (tur_head.frame) and no CSQC turret net layer — the head never visibly aims or spins; renders as a static base.md3 block."
    verification:
      - { kind: code, ref: "no tr_think head-frame / TNSF_ANIM equivalent in port (PlasmaDualTurret.Attack documents ++tur_head.frame as un-ported cross-file work)", result: fail }
    confidence: high
    notes: "The 5-frame head spin per shot is still unimplemented; subsumed by the broader no-presentation gap. Unchanged by the fix-up pass."

  - id: turret-plasma.damage.gate_retaliate
    name: "turret_damage: inactive invuln, friendly-fire scale, headshake (no retaliate)"
    layer: [authority]
    base_refs:
      - "common/turrets/sv_turrets.qc:turret_damage"
    port_refs:
      - "src/XonoticGodot.Common/Gameplay/Turrets/TurretAI.cs:Damage"
      - "src/XonoticGodot.Common/Gameplay/Turrets/TurretAI.cs:EventDamage"
      - "src/XonoticGodot.Common/Gameplay/Turrets/TurretSpawn.cs:Init (e.GtEventDamage = TurretAI.EventDamage)"
      - "src/XonoticGodot.Common/Gameplay/Damage/DamageSystem.cs:331 (EventDamage routes non-player GtEventDamage)"
    constants:
      - { name: "dead/inactive reject", base: "deadflag==DEAD or !active => return (no dmg)", port: "same (Damage returns 0)", match: true }
      - { name: "friendly fire scale", base: "*g_friendlyfire (0 => no dmg)", port: "*g_friendlyfire (<=0 => return 0)", match: true }
      - { name: "retaliate (TFL_DMG_RETALIATE)", base: "flag set but NEVER read — no this.enemy=attacker", port: "no retaliation (matches Base)", match: true }
      - { name: "MOVE shove", base: "velocity += vforce if TUR_FLAG_MOVE", port: "Velocity += force if st.Movable", match: true }
      - { name: "headshake jitter", base: "head angles += (random-0.5)*damage", port: "not modeled (no server head bone)", match: false }
    status:
      logic: faithful
      values: faithful
      timing: na
      presentation: na
      audio: na
      liveness: live
    intended_divergence: false
    divergence_rationale: null
    gaps:
      - "TFL_DMG_HEADSHAKE jitter (head angles += (random-0.5)*damage) is not applied — the port has no rendered/networked head bone to perturb."
    verification:
      - { kind: code, ref: "Base turret_damage (sv_turrets.qc:207-251) has NO this.enemy=attacker; port Damage no longer retaliates (fix-up removed the port-added `turret.Enemy = attacker`), now matching Base", result: pass }
      - { kind: code, ref: "TurretSpawn.Init sets e.GtEventDamage = TurretAI.EventDamage; DamageSystem.EventDamage (l.331) routes non-player edicts with GtEventDamage to it (same seam monsters/onslaught use); EventDamage gates via Damage(), then TakeResource(Health), fires Combat.Death when health<=0", result: pass }
      - { kind: test, ref: "tests/XonoticGodot.Tests/TurretLifecycleTests.cs", result: pass }
    confidence: high
    notes: "FIX: the fix-up pass removed the port-added retaliation (`turret.Enemy = attacker`). Base sets TFL_DMG_RETALIATE on machinegun defaults but NO code reads it — a Base turret never turns to face its attacker; acquisition is scan/score-only. The earlier baseline wrongly recorded retaliate as a faithful match. Inactive invuln, FF scaling, MOVE shove all faithful and LIVE via the GtEventDamage shim. HEADSHAKE jitter is the one unmodeled sub-feature (presentation-coupled)."

  - id: turret-plasma.death.die_respawn
    name: "Death + respawn (turret_die / turret_hide / turret_respawn)"
    layer: [authority, presentation]
    base_refs:
      - "common/turrets/sv_turrets.qc:turret_die"
      - "common/turrets/sv_turrets.qc:turret_respawn"
      - "common/turrets/cl_turrets.qc:turret_die (gibs)"
    port_refs:
      - "src/XonoticGodot.Common/Gameplay/Turrets/TurretAI.cs:Die"
      - "src/XonoticGodot.Common/Gameplay/Turrets/TurretAI.cs:Respawn"
      - "src/XonoticGodot.Common/Gameplay/Turrets/TurretAI.cs:OnAnyDeath"
    constants:
      - { name: "respawntime", base: "60", port: "60", match: true }
      - { name: "death blast", base: "COMMENTED OUT (no blast, sv_turrets.qc:182)", port: "no blast (RadiusDamage removed)", match: true }
      - { name: "death gibs/explosion (client)", base: "base-gib1..4 + head gib + ROCKET_EXPLODE", port: "none", match: false }
    status:
      logic: faithful
      values: faithful
      timing: faithful
      presentation: missing
      audio: missing
      liveness: live
    intended_divergence: false
    divergence_rationale: null
    gaps:
      - "No death presentation: no client gibs (base-gib1..4 + head gib) and no EFFECT_ROCKET_EXPLODE / SND_ROCKET_IMPACT on death (the standard non-NORESPAWN turret path)."
    verification:
      - { kind: code, ref: "Base turret_die RadiusDamage is commented (//RadiusDamage..., sv_turrets.qc:182); port TurretAI.Die now has the blast REMOVED (lines 753-754 comment 'No death blast') — now matches Base", result: pass }
      - { kind: test, ref: "tests/XonoticGodot.Tests/TurretLifecycleTests.cs Die/Respawn", result: pass }
    confidence: high
    notes: "FIX: the fix-up pass removed the port-added ammo-scaled RadiusDamage death blast — Base has it commented out, so a dying turret now deals no area damage in either, matching Base. Death flows live via EventDamage -> Combat.Death -> OnAnyDeath -> Die; respawn scheduled via NextThink. Remaining divergence is the missing client gib/explosion FX (presentation/audio only). Logic+values now faithful (was partial/partial in the baseline)."
