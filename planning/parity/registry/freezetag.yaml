unit: freezetag
category: gametype
base_ref_rev: "v0.8.6-1779-g863cd3e84"
last_audited: "2026-06-22"
summary:
  feature_count: 22
  worst_gaps:
    - "Frozen players take full damage (no frag_damage=0 / g_frozen_force) — HP=1 frozen body is re-killed by chip damage. Still verified: DamageSystem.cs subtract path (~466) applies health damage with no frozen guard; DamageEntityState.cs:90 documents the deliberate 'still takes lethal damage' choice. NO FT DamageCalculate handler exists."
    - "revive_auto_reducible STILL missing: shooting a frozen enemy never speeds their thaw (core FT tactic gone)."
    - "FT start loadout (g_ft_start_*) STILL never applied (ApplyStartItems has no live caller — only doc-comment mentions)."
    - "No ice (MDL_ICE) entity model, no Frozen/Reviving waypoint sprites, no freeze/revive notifications, no full-screen frozen overlay — AND the crosshair thaw ring is STILL unfed (server mirrors ReviveProgress to NetEntity but no client copies it into CrosshairPanel.ReviveProgress). Only the icy player-model tint is live frozen-state feedback."
    - "Wave-2 CLOSED: time-to-score revive model (revive_time_to_score=1.5 / speed_t2s=0.25 / no-clear), auto_progress ramp + revive_auto gate, post-revive spawnshield, and the FT-predicate round flow (EnableRounds now uses ft.CheckTeams/CheckWinner + thaw-everyone onRoundStart + FT cvars, FT.Handler synced) are now implemented and live."
features:
  - id: freezetag.mode.registration
    name: "Mode registration: teams, team-spawns, score/lead limits, FT scoring fields"
    layer: [authority]
    base_refs:
      - "common/gametypes/gametype/freezetag/sv_freezetag.qh:REGISTER_MUTATOR(ft)"
      - "common/gametypes/gametype/freezetag/sv_freezetag.qc:freezetag_Initialize"
      - "common/gametypes/gametype/freezetag/freezetag.qh:INIT(FreezeTag)"
    port_refs:
      - "src/XonoticGodot.Common/Gameplay/GameTypes/FreezeTag.cs:Activate"
      - "src/XonoticGodot.Common/Gameplay/GameTypes/FreezeTag.cs:TeamCount"
    constants:
      - { name: "default teams", base: "2 (1..4 via g_freezetag_teams/_override, BITS(bound(2,n,4)))", port: "2 (clamp 2..4)", match: true }
      - { name: "mapinfo defaults", base: "timelimit=20 pointlimit=10 teams=2 leadlimit=6", port: "fraglimit→10 round-limit", match: false }
      - { name: "g_freezetag_team_spawns", base: "1", port: "RequestsTeamSpawns reads cvar (default 1)", match: true }
      - { name: "team primary slot", base: "ST_FT_ROUNDS 'rounds'", port: "TeamSlotSecondary 'rounds'", match: true }
      - { name: "player columns", base: "SP_SCORE primary + FREEZETAG_REVIVALS", port: "Score primary + FREEZETAG_REVIVALS", match: true }
    status:
      logic: faithful
      values: partial
      timing: na
      presentation: na
      audio: na
      liveness: live
    intended_divergence: false
    divergence_rationale: null
    gaps:
      - "Match limit semantics differ: Base reads mapinfo pointlimit (10) / leadlimit (6) via g_freezetag_point_limit=-1 passthrough; port keys off fraglimit/leadlimit. Live default round target (10) coincidentally matches, leadlimit 6 likely not honored."
    verification:
      - { kind: code-read, ref: "FreezeTag.Activate GameScores setup; freezetag_Initialize", result: pass }
    confidence: high
    notes: "Scoring-field shape (team rounds + revivals column + SP_SCORE primary) is faithful. Limit cvar plumbing still diverges (unchanged from baseline)."

  - id: freezetag.freeze.apply
    name: "Freeze a fragged player (ice, HP=1, status effect, score matrix)"
    layer: [authority]
    base_refs:
      - "common/gametypes/gametype/freezetag/sv_freezetag.qc:freezetag_Freeze"
      - "common/gametypes/gametype/freezetag/sv_freezetag.qc:freezetag_Add_Score"
      - "common/gametypes/gametype/freezetag/sv_freezetag.qc:MUTATOR_HOOKFUNCTION(ft, PlayerDies)"
    port_refs:
      - "src/XonoticGodot.Common/Gameplay/GameTypes/FreezeTag.cs:Freeze"
      - "src/XonoticGodot.Common/Gameplay/GameTypes/FreezeTag.cs:OnDeath"
    constants:
      - { name: "freeze HP", base: "RES_HEALTH = 1", port: "SetResource(Health, 1)", match: true }
      - { name: "score self-freeze", base: "victim -1", port: "victim -1", match: true }
      - { name: "score teammate freeze", base: "attacker -1, victim -1", port: "attacker -1, victim -1", match: true }
      - { name: "score enemy freeze", base: "attacker +1, victim -1", port: "attacker +1, victim -1", match: true }
      - { name: "score rules-freeze (null/non-player)", base: "no score", port: "no score", match: true }
    status:
      logic: faithful
      values: faithful
      timing: faithful
      presentation: partial
      audio: na
      liveness: live
    intended_divergence: false
    divergence_rationale: null
    gaps:
      - "No ice entity / waypoint / notification side effects fire (covered by their own rows). The state transition + score are faithful."
    verification:
      - { kind: code-read, ref: "OnDeath wired via Combat.Death.Add in Activate; Freeze applies Frozen status effect + arms auto-thaw timer (now gated on ReviveAuto && maxtime>0) + score matrix", result: pass }
    confidence: high
    notes: "Applies StatusEffectsCatalog.Frozen (the shared status effect the weapon/movement locks read). Freeze now correctly arms FrozenTimeout only when revive_auto on AND maxtime>0 (Wave-2 refinement)."

  - id: freezetag.freeze.weaponlock
    name: "Frozen player cannot fire weapons"
    layer: [shared]
    base_refs:
      - "common/gametypes/gametype/freezetag/sv_freezetag.qc:MUTATOR_HOOKFUNCTION(ft, LockWeapon)"
      - "common/physics/player.qc:(weapon ready gate PHYS_FROZEN)"
    port_refs:
      - "src/XonoticGodot.Common/Gameplay/Weapons/WeaponFireDriver.cs:WeaponLocked"
      - "src/XonoticGodot.Common/Gameplay/Weapons/WeaponFiring.cs:IsFrozen"
    constants: []
    status:
      logic: faithful
      values: na
      timing: faithful
      presentation: na
      audio: na
      liveness: live
    intended_divergence: false
    divergence_rationale: null
    gaps: []
    verification:
      - { kind: code-read, ref: "WeaponLocked returns true on FrozenStat!=0 OR Frozen status effect; Freeze sets the status effect", result: pass }
    confidence: high
    notes: "Reads the shared Frozen status effect, so it triggers for FT freezes. Unchanged from baseline."

  - id: freezetag.freeze.movementlock
    name: "Frozen player cannot move or jump"
    layer: [shared]
    base_refs:
      - "common/physics/player.qc:PM_check_frozen"
      - "common/physics/player.qc:PlayerJump (PHYS_FROZEN no-jump)"
    port_refs:
      - "src/XonoticGodot.Common/Physics/PlayerPhysics.cs:IsFrozen"
    constants:
      - { name: "frozen move", base: "movement = '0 0 0' (dodging-frozen sub-case allows ±2)", port: "move = Vector3.Zero (dodging-frozen omitted)", match: false }
    status:
      logic: faithful
      values: faithful
      timing: faithful
      presentation: na
      audio: na
      liveness: live
    intended_divergence: false
    divergence_rationale: null
    gaps:
      - "sv_dodging_frozen sub-case (±2 movement for dodge direction) not modeled — only matters with the dodging mutator + sv_dodging_frozen 1 (both default off)."
    verification:
      - { kind: code-read, ref: "PlayerPhysics zeros move when IsFrozen; blocks jump", result: pass }
    confidence: high
    notes: "Core 'encased in ice can't move' behavior is faithful at stock defaults. Unchanged from baseline."

  - id: freezetag.unfreeze
    name: "Unfreeze: clear ice state, restore health, re-enable"
    layer: [authority]
    base_refs:
      - "common/gametypes/gametype/freezetag/sv_freezetag.qc:freezetag_Unfreeze"
    port_refs:
      - "src/XonoticGodot.Common/Gameplay/GameTypes/FreezeTag.cs:Unfreeze"
    constants:
      - { name: "restored HP", base: "start_health (player) / max_health", port: "StartHealth", match: true }
      - { name: "spawnshield on revive", base: "spawnshieldtime = time + g_freezetag_revive_spawnshield (1s)", port: "SpawnShieldExpire = time + ReviveSpawnShield (read by damage pipeline)", match: true }
      - { name: "pauseregen", base: "pauseregen_finished = time + g_balance_pause_health_regen", port: "not set", match: false }
    status:
      logic: faithful
      values: partial
      timing: faithful
      presentation: partial
      audio: na
      liveness: live
    intended_divergence: false
    divergence_rationale: null
    gaps:
      - "Does not set pauseregen_finished after revive (minor regen-timing divergence) — still open."
      - "Does not delete an ice entity or kill a waypoint sprite (those are not spawned in the first place)."
    verification:
      - { kind: code-read, ref: "Unfreeze clears status effect + restores StartHealth + now sets SpawnShieldExpire when spawnShieldSeconds>0", result: pass }
    confidence: high
    notes: "Wave-2 added the spawn-shield-on-revive (was a separate baseline gap, now closed here). Still does not set pauseregen."

  - id: freezetag.revive.manual
    name: "Manual revive by nearby teammate (range geometry + progress)"
    layer: [authority]
    base_refs:
      - "common/gametypes/gametype/freezetag/sv_freezetag.qc:MUTATOR_HOOKFUNCTION(ft, PlayerPreThink)"
      - "common/gametypes/gametype/freezetag/sv_freezetag.qc:IN_REVIVING_RANGE"
    port_refs:
      - "src/XonoticGodot.Common/Gameplay/GameTypes/FreezeTag.cs:ReviveTick"
      - "src/XonoticGodot.Common/Gameplay/GameTypes/FreezeTag.cs:InRevivingRange"
      - "src/XonoticGodot.Server/GameWorld.cs:DriveGametypeFrame"
    constants:
      - { name: "g_freezetag_revive_extra_size", base: "100 qu bbox expand", port: "100 (DefaultReviveExtraSize)", match: true }
      - { name: "revive speed used", base: "revive_speed_t2s=0.25 (t2s on, default) / revive_speed=0.4*(1-base) (t2s off)", port: "t2s ON → ReviveSpeedT2s 0.25; t2s OFF → ReviveSpeed 0.4*(1-base)", match: true }
      - { name: "clearspeed", base: "1.6 (only decays when revive_time_to_score<=0)", port: "1.6; with t2s ON progress is NOT cleared, with t2s OFF decays at clearspeed*(1-base)", match: true }
    status:
      logic: faithful
      values: faithful
      timing: faithful
      presentation: na
      audio: na
      liveness: live
    intended_divergence: false
    divergence_rationale: null
    gaps: []
    verification:
      - { kind: code-read, ref: "ReviveTick now branches on t2s: uses ReviveSpeedT2s when ReviveTimeToScore>0 (the default), holds progress (no clear) out of range, decays at clearspeed only with t2s off; called per-frame from DriveGametypeFrame after SetRoster", result: pass }
    confidence: high
    notes: "FIXED from baseline partial/partial/partial. The Base-default time-to-score path is now the live path; range geometry + speed + decay all match Base."

  - id: freezetag.revive.score
    name: "Revive scoring (reviver +1, FREEZETAG_REVIVALS +1; revived player return)"
    layer: [authority]
    base_refs:
      - "common/gametypes/gametype/freezetag/sv_freezetag.qc:MUTATOR_HOOKFUNCTION(ft, PlayerPreThink)"
    port_refs:
      - "src/XonoticGodot.Common/Gameplay/GameTypes/FreezeTag.cs:ReviveTick"
      - "src/XonoticGodot.Common/Gameplay/GameTypes/FreezeTag.cs:AddRevival"
      - "src/XonoticGodot.Common/Gameplay/GameTypes/FreezeTag.cs:TryRevive"
    constants:
      - { name: "revival credit", base: "FREEZETAG_REVIVALS +1 per nearby reviver", port: "FREEZETAG_REVIVALS +1 per nearby reviver", match: true }
      - { name: "SCORE on revive", base: "t2s ON (default): SCORE accrues over time (+1 per 1.5s); t2s OFF: +1 per reviver on completion", port: "same — t2s ON accrues via _reviveTime, t2s OFF awards +1 on completion", match: true }
      - { name: "nade bonus", base: "nades_GiveBonus(g_nades_bonus_score_low)", port: "not awarded", match: false }
    status:
      logic: faithful
      values: partial
      timing: faithful
      presentation: na
      audio: na
      liveness: live
    intended_divergence: false
    divergence_rationale: null
    gaps:
      - "No nade revival bonus (g_nades_bonus_score_low) on a completed revive — minor, requires the nades mutator."
    verification:
      - { kind: code-read, ref: "ReviveTick accrues per-reviver SCORE over time with t2s on (+1 per revive_time_to_score), awards +1 on completion only with t2s off; AddRevival credits FREEZETAG_REVIVALS to each nearby reviver", result: pass }
    confidence: high
    notes: "FIXED from baseline partial. SCORE timing/path now matches Base (time-accrual under the default t2s). Only the nade bonus remains."

  - id: freezetag.revive.time_to_score
    name: "Time-to-score revive model (revive_time_to_score / speed_t2s)"
    layer: [authority]
    base_refs:
      - "common/gametypes/gametype/freezetag/sv_freezetag.qc:MUTATOR_HOOKFUNCTION(ft, PlayerPreThink)"
      - "common/gametypes/gametype/freezetag/sv_freezetag.qh:autocvar_g_freezetag_revive_time_to_score"
    port_refs:
      - "src/XonoticGodot.Common/Gameplay/GameTypes/FreezeTag.cs:ReviveTick"
      - "src/XonoticGodot.Common/Gameplay/GameTypes/FreezeTag.cs:TryRevive"
      - "src/XonoticGodot.Common/Gameplay/GameTypes/FreezeTag.cs:ReviveTimeToScore"
      - "src/XonoticGodot.Common/Gameplay/GameTypes/FreezeTag.cs:ReviveSpeedT2s"
    constants:
      - { name: "g_freezetag_revive_time_to_score", base: "1.5 (ON by default)", port: "DefaultReviveTimeToScore 1.5", match: true }
      - { name: "g_freezetag_revive_speed_t2s", base: "0.25", port: "DefaultReviveSpeedT2s 0.25", match: true }
      - { name: "progress not cleared (t2s on)", base: "REVIVE_PROGRESS sticks at max(manual, base_progress) out of range", port: "same — held, never cleared when t2s on; higher manual progress shortens auto-thaw remaining", match: true }
    status:
      logic: faithful
      values: faithful
      timing: faithful
      presentation: na
      audio: na
      liveness: live
    intended_divergence: false
    divergence_rationale: null
    gaps: []
    verification:
      - { kind: code-read, ref: "ReviveTick implements the full t2s branch: speed_t2s 0.25, per-reviver time accrual (+1 SCORE per 1.5s), no-clear hold of progress; ReviveTimeToScore/ReviveSpeedT2s cvar-backed with correct defaults", result: pass }
    confidence: high
    notes: "FIXED from baseline missing/na. This is the Base default model and is now the live path."

  - id: freezetag.revive.auto_timeout
    name: "Auto-thaw after g_freezetag_frozen_maxtime"
    layer: [authority]
    base_refs:
      - "common/gametypes/gametype/freezetag/sv_freezetag.qc:freezetag_Freeze"
      - "common/gametypes/gametype/freezetag/sv_freezetag.qc:MUTATOR_HOOKFUNCTION(ft, PlayerPreThink)"
    port_refs:
      - "src/XonoticGodot.Common/Gameplay/GameTypes/FreezeTag.cs:Freeze"
      - "src/XonoticGodot.Common/Gameplay/GameTypes/FreezeTag.cs:ReviveTick"
      - "src/XonoticGodot.Common/Gameplay/GameTypes/FreezeTag.cs:ReviveAuto"
      - "src/XonoticGodot.Common/Gameplay/GameTypes/FreezeTag.cs:ReviveAutoProgress"
    constants:
      - { name: "g_freezetag_frozen_maxtime", base: "60 s", port: "live cvar; FrozenMaxtime fallback 60", match: true }
      - { name: "g_freezetag_revive_auto", base: "1", port: "ReviveAuto cvar (default true); arms FrozenTimeout only when on", match: true }
      - { name: "g_freezetag_revive_auto_progress", base: "1 (continuous base_progress ramp)", port: "ReviveAutoProgress cvar (default true); base_progress = clamp(1-(timeout-now)/maxtime)", match: true }
    status:
      logic: faithful
      values: faithful
      timing: faithful
      presentation: na
      audio: na
      liveness: live
    intended_divergence: false
    divergence_rationale: null
    gaps: []
    verification:
      - { kind: code-read, ref: "Freeze arms FrozenTimeout iff (ReviveAuto && maxtime>0); ReviveTick computes a continuous baseProgress ramp when autoProgress on, treats n=0 + elapsed timeout as an n=-1 auto-revive of last resort that credits nobody", result: pass }
    confidence: high
    notes: "FIXED from baseline partial. The continuous base_progress ramp and the revive_auto on/off gate are now both implemented and live."

  - id: freezetag.revive.auto_reducible
    name: "Hitting a frozen enemy speeds their auto-thaw (revive_auto_reducible)"
    layer: [authority]
    base_refs:
      - "common/gametypes/gametype/freezetag/sv_freezetag.qc:MUTATOR_HOOKFUNCTION(ft, Damage_Calculate)"
    port_refs: []
    constants:
      - { name: "g_freezetag_revive_auto_reducible", base: "1 (enemy hits reduce; -1 also teammates)", port: "none", match: false }
      - { name: "revive_auto_reducible_forcefactor", base: "0.01", port: "none", match: false }
      - { name: "revive_auto_reducible_maxforce", base: "400", port: "none", match: false }
    status:
      logic: missing
      values: missing
      timing: missing
      presentation: na
      audio: na
      liveness: na
    intended_divergence: false
    divergence_rationale: null
    gaps:
      - "Shooting a frozen enemy still does nothing to their thaw timer; the core 'avoid damaging frozen enemies' tactic is absent. No FT DamageCalculate handler exists, and no revive_auto_reducible reference in src."
    verification:
      - { kind: code-read, ref: "grep revive_auto_reducible across src → no matches; FreezeTag registers no DamageCalculate handler", result: fail }
    confidence: high
    notes: "STILL OPEN — unchanged from baseline."

  - id: freezetag.damage.frozen_invuln
    name: "Frozen players take 0 health damage + g_frozen_force knockback scaling"
    layer: [authority]
    base_refs:
      - "common/gametypes/gametype/freezetag/sv_freezetag.qc:MUTATOR_HOOKFUNCTION(ft, Damage_Calculate)"
    port_refs:
      - "src/XonoticGodot.Common/Gameplay/Damage/DamageSystem.cs"
    constants:
      - { name: "frag_damage on frozen", base: "0 (non-NEEDKILL/teamchange)", port: "full damage applied", match: false }
      - { name: "g_frozen_force", base: "0.6 force multiplier", port: "none", match: false }
    status:
      logic: missing
      values: missing
      timing: na
      presentation: na
      audio: na
      liveness: na
    intended_divergence: false
    divergence_rationale: null
    gaps:
      - "Frozen players (HP=1) are STILL NOT made damage-immune; chip damage re-kills the frozen body. DamageSystem only special-cases frozen for teamplay-nullify bypass (IsFrozenStat at ~177) and pain-sound suppression (~471) — the subtract path (~466) applies health damage with no frozen guard, and DamageEntityState.cs:90 documents this as a deliberate 'still takes lethal damage to allow ice-shatter' stance (no FT DamageCalculate handler ever zeroes it)."
      - "Knockback on frozen players is full, not scaled by g_frozen_force=0.6."
    verification:
      - { kind: code-read, ref: "DamageSystem subtract path (~466) TakeResource(Health, take) with no frozen guard; Wave-1 DamageSystem diff only added splash-deathtype tagging + a doc comment that NAMES 'FreezeTag frozen-immunity' as a hook responsibility but no such hook is registered", result: fail }
    confidence: high
    notes: "STILL OPEN, most impactful gameplay-correctness gap. The Wave-1 comment claims this rides the Damage_Calculate hook, but no FT handler subscribes — it is unimplemented, not merely unwired."

  - id: freezetag.damage.softkill_void
    name: "Frozen void/lava soft-kill teleport + g_frozen_damage_trigger; fall/nade revive"
    layer: [authority]
    base_refs:
      - "common/gametypes/gametype/freezetag/sv_freezetag.qc:MUTATOR_HOOKFUNCTION(ft, Damage_Calculate)"
      - "common/gametypes/gametype/freezetag/sv_freezetag.qc:MUTATOR_HOOKFUNCTION(ft, PlayerDies)"
    port_refs:
      - "src/XonoticGodot.Common/Gameplay/Nades/NadesMutator.cs:OnDamageCalculate"
    constants:
      - { name: "g_frozen_damage_trigger", base: "1 (frozen die in void/lava)", port: "none", match: false }
      - { name: "g_frozen_revive_falldamage", base: "0 (off)", port: "none", match: false }
      - { name: "g_frozen_revive_falldamage_health", base: "40", port: "none", match: false }
      - { name: "g_freezetag_revive_nade / _nade_health", base: "1 / 40", port: "cvar read but defaults to 0 in fallback; only suppresses nade damage on a frozen self-hit, NO unfreeze/revive-health", match: false }
    status:
      logic: missing
      values: missing
      timing: na
      presentation: missing
      audio: missing
      liveness: partial
    intended_divergence: false
    divergence_rationale: null
    gaps:
      - "No soft-kill relocate path (g_frozen_damage_trigger) — the NEEDKILL soft-kill (restore HP=1+armor, relocate, no weapon reset) in PlayerDies is absent."
      - "No fall-damage revive (g_frozen_revive_falldamage)."
      - "Nade self-revive is only a damage-suppression stub: NadesMutator.OnDamageCalculate zeroes damage/force for a frozen player's own DEATH_NADE within 0.1s, but explicitly does NOT unfreeze or restore revive-health (comment: 'FreezeTag owns freezetag_Unfreeze'), and its cvar fallback defaults g_freezetag_revive_nade to 0 (Base default 1). So no actual self-revive occurs."
    verification:
      - { kind: code-read, ref: "no g_frozen_revive_falldamage / damage_trigger references; NadesMutator.OnDamageCalculate suppresses damage only, no unfreeze", result: fail }
    confidence: high
    notes: "Mostly STILL OPEN. A partial nade-damage-suppression seam appeared (Wave-2) but the revive itself is unwired and default-off; the soft-kill teleport + fall-damage revive remain entirely missing."

  - id: freezetag.revive.spawnshield
    name: "Post-revive spawn shield (g_freezetag_revive_spawnshield)"
    layer: [authority]
    base_refs:
      - "common/gametypes/gametype/freezetag/sv_freezetag.qc:MUTATOR_HOOKFUNCTION(ft, PlayerPreThink)"
    port_refs:
      - "src/XonoticGodot.Common/Gameplay/GameTypes/FreezeTag.cs:Unfreeze"
      - "src/XonoticGodot.Common/Gameplay/GameTypes/FreezeTag.cs:ReviveSpawnShield"
      - "src/XonoticGodot.Common/Gameplay/Damage/DamageSystem.cs:HasSpawnShield"
    constants:
      - { name: "g_freezetag_revive_spawnshield", base: "1 s", port: "DefaultReviveSpawnShield 1f", match: true }
    status:
      logic: faithful
      values: faithful
      timing: faithful
      presentation: na
      audio: na
      liveness: live
    intended_divergence: false
    divergence_rationale: null
    gaps: []
    verification:
      - { kind: code-read, ref: "Unfreeze sets targ.SpawnShieldExpire = now + ReviveSpawnShield when spawnShieldSeconds>0; TryRevive/ReviveTick pass ReviveSpawnShield; DamageSystem.HasSpawnShield reads SpawnShieldExpire and gates damage", result: pass }
    confidence: high
    notes: "FIXED from baseline missing. A freshly revived player now gets a 1s spawn shield that the live damage pipeline honors."

  - id: freezetag.round.flow
    name: "Round handler flow (warmup/countdown/round/end-delay, ROUND_OVER tie)"
    layer: [authority]
    base_refs:
      - "common/gametypes/gametype/freezetag/sv_freezetag.qc:freezetag_CheckTeams"
      - "common/gametypes/gametype/freezetag/sv_freezetag.qc:freezetag_CheckWinner"
      - "server/round_handler.qc:round_handler_Think"
    port_refs:
      - "src/XonoticGodot.Server/RoundHandler.cs:Think"
      - "src/XonoticGodot.Server/GameWorld.cs:EnableRounds"
      - "src/XonoticGodot.Server/GameWorld.cs:MirrorRoundTiming"
      - "src/XonoticGodot.Common/Gameplay/GameTypes/FreezeTag.cs:CheckTeams"
      - "src/XonoticGodot.Common/Gameplay/GameTypes/FreezeTag.cs:CheckWinner"
    constants:
      - { name: "g_freezetag_warmup", base: "10 s", port: "live cvar; EnableRounds countdown = g_freezetag_warmup (fallback 5)", match: true }
      - { name: "g_freezetag_round_timelimit", base: "360 s", port: "live cvar (fallback 180)", match: true }
      - { name: "g_freezetag_round_enddelay", base: "0 s", port: "live cvar (fallback 5)", match: true }
      - { name: "countdown", base: "5 s", port: "5 s (RoundHandler default)", match: true }
    status:
      logic: faithful
      values: partial
      timing: faithful
      presentation: na
      audio: na
      liveness: live
    intended_divergence: false
    divergence_rationale: null
    gaps:
      - "No round-timelimit-expiry tie path (Base CENTER/INFO_ROUND_OVER → thaw all + tie) — when the round timer elapses with both teams still alive, the FT CheckWinner does not resolve a tie/round-over; the live RoundHandler's own timeout handling drives reset but the ROUND_OVER notification + explicit tie are absent."
      - "C# fallback timings (warmup 5/timelimit 180/enddelay 5) still differ from Base (10/360/0); masked live because gametypes-server.cfg ships."
    verification:
      - { kind: code-read, ref: "GameWorld FreezeTag Activate now calls EnableRounds(ft.CheckTeams, ()=>ft.CheckWinner()!=0, onRoundStart=thaw-all, endDelay/countdown/roundTimeLimit from FT cvars); _roundSync mirrors timing into ft.Handler via MirrorRoundTiming; DriveGametypeFrame FT case calls ReviveTick only (NOT CheckRound) to avoid double-award", result: pass }
    confidence: high
    notes: "FIXED from baseline partial. The live server RoundHandler now uses FT's own CheckTeams/CheckWinner (frozen players count as eliminated) + a thaw-everyone onRoundStart + FT's cvars, and ft.Handler is kept in sync (no longer dead). CheckWinner awards ST_FT_ROUNDS itself. Only the round-timelimit tie path remains open."

  - id: freezetag.win.roundlimit
    name: "Match win on round/lead limit (ST_FT_ROUNDS)"
    layer: [authority]
    base_refs:
      - "common/gametypes/gametype/freezetag/sv_freezetag.qc:freezetag_CheckWinner"
      - "common/gametypes/gametype/freezetag/sv_freezetag.qh:GameRules_limit_score/limit_lead"
    port_refs:
      - "src/XonoticGodot.Common/Gameplay/GameTypes/FreezeTag.cs:CheckWinner"
      - "src/XonoticGodot.Common/Gameplay/GameTypes/FreezeTag.cs:CheckRound"
      - "src/XonoticGodot.Common/Gameplay/GameTypes/FreezeTag.cs:UpdateLeaderAndCheckLimit"
    constants:
      - { name: "round win score", base: "TeamScore_AddToTeam(winner, ST_FT_ROUNDS, +1)", port: "AddToTeam(winner, TeamSlotSecondary, +1)", match: true }
      - { name: "round limit default", base: "mapinfo pointlimit 10", port: "fraglimit else 10", match: true }
      - { name: "lead limit default", base: "mapinfo leadlimit 6", port: "leadlimit cvar else 0", match: false }
    status:
      logic: faithful
      values: partial
      timing: faithful
      presentation: na
      audio: na
      liveness: live
    intended_divergence: false
    divergence_rationale: null
    gaps:
      - "Lead-limit default (Base 6 via mapinfo) likely not honored (port reads leadlimit cvar, default 0 = off)."
    verification:
      - { kind: code-read, ref: "CheckWinner (the live canRoundEnd predicate) adds team rounds + UpdateLeaderAndCheckLimit sets MatchEnded; round resolution now runs on the live RoundHandler path, not a duplicate per-frame CheckRound", result: pass }
    confidence: high
    notes: "Round resolution is now driven via the live RoundHandler polling CheckWinner (which awards ST_FT_ROUNDS). The duplicate per-frame CheckRound is no longer called in DriveGametypeFrame (avoiding double-award). Lead-limit default still diverges."

  - id: freezetag.startitems
    name: "FT start loadout (g_ft_start_health/armor/ammo)"
    layer: [authority]
    base_refs:
      - "common/gametypes/gametype/freezetag/sv_freezetag.qc:MUTATOR_HOOKFUNCTION(ft, SetStartItems)"
    port_refs:
      - "src/XonoticGodot.Common/Gameplay/GameTypes/FreezeTag.cs:ApplyStartItems"
    constants:
      - { name: "g_ft_start_health", base: "100", port: "100 (DefaultStartHealth)", match: true }
      - { name: "g_ft_start_armor", base: "100", port: "100", match: true }
      - { name: "g_ft_start_ammo (shells/nails/rockets/cells/fuel)", base: "60/320/160/180/0", port: "60/320/160/180/0", match: true }
    status:
      logic: faithful
      values: faithful
      timing: na
      presentation: na
      audio: na
      liveness: dead
    intended_divergence: false
    divergence_rationale: null
    gaps:
      - "ApplyStartItems STILL has NO live caller (only doc-comment references in FreezeTag.cs + ClanArena.cs; no spawn/PutPlayerInServer call site) — FT players do not actually receive the FT-specific 100/100 + ammo loadout."
    verification:
      - { kind: code-read, ref: "grep ApplyStartItems across src/game → only the definition + doc-comments, no invocation", result: fail }
    confidence: high
    notes: "STILL OPEN — unchanged from baseline. Method body is faithful; it is simply never invoked."

  - id: freezetag.eliminated.net
    name: "Networked alive counts + eliminated set (mod icons, scoreboard grey-out)"
    layer: [authority, presentation]
    base_refs:
      - "common/gametypes/gametype/freezetag/sv_freezetag.qc:freezetag_count_alive_players"
      - "common/gametypes/gametype/freezetag/sv_freezetag.qc:freezetag_isEliminated"
    port_refs:
      - "src/XonoticGodot.Net/GametypeStatusBlock.cs:Capture"
      - "src/XonoticGodot.Common/Gameplay/GameTypes/FreezeTag.cs:AliveCount"
      - "src/XonoticGodot.Common/Gameplay/GameTypes/FreezeTag.cs:IsEliminated"
      - "game/hud/ModIconsPanel.cs:DrawCaStyle"
    constants:
      - { name: "alive stats", base: "STAT(REDALIVE..PINKALIVE)", port: "4-byte alive counts red,blue,yellow,pink", match: true }
      - { name: "eliminated predicate", base: "frozen || dead", port: "IsEliminated = IsDead || IsFrozen", match: true }
    status:
      logic: faithful
      values: faithful
      timing: faithful
      presentation: faithful
      audio: na
      liveness: live
    intended_divergence: false
    divergence_rationale: null
    gaps: []
    verification:
      - { kind: code-read, ref: "GametypeStatusBlock FreezeTag case writes alive counts + ft.IsEliminated id list; ModIconsPanel FreezeTag mode draws CA-style grid", result: pass }
    confidence: high
    notes: "FT reuses the CA mod-icon drawer with its own layout cvar, matching HUD_Mod_FreezeTag. Unchanged from baseline (faithful)."

  - id: freezetag.presentation.ice_model
    name: "Ice entity model over frozen players (MDL_ICE, 20 frames, team color)"
    layer: [presentation]
    base_refs:
      - "common/gametypes/gametype/freezetag/sv_freezetag.qc:freezetag_Freeze (ice entity)"
      - "common/gametypes/gametype/freezetag/sv_freezetag.qc:freezetag_Ice_Think"
    port_refs:
      - "game/client/ClientWorld.cs:FrozenColormod"
    constants:
      - { name: "ice model", base: "MDL_ICE, frame = floor(random()*21)", port: "none (only a blue model tint)", match: false }
      - { name: "ice tint", base: "colormod/glowmod = Team_ColorRGB(team)", port: "FrozenColormod on player model", match: false }
    status:
      logic: missing
      values: missing
      timing: na
      presentation: partial
      audio: na
      liveness: partial
    intended_divergence: false
    divergence_rationale: null
    gaps:
      - "No distinct ice block entity (random 1-of-20 frame, team-colored, following the player at origin-16) — frozen players only get an icy-blue colormod on their normal model."
    verification:
      - { kind: code-read, ref: "ClientWorld applies FrozenColormod when HasStatusEffect(Frozen); no MDL_ICE entity spawn (grep MDL_ICE → no matches)", result: pass }
    confidence: high
    notes: "STILL OPEN — unchanged from baseline. Tint conveys frozen-ness but the signature ice cube is absent."

  - id: freezetag.presentation.waypoints
    name: "Frozen / Reviving waypoint sprites over frozen players"
    layer: [presentation]
    base_refs:
      - "common/gametypes/gametype/freezetag/sv_freezetag.qc:freezetag_Freeze (WP_Frozen)"
      - "common/gametypes/gametype/freezetag/sv_freezetag.qc:MUTATOR_HOOKFUNCTION(ft, PlayerPreThink) (WP_Reviving/health bar)"
    port_refs:
      - "src/XonoticGodot.Common/Gameplay/Waypoints/Waypoints.cs"
    constants:
      - { name: "WP offset", base: "'0 0 64'", port: "n/a (never spawned)", match: false }
      - { name: "WP health bar", base: "REVIVE_PROGRESS 0..1", port: "n/a", match: false }
    status:
      logic: missing
      values: na
      timing: na
      presentation: stub
      audio: na
      liveness: dead
    intended_divergence: false
    divergence_rationale: null
    gaps:
      - "Waypoints catalog defines 'Frozen' (Frozen! / FrozenCol) and 'Reviving' (Orange/WP_REVIVING_COLOR) sprite types but nothing spawns them for a frozen player — no overhead/radar marker, no thaw-progress health bar."
    verification:
      - { kind: code-read, ref: "Wave-2 added 'Frozen'/'Reviving' catalog entries to Waypoints.cs; grep shows no FT spawn site references them", result: pass }
    confidence: high
    notes: "STILL OPEN. The Wave-2 change only added catalog entries (sprite types); no spawn/attach to a frozen player exists, so they remain dead."

  - id: freezetag.presentation.notifications
    name: "Freeze/revive/self/auto-revive/spawn-late notifications (+ sounds)"
    layer: [presentation, audio]
    base_refs:
      - "common/gametypes/gametype/freezetag/sv_freezetag.qc:(Send_Notification INFO/CENTER_FREEZETAG_*)"
    port_refs:
      - "src/XonoticGodot.Common/Gameplay/Notifications/NotificationsList.cs"
    constants: []
    status:
      logic: missing
      values: na
      timing: na
      presentation: stub
      audio: stub
      liveness: dead
    intended_divergence: false
    divergence_rationale: null
    gaps:
      - "INFO/CENTER_FREEZETAG_FREEZE/REVIVE/REVIVE_SELF/REVIVED/AUTO_REVIVED/SELF/SPAWN_LATE are declared in NotificationsList but never sent on the live path → no kill-feed lines, no center prints, and none of their notification sounds for any FT event."
    verification:
      - { kind: code-read, ref: "grep FREEZETAG_* sends across src/game → only the declarations in NotificationsList.cs, no Send sites in FreezeTag/GameWorld", result: fail }
    confidence: high
    notes: "STILL OPEN — unchanged from baseline."

  - id: freezetag.presentation.overlay_eventchase
    name: "Full-screen frozen overlay tint + cl_eventchase_frozen cam + damage HUD"
    layer: [presentation]
    base_refs:
      - "common/gametypes/gametype/freezetag/cl_freezetag.qc:MUTATOR_HOOKFUNCTION(cl_ft, HUD_Draw_overlay)"
      - "common/gametypes/gametype/freezetag/cl_freezetag.qc:MUTATOR_HOOKFUNCTION(cl_ft, WantEventchase)"
      - "common/gametypes/gametype/freezetag/cl_freezetag.qc:MUTATOR_HOOKFUNCTION(cl_ft, HUD_Damage_show)"
    port_refs:
      - "game/hud/CrosshairPanel.cs:ReviveProgress"
    constants:
      - { name: "overlay color", base: "'0.25 0.90 1' warming with REVIVE_PROGRESS", port: "none", match: false }
      - { name: "overlay alpha", base: "0.3 + 0.7*(1 - max(0, REVIVE_PROGRESS*4-3))", port: "none", match: false }
      - { name: "cl_eventchase_frozen", base: "third-person cam while frozen", port: "none", match: false }
      - { name: "crosshair revive ring", base: "REVIVE_PROGRESS ring", port: "CrosshairPanel.ReviveProgress draws a ring, but the property is STILL never assigned on the client", match: false }
    status:
      logic: missing
      values: missing
      timing: na
      presentation: stub
      audio: na
      liveness: dead
    intended_divergence: false
    divergence_rationale: null
    gaps:
      - "No full-screen icy-blue overlay while frozen (with the thaw-progress alpha fade and color warming)."
      - "No cl_eventchase_frozen third-person camera while frozen."
      - "The crosshair thaw ring is STILL dead: the server now mirrors FrozenState.ReviveProgress onto Player.ReviveProgress (ReviveTick ~493) and ships it (ServerNet.cs:1665 → NetEntityState.ReviveProgress → decoded NetEntity.ReviveProgress), but NOTHING on the client copies the decoded value into CrosshairPanel.ReviveProgress. The only '.ReviveProgress =' write in game/ is the server-side encode at ServerNet.cs:1665; the property has no client assignment site, so the ring never draws."
    verification:
      - { kind: code-read, ref: "grep '\\.ReviveProgress' across game/ → only ServerNet.cs:1665 (server encode). No client copy into CrosshairPanel.ReviveProgress; overlay/eventchase have no port code", result: fail }
    confidence: high
    notes: "STILL OPEN. Wave-2 added the server-side ReviveProgress mirror/encode but the final wire→HUD hop is still missing, so there is NO live frozen-state HUD feedback beyond the player-model tint."

  - id: freezetag.bots
    name: "Bot freeing / offense roles + frozen-target gating"
    layer: [authority]
    base_refs:
      - "common/gametypes/gametype/freezetag/sv_freezetag.qc:havocbot_role_ft_offense"
      - "common/gametypes/gametype/freezetag/sv_freezetag.qc:havocbot_role_ft_freeing"
      - "common/gametypes/gametype/freezetag/sv_freezetag.qc:MUTATOR_HOOKFUNCTION(ft, HavocBot_ChooseRole/Bot_ForbidAttack)"
    port_refs: []
    constants:
      - { name: "role timeout", base: "time + 10..20 s (random)", port: "none", match: false }
      - { name: "free-player rating", base: "offense 9000 / freeing 20000", port: "none", match: false }
    status:
      logic: missing
      values: missing
      timing: missing
      presentation: na
      audio: na
      liveness: na
    intended_divergence: false
    divergence_rationale: null
    gaps:
      - "Bots have no FT-specific freeing/offense behavior; they won't deliberately revive frozen teammates, and frozen-target attack gating (Bot_ForbidAttack) is not modeled in FT."
    verification:
      - { kind: code-read, ref: "no havocbot_role_ft / FT bot role references in src", result: fail }
    confidence: medium
    notes: "STILL OPEN — unchanged from baseline. Generic bot combat may still wander into range and incidentally revive, but no targeted role logic."
