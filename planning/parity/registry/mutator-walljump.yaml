unit: mutator-walljump
category: mutator
base_ref_rev: "v0.8.6-1779-g863cd3e84"
last_audited: "2026-06-25"
summary:
  feature_count: 9
  worst_gaps:
    - "Jump animation action (animdecide_setaction ANIMACTION_JUMP) still not triggered on a wall jump (no anim-action seam in the port; WalljumpMutator.cs:128 is a comment only). UNCHANGED — same missing server->client one-shot action channel as Dodging. Purely cosmetic."
    - "Minor: jump voice plays on SoundChannel.VoiceAuto (CH_VOICE) vs Base CH_PLAYER. The sample resolves and is audible, so this is the only residual on fx.sound. UNCHANGED."
    - "Client facade traces against a flat test floor (BootClientFacade), not the replicated server BSP, so a remote-client wall jump predicts against approximate local geometry until client-side map collision lands. Out of scope for this unit; noted on predict.shared_impulse."
features:
  - id: mutator-walljump.activation.enable
    name: "Mutator registration + g_walljump enable gate"
    layer: [authority, shared]
    base_refs:
      - "common/mutators/mutator/walljump/walljump.qc:REGISTER_MUTATOR"
      - "common/stats.qh:322"
    port_refs:
      - "src/XonoticGodot.Common/Gameplay/Mutators/WalljumpMutator.cs:IsEnabled"
      - "src/XonoticGodot.Common/Gameplay/Mutators/MutatorActivation.cs:Apply"
      - "src/XonoticGodot.Server/GameWorld.cs:602"
      - "game/net/NetGame.cs:1011"
    constants:
      - { name: "g_walljump", base: "0", port: "0 (mutators.cfg:485)", match: true }
    status:
      logic: faithful
      values: faithful
      timing: na
      presentation: na
      audio: na
      liveness: live
    intended_divergence: false
    divergence_rationale: null
    gaps: []
    verification:
      - { kind: code, ref: "MutatorActivation.Apply is now invoked on BOTH processes: server GameWorld.cs:602 AND client NetGame.cs:1011 (BootClientFacade, the non-listen-server branch reached from _Ready->StartGame else-branch at NetGame.cs:428). Both call Mutators.All -> IsEnabled (Api.Services!=null && g_walljump!=0) -> Add -> Hook() subscribes the static PlayerJump chain. This mirrors Base REGISTER_MUTATOR(walljump, true) on CSQC + autocvar on SVQC.", result: pass }
    confidence: high
    notes: "FIXED since last audit: the client now activates mutators via MutatorActivation.Apply() in BootClientFacade (NetGame.cs:1011), so the walljump PlayerJump hook is subscribed on the client process too (not just the server). Liveness server-only(partial)->live. The gate value (g_walljump!=0) matches Base. Apply() call line moved 563->602 (server)."

  - id: mutator-walljump.detect.touchwall
    name: "Nearby-wall detection (4-way tracebox, steep non-NOIMPACT surface)"
    layer: [shared]
    base_refs:
      - "common/mutators/mutator/walljump/walljump.qc:PlayerTouchWall"
    port_refs:
      - "src/XonoticGodot.Common/Gameplay/Mutators/WalljumpMutator.cs:PlayerTouchWall"
    constants:
      - { name: "dist", base: "10", port: "10", match: true }
      - { name: "max_normal", base: "0.2", port: "0.2", match: true }
      - { name: "scaler", base: "100", port: "100", match: true }
      - { name: "cast_order", base: "+fwd,-fwd,+right,-right", port: "+fwd,-fwd,+right,-right", match: true }
      - { name: "Q3SURFACEFLAG_NOIMPACT", base: "BIT(0)=0x1 skipped", port: "0x1 skipped (MutatorConstants.Q3SurfaceFlagNoImpact)", match: true }
    status:
      logic: faithful
      values: faithful
      timing: na
      presentation: na
      audio: na
      liveness: live
    intended_divergence: false
    divergence_rationale: null
    gaps: []
    verification:
      - { kind: code, ref: "side-by-side read of PlayerTouchWall (Base walljump.qc:16-34 vs WalljumpMutator.cs:139-172): same bbox tracebox start=origin, same four cast directions in order, same fraction<1 + dist + normal.z<max_normal + NOIMPACT guards, returns first qualifying plane normal (+ endpos in hitPos)", result: pass }
    confidence: high
    notes: "Faithful, unchanged. Port additionally returns the contact point (hitPos = trace EndPos) for the smoke ring; Base reads trace_endpos from the global trace state. Equivalent. Method line moved 129->139."

  - id: mutator-walljump.gate.guards
    name: "Wall-jump eligibility guards (delay, airborne, movetype, jump-tapped, unfrozen, alive)"
    layer: [shared]
    base_refs:
      - "common/mutators/mutator/walljump/walljump.qc:40-46"
    port_refs:
      - "src/XonoticGodot.Common/Gameplay/Mutators/WalljumpMutator.cs:74-84"
    constants: []
    status:
      logic: faithful
      values: faithful
      timing: faithful
      presentation: na
      audio: na
      liveness: live
    intended_divergence: false
    divergence_rationale: null
    gaps: []
    verification:
      - { kind: code, ref: "guard-by-guard read (WalljumpMutator.cs:74-84): now-LastWallJumpTime>Delay, !OnGround, movetype not in {None,Follow,Fly,Noclip}, jump not held ((Flags&JumpReleased)==0), !Frozen (StatusEffectsCatalog.Frozen), DeadState==No", result: pass }
    confidence: high
    notes: "Unchanged. IS_JUMP_HELD modeled identically (EntFlags.JumpReleased==0). Frozen uses the StatusEffects Frozen def. Delay gate matches Base time - STAT(LASTWJ) > DELAY. (Note: port checks DeadState before Frozen, vs Base order Frozen-then-Dead — same set of guards, order is immaterial since all must pass.)"

  - id: mutator-walljump.impulse.velocity
    name: "Off-wall velocity impulse (horizontal push /xy, z = jumpvel*z, crouch slam) + standard-jump composition"
    layer: [shared]
    base_refs:
      - "common/mutators/mutator/walljump/walljump.qc:52-60"
      - "common/mutators/mutator/walljump/walljump.qc:70"
      - "common/physics/player.qc:481"
    port_refs:
      - "src/XonoticGodot.Common/Gameplay/Mutators/WalljumpMutator.cs:90-98"
      - "src/XonoticGodot.Common/Gameplay/Mutators/WalljumpMutator.cs:53-58"
      - "src/XonoticGodot.Common/Physics/PlayerPhysics.cs:1045-1055"
    constants:
      - { name: "g_walljump_force", base: "300", port: "300 (mutators.cfg:487); C# field default 300", match: true }
      - { name: "g_walljump_velocity_xy_factor", base: "1.15", port: "1.15 (mutators.cfg:488); C# field default 1.05 but cfg wins", match: true }
      - { name: "g_walljump_velocity_z_factor", base: "0.5", port: "0.5 (mutators.cfg:489); C# field default 1.0 but cfg wins", match: true }
      - { name: "g_walljump_delay", base: "1", port: "1 (mutators.cfg:486); C# field default 0.3 but cfg wins", match: true }
      - { name: "sv_jumpvelocity (PHYS_JUMPVELOCITY)", base: "260", port: "260", match: true }
      - { name: "final velocity_z (normal walljump, stock cvars)", base: "260*0.5 + 260 = 390", port: "390", match: true }
      - { name: "final velocity_z (crouch walljump, stock cvars)", base: "-130 + 260 = 130", port: "130", match: true }
    status:
      logic: faithful
      values: faithful
      timing: faithful
      presentation: na
      audio: na
      liveness: live
    intended_divergence: false
    divergence_rationale: null
    gaps: []
    verification:
      - { kind: code, ref: "impulse arithmetic matches Base walljump.qc:55-60 (v.X+=n.X*force; v.X/=xy; v.Y likewise; v.Z=jumpvel*z; crouch inverts; WalljumpMutator.cs:93-97). The impulse runs OUTSIDE the !Predicted guard so it applies on both the server and the predicting client. Hook sets args.Multijump=true and returns false (WalljumpMutator.cs:131-132) so PlayerJump.Call returns false; doublejump=true skips the air-bail and vel.Z+=mjumpheight adds the standard jump on top, reproducing Base player.qc:481.", result: pass }
      - { kind: code, ref: "cvar-override-zero guard intact. Hook() reads each g_walljump* via `if (IsSet(name)) Field = GetFloat(name)` where IsSet = !IsNullOrEmpty(GetString(name)) (WalljumpMutator.cs:53-58). A shipped/registered cvar returns a non-empty string, so an admin explicitly setting any tunable to 0 (legal in Base) is honored instead of being masked by the C# field default.", result: pass }
      - { kind: code, ref: "mutators.cfg:485-489 ships the exact Base defaults (delay 1, force 300, xy 1.15, z 0.5) — verified byte-identical to Base mutators.cfg:485-489 this pass; Hook() reads them and overrides the (differing) C# field defaults at activation", result: pass }
    confidence: high
    notes: "Logic/values/timing faithful at runtime AND now predicted on the client (liveness partial->live): the velocity impulse is applied on the shared (non-Predicted-gated) path, and the client process subscribes the hook via NetGame.cs:1011. The C# field defaults (xy 1.05, z 1.0, delay 0.3) still differ from Base but are masked by the shipped cfg reads. Critical composition (multijump-grant-then-add-standard-jump) verified end-to-end. Hook line refs shifted (PlayerPhysics PlayerJump.Call now 1045-1055)."

  - id: mutator-walljump.predict.shared_impulse
    name: "Client-side prediction of the wall-jump impulse (Base runs it shared CSQC+SVQC)"
    layer: [shared, presentation]
    base_refs:
      - "common/mutators/mutator/walljump/walljump.qc:5"
      - "common/mutators/mutator/walljump/walljump.qc:36-60"
    port_refs:
      - "game/net/NetGame.cs:1011 (BootClientFacade -> MutatorActivation.Apply)"
      - "src/XonoticGodot.Common/Physics/PlayerPhysics.cs:1050 (Predicted = input.Predicted)"
      - "src/XonoticGodot.Common/Gameplay/Mutators/WalljumpMutator.cs:108 (if (!args.Predicted) SVQC-only block)"
    constants: []
    status:
      logic: faithful
      values: na
      timing: na
      presentation: faithful
      audio: na
      liveness: live
    intended_divergence: false
    divergence_rationale: null
    gaps:
      - "Residual (out-of-scope for this unit): BootClientFacade traces the predicted move against a flat test floor, not the replicated server BSP — so a remote-client wall jump predicts against approximate local geometry until client-side map collision lands. The mutator wiring itself is correct; this is a general client-collision-replication gap noted in NetGame.cs:988-994."
    verification:
      - { kind: code, ref: "The PlayerJump chain is now subscribed on the client process: NetGame.BootClientFacade (NetGame.cs:995-1012, reached from _Ready->StartGame else-branch at :428 on a non-listen-server client) calls MutatorActivation.Apply(), mirroring Base's CSQC REGISTER_MUTATOR(walljump, true). PlayerPhysics.PlayerJump sets PlayerJumpArgs.Predicted = input.Predicted (line 1050); WalljumpMutator gates ONLY the #ifdef-SVQC side-effects (smoke ring, jump voice, LASTWJ + oldvelocity stamps) behind if(!args.Predicted) (line 108), while the velocity impulse runs shared. So the predicting client applies the same impulse as the server (no double-effect, no rubberband from a missing impulse).", result: pass }
    confidence: high
    notes: "FIXED since last audit. The baseline flagged the dedicated-server-client case as mispredicting because no client-side MutatorActivation.Apply caller existed; one was added in BootClientFacade (NetGame.cs:1011), and a Predicted flag was added to PlayerJumpArgs so the shared impulse predicts while the SVQC-only side-effects stay server-networked — exactly matching Base's CSQC-shares-impulse / SVQC-emits-effects split. logic partial->faithful, presentation partial->faithful, liveness partial->live."

  - id: mutator-walljump.fx.smokering
    name: "Smoke-ring particle at the wall contact point"
    layer: [presentation]
    base_refs:
      - "common/mutators/mutator/walljump/walljump.qc:65"
    port_refs:
      - "src/XonoticGodot.Common/Gameplay/Mutators/WalljumpMutator.cs:120"
      - "src/XonoticGodot.Common/Gameplay/Effects/EffectsList.cs:228"
      - "src/XonoticGodot.Common/Gameplay/Effects/EffectEmitter.cs:Emit"
    constants:
      - { name: "EFFECT_SMOKE_RING count", base: "5 (Send_Effect EFFECT_SMOKE_RING, trace_endpos, plane_normal, 5)", port: "5 (EffectEmitter.Emit(\"SMOKE_RING\", hitPos, planeNormal, 5))", match: true }
    status:
      logic: faithful
      values: faithful
      timing: na
      presentation: faithful
      audio: na
      liveness: live
    intended_divergence: false
    divergence_rationale: null
    gaps: []
    verification:
      - { kind: code, ref: "WalljumpMutator.cs:120 calls EffectEmitter.Emit(\"SMOKE_RING\", hitPos, planeNormal, 5) INSIDE the if(!args.Predicted) block — origin=contact point (trace EndPos), velocity=plane_normal, count=5, matching Base Send_Effect(EFFECT_SMOKE_RING, trace_endpos, plane_normal, 5) under #ifdef SVQC. SMOKE_RING registered at EffectsList.cs:228 (effectinfo name \"smoke_ring\").", result: pass }
      - { kind: code, ref: "live sink: EffectEmitter.Sink is replaced by EffectNetSink on the server, so the emission networks to clients; the client decodes it and renders via Effects.Spawn. The !Predicted gate prevents the predicting client from also emitting locally (which would double the ring). Server-side emit matches Base #ifdef SVQC.", result: pass }
    confidence: high
    notes: "Faithful and live, unchanged this pass. The new if(!args.Predicted) gate correctly keeps the emit server-only (matching Base SVQC) so the networked ring is not doubled by client prediction. Emit call line moved 111->120."

  - id: mutator-walljump.fx.sound
    name: "Wall-jump jump voice (PlayerSound playersound_jump)"
    layer: [presentation]
    base_refs:
      - "common/mutators/mutator/walljump/walljump.qc:66"
    port_refs:
      - "src/XonoticGodot.Common/Gameplay/Mutators/WalljumpMutator.cs:126"
      - "src/XonoticGodot.Common/Gameplay/Sounds/SoundSystem.cs:335"
      - "src/XonoticGodot.Common/Gameplay/Sounds/GameSound.cs:195"
      - "game/loaders/PlayerSoundResolver.cs:Install"
      - "game/net/NetGame.cs:398"
    constants:
      - { name: "sound cue", base: "playersound_jump (per-model voice, CH_PLAYER, VOL_BASE, VOICETYPE_PLAYERSOUND)", port: "PlayPlayerSound(player,\"jump\",ModelSoundsFile(model,skin),VolBase,AttenNorm) — resolves via .sounds manifest to sound/player/espeak/player/jump.ogg (default pack)", match: true }
      - { name: "channel", base: "CH_PLAYER", port: "SoundChannel.VoiceAuto (CH_VOICE/-2)", match: false }
    status:
      logic: faithful
      values: faithful
      timing: faithful
      presentation: faithful
      audio: faithful
      liveness: live
    intended_divergence: false
    divergence_rationale: null
    gaps:
      - "Minor channel divergence: Base plays on CH_PLAYER; SoundSystem.PlayPlayerSound hardcodes SoundChannel.VoiceAuto (CH_VOICE, SoundSystem.cs:342). Audible and per-model-correct otherwise; channel only affects which slot the cue occupies/cuts. UNCHANGED."
    verification:
      - { kind: code, ref: "WalljumpMutator.cs:126 SoundSystem.PlayPlayerSound(player,\"jump\",Sounds.ModelSoundsFile(player.Model,(int)player.Skin),VolBase,AttenNorm) INSIDE the if(!args.Predicted) block (matching Base #ifdef SVQC). PlayPlayerSound -> resolver maps `jump -> sound/player/espeak/player/jump`; asset assets/data/xonotic-data.pk3dir/sound/player/espeak/player/jump.ogg EXISTS. Plays on SoundChannel.VoiceAuto (SoundSystem.cs:342).", result: pass }
      - { kind: code, ref: "resolver is LIVE: PlayerSoundResolver.Install(_vfs) is called in NetGame._Ready (NetGame.cs:398) when a VFS is mounted, installing Sounds.ModelSoundResolver = Resolve. ModelSounds.ParseEntries faithfully parses the id/path/variant lines (last-wins, //comments skipped), matching QC LoadPlayerSounds.", result: pass }
    confidence: high
    notes: "liveness partial->live: the cue is networked from the server (the !Predicted gate keeps it server-only, exactly matching Base's #ifdef SVQC PlayerSound — it was never meant to play in CSQC prediction). The earlier liveness=partial caveat ('does not play in remote-client prediction') was actually the FAITHFUL behavior, so it is no longer a defect. Only the CH_PLAYER vs CH_VOICE channel choice remains (values/channel match:false). Call line moved 117->126; SoundSystem channel line 236->342."

  - id: mutator-walljump.fx.anim_and_oldvel
    name: "Jump animation action + post-impulse oldvelocity stash"
    layer: [presentation, authority]
    base_refs:
      - "common/mutators/mutator/walljump/walljump.qc:64"
      - "common/mutators/mutator/walljump/walljump.qc:67"
    port_refs:
      - "src/XonoticGodot.Common/Gameplay/Mutators/WalljumpMutator.cs:116"
      - "src/XonoticGodot.Common/Gameplay/Vehicles/VehicleCommon.cs:80"
    constants: []
    status:
      logic: faithful
      values: faithful
      timing: na
      presentation: missing
      audio: na
      liveness: live
    intended_divergence: false
    divergence_rationale: null
    gaps:
      - "animdecide_setaction(player, ANIMACTION_JUMP, true) not triggered on a wall jump (no anim-action seam in the port; WalljumpMutator.cs:128 is a comment only). UNCHANGED — same missing server->client one-shot action channel noted on Dodging. Purely cosmetic (jump pose)."
    verification:
      - { kind: code, ref: "WalljumpMutator.cs:116 stashes player.OldVelocity = player.Velocity (the POST-impulse velocity) INSIDE the if(!args.Predicted) block, faithful to Base walljump.qc:64 (#ifdef SVQC; player.oldvelocity = player.velocity). Entity.OldVelocity is a real shared field declared on the vehicles partial (VehicleCommon.cs:80). LastWallJumpTime (STAT LASTWJ) is also stamped here (walljump.qc:63). animdecide ANIMACTION_JUMP still absent (line 128 is a comment).", result: pass }
    confidence: high
    notes: "oldvelocity/LASTWJ stash faithful and now correctly gated to the server leg (if(!args.Predicted)) matching Base's #ifdef SVQC. liveness partial->live: these stamps run on the live server path and are no longer dependent on process topology for correctness (the client predicts the impulse but does not re-stamp). animdecide (presentation) remains missing. Stash line moved 107->116."

  - id: mutator-walljump.menu.describe
    name: "MENUQC mutator description text (Mutators dialog)"
    layer: [presentation]
    base_refs:
      - "common/mutators/mutator/walljump/walljump.qc:77-85"
    port_refs: []
    constants: []
    status:
      logic: na
      values: na
      timing: na
      presentation: missing
      audio: na
      liveness: na
    intended_divergence: false
    divergence_rationale: null
    gaps:
      - "Base METHOD(MutatorWallJump, describe) supplies the localized blurb shown for the walljump mutator in the MENUQC Mutators dialog ('...enables jumping off walls for added mobility. To do a wall jump, tap jump when against a wall.'). No equivalent per-mutator describe text is wired in the port's mutator-selection menu. Cosmetic menu-only text; newly mapped this audit (not in the prior baseline)."
    verification:
      - { kind: code, ref: "Base walljump.qc:77-85 has the #ifdef MENUQC describe METHOD; grep of the port for the walljump description blurb finds no port_ref. Consistent with other mutators whose MENUQC describe text is unported.", result: fail }
    confidence: medium
    notes: "Newly-mapped registry coverage gap (the MENUQC describe text). Low-impact (menu description only); flagged for completeness."
