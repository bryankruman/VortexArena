unit: cl-view
category: client
base_ref_rev: "v0.8.6-1779-g863cd3e84"
last_audited: "2026-06-22"  # re-audited post Wave-5 (claude/parity-port-waves)
summary:
  feature_count: 21
  worst_gaps:
    - "Spectator third-person camera (View_SpectatorCamera / STAT(CAMERA_SPECTATOR) toggle / IMP_weapon_drop cycling) is still absent — spectating a player renders FIRST person (NetGame:4174 follow-cam returns before the chase logic). The user chase_active third-person cam IS now live (Wave-5), but chase_front (spectating-only) is hardcoded off."
    - "HUD_Damage draws a solid red ColorRect instead of the gfx/blood splatter texture, and the gentle-mode (cl_gentle) solid/randomized-colour path is not ported."
    - "GLSL blur/sharpen post-process (damage/content blur, powerup edge-sharpen, View_BlurTest) and night-vision are not implemented (engine shader cvars, no Godot analogue)."
    - "Velocity-zoom, zoom-scroll, and the zoomscript auto-zoom (fov<=59.5 -> +button9) are not implemented (menu cvars exist but no live reader)."
    - "viewloc (2.5D side-scroller View Location camera, GetViewLocationFOV) is not ported."
features:
  - id: cl-view.zoom.fov_state_machine
    name: "Zoom / FOV state machine (current_viewzoom integration, 0.75 frustum, sensitivity)"
    layer: [presentation]
    base_refs:
      - "client/view.qc:GetCurrentFov"
      - "client/view.qc:IsZooming"
      - "client/view.qc:View_UpdateFov"
    port_refs:
      - "game/client/FirstPersonView.cs:UpdateZoom"
      - "game/client/FirstPersonView.cs:ComputeVerticalFov"
      - "game/net/NetGame.cs:UpdateCamera"
      - "game/net/NetGame.cs:SeedViewCvars"
    constants:
      - { name: "fov", base: "100", port: "100 (seeded NetGame:1352)", match: true }
      - { name: "cl_zoomfactor", base: "5 (clamp 1..30 else 2.5)", port: "5 (seeded NetGame:1346; fallback 2.5)", match: true }
      - { name: "cl_zoomspeed", base: "8 (0.5..16 else 3.5)", port: "8 (seeded NetGame:1347; fallback 3.5)", match: true }
      - { name: "cl_zoomsensitivity", base: "0", port: "0 (seeded)", match: true }
      - { name: "frustum_aspect_norm", base: "0.75", port: "0.75 (FirstPersonView:219)", match: true }
      - { name: "MAX_ZOOMFACTOR", base: "30", port: "30", match: true }
    status:
      logic: faithful
      values: faithful
      timing: faithful
      presentation: faithful
      audio: na
      liveness: live
    intended_divergence: false
    divergence_rationale: null
    gaps: []
    verification:
      - { kind: code, ref: "FirstPersonView.UpdateZoom + ComputeVerticalFov match GetCurrentFov; cvars seeded NetGame.SeedViewCvars 1346-1352; UpdateCamera live on net path", result: pass }
    confidence: high
    notes: "current_viewzoom integration, current_zoomfraction, setsensitivityscale (current_viewzoom**(1-zoomsensitivity)), and the *0.75 4:3 frustum all match exactly. Velocityzoom multiplier (GetCurrentFov:574-601) is absent but defaults off (see velocity_zoom row). The 2.5/3.5 fallbacks match Base's out-of-range clamp."

  - id: cl-view.zoom.spawn_zoom
    name: "Spawn-zoom (zoom out from 1/factor on respawn)"
    layer: [presentation]
    base_refs:
      - "client/view.qc:GetCurrentFov (spawn branch ~512-520)"
      - "client/spawnpoints.qc:82-86"
    port_refs:
      - "game/client/FirstPersonView.cs:TriggerSpawnZoom"
      - "game/net/NetGame.cs:2361-2369"
    constants:
      - { name: "cl_spawnzoom", base: "1", port: "1 (seeded)", match: true }
      - { name: "cl_spawnzoom_factor", base: "2", port: "2 (seeded)", match: true }
      - { name: "cl_spawnzoom_speed", base: "1", port: "1 (seeded)", match: true }
    status:
      logic: faithful
      values: faithful
      timing: faithful
      presentation: faithful
      audio: na
      liveness: live
    intended_divergence: false
    divergence_rationale: null
    gaps: []
    verification:
      - { kind: code, ref: "Health 0->>0 edge arms TriggerSpawnZoom (NetGame:2361-2369); ease-out in UpdateZoom 170-178", result: pass }
    confidence: high
    notes: "Net path derives the (re)spawn edge from a Health 0->>0 transition since it has no Teleport; faithful intended substitution. Spawn-zoom branch sits above the normal zoom and is cancelled by manual +zoom, matching GetCurrentFov."

  - id: cl-view.zoom.weapon_zoom
    name: "Weapon zoom fold-in (Vortex secondary zoom)"
    layer: [presentation]
    base_refs:
      - "client/view.qc:IsZooming (wr_zoomdir loop)"
    port_refs:
      - "game/net/NetGame.cs:2379-2382"
    constants: []
    status:
      logic: faithful
      values: faithful
      timing: faithful
      presentation: faithful
      audio: na
      liveness: live
    intended_divergence: false
    divergence_rationale: null
    gaps: []
    verification:
      - { kind: code, ref: "activeWep.ZoomOnSecondary && Attack2Held -> _view.ZoomHeld (NetGame:2381-2382)", result: pass }
    confidence: medium
    notes: "Port folds the active weapon's secondary-fire zoom into button_zoom like IsZooming. Only the one active weapon is checked (port has a single weapon slot on this path); Base loops MAX_WEAPONSLOTS but stock single-wield matches."

  - id: cl-view.zoom.velocity_zoom
    name: "Velocity-based FOV zoom (cl_velocityzoom)"
    layer: [presentation]
    base_refs:
      - "client/view.qc:GetCurrentFov (velocityzoom block 574-601)"
    port_refs: []
    constants:
      - { name: "cl_velocityzoom_enabled", base: "0 (off by default)", port: "none", match: false }
      - { name: "cl_velocityzoom_factor", base: "0", port: "none", match: false }
      - { name: "cl_velocityzoom_type", base: "3", port: "none", match: false }
      - { name: "cl_velocityzoom_speed", base: "1000", port: "none", match: false }
      - { name: "cl_velocityzoom_time", base: "0.2", port: "none", match: false }
    status:
      logic: missing
      values: missing
      timing: missing
      presentation: missing
      audio: na
      liveness: na
    intended_divergence: false
    divergence_rationale: null
    gaps:
      - "FOV does not widen/narrow with player speed; the cl_velocityzoom_* menu controls do nothing (ComputeVerticalFov omits the velocityzoom frustum multiplier)."
    verification:
      - { kind: grep, ref: "velocityzoom only in DialogSettingsEffects.cs/DialogSettingsGame.cs (menu widgets), no view reader; FirstPersonView.ComputeVerticalFov has no velocity term", result: fail }
    confidence: high
    notes: "Off by default in stock Xonotic (cl_velocityzoom_enabled 0), so a default match is unaffected; opt-in only."

  - id: cl-view.zoom.zoom_scroll
    name: "Zoom-scroll (mousewheel adjusts zoom factor while zooming)"
    layer: [presentation]
    base_refs:
      - "client/view.qc:ZoomScroll"
      - "client/view.qc:View_InputEvent"
    port_refs: []
    constants:
      - { name: "cl_zoomscroll", base: "1", port: "none", match: false }
      - { name: "cl_zoomscroll_scale", base: "0.2", port: "none", match: false }
      - { name: "cl_zoomscroll_speed", base: "16", port: "none", match: false }
    status:
      logic: missing
      values: missing
      timing: missing
      presentation: missing
      audio: na
      liveness: na
    intended_divergence: false
    divergence_rationale: null
    gaps:
      - "Holding +zoom and scrolling the mousewheel does not change the zoom factor (no ZoomScroll / View_InputEvent reader)."
    verification:
      - { kind: grep, ref: "no ZoomScroll / cl_zoomscroll reader in src/ or game/", result: fail }
    confidence: high
    notes: "Default ON but requires the player to actively scroll while zoomed; no passive effect, so a stock match without scrolling is unaffected."

  - id: cl-view.zoom.zoomscript_buttonstatus
    name: "Zoomscript auto-zoom + zoom-button management (View_CheckButtonStatus)"
    layer: [presentation]
    base_refs:
      - "client/view.qc:View_CheckButtonStatus"
    port_refs:
      - "game/net/NetGame.cs:2378 (zoomActive gate)"
    constants:
      - { name: "cl_unpress_zoom_on_death", base: "1", port: "implicit (zoom suppressed when Health<=0)", match: true }
      - { name: "cl_unpress_zoom_on_weapon_switch", base: "1", port: "none", match: false }
      - { name: "cl_unpress_attack_on_weapon_switch", base: "1", port: "none", match: false }
      - { name: "fov_auto_zoom_threshold", base: "59.5 (+button9 when fov<=59.5)", port: "none", match: false }
    status:
      logic: partial
      values: partial
      timing: na
      presentation: na
      audio: na
      liveness: partial
    intended_divergence: false
    divergence_rationale: null
    gaps:
      - "Zoomscript auto-zoom (autocvar_fov <= 59.5 -> localcmd +button9, zoomscript_caught) is not ported."
      - "cl_unpress_zoom_on_weapon_switch / cl_unpress_attack_on_weapon_switch and the cl_hook_activeweapon localcmd are not modelled."
      - "isdemo() zoom-via-__togglezoom fallback is absent."
    verification:
      - { kind: code, ref: "NetGame:2378 zoomActive = ... && Health>0 covers unpress-zoom-on-death; no zoomscript/weapon-switch-unpress/hook handling found", result: fail }
    confidence: high
    notes: "Only the unpress-zoom-on-death behaviour is reproduced (host gates ZoomHeld off when dead). The rest is niche scripting glue. The zoomscript_caught state also feeds DrawReticle's fade (reticle row notes its absence)."

  - id: cl-view.chase.event_death_cam
    name: "Event / death chase camera (cl_eventchase_death corpse-settle pull-back)"
    layer: [presentation]
    base_refs:
      - "client/view.qc:WantEventchase"
      - "client/view.qc:View_EventChase"
    port_refs:
      - "game/client/FirstPersonView.cs:ApplyEventChase"
    constants:
      - { name: "cl_eventchase_death", base: "2 (settle then latch)", port: "2 (seeded)", match: true }
      - { name: "cl_eventchase_distance", base: "140", port: "140 (seeded)", match: true }
      - { name: "cl_eventchase_speed", base: "1.3", port: "1.3 (seeded)", match: true }
      - { name: "cl_eventchase_viewoffset", base: "'0 0 20'", port: "'0 0 20'", match: true }
      - { name: "cl_eventchase_mins", base: "'-12 -12 -8'", port: "'-12 -12 -8'", match: true }
      - { name: "cl_eventchase_maxs", base: "'12 12 8'", port: "'12 12 8'", match: true }
      - { name: "cl_eventchase_frozen", base: "0 (off by default)", port: "0 (read live via Cvar default; not seeded but default matches)", match: true }
    status:
      logic: faithful
      values: faithful
      timing: faithful
      presentation: faithful
      audio: na
      liveness: live
    intended_divergence: false
    divergence_rationale: null
    gaps:
      - "Vehicle chase (cl_eventchase_vehicle/_vehicle_distance), spectated-change chase (cl_eventchase_spectated_change), and the game-stop/intermission/viewloc chase triggers in WantEventchase are not ported (death + frozen + host-forced chase only)."
    verification:
      - { kind: code, ref: "ApplyEventChase: death==2 velocity-settle gate + latch + viewoffset ceiling trace + box-trace pull-back; frozenChase = st.IsFrozen && cl_eventchase_frozen!=0 (FirstPersonView:284-287); IsFrozen fed live from FreezeTag Frozen status (NetGame:4176)", result: pass }
    confidence: high
    notes: "Death-cam is faithful (corpse-settle gate, eventchase_running latch, ceiling-aware viewoffset, eased distance, world-only box-trace with start-solid fallback). Wave-5 closed the frozen-state chase: cl_ft WantEventchase is now ported — IsFrozen (live FreezeTag Frozen status effect on the host-side local Player) + cl_eventchase_frozen (read live, default 0 = off, matching Base) engages the pull-back. The other WantEventchase entry conditions (vehicle/spectated-change/intermission/viewloc) remain absent."

  - id: cl-view.chase.classic_thirdperson
    name: "Classic chase_active third-person + spectator camera (chase_back/up/front/overhead; View_SpectatorCamera)"
    layer: [presentation]
    base_refs:
      - "lib/csqcmodel/cl_player.qc:CSQCPlayer_ApplyChase"
      - "client/view.qc:View_SpectatorCamera"
    port_refs:
      - "game/client/FirstPersonView.cs:ApplyClassicChase (FirstPersonView:401)"
      - "game/client/FirstPersonView.cs:UpdateCamera (classicChase branch, ~247)"
      - "game/net/NetGame.cs:2611 (CameraMode = chase_active != 0 ? Chase : None)"
    constants:
      - { name: "chase_back", base: "user (menu 10..100; DP default 48)", port: "live Cvar(\"chase_back\", 48)", match: true }
      - { name: "chase_up", base: "user (menu 10..50; DP default 24)", port: "live Cvar(\"chase_up\", 24)", match: true }
      - { name: "chase_front", base: "0 (spectating only)", port: "live Cvar(\"chase_front\", 0); but spectating hardcoded false → selfie branch dead", match: false }
      - { name: "chase_overhead", base: "0", port: "live Cvar(\"chase_overhead\", 0); 5x5 grid trace ported", match: true }
      - { name: "chase_pitchangle", base: "user", port: "live Cvar(\"chase_pitchangle\", 0)", match: true }
    status:
      logic: partial
      values: faithful
      timing: faithful
      presentation: partial
      audio: na
      liveness: partial
    intended_divergence: false
    divergence_rationale: null
    gaps:
      - "Spectator third-person (View_SpectatorCamera + STAT(CAMERA_SPECTATOR) 1/2 toggle, IMP_weapon_drop cycling) is NOT ported — spectating a player renders FIRST person: the follow-cam (NetGame:4174) builds its own first-person ViewState and returns before the chase_active CameraMode logic runs."
      - "chase_front (the frontal selfie cam) is effectively dead: ApplyClassicChase hardcodes `spectating = false` (FirstPersonView:~415), and chase_front is QC-guarded to spectating only — so it never engages. The view-flip + overhead-while-spectating paths are untested live."
    verification:
      - { kind: code, ref: "NetGame:2611 sets CameraMode = chase_active!=0 (live render path); ApplyClassicChase (FirstPersonView:401) ports CSQCPlayer_ApplyChase: pull-back along forward by chase_back + lift chase_up + world trace + 8/4 back-off, chase_overhead 5x5 grid lowest-ceiling, chase_pitchangle; chase_* read live via Cvar(); menu radio/sliders wired (DialogSettingsGame:457,487,490,496). Spectate follow-cam still first-person (NetGame:4174); spectating=false disables chase_front.", result: partial }
    confidence: high
    notes: "Wave-5 closed the BIG gap: the user chase_active third-person cam is now genuinely LIVE — NetGame:2611 sets CameraMode from the chase_active cvar each frame (the menu Perspective radio + chase_back/chase_up sliders are no longer dead), and ApplyClassicChase faithfully ports CSQCPlayer_ApplyChase (default pull-back branch + the chase_overhead 5x5 grid + chase_pitchangle, with world box/line traces). STILL OPEN: (1) the dedicated SPECTATOR camera (View_SpectatorCamera / CAMERA_SPECTATOR toggle / weapon-drop cycling) — spectating renders first-person; (2) chase_front, guarded to spectating in QC, is wired but unreachable because the local-player chase sets spectating=false. liveness: partial (own-player chase live; spectator-chase path absent)."

  - id: cl-view.kick.punch_angle
    name: "Punch-angle recoil kick (view angle, networked + decayed)"
    layer: [authority, presentation]
    base_refs:
      - "lib/csqcmodel/cl_player.qc:CSQCPlayer_CalcRefdef (view_angles += view_punchangle)"
      - "common/physics/player.qc:PM_check_punch (punchangle decay)"
    port_refs:
      - "src/XonoticGodot.Common/Physics/PlayerPhysics.cs:CheckPunch"
      - "game/net/ServerNet.cs:1980 (writes PunchAngle)"
      - "game/net/ClientNet.cs:772 (reads PunchAngle)"
      - "game/net/NetGame.cs:3529 (view_angles += PunchAngle)"
    constants:
      - { name: "punchangle_decay_deg_per_s", base: "10", port: "10 (PlayerPhysics:1585)", match: true }
    status:
      logic: faithful
      values: faithful
      timing: faithful
      presentation: faithful
      audio: na
      liveness: live
    intended_divergence: false
    divergence_rationale: null
    gaps: []
    verification:
      - { kind: code, ref: "PunchAngle decayed 10deg/s server-side (CheckPunch, skipped on predicted replays), networked (ServerNet:1980 / ClientNet:772), added to rendered view only (NetGame:3529)", result: pass }
    confidence: high
    notes: "Added to the rendered view only; the aim sent to the server (_viewAngles) stays unpunched so shots track the crosshair, matching QC. Decay matches PM_check_punch (vlen - 10*dt, normalize)."

  - id: cl-view.kick.punch_vector
    name: "Punch-vector recoil kick (view origin)"
    layer: [authority, presentation]
    base_refs:
      - "lib/csqcmodel/cl_player.qc:CSQCPlayer_CalcRefdef (vieworg += view_punchvector)"
      - "common/physics/player.qc:PM_check_punch (punchvector decay 30u/s)"
    port_refs:
      - "src/XonoticGodot.Common/Physics/PlayerPhysics.cs:CheckPunch (decay, PlayerPhysics:1630)"
      - "game/net/ServerNet.cs:2063 (writes PunchVector, owner block)"
      - "game/net/ClientNet.cs:814 (reads PunchVector)"
      - "game/net/NetGame.cs:4283 (PunchOriginQuake = _client.PunchVector)"
      - "game/client/FirstPersonView.cs:UpdateCamera (camQuake += st.PunchOriginQuake, first-person branch only)"
    constants:
      - { name: "punchvector_decay_u_per_s", base: "30", port: "30 (decayed server-side, PlayerPhysics:1630)", match: true }
    status:
      logic: faithful
      values: faithful
      timing: faithful
      presentation: faithful
      audio: na
      liveness: live
    intended_divergence: false
    divergence_rationale: null
    gaps: []
    verification:
      - { kind: code, ref: "Full seam wired end-to-end: PunchVector decayed 30u/s (PlayerPhysics:1630), networked in the owner block right after PunchAngle (ServerNet:2063 / ClientNet:814), fed to the shared view as PunchOriginQuake (NetGame:4283), and added to the rendered camera origin in the FIRST-PERSON branch ONLY (FirstPersonView UpdateCamera: `if(!ChaseActive) camQuake += st.PunchOriginQuake`) so a death/frozen/chase pull-back is never jolted — matching cl_player.qc:570 `vieworg += view_punchvector`.", result: pass }
    confidence: high
    notes: "Wave-5 closed this: the view-origin recoil kick is now wired end-to-end and applied to the rendered eye (first-person only, suppressed during chase). Decay matches PM_check_punch (30 u/s). NOTE: no STOCK Xonotic content drives view_punchvector non-zero (only resets/decay; Minelayer reuses .PunchVector as a movedir alias, not a view kick), so the on-screen effect is still nil under default content — but the parity seam is genuinely faithful + live now, so any future origin kick renders correctly."

  - id: cl-view.bob.view_bob
    name: "View bobbing (vertical cl_bob, horizontal cl_bob2, fall-bob cl_bobfall)"
    layer: [presentation]
    base_refs:
      - "client/view.qc:viewmodel_animate (TODO: cl_bob/cl_bob2/cl_bobfall stubs, 273-285)"
    port_refs: []
    constants:
      - { name: "cl_bob", base: "0 (off by default; DP 0.02)", port: "none", match: false }
      - { name: "cl_bobcycle", base: "0.5", port: "none", match: false }
      - { name: "cl_bob2", base: "0", port: "none", match: false }
      - { name: "cl_bobfall", base: "0.05", port: "none", match: false }
      - { name: "cl_bobfallminspeed", base: "200", port: "none", match: false }
    status:
      logic: missing
      values: missing
      timing: missing
      presentation: missing
      audio: na
      liveness: na
    intended_divergence: false
    divergence_rationale: null
    gaps:
      - "The view does not bob up/down while running (cl_bob), side-to-side (cl_bob2), or swing on landing (cl_bobfall)."
    verification:
      - { kind: grep, ref: "no ApplyBobbing / cl_bob view reader (cl_bob/cl_bobfall only in DialogSettingsGame.cs). Note: even BASE leaves view-bob as TODO stubs in viewmodel_animate — only the gun bobs (bobmodel), not the view.", result: fail }
    confidence: high
    notes: "cl_bob 0 / cl_bob2 0 are the shipped defaults and Base itself only TODO-stubs the view-bob (the actual cl_bobfall etc. is unimplemented in stock CSQC too); so a stock match shows no view-bob in EITHER. The gun-bob (cl_bobmodel) IS ported — see viewmodel.sway."

  - id: cl-view.roll.view_roll
    name: "View roll when strafing (cl_rollangle / CalcRoll)"
    layer: [presentation]
    base_refs:
      - "lib/csqcmodel/cl_player.qc:CSQCPlayer_CalcRefdef (V_CalcRefdef / roll)"
    port_refs: []
    constants:
      - { name: "cl_rollangle", base: "0 (off by default; DP 2.0)", port: "none", match: false }
      - { name: "cl_rollspeed", base: "200", port: "none", match: false }
    status:
      logic: missing
      values: missing
      timing: missing
      presentation: missing
      audio: na
      liveness: na
    intended_divergence: false
    divergence_rationale: null
    gaps:
      - "The camera does not roll/bank when strafing sideways."
    verification:
      - { kind: grep, ref: "no CalcRoll / cl_rollangle reader in the view path", result: fail }
    confidence: high
    notes: "cl_rollangle 0 is the shipped default, so a stock match shows no roll regardless; opt-in only."

  - id: cl-view.tilt.death_idle
    name: "Death tilt (v_deathtilt) + idle view-wave (v_idlescale)"
    layer: [presentation]
    base_refs:
      - "lib/csqcmodel/cl_player.qc:CSQCPlayer_CalcRefdef (deathtilt / idlescale)"
    port_refs: []
    constants:
      - { name: "v_deathtilt", base: "0 (off by default)", port: "none", match: false }
      - { name: "v_idlescale", base: "0 (off by default)", port: "none", match: false }
    status:
      logic: missing
      values: missing
      timing: missing
      presentation: missing
      audio: na
      liveness: na
    intended_divergence: false
    divergence_rationale: null
    gaps:
      - "No view roll on death (v_deathtilt) and no idle sin-wave of the view angles (v_idlescale)."
    verification:
      - { kind: grep, ref: "no DeathTilt / idlescale reader (v_idlescale only a dead menu checkbox in DialogSettingsGame.cs)", result: fail }
    confidence: high
    notes: "Both off by default in stock Xonotic (and v_deathtilt is incompatible with cl_eventchase_death 2 anyway, which is the default); opt-in only."

  - id: cl-view.smooth.stair_viewheight
    name: "Stair + viewheight smoothing (stairsmoothz glide + viewheightavg blend)"
    layer: [presentation]
    base_refs:
      - "lib/csqcmodel/cl_player.qc:CSQCPlayer_ApplySmoothing (220-242)"
    port_refs:
      - "src/XonoticGodot.Net/FaithfulViewSmoothing.cs"
      - "game/net/NetGame.cs:3494-3521"
    constants:
      - { name: "cl_stairsmoothspeed", base: "200", port: "200", match: true }
      - { name: "cl_smoothviewheight", base: "0.05", port: "0.05", match: true }
      - { name: "PHYS_STEPHEIGHT (sv_stepheight)", base: "31", port: "31", match: true }
    status:
      logic: faithful
      values: faithful
      timing: faithful
      presentation: faithful
      audio: na
      liveness: partial
    intended_divergence: true
    divergence_rationale: "Faithful mode (cl_movement_smoothing_faithful, default on) uses the exact bounded stairsmoothz glide + viewheightavg blend; the port-default-mode alternative uses an adaptive render-only stair offset (ClientNet.PredictedStairOffset) to fight the 1/72-quantized server-render-clock jitter (see camera-drift-render-smoothing memory). The stepheight processing is the only intentional divergence in the faithful path."
    gaps: []
    verification:
      - { kind: code, ref: "FaithfulViewSmoothing.Apply mirrors cl_player.qc 220-242 (bound() glide + lowpass eye blend, teleport/airborne/networked-ground snaps); wired live NetGame:3494-3510 when faithfulSmoothing; port-mode adaptive offset is the deliberate alternative", result: pass }
    confidence: high
    notes: "Two paths: faithful (bit-faithful transcription) and the adaptive port-default. liveness: partial because only one path runs at a time per cl_movement_smoothing_faithful."

  - id: cl-view.screen.damage_flash
    name: "Damage red screen flash (HUD_Damage)"
    layer: [presentation]
    base_refs:
      - "client/view.qc:HUD_Damage (1237)"
    port_refs:
      - "game/client/ViewEffects.cs:UpdateDamage"
      - "game/net/NetGame.cs:2436-2451"
    constants:
      - { name: "hud_damage", base: "0.55", port: "0.55", match: true }
      - { name: "hud_damage_factor", base: "0.025", port: "0.025", match: true }
      - { name: "hud_damage_fade_rate", base: "0.75", port: "0.75", match: true }
      - { name: "hud_damage_maxalpha", base: "1.5", port: "1.5", match: true }
      - { name: "hud_damage_pain_threshold", base: "0.1", port: "0.1", match: true }
      - { name: "hud_damage_color", base: "'1 0 0'", port: "'1 0 0'", match: true }
      - { name: "hud_damage_pain_threshold_lower", base: "1.25", port: "1.25", match: true }
      - { name: "hud_damage_pain_threshold_lower_health", base: "50", port: "50", match: true }
    status:
      logic: faithful
      values: faithful
      timing: faithful
      presentation: partial
      audio: na
      liveness: live
    intended_divergence: false
    divergence_rationale: null
    gaps:
      - "Draws a flat red ColorRect, not the gfx/blood splatter texture Base uses."
      - "The gentle-mode (cl_gentle / cl_gentle_damage) solid/randomized-colour path is not ported."
    verification:
      - { kind: code, ref: "UpdateDamage: myhealth_flash accumulate/decay, pain threshold + near-death sin-pulse (1256-1264 ported), respawn/observe clears; reads hud_damage_color cvar; live via NetGame UpdateEffects", result: pass }
    confidence: high
    notes: "Accumulator + pain-threshold + near-death sin-pulse + respawn/observe edge handling all match; only the visual is a solid rect vs the gfx/blood texture, plus the gentle-mode path."

  - id: cl-view.screen.liquid_tint
    name: "Liquid screen tint (HUD_Contents)"
    layer: [presentation]
    base_refs:
      - "client/view.qc:HUD_Contents (1167)"
    port_refs:
      - "game/client/ViewEffects.cs:UpdateContents"
      - "game/client/FirstPersonView.cs:EyeContents"
    constants:
      - { name: "hud_contents_water_alpha", base: "0.5", port: "0.5", match: true }
      - { name: "hud_contents_lava_alpha", base: "0.7", port: "0.7", match: true }
      - { name: "hud_contents_slime_alpha", base: "0.7", port: "0.7", match: true }
      - { name: "hud_contents_fadeintime", base: "0.02", port: "0.02", match: true }
      - { name: "hud_contents_fadeouttime", base: "0.25", port: "0.25", match: true }
      - { name: "water_color", base: "'0.4 0.6 1.0'", port: "'0.4 0.6 1.0'", match: true }
      - { name: "lava_color", base: "'1.0 0.3 0.0'", port: "'1.0 0.3 0.0'", match: true }
      - { name: "slime_color", base: "'0.0 0.8 0.0'", port: "'0.0 0.8 0.0'", match: true }
    status:
      logic: faithful
      values: faithful
      timing: faithful
      presentation: faithful
      audio: na
      liveness: live
    intended_divergence: false
    divergence_rationale: null
    gaps: []
    verification:
      - { kind: code, ref: "UpdateContents: contentavgalpha low-pass fade in/out + per-content colour + colour latch while fading out; eye contents sampled at final render origin (FirstPersonView:254)", result: pass }
    confidence: high
    notes: "Eye contents sampled at the FINAL render origin (the pulled-back cam during chase) matching pointcontents(view_origin), view.qc:1176."

  - id: cl-view.screen.frozen_overlay
    name: "Freeze-Tag icy screen overlay (cl_ft HUD_Draw_overlay)"
    layer: [presentation]
    base_refs:
      - "common/gametypes/gametype/freezetag/cl_freezetag.qc:HUD_Draw_overlay (29-44)"
    port_refs:
      - "game/client/ViewEffects.cs:UpdateFrozenOverlay"
      - "game/net/NetGame.cs:2655-2661"
    constants:
      - { name: "base_color", base: "'0.25 0.90 1'", port: "'0.25 0.90 1'", match: true }
      - { name: "col_fade", base: "max(0, REVIVE_PROGRESS*2 - 1)", port: "max(0, p*2-1)", match: true }
      - { name: "alpha_fade", base: "0.3 + 0.7*(1 - max(0, REVIVE_PROGRESS*4 - 3))", port: "0.3 + 0.7*(1 - max(0, p*4-3))", match: true }
      - { name: "col_warm", base: "col += (col_fade, -col_fade, -col_fade)", port: "(0.25+cf, 0.90-cf, 1-cf)", match: true }
    status:
      logic: faithful
      values: faithful
      timing: faithful
      presentation: faithful
      audio: na
      liveness: live
    intended_divergence: false
    divergence_rationale: null
    gaps: []
    verification:
      - { kind: code, ref: "UpdateFrozenOverlay mirrors HUD_Draw_overlay exactly: base '0.25 0.90 1', col_fade/alpha_fade formulas + colour-warm; gated on STAT(FROZEN). Wired live NetGame:2655-2661 off the host-side local Player Frozen status effect + REVIVE_PROGRESS (FreezeTag.ReviveTick). A pure remote client reads frozen=false (no overlay).", result: pass }
    confidence: high
    notes: "Wave-5 addition (previously unmapped). Full-screen ColorRect composited above the damage + liquid tints. Faithful icy-blue tint that warms toward white and fades out as the thaw ring (STAT(REVIVE_PROGRESS)) fills. Driven by the live FreezeTag gametype, same source as the crosshair thaw ring. liveness: live only in Freeze Tag (the gametype gates STAT(FROZEN)); a pure remote (non-host) client has no LocalServerPlayer so reads false — the cross-client wepent/stat follow-up is the same limitation noted on the crosshair revive ring."

  - id: cl-view.screen.postprocess_nightvision
    name: "GLSL blur/sharpen post-process + night-vision + blur-test"
    layer: [presentation]
    base_refs:
      - "client/view.qc:View_PostProcessing (1326)"
      - "client/view.qc:View_NightVision (1113)"
      - "client/view.qc:View_BlurTest (1453)"
    port_refs: []
    constants:
      - { name: "hud_postprocessing", base: "1 (master)", port: "none", match: false }
      - { name: "hud_damage_blur", base: "1 (effect)", port: "none", match: false }
      - { name: "hud_powerup", base: "0 (sharpen)", port: "none", match: false }
    status:
      logic: missing
      values: missing
      timing: missing
      presentation: missing
      audio: na
      liveness: na
    intended_divergence: true
    divergence_rationale: "The damage/content blur, powerup edge-sharpen and blur-test drive engine GLSL shader cvars (r_glsl_postprocess + r_glsl_postprocess_uservec1/2) with no Godot analogue; ViewEffects deliberately ships only the coloured screen reaction (documented in ViewEffects.cs class header). Night-vision is a fullbright/fakelight debug aid, not a normal-match feature."
    gaps:
      - "No screen blur when damaged / underwater, no powerup edge-sharpen, no night-vision tint, no blur-test debug effect."
    verification:
      - { kind: grep, ref: "no postprocess/nightvision/blurtest implementation; 'postprocess' appears only in ViewEffects.cs (class doc stating intentional omission)", result: fail }
    confidence: high
    notes: "Documented intended omission in ViewEffects.cs. hud_powerup defaults 0 so the sharpen is off in stock anyway; the damage/content blur is the only default-on effect lost (a subtle blur under heavy damage / underwater)."

  - id: cl-view.reticle.zoom_reticle
    name: "Zoom reticle (generic +zoom + weapon scope)"
    layer: [presentation]
    base_refs:
      - "client/hud/crosshair.qc:DrawReticle (648-717)"
    port_refs:
      - "game/client/ReticleOverlay.cs:UpdateReticle"
      - "game/net/NetGame.cs:2420"
    constants:
      - { name: "cl_reticle", base: "1", port: "1", match: true }
      - { name: "cl_reticle_stretch", base: "0", port: "0", match: true }
      - { name: "cl_reticle_chase", base: "0", port: "0", match: true }
      - { name: "cl_reticle_weapon", base: "1", port: "1", match: true }
      - { name: "cl_reticle_normal_alpha", base: "1", port: "1", match: true }
      - { name: "reticle_fade_floor", base: "max(0.25, zoomfraction)", port: "Max(0.25, zoomFraction)", match: true }
    status:
      logic: faithful
      values: faithful
      timing: faithful
      presentation: faithful
      audio: na
      liveness: live
    intended_divergence: false
    divergence_rationale: null
    gaps:
      - "zoomscript_caught is not modelled (always the max(0.25, fraction) branch) — a negligible edge tied to the unported auto-zoom-on-low-fov script (see zoomscript_buttonstatus row)."
    verification:
      - { kind: code, ref: "UpdateReticle: type selection (generic/weapon), 0.25 fade floor, dead/spectate/chase suppression (unless cl_reticle_chase), stretch vs square geometry; live NetGame:2420", result: pass }
    confidence: high
    notes: "Type selection (none/generic/weapon-scope), the max(0.25, zoomfraction) fade, the square-centred vs stretched geometry, and the suppression conditions all match DrawReticle."

  - id: cl-view.viewmodel.sway
    name: "Viewmodel sway (cl_followmodel / cl_leanmodel / cl_bobmodel)"
    layer: [presentation]
    base_refs:
      - "client/view.qc:viewmodel_animate (258)"
      - "client/view.qc:calc_followmodel_ofs (113)"
      - "client/view.qc:leanmodel_ofs (167)"
      - "client/view.qc:bobmodel_ofs (203)"
    port_refs:
      - "game/client/ViewModel.cs:UpdateSway"
      - "game/client/ViewModel.cs:FollowModelOffset"
      - "game/client/ViewModel.cs:LeanModelOffset"
      - "game/client/ViewModel.cs:BobModelOffset"
      - "game/net/NetGame.cs:1127 (ViewStateProvider = BuildViewState)"
    constants:
      - { name: "cl_followmodel", base: "1", port: "true", match: true }
      - { name: "cl_followmodel_speed", base: "0.3", port: "0.3", match: true }
      - { name: "cl_followmodel_limit", base: "135", port: "135", match: true }
      - { name: "cl_followmodel_velocity_lowpass", base: "0.05", port: "0.05", match: true }
      - { name: "cl_followmodel_highpass", base: "0.05", port: "0.05", match: true }
      - { name: "cl_followmodel_lowpass", base: "0.03", port: "0.03", match: true }
      - { name: "cl_leanmodel", base: "1", port: "true", match: true }
      - { name: "cl_leanmodel_speed", base: "0.3", port: "0.3", match: true }
      - { name: "cl_leanmodel_limit", base: "30", port: "30", match: true }
      - { name: "cl_leanmodel_highpass1", base: "0.2", port: "0.2", match: true }
      - { name: "cl_leanmodel_highpass", base: "0.2", port: "0.2", match: true }
      - { name: "cl_leanmodel_lowpass", base: "0.05", port: "0.05", match: true }
      - { name: "cl_bobmodel", base: "1", port: "true", match: true }
      - { name: "cl_bobmodel_side", base: "0.2", port: "0.2", match: true }
      - { name: "cl_bobmodel_up", base: "0.1", port: "0.1", match: true }
      - { name: "cl_bobmodel_speed", base: "10", port: "10", match: true }
    status:
      logic: faithful
      values: faithful
      timing: faithful
      presentation: faithful
      audio: na
      liveness: live
    intended_divergence: false
    divergence_rationale: null
    gaps:
      - "Sway constants are [Export] fields with hardcoded defaults rather than live cl_*model_* cvar reads, so changing those cvars at runtime has no effect (values still match the shipped defaults)."
      - "cl_followmodel_velocity_absolute branch (calc_followmodel_ofs:125,152-161) is not ported (default 0, so the view-relative branch — which IS ported — is the default path)."
      - "UpdateSway does not gate on chase/dead like viewmodel_animate:260 (autocvar_chase_active || HEALTH<=0), but the whole viewmodel node is hidden during chase/dead (NetGame:1373), so the sway is not observable then anyway."
    verification:
      - { kind: code, ref: "UpdateSway ports avg_factor/lowpass/highpass macro chains + bobmodel time-offset + map_bound_ranges; ViewStateProvider=BuildViewState live (NetGame:1127); constants match xonotic-client.cfg", result: pass }
    confidence: high
    notes: "Faithful low/high-pass chains, the bob run-up scale, and all constants match. Divergences: cvar liveness (defaults baked as Exports) and the unported velocity_absolute opt-in branch — both default-equivalent."

  - id: cl-view.demo.free_camera_lockview_ortho
    name: "Demo free camera + lockview + orthoview + viewloc + FPS report"
    layer: [presentation]
    base_refs:
      - "client/view.qc:CSQC_Demo_Camera (1896)"
      - "client/view.qc:View_Lock (1395)"
      - "client/view.qc:View_Ortho (1568)"
      - "client/view.qc:GetViewLocationFOV (612)"
      - "client/view.qc:fpscounter_update (398)"
    port_refs: []
    constants:
      - { name: "cl_lockview", base: "0", port: "none", match: false }
      - { name: "cl_orthoview", base: "0", port: "none", match: false }
    status:
      logic: missing
      values: missing
      timing: missing
      presentation: missing
      audio: na
      liveness: na
    intended_divergence: false
    divergence_rationale: null
    gaps:
      - "No demo free/chase camera (CSQC_Demo_Camera), no view lock (cl_lockview), no orthographic radar-render view (cl_orthoview / View_Ortho), no viewloc 2.5D side-scroller camera (GetViewLocationFOV), and no FPS report to the server (fpscounter_update STAT(SHOWFPS))."
    verification:
      - { kind: grep, ref: "no CSQC_Demo_Camera / cl_lockview / cl_orthoview / viewloc implementation in src/ or game/", result: fail }
    confidence: high
    notes: "Demo replay camera is tracked separately on the demo-cinematics branch. Orthoview is a dev/radar tool; viewloc is a niche 2.5D-map feature; lockview and the server FPS report are minor. None are on the normal-match camera path."
