unit: cl-view
category: client
base_ref_rev: "v0.8.6-1779-g863cd3e84"
last_audited: "2026-06-28"  # Re-verify x2 (opus parity-diff, claude/parity-port-waves): re-read FirstPersonView/ChaseCamera/ViewEffects/ReticleOverlay + NetGame wiring + ClientNet/ServerNet against current source AGAIN this wave. NO DRIFT — all feature scores hold. Live wiring re-confirmed at current line numbers: own-player UpdateView NetGame:5578, spectate UpdateView 5458; CameraMode from chase_active 3044 (own) / 5447 (spectate); Spectating=true 5446 then reset 5461; TriggerSpawnZoom 3022; NotifyZoomScroll on wheel 4904; ZoomScriptCaught->reticle 3084-3086; UpdateEffects 3116 + UpdateFrozenOverlay 3125. Punch seam intact: owner block order PressedKeys(ClientNet:1023)->PunchAngle(1024)->PunchVector(1025); ServerNet writes PunchVector:2783; ViewState PunchOriginQuake=_client.PunchVector NetGame:5563 (own, first-person-only). ViewEffects damage flash uses gfx/blood splatter (MakeBloodSplat:114, TextureCache.Get("gfx/blood")) + gentle path (cl_gentle/cl_gentle_damage:256). ChaseCamera.cs (extracted classic+event chase primitives) live via FirstPersonView. port_ref line numbers in feature rows below shifted again (warpzone/wepent waves) but left as-is except where edited.
summary:
  feature_count: 27
  worst_gaps:
    - "Spectator-camera SERVER mode toggle (STAT(CAMERA_SPECTATOR) 1/2 + IMP_weapon_drop cycling, View_SpectatorCamera's chase_active<=0 auto-switch path) is not ported — the user chase_active-driven third-person spectate IS now live (NetGame UpdateCamera sets _view.Spectating + Chase, anchored to the spectatee pose) and chase_front is now reachable, but pressing weapon-drop to cycle the server-side spectator camera mode is absent."
    - "GLSL blur/sharpen post-process (damage/content blur, powerup edge-sharpen, View_BlurTest) and night-vision are not implemented (engine shader cvars, no Godot analogue)."
    - "viewloc (2.5D side-scroller View Location camera, GetViewLocationFOV), demo free camera (CSQC_Demo_Camera), orthoview, and the server FPS report are not ported (niche dev/demo tools, off the normal-match camera path). NOTE: lockview (cl_lockview / View_Lock) IS now ported+live (FirstPersonView.ApplyViewLock) — no longer a gap."
features:
  - id: cl-view.zoom.fov_state_machine
    name: "Zoom / FOV state machine (current_viewzoom integration, 0.75 frustum, sensitivity)"
    layer: [presentation]
    base_refs:
      - "client/view.qc:GetCurrentFov (488)"
      - "client/view.qc:IsZooming (424)"
      - "client/view.qc:View_UpdateFov (1650)"
    port_refs:
      - "game/client/FirstPersonView.cs:UpdateZoom"
      - "game/client/FirstPersonView.cs:ComputeVerticalFov"
      - "game/net/NetGame.cs:UpdateCamera"
      - "game/net/NetGame.cs:SeedViewCvars"
    constants:
      - { name: "fov", base: "100", port: "100 (BaseFov; fov cvar overrides live)", match: true }
      - { name: "cl_zoomfactor", base: "5 (clamp 1..30 else 2.5)", port: "5 (seeded; fallback 2.5, clamp 1..30)", match: true }
      - { name: "cl_zoomspeed", base: "8 (0.5..16 else 3.5)", port: "8 (seeded; fallback 3.5, clamp 0.5..16)", match: true }
      - { name: "cl_zoomsensitivity", base: "0", port: "0 (seeded)", match: true }
      - { name: "frustum_aspect_norm", base: "0.75", port: "0.75 (FirstPersonView:332)", match: true }
      - { name: "MAX_ZOOMFACTOR", base: "30", port: "30 (FirstPersonView:126)", match: true }
    status:
      logic: faithful
      values: faithful
      timing: faithful
      presentation: faithful
      audio: na
      liveness: live
    intended_divergence: false
    divergence_rationale: null
    gaps: []
    verification:
      - { kind: code, ref: "FirstPersonView.UpdateZoom + ComputeVerticalFov match GetCurrentFov; cvars seeded SeedViewCvars; UpdateCamera live on net path (NetGame:5138 UpdateView)", result: pass }
    confidence: high
    notes: "current_viewzoom integration, current_zoomfraction, setsensitivityscale (current_viewzoom**(1-zoomsensitivity)), and the *0.75 4:3 frustum all match. The velocityzoom multiplier is now folded in via ComputeVelocityZoom (FirstPersonView:423) — see velocity_zoom row (default off, no effect). The 2.5/3.5 fallbacks match Base's out-of-range clamp."

  - id: cl-view.zoom.spawn_zoom
    name: "Spawn-zoom (zoom out from 1/factor on respawn)"
    layer: [presentation]
    base_refs:
      - "client/view.qc:GetCurrentFov (spawn branch)"
      - "client/spawnpoints.qc:82-86"
    port_refs:
      - "game/client/FirstPersonView.cs:TriggerSpawnZoom"
      - "game/net/NetGame.cs:2855 (TriggerSpawnZoom on Health 0->>0 edge)"
    constants:
      - { name: "cl_spawnzoom", base: "1", port: "1 (seeded)", match: true }
      - { name: "cl_spawnzoom_factor", base: "2", port: "2 (seeded)", match: true }
      - { name: "cl_spawnzoom_speed", base: "1", port: "1 (seeded)", match: true }
    status:
      logic: faithful
      values: faithful
      timing: faithful
      presentation: faithful
      audio: na
      liveness: live
    intended_divergence: false
    divergence_rationale: null
    gaps: []
    verification:
      - { kind: code, ref: "Health 0->>0 edge arms TriggerSpawnZoom (NetGame:2608); ease-out in UpdateZoom 253-261", result: pass }
    confidence: high
    notes: "Net path derives the (re)spawn edge from a Health 0->>0 transition since it has no Teleport; faithful intended substitution. Spawn-zoom branch sits above the normal zoom and is cancelled by manual +zoom, matching GetCurrentFov."

  - id: cl-view.zoom.weapon_zoom
    name: "Weapon zoom fold-in (Vortex secondary zoom)"
    layer: [presentation]
    base_refs:
      - "client/view.qc:IsZooming (wr_zoomdir loop)"
    port_refs:
      - "game/net/NetGame.cs:~2890 (weaponZoom -> ZoomHeld, zoomActive gate)"
    constants: []
    status:
      logic: faithful
      values: faithful
      timing: faithful
      presentation: faithful
      audio: na
      liveness: live
    intended_divergence: false
    divergence_rationale: null
    gaps: []
    verification:
      - { kind: code, ref: "active weapon secondary-zoom folded into ZoomHeld (NetGame:2624)", result: pass }
    confidence: medium
    notes: "Port folds the active weapon's secondary-fire zoom into button_zoom like IsZooming. Only the one active weapon is checked (port has a single weapon slot on this path); Base loops MAX_WEAPONSLOTS but stock single-wield matches."

  - id: cl-view.zoom.velocity_zoom
    name: "Velocity-based FOV zoom (cl_velocityzoom)"
    layer: [presentation]
    base_refs:
      - "client/view.qc:GetCurrentFov (velocityzoom block 574-601)"
    port_refs:
      - "game/client/FirstPersonView.cs:ComputeVelocityZoom (434)"
      - "game/client/FirstPersonView.cs:UpdateCamera (velocityZoom fold, 422-423)"
    constants:
      - { name: "cl_velocityzoom_enabled", base: "0 (off by default)", port: "0 (read live)", match: true }
      - { name: "cl_velocityzoom_factor", base: "0", port: "0 (read live)", match: true }
      - { name: "cl_velocityzoom_type", base: "3", port: "3 (read live)", match: true }
      - { name: "cl_velocityzoom_speed", base: "1000", port: "1000 (read live)", match: true }
      - { name: "cl_velocityzoom_time", base: "0.2", port: "0.2 (read live)", match: true }
    status:
      logic: faithful
      values: faithful
      timing: faithful
      presentation: faithful
      audio: na
      liveness: live
    intended_divergence: false
    divergence_rationale: null
    gaps: []
    verification:
      - { kind: code, ref: "ComputeVelocityZoom ports the velocityzoom block: curspeed type 1/2/3 (fwd-dot velocity), low-pass _avgSpeed over cl_velocityzoom_time, float2range11 (f/(|f|+1)) + exp(); folded into the rendered fov in UpdateCamera (FirstPersonView:428-429). Live on the net path (UpdateView NetGame:5138). All cvars read live with faithful fallbacks.", result: pass }
    confidence: high
    notes: "FIXED since the 2026-06-22 baseline (was missing): the velocity-based FOV widen/narrow is now ported and wired. Off by default in stock Xonotic (cl_velocityzoom_enabled 0), so a default match is unaffected; opt-in only — but the seam is now faithful and live so enabling the cvars takes effect. The type 0 disable and the intermission/spectator zero branch are handled (intermission branch not on the net first-person path)."

  - id: cl-view.zoom.zoom_scroll
    name: "Zoom-scroll (mousewheel adjusts zoom factor while zooming)"
    layer: [presentation]
    base_refs:
      - "client/view.qc:ZoomScroll (450)"
      - "client/view.qc:View_InputEvent (465)"
      - "client/view.qc:GetCurrentFov (523-544 zoomscroll integration)"
    port_refs:
      - "game/client/FirstPersonView.cs:NotifyZoomScroll (197)"
      - "game/client/FirstPersonView.cs:UpdateZoom (zoomscroll branch 264-289)"
      - "game/net/NetGame.cs:4503 (mousewheel -> NotifyZoomScroll)"
    constants:
      - { name: "cl_zoomscroll", base: "1", port: "1 (read live)", match: true }
      - { name: "cl_zoomscroll_scale", base: "0.2", port: "0.2 (read live)", match: true }
      - { name: "cl_zoomscroll_speed", base: "16", port: "16 (read live)", match: true }
    status:
      logic: faithful
      values: faithful
      timing: faithful
      presentation: faithful
      audio: na
      liveness: live
    intended_divergence: false
    divergence_rationale: null
    gaps: []
    verification:
      - { kind: code, ref: "NotifyZoomScroll (ZoomScroll port): only while cl_zoomscroll on + actively zooming, scales zoomscroll_factor_target by 1+min(|scale|,1) clamped [1,MAX_ZOOMFACTOR]; UpdateZoom eases zoomscroll_factor toward target (frametime-independent avg) and REPLACES zoomfactor; wired to K_MWHEELUP/DOWN gated on ZoomHeld (NetGame:3873-3876).", result: pass }
    confidence: high
    notes: "FIXED since the 2026-06-22 baseline (was missing): holding +zoom and scrolling now changes the zoom factor. Default ON but requires the player to actively scroll while zoomed; no passive effect, so a stock match without scrolling is unaffected."

  - id: cl-view.zoom.zoomscript_buttonstatus
    name: "Zoomscript auto-zoom + zoom-button management (View_CheckButtonStatus)"
    layer: [presentation]
    base_refs:
      - "client/view.qc:View_CheckButtonStatus (1472)"
    port_refs:
      - "game/client/FirstPersonView.cs:UpdateZoom (zoomscript_caught 233-239)"
      - "game/net/NetGame.cs:~2890 (zoomActive gate, Health>0)"
    constants:
      - { name: "cl_unpress_zoom_on_death", base: "1", port: "implicit (zoom suppressed when Health<=0)", match: true }
      - { name: "fov_auto_zoom_threshold", base: "59.5 (+button9 when fov<=59.5)", port: "59.5 (FirstPersonView:237)", match: true }
      - { name: "cl_unpress_zoom_on_weapon_switch", base: "1", port: "none", match: false }
      - { name: "cl_unpress_attack_on_weapon_switch", base: "1", port: "none", match: false }
    status:
      logic: faithful
      values: faithful
      timing: na
      presentation: na
      audio: na
      liveness: live
    intended_divergence: false
    divergence_rationale: null
    gaps:
      - "cl_unpress_zoom_on_weapon_switch / cl_unpress_attack_on_weapon_switch and the cl_hook_activeweapon localcmd are not modelled (niche scripting glue)."
      - "isdemo() zoom-via-__togglezoom fallback is absent."
    verification:
      - { kind: code, ref: "FirstPersonView.UpdateZoom (233-239): autocvar_fov <= 59.5 sets _zoomScriptCaught, which folds into the zoom integration exactly like held +zoom AND feeds the reticle full-alpha branch (ZoomScriptCaught exposed, read NetGame:2672 -> ReticleOverlay). unpress-zoom-on-death covered by NetGame:2624 zoomActive && Health>0.", result: pass }
    confidence: high
    notes: "FIXED since the 2026-06-22 baseline (was partial): the zoomscript auto-zoom (autocvar_fov <= 59.5 -> zoomscript_caught) is now ported in UpdateZoom — it engages the zoom like +button9 and drives the reticle's full-alpha branch (closing the gap noted in the reticle row). Remaining absences are the weapon-switch unpress + cl_hook_activeweapon localcmd glue and the demo togglezoom fallback — niche scripting only."

  - id: cl-view.chase.event_death_cam
    name: "Event / death chase camera (cl_eventchase_death corpse-settle pull-back)"
    layer: [presentation]
    base_refs:
      - "client/view.qc:WantEventchase (730)"
      - "client/view.qc:View_EventChase (767)"
    port_refs:
      - "game/client/FirstPersonView.cs:WantEventChase (628)"
      - "game/client/FirstPersonView.cs:ApplyEventChase (642)"
    constants:
      - { name: "cl_eventchase_death", base: "2 (settle then latch)", port: "2 (seeded)", match: true }
      - { name: "cl_eventchase_distance", base: "140", port: "140 (seeded)", match: true }
      - { name: "cl_eventchase_speed", base: "1.3", port: "1.3 (seeded)", match: true }
      - { name: "cl_eventchase_viewoffset", base: "'0 0 20'", port: "'0 0 20'", match: true }
      - { name: "cl_eventchase_mins", base: "'-12 -12 -8'", port: "'-12 -12 -8'", match: true }
      - { name: "cl_eventchase_maxs", base: "'12 12 8'", port: "'12 12 8'", match: true }
      - { name: "cl_eventchase_frozen", base: "0 (off by default)", port: "0 (read live; default matches)", match: true }
    status:
      logic: faithful
      values: faithful
      timing: faithful
      presentation: faithful
      audio: na
      liveness: live
    intended_divergence: false
    divergence_rationale: null
    gaps:
      - "Vehicle chase (cl_eventchase_vehicle/_vehicle_distance), spectated-change chase (cl_eventchase_spectated_change), and the game-stop/intermission/viewloc chase triggers in WantEventchase are not ported (death + frozen + host-forced chase only)."
    verification:
      - { kind: code, ref: "ApplyEventChase: death==2 velocity-settle gate + latch + viewoffset ceiling trace + box-trace pull-back; frozenChase = st.IsFrozen && cl_eventchase_frozen!=0 (FirstPersonView:638); IsFrozen fed live from FreezeTag Frozen status (NetGame:4449)", result: pass }
    confidence: high
    notes: "Death-cam is faithful (corpse-settle gate, eventchase_running latch, ceiling-aware viewoffset, eased distance, world-only box-trace with start-solid fallback). The frozen-state chase is ported — IsFrozen (live FreezeTag Frozen status effect on the host-side local Player) + cl_eventchase_frozen (read live, default 0 = off, matching Base) engages the pull-back. The other WantEventchase entry conditions (vehicle/spectated-change/intermission/viewloc) remain absent."

  - id: cl-view.chase.classic_thirdperson
    name: "Classic chase_active third-person + spectator camera (chase_back/up/front/overhead; View_SpectatorCamera)"
    layer: [presentation]
    base_refs:
      - "lib/csqcmodel/cl_player.qc:CSQCPlayer_ApplyChase (453)"
      - "client/view.qc:View_SpectatorCamera (655)"
    port_refs:
      - "game/client/FirstPersonView.cs:ApplyClassicChase (725)"
      - "game/client/FirstPersonView.cs:UpdateCamera (classicChase branch, 357)"
      - "game/net/NetGame.cs:2877 (own-player CameraMode = chase_active != 0 ? Chase : None)"
      - "game/net/NetGame.cs:5001-5021 (spectate branch: _view.Spectating = true + CameraMode from chase_active, anchored to spectatee pose)"
    constants:
      - { name: "chase_back", base: "user (menu 10..100; DP default 48)", port: "live Cvar(\"chase_back\", 48)", match: true }
      - { name: "chase_up", base: "user (menu 10..50; DP default 24)", port: "live Cvar(\"chase_up\", 24)", match: true }
      - { name: "chase_front", base: "0 (spectating only)", port: "live Cvar(\"chase_front\", 0); now reachable — NetGame spectate branch sets _view.Spectating = true so the QC spectating-only selfie guard is satisfied", match: true }
      - { name: "chase_overhead", base: "0", port: "live Cvar(\"chase_overhead\", 0); 5x5 grid trace ported", match: true }
      - { name: "chase_pitchangle", base: "user", port: "live Cvar(\"chase_pitchangle\", 0)", match: true }
    status:
      logic: partial
      values: faithful
      timing: faithful
      presentation: faithful
      audio: na
      liveness: live
    intended_divergence: false
    divergence_rationale: null
    gaps:
      - "The server-side spectator-camera mode toggle is not ported: View_SpectatorCamera's STAT(CAMERA_SPECTATOR) 1/2 state + IMP_weapon_drop cycling (the chase_active<=0 auto-switch that flips chase_active to -2 and Impulse_Sends weapon-drop to cycle the server spectator camera) has no port analogue. The port drives the spectator third-person purely from the user chase_active cvar."
    verification:
      - { kind: code, ref: "NetGame:2877 sets own-player CameraMode = chase_active!=0; NetGame:5001-5021 spectate branch sets _view.Spectating = true AND CameraMode = chase_active!=0 ? Chase, anchored to the sampled spectatee pose -> spectating a player with chase_active set now renders THIRD person and the chase_front selfie branch (Spectating-gated) is reachable. ApplyClassicChase (FirstPersonView:725) ports CSQCPlayer_ApplyChase (pull-back + chase_overhead 5x5 grid + chase_pitchangle + chase_front flip).", result: pass }
    confidence: high
    notes: "FIXED since the prior baseline: spectating a player now renders THIRD person when the user enables chase_active (NetGame UpdateCamera spectate branch sets _view.Spectating + Chase anchored to the spectatee pose), and chase_front (the frontal selfie, QC-guarded to spectating) is now reachable because Spectating is true on that path. liveness: live (own-player chase + spectator chase both wired). STILL OPEN (logic: partial): the STAT(CAMERA_SPECTATOR) server mode toggle + IMP_weapon_drop cycling auto-switch path in View_SpectatorCamera is not ported — pressing weapon-drop to cycle the server-side spectator camera mode has no effect."

  - id: cl-view.kick.punch_angle
    name: "Punch-angle recoil kick (view angle, networked + decayed)"
    layer: [authority, presentation]
    base_refs:
      - "lib/csqcmodel/cl_player.qc:CSQCPlayer_CalcRefdef (view_angles += view_punchangle, 566)"
      - "common/physics/player.qc:PM_check_punch (punchangle decay)"
    port_refs:
      - "src/XonoticGodot.Common/Physics/PlayerPhysics.cs:CheckPunch"
      - "game/net/ServerNet.cs (writes PunchAngle)"
      - "game/net/ClientNet.cs (reads PunchAngle)"
      - "game/net/NetGame.cs (view_angles += PunchAngle)"
    constants:
      - { name: "punchangle_decay_deg_per_s", base: "10", port: "10 (PlayerPhysics CheckPunch)", match: true }
    status:
      logic: faithful
      values: faithful
      timing: faithful
      presentation: faithful
      audio: na
      liveness: live
    intended_divergence: false
    divergence_rationale: null
    gaps: []
    verification:
      - { kind: code, ref: "PunchAngle decayed 10deg/s server-side (CheckPunch, skipped on predicted replays), networked (ServerNet/ClientNet), added to rendered view only", result: pass }
    confidence: high
    notes: "Added to the rendered view only; the aim sent to the server (_viewAngles) stays unpunched so shots track the crosshair, matching QC. Decay matches PM_check_punch (vlen - 10*dt, normalize)."

  - id: cl-view.kick.punch_vector
    name: "Punch-vector recoil kick (view origin)"
    layer: [authority, presentation]
    base_refs:
      - "lib/csqcmodel/cl_player.qc:CSQCPlayer_CalcRefdef (vieworg += view_punchvector, 570)"
      - "common/physics/player.qc:PM_check_punch (punchvector decay 30u/s)"
    port_refs:
      - "src/XonoticGodot.Common/Physics/PlayerPhysics.cs:CheckPunch (decay)"
      - "game/net/ServerNet.cs (writes PunchVector, owner block)"
      - "game/net/ClientNet.cs (reads PunchVector)"
      - "game/net/NetGame.cs:5123 (PunchOriginQuake = _client.PunchVector)"
      - "game/client/FirstPersonView.cs:UpdateCamera (camQuake += st.PunchOriginQuake, first-person branch only, 391)"
    constants:
      - { name: "punchvector_decay_u_per_s", base: "30", port: "30 (decayed server-side)", match: true }
    status:
      logic: faithful
      values: faithful
      timing: faithful
      presentation: faithful
      audio: na
      liveness: live
    intended_divergence: false
    divergence_rationale: null
    gaps: []
    verification:
      - { kind: code, ref: "Full seam wired end-to-end: PunchVector decayed 30u/s, networked in the owner block right after PunchAngle, fed to the shared view as PunchOriginQuake (NetGame:4445), and added to the rendered camera origin in the FIRST-PERSON branch ONLY (FirstPersonView:389-391 `if(!ChaseActive) camQuake += st.PunchOriginQuake`) so a death/frozen/chase pull-back is never jolted — matching cl_player.qc:570.", result: pass }
    confidence: high
    notes: "The view-origin recoil kick is wired end-to-end and applied to the rendered eye (first-person only, suppressed during chase). Decay matches PM_check_punch (30 u/s). NOTE: no STOCK Xonotic content drives view_punchvector non-zero (only resets/decay; Minelayer reuses .PunchVector as a movedir alias, not a view kick), so the on-screen effect is still nil under default content — but the parity seam is faithful + live."

  - id: cl-view.bob.view_bob
    name: "View bobbing (vertical cl_bob, horizontal cl_bob2, fall-bob cl_bobfall)"
    layer: [presentation]
    base_refs:
      - "lib/csqcmodel/cl_player.qc:CSQCPlayer_ApplyBobbing (321)"
    port_refs:
      - "game/client/FirstPersonView.cs:ApplyBobbing (532)"
      - "game/net/NetGame.cs:5135-5136 (OnGround/JumpHeld fed into ViewState)"
    constants:
      - { name: "cl_bob", base: "0 (off by default; DP 0.02)", port: "0 (read live)", match: true }
      - { name: "cl_bobcycle", base: "0.5", port: "0.5 (read live)", match: true }
      - { name: "cl_bob_limit", base: "7", port: "7 (read live)", match: true }
      - { name: "cl_bobup", base: "0.5", port: "0.5 (read live)", match: true }
      - { name: "cl_bob_velocity_limit", base: "400", port: "400 (read live)", match: true }
      - { name: "cl_bob2", base: "0", port: "0 (read live)", match: true }
      - { name: "cl_bob2cycle", base: "1", port: "1 (read live)", match: true }
      - { name: "cl_bob2smooth", base: "0.05", port: "0.05 (read live)", match: true }
      - { name: "cl_bobfall", base: "0.05", port: "0.05 (read live)", match: true }
      - { name: "cl_bobfallcycle", base: "3", port: "3 (read live)", match: true }
      - { name: "cl_bobfallminspeed", base: "200", port: "200 (read live)", match: true }
    status:
      logic: faithful
      values: faithful
      timing: faithful
      presentation: faithful
      audio: na
      liveness: live
    intended_divergence: false
    divergence_rationale: null
    gaps:
      - "The cl_bob_limit_heightcheck trace path (default 0) is omitted (documented; the default-0 branch is the one that runs)."
    verification:
      - { kind: code, ref: "ApplyBobbing ports CSQCPlayer_ApplyBobbing: vertical bob (bobup sin, xyspeed-proportional), horizontal bob2 (cos cycle + bob2_smooth ground-decay blocked while jumping), fall-bob (bobfall_speed/swing). All cvars read live with Base defaults. OnGround/JumpHeld/Velocity fed via ViewState (NetGame:5135-5136); UpdateView live (NetGame:5138).", result: pass }
    confidence: high
    notes: "FIXED since the 2026-06-22 baseline (was missing — and the baseline note that even Base only TODO-stubbed this was WRONG: CSQCPlayer_ApplyBobbing IS a full implementation in cl_player.qc:321, the view.qc:273-285 stubs were a separate dead branch). The port now faithfully transcribes the cl_player.qc bobbing. cl_bob 0 / cl_bob2 0 are the shipped defaults so a stock match still shows no view-bob — opt-in only. Only the heightcheck trace (default off) is omitted."

  - id: cl-view.roll.view_roll
    name: "View roll when strafing (cl_rollangle / CalcRoll)"
    layer: [presentation]
    base_refs:
      - "lib/csqcmodel/cl_player.qc:CSQCPlayer_CalcRoll (432)"
    port_refs:
      - "game/client/FirstPersonView.cs:CalcRoll (509)"
      - "game/client/FirstPersonView.cs:UpdateCamera (viewAngles.Z += CalcRoll, 377)"
    constants:
      - { name: "cl_rollangle", base: "0 (off by default; DP 2.0)", port: "0 (read live)", match: true }
      - { name: "cl_rollspeed", base: "200 (DP default; QC autocvar 0)", port: "200 (fallback)", match: true }
    status:
      logic: faithful
      values: faithful
      timing: faithful
      presentation: faithful
      audio: na
      liveness: live
    intended_divergence: false
    divergence_rationale: null
    gaps: []
    verification:
      - { kind: code, ref: "CalcRoll ports CSQCPlayer_CalcRoll: side = velocity.v_right, ramp to cl_rollangle past cl_rollspeed, signed; added to view_angles.z in UpdateCamera (FirstPersonView:377). Live on the net path.", result: pass }
    confidence: high
    notes: "FIXED since the 2026-06-22 baseline (was missing): the camera now banks when strafing. cl_rollangle 0 is the shipped default so a stock match shows no roll regardless; opt-in only. cl_rollspeed has no QC autocvar default (0) so the DP 200 fallback is used — matches Base behaviour."

  - id: cl-view.tilt.death_idle
    name: "Death tilt (v_deathtilt) + idle view-wave (v_idlescale)"
    layer: [presentation]
    base_refs:
      - "lib/csqcmodel/cl_player.qc:CSQCPlayer_ApplyDeathTilt (278)"
      - "lib/csqcmodel/cl_player.qc:CSQCPlayer_ApplyIdleScaling (296)"
    port_refs:
      - "game/client/FirstPersonView.cs:ApplyDeathTilt (470)"
      - "game/client/FirstPersonView.cs:ApplyIdleScaling (493)"
    constants:
      - { name: "v_deathtilt", base: "0 (off by default)", port: "0 (read live)", match: true }
      - { name: "v_deathtiltangle", base: "80", port: "80 (fallback)", match: true }
      - { name: "v_idlescale", base: "0 (off by default)", port: "0 (read live)", match: true }
      - { name: "v_ipitch_cycle/_iyaw_cycle/_iroll_cycle", base: "1 / 2 / 0.5", port: "1 / 2 / 0.5 (fallback)", match: true }
      - { name: "v_ipitch_level/_iyaw_level/_iroll_level", base: "0.3 / 0.3 / 0.1", port: "0.3 / 0.3 / 0.1 (fallback)", match: true }
    status:
      logic: faithful
      values: faithful
      timing: faithful
      presentation: faithful
      audio: na
      liveness: live
    intended_divergence: false
    divergence_rationale: null
    gaps: []
    verification:
      - { kind: code, ref: "ApplyDeathTilt ports CSQCPlayer_ApplyDeathTilt (min((time-death_time)*8,1)*v_deathtiltangle, bails under cl_eventchase_death 2 default); ApplyIdleScaling ports CSQCPlayer_ApplyIdleScaling (sin-wave pitch/yaw/roll). Death edge tracked via _deathTime/_wasDead; idle runs after the chase split (FirstPersonView:398). Live on the net path.", result: pass }
    confidence: high
    notes: "FIXED since the 2026-06-22 baseline (was missing): both death-tilt and idle-wave are now ported. Both off by default in stock Xonotic (v_deathtilt 0 / v_idlescale 0) and v_deathtilt is QC-skipped under the default cl_eventchase_death 2, so a stock match shows neither — opt-in only. The death-cam incompatibility guard is faithfully reproduced."

  - id: cl-view.smooth.stair_viewheight
    name: "Stair + viewheight smoothing (stairsmoothz glide + viewheightavg blend)"
    layer: [presentation]
    base_refs:
      - "lib/csqcmodel/cl_player.qc:CSQCPlayer_ApplySmoothing (227)"
    port_refs:
      - "src/XonoticGodot.Net/FaithfulViewSmoothing.cs"
      - "game/net/NetGame.cs:5085-5098 (faithfulSmoothing path) / 5111 (port-mode adaptive offset)"
    constants:
      - { name: "cl_stairsmoothspeed", base: "200", port: "200", match: true }
      - { name: "cl_smoothviewheight", base: "0.05", port: "0.05", match: true }
      - { name: "PHYS_STEPHEIGHT (sv_stepheight)", base: "31", port: "31", match: true }
    status:
      logic: faithful
      values: faithful
      timing: faithful
      presentation: faithful
      audio: na
      liveness: partial
    intended_divergence: true
    divergence_rationale: "Faithful mode (cl_movement_smoothing_faithful, default on) uses the exact bounded stairsmoothz glide + viewheightavg blend; the port-default-mode alternative uses an adaptive render-only stair offset (ClientNet.PredictedStairOffset) to fight the 1/72-quantized server-render-clock jitter (see camera-drift-render-smoothing memory). The stepheight processing is the only intentional divergence in the faithful path."
    gaps: []
    verification:
      - { kind: code, ref: "FaithfulViewSmoothing.Apply mirrors cl_player.qc CSQCPlayer_ApplySmoothing (bound() glide + lowpass eye blend, teleport/airborne/networked-ground snaps); wired live NetGame:5085-5098 when faithfulSmoothing; port-mode adaptive offset (NetGame:5111) is the deliberate alternative", result: pass }
    confidence: high
    notes: "Two paths: faithful (bit-faithful transcription) and the adaptive port-default. liveness: partial because only one path runs at a time per cl_movement_smoothing_faithful."

  - id: cl-view.screen.damage_flash
    name: "Damage red screen flash (HUD_Damage)"
    layer: [presentation]
    base_refs:
      - "client/view.qc:HUD_Damage (1237)"
    port_refs:
      - "game/client/ViewEffects.cs:UpdateDamage (177)"
      - "game/client/ViewEffects.cs:SetSplatAlpha (286, gfx/blood splatter)"
      - "game/client/ViewEffects.cs:MakeBloodSplat (114)"
      - "game/net/NetGame.cs:2949 (UpdateEffects)"
    constants:
      - { name: "hud_damage", base: "0.55", port: "0.55", match: true }
      - { name: "hud_damage_factor", base: "0.025", port: "0.025", match: true }
      - { name: "hud_damage_fade_rate", base: "0.75", port: "0.75", match: true }
      - { name: "hud_damage_maxalpha", base: "1.5", port: "1.5", match: true }
      - { name: "hud_damage_pain_threshold", base: "0.1", port: "0.1", match: true }
      - { name: "hud_damage_color", base: "'1 0 0'", port: "'1 0 0'", match: true }
      - { name: "hud_damage_pain_threshold_lower", base: "1.25", port: "1.25", match: true }
      - { name: "hud_damage_pain_threshold_lower_health", base: "50", port: "50", match: true }
      - { name: "hud_damage_pain_threshold_pulsating_min", base: "0.6", port: "0.6", match: true }
      - { name: "hud_damage_pain_threshold_pulsating_period", base: "0.8", port: "0.8", match: true }
      - { name: "hud_damage_gentle_alpha_multiplier", base: "0.10", port: "0.10", match: true }
      - { name: "hud_damage_gentle_color", base: "'1 0.7 1'", port: "'1 0.7 1'", match: true }
      - { name: "cl_gentle", base: "0", port: "0 (read live)", match: true }
      - { name: "cl_gentle_damage", base: "0", port: "0 (read live)", match: true }
    status:
      logic: faithful
      values: faithful
      timing: faithful
      presentation: faithful
      audio: na
      liveness: live
    intended_divergence: false
    divergence_rationale: null
    gaps: []
    verification:
      - { kind: code, ref: "UpdateDamage: myhealth_flash accumulate/decay, pain threshold + near-death sin-pulse, respawn/observe clears; the default path now draws the gfx/blood splatter (SetSplatAlpha: square max(conwidth,conheight) centred, tinted hud_damage_color) and the gentle path (cl_gentle/cl_gentle_damage) draws the solid coloured rect with gentle_alpha_multiplier + the cl_gentle_damage==2 randomized colour — matching HUD_Damage view.qc:1293-1305; live via NetGame:2949 UpdateEffects.", result: pass }
    confidence: high
    notes: "FIXED since the 2026-06-22 baseline (was presentation: partial): the default damage flash now uses the gfx/blood splatter texture (centred square, max(conwidth,conheight), tinted hud_damage_color) instead of a flat red rect, with a solid-rect fallback only when gfx/blood is missing; AND the gentle-mode (cl_gentle / cl_gentle_damage) solid-flash path is ported (alpha * gentle_alpha_multiplier; cl_gentle_damage==2 randomizes the colour while the flash is gone). Accumulator + pain-threshold + near-death sin-pulse + respawn/observe edge handling all match. Both former presentation gaps closed."

  - id: cl-view.screen.liquid_tint
    name: "Liquid screen tint (HUD_Contents)"
    layer: [presentation]
    base_refs:
      - "client/view.qc:HUD_Contents (1167)"
    port_refs:
      - "game/client/ViewEffects.cs:UpdateContents"
      - "game/client/FirstPersonView.cs:EyeContents"
    constants:
      - { name: "hud_contents_water_alpha", base: "0.5", port: "0.5", match: true }
      - { name: "hud_contents_lava_alpha", base: "0.7", port: "0.7", match: true }
      - { name: "hud_contents_slime_alpha", base: "0.7", port: "0.7", match: true }
      - { name: "hud_contents_fadeintime", base: "0.02", port: "0.02", match: true }
      - { name: "hud_contents_fadeouttime", base: "0.25", port: "0.25", match: true }
      - { name: "water_color", base: "'0.4 0.6 1.0'", port: "'0.4 0.6 1.0'", match: true }
      - { name: "lava_color", base: "'1.0 0.3 0.0'", port: "'1.0 0.3 0.0'", match: true }
      - { name: "slime_color", base: "'0.0 0.8 0.0'", port: "'0.0 0.8 0.0'", match: true }
    status:
      logic: faithful
      values: faithful
      timing: faithful
      presentation: faithful
      audio: na
      liveness: live
    intended_divergence: false
    divergence_rationale: null
    gaps: []
    verification:
      - { kind: code, ref: "UpdateContents: contentavgalpha low-pass fade in/out + per-content colour + colour latch while fading out; eye contents sampled at final render origin (FirstPersonView:416)", result: pass }
    confidence: high
    notes: "Eye contents sampled at the FINAL render origin (the pulled-back cam during chase) matching pointcontents(view_origin), view.qc:1176."

  - id: cl-view.screen.frozen_overlay
    name: "Freeze-Tag icy screen overlay (cl_ft HUD_Draw_overlay)"
    layer: [presentation]
    base_refs:
      - "common/gametypes/gametype/freezetag/cl_freezetag.qc:HUD_Draw_overlay (29-44)"
    port_refs:
      - "game/client/ViewEffects.cs:UpdateFrozenOverlay (292)"
      - "game/net/NetGame.cs:~2955-2961 (UpdateFrozenOverlay off FreezeTag Frozen status + ReviveProgress)"
    constants:
      - { name: "base_color", base: "'0.25 0.90 1'", port: "'0.25 0.90 1'", match: true }
      - { name: "col_fade", base: "max(0, REVIVE_PROGRESS*2 - 1)", port: "max(0, p*2-1)", match: true }
      - { name: "alpha_fade", base: "0.3 + 0.7*(1 - max(0, REVIVE_PROGRESS*4 - 3))", port: "0.3 + 0.7*(1 - max(0, p*4-3))", match: true }
      - { name: "col_warm", base: "col += (col_fade, -col_fade, -col_fade)", port: "(0.25+cf, 0.90-cf, 1-cf)", match: true }
    status:
      logic: faithful
      values: faithful
      timing: faithful
      presentation: faithful
      audio: na
      liveness: live
    intended_divergence: false
    divergence_rationale: null
    gaps: []
    verification:
      - { kind: code, ref: "UpdateFrozenOverlay mirrors HUD_Draw_overlay exactly: base '0.25 0.90 1', col_fade/alpha_fade formulas + colour-warm; gated on STAT(FROZEN). Wired live NetGame:~2958 off the host-side local Player Frozen status effect + REVIVE_PROGRESS. A pure remote client reads frozen=false (no overlay).", result: pass }
    confidence: high
    notes: "Full-screen ColorRect composited above the damage + liquid tints. Faithful icy-blue tint that warms toward white and fades out as the thaw ring (STAT(REVIVE_PROGRESS)) fills. Driven by the live FreezeTag gametype, same source as the crosshair thaw ring. liveness: live only in Freeze Tag (the gametype gates STAT(FROZEN)); a pure remote (non-host) client has no LocalServerPlayer so reads false — the cross-client wepent/stat follow-up is the same limitation noted on the crosshair revive ring."

  - id: cl-view.screen.postprocess_nightvision
    name: "GLSL blur/sharpen post-process + night-vision + blur-test"
    layer: [presentation]
    base_refs:
      - "client/view.qc:View_PostProcessing (1326)"
      - "client/view.qc:View_NightVision (1113)"
      - "client/view.qc:View_BlurTest (1453)"
    port_refs: []
    constants:
      - { name: "hud_postprocessing", base: "1 (master)", port: "none", match: false }
      - { name: "hud_damage_blur", base: "1 (effect)", port: "none", match: false }
      - { name: "hud_powerup", base: "0 (sharpen)", port: "none", match: false }
    status:
      logic: missing
      values: missing
      timing: missing
      presentation: missing
      audio: na
      liveness: na
    intended_divergence: true
    divergence_rationale: "The damage/content blur, powerup edge-sharpen and blur-test drive engine GLSL shader cvars (r_glsl_postprocess + r_glsl_postprocess_uservec1/2) with no Godot analogue; ViewEffects deliberately ships only the coloured screen reaction (documented in ViewEffects.cs class header). Night-vision is a fullbright/fakelight debug aid, not a normal-match feature."
    gaps:
      - "No screen blur when damaged / underwater, no powerup edge-sharpen, no night-vision tint, no blur-test debug effect."
    verification:
      - { kind: grep, ref: "no postprocess/nightvision/blurtest implementation; 'postprocess' appears only in ViewEffects.cs (class doc stating intentional omission)", result: fail }
    confidence: high
    notes: "Documented intended omission in ViewEffects.cs. hud_powerup defaults 0 so the sharpen is off in stock anyway; the damage/content blur is the only default-on effect lost (a subtle blur under heavy damage / underwater)."

  - id: cl-view.reticle.zoom_reticle
    name: "Zoom reticle (generic +zoom + weapon scope)"
    layer: [presentation]
    base_refs:
      - "client/hud/crosshair.qc:DrawReticle (648-717)"
    port_refs:
      - "game/client/ReticleOverlay.cs:UpdateReticle (75)"
      - "game/net/NetGame.cs:2917-2919 (UpdateReticle, passes _view.ZoomScriptCaught)"
    constants:
      - { name: "cl_reticle", base: "1", port: "1", match: true }
      - { name: "cl_reticle_stretch", base: "0", port: "0", match: true }
      - { name: "cl_reticle_chase", base: "0", port: "0", match: true }
      - { name: "cl_reticle_weapon", base: "1", port: "1", match: true }
      - { name: "cl_reticle_normal_alpha", base: "1", port: "1", match: true }
      - { name: "reticle_fade_floor", base: "zoomscript_caught ? 1 : max(0.25, zoomfraction)", port: "zoomScriptCaught ? 1 : Max(0.25, zoomFraction)", match: true }
    status:
      logic: faithful
      values: faithful
      timing: faithful
      presentation: faithful
      audio: na
      liveness: live
    intended_divergence: false
    divergence_rationale: null
    gaps: []
    verification:
      - { kind: code, ref: "UpdateReticle: type selection (generic/weapon), zoomscript_caught full-alpha vs 0.25 fade floor (ReticleOverlay:107,118), dead/spectate/chase suppression (unless cl_reticle_chase), stretch vs square geometry; live NetGame:2917-2919 (now passes _view.ZoomScriptCaught)", result: pass }
    confidence: high
    notes: "Type selection (none/generic/weapon-scope), the zoomscript_caught ? 1 : max(0.25, zoomfraction) fade, the square-centred vs stretched geometry, and the suppression conditions all match DrawReticle. The prior zoomscript_caught gap is CLOSED — the zoomscript auto-zoom (FirstPersonView.ZoomScriptCaught) now feeds the full-alpha branch."

  - id: cl-view.viewmodel.sway
    name: "Viewmodel sway (cl_followmodel / cl_leanmodel / cl_bobmodel)"
    layer: [presentation]
    base_refs:
      - "client/view.qc:viewmodel_animate (258)"
      - "client/view.qc:calc_followmodel_ofs (113)"
      - "client/view.qc:leanmodel_ofs (167)"
      - "client/view.qc:bobmodel_ofs (203)"
    port_refs:
      - "game/client/ViewModel.cs:UpdateSway (639)"
      - "game/client/ViewModel.cs:FollowModelOffset (697)"
      - "game/client/ViewModel.cs:LeanModelOffset (730)"
      - "game/client/ViewModel.cs:BobModelOffset (765)"
      - "game/net/NetGame.cs:1379 (ViewStateProvider = BuildViewState)"
    constants:
      - { name: "cl_followmodel", base: "1", port: "true", match: true }
      - { name: "cl_followmodel_speed", base: "0.3", port: "0.3", match: true }
      - { name: "cl_followmodel_limit", base: "135", port: "135", match: true }
      - { name: "cl_followmodel_velocity_lowpass", base: "0.05", port: "0.05", match: true }
      - { name: "cl_followmodel_highpass", base: "0.05", port: "0.05", match: true }
      - { name: "cl_followmodel_lowpass", base: "0.03", port: "0.03", match: true }
      - { name: "cl_leanmodel", base: "1", port: "true", match: true }
      - { name: "cl_leanmodel_speed", base: "0.3", port: "0.3", match: true }
      - { name: "cl_leanmodel_limit", base: "30", port: "30", match: true }
      - { name: "cl_leanmodel_highpass1", base: "0.2", port: "0.2", match: true }
      - { name: "cl_leanmodel_highpass", base: "0.2", port: "0.2", match: true }
      - { name: "cl_leanmodel_lowpass", base: "0.05", port: "0.05", match: true }
      - { name: "cl_bobmodel", base: "1", port: "true", match: true }
      - { name: "cl_bobmodel_side", base: "0.2", port: "0.2", match: true }
      - { name: "cl_bobmodel_up", base: "0.1", port: "0.1", match: true }
      - { name: "cl_bobmodel_speed", base: "10", port: "10", match: true }
    status:
      logic: faithful
      values: faithful
      timing: faithful
      presentation: faithful
      audio: na
      liveness: live
    intended_divergence: false
    divergence_rationale: null
    gaps:
      - "Sway constants are [Export] fields with hardcoded defaults rather than live cl_*model_* cvar reads, so changing those cvars at runtime has no effect (values still match the shipped defaults)."
      - "cl_followmodel_velocity_absolute branch (calc_followmodel_ofs:125,152-161) is not ported (default 0, so the view-relative branch — which IS ported — is the default path)."
      - "UpdateSway does not gate on chase/dead like viewmodel_animate:260 (autocvar_chase_active || HEALTH<=0), but the whole viewmodel node is hidden during chase/dead, so the sway is not observable then anyway."
    verification:
      - { kind: code, ref: "UpdateSway ports avg_factor/lowpass/highpass macro chains + bobmodel time-offset + map_bound_ranges; ViewStateProvider=BuildViewState live (NetGame:1379); constants match xonotic-client.cfg", result: pass }
    confidence: high
    notes: "Faithful low/high-pass chains, the bob run-up scale, and all constants match. Divergences: cvar liveness (defaults baked as Exports) and the unported velocity_absolute opt-in branch — both default-equivalent."

  - id: cl-view.demo.free_camera_lockview_ortho
    name: "Demo free camera + lockview + orthoview + viewloc + FPS report"
    layer: [presentation]
    base_refs:
      - "client/view.qc:CSQC_Demo_Camera (1896)"
      - "client/view.qc:View_Lock (1395)"
      - "client/view.qc:View_Ortho (1568)"
      - "client/view.qc:GetViewLocationFOV (612)"
      - "client/view.qc:fpscounter_update (398)"
    port_refs:
      - "game/client/FirstPersonView.cs:ApplyViewLock (825) — View_Lock port (cl_lockview only)"
      - "game/client/FirstPersonView.cs:UpdateCamera (ApplyViewLock call, 413)"
    constants:
      - { name: "cl_lockview", base: "0 (0=off; 1=freeze origin+angles; 2=freeze origin only)", port: "0 (read live; lock_type 1/2 both ported)", match: true }
      - { name: "cl_orthoview", base: "0", port: "none", match: false }
    status:
      logic: partial
      values: partial
      timing: partial
      presentation: partial
      audio: na
      liveness: partial
    intended_divergence: false
    divergence_rationale: null
    gaps:
      - "Demo free/chase camera (CSQC_Demo_Camera) not ported (tracked separately on the demo-cinematics branch)."
      - "Orthographic radar-render view (cl_orthoview / View_Ortho) not ported (dev/radar tool)."
      - "viewloc 2.5D side-scroller camera (GetViewLocationFOV) not ported (niche 2.5D-map feature)."
      - "FPS report to the server (fpscounter_update STAT(SHOWFPS)) not ported."
      - "cl_lockview: only the bare cvar lock_type is honored; the UI-forced lock_type=1 conditions (hud_configure / clickable radar / minigame menu / quickmenu) are not on the net play path so they are not modelled (documented in ApplyViewLock)."
    verification:
      - { kind: code, ref: "cl_lockview is now ported: FirstPersonView.ApplyViewLock (FirstPersonView:825) freezes the final Quake render origin/angles (lock_type>=1 pins origin to freeze_org; ==1 also pins angles to freeze_ang; ==2 origin-only with continuous capture while off), called every frame from UpdateCamera:413 (live on the net path via UpdateView). The other 4 features (demo cam / orthoview / viewloc / FPS report) remain unimplemented in src/ or game/.", result: pass }
    confidence: high
    notes: "PARTIAL FIX since the 2026-06-27 baseline: cl_lockview (View_Lock) is now ported and live — it freezes the rendered view at the captured origin/angles (lock_type 1=both, 2=origin-only), read live each frame from the cvar (default 0=off so a stock match is unaffected). The remaining four features (demo free camera, orthoview radar render, viewloc 2.5D camera, server FPS report) are still unported — none are on the normal-match camera path. Status is partial (was missing) because this row bundles five features and only lockview landed."

  - id: cl-view.zoom.header_defaults
    name: "view.qh bare autocvar defaults gating live zoom paths (spawnzoom / velocityzoom_type / zoomscroll)"
    layer: [presentation]
    base_refs:
      - "client/view.qh:23-25 (cl_spawnzoom 1 / _speed 1 / _factor 2)"
      - "client/view.qh:29 (cl_velocityzoom_type 3)"
      - "client/view.qh:35-37 (cl_zoomscroll 1 / _scale 0.2 / _speed 16)"
      - "client/spawnpoints.qc:85 (spawnzoom_factor bound 1..16 at the seed site)"
    port_refs:
      - "game/net/NetGame.cs:1769-1771 (SeedViewCvars registers cl_spawnzoom 1 / _speed 1 / _factor 2)"
      - "game/client/FirstPersonView.cs:221-224 (spawnzoom read, factor bound 1..16 matching spawnpoints.qc:85)"
      - "game/client/FirstPersonView.cs:256-257 + 324-335 (cl_zoomscroll 1 / 0.2 / 16 read live)"
      - "game/client/FirstPersonView.cs:545-552 (cl_velocityzoom_type fallback 3)"
    constants:
      - { name: "cl_spawnzoom / _speed / _factor", base: "1 / 1 / 2", port: "1 / 1 / 2 (seeded + matching fallbacks)", match: true }
      - { name: "cl_velocityzoom_type", base: "3", port: "3 (read-live fallback)", match: true }
      - { name: "cl_zoomscroll / _scale / _speed", base: "1 / 0.2 / 16", port: "1 / 0.2 / 16 (read-live fallbacks)", match: true }
    status:
      logic: na
      values: faithful
      timing: na
      presentation: na
      audio: na
      liveness: live
    intended_divergence: false
    divergence_rationale: null
    gaps: []
    verification:
      - { kind: code, ref: "port fallbacks/seeds re-read at the cited lines; behavior itself audited in cl-view.zoom.spawn_zoom / velocity_zoom / zoom_scroll (all live)", result: pass }
    confidence: high
    notes: "Declaration-surface row for client/view.qh: the header's bare autocvar defaults are exactly the values the live port paths seed or fall back to. Behavioral audits live in the owning rows (zoom.spawn_zoom / zoom.velocity_zoom / zoom.zoom_scroll). The header's other declaration families are owned elsewhere: camera_* -> cl-view.demo.camera_cvars; hud_contents_blur*/hud_damage_blur*/hud_postprocessing_* -> cl-view.screen.postprocess_nightvision (intended omission); cl_eventchase_vehicle* -> the vehicle-chase gap in cl-view.chase.event_death_cam; cl_hitsound_* -> the hitsound/sound units. added 2026-07-02 (coverage fold)"

  - id: cl-view.zoom.unpress_defaults
    name: "Zoom/attack unpress-on-event defaults (cl_unpress_zoom_on_spawn/death/weapon_switch, cl_unpress_attack_on_weapon_switch)"
    layer: [presentation]
    base_refs:
      - "client/main.qh:7-10 (defaults: true / true / true / false)"
      - "client/spawnpoints.qc:88-92 (localcmd -zoom + button_zoom=false on own spawn)"
      - "client/view.qc:View_CheckButtonStatus (1483 death, 1531-1536 weapon switch)"
    port_refs:
      - "game/net/NetGame.cs:3106 (zoomActive = ... Health > 0 — continuous dead-suppression standing in for unpress-on-death)"
      - "game/menu/dialogs/DialogSettingsGame.cs:573-577, 620 (menu checkboxes exist; no gameplay reader)"
    constants:
      - { name: "cl_unpress_zoom_on_spawn", base: "true (forced -zoom release on own spawn)", port: "none (ZoomHeld re-derived from raw key state; no forced release)", match: false }
      - { name: "cl_unpress_zoom_on_death", base: "true", port: "approximated (Health>0 gate zooms out while dead)", match: true }
      - { name: "cl_unpress_zoom_on_weapon_switch", base: "true", port: "none", match: false }
      - { name: "cl_unpress_attack_on_weapon_switch", base: "false", port: "none (default-off in Base too)", match: true }
    status:
      logic: partial
      values: partial
      timing: na
      presentation: na
      audio: na
      liveness: partial
    intended_divergence: false
    divergence_rationale: null
    gaps:
      - "No forced -zoom release on spawn: the port recomputes ZoomHeld from live key state each frame, so a player physically holding +zoom across a respawn stays zoomed (Base with cl_unpress_zoom_on_spawn 1 releases the button and requires a re-press; it also lets the spawn-zoom ease play uncancelled)."
      - "Weapon-switch unpress (zoom + attack buttons) absent — same gap already noted in cl-view.zoom.zoomscript_buttonstatus."
      - "All four cvars have menu checkboxes (DialogSettingsGame) but zero gameplay readers, so toggling them changes nothing."
    verification:
      - { kind: code, ref: "grep unpress over src/+game/: only menu widgets + comments; NetGame:3106 is the sole death-side analogue; no -zoom-on-spawn logic near TriggerSpawnZoom (NetGame:3092-3096)", result: fail }
    confidence: medium
    notes: "main.qh carries the defaults; the on-death suppression is behaviorally equivalent for the common case (zoom always drops while dead) but is a continuous gate, not a one-shot button release. The on-spawn divergence is observable only while deliberately holding +zoom through a respawn. Two of these constants were already tracked in cl-view.zoom.zoomscript_buttonstatus; this row owns the main.qh defaults + the spawn branch (spawnpoints.qc:88). added 2026-07-02 (coverage fold)"

  - id: cl-view.demo.camera_cvars
    name: "Demo free-camera cvar family (camera_enable/free/chase_smoothly/speed_*/look_*/mouse_threshold/reset)"
    layer: [presentation]
    base_refs:
      - "client/view.qh:5-16 (camera_* autocvar declarations)"
      - "client/view.qc:CSQC_Demo_Camera (1896, sole consumer)"
    port_refs: []
    constants:
      - { name: "camera_enable", base: "0 (off; demo playback only)", port: "none", match: false }
    status:
      logic: missing
      values: missing
      timing: missing
      presentation: missing
      audio: na
      liveness: na
    intended_divergence: false
    divergence_rationale: null
    gaps:
      - "The entire camera_* demo free-camera cvar surface (enable/free/chase_smoothly/speed_chase/speed_free/speed_attenuation/speed_roll/look_player/look_attenuation/mouse_threshold/reset) has no port counterpart — grep over src/+game/ finds no reader."
    verification:
      - { kind: grep, ref: "camera_free|camera_speed|camera_chase_smoothly|camera_enable -> zero matches in port .cs", result: fail }
    confidence: high
    notes: "Declaration surface of CSQC_Demo_Camera, whose absence is already tracked in cl-view.demo.free_camera_lockview_ortho; this row adds the view.qh cvar family citation. Demo replay is being built separately on the demo-cinematics branch (T62-T66) — the free camera belongs there. Only reachable during demo playback, never in a normal match. added 2026-07-02 (coverage fold)"

  - id: cl-view.spawn.event_effects
    name: "Spawn-event presentation (spawn flash particles + spawn sound + spawn-point idle glow, cl_spawn_* gating)"
    layer: [presentation]
    base_refs:
      - "client/spawnpoints.qh:3-6 (cl_spawn_event_particles / cl_spawn_event_sound true / cl_spawn_point_particles / cl_spawn_point_dist_max 1200)"
      - "client/spawnpoints.qc:Spawn_Draw (8-22, spawn-point idle glow + dist cull)"
      - "client/spawnpoints.qc:NET_HANDLE(ENT_CLIENT_SPAWNEVENT) (40-95, team-colored spawn flash + SND_SPAWN)"
    port_refs:
      - "game/client/SpawnPointParticles.cs:65, 90 (reads cl_spawn_point_particles + cl_spawn_point_dist_max live)"
      - "game/client/ClientWorld.cs:451-452 (SpawnPointParticles node added — live)"
      - "src/XonoticGodot.Common/Gameplay/Player/SpawnSystem.cs:849-861 (team-colored SPAWN burst + SND SPAWN on CH_TRIGGER, gated on g_spawn_alloweffects)"
      - "game/menu/framework/ClientSettings.cs:189-194 (registers cl_spawn_point_particles 1 / cl_spawn_event_particles 1 / cl_spawn_point_dist_max 1200)"
    constants:
      - { name: "cl_spawn_point_particles", base: "1 (xonotic-client.cfg:76; effects-low/med presets set 0)", port: "1 (registered; read live)", match: true }
      - { name: "cl_spawn_point_dist_max", base: "1200 (cull glow beyond 1200qu; 0 = no cull)", port: "1200 (registered 2026-07-03; read live, 0 = no cull)", match: true }
      - { name: "cl_spawn_event_particles", base: "1", port: "1 (registered, but NO reader — burst always renders)", match: true }
      - { name: "cl_spawn_event_sound", base: "1 (true, spawnpoints.qh:4)", port: "none (sound gated server-side only)", match: false }
      - { name: "g_spawn_alloweffects", base: "3 (particles|sound)", port: "3 (CvarOr fallback, SpawnSystem:860)", match: true }
    status:
      logic: partial
      values: partial
      timing: partial
      presentation: partial
      audio: partial
      liveness: live
    intended_divergence: false
    divergence_rationale: null
    gaps:
      - "cl_spawn_event_particles is registered (default 1, matching) but has no reader — the spawn flash renders regardless, so turning it off does nothing (knob dead; default-equivalent)."
      - "cl_spawn_event_sound has no port counterpart — the spawn sound is gated only by the server's g_spawn_alloweffects bit1 (SpawnSystem models the client cvar as always-on); a client cannot locally mute spawn sounds."
      - "Spawn-point glow pulses on a 0.2s accumulator instead of Base's per-frame frametime-scaled boxparticles (documented approximation in the file header; since 2026-07-03 each pulse passes count = PulseInterval so the average per-point rate matches Base's frametime scaling — count 1 was a 5x overspawn)."
    verification:
      - { kind: code, ref: "SpawnPointParticles._Process: cl_spawn_point_particles gate (65), distMax read live (registered 1200 in ClientSettings, fixed 2026-07-03 — was unregistered -> 0 -> no cull); SpawnSystem:849-861 burst+sound live on the server spawn path; team coloring matches Team_ColorRGB / entcs color split", result: pass }
    confidence: high
    notes: "Both halves are live: the idle spawn-point glow (Spawn_Draw port, team-colored, added by ClientWorld) and the spawn flash + sound (modeled SERVER-side in SpawnSystem instead of the ENT_CLIENT_SPAWNEVENT client entity — the client-side cvar gating collapses, which is what kills the two client toggles). spawnpoints.qc:82-92 local spawn actions (spawn-zoom seed, unpress-zoom) are owned by cl-view.zoom.spawn_zoom / cl-view.zoom.unpress_defaults; the cl_autodemo_delete branch (75-79) belongs to the demo-cinematics track. added 2026-07-02 (coverage fold)"

  - id: cl-view.race.cptimes_defaults
    name: "Race checkpoint-times display defaults (cl_race_cptimes_* AUTOCVAR_SAVE set + splits-console print)"
    layer: [presentation]
    base_refs:
      - "client/main.qh:24-30 (AUTOCVAR_SAVE cl_race_checkpoint_splits_console 1 / cptimes_showspeed 0 / _showspeed_unit 1 / _namesize 10; bare _onlyself / _showself 0)"
    port_refs:
      - "game/hud/RaceTimerPanel.cs:141-144 (registers showspeed 0 / showspeed_unit 1 / showself 0 / namesize 10)"
      - "game/hud/RaceTimerPanel.cs:259, 344-349, 437 (live reads in the race-timer draw path)"
    constants:
      - { name: "cl_race_checkpoint_splits_console", base: "1 (print checkpoint splits to console)", port: "none", match: false }
      - { name: "cl_race_cptimes_showspeed", base: "0", port: "0 (registered + read)", match: true }
      - { name: "cl_race_cptimes_showspeed_unit", base: "1", port: "1 (registered + read)", match: true }
      - { name: "cl_race_cptimes_namesize", base: "10", port: "10 (registered + read)", match: true }
      - { name: "cl_race_cptimes_showself", base: "0", port: "0 (registered + read)", match: true }
      - { name: "cl_race_cptimes_onlyself", base: "false (bare)", port: "none (no reader)", match: false }
    status:
      logic: partial
      values: partial
      timing: na
      presentation: partial
      audio: na
      liveness: live
    intended_divergence: false
    divergence_rationale: null
    gaps:
      - "cl_race_checkpoint_splits_console (default 1 — checkpoint splits printed to the console every lap) has no port counterpart: splits render only in the HUD panels."
      - "cl_race_cptimes_onlyself (suppress other players' checkpoint messages) has no port reader — default-off in Base, so default-equivalent, but the opt-in filter is absent."
    verification:
      - { kind: code, ref: "RaceTimerPanel.cs:141-144 registers the four cptimes cvars with Base defaults and reads them in the draw path (259/344-349/437); grep checkpoint_splits_console over port .cs -> zero matches", result: pass }
    confidence: medium
    notes: "Defaults row for the main.qh race-display autocvars (the AUTOCVAR_SAVE branch is the live one; the #if 0 branch is dead upstream). Behavioral audit of the race checkpoint messages/panels belongs to the race unit (registry/race.yaml) + hud-panels — cross-ref; this row pins the main.qh defaults and the two missing knobs. liveness: live only in race/cts (the panel is gametype-gated). added 2026-07-02 (coverage fold)"

  - id: cl-view.client.welcome_dialog
    name: "Welcome dialog on connect (cl_welcome gate + Welcome_Message_Show_Try)"
    layer: [presentation]
    base_refs:
      - "client/main.qh:31 (cl_welcome = true)"
      - "client/main.qc:Welcome_Message_Show_Try (1455-1476; gated on intermission/isdemo/cl_welcome)"
      - "client/view.qc:1808 (per-frame show-try call)"
    port_refs:
      - "game/menu/dialogs/DialogWelcome.cs (faithful UI port of dialog_welcome.qc — documented INERT: placeholder body, no connect backend)"
      - "game/Shell.cs:184 (menu route \"welcome\")"
      - "src/XonoticGodot.Server/GameWorld.cs:1397-1554 (welcome CONTENT — campaign line / mutators / MOTD — rerouted to per-player console prints)"
    constants:
      - { name: "cl_welcome", base: "1 (show Welcome dialog when connecting)", port: "none (cvar unregistered, no reader)", match: false }
    status:
      logic: stub
      values: missing
      timing: na
      presentation: partial
      audio: na
      liveness: partial
    intended_divergence: false
    divergence_rationale: null
    gaps:
      - "The cl_welcome-gated auto-show on connect is not wired: nothing opens DialogWelcome when joining a server; it is reachable only via the menu route."
      - "DialogWelcome's body is a placeholder — the server-supplied hostname/MOTD/welcome blob (net_handle_ServerWelcome) has no networked channel; the welcome content is instead surfaced as console prints (GameWorld SendCampaignWelcome/SendMutatorsWelcome — see campaign-flow rows)."
    verification:
      - { kind: code, ref: "DialogWelcome.cs header documents INERT status (no connect backend); grep cl_welcome over port .cs -> zero matches; Shell.cs:184 is the only instantiation site", result: fail }
    confidence: medium
    notes: "main.qh's cl_welcome + the main.qc show-try loop are the client half of the server SendWelcomeMessage seam already audited in GameWorld.cs (welcome content rerouted to console prints as a documented substitution). The dialog UI itself is a faithful widget port awaiting the networked welcome channel. Remaining main.qh content is infrastructure (spectatee_status/zoom globals owned by the rows above; hud_showbinds -> HUD keybind display; cl_autodemo_* -> demo-cinematics track; menu_gamemenu -> menu unit). added 2026-07-02 (coverage fold)"
