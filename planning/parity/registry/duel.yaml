unit: duel
category: gametype
base_ref_rev: "v0.8.6-1779-g863cd3e84"
last_audited: "2026-06-22"
summary:
  feature_count: 9
  worst_gaps:
    - "2-player (1v1) cap STILL not enforced on EITHER path: a 3rd+ client can join (no free-slot join gate), and bot-fill still reads g_maxplayers not the duel cap (BotPopulation.cs:251)"
    - "no 'playerA vs playerB' duel centerprint title on the live path (SetDuelTitle still has no caller)"
    - "map-support gating (diameter<3250 / force on DM maps) still unimplemented"
features:
  - id: duel.identity.registration
    name: "Duel gametype identity + default match limits"
    layer: [shared]
    base_refs:
      - "common/gametypes/gametype/duel/duel.qh:INIT(Duel)"
      - "common/gametypes/gametype/duel/duel.qh:REGISTER_GAMETYPE(DUEL)"
    port_refs:
      - "src/XonoticGodot.Common/Gameplay/GameTypes/Duel.cs:Duel"
      - "src/XonoticGodot.Server/GameWorld.cs:ResolveGameType"
    constants:
      - { name: "netname", base: "duel", port: "duel", match: true }
      - { name: "flags", base: "USEPOINTS|1V1", port: "TeamGame=false; 1v1 via netname", match: true }
      - { name: "timelimit", base: "10 (min)", port: "generic timelimit (not seeded per-gametype)", match: false }
      - { name: "pointlimit", base: "0 (no frag limit)", port: "DefaultFragLimit=0", match: true }
      - { name: "leadlimit", base: "0", port: "not modeled", match: false }
    status:
      logic: faithful
      values: partial
      timing: na
      presentation: na
      audio: na
      liveness: live
    intended_divergence: false
    divergence_rationale: null
    gaps:
      - "timelimit=10 / leadlimit=0 gametype-init defaults are not seeded per-gametype (timelimit handling is generic); pointlimit=0 is matched. UNCHANGED by Wave 1-2."
    verification:
      - { kind: code_read, ref: "duel.qh gametype_init vs Duel.cs ctor/OnInit + ResolveGameType; Duel.Activate dispatched live at GameWorld.cs:1466", result: pass }
    confidence: high
    notes: "Duel is selectable and resolves via GameTypes.ByName(\"duel\"); identity faithful. Match-limit metadata only partially seeded but pointlimit=0 (the duel-distinctive one) matches."

  - id: duel.rules.player_limit_2
    name: "Hard 1v1 player limit (GetPlayerLimit -> 2)"
    layer: [authority]
    base_refs:
      - "common/gametypes/gametype/duel/sv_duel.qc:MUTATOR_HOOKFUNCTION(duel, GetPlayerLimit)"
      - "server/client.qc:2155:GetPlayerLimit (if(g_duel) return 2 short-circuit)"
      - "server/client.qc:2192:nJoinAllowed (free_slots = max(0, player_limit - currentlyPlaying))"
      - "server/client.qc:1105:status advert; server/bot/scripting bot-fill uses GetPlayerLimit()"
    port_refs:
      - "src/XonoticGodot.Common/Gameplay/GameTypes/Duel.cs:50:PlayerLimit"
    constants:
      - { name: "player_limit", base: "2", port: "2 (const, STILL unenforced)", match: false }
    status:
      logic: stub
      values: faithful
      timing: na
      presentation: na
      audio: na
      liveness: dead
    intended_divergence: false
    divergence_rationale: null
    gaps:
      - "STILL OPEN after Wave 1-2. A 3rd+ client can still +jump-join a duel and play simultaneously: ClientManager.JoinAllowed (ClientManager.cs:276-285) STILL has no free-slot/player-limit gate (its own doc-comment at :273-275 says the version-mismatch/playban/g_maxping/queue/nJoinAllowed gates are 'deferred'); the Base free-slot cap (nJoinAllowed, client.qc:2192-2194) is unported, and Duel.PlayerLimit (=2) has no caller."
      - "Bot-fill is STILL uncapped in duel: BotPopulation.FixCount passes playerLimit: Cvars.Int(\"g_maxplayers\") (BotPopulation.cs:251) into TargetBotCount with NO duel->2 override, so bot fill ignores the duel cap. (TargetBotCount itself correctly clamps to playerLimit at BotPopulation.cs:292-293 — only the value fed in is wrong.) The free-slot status advert (client.qc:1105) is likewise not duel-aware. Duel.cs:28-31 explicitly records this as deferred."
    verification:
      - { kind: code_read, ref: "Duel.PlayerLimit refs are self-only (Duel.cs:19/49/50); ClientManager.JoinAllowed (ClientManager.cs:276-285) body has no player_limit/free_slots check; BotPopulation.cs:251 passes g_maxplayers not the duel cap", result: fail }
    confidence: high
    notes: "The const holds the correct value (2) but nothing reads it. Both player-facing enforcement points (join gate AND bot fill) still bypass it. Single most impactful gap — duel's defining rule is not enforced. NOT addressed by Wave 1-2."

  - id: duel.items.powerup_filter
    name: "Powerup item filter (block powerups unless g_duel_with_powerups)"
    layer: [authority]
    base_refs:
      - "common/gametypes/gametype/duel/sv_duel.qc:MUTATOR_HOOKFUNCTION(duel, FilterItemDefinition)"
    port_refs:
      - "src/XonoticGodot.Common/Gameplay/GameTypes/Duel.cs:OnFilterItemDefinition"
      - "src/XonoticGodot.Common/Gameplay/GameTypes/Duel.cs:WithPowerups"
      - "src/XonoticGodot.Common/Gameplay/GameTypes/Duel.cs:Activate"
      - "src/XonoticGodot.Common/Gameplay/Items/StartItem.cs:103:FilterItemDefinition.Call"
    constants:
      - { name: "g_duel_with_powerups", base: "0", port: "0 (read via WithPowerups, now live)", match: true }
    status:
      logic: faithful
      values: faithful
      timing: na
      presentation: na
      audio: na
      liveness: live
    intended_divergence: false
    divergence_rationale: null
    gaps: []
    verification:
      - { kind: code_read, ref: "Duel.Activate (Duel.cs:132-133) does MutatorHooks.FilterItemDefinition.Add(OnFilterItemDefinition); Duel.Activate dispatched live at GameWorld.cs:1466 in ActivateGameType (called from match start GameWorld.cs:521); the chain is consumed on the live item-spawn path at StartItem.SpawnInternal (StartItem.cs:103). OnFilterItemDefinition returns !WithPowerups for powerup classnames (item_strength/item_shield/item_invincible), matching the real powerup spawnfuncs (ItemSpawnFuncs.cs:60-68) and Base instanceOfPowerup.", result: pass }
    confidence: high
    notes: "FIXED by Wave 2 (was stub/dead). Now subscribed live the same way Mayhem/TeamMayhem/ClanArena register their item filters; args.Definition.ClassName is the spawned entity classname, set at StartItem.cs:79 BEFORE the hook fires, so the classname stand-in for instanceOfPowerup resolves correctly. Default g_duel_with_powerups 0 => powerups filtered out, matching Base. NOTE: the stale test FilterItemHookTests.DuelFilterItem_NotRegistered_PowerupStillSpawns (line 108) still documents the OLD dead behavior and only passes because it boots a plain server without activating the Duel gametype — harmless but misleading; out of scope to edit here."

  - id: duel.scoring.frag_matrix
    name: "Deathmatch frag/obituary scoring matrix (inherited)"
    layer: [authority]
    base_refs:
      - "common/gametypes/gametype/duel/duel.qh:CLASS(Duel, Gametype)"
      - "common/gametypes/gametype/deathmatch/deathmatch.qh"
      - "server/damage.qc:Obituary"
    port_refs:
      - "src/XonoticGodot.Common/Gameplay/GameTypes/Duel.cs:OnDeath"
      - "src/XonoticGodot.Server/GameWorld.cs:ActivateGameType"
    constants:
      - { name: "enemy_frag", base: "+1", port: "+1", match: true }
      - { name: "suicide", base: "-1", port: "-1", match: true }
      - { name: "world_death", base: "-1", port: "-1", match: true }
    status:
      logic: faithful
      values: faithful
      timing: faithful
      presentation: na
      audio: na
      liveness: live
    intended_divergence: false
    divergence_rationale: null
    gaps: []
    verification:
      - { kind: code_read, ref: "Duel.OnDeath mirrors Deathmatch.OnDeath; Duel.Activate subscribes Combat.Death (Duel.cs:127-128) and is dispatched at GameWorld.cs:1466", result: pass }
    confidence: high
    notes: "Duel reuses the DM matrix verbatim (enemy +1, suicide/world -1). Death handler is subscribed live on activation. UNCHANGED by Wave 1-2 (already faithful)."

  - id: duel.rules.match_end_latch
    name: "End-of-match frag-limit latch + winner resolution"
    layer: [authority]
    base_refs:
      - "common/gametypes/gametype/duel/sv_duel.qc:MUTATOR_HOOKFUNCTION(duel, Scores_CountFragsRemaining)"
      - "server/scores.qc:WinningConditionHelper"
    port_refs:
      - "src/XonoticGodot.Common/Gameplay/GameTypes/Duel.cs:MatchEnded"
      - "src/XonoticGodot.Common/Gameplay/GameTypes/Duel.cs:FragLimit"
      - "src/XonoticGodot.Common/Gameplay/GameTypes/Duel.cs:RecomputeLeader"
      - "src/XonoticGodot.Server/GameWorld.cs:304"
    constants:
      - { name: "default_fraglimit", base: "0 (pointlimit=0, unlimited)", port: "0", match: true }
    status:
      logic: faithful
      values: faithful
      timing: partial
      presentation: na
      audio: na
      liveness: live
    intended_divergence: false
    divergence_rationale: null
    gaps:
      - "DriveGametypeFrame (GameWorld.cs:1698) still does not call Duel.RecomputeLeader each frame (only Deathmatch at :1706 and Mayhem at :1717 get a per-frame FFA recompute); duel relies on the incremental on-death leader/limit update (Duel.cs:168/190), so a limit re-check only fires on a kill. Correct for a 2-player FFA in practice but diverges from a per-frame checkrules. UNCHANGED by Wave 1-2."
    verification:
      - { kind: code_read, ref: "Duel.MatchEnded read at GameWorld.cs:304 + winner at :2160; RecomputeLeader still absent from DriveGametypeFrame switch (GameWorld.cs:1698-1717)", result: pass }
    confidence: high
    notes: "Frag-limit latch + leader are correct; default 0 = decided by timelimit, matching pointlimit=0. Per-frame recompute omitted but on-death path keeps it correct between kills."

  - id: duel.timing.respawn_delay
    name: "Respawn delay (effective 2s via global fallback)"
    layer: [authority]
    base_refs:
      - "server/client.qc:calculate_player_respawn_time"
      - "server/client.qh:GAMETYPE_DEFAULTED_SETTING"
    port_refs:
      - "src/XonoticGodot.Common/Gameplay/GameTypes/Duel.cs:ScheduleRespawn"
      - "src/XonoticGodot.Common/Gameplay/GameTypes/Duel.cs:RespawnDelay"
    constants:
      - { name: "g_duel_respawn_delay_small", base: "0 (=> falls back to g_respawn_delay_small)", port: "not read; uses generic", match: true }
      - { name: "g_respawn_delay_small (effective)", base: "2 (s)", port: "2 (s)", match: true }
      - { name: "g_duel_respawn_waves", base: "0", port: "not modeled (no waves)", match: true }
      - { name: "g_duel_weapon_stay", base: "0", port: "not modeled", match: true }
    status:
      logic: faithful
      values: faithful
      timing: faithful
      presentation: na
      audio: na
      liveness: live
    intended_divergence: false
    divergence_rationale: null
    gaps:
      - "Port does not model the per-gametype g_duel_respawn_delay_* / g_duel_weapon_stay override layer; reads the generic g_respawn_delay_small directly. Cosmetic at defaults (all duel overrides are 0 => Base falls back to the same generic 2s) but would diverge if an admin set a duel-specific respawn cvar. UNCHANGED by Wave 1-2."
    verification:
      - { kind: code_read, ref: "GAMETYPE_DEFAULTED_SETTING(client.qh:347) 0->global fallback; g_respawn_delay_small=2 (xonotic-server.cfg:264); Duel.RespawnDelay reads g_respawn_delay_small default 2", result: pass }
    confidence: high
    notes: "Effective respawn delay matches at defaults (2s). The earlier-looking 'instant respawn' from g_duel_respawn_delay_small 0 is a false read: 0 means 'use the global default' in Base, which is 2."

  - id: duel.presentation.duel_title
    name: "Duel 'playerA vs playerB' centerprint title"
    layer: [presentation]
    base_refs:
      - "client/announcer.qc:Announcer_Duel"
      - "client/hud/panel/centerprint.qc:centerprint_SetDuelTitle"
    port_refs:
      - "game/hud/CenterPrintPanel.cs:174:SetDuelTitle"
    constants: []
    status:
      logic: stub
      values: na
      timing: missing
      presentation: stub
      audio: na
      liveness: dead
    intended_divergence: false
    divergence_rationale: null
    gaps:
      - "STILL OPEN after Wave 1-2. The characteristic 'name1 vs name2' duel title is never displayed: CenterPrintPanel.SetDuelTitle (CenterPrintPanel.cs:174) exists and renders left/right title halves, but there is no port equivalent of Announcer_Duel to call it (the only references are the definition + the Duel.cs:31 doc-comment, no live caller)."
    verification:
      - { kind: code_read, ref: "grep SetDuelTitle -> only the definition (CenterPrintPanel.cs:174) + a doc-comment in Duel.cs:31, no caller", result: fail }
    confidence: high
    notes: "Panel-side support is complete (matches centerprint_SetDuelTitle: shorten to namesize, store left/right). Only the m_1v1 countdown driver (Announcer_Duel) is missing. UNCHANGED by Wave 1-2."

  - id: duel.presentation.forced_colors
    name: "Forced enemy player colors in 1v1 (cl_forceplayercolors / suppress unique)"
    layer: [presentation]
    base_refs:
      - "client/csqcmodel_hooks.qc:244"
      - "client/csqcmodel_hooks.qc:309"
    port_refs:
      - "src/XonoticGodot.Engine/Simulation/CsqcModelAppearance.cs:ForcePlayerColorsEnabled"
      - "game/client/ClientWorld.cs:1247"
      - "game/net/NetGame.cs:3921"
    constants:
      - { name: "fpc_enable_modes_1v1", base: "1,2,3,5", port: "1,2,3,5", match: true }
      - { name: "suppress_forceunique_in_1v1", base: "true", port: "true (!is1v1 guard)", match: true }
    status:
      logic: faithful
      values: faithful
      timing: na
      presentation: faithful
      audio: na
      liveness: live
    intended_divergence: false
    divergence_rationale: null
    gaps: []
    verification:
      - { kind: code_read, ref: "CsqcModelAppearance.ForcePlayerColorsEnabled(is1v1) + ClientWorld FORCECOLORS path; Is1v1 set from netname==\"duel\" at NetGame.cs:3921/3932", result: pass }
    confidence: high
    notes: "is1v1 flows from the duel netname into the live color-resolution path; mode gating (1/2/3/5) and the unique-color suppression match csqcmodel_hooks.qc. UNCHANGED by Wave 1-2 (already faithful)."

  - id: duel.mapsupport.gating
    name: "Map-support gating (diameter < 3250; force duel on DM maps)"
    layer: [shared]
    base_refs:
      - "common/gametypes/gametype/duel/duel.qh:m_isAlwaysSupported"
      - "common/gametypes/gametype/duel/duel.qh:m_isForcedSupported"
    port_refs: []
    constants:
      - { name: "max_diameter", base: "3250 (units)", port: "none", match: false }
      - { name: "g_duel_not_dm_maps", base: "0", port: "seeded in cfg, not read", match: false }
    status:
      logic: missing
      values: missing
      timing: na
      presentation: na
      audio: na
      liveness: na
    intended_divergence: false
    divergence_rationale: null
    gaps:
      - "STILL OPEN after Wave 1-2. No port logic gates duel auto-support by map size (diameter<3250) or force-lists duel on DM maps. Map-pool/menu concern: a too-large map could be offered for duel, or a DM-only map might not list duel."
    verification:
      - { kind: code_read, ref: "no port symbol for m_isAlwaysSupported/m_isForcedSupported diameter gate", result: fail }
    confidence: medium
    notes: "Low gameplay impact (affects map selection/listing, not in-match behavior). MapInfoBackend handles gametype listing but does not implement the duel diameter/forcing predicates."
