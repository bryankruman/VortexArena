unit: duel
category: gametype
base_ref_rev: "v0.8.6-1779-g863cd3e84"
last_audited: "2026-06-22"
summary:
  feature_count: 9
  worst_gaps:
    - "map-support gating (diameter<3250 / force on DM maps) still unimplemented (mapsupport.gating, MISSING)"
    - "timelimit=10 / leadlimit=0 gametype-init defaults still not seeded per-gametype (identity.registration, generic timelimit); only pointlimit=0 matched"
    - "per-frame Duel.RecomputeLeader still absent from DriveGametypeFrame; Duel falls through the switch (match_end_latch timing partial). On-death incremental update keeps it correct between kills."
features:
  - id: duel.identity.registration
    name: "Duel gametype identity + default match limits"
    layer: [shared]
    base_refs:
      - "common/gametypes/gametype/duel/duel.qh:INIT(Duel)"
      - "common/gametypes/gametype/duel/duel.qh:REGISTER_GAMETYPE(DUEL)"
    port_refs:
      - "src/XonoticGodot.Common/Gameplay/GameTypes/Duel.cs:Duel (ctor)"
      - "src/XonoticGodot.Server/GameWorld.cs:ResolveGameType"
    constants:
      - { name: "netname", base: "duel", port: "duel", match: true }
      - { name: "flags", base: "USEPOINTS|1V1", port: "TeamGame=false; 1v1 via netname", match: true }
      - { name: "timelimit", base: "10 (min)", port: "generic timelimit (not seeded per-gametype)", match: false }
      - { name: "pointlimit", base: "0 (no frag limit)", port: "DefaultFragLimit=0", match: true }
      - { name: "leadlimit", base: "0", port: "not modeled", match: false }
    status:
      logic: faithful
      values: partial
      timing: na
      presentation: na
      audio: na
      liveness: live
    intended_divergence: false
    divergence_rationale: null
    gaps:
      - "timelimit=10 / leadlimit=0 gametype-init defaults are not seeded per-gametype (timelimit handling is generic); pointlimit=0 is matched. UNCHANGED."
    verification:
      - { kind: code_read, ref: "Duel.cs ctor (NetName=\"duel\", TeamGame=false) vs duel.qh gametype_init; Duel.Activate dispatched live at GameWorld.cs:1859 (case Duel duel: duel.Activate(), inside ActivateGameType called from match start GameWorld.cs:545)", result: pass }
    confidence: high
    notes: "Duel is selectable and resolves via GameTypes.ByName(\"duel\"); identity faithful. Match-limit metadata only partially seeded but pointlimit=0 (the duel-distinctive one) matches. (Activation switch case line is now GameWorld.cs:1859.)"

  - id: duel.rules.player_limit_2
    name: "Hard 1v1 player limit (GetPlayerLimit -> 2)"
    layer: [authority]
    base_refs:
      - "common/gametypes/gametype/duel/sv_duel.qc:MUTATOR_HOOKFUNCTION(duel, GetPlayerLimit)"
      - "server/client.qc:2155:GetPlayerLimit (duel mutator hook -> 2)"
      - "server/client.qc:2192:nJoinAllowed (free_slots = max(0, player_limit - currentlyPlaying))"
      - "server/client.qc:1105:status advert; server/bot bot_fixcount uses GetPlayerLimit()"
    port_refs:
      - "src/XonoticGodot.Common/Gameplay/GameTypes/Duel.cs:54:PlayerLimit (const 2)"
      - "src/XonoticGodot.Server/GameWorld.cs:933:GetPlayerLimit"
      - "src/XonoticGodot.Server/GameWorld.cs:941:GametypeHasFreeSlot"
      - "src/XonoticGodot.Server/GameWorld.cs:919:Clients.GametypeJoinGate wiring"
      - "src/XonoticGodot.Server/ClientManager.cs:316:JoinAllowed (GametypeJoinGate consume)"
      - "src/XonoticGodot.Server/Bot/BotPopulation.cs:254:FixCount playerLimit feed"
    constants:
      - { name: "player_limit", base: "2", port: "2 (Duel.PlayerLimit, now enforced)", match: true }
    status:
      logic: faithful
      values: faithful
      timing: na
      presentation: na
      audio: na
      liveness: live
    intended_divergence: false
    divergence_rationale: null
    gaps: []
    verification:
      - { kind: code_read, ref: "GameWorld.GetPlayerLimit() (GameWorld.cs:933) returns Duel.PlayerLimit (=2) for duel; GametypeHasFreeSlot (GameWorld.cs:941-957) replicates QC nJoinAllowed (counts non-observer roster minus joiner, refuses once currentlyPlaying >= limit); wired into Clients.GametypeJoinGate (GameWorld.cs:919-921) which JoinAllowed consumes on the live +jump autojoin path (ClientManager.cs:316, called at ClientManager.cs:370/373/433). Bot-fill: BotPopulation.FixCount feeds playerLimit: GameType is Duel ? Duel.PlayerLimit : g_maxplayers (BotPopulation.cs:254), and TargetBotCount clamps bots to max(playerLimit - activeRealPlayers, 0) (BotPopulation.cs:295-296). Both enforcement points (human join gate + bot fill) now honor the 1v1 cap.", result: pass }
    confidence: high
    notes: "CLOSED (Wave-5 fix-up). This was the unit's single most impactful gap — duel's defining rule was unenforced on BOTH paths in the baseline (GametypeJoinGate was LMS-only; bot-fill fed g_maxplayers). Now GetPlayerLimit short-circuits to 2 for duel, the free-slot gate replicates Base nJoinAllowed (a 3rd+ client can't +jump-join), and bot-fill stops at the cap. Base sv_duel.qc GetPlayerLimit hook -> 2 confirmed verbatim."

  - id: duel.items.powerup_filter
    name: "Powerup item filter (block powerups unless g_duel_with_powerups)"
    layer: [authority]
    base_refs:
      - "common/gametypes/gametype/duel/sv_duel.qc:MUTATOR_HOOKFUNCTION(duel, FilterItemDefinition)"
    port_refs:
      - "src/XonoticGodot.Common/Gameplay/GameTypes/Duel.cs:97:OnFilterItemDefinition"
      - "src/XonoticGodot.Common/Gameplay/GameTypes/Duel.cs:87:WithPowerups"
      - "src/XonoticGodot.Common/Gameplay/GameTypes/Duel.cs:121:Activate"
      - "src/XonoticGodot.Common/Gameplay/Items/StartItem.cs:FilterItemDefinition.Call"
    constants:
      - { name: "g_duel_with_powerups", base: "0", port: "0 (read via WithPowerups, live)", match: true }
    status:
      logic: faithful
      values: faithful
      timing: na
      presentation: na
      audio: na
      liveness: live
    intended_divergence: false
    divergence_rationale: null
    gaps: []
    verification:
      - { kind: code_read, ref: "Duel.Activate (Duel.cs:136-137) does MutatorHooks.FilterItemDefinition.Add(OnFilterItemDefinition); Duel.Activate dispatched live at GameWorld.cs:1859 (case Duel duel, in ActivateGameType from match start GameWorld.cs:545); the chain is consumed on the live item-spawn path at StartItem.SpawnInternal. OnFilterItemDefinition returns !WithPowerups for powerup classnames (item_strength/item_shield/item_invincible), matching Base instanceOfPowerup. Same registration pattern as Mayhem/TeamMayhem.", result: pass }
    confidence: high
    notes: "Faithful + live (unchanged). args.Definition.ClassName is the classname stand-in for instanceOfPowerup. Default g_duel_with_powerups 0 => powerups filtered out, matching Base. NOTE: FilterItemHookTests.DuelFilterItem_* documents the dead-without-activation case (boots a plain server without activating Duel) — harmless, documents a non-live config."

  - id: duel.scoring.frag_matrix
    name: "Deathmatch frag/obituary scoring matrix (inherited)"
    layer: [authority]
    base_refs:
      - "common/gametypes/gametype/duel/duel.qh:CLASS(Duel, Gametype)"
      - "common/gametypes/gametype/deathmatch/deathmatch.qh"
      - "server/damage.qc:Obituary"
    port_refs:
      - "src/XonoticGodot.Common/Gameplay/GameTypes/Duel.cs:157:OnDeath"
      - "src/XonoticGodot.Server/GameWorld.cs:ActivateGameType"
    constants:
      - { name: "enemy_frag", base: "+1", port: "+1", match: true }
      - { name: "suicide", base: "-1", port: "-1", match: true }
      - { name: "world_death", base: "-1", port: "-1", match: true }
    status:
      logic: faithful
      values: faithful
      timing: faithful
      presentation: na
      audio: na
      liveness: live
    intended_divergence: false
    divergence_rationale: null
    gaps: []
    verification:
      - { kind: code_read, ref: "Duel.OnDeath (Duel.cs:157-175) mirrors Deathmatch.OnDeath; Duel.Activate subscribes Combat.Death (Duel.cs:132) which DamageSystem calls live; activation dispatched at GameWorld.cs:1859", result: pass }
    confidence: high
    notes: "Duel reuses the DM matrix verbatim (enemy +1, suicide/world -1). Death handler subscribed live on activation. UNCHANGED (already faithful)."

  - id: duel.rules.match_end_latch
    name: "End-of-match frag-limit latch + winner resolution"
    layer: [authority]
    base_refs:
      - "common/gametypes/gametype/duel/sv_duel.qc:MUTATOR_HOOKFUNCTION(duel, Scores_CountFragsRemaining)"
      - "server/scores.qc:WinningConditionHelper"
    port_refs:
      - "src/XonoticGodot.Common/Gameplay/GameTypes/Duel.cs:63:MatchEnded"
      - "src/XonoticGodot.Common/Gameplay/GameTypes/Duel.cs:111:FragLimit"
      - "src/XonoticGodot.Common/Gameplay/GameTypes/Duel.cs:206:RecomputeLeader"
      - "src/XonoticGodot.Server/GameWorld.cs:2638:MatchHasEnded (Duel case)"
    constants:
      - { name: "default_fraglimit", base: "0 (pointlimit=0, unlimited)", port: "0", match: true }
    status:
      logic: faithful
      values: faithful
      timing: partial
      presentation: na
      audio: na
      liveness: live
    intended_divergence: false
    divergence_rationale: null
    gaps:
      - "DriveGametypeFrame (GameWorld.cs:2137) still does not call Duel.RecomputeLeader each frame — only Deathmatch (:2145), Mayhem (:2156), TeamMayhem (:2157) and Tdm (:2158) get a per-frame FFA/team recompute; Duel falls through the switch with no case. Duel relies on the incremental on-death leader/limit update (Duel.cs:172/194-203), so a limit re-check only fires on a kill. Correct for a 2-player FFA in practice but diverges from a per-frame checkrules. UNCHANGED. (RespawnDuePlayers DOES run for duel via the fall-through at GameWorld.cs:2152.)"
    verification:
      - { kind: code_read, ref: "Duel.MatchEnded consumed at GameWorld.cs:2638-2640 (MatchHasEnded, winner = duel.Leader); RecomputeLeader still absent from the DriveGametypeFrame switch (GameWorld.cs:2154-2156...)", result: pass }
    confidence: high
    notes: "Frag-limit latch + leader are correct and consumed live; default 0 = decided by timelimit, matching pointlimit=0. Per-frame recompute omitted but on-death path keeps it correct between kills (2 players)."

  - id: duel.timing.respawn_delay
    name: "Respawn delay (effective 2s via global fallback)"
    layer: [authority]
    base_refs:
      - "server/client.qc:calculate_player_respawn_time"
      - "server/client.qh:GAMETYPE_DEFAULTED_SETTING"
    port_refs:
      - "src/XonoticGodot.Common/Gameplay/GameTypes/Duel.cs:178:ScheduleRespawn"
      - "src/XonoticGodot.Common/Gameplay/GameTypes/Duel.cs:184:RespawnDelay"
    constants:
      - { name: "g_duel_respawn_delay_small", base: "0 (=> falls back to g_respawn_delay_small)", port: "not read; uses generic", match: true }
      - { name: "g_respawn_delay_small (effective)", base: "2 (s)", port: "2 (s)", match: true }
      - { name: "g_duel_respawn_waves", base: "0", port: "not modeled (no waves)", match: true }
      - { name: "g_duel_weapon_stay", base: "0", port: "not modeled", match: true }
    status:
      logic: faithful
      values: faithful
      timing: faithful
      presentation: na
      audio: na
      liveness: live
    intended_divergence: false
    divergence_rationale: null
    gaps:
      - "Port does not model the per-gametype g_duel_respawn_delay_* / g_duel_weapon_stay override layer; reads the generic g_respawn_delay_small directly (Duel.RespawnDelay, default 2). Cosmetic at defaults (all duel overrides are 0 => Base falls back to the same generic 2s) but would diverge if an admin set a duel-specific respawn cvar. UNCHANGED."
    verification:
      - { kind: code_read, ref: "GAMETYPE_DEFAULTED_SETTING 0->global fallback; Duel.RespawnDelay (Duel.cs:184-192) reads g_respawn_delay_small default 2; no g_duel_respawn_delay_small in gametypes-server.cfg", result: pass }
    confidence: high
    notes: "Effective respawn delay matches at defaults (2s). The earlier-looking 'instant respawn' from g_duel_respawn_delay_small 0 is a false read: 0 means 'use the global default' in Base, which is 2."

  - id: duel.presentation.duel_title
    name: "Duel 'playerA vs playerB' centerprint title"
    layer: [presentation]
    base_refs:
      - "client/announcer.qc:Announcer_Duel"
      - "client/hud/panel/centerprint.qc:centerprint_SetDuelTitle"
    port_refs:
      - "src/XonoticGodot.Server/GameWorld.cs:1633:BroadcastCountdownTitle (Duel branch)"
      - "src/XonoticGodot.Common/Gameplay/Notifications/NotificationSystem.cs:250:SendCenterDuelTitle"
      - "game/hud/HudNotifications.cs:142:MsgType.CenterDuelTitle"
      - "game/hud/CenterPrintPanel.cs:SetDuelTitle"
      - "game/hud/CenterPrintPanel.cs:ShortenColored (textShortenToWidth)"
    constants:
      - { name: "namesize_shorten", base: "hud_panel_scoreboard_namesize * hud_fontsize.x", port: "hud_panel_scoreboard_namesize (def 15) * Cfg.FontSize", match: true }
      - { name: "refresh_on_pair_change", base: "Announcer_Duel: re-send only when the two players change", port: "guarded by _duelTitleLeft/_duelTitleRight (GameWorld.cs:1648)", match: true }
    status:
      logic: faithful
      values: faithful
      timing: faithful
      presentation: faithful
      audio: na
      liveness: live
    intended_divergence: false
    divergence_rationale: null
    gaps: []
    verification:
      - { kind: code_read, ref: "Server-side Announcer_Duel driver: BroadcastCountdownTitle (GameWorld.cs:1633-1652) takes the Duel branch (GameType is Duel), picks the top-two non-spectator players (\"???\" until they exist), re-sends only when the pair changes, and calls NotificationSystem.SendCenterDuelTitle (NotificationSystem.cs:250). LIVE countdown path: Warmup.OnCountdownTick = BroadcastGameStartCountdown (GameWorld.cs:729) -> BroadcastCountdownTitle (GameWorld.cs:1611). Arrives client-side as MsgType.CenterDuelTitle (HudNotifications.cs:142) -> CenterPrint.SetDuelTitle. Title clears on round-stop (GameWorld.cs:1678-1683) matching Announcer_ClearTitle.", result: pass }
    confidence: high
    notes: "Faithful + live (unchanged from baseline). The m_1v1 countdown driver (Announcer_Duel) is implemented server-side (the port has no CSQC announcer timer) and wired through the same notification channel as the gametype-name title. Name-shortening (CenterPrintPanel.ShortenColored, QC textShortenToWidth) present."

  - id: duel.presentation.forced_colors
    name: "Forced enemy player colors in 1v1 (cl_forceplayercolors / suppress unique)"
    layer: [presentation]
    base_refs:
      - "client/csqcmodel_hooks.qc:244"
      - "client/csqcmodel_hooks.qc:309"
    port_refs:
      - "src/XonoticGodot.Engine/Simulation/CsqcModelAppearance.cs:146:ForcePlayerColorsEnabled"
      - "game/client/ClientWorld.cs:1329:ForcePlayerColorsEnabled call"
      - "game/net/NetGame.cs:4679:Is1v1 (netname==duel)"
    constants:
      - { name: "fpc_enable_modes_1v1", base: "1,2,3,5", port: "1,2,3,5", match: true }
      - { name: "suppress_forceunique_in_1v1", base: "true", port: "true (!is1v1 guard)", match: true }
    status:
      logic: faithful
      values: faithful
      timing: na
      presentation: faithful
      audio: na
      liveness: live
    intended_divergence: false
    divergence_rationale: null
    gaps: []
    verification:
      - { kind: code_read, ref: "CsqcModelAppearance.ForcePlayerColorsEnabled(is1v1) (CsqcModelAppearance.cs:146) + ClientWorld FORCECOLORS path (ClientWorld.cs:1329/1338); Is1v1 set from netname==\"duel\" at NetGame.cs:4679/4690", result: pass }
    confidence: high
    notes: "is1v1 flows from the duel netname into the live color-resolution path; mode gating (1/2/3/5) and the unique-color suppression match csqcmodel_hooks.qc. UNCHANGED (already faithful)."

  - id: duel.mapsupport.gating
    name: "Map-support gating (diameter < 3250; force duel on DM maps)"
    layer: [shared]
    base_refs:
      - "common/gametypes/gametype/duel/duel.qh:m_isAlwaysSupported"
      - "common/gametypes/gametype/duel/duel.qh:m_isForcedSupported"
    port_refs: []
    constants:
      - { name: "max_diameter", base: "3250 (units)", port: "none", match: false }
      - { name: "g_duel_not_dm_maps", base: "0", port: "seeded in cfg, not read", match: false }
    status:
      logic: missing
      values: missing
      timing: na
      presentation: na
      audio: na
      liveness: na
    intended_divergence: false
    divergence_rationale: null
    gaps:
      - "STILL OPEN. No port logic gates duel auto-support by map size (diameter<3250) or force-lists duel on DM maps. grep 3250 / m_isAlwaysSupported / m_isForcedSupported / g_duel_not_dm -> only doc-comments (Duel.cs:37); no implementation. Map-pool/menu concern: a too-large map could be offered for duel, or a DM-only map might not list duel."
    verification:
      - { kind: code_read, ref: "no port symbol for m_isAlwaysSupported/m_isForcedSupported diameter gate (only doc-comments in Duel.cs:36-38)", result: fail }
    confidence: medium
    notes: "Low gameplay impact (affects map selection/listing, not in-match behavior). MapInfoBackend handles gametype listing but does not implement the duel diameter/forcing predicates."
