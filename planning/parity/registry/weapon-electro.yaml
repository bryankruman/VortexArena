unit: weapon-electro
category: weapon
base_ref_rev: "v0.8.6-1779-g863cd3e84"
last_audited: "2026-06-25"
summary:
  feature_count: 15
  worst_gaps:
    - "Midair-combo (primary_midaircombo_radius) is logic/values FAITHFUL (a dedicated MidairTriggerCombo with own/teammate/enemy gating + the separate midaircombo_speed first-orb delay), but it is still DEAD on the live path because the xonotic default cvar is 0 (it only runs in samual/xdf where radius=100)."
    - "IS_INDEPENDENT_PLAYER owner-gate is ported in the midair path but Entity.IsIndependentPlayer is hardwired false (DamageEntityState.cs:158, no independent-player mode ported), so it is dead-OFF-equivalent; the non-midair W_Electro_TriggerCombo still omits the gate entirely (same harmless effect)."
    - "Combo explode-over-time (combo_duration, default 0) and sticky orbs (MOVETYPE_FOLLOW, stick default 0) are intentionally omitted; harmless at defaults."
    - "Registry coverage gaps (newly added as rows this audit): bot wr_aim targeting, the Electro_Orb CSQC net-link/scale-pulse draw, and the MENUQC weapon-guide describe text."
features:
  - id: electro.primary.fire_gate
    name: "Primary bolt fire gate (refire + refire2 interlock)"
    layer: [authority, shared]
    base_refs:
      - "common/weapons/weapon/electro.qc:wr_think (fire & 1)"
    port_refs:
      - "src/XonoticGodot.Common/Gameplay/Weapons/Electro.cs:WrThink"
      - "src/XonoticGodot.Common/Gameplay/Weapons/WeaponFireDriver.cs:Frame"
    constants:
      - { name: "primary_refire", base: "0.6", port: "0.6", match: true }
      - { name: "secondary_refire2", base: "0.2", port: "0.2", match: true }
      - { name: "primary_animtime", base: "0.3", port: "0.3", match: true }
    status:
      logic: faithful
      values: faithful
      timing: faithful
      presentation: na
      audio: na
      liveness: live
    intended_divergence: false
    divergence_rationale: null
    gaps: []
    verification:
      - { kind: code, ref: "Electro.WrThink (Electro.cs:142-150) mirrors time>=electro_secondarytime+refire2*rate && PrepareAttack(refire); WeaponFireDriver.Frame calls WrThink(Primary) every tick. Unchanged by Wave-2.", result: pass }
    confidence: high
    notes: "electro_secondarytime on EntityWeaponState; rate factor via WeaponRateFactor(). No change in this wave; remains faithful+live."

  - id: electro.primary.bolt_projectile
    name: "Primary bolt projectile (MOVETYPE_FLY, velocity, lifetime, in-flight visual)"
    layer: [authority, shared, presentation]
    base_refs:
      - "common/weapons/weapon/electro.qc:W_Electro_Attack_Bolt"
      - "common/weapons/weapon/electro.qc:W_Electro_Bolt_Think"
      - "client/weapons/projectile.qc:Projectile_Draw (PROJECTILE_ELECTRO_BEAM)"
    port_refs:
      - "src/XonoticGodot.Common/Gameplay/Weapons/Electro.cs:AttackBolt"
      - "src/XonoticGodot.Common/Gameplay/Weapons/Electro.cs:BoltThink"
      - "src/XonoticGodot.Common/Gameplay/Weapons/WeaponFiring.cs:ProjectileVelocity"
      - "game/client/ProjectileCatalog.cs:ProjectileType.ElectroBeam (elaser model, TR_NEXUIZPLASMA, no fly loop)"
      - "game/client/ProjectileRenderer.cs:OnSpawn (driven by ClientWorld.OnEntitySpawn->IsProjectile)"
    constants:
      - { name: "primary_speed", base: "2500", port: "2500", match: true }
      - { name: "primary_lifetime", base: "5", port: "5", match: true }
      - { name: "primary_spread", base: "0", port: "0", match: true }
      - { name: "primary_midaircombo_interval", base: "0.1", port: "0.1 (idle branch now nextthink=ltime, no per-tick think)", match: true }
    status:
      logic: faithful
      values: faithful
      timing: faithful
      presentation: faithful
      audio: faithful
      liveness: live
    intended_divergence: false
    divergence_rationale: null
    gaps:
      - "Minor: the trail/glow are the port's procedural ProjectileCatalog visual (blue plasma TR_NEXUIZPLASMA + saturated blue glow light + elaser model via the host ModelFactory), not a 1:1 CSQC particle replay; the trail spacing rides DP's faithful per-segment sim. Visual identity matches; exact particle counts are presentation-tuned."
    verification:
      - { kind: code, ref: "AttackBolt (Electro.cs:200-232): MoveType.Fly + ProjectileVelocity==W_SetupProjVelocity_PRI; touch=ExplodeBolt(self,other), think=BoltThink, plays electro_fire + ELECTRO_MUZZLEFLASH. BoltThink default branch (Electro.cs:266) nextthink=deathTime (no per-tick), matching QC. ServerNet.Classify->NetEntityKind.Projectile (FL_PROJECTILE marker + owner + Fly); ProjectileCatalog.Classify('electro_bolt')->ElectroBeam; ClientWorld.IsProjectile matches 'electro'->Projectiles.OnSpawn renders model+blue trail+light.", result: pass }
    confidence: high
    notes: "MAJOR CORRECTION vs prior audit: ProjectileCatalog/ProjectileRenderer DO exist (game/client/, on main since 2026-06-12 commit 0ab5fba) — the earlier 'no client/ProjectileCatalog, in-flight invisible' claim only searched src/ and was WRONG. The bolt is networked as a projectile and rendered with the elaser-style glow body + TR_NEXUIZPLASMA blue plasma trail + dynamic light. Bolt has no fly loop in Base either. Timing gap (per-0.05s think) also closed: idle branch is nextthink=ltime."

  - id: electro.primary.explode_splash
    name: "Bolt explosion splash damage + impact effect/sound"
    layer: [authority]
    base_refs:
      - "common/weapons/weapon/electro.qc:W_Electro_Explode"
      - "common/weapons/weapon/electro.qc:wr_impacteffect (primary branch)"
    port_refs:
      - "src/XonoticGodot.Common/Gameplay/Weapons/Electro.cs:ExplodeBolt"
      - "src/XonoticGodot.Common/Gameplay/Weapons/WeaponSplash.cs:RadiusDamage"
      - "src/XonoticGodot.Common/Gameplay/Weapons/WeaponSplash.cs:ImpactSound"
    constants:
      - { name: "primary_damage", base: "40", port: "40", match: true }
      - { name: "primary_edgedamage", base: "20", port: "20", match: true }
      - { name: "primary_radius", base: "100", port: "100", match: true }
      - { name: "primary_force", base: "200", port: "200", match: true }
    status:
      logic: faithful
      values: faithful
      timing: na
      presentation: faithful
      audio: faithful
      liveness: live
    intended_divergence: false
    divergence_rationale: null
    gaps: []
    verification:
      - { kind: code, ref: "ExplodeBolt (Electro.cs:275-302) calls WeaponSplash.RadiusDamage with primary balance + directHit (LOS-skip), emits ELECTRO_IMPACT particle, and now WeaponSplash.ImpactSound(self,'weapons/electro_impact.wav') — matching QC wr_impacteffect primary-no-bounce branch (EFFECT_ELECTRO_IMPACT + SND_ELECTRO_IMPACT). ImpactSound routes Api.Sound.Play on CH_SHOTS (live, networked).", result: pass }
    confidence: high
    notes: "FIXED: the bolt blast is no longer silent — ImpactSound (electro_impact) is now called. Particle blast (ELECTRO_IMPACT, EffectsList.cs) faithful+live. directHit threaded from the touch handler."

  - id: electro.combo.trigger_chain
    name: "Combo: bolt blast triggers nearby orbs into a chained explosion"
    layer: [authority]
    base_refs:
      - "common/weapons/weapon/electro.qc:W_Electro_TriggerCombo"
      - "common/weapons/weapon/electro.qc:W_Electro_ExplodeCombo"
    port_refs:
      - "src/XonoticGodot.Common/Gameplay/Weapons/Electro.cs:TriggerCombo"
      - "src/XonoticGodot.Common/Gameplay/Weapons/Electro.cs:ExplodeCombo"
    constants:
      - { name: "primary_comboradius", base: "300", port: "300", match: true }
      - { name: "combo_comboradius", base: "300", port: "300", match: true }
      - { name: "combo_damage", base: "50", port: "50", match: true }
      - { name: "combo_edgedamage", base: "25", port: "25", match: true }
      - { name: "combo_radius", base: "150", port: "150", match: true }
      - { name: "combo_force", base: "120", port: "120", match: true }
      - { name: "combo_speed", base: "2000", port: "2000 (Configure() now seeds ComboSpeed from g_balance_electro_combo_speed)", match: true }
      - { name: "combo_comboradius_thruwall", base: "200", port: "200 (seeded; LOS abort beyond thruwall dist)", match: true }
      - { name: "combo_duration", base: "0", port: "none (explode-over-time path absent; OK while default 0)", match: true }
    status:
      logic: faithful
      values: faithful
      timing: faithful
      presentation: faithful
      audio: faithful
      liveness: live
    intended_divergence: false
    divergence_rationale: null
    gaps:
      - "IS_INDEPENDENT_PLAYER owner-gating in W_Electro_TriggerCombo (Electro.cs:480-511) is not ported here (it IS ported in the midair path); dead-OFF-equivalent since Entity.IsIndependentPlayer is hardwired false and default modes have no independent players."
    verification:
      - { kind: code, ref: "Configure (Electro.cs:131-132) now sets ComboSpeed=Bal(combo_speed,2000) and ComboComboRadiusThruwall=Bal(...,200). TriggerCombo (Electro.cs:431-462) snapshots electro_orb in radius, applies the thruwall LOS abort beyond ComboComboRadiusThruwall (Trace, frac<1 -> continue), renames to electro_orb_chain, schedules ExplodeCombo with (origin-org).Length()/ComboSpeed delay, recursing within Combo.ComboRadius. ExplodeCombo (465-477) re-triggers then RadiusDamage(combo, deathTag=DeathCombo) + ELECTRO_COMBO + ImpactSound(electro_impact_combo). Driven live from ExplodeBolt->TriggerCombo.", result: pass }
    confidence: high
    notes: "FIXED (3 gaps): combo_speed seeded 2000 (was permanently 1000); combo_comboradius_thruwall LOS test added matching QC structure; combo blast now plays SND_ELECTRO_IMPACT_COMBO. Recursive chain faithful+live. Only the independent-player gate (dead-OFF-equivalent at defaults) remains."

  - id: electro.primary.midaircombo
    name: "Primary bolt midair-combo (trigger orbs in flight)"
    layer: [authority]
    base_refs:
      - "common/weapons/weapon/electro.qc:W_Electro_Bolt_Think"
    port_refs:
      - "src/XonoticGodot.Common/Gameplay/Weapons/Electro.cs:BoltThink"
    constants:
      - { name: "primary_midaircombo_radius", base: "0 (OFF in xonotic; 100 in samual/xdf)", port: "0 (seeded from cvar)", match: true }
      - { name: "primary_midaircombo_explode", base: "1", port: "true", match: true }
      - { name: "primary_midaircombo_interval", base: "0.1", port: "0.1", match: true }
      - { name: "primary_midaircombo_own", base: "1", port: "true (Primary.MidairComboOwn — gated)", match: true }
      - { name: "primary_midaircombo_teammate", base: "1", port: "true (Primary.MidairComboTeammate — gated)", match: true }
      - { name: "primary_midaircombo_enemy", base: "1", port: "true (Primary.MidairComboEnemy — gated)", match: true }
      - { name: "primary_midaircombo_speed", base: "2000", port: "2000 (first-orb delay = dist/midaircombo_speed; rest use combo_speed via the ExplodeCombo recursion)", match: true }
    status:
      logic: faithful
      values: faithful
      timing: faithful
      presentation: faithful
      audio: faithful
      liveness: dead
    intended_divergence: false
    divergence_rationale: null
    gaps:
      - "DEAD on the live path: midaircombo_radius defaults 0 (bal-wep-xonotic.cfg), so the BoltThink midair branch (Electro.cs:258-269 -> MidairTriggerCombo) never runs under stock xonotic balance. The branch IS reachable (and now faithful) under samual/xdf balance where the cvar is 100."
      - "IS_INDEPENDENT_PLAYER orb-owner skip is ported (Electro.cs:291) but Entity.IsIndependentPlayer is hardwired false, so the guard is dead-OFF-equivalent (no independent-player mode is ported). Harmless: default modes have no independent players."
    verification:
      - { kind: code, ref: "BoltThink (Electro.cs:258) guards on Primary.MidairComboRadius>0 (Bal default 0, verified bal-wep-xonotic.cfg). When on, it calls MidairTriggerCombo (Electro.cs:280-321): FindInRadius(electro_orb) -> per-orb own/teammate/enemy gate (own==e.Owner?MidairComboOwn : Teams.SameTeam?MidairComboTeammate : MidairComboEnemy), renames to electro_orb_chain, schedules ExplodeCombo with first-orb delay dist/MidairComboSpeed (0=instant->immediate), the recursive ExplodeCombo->TriggerCombo ripple then uses combo_speed. If found && MidairComboExplode -> ExplodeBolt, else reschedule min(time+0.1, deathTime). Mirrors Base electro.qc:261-309 line-for-line. Wave-5 (commit 023ba70) added this; FAILS only on liveness (radius 0 at xonotic default).", result: fail }
    confidence: high
    notes: "FIX (logic+values partial->faithful) since the 2026-06-22 baseline: Wave-5 (commit 023ba70) replaced the old TriggerCombo-reuse with a dedicated MidairTriggerCombo (Electro.cs:280-321) that ports the full Base W_Electro_Bolt_Think midair loop — per-orb own/teammate/enemy team-gating (electro.qc:276-281) AND the separate midaircombo_speed first-orb delay with the rest on combo_speed (electro.qc:290-298). All midaircombo cvars now seeded in Configure (Electro.cs:108-114) and verified against bal-wep-*.cfg. STILL OPEN / liveness DEAD: midaircombo_radius is 0 in xonotic balance so the branch is unreachable in stock play (faithful-but-dead; it is live+faithful under samual/xdf where radius=100). This is a deliberate faithful port of an off-by-default feature, not a divergence."

  - id: electro.secondary.orb_stream
    name: "Secondary orb stream (count orbs, one per animtime while held)"
    layer: [authority, shared]
    base_refs:
      - "common/weapons/weapon/electro.qc:wr_think (fire & 2)"
      - "common/weapons/weapon/electro.qc:W_Electro_CheckAttack"
    port_refs:
      - "src/XonoticGodot.Common/Gameplay/Weapons/Electro.cs:WrThink"
      - "src/XonoticGodot.Common/Gameplay/Weapons/Electro.cs:CheckAttack"
      - "src/XonoticGodot.Common/Gameplay/Weapons/Electro.cs:ScheduleCheckAttack"
    constants:
      - { name: "secondary_count", base: "3", port: "3", match: true }
      - { name: "secondary_refire", base: "1.2", port: "1.2", match: true }
      - { name: "secondary_animtime", base: "0.2", port: "0.2", match: true }
    status:
      logic: faithful
      values: faithful
      timing: faithful
      presentation: na
      audio: na
      liveness: live
    intended_divergence: false
    divergence_rationale: null
    gaps: []
    verification:
      - { kind: code, ref: "WrThink(Secondary) (Electro.cs:151-165) fires first orb, sets ElectroCount=count, ElectroSecondaryTime=time, ScheduleCheckAttack(animtime); CheckAttack (170-185) streams while ElectroCount>1 && ButtonAttack2 held, decrementing. Unchanged by Wave-2.", result: pass }
    confidence: high
    notes: "ElectroCount/ElectroSecondaryTime on EntityWeaponState; held-button state propagated by WeaponFireDriver. No change this wave; faithful+live."

  - id: electro.secondary.orb_projectile
    name: "Secondary orb projectile (MOVETYPE_BOUNCE, gravity, shootable HP, in-flight visual)"
    layer: [authority, shared, presentation]
    base_refs:
      - "common/weapons/weapon/electro.qc:W_Electro_Attack_Orb"
      - "client/weapons/projectile.qc:Projectile_Draw (PROJECTILE_ELECTRO)"
    port_refs:
      - "src/XonoticGodot.Common/Gameplay/Weapons/Electro.cs:AttackOrb"
      - "src/XonoticGodot.Engine/Simulation/MoveTypePhysics.cs:PhysicsToss (Bounce case)"
      - "game/client/ProjectileCatalog.cs:ProjectileType.Electro (ebomb model, TR_NEXUIZPLASMA, electro_fly loop)"
      - "game/client/ProjectileRenderer.cs:OnSpawn / AttachLoopSound"
    constants:
      - { name: "secondary_speed", base: "1000", port: "1000", match: true }
      - { name: "secondary_speed_up", base: "200", port: "200", match: true }
      - { name: "secondary_speed_z", base: "0", port: "0 (not passed)", match: true }
      - { name: "secondary_lifetime", base: "4", port: "4", match: true }
      - { name: "secondary_health", base: "5", port: "5", match: true }
      - { name: "secondary_bouncefactor", base: "0.3", port: "0.3 (MoveTypePhysics now reads ent.BounceFactor)", match: true }
      - { name: "secondary_bouncestop", base: "0.05", port: "0.05 (MoveTypePhysics now reads ent.BounceStop)", match: true }
    status:
      logic: faithful
      values: faithful
      timing: faithful
      presentation: faithful
      audio: faithful
      liveness: live
    intended_divergence: false
    divergence_rationale: null
    gaps:
      - "Minor: in-flight visual is the port's procedural ProjectileCatalog body (glow sprite + ebomb model via host ModelFactory) with the TR_NEXUIZPLASMA blue plasma trail + dynamic light; the CSQC electro_orb_draw scale-pulse is not reproduced (cosmetic)."
    verification:
      - { kind: code, ref: "AttackOrb (Electro.cs:305-367): MoveType.Bounce + Gravity 1 + BounceFactor 0.3 + BounceStop 0.05 + Health 5 + ProjectileVelocity==W_SetupProjVelocity_UP_SEC + electro_fire2 + ELECTRO_MUZZLEFLASH. MoveTypePhysics Bounce case (MoveTypePhysics.cs:169-191) now uses bf=ent.BounceFactor?0.3:0.5 and bouncestop=ent.BounceStop?0.05:engine, so the orb's 0.3/0.05 are LIVE. ServerNet.Classify->Projectile; ProjectileCatalog.Classify('electro_orb')->Electro (ebomb model, electro_fly loop); ProjectileRenderer.AttachLoopSound plays the fly loop.", result: pass }
    confidence: high
    notes: "FIXED (3 gaps): bounce VALUES now read from the entity (MoveTypePhysics.cs Bounce case reads ent.BounceFactor/BounceStop — cross-cutting engine fix, also helps Mortar); in-flight model+blue plasma trail+light now rendered (ProjectileCatalog Electro); electro_fly looping sound now attached. The earlier 'invisible orb / fabricated ProjectileCatalog' claim was WRONG (catalog is on main)."

  - id: electro.secondary.orb_touch_explode
    name: "Orb touch: explode on player vs bounce (+ bounce sound, detonation sound)"
    layer: [authority]
    base_refs:
      - "common/weapons/weapon/electro.qc:W_Electro_Orb_Touch"
      - "common/weapons/weapon/electro.qc:W_Electro_Explode (orb path)"
      - "common/weapons/weapon/electro.qc:wr_impacteffect (secondary branch)"
    port_refs:
      - "src/XonoticGodot.Common/Gameplay/Weapons/Electro.cs:OrbTouch"
      - "src/XonoticGodot.Common/Gameplay/Weapons/Electro.cs:ExplodeOrb"
    constants:
      - { name: "secondary_touchexplode", base: "1", port: "true", match: true }
      - { name: "secondary_damage", base: "30", port: "30", match: true }
      - { name: "secondary_edgedamage", base: "15", port: "15", match: true }
      - { name: "secondary_radius", base: "150", port: "150", match: true }
      - { name: "secondary_force", base: "50", port: "50", match: true }
    status:
      logic: faithful
      values: faithful
      timing: na
      presentation: faithful
      audio: faithful
      liveness: live
    intended_divergence: false
    divergence_rationale: null
    gaps:
      - "Sticky-orb branch (stick, default 0) is intentionally omitted (no detonation/bounce effect at defaults)."
    verification:
      - { kind: code, ref: "OrbTouch (Electro.cs:370-386): explode on DAMAGE_AIM/client with touchexplode; else, with the QC self-projectile guard (other.Owner==self.Owner && other.ClassName==self.ClassName -> return, no bounce-sound/stick), play electro_bounce + set bounced flag + reface. ExplodeOrb (389-405): RadiusDamage with secondary balance + deathTag carrying HITTYPE_SECONDARY (and HITTYPE_BOUNCE if bounced) + ELECTRO_BALLEXPLODE particle + ImpactSound(electro_impact). Matches QC secondary wr_impacteffect.", result: pass }
    confidence: high
    notes: "FIXED (2 gaps): orb detonation now plays SND_ELECTRO_IMPACT (was silent); the self-projectile bounce guard (don't bounce-sound off / stick to own projectiles) is now present; HITTYPE_BOUNCE folded onto the bounced-orb deathtype. Logic upgraded partial->faithful."

  - id: electro.secondary.orb_shotdown_combo
    name: "Shoot-down orb converts to a combo blast"
    layer: [authority]
    base_refs:
      - "common/weapons/weapon/electro.qc:W_Electro_Orb_Damage"
      - "server/weapons/common.qc:W_CheckProjectileDamage / W_PrepareExplosionByDamage"
    port_refs:
      - "src/XonoticGodot.Common/Gameplay/Weapons/Electro.cs:AttackOrb (ProjectileDamage callback)"
      - "src/XonoticGodot.Common/Gameplay/Weapons/Projectiles.cs:MakeShootable / ShootDown / CheckProjectileDamage"
      - "src/XonoticGodot.Common/Gameplay/Damage/DamageSystem.cs:EventDamage (GtEventDamage dispatch)"
    constants:
      - { name: "secondary_health", base: "5", port: "5", match: true }
      - { name: "secondary_damageforcescale", base: "4", port: "4", match: true }
      - { name: "combo_damage", base: "50", port: "50", match: true }
    status:
      logic: faithful
      values: faithful
      timing: na
      presentation: faithful
      audio: faithful
      liveness: live
    intended_divergence: false
    divergence_rationale: null
    gaps: []
    verification:
      - { kind: code, ref: "AttackOrb sets ProjectileDamage (combo explode: RadiusDamage combo balance, deathTag=DeathCombo, ELECTRO_COMBO, ImpactSound electro_impact_combo) then Projectiles.MakeShootable(orb, exception:1). MakeShootable installs e.GtEventDamage=ShootDown; DamageSystem.EventDamage (DamageSystem.cs:331-335) dispatches a non-player victim's GtEventDamage. ShootDown (Projectiles.cs:82-99) runs CheckProjectileDamage (W_CheckProjectileDamage, exception 1 passes the stock g_projectiles_damage -2), subtracts hp, fires ProjectileDamage at hp<=0. So an orb CAN now be shot down into a combo on the live path.", result: pass }
    confidence: high
    notes: "FIXED — liveness dead->live: the orb's ProjectileDamage is now invoked via the Wave-1 GtEventDamage seam in DamageSystem.EventDamage (was only ever called by BreakablehookMutator). HP-subtract + g_projectiles_damage gate + combo exception now ported faithfully. Combo detonation sound added. Cross-cutting fix shared by every shoot-down weapon (Devastator/Mortar/Hagar/Minelayer/Arc)."

  - id: electro.ammo_reload
    name: "Ammo checks + reload (cells, combo_safeammocheck)"
    layer: [authority]
    base_refs:
      - "common/weapons/weapon/electro.qc:wr_checkammo1"
      - "common/weapons/weapon/electro.qc:wr_checkammo2"
      - "common/weapons/weapon/electro.qc:wr_reload"
    port_refs:
      - "src/XonoticGodot.Common/Gameplay/Weapons/Electro.cs:CheckAmmoPrimary"
      - "src/XonoticGodot.Common/Gameplay/Weapons/Electro.cs:CheckAmmoSecondary"
      - "src/XonoticGodot.Common/Gameplay/Weapons/WeaponFireGate.cs:WeaponAmmo.Check"
      - "src/XonoticGodot.Common/Gameplay/Weapons/WeaponFireGate.cs:WrReload"
    constants:
      - { name: "primary_ammo", base: "4", port: "4", match: true }
      - { name: "secondary_ammo", base: "2", port: "2", match: true }
      - { name: "combo_safeammocheck", base: "1", port: "true (CheckAmmoSecondary needs Secondary.Ammo+Primary.Ammo)", match: true }
      - { name: "reload_ammo", base: "0 (reload off by default)", port: "generic WrReload path", match: true }
    status:
      logic: faithful
      values: faithful
      timing: na
      presentation: na
      audio: na
      liveness: live
    intended_divergence: false
    divergence_rationale: null
    gaps:
      - "No Electro-specific wr_reload override (Base's is W_Reload(min(pri,sec) ammo, SND_RELOAD)); relies on the generic WeaponFireGate.WrReload. Acceptable since reload_ammo defaults 0; the reload amount min(4,2)=2 differs from the generic path only on a reload-enabled server (harmless at stock defaults)."
    verification:
      - { kind: code, ref: "Configure seeds CombocSafeAmmoCheck=BalBool(combo_safeammocheck,true). CheckAmmoPrimary (Electro.cs:542-544): GetResource(Cells)>=Primary.Ammo OR GetWeaponLoad(state,RegistryId)>=Primary.Ammo — both terms now present, matching QC wr_checkammo1:669-671. CheckAmmoSecondary (Electro.cs:549-554): need = safeammocheck ? Secondary.Ammo+Primary.Ammo : Secondary.Ammo; GetResource(Cells)>=need OR GetWeaponLoad>=need — matches QC wr_checkammo2:677-687 incl. the clip-load OR-term. WeaponAmmo.Check routes Electro live; consumed by CheckAmmoWithAutoSwitch.", result: pass }
    confidence: high
    notes: "FIX since 2026-06-22 baseline (logic partial->faithful): the clip-load OR-term (weapon_load[m_id] >= ammo) is now present in BOTH CheckAmmoPrimary and CheckAmmoSecondary via GetWeaponLoad(...), so wr_checkammo1/2 are now line-for-line faithful (the baseline's sole logic gap is closed). combo_safeammocheck stays implemented. Only the cosmetic no-Electro-specific-wr_reload remains (no-op at stock reload_ammo 0)."

  - id: electro.deathmessages
    name: "Kill / suicide obituary lines (bolt/orb/combo variants)"
    layer: [authority]
    base_refs:
      - "common/weapons/weapon/electro.qc:wr_killmessage"
      - "common/weapons/weapon/electro.qc:wr_suicidemessage"
    port_refs:
      - "src/XonoticGodot.Common/Gameplay/Notifications/DeathMessages.cs"
      - "src/XonoticGodot.Common/Gameplay/Weapons/Electro.cs:DeathBolt/DeathOrb/DeathCombo"
    constants: []
    status:
      logic: faithful
      values: faithful
      timing: na
      presentation: faithful
      audio: na
      liveness: live
    intended_divergence: false
    divergence_rationale: null
    gaps: []
    verification:
      - { kind: code, ref: "Electro.cs:420-422 builds DeathBolt=FromWeapon(netname), DeathOrb=Bolt|HITTYPE_SECONDARY, DeathCombo=Bolt|HITTYPE_BOUNCE as STRING deathtypes; passed as deathTag to RadiusDamage, which routes them through Damage.Combat.Damage (string pipeline) so the bits survive (NOT the int RegistryId path). DeathMessages.SelectKillMessage (DeathMessages.cs:57-58): SECONDARY->MURDER_ORBS else BOUNCE->MURDER_COMBO else MURDER_BOLT — matches electro.qc:703. ExplodeCombo passes DeathCombo (bounce->MURDER_COMBO); ExplodeOrb passes DeathOrb (secondary->MURDER_ORBS).", result: pass }
    confidence: high
    notes: "Confidence UPGRADED medium->high: the HITTYPE bit-propagation concern is resolved — the port now carries deathtype as a STRING tag (DeathCombo/DeathOrb) through WeaponSplash.RadiusDamage's deathTag string path, not the bit-losing int RegistryId path. Combo kills correctly read MURDER_COMBO."

  - id: electro.electrobitch_announce
    name: "ELECTROBITCH airshot announcement (direct bolt kill on flying enemy)"
    layer: [authority]
    base_refs:
      - "common/weapons/weapon/electro.qc:W_Electro_Explode"
      - "common/physics/player.qc:IsFlying"
    port_refs:
      - "src/XonoticGodot.Common/Gameplay/Weapons/Electro.cs:ExplodeBolt (ELECTROBITCH test)"
      - "src/XonoticGodot.Common/Gameplay/Weapons/Electro.cs:IsFlying"
      - "src/XonoticGodot.Common/Gameplay/Notifications/NotificationSystem.cs:Announce"
      - "src/XonoticGodot.Common/Gameplay/Notifications/NotificationsList.cs:A(ACHIEVEMENT_ELECTROBITCH)"
    constants: []
    status:
      logic: faithful
      values: faithful
      timing: na
      presentation: faithful
      audio: faithful
      liveness: live
    intended_divergence: false
    divergence_rationale: null
    gaps: []
    verification:
      - { kind: code, ref: "ExplodeBolt (Electro.cs:279-283) now tests directHit (threaded from the bolt Touch handler) TakeDamage==Aim && Flags&Client && !=owner && !SameTeam && DeadState==No && IsFlying(directHit) and calls NotificationSystem.Announce(owner,'ACHIEVEMENT_ELECTROBITCH'). IsFlying (481-488) ports the airshot test (not onground, not swimming, 24u floor clearance). ANNCE 'electrobitch' registered (NotificationsList.cs:85) + sound (SoundsList.cs:283).", result: pass }
    confidence: high
    notes: "FIXED missing->faithful+live: the airshot ELECTROBITCH announcement is now ported and fired on a direct bolt kill of a flying enemy. Matches W_Electro_Explode:197-200. Resolves the standing audio-parity MEMORY note for electro."

  - id: electro.bot_aim
    name: "Bot aim heuristic (primary lead-aim, occasional secondary mortar lob)"
    layer: [authority]
    base_refs:
      - "common/weapons/weapon/electro.qc:wr_aim (METHOD Electro, wr_aim)"
    port_refs: []
    constants:
      - { name: "primary_speed", base: "2500", port: "n/a", match: false }
      - { name: "secondary_speed", base: "1000", port: "n/a", match: false }
      - { name: "secondary_speed_up", base: "200", port: "n/a", match: false }
    status:
      logic: missing
      values: missing
      timing: na
      presentation: na
      audio: na
      liveness: na
    intended_divergence: false
    divergence_rationale: null
    gaps:
      - "No Electro-specific bot aim is ported. Base wr_aim leads with bot_aim at primary_speed (or hitscan-ish when speed==0) and, with a small random chance (bot_secondary_electromooth), switches to a secondary mortar-style lob using secondary_speed/speed_up; switches back at >1000u range. The port BotBrain.cs has no electro branch. Bots can still fire generically but without the primary/secondary mix or the lead-time tuning."
    verification:
      - { kind: code, ref: "Grep '[Ee]lectro' in src/XonoticGodot.Server/Bot/BotBrain.cs -> no matches; no per-weapon wr_aim port for electro found.", result: fail }
    confidence: medium
    notes: "NEW ROW (registry coverage gap, not a drift). Base electro.qc:599-626 wr_aim is unmapped. Bot-aim parity is generally weak across the weapon registry; tracking here for completeness. Not on the stock human play path."

  - id: electro.orb.csqc_netlink_draw
    name: "Orb CSQC net-link + scale-pulse / spin draw"
    layer: [presentation, shared]
    base_refs:
      - "common/weapons/weapon/electro.qc:electro_orb_send (SVQC Net_LinkEntity)"
      - "common/weapons/weapon/electro.qc:electro_orb_draw (CSQC scale pulse + avelocity spin)"
      - "common/weapons/weapon/electro.qc:electro_orb_setup"
    port_refs:
      - "game/client/ProjectileCatalog.cs:ProjectileType.Electro (ebomb model, electro_fly loop)"
      - "game/client/ProjectileRenderer.cs:OnSpawn / AttachLoopSound"
    constants:
      - { name: "orb_setup_scale_factor", base: "combo_radius*0.05 * bound(0,(ltime-time)*4,1)", port: "none (procedural catalog glow, no shrink-on-death pulse)", match: false }
      - { name: "orb_avelocity", base: "'7 0 11' (spin)", port: "none", match: false }
      - { name: "orb_alpha", base: "0.7", port: "catalog-tuned", match: false }
    status:
      logic: faithful
      values: partial
      timing: na
      presentation: partial
      audio: faithful
      liveness: live
    intended_divergence: false
    divergence_rationale: null
    gaps:
      - "The dedicated REGISTER_NET_LINKED(Electro_Orb) link/draw is not reproduced 1:1: Base streams the orb as its own CSQC entity (electro_orb_send writes origin+owner+lifetime; electro_orb_draw applies a (ltime-time)-based scale shrink as the orb nears expiry plus an avelocity spin '7 0 11', alpha 0.7). The port renders the orb via the generic ProjectileCatalog/ProjectileRenderer Electro body (ebomb model + TR_NEXUIZPLASMA trail + dynamic light + electro_fly loop). The in-flight identity matches; the time-based shrink pulse and explicit spin axis are cosmetic and unreproduced."
    verification:
      - { kind: code, ref: "Base electro.qc:7-66 (electro_orb_draw scale pulse + electro_orb_send/NET_HANDLE) has no 1:1 port; the orb is instead networked as a generic projectile (ServerNet.Classify->Projectile) and drawn by ProjectileCatalog.Electro / ProjectileRenderer.OnSpawn. Loop sound (electro_fly) is attached. Overlaps electro.secondary.orb_projectile (which records the same scale-pulse omission).", result: pass }
    confidence: medium
    notes: "NEW ROW (registry coverage gap). The orb's in-flight render is already covered live under electro.secondary.orb_projectile; this row tracks the specific CSQC net-link entity + scale-pulse/spin draw as its own line. Visual identity is faithful; only the death-shrink pulse and explicit spin axis are missing (cosmetic)."

  - id: electro.describe_guide
    name: "Weapon-guide describe text (MENUQC)"
    layer: [presentation]
    base_refs:
      - "common/weapons/weapon/electro.qc:describe (METHOD Electro, describe)"
    port_refs: []
    constants: []
    status:
      logic: missing
      values: na
      timing: na
      presentation: missing
      audio: na
      liveness: na
    intended_divergence: false
    divergence_rationale: null
    gaps:
      - "The MENUQC weapon-guide describe paragraphs (electro.qc:753-767 — primary/secondary/combo description + DPS guide line) are not ported; the port has no in-menu weapon guide for electro."
    verification:
      - { kind: code, ref: "Base electro.qc:749-769 describe() (MENUQC) has no corresponding port symbol; no weapon-guide page text for electro found in the port.", result: fail }
    confidence: medium
    notes: "NEW ROW (registry coverage gap). MENUQC guide text — low priority, not gameplay; tracked for registry completeness."
