unit: turret-plasma_dual
category: turret
base_ref_rev: "v0.8.6-1779-g863cd3e84"
last_audited: "2026-06-22"
summary:
  feature_count: 11
  worst_gaps:
    - "TFL_DMG_HEADSHAKE jitter (tur_head.angles += (random-0.5)*damage on every hit) is STILL NOT implemented — TurretAI.Damage does friendly-fire + MOVE-shove only, no head jolt. Head does not flinch when shot. (Second-pass fix-up did not touch this.)"
    - "TFL_DMG_DEATH_NORESPAWN-only death FX/sound branch aside, the death blast is now Base-faithful: the port-added ammo-scaled RadiusDamage was removed from TurretAI.Die (Base turret_die has it commented out for all turrets)."
    - "Dual aim zPredict was true (PlasmaDualTurret.Think) diverging from Base plasma tr_setup (LEAD|SHOTTIMECOMPENSATE|SPLASH, no TFL_AIM_ZPREDICT); Wave-4 set it to false, resolving the single-vs-dual asymmetry."
    - "Head-frame wheel animation (tr_attack ++tur_head.frame; tr_think 0..6 idling at 0 and 3) STILL never implemented; muzzle/projectile/beam CSQC effects all NOTE-only (server head bone not modeled)."
    - "Respawn -0.2 hide/respawn split + EF_NODRAW hide-frame still not modeled (turret schedules Respawn directly at now+RespawnTime)."
features:
  - id: turret-plasma_dual.spawn.registration
    name: "Map spawnfunc + turret_initialize (registration, model, hitbox, lifecycle)"
    layer: [authority]
    base_refs:
      - "common/turrets/turret/plasma_dual.qc:spawnfunc(turret_plasma_dual)"
      - "common/turrets/sv_turrets.qc:turret_initialize"
      - "common/turrets/turret/plasma_dual.qh:DualPlasmaTurret"
    port_refs:
      - "src/XonoticGodot.Common/Gameplay/Turrets/TurretSpawnFuncs.cs:PlasmaDual"
      - "src/XonoticGodot.Common/Gameplay/MapObjects/MapObjectsRegistry.cs:207"
      - "src/XonoticGodot.Common/Gameplay/Turrets/TurretSpawn.cs:Init"
      - "src/XonoticGodot.Common/Gameplay/Turrets/PlasmaDualTurret.cs:Spawn"
    constants:
      - { name: "health", base: "500", port: "500", match: true }
      - { name: "mins", base: "'-32 -32 0'", port: "(-32,-32,0)", match: true }
      - { name: "maxs", base: "'32 32 64'", port: "(32,32,64)", match: true }
      - { name: "model", base: "models/turrets/base.md3", port: "models/turrets/base.md3", match: true }
      - { name: "head_model", base: "models/turrets/plasmad.md3", port: "none (head bone not modeled server-side)", match: false }
      - { name: "respawntime", base: "60", port: "60", match: true }
    status:
      logic: faithful
      values: faithful
      timing: faithful
      presentation: na
      audio: na
      liveness: live
    intended_divergence: false
    divergence_rationale: null
    gaps:
      - "head_model plasmad.md3 not used server-side (head-bone/tag_head networking is client-render, skipped)."
    verification:
      - { kind: code-trace, ref: "MapObjectsRegistry.cs:207 RegisterAll (GameInit.InstallGameplaySystems) -> GameWorld SpawnFuncs.TrySpawn -> TurretSpawnFuncs.Spawn -> Turrets registry + per-frame Think", result: pass }
    confidence: high
    notes: "Unchanged by Wave-2. g_turrets master switch honored. Subclass of PlasmaTurret per QC; registered [Turret]."

  - id: turret-plasma_dual.ai.think_loop
    name: "Per-frame AI: ammo regen, target scan throttle, validate, aim, track, firecheck, fire"
    layer: [authority]
    base_refs:
      - "common/turrets/sv_turrets.qc:turret_think"
      - "common/turrets/sv_turrets.qc:turret_firecheck"
      - "common/turrets/sv_turrets.qc:turret_fire"
    port_refs:
      - "src/XonoticGodot.Common/Gameplay/Turrets/TurretAI.cs:RunCombat"
      - "src/XonoticGodot.Common/Gameplay/Turrets/TurretAI.cs:Fire"
      - "src/XonoticGodot.Common/Gameplay/Turrets/PlasmaDualTurret.cs:Think"
    constants:
      - { name: "targetscan_mindelay", base: "0.1", port: "0.1 (Cvar g_turrets_targetscan_mindelay fallback)", match: true }
      - { name: "targetscan_maxdelay", base: "1 (autocvar default; turrets.cfg)", port: "1 (Cvar g_turrets_targetscan_maxdelay fallback; was hardcoded 0.6)", match: true }
      - { name: "aimidle_delay", base: "5", port: "5 (Cvar g_turrets_aimidle_delay fallback)", match: true }
      - { name: "nofire_master", base: "autocvar_g_turrets_nofire short-circuits fire", port: "Cvar g_turrets_nofire gate in Fire()", match: true }
      - { name: "ammo_recharge_per_s", base: "40", port: "40", match: true }
    status:
      logic: faithful
      values: faithful
      timing: faithful
      presentation: na
      audio: na
      liveness: live
    intended_divergence: false
    divergence_rationale: null
    gaps: []
    verification:
      - { kind: code-read, ref: "RunCombat scan-throttle now cvar-backed (Cvar() falls back to turrets.cfg defaults when store empty); maxdelay corrected 0.6->1 to match autocvar default; g_turrets_nofire master gate added in Fire()", result: pass }
    confidence: high
    notes: "Wave-2 IMPROVEMENT: targetscan_maxdelay default corrected from a hardcoded 0.6 to the Base autocvar default 1 (rescans less often, matching Base) and the g_turrets_nofire master-disable gate was added. Fire gate order matches turret_firecheck."

  - id: turret-plasma_dual.attack.plasma_ball
    name: "tr_attack non-instagib: electro-style splash plasma projectile"
    layer: [authority]
    base_refs:
      - "common/turrets/turret/plasma_dual.qc:tr_attack (SUPER PlasmaTurret)"
      - "common/turrets/turret/plasma_weapon.qc:PlasmaAttack.wr_think"
      - "common/turrets/sv_turrets.qc:turret_projectile"
    port_refs:
      - "src/XonoticGodot.Common/Gameplay/Turrets/PlasmaDualTurret.cs:Attack"
      - "src/XonoticGodot.Common/Gameplay/Turrets/PlasmaTurret.cs:Attack"
      - "src/XonoticGodot.Common/Gameplay/Turrets/TurretSpawn.cs:Projectile"
    constants:
      - { name: "shot_dmg", base: "80", port: "80", match: true }
      - { name: "shot_radius", base: "150", port: "150", match: true }
      - { name: "shot_speed", base: "2000", port: "2000", match: true }
      - { name: "shot_force", base: "100", port: "100", match: true }
      - { name: "shot_spread", base: "0.015", port: "0.015 (DualShotSpread; was 0.0125)", match: true }
      - { name: "edge_damage", base: "0", port: "0", match: true }
      - { name: "lifetime_s", base: "9", port: "9", match: true }
      - { name: "fire_sound", base: "hagar_fire (SND_PlasmaAttack_FIRE)", port: "weapons/hagar_fire.wav", match: true }
    status:
      logic: faithful
      values: faithful
      timing: faithful
      presentation: missing
      audio: faithful
      liveness: live
    intended_divergence: false
    divergence_rationale: null
    gaps:
      - "EFFECT_BLASTER_MUZZLEFLASH and PROJECTILE_ELECTRO_BEAM CSQC trail are NOTE-only (not emitted)."
    verification:
      - { kind: value-diff, ref: "turrets.cfg:366 g_turrets_unit_plasma_dual_shot_spread=0.015 == DualShotSpread 0.015f (PlasmaDualTurret.Attack re-emits its own TurretSpawn.Projectile with spread:DualShotSpread)", result: pass }
      - { kind: code-read, ref: "PlasmaDualTurret.Attack now builds its own non-instagib projectile (was base.Attack) so the 0.015 dual spread is used; same shot_dmg/radius/speed/force/edge0/9s lifetime", result: pass }
    confidence: high
    notes: "FIX (Wave-2): shot_spread 0.0125->0.015 now matches plasma_dual.cfg (both single AND dual cfg are 0.015; the old 0.0125 was the QC global initparams fallback). Dual now re-emits the ball locally with DualShotSpread instead of delegating to base.Attack. fire sound hagar_fire matches. CSQC muzzle/trail still NOTE-only."

  - id: turret-plasma_dual.attack.instagib_rail
    name: "tr_attack instagib branch: instant instakill railgun beam"
    layer: [authority]
    base_refs:
      - "common/turrets/turret/plasma_dual.qc:tr_attack (MUTATOR_IS_ENABLED mutator_instagib)"
    port_refs:
      - "src/XonoticGodot.Common/Gameplay/Turrets/PlasmaTurret.cs:Attack (instagib branch)"
      - "src/XonoticGodot.Common/Gameplay/Turrets/TurretCombat.cs:FireBullet"
    constants:
      - { name: "instagib_rail_damage", base: "1e10 (10000000000 — an instakill)", port: "1e10 (InstagibRailDamage; was 800)", match: true }
      - { name: "instagib_force", base: "800", port: "800 (InstagibRailForce; was 0)", match: true }
      - { name: "instagib_spread", base: "0 (no spread)", port: "0", match: true }
      - { name: "instagib_enable", base: "MUTATOR_IS_ENABLED(mutator_instagib)", port: "g_instagib != 0", match: true }
      - { name: "instagib_fire_sound", base: "none in tr_attack", port: "none (removed spurious electro_fire.wav)", match: true }
    status:
      logic: faithful
      values: faithful
      timing: faithful
      presentation: missing
      audio: faithful
      liveness: live
    intended_divergence: false
    divergence_rationale: null
    gaps:
      - "EFFECT_VORTEX_MUZZLEFLASH + team-coloured EFFECT_VAPORIZER_BEAM hit beam are NOTE-only (not emitted)."
      - "FireBullet is a single trace; QC FireRailgunBullet penetration/distance falloff deferred (matches WeaponFiring/TurretCombat documented gaps)."
    verification:
      - { kind: value-diff, ref: "plasma_dual.qc FireRailgunBullet(it,...,10000000000 /*dmg*/, false, 800 /*force*/,...) vs InstagibRailDamage=1e10 + InstagibRailForce=800", result: pass }
      - { kind: code-read, ref: "Dual inherits base.Attack instagib branch: FireBullet(spread 0, damage 1e10, force 800); the wrongly-added weapons/electro_fire.wav was removed (Base passes no fire sound)", result: pass }
    confidence: high
    notes: "FIX (Wave-2): the baseline mis-recorded Base damage as 800 and force as 0 — Base actually passes damage=1e10 (instakill) and force=800. Port now matches both, and the spurious electro_fire.wav fire sound was removed (Base plays none here). Dual still inherits this from base.Attack."

  - id: turret-plasma_dual.values.engagement_envelope
    name: "Engagement envelope: refire, ranges, ammo, aim/track tunables"
    layer: [authority]
    base_refs:
      - "turrets.cfg:g_turrets_unit_plasma_dual_* (lines 359-394)"
    port_refs:
      - "src/XonoticGodot.Common/Gameplay/Turrets/PlasmaDualTurret.cs"
      - "src/XonoticGodot.Common/Gameplay/Turrets/PlasmaDualTurret.cs:Think (direct TurretParams ctor)"
    constants:
      - { name: "shot_refire", base: "0.35", port: "0.35 (DualRefire)", match: true }
      - { name: "target_range", base: "3000", port: "3000 (DualTargetRange)", match: true }
      - { name: "target_range_min", base: "80", port: "80 (DualTargetRangeMin)", match: true }
      - { name: "target_range_optimal", base: "1000", port: "1000 (DualTargetRangeOptimal, now passed)", match: true }
      - { name: "ammo_max", base: "640", port: "640 (DualAmmoMax)", match: true }
      - { name: "ammo_recharge", base: "40", port: "40 (DualAmmoRecharge)", match: true }
      - { name: "aim_speed", base: "100", port: "100 (DualAimSpeed)", match: true }
      - { name: "aim_firetolerance_dist", base: "200", port: "200 (DualFireTolerance)", match: true }
      - { name: "aim_maxpitch", base: "30", port: "30 (AimMaxPitch)", match: true }
      - { name: "aim_maxrot", base: "360", port: "360 (AimMaxRot)", match: true }
      - { name: "track_type", base: "3 (FLUIDINERTIA)", port: "3 (TrackFluidInertia)", match: true }
      - { name: "track_accel_pitch", base: "0.5", port: "0.5 (DualTrackAccelPitch, passed)", match: true }
      - { name: "track_accel_rot", base: "0.7", port: "0.7 (DualTrackAccelRot, now passed)", match: true }
      - { name: "track_blendrate", base: "0.2", port: "0.2 (DualTrackBlendRate, now passed)", match: true }
    status:
      logic: faithful
      values: faithful
      timing: faithful
      presentation: na
      audio: na
      liveness: live
    intended_divergence: false
    divergence_rationale: null
    gaps: []
    verification:
      - { kind: value-diff, ref: "turrets.cfg:359-394 plasma_dual block vs PlasmaDualTurret consts; Think now builds the TurretParams DIRECTLY (not the single-plasma MakeParams) so rangeOptimal 1000 + track accel_rot 0.7 + blendrate 0.2 are all passed explicitly", result: pass }
    confidence: high
    notes: "FIX (Wave-2): target_range_optimal 500->1000, track_accel_rot 0.5->0.7, track_blendrate 0.35->0.2 now all fed via the dual's own TurretParams ctor in Think. All balance numbers now faithful."

  - id: turret-plasma_dual.target.scoring_biases
    name: "Target scoring biases (rangebias/samebias/anglebias/playerbias/missilebias)"
    layer: [authority]
    base_refs:
      - "common/turrets/sv_turrets.qc:turret_targetscore_generic"
      - "turrets.cfg:g_turrets_unit_plasma_dual_target_select_* (lines 376-380)"
    port_refs:
      - "src/XonoticGodot.Common/Gameplay/Turrets/TurretAI.cs:ScoreTarget"
      - "src/XonoticGodot.Common/Gameplay/Turrets/TurretAI.cs:SelectTarget"
      - "src/XonoticGodot.Common/Gameplay/Turrets/PlasmaDualTurret.cs:Think"
    constants:
      - { name: "target_select_rangebias", base: "0.2", port: "0.2 (DualRangeBias, now passed)", match: true }
      - { name: "target_select_samebias", base: "0.4", port: "0.4 (DualSameBias, now passed)", match: true }
      - { name: "target_select_anglebias", base: "0.4", port: "0.4 (DualAngleBias, now passed)", match: true }
      - { name: "target_select_playerbias", base: "1", port: "1 (DualPlayerBias)", match: true }
      - { name: "target_select_missilebias", base: "0", port: "0 (DualMissileBias; moot — SelectMissiles flag not set)", match: true }
    status:
      logic: faithful
      values: faithful
      timing: na
      presentation: na
      audio: na
      liveness: live
    intended_divergence: false
    divergence_rationale: null
    gaps: []
    verification:
      - { kind: code-read, ref: "ScoreTarget consumes p.RangeBias/AngleBias/MissileBias/PlayerBias; SelectTarget seeds with *p.SameBias; PlasmaDualTurret.Think now passes DualRangeBias 0.2 / DualSameBias 0.4 / DualAngleBias 0.4 / DualMissileBias 0 / DualPlayerBias 1 into the TurretParams ctor", result: pass }
    confidence: high
    notes: "FIX (Wave-2): rangebias/samebias/anglebias 1/1/1 -> 0.2/0.4/0.4 now fed; missilebias 0. The dual's own ctor in Think supplies all biases. (Single plasma's own MakeParams now also carries its 0.5/0.01/0.25 defaults.) missilebias still moot — Select lacks SelectMissiles."

  - id: turret-plasma_dual.aim.lead_splash_predict
    name: "Aiming: lead + shot-time compensation + z-predict + splash (aim at feet)"
    layer: [authority]
    base_refs:
      - "common/turrets/sv_turrets.qc:turret_aim_generic"
      - "common/turrets/turret/plasma.qc:tr_setup (aim_flags = LEAD|SHOTTIMECOMPENSATE|SPLASH)"
    port_refs:
      - "src/XonoticGodot.Common/Gameplay/Turrets/TurretAI.cs:AimPoint"
      - "src/XonoticGodot.Common/Gameplay/Turrets/PlasmaDualTurret.cs:Think"
    constants:
      - { name: "aim_lead", base: "TFL_AIM_LEAD set", port: "lead:true", match: true }
      - { name: "aim_shottimecompensate", base: "TFL_AIM_SHOTTIMECOMPENSATE set", port: "shotTimeCompensate:true", match: true }
      - { name: "aim_splash", base: "TFL_AIM_SPLASH set", port: "aimSplash:true", match: true }
      - { name: "aim_zpredict", base: "TFL_AIM_ZPREDICT NOT set (plasma tr_setup omits it)", port: "zPredict:false (dual Think) — now matches", match: true }
    status:
      logic: faithful
      values: faithful
      timing: faithful
      presentation: na
      audio: na
      liveness: live
    intended_divergence: false
    divergence_rationale: null
    gaps: []
    verification:
      - { kind: code-read, ref: "plasma.qc tr_setup aim_flags omits TFL_AIM_ZPREDICT; sv_turrets.qc gates the z-arc on (aim_flags & TFL_AIM_ZPREDICT). Both PlasmaTurret.MakeParams and PlasmaDualTurret.Think now pass zPredict:false.", result: pass }
    confidence: high
    notes: "FIX (Wave-4): dual Think zPredict:true -> false, matching the single plasma's MakeParams and Base plasma tr_setup (which never sets TFL_AIM_ZPREDICT). The single-vs-dual asymmetry from Wave-2 is resolved. lead/shottime/splash remain faithful."

  - id: turret-plasma_dual.track.head_motor
    name: "Head tracking: FLUIDINERTIA motor, pitch/rot clamps"
    layer: [authority]
    base_refs:
      - "common/turrets/sv_turrets.qc:turret_track"
    port_refs:
      - "src/XonoticGodot.Common/Gameplay/Turrets/TurretAI.cs:Track"
      - "src/XonoticGodot.Common/Gameplay/Turrets/PlasmaDualTurret.cs:Think"
    constants:
      - { name: "aim_maxpitch_clamp", base: "30", port: "30", match: true }
      - { name: "aim_maxrot_clamp", base: "360", port: "360", match: true }
      - { name: "track_type", base: "3 FLUIDINERTIA", port: "3 TrackFluidInertia", match: true }
      - { name: "track_accel_pitch", base: "0.5", port: "0.5 (DualTrackAccelPitch, passed)", match: true }
      - { name: "track_accel_rot", base: "0.7", port: "0.7 (DualTrackAccelRot, passed)", match: true }
      - { name: "track_blendrate", base: "0.2", port: "0.2 (DualTrackBlendRate, passed)", match: true }
    status:
      logic: faithful
      values: faithful
      timing: faithful
      presentation: na
      audio: na
      liveness: live
    intended_divergence: false
    divergence_rationale: null
    gaps: []
    verification:
      - { kind: code-read, ref: "Track FLUIDINERTIA branch uses p.TrackAccelPitch/Rot/BlendRate; PlasmaDualTurret.Think now passes 0.5/0.7/0.2 explicitly (was ctor-default 0.5/0.5/0.35)", result: pass }
    confidence: high
    notes: "FIX (Wave-2): track_accel_rot 0.7 + track_blendrate 0.2 now fed (was ctor-default 0.5/0.35). Motor algorithm faithful (per-axis accel*frametime clamp + avelocity blend + brake-at-limit); all constants now match plasma_dual.cfg."

  - id: turret-plasma_dual.damage.gating_die_respawn
    name: "Damage handling: friendly-fire scale, MOVE-shove, die + timed respawn"
    layer: [authority]
    base_refs:
      - "common/turrets/sv_turrets.qc:turret_damage"
      - "common/turrets/sv_turrets.qc:turret_die"
      - "common/turrets/sv_turrets.qc:turret_hide"
      - "common/turrets/sv_turrets.qc:turret_respawn"
    port_refs:
      - "src/XonoticGodot.Common/Gameplay/Turrets/TurretAI.cs:EventDamage"
      - "src/XonoticGodot.Common/Gameplay/Turrets/TurretAI.cs:Damage"
      - "src/XonoticGodot.Common/Gameplay/Turrets/TurretAI.cs:Die"
      - "src/XonoticGodot.Common/Gameplay/Turrets/TurretAI.cs:Respawn"
      - "src/XonoticGodot.Common/Gameplay/Turrets/TurretSpawn.cs:Init (GtEventDamage wiring)"
      - "src/XonoticGodot.Common/Gameplay/Damage/DamageSystem.cs:331 (EventDamage routing)"
    constants:
      - { name: "respawntime", base: "60", port: "60", match: true }
      - { name: "friendlyfire_scale", base: "g_friendlyfire (default 0 => no team dmg)", port: "g_friendlyfire honored", match: true }
      - { name: "death_blast", base: "RadiusDamage COMMENTED OUT in turret_die (no blast)", port: "none (RadiusDamage removed to match Base)", match: true }
      - { name: "inactive_takes_damage", base: "no (return)", port: "no (Damage returns 0 if !Active)", match: true }
    status:
      logic: faithful
      values: faithful
      timing: unknown
      presentation: missing
      audio: missing
      liveness: live
    intended_divergence: false
    divergence_rationale: null
    gaps:
      - "Respawn timing: Base hides (EF_NODRAW) at time+0.2 then turret_respawn at respawntime-0.2; port schedules Respawn directly at now+RespawnTime (60). The -0.2 hide/respawn split + EF_NODRAW hide-frame not modeled (timing unknown). UNCHANGED."
      - "Death FX/sound (SND_ROCKET_IMPACT + EFFECT_ROCKET_EXPLODE) only fire in Base on TFL_DMG_DEATH_NORESPAWN; plasma_dual always respawns so this branch never applies. tr_death is a no-op for plasma_dual."
    verification:
      - { kind: code-trace, ref: "Damage gate is LIVE: TurretSpawn.Init:90 sets e.GtEventDamage=TurretAI.EventDamage; DamageSystem.cs:331-333 routes a non-player edict with a GtEventDamage to it (and returns) -> TurretAI.EventDamage -> Damage() gate + TakeResource + Combat.Death -> OnAnyDeath -> Die", result: pass }
      - { kind: code-read, ref: "TurretAI.Die (line 742) no longer does WeaponSplash.RadiusDamage; matches sv_turrets.qc:182 //RadiusDamage commented out (no death blast)", result: pass }
      - { kind: code-read, ref: "TurretAI.Damage (line 673-696) no longer sets turret.Enemy=attacker; verified against Base turret_damage (sv_turrets.qc:207-251) which has NO `this.enemy=attacker` — TFL_DMG_RETALIATE is set (sv_turrets.qc:1274) but never read anywhere. The retaliation removal (commit 4be8357) closes a previously-undetected port-only divergence.", result: pass }
    confidence: high
    notes: "FIX (second-pass): the port-added retaliation (turret adopts whoever shot it as its enemy) was REMOVED from TurretAI.Damage — Base turret_damage never adopts the attacker (the TFL_DMG_RETALIATE flag is set but unread), so target acquisition is now scan/score-only as in Base. Earlier FIX: the port-added ammo-scaled RadiusDamage death blast in TurretAI.Die was removed, matching Base turret_die. The pre-damage gate (TurretAI.Damage) is wired live via GtEventDamage + DamageSystem routing. STILL OPEN: the unmodeled -0.2 hide/respawn split + EF_NODRAW hide-frame (timing unknown)."

  - id: turret-plasma_dual.damage.headshake
    name: "TFL_DMG_HEADSHAKE: jolt head angles by random*damage on hit"
    layer: [authority]
    base_refs:
      - "common/turrets/sv_turrets.qc:turret_damage (TFL_DMG_HEADSHAKE branch, line 228)"
      - "common/turrets/turret/plasma.qc:tr_setup (damage_flags |= TFL_DMG_HEADSHAKE)"
    port_refs: []
    constants:
      - { name: "headshake_jitter", base: "tur_head.angles.x/y += (random()-0.5)*damage", port: "none", match: false }
    status:
      logic: missing
      values: missing
      timing: na
      presentation: missing
      audio: na
      liveness: na
    intended_divergence: false
    divergence_rationale: null
    gaps:
      - "TFL_DMG_HEADSHAKE is set by plasma tr_setup but STILL NEVER honored: TurretAI.EventDamage/Damage do friendly-fire-scale + MOVE-shove + health subtract only — no tur_head.angles jolt (Base sv_turrets.qc:226-234: tur_head.angles.x/y += (random()-0.5)*damage + SendFlags TNSF_ANG). The damage path is live but the headshake branch was not ported. grep confirms zero headshake code in src/.../Turrets/ (only NOTE comments)."
    verification:
      - { kind: code-read, ref: "grep headshake/HeadAngles+=random across src/.../Turrets -> only NOTE comments; TurretAI.Damage + TurretAI.EventDamage bodies have no head-angle jitter", result: fail }
    confidence: high
    notes: "STILL OPEN. Head reacts to hits in Base (visible flinch that also nudges aim while alive); the port's head is rigid under fire. The damage gate is live but the headshake branch was not ported. Tracking concern: the server-side tur_head bone is not modeled in the port, so this is cross-file work shared with the head-frame-anim gap."

  - id: turret-plasma_dual.presentation.head_frame_anim
    name: "Head-frame wheel animation (tr_think 0..6 + ++frame on fire)"
    layer: [presentation]
    base_refs:
      - "common/turrets/turret/plasma_dual.qc:tr_think (frame!=0 && frame!=3 -> ++frame; >6 -> 0)"
      - "common/turrets/turret/plasma_dual.qc:tr_attack (++it.tur_head.frame)"
    port_refs: []
    constants:
      - { name: "head_frame_cycle", base: "0..6 (idle at 0 and 3, advance on fire)", port: "none", match: false }
    status:
      logic: missing
      values: missing
      timing: missing
      presentation: missing
      audio: na
      liveness: na
    intended_divergence: false
    divergence_rationale: null
    gaps:
      - "tur_head.frame wheel (0..6, idles at 0 and 3) STILL never implemented — the dual cannon head does not spin/cycle when firing; left as a client-render NOTE in PlasmaDualTurret.Attack. Wave-2 left an explicit NOTE that the server-side head bone (tur_head.frame) is not modeled (cross-file work)."
      - "Two-barrel tag alternation (tag_fire on plasmad.md3) not modeled — single muzzle origin."
    verification:
      - { kind: code-read, ref: "PlasmaDualTurret.Attack NOTE comment (head wheel cross-file); no frame/tur_head animation in port", result: fail }
    confidence: high
    notes: "STILL OPEN. Purely cosmetic; the doubled DualRefire already matches the effective fire rate so gameplay is unaffected. (Dual idles at 0 AND 3, range 0..6 — distinct from single plasma's 0..5.)"
