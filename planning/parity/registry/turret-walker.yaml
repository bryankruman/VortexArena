unit: turret-walker
category: turret
base_ref_rev: "v0.8.6-1779-g863cd3e84"
last_audited: "2026-06-28"
summary:
  feature_count: 12
  worst_gaps:
    - "No head entity (walker_head_minigun.md3): the minigun head is not a separate tracked/spinning tur_head entity (no tag attachment). The minigun muzzleflash now falls back to the shot org, and rocket muzzle alternation now selects tag_rocket01/02 via the model service but gracefully falls back to a single shot org when the body model is headless."
    - "Remaining CSQC walker_draw gap is now ONLY the head avelocity spin (pure client view): the 4-point ground-align over terrain IS now ported (WalkerTurret.DoMovement folds movelib_groundalign4point(300,100,0.25,45) pitch/roll onto the networked e.Angles via the shared VehiclePhysics.GroundAlign4Point, gated to the ground gaits). The per-gait Entity.Frame IS stamped server-side AND consumed client-side (the walker classifies as NetEntityKind.Generic -> frameDriven -> ModelAnimator.FollowEntityFrame), so the walk/run/melee gait animations play when walker_body.md3 has frame groups (FrameCount>1)."
features:
  - id: turret-walker.identity.def
    name: "Identity, hitbox, models, spawnflags"
    layer: [authority, presentation]
    base_refs:
      - "common/turrets/turret/walker.qh:WalkerTurret"
    port_refs:
      - "src/XonoticGodot.Common/Gameplay/Turrets/WalkerTurret.cs:WalkerTurret"
    constants:
      - { name: "mins", base: "'-70 -70 0'", port: "(-70,-70,0)", match: true }
      - { name: "maxs", base: "'70 70 95'", port: "(70,70,95)", match: true }
      - { name: "model", base: "models/turrets/walker_body.md3", port: "models/turrets/walker_body.md3", match: true }
      - { name: "head_model", base: "walker_head_minigun.md3", port: "none (no head bone entity)", match: false }
      - { name: "netname", base: "walker", port: "walker", match: true }
      - { name: "health", base: "500", port: "500", match: true }
      - { name: "spawnflags", base: "TUR_FLAG_PLAYER|TUR_FLAG_MOVE", port: "movable=true (MOVE); PLAYER select implicit", match: true }
    status:
      logic: faithful
      values: faithful
      timing: na
      presentation: partial
      audio: na
      liveness: live
    intended_divergence: false
    divergence_rationale: null
    gaps:
      - "Head model (walker_head_minigun.md3) not instantiated as a separate tur_head entity; no tag attachment, so the minigun head never visibly tracks."
    verification:
      - { kind: code_read, ref: "walker.qh vs WalkerTurret ctor + Spawn", result: pass }
    confidence: high
    notes: "Unchanged: identity/hitbox/body model/health faithful; head bone still not modeled."
  - id: turret-walker.setup.tr_setup
    name: "tr_setup: SlideBox/Step creature, select flags, ammo flags, home pose"
    layer: [authority]
    base_refs:
      - "common/turrets/turret/walker.qc:568"
      - "common/turrets/sv_turrets.qc:turret_initialize"
    port_refs:
      - "src/XonoticGodot.Common/Gameplay/Turrets/WalkerTurret.cs:Spawn"
      - "src/XonoticGodot.Common/Gameplay/Turrets/TurretSpawn.cs:Init"
    constants:
      - { name: "solid", base: "SOLID_SLIDEBOX", port: "Solid.SlideBox", match: true }
      - { name: "movetype", base: "MOVETYPE_STEP", port: "MoveType.Step", match: true }
      - { name: "takedamage", base: "DAMAGE_AIM", port: "DamageMode.Aim", match: true }
      - { name: "select_flags", base: "PLAYERS|RANGELIMITS|TEAMCHECK|LOS", port: "Players|RangeLimits|TeamCheck|Los", match: true }
      - { name: "ammo_flags", base: "BULLETS|RECHARGE|RECIEVE", port: "ammo pool 4000/recharge 100 (flags not modeled)", match: true }
      - { name: "ammo_max", base: "4000", port: "4000", match: true }
      - { name: "ammo_recharge", base: "100", port: "100", match: true }
      - { name: "aim_flags", base: "TFL_AIM_LEAD", port: "lead=true", match: true }
      - { name: "ground-snap home pose", base: "non-STEP first spawn: tracebox down 10000, origin = endpos+'0 0 4', store pos1/pos2", port: "tracebox from origin+'0 0 128' down 10000, origin = endpos+'0 0 4' (Spawn:111-121), then home pose stored for OnRespawn", match: true }
    status:
      logic: faithful
      values: faithful
      timing: na
      presentation: na
      audio: na
      liveness: live
    intended_divergence: false
    divergence_rationale: null
    gaps: []
    verification:
      - { kind: code_read, ref: "walker.qc tr_setup vs Spawn/Init; Select const = Players|RangeLimits|TeamCheck|Los; ground-snap drop now done in Spawn (tracebox down 10000 with mins/maxs, +'0 0 4') before Init/STEP, home = post-drop origin", result: pass }
    confidence: high
    notes: "FIXED since last audit: the first-spawn ground-snap home pose is now ported (Spawn tracebox-drops the map origin to the floor before switching to STEP, mirroring Base tr_setup). Select-flag set already matched Base."
  - id: turret-walker.spawn.liveness
    name: "Map spawnfunc wiring + per-frame think (turret_walker placed on map)"
    layer: [authority]
    base_refs:
      - "common/turrets/turret/walker.qc:350"
      - "common/turrets/sv_turrets.qc:turret_link"
    port_refs:
      - "src/XonoticGodot.Common/Gameplay/Turrets/TurretSpawnFuncs.cs:Walker"
      - "src/XonoticGodot.Common/Gameplay/MapObjects/MapObjectsRegistry.cs:238"
    constants:
      - { name: "g_turrets master switch", base: "1 (default on)", port: "MasterSwitchEnabled default-on", match: true }
    status:
      logic: faithful
      values: faithful
      timing: faithful
      presentation: na
      audio: na
      liveness: live
    intended_divergence: false
    divergence_rationale: null
    gaps: []
    verification:
      - { kind: code_read, ref: "MapObjectsRegistry registers turret_walker -> TurretSpawnFuncs.Walker -> Spawn(walker); Spawn re-arms OnRespawn + Think runs the combat/move loop", result: pass }
    confidence: high
    notes: "Still wired: spawnfunc registered, descriptor auto-registered via [Turret], Think drives RunCombat+DoMovement. Whether a shipped map actually places one is unconfirmed."
  - id: turret-walker.combat.brain
    name: "Acquire/aim/fire combat brain (shared turret_think framework)"
    layer: [authority]
    base_refs:
      - "common/turrets/sv_turrets.qc:turret_think"
      - "common/turrets/sv_turrets.qc:turret_select_target"
      - "common/turrets/sv_turrets.qc:turret_firecheck"
    port_refs:
      - "src/XonoticGodot.Common/Gameplay/Turrets/TurretAI.cs:RunCombat"
      - "src/XonoticGodot.Common/Gameplay/Turrets/WalkerTurret.cs:Think"
    constants:
      - { name: "targetscan_mindelay", base: "0.1", port: "0.1 (cvar g_turrets_targetscan_mindelay)", match: true }
      - { name: "targetscan_maxdelay", base: "1", port: "1 (cvar g_turrets_targetscan_maxdelay default 1)", match: true }
      - { name: "aimidle_delay", base: "5", port: "5", match: true }
      - { name: "target_range", base: "5000", port: "5000", match: true }
      - { name: "target_range_optimal", base: "100", port: "100", match: true }
      - { name: "firetolerance_dist", base: "100", port: "100", match: true }
      - { name: "walker_firecheck (no-fire while ANIM_MELEE)", base: "yes", port: "MeleeUntil gate in Think", match: true }
    status:
      logic: faithful
      values: faithful
      timing: faithful
      presentation: na
      audio: na
      liveness: live
    intended_divergence: false
    divergence_rationale: null
    gaps: []
    verification:
      - { kind: code_read, ref: "RunCombat vs turret_think/firecheck; Think MeleeUntil gate vs walker_firecheck (WalkerTurret.cs:159-167)", result: pass }
    confidence: high
    notes: "Unchanged-faithful: combat flow + fire-while-melee block faithful; targetscan_maxdelay cvar-backed default 1 matching Base."
  - id: turret-walker.drive.chase
    name: "tr_think locomotion: chase enemy (run/walk) + body yaw"
    layer: [authority]
    base_refs:
      - "common/turrets/turret/walker.qc:421"
      - "common/turrets/turret/walker.qc:249"
      - "common/turrets/turret/walker.qc:463"
    port_refs:
      - "src/XonoticGodot.Common/Gameplay/Turrets/WalkerTurret.cs:DoMovement"
      - "src/XonoticGodot.Common/Gameplay/Turrets/WalkerTurret.cs:WalkerMoveTo"
      - "src/XonoticGodot.Common/Gameplay/Turrets/WalkerTurret.cs:ApplyGait"
    constants:
      - { name: "speed_run", base: "300", port: "300", match: true }
      - { name: "speed_walk", base: "200", port: "200", match: true }
      - { name: "run/walk threshold", base: "dist>500", port: "dist>500", match: true }
      - { name: "turn_run", base: "7", port: "7", match: true }
      - { name: "turn_walk", base: "15", port: "15", match: true }
      - { name: "attract2 params", base: "(0.5,500,0.95)", port: "(0.5,500,0.95)", match: true }
      - { name: "move friction (move_simple blendrate)", base: "0.6", port: "0.6", match: true }
      - { name: "body-yaw turn step", base: "angles.y += bound(-turny, shortangle, turny) — raw degrees PER THINK (no frametime)", port: "angles.y += bound(-turny, shortangle, turny) — raw per-think (ApplyGait, no frametime)", match: true }
    status:
      logic: faithful
      values: faithful
      timing: faithful
      presentation: na
      audio: na
      liveness: live
    intended_divergence: false
    divergence_rationale: null
    gaps: []
    verification:
      - { kind: code_read, ref: "ApplyGait (WalkerTurret.cs:319-379): turny applied raw via bound(-turny, ShortAngle(realAngle.Y,..), turny) with NO frameTime factor", result: pass }
    confidence: high
    notes: "Unchanged-faithful: body-yaw turn applied as raw degrees once per think (matching Base turret_think nextthink=time). Speeds/threshold/attract2/friction faithful."
  - id: turret-walker.drive.idle_roam
    name: "Idle roam/wander + last-seen pursuit + waypoint path following"
    layer: [authority]
    base_refs:
      - "common/turrets/turret/walker.qc:356"
      - "common/turrets/turret/walker.qc:279"
      - "common/turrets/turret/walker.qc:326"
    port_refs:
      - "src/XonoticGodot.Common/Gameplay/Turrets/WalkerTurret.cs:IdleSelect"
    constants:
      - { name: "idle stop time", base: "1 + random*5", port: "1 + Prandom.Float()*5", match: true }
      - { name: "roam walk time (TSL_ROAM, 50%)", base: "4 + random*2", port: "now + 4 + Prandom.Float()*2 (TslRoam spawnflag plumbed; IdleSelect roam branch)", match: true }
      - { name: "last-seen chase window", base: "<=10s and within 128u", port: "<=10s and within 128u", match: true }
      - { name: "wander moveto blend", base: "moveto*0.9 + (origin+v_forward*500+randomvec*400)*0.1", port: "moveto*0.9 + (origin+fwd*500 + Prandom.Vec()*400)*0.1", match: true }
      - { name: "wander LOS probe", base: "traceline ahead 128 + down 256, set idletime=-1337 to re-pick", port: "fwd 128 + down 256 probes, HeadIdleTime=-1337", match: true }
      - { name: "path node proximity", base: "64 (turret_checkpoint chain)", port: "64 (PathNodeProximity; WalkerMovePath advances PathCurrent via CloseToTarget)", match: true }
      - { name: "TSF_NO_PATHBREAK (stick to path with enemy)", base: "walker.qc:360", port: "TsfNoPathbreak = BIT(3); DoMovement:228 sticks to WalkerMovePath even with an enemy", match: true }
      - { name: "speed_roam", base: "100", port: "100 (SpeedRoam; reached via Gait.Roam — assigned only in Base via ANIM_ROAM, which the SVQC brain itself never sets, parity-by-omission)", match: true }
    status:
      logic: faithful
      values: faithful
      timing: faithful
      presentation: na
      audio: na
      liveness: live
    intended_divergence: false
    divergence_rationale: null
    gaps: []
    verification:
      - { kind: code_read, ref: "IdleSelect (WalkerTurret.cs:384-438): enemy_last_loc/time pursuit + LOS-probe wander + idle stop/roam cycle (TslRoam-gated roam branch) vs walker.qc:362-419; called live from DoMovement when e.Enemy is null and no path. WalkerMovePath (WalkerTurret.cs:340-352) + WalkerFindTarget (176-182) + NextCheckpoint (359-364) consume the turret_checkpoint chain via TurretState.PathCurrent; wired live from Spawn (WalkerFindTarget at :167, re-armed in OnRespawn) and DoMovement (TsfNoPathbreak branch :228, no-enemy branch :235).", result: pass }
    confidence: high
    notes: "FIXED since last audit: (1) the walker brain now consumes the turret_checkpoint waypoint chain — WalkerFindTarget resolves the entry checkpoint by targetname, WalkerMovePath advances the PathCurrent chain (NextCheckpoint follows checkpoint.target), and the TSF_NO_PATHBREAK flag (TsfNoPathbreak) makes it stick to the path even with an enemy in view; (2) the TSL_ROAM roam-walk branch is now plumbed (TslRoam-gated, 4+random*2s roam). Logic is now faithful."
  - id: turret-walker.drive.gaits_extra
    name: "Extra gaits: swim / jump / land / pain / strafe / turn"
    layer: [authority, presentation]
    base_refs:
      - "common/turrets/turret/walker.qc:471"
      - "common/turrets/turret/walker.qc:249"
    port_refs:
      - "src/XonoticGodot.Common/Gameplay/Turrets/WalkerTurret.cs:WalkerMoveTo"
      - "src/XonoticGodot.Common/Gameplay/Turrets/WalkerTurret.cs:ApplyGait"
    constants:
      - { name: "speed_swim", base: "200", port: "200", match: true }
      - { name: "speed_jump", base: "800", port: "800 (Gait.Jump case, never assigned)", match: true }
      - { name: "turn_swim", base: "10", port: "10", match: true }
      - { name: "turn_strafe", base: "5", port: "none (no STRAFE gait)", match: false }
      - { name: "turn (ANIM_TURN)", base: "20", port: "20 (Gait.Turn case, never assigned)", match: true }
      - { name: "swim bob", base: "vz += sin(time*4)*8", port: "vz += sin(now*4)*8 (Swim case)", match: true }
      - { name: "swim pitch clamp", base: "angles.x += bound(-10, shortangle, 10)", port: "bound(-10, ShortAngle, 10)", match: true }
      - { name: "pain defer", base: "walker_setnoanim 0.25", port: "none (no PAIN gait)", match: false }
    status:
      logic: partial
      values: faithful
      timing: faithful
      presentation: missing
      audio: na
      liveness: live
    intended_divergence: false
    divergence_rationale: null
    gaps:
      - "No standalone STRAFE_L/R or PAIN gaits (Base never drives these from the SVQC brain either — they are leftover model anim states — so this is parity-by-omission, not a behavioral gap)."
      - "JUMP/LAND/TURN gait cases exist in ApplyGait but are never assigned (same as Base — the brain never sets them)."
    verification:
      - { kind: code_read, ref: "WalkerMoveTo waterlevel switch + ApplyGait Swim/Jump/Land/Turn/Roam cases vs walker.qc:471-537. SWIM gait is LIVE: the walker is MOVETYPE_STEP, and MoveTypePhysics routes Step through WalkMove -> CheckWater -> WaterLevel every tick, so the WETFEET/SWIMMING/SUBMERGED branches fire.", result: pass }
      - { kind: code_read, ref: "WalkerMoveTo case 0 (WATERLEVEL_NONE) does `goto case 1` (no break) — faithfully reproducing Base's fall-through (walker.qc:253-264).", result: pass }
    confidence: high
    notes: "Unchanged: SWIM gait ported faithfully AND reachable (STEP turrets get WaterLevel via WalkMove/CheckWater each tick). JUMP/LAND/TURN/STRAFE/PAIN are unreachable in Base too (parity-by-omission)."
  - id: turret-walker.weapon.minigun
    name: "WalkerTurretAttack: near-hitscan minigun (fireBullet + force)"
    layer: [authority]
    base_refs:
      - "common/turrets/turret/walker_weapon.qc:6"
      - "common/turrets/sv_turrets.qc:fireBullet path"
    port_refs:
      - "src/XonoticGodot.Common/Gameplay/Turrets/WalkerTurret.cs:Attack"
      - "src/XonoticGodot.Common/Gameplay/Turrets/TurretCombat.cs:FireBullet"
    constants:
      - { name: "shot_dmg", base: "5", port: "5", match: true }
      - { name: "shot_spread", base: "0.025", port: "0.025", match: true }
      - { name: "shot_force", base: "10", port: "10", match: true }
      - { name: "shot_refire", base: "0.05", port: "0.05", match: true }
      - { name: "shot_volly", base: "10", port: "10", match: true }
      - { name: "shot_volly_refire", base: "1", port: "1", match: true }
      - { name: "shot_speed (lead-aim)", base: "18000", port: "18000", match: true }
      - { name: "deathtype", base: "DEATH_TURRET_WALK_GUN", port: "DeathTypes.TurretWalkGun", match: true }
      - { name: "fire sound", base: "uzi_fire", port: "weapons/uzi_fire.wav", match: true }
    status:
      logic: faithful
      values: faithful
      timing: faithful
      presentation: faithful
      audio: faithful
      liveness: live
    intended_divergence: false
    divergence_rationale: null
    gaps:
      - "Muzzleflash + bullet tracer are emitted at the shared shot org (st.ShotOrg) rather than a dedicated head muzzle tag, since the port has no tur_head sub-entity. Both effects ARE emitted (networked, viewer-independent)."
    verification:
      - { kind: test, ref: "MonsterTurretVehicleObituaryTests (shared turret-gun deathtype, walker-gun call site)", result: pass }
      - { kind: code_read, ref: "walker_weapon.qc wr_think (EFFECT_BULLET + EFFECT_BLASTER_MUZZLEFLASH, both OUTSIDE the isPlayer gate) vs Attack (WalkerTurret.cs:582-603): FireBullet(..., tracerEffect: BULLET) emits the EFFECT_BULLET trail and EffectEmitter.Emit(BLASTER_MUZZLEFLASH, st.ShotOrg, dir*1000f, 1) emits the muzzleflash, both server-side networked effects. ShotSpeed const 18000.", result: pass }
    confidence: high
    notes: "FIXED since last audit: the EFFECT_BLASTER_MUZZLEFLASH and EFFECT_BULLET tracer are now emitted server-side (the same convention as the ewheel/flac/machinegun turrets), so the minigun presentation is now faithful. Only divergence: emitted at the shot org, not a head tag (no tur_head sub-entity in the port)."
  - id: turret-walker.weapon.rocket
    name: "Rocket volley: 4 homing rockets + reload (walker_fire_rocket / walker_rocket_think)"
    layer: [authority]
    base_refs:
      - "common/turrets/turret/walker.qc:204"
      - "common/turrets/turret/walker.qc:108"
      - "common/turrets/turret/walker.qc:435"
    port_refs:
      - "src/XonoticGodot.Common/Gameplay/Turrets/WalkerTurret.cs:FireRocket"
      - "src/XonoticGodot.Common/Gameplay/Turrets/GuidedProjectile.cs:WalkerRocketThink"
      - "src/XonoticGodot.Common/Gameplay/Turrets/GuidedProjectile.cs:WalkerRocketLoop"
    constants:
      - { name: "rocket_damage", base: "45", port: "45", match: true }
      - { name: "rocket_radius", base: "150", port: "150", match: true }
      - { name: "rocket_force", base: "150", port: "150", match: true }
      - { name: "rocket_speed", base: "1000", port: "1000", match: true }
      - { name: "rocket_turnrate", base: "0.05", port: "0.05", match: true }
      - { name: "rocket_range", base: "4000", port: "4000", match: true }
      - { name: "rocket_range_min", base: "500", port: "500", match: true }
      - { name: "rocket_refire (reload)", base: "10", port: "10", match: true }
      - { name: "burst size", base: "4 rockets, 0.2s apart", port: "4 rockets, 0.2s apart", match: true }
      - { name: "rocket hp / TTL", base: "25 / time+9", port: "25 / ttl 9", match: true }
      - { name: "jitter re-roll", base: "0.5s, randomvec*min(edist,...)", port: "0.5s, Prandom.Vec*min(edist,...)", match: true }
      - { name: "launch sound", base: "SND_TUR_WALKER_FIRE = hagar_fire", port: "weapons/hagar_fire.wav", match: true }
      - { name: "te_explosion launch flash", base: "yes", port: "EffectEmitter.TeExplosion(st.ShotOrg) in FireRocket:425", match: true }
      - { name: "walker_rocket_loop maneuver (1%)", base: "yes (up-and-over): 1% on launch AND 1% per in-flight think -> loop/loop2/loop3", port: "yes: 1% on launch (Launch:142) + 1%/think (WalkerRocketThink:346) -> WalkerRocketLoop/2/3 with InLoop sentinel", match: true }
      - { name: "tag_rocket01/02 muzzle alternation", base: "yes (gettaginfo of tag_rocket01/02 by shot_volly parity)", port: "yes: FireRocket selects tag_rocket01/02 by post-decrement volly parity and resolves it via Api.Models.TryGetTag; falls back to st.ShotOrg if the body model is headless", match: true }
    status:
      logic: faithful
      values: faithful
      timing: faithful
      presentation: faithful
      audio: faithful
      liveness: live
    intended_divergence: false
    divergence_rationale: null
    gaps:
      - "Tag resolution is graceful-fallback: if the loaded walker_body.md3 has no tag_rocket01/02 bones, all rockets fire from the shared shot org (the port has no separate tur_head sub-entity that owns the tags)."
    verification:
      - { kind: code_read, ref: "FireRocket (WalkerTurret.cs:551-578): muzzleTag = (RocketVolly>1 ? tag_rocket01 : tag_rocket02) matching walker.qc:447 post-decrement parity, resolved via Api.Models.TryGetTag(turret, muzzleTag,...) with graceful st.ShotOrg fallback; plays weapons/hagar_fire.wav + EffectEmitter.TeExplosion(org). WalkerRocketThink (GuidedProjectile.cs:332-364) has the random()<0.01 loop roll + InLoop sentinel; WalkerRocketLoop/Loop2/Loop3 (GuidedProjectile.cs:372-421) model the up-and-over (+300 up, then back to start, then -200) faithful to walker_weapon.qc:153-202; Launch:144-150 has the 1% launch roll. Ballistics/damage/burst/jitter/steer-pull faithful.", result: pass }
    confidence: high
    notes: "FIXED since last audit: (1) tag_rocket01/02 muzzle alternation is now ported (post-decrement volly parity resolved via Api.Models.TryGetTag), so presentation is now faithful; (2) the walker_rocket_loop up-and-over maneuver and the te_explosion launch flash were already ported. Base emits NO EFFECT_BLASTER_MUZZLEFLASH for rockets (only te_explosion), so there was never a muzzleflash gap here."
  - id: turret-walker.weapon.melee
    name: "Melee bite (walker_melee_do_dmg): radius hit in front"
    layer: [authority]
    base_refs:
      - "common/turrets/turret/walker_weapon.qc:51"
      - "common/turrets/turret/walker.qc:423"
      - "common/turrets/turret/walker.qc:514"
    port_refs:
      - "src/XonoticGodot.Common/Gameplay/Turrets/WalkerTurret.cs:DoMovement"
      - "src/XonoticGodot.Common/Gameplay/Turrets/WalkerTurret.cs:ScheduleMelee"
    constants:
      - { name: "melee_range", base: "100", port: "100", match: true }
      - { name: "melee_damage", base: "100", port: "100", match: true }
      - { name: "melee_force", base: "600", port: "600", match: true }
      - { name: "facing gate", base: "|wish_angle.y| < 15", port: "|wishAngle.Y| < 15", match: true }
      - { name: "findradius", base: "origin + v_forward*128, 32", port: "origin + fwd*128, 32", match: true }
      - { name: "damage defer", base: "0.21s", port: "0.21s", match: true }
      - { name: "swing lock (setnoanim)", base: "0.41s", port: "0.41s (MeleeUntil)", match: true }
      - { name: "deathtype", base: "DEATH_TURRET_WALK_MELEE", port: "DeathTypes.TurretWalkMelee", match: true }
    status:
      logic: faithful
      values: faithful
      timing: faithful
      presentation: na
      audio: na
      liveness: live
    intended_divergence: false
    divergence_rationale: null
    gaps: []
    verification:
      - { kind: code_read, ref: "walker_melee_do_dmg + ANIM_MELEE defers vs DoMovement melee branch + ScheduleMelee", result: pass }
    confidence: high
    notes: "Unchanged-faithful: melee range/facing/radius/damage/force and the deferred-damage + swing-lock timings all match Base."
  - id: turret-walker.head.track
    name: "Head rotation: track type + aim clamps"
    layer: [authority]
    base_refs:
      - "common/turrets/sv_turrets.qc:turret_track"
    port_refs:
      - "src/XonoticGodot.Common/Gameplay/Turrets/TurretAI.cs:Track"
      - "src/XonoticGodot.Common/Gameplay/Turrets/WalkerTurret.cs:Think"
    constants:
      - { name: "aim_speed", base: "45", port: "45", match: true }
      - { name: "aim_maxrot", base: "90", port: "90", match: true }
      - { name: "aim_maxpitch", base: "15", port: "15", match: true }
      - { name: "track_type", base: "1 (STEPMOTOR)", port: "1 (TrackStepMotor)", match: true }
    status:
      logic: faithful
      values: faithful
      timing: faithful
      presentation: na
      audio: na
      liveness: live
    intended_divergence: false
    divergence_rationale: null
    gaps: []
    verification:
      - { kind: code_read, ref: "Think passes trackType: TurretAI.TrackStepMotor (WalkerTurret.cs:156); Track honors STEPMOTOR with hard per-frame angle clamp; turrets.cfg track_type 1", result: pass }
    confidence: high
    notes: "Unchanged-faithful: track_type STEPMOTOR(1), hard per-frame angle clamp; aim speed + clamps match."
  - id: turret-walker.client.draw
    name: "CSQC walker_draw + locomotion anim frames"
    layer: [presentation]
    base_refs:
      - "common/turrets/turret/walker.qc:615"
      - "common/turrets/turret/walker.qc:558"
      - "common/turrets/sv_turrets.qc:turrets_setframe"
    port_refs:
      - "src/XonoticGodot.Common/Gameplay/Turrets/WalkerTurret.cs:DoMovement"
    constants:
      - { name: "groundalign4point", base: "(300,100,0.25,45)", port: "(300,100,0.25,45) — VehiclePhysics.GroundAlign4Point folds pitch(X)/roll(Z) onto networked e.Angles (WalkerTurret.cs:304-308), gated to ground gaits (skip while WaterLevel>=2 / Swim)", match: true }
      - { name: "spark health threshold", base: "127", port: "127 (SparkHealthThreshold)", match: true }
      - { name: "spark chance/frame", base: "0.15", port: "0.15 (SparkChance)", match: true }
      - { name: "spark TE", base: "te_spark(origin+'0 0 40', randomvec*256+'0 0 256', 16)", port: "EffectEmitter.TeSpark(origin+'0 0 40', Prandom.Vec*256+'0 0 256', 16)", match: true }
      - { name: "client movetype", base: "MOVETYPE_BOUNCE + gravity 1 (client render copy)", port: "none (pure-client render movetype; server drives origin via MoveType.Step + shared entity feed)", match: false }
      - { name: "anim frames", base: "12-state animflag via turrets_setframe + TNSF_MOVE", port: "Entity.Frame stamped server-side + consumed client-side via Generic->frameDriven->ModelAnimator.FollowEntityFrame (needs walker_body.md3 FrameCount>1)", match: true }
    status:
      logic: faithful
      values: partial
      timing: faithful
      presentation: partial
      audio: na
      liveness: live
    intended_divergence: false
    divergence_rationale: null
    gaps:
      - "Head avelocity spin is the ONLY remaining unported walker_draw piece: Base's CSQC copy spins the head model by its avelocity; the port has no tur_head sub-entity so the minigun head never visibly spins (tracked on the identity row's head-model gap). Everything else in walker_draw now has a server-side counterpart."
      - "Client render-copy MOVETYPE_BOUNCE + gravity 1 has no direct counterpart (values:partial): the port drives the walker authoritatively on the server (MoveType.Step) and nets the origin through the shared entity feed instead of a CSQC bounce sim — gameplay-equivalent, but the client-side bounce/gravity render nicety is absent."
    verification:
      - { kind: code_read, ref: "Low-hp te_spark (<127 hp, 15%/frame) emitted server-side in DoMovement (WalkerTurret.cs:288-294) as a networked temp-entity (EffectEmitter.TeSpark -> Sink.Emit), faithful to walker.qc:627-629. Gait frame: SetFrame (WalkerTurret.cs:315/327-330) writes Entity.Frame=animflag; ServerNet.cs:2210 nets Frame=(int)e.Frame; ServerNet.Classify (ServerNet.cs:2600) returns NetEntityKind.Generic for the turret; ClientEntityView.cs:216-219 routes Generic with frameDriven:true; ClientWorld.cs:1728 sets ModelAnimator.FollowEntityFrame from _frameDriven; ModelAnimator.cs:275-290 plays Entity.Frame as the raw frame (gated on FrameCount>1). Gait animation IS consumed.", result: pass }
      - { kind: code_read, ref: "Ground-align: WalkerTurret.cs:304-308 calls VehiclePhysics.GroundAlign4Point(e, fwd, right, up, 300,100,0.25,45), gated to ws.AnimFlag != Swim && e.WaterLevel < 2. VehiclePhysicsHelpers.cs:135-165 4-point-traces the springs and writes ONLY pitch (ang.X) and roll (ang.Z) onto e.Angles via the blendRate lerp (yaw untouched) — both networked quantities reach every client; same shared helper Spiderbot uses. Folded server-side, faithful to movelib_groundalign4point(300,100,0.25,45) at walker.qc:615.", result: pass }
    confidence: high
    notes: "FIXED since the (already-re-audited) baseline: the CSQC walker_draw 4-point GROUND-ALIGN is now ported — DoMovement folds movelib_groundalign4point pitch/roll onto the networked e.Angles via the shared VehiclePhysics.GroundAlign4Point (gated to the ground gaits so it never fights the SWIM pitch). Combined with the already-consumed per-gait Entity.Frame (Generic->frameDriven->ModelAnimator.FollowEntityFrame) and the server-side low-hp te_spark, the only remaining walker_draw gap is the pure-client head avelocity spin (no tur_head sub-entity, tracked on the identity row) plus the client render-copy MOVETYPE_BOUNCE+gravity nicety. logic/timing -> faithful; presentation stays partial (head spin); values stays partial (client bounce movetype)."
