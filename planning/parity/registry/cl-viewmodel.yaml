unit: cl-viewmodel
category: client
base_ref_rev: "v0.8.6-1779-g863cd3e84"
last_audited: "2026-06-27"  # re-audited (consolidated re-verify): casings timing partial->faithful + gap cleared; all other rows confirmed accurate against current port
summary:
  feature_count: 13
  worst_gaps:
    - "Per-child m_skin (wepskin) is not applied; the gun renders with its base material. Team colormap is an approximate whole-model albedo tint, not a true palette colormap swap (colormap/glowmod ARE now applied — colormod + Vortex/team glow)."
    - "Weapon-switch animation is a slide-down, not Base's barrel-tip (this.angles_x = -90*f*f), and is not keyed to per-weapon switchdelay_raise/drop or weapon_nextthink (port does not network wframe)."
    - "No networked weapon fire/idle/reload animation (the QC wframe temp entity is not networked); the gun never visibly cycles or reloads."
    - "Muzzle-flash MODEL md3 frame is not cycled (port spawns a static node and fades/shrinks it without advancing the md3 flash frame); all 7 Base m_muzzlemodel weapons are now mapped."
features:
  - id: cl-viewmodel.model.select_and_rig
    name: "View-model entity + model select (invisible-hand vs full-rig), rebuild on weapon change"
    layer: [presentation]
    base_refs:
      - "client/view.qc:viewmodel_draw"
      - "common/weapons/all.qc:CL_WeaponEntity_SetModel"
    port_refs:
      - "game/client/ViewModel.cs:SetWeaponModel"
      - "game/client/ViewModelEquip.cs:Build"
      - "game/net/NetGame.cs:EquipNetworkedWeapon"
    constants:
      - { name: "r_drawviewmodel", base: "1", port: "implicit (always drawn when alive/1st-person)", match: true }
      - { name: "anim framegroups (fire1/fire2/idle/reload)", base: "'0 1 0.01' / '1 1 0.01' / '2 1 0.01' / '3 1 0.01'", port: "best-effort local clip (no framegroup preset)", match: false }
    status:
      logic: faithful
      values: faithful
      timing: faithful
      presentation: faithful
      audio: na
      liveness: live
    intended_divergence: false
    divergence_rationale: null
    gaps:
      - "anim framegroup presets (animfixfps fire1/fire2/idle/reload) not preset; port plays a best-effort local clip."
    verification:
      - { kind: code, ref: "ViewModel.SetWeaponModel + ViewModelEquip.Build invisible-hand/full-rig branch; EquipNetworkedWeapon rebuilds on weapon-id or wr_viewmodel change (NetGame:1624), hides on dead/holster/chase/intermission", result: pass }
    confidence: high
    notes: "Invisible-hand (v_ attached to h_ rig 'weapon'/'tag_weapon' tag) vs full-rig classification is faithful to CL_WeaponEntity_SetModel. movedir from 'shot' tag drives shot origin. Live on NetGame match path."

  - id: cl-viewmodel.anim.networked_frames
    name: "Networked weapon fire/idle/reload animation (wframe temp entity)"
    layer: [presentation]
    base_refs:
      - "common/weapons/all.qc:wframe_send"
      - "common/weapons/all.qc:NET_HANDLE(wframe)"
      - "client/view.qc:viewmodel_draw (anim_update/anim_set idle, lines 335-337)"
    port_refs:
      - "game/client/ViewModel.cs (best-effort local fire/idle clip via ModelAnimator)"
    constants:
      - { name: "wframe states", base: "WFRAME_FIRE1 / WFRAME_FIRE2 / WFRAME_IDLE / WFRAME_RELOAD via networked attackrate+restartanim", port: "local fire/idle only; no network sync", match: false }
    status:
      logic: partial
      values: missing
      timing: missing
      presentation: partial
      audio: na
      liveness: partial
    intended_divergence: false
    divergence_rationale: null
    gaps:
      - "No networked weapon fire/idle/reload animation (the QC wframe temp entity is not networked); the gun never visibly cycles or reloads."
    verification:
      - { kind: code, ref: "grep: no wframe networking in port (the WFRAME refs in src/ are server-side weapon_thinkf, not networked viewmodel frames); ViewModel plays a best-effort local fire/idle clip via ModelAnimator when the model has one, no reload clip, no server-synced frame timing", result: pass }
    confidence: high
    notes: "Base networks the weapon frame (wframe_send -> NET_HANDLE) so fire/idle/reload play with server timing. Port has no wframe channel; it locally plays fire/idle best-effort and never plays a reload animation."

  - id: cl-viewmodel.model.skin_colormap_glow_effects
    name: "Per-child skin / team colormap / glowmod / EF_NODEPTHTEST / cheap effects mask"
    layer: [presentation]
    base_refs:
      - "client/view.qc:viewmodel_draw (lines 302-321)"
      - "common/weapons/all.qh:weaponentity_glowmod (line 402)"
    port_refs:
      - "game/client/ViewModel.cs:ApplyMaterialFx"
      - "game/client/ViewModel.cs:SetTeamGlow"
      - "game/net/NetGame.cs:UpdateViewModelTeamGlow"
      - "game/net/NetGame.cs:VortexGlowColor"
    constants:
      - { name: "colormap", base: "256 + entcs colors (team/player palette swap)", port: "SetTeamGlow team-color albedo tint (approx whole-model, not a region palette swap)", match: false }
      - { name: "glowmod", base: "weaponentity_glowmod(wep,c) = wr_glow override else colormapPaletteColor(c&0x0F)", port: "team palette color, Vortex/Vaporizer wr_glow charge override (VortexGlowColor) -> material emission", match: true }
      - { name: "csqcmodel_effects EF_NODEPTHTEST", base: "(EFMASK_CHEAP | EF_NODEPTHTEST) & ~EF_FULLBRIGHT", port: "NoDepthTest=true applied to every viewmodel material (ApplyMaterialFx)", match: true }
      - { name: "skin", base: "m_skin per child (wepskin)", port: "none (base material)", match: false }
    status:
      logic: faithful
      values: partial
      timing: na
      presentation: partial
      audio: na
      liveness: live
    intended_divergence: false
    divergence_rationale: null
    gaps:
      - "Team colormap is an approximate whole-model albedo tint (0.6 + 0.4*teamColor), not Base's true colormap palette region swap (256 + c); reads as your team color but is not pixel-faithful."
      - "Per-child m_skin (wepskin) not applied; the gun renders with its base material."
    verification:
      - { kind: code, ref: "ViewModel.ApplyMaterialFx sets NoDepthTest=true (EF_NODEPTHTEST) + a team colormod albedo tint + glowmod emission on every viewmodel surface override, called from RefreshCvars->ApplyModelAlpha each _Process frame; SetTeamGlow stores team color/glow, pushed live every frame from NetGame.UpdateViewModelTeamGlow (called in EquipNetworkedWeapon, NetGame:1616); glowmod resolves wr_glow (Vortex/Vaporizer charge via VortexGlowColor) else team palette, matching weaponentity_glowmod; grep ViewModel.cs/ViewModelEquip.cs for m_skin/wepskin -> no matches", result: pass }
    confidence: high
    notes: "FIXED since baseline (was logic:partial values:partial presentation:partial): team colormap tint AND per-weapon glowmod are now implemented and LIVE. SetTeamGlow + ApplyMaterialFx tint the gun toward the local player's team color and drive a team/wr_glow emission (Vortex/Vaporizer charge glow via VortexGlowColor, matching weaponentity_glowmod's wr_glow->palette fallback). Wired live every frame from NetGame.UpdateViewModelTeamGlow. EF_NODEPTHTEST + continuous alpha remain applied. Residual gaps: the colormap is an approximate whole-model albedo tint rather than Base's true palette region swap (256 + c), and per-child m_skin (wepskin) is still not applied."

  - id: cl-viewmodel.alpha.viewmodel_alpha
    name: "View-model alpha (cl_viewmodel_alpha / _alpha_min, vehicle/dead/intermission/chase hide)"
    layer: [presentation]
    base_refs:
      - "client/view.qc:viewmodel_draw (lines 297-304, 322)"
    port_refs:
      - "game/client/ViewModel.cs:RefreshCvars"
      - "game/client/ViewModel.cs:ApplyModelAlpha"
      - "game/net/NetGame.cs:EquipNetworkedWeapon (Visible toggle)"
    constants:
      - { name: "cl_viewmodel_alpha", base: "1", port: "read live (RefreshCvars), amax = (alpha!=0)?alpha:1", match: true }
      - { name: "cl_viewmodel_alpha_min", base: "0", port: "read live (RefreshCvars), amin = (alpha_min!=0)?alpha_min:-1", match: true }
    status:
      logic: faithful
      values: faithful
      timing: na
      presentation: faithful
      audio: na
      liveness: live
    intended_divergence: false
    divergence_rationale: null
    gaps:
      - "Vehicle force-hide (a=-1 when in a vehicle) is handled via the Visible toggle on dead/holster/chase/intermission, not via the continuous alpha=-1 path; functionally equivalent (gun hidden), so no visible defect."
    verification:
      - { kind: code, ref: "ViewModel.RefreshCvars reads cl_viewmodel_alpha/_alpha_min via CvarFloat and computes amax/amin/a exactly per Base (amax=(alpha!=0)?alpha:1; amin=(alpha_min!=0)?alpha_min:-1; a=(amin>=amax)?amax:bound), then ApplyModelAlpha walks the model materials; EquipNetworkedWeapon sets Visible on dead/holster/chase/intermission (NetGame:1610)", result: pass }
    confidence: high
    notes: "RefreshCvars reads cl_viewmodel_alpha + cl_viewmodel_alpha_min live each frame from the shared cvar store and applies the exact Base amax/amin/bound formula as continuous opacity on the viewmodel materials. Dead/holster/chase/intermission hide is via Visible (matches Base drawmask 0)."

  - id: cl-viewmodel.align.gunalign_gunoffset
    name: "Shot-origin alignment (cl_gunalign 1/2/3/4) + cl_gunoffset"
    layer: [presentation, shared]
    base_refs:
      - "common/weapons/all.qc:shotorg_adjust"
      - "common/weapons/all.qc:shotorg_adjustfromclient"
      - "common/weapons/calculations.qc:W_GunAlign"
      - "client/view.qc:viewmodel_draw (line 333, cl_gunoffset)"
    port_refs:
      - "game/client/ViewModel.cs:RefreshCvars"
      - "game/client/ViewModel.cs:GunAlignOffset"
      - "game/client/ViewModel.cs:GunOffset"
    constants:
      - { name: "cl_gunalign", base: "3 (1/2=center vecs.y=0 z-=2; 3=right; 4=left mirror Y)", port: "read live (RefreshCvars); right=authored, left=mirror -2*side, center=-side z-=2", match: false }
      - { name: "cl_gunoffset", base: "'0 0 0'", port: "read live from cl_gunoffset cvar (ParseVec), default '0 0 0'", match: true }
    status:
      logic: faithful
      values: partial
      timing: na
      presentation: faithful
      audio: na
      liveness: live
    intended_divergence: false
    divergence_rationale: null
    gaps:
      - "cl_gunalign side magnitude is a fixed representative AuthoredShotSideY (-3.5u) rather than each v_ model's own shot-tag vecs.y; left/center now move the gun but by an approximate amount, not the per-model Base value."
    verification:
      - { kind: code, ref: "ViewModel.RefreshCvars reads cl_gunalign (CvarFloat) + cl_gunoffset (ParseVec 'x y z') live and rebuilds rest placement on change; GunAlignOffset: case 3 -> 0 (authored right), case 4 -> (0,-2*side,0) mirror, case 1/2 -> (0,-side,-2) center with side=AuthoredShotSideY()=-3.5", result: pass }
    confidence: high
    notes: "cl_gunalign + cl_gunoffset are read live from the cvar store; left/right/center all read distinctly (AuthoredShotSideY makes the mirror real). Residual values gap: the side magnitude is a fixed representative -3.5u, not each model's authored shot-tag Y (Base mirrors/zeroes the model's own vecs.y)."

  - id: cl-viewmodel.switch.raise_lower_anim
    name: "Weapon-switch raise/lower (barrel-tip -90*f*f keyed to switchdelay/weapon_nextthink)"
    layer: [presentation]
    base_refs:
      - "client/view.qc:viewmodel_draw (lines 339-362)"
      - "common/weapons/all.qc (wframe temp entity, lines 533-573)"
    port_refs:
      - "game/client/ViewModel.cs:PlayHolster"
      - "game/client/ViewModel.cs:PlayRaise"
      - "game/client/ViewModel.cs:UpdateSwitch"
      - "game/net/NetGame.cs:EquipNetworkedWeapon"
      - "game/net/NetGame.cs:RunBoundCommand (holster on switch impulse)"
    constants:
      - { name: "angles_x tip", base: "-90 * f * f (barrel swings down out of view)", port: "slide straight down off-screen", match: false }
      - { name: "switch timing", base: "switchdelay_raise / switchdelay_drop via weapon_nextthink, /WEAPONRATEFACTOR", port: "custom durations, keypress-predicted + server-confirm + auto-recover", match: false }
    status:
      logic: partial
      values: partial
      timing: partial
      presentation: partial
      audio: na
      liveness: live
    intended_divergence: true
    divergence_rationale: "Port does not network the QC wframe temp entity / weapon_switchdelay / weapon_nextthink, so it cannot drive the -90*f*f tip from server-synced state. Instead the gun slides straight down off-screen on a keypress-predicted holster (RunBoundCommand) with a server-confirmed raise (EquipNetworkedWeapon) + auto-recover grace. Documented in ViewModel.PlayHolster/PlayRaise."
    gaps:
      - "Weapon-switch animation is a slide-down, not Base's barrel-tip (this.angles_x = -90*f*f), and is not keyed to per-weapon switchdelay_raise/drop or weapon_nextthink (port does not network wframe)."
    verification:
      - { kind: code, ref: "ViewModel.PlayHolster/PlayRaise/UpdateSwitch; live from RunBoundCommand (holster) + EquipNetworkedWeapon (PlayRaise, NetGame:1651)", result: pass }
    confidence: high
    notes: "Functionally LIVE (gun lowers on switch, raises on confirm). Deliberate re-implementation; flagged intended_divergence so it isn't re-reported as a bug, but the visual is not the Base barrel-tip."

  - id: cl-viewmodel.sway.followmodel
    name: "Follow sway (velocity lowpass -> acceleration offset, cl_followmodel)"
    layer: [presentation]
    base_refs:
      - "client/view.qc:calc_followmodel_ofs"
      - "client/view.qc:viewmodel_animate"
    port_refs:
      - "game/client/ViewModel.cs:FollowModelOffset"
      - "game/client/ViewModel.cs:RefreshCvars (cl_followmodel toggle live)"
      - "game/net/NetGame.cs:BuildViewState"
    constants:
      - { name: "cl_followmodel", base: "1", port: "read live (RefreshCvars)", match: true }
      - { name: "cl_followmodel_speed", base: "0.3 (scale * -0.042)", port: "0.3", match: true }
      - { name: "cl_followmodel_limit", base: "135", port: "135", match: true }
      - { name: "cl_followmodel_velocity_lowpass", base: "0.05", port: "0.05", match: true }
      - { name: "cl_followmodel_highpass", base: "0.05", port: "0.05", match: true }
      - { name: "cl_followmodel_lowpass", base: "0.03", port: "0.03", match: true }
      - { name: "cl_followmodel_velocity_absolute", base: "0", port: "not handled (always relative path)", match: false }
    status:
      logic: faithful
      values: faithful
      timing: faithful
      presentation: faithful
      audio: na
      liveness: live
    intended_divergence: false
    divergence_rationale: null
    gaps:
      - "cl_followmodel_velocity_absolute (1) branch not ported (port always uses the view-relative path; matches Base default 0)."
      - "cl_followmodel_speed/limit/lowpass/highpass constants are baked [Export] defaults, not read from cvars (only the cl_followmodel on/off toggle is read live)."
    verification:
      - { kind: code, ref: "FollowModelOffset diffed vs calc_followmodel_ofs (lowpass3/highpass3/avg_factor, scale -0.042); fed by BuildViewState predicted velocity; cl_followmodel toggle read live in RefreshCvars", result: pass }
    confidence: high
    notes: "Near line-by-line port of the QC lowpass/highpass macros, same constants. Live on the match path via ViewStateProvider = BuildViewState. The cl_followmodel toggle is read live (RefreshCvars); the numeric constants remain Export defaults (matching Base defaults)."

  - id: cl-viewmodel.sway.leanmodel
    name: "Lean sway (view-angle highpass -> gun roll/pitch, cl_leanmodel)"
    layer: [presentation]
    base_refs:
      - "client/view.qc:leanmodel_ofs"
      - "client/view.qc:viewmodel_animate"
    port_refs:
      - "game/client/ViewModel.cs:LeanModelOffset"
      - "game/client/ViewModel.cs:RefreshCvars (cl_leanmodel toggle live)"
      - "game/net/NetGame.cs:BuildViewState"
    constants:
      - { name: "cl_leanmodel", base: "1", port: "read live (RefreshCvars)", match: true }
      - { name: "cl_leanmodel_speed", base: "0.3", port: "0.3", match: true }
      - { name: "cl_leanmodel_limit", base: "30", port: "30", match: true }
      - { name: "cl_leanmodel_highpass1", base: "0.2", port: "0.2", match: true }
      - { name: "cl_leanmodel_highpass", base: "0.2", port: "0.2", match: true }
      - { name: "cl_leanmodel_lowpass", base: "0.05", port: "0.05", match: true }
    status:
      logic: faithful
      values: faithful
      timing: faithful
      presentation: faithful
      audio: na
      liveness: live
    intended_divergence: false
    divergence_rationale: null
    gaps:
      - "Lean teleport reset (csqcmodel_teleported -> gunangles_prev = view_angles) not ported; gun may snap on teleport/warpzone."
      - "cl_leanmodel_speed/limit/highpass/lowpass constants are baked [Export] defaults (only the cl_leanmodel on/off toggle is read live)."
    verification:
      - { kind: code, ref: "LeanModelOffset diffed vs leanmodel_ofs (highpass2_limited, pitch re-inversion, same avg_factor); fed by BuildViewState live view angles; cl_leanmodel toggle read live in RefreshCvars", result: pass }
    confidence: high
    notes: "Faithful port of the angle highpass chain incl. final pitch re-inversion. The cl_leanmodel toggle is read live (RefreshCvars). Missing the teleported reset (a minor warpzone-snap edge)."

  - id: cl-viewmodel.sway.bobmodel
    name: "Bob sway (ground-walk sinusoidal side/up sway, cl_bobmodel)"
    layer: [presentation]
    base_refs:
      - "client/view.qc:bobmodel_ofs"
      - "client/view.qc:viewmodel_animate"
    port_refs:
      - "game/client/ViewModel.cs:BobModelOffset"
      - "game/client/ViewModel.cs:RefreshCvars (cl_bobmodel toggle live)"
      - "game/net/NetGame.cs:BuildViewState"
    constants:
      - { name: "cl_bobmodel", base: "1", port: "read live (RefreshCvars)", match: true }
      - { name: "cl_bobmodel_side", base: "0.2", port: "0.2", match: true }
      - { name: "cl_bobmodel_up", base: "0.1", port: "0.1", match: true }
      - { name: "cl_bobmodel_speed", base: "10", port: "10", match: true }
      - { name: "bobmodel_scale ramp", base: "+/- frametime*5 (ground/air)", port: "+/- frametime*5", match: true }
      - { name: "xyspeed clamp", base: "0..400", port: "0..400", match: true }
    status:
      logic: faithful
      values: faithful
      timing: faithful
      presentation: faithful
      audio: na
      liveness: live
    intended_divergence: false
    divergence_rationale: null
    gaps:
      - "cl_bobmodel_side/up/speed constants are baked [Export] defaults (only the cl_bobmodel on/off toggle is read live)."
    verification:
      - { kind: code, ref: "BobModelOffset diffed vs bobmodel_ofs (bobmodel_scale ramp, avg_xyspeed avg_time 0.1, map_bound_ranges 150..400 crouch slowdown, sin(s)/cos(2s)); onground fed by BuildViewState; cl_bobmodel toggle read live in RefreshCvars", result: pass }
    confidence: high
    notes: "Faithful incl. crouch-walk frequency reduction (map_bound_ranges) and the hit-ground 0.05s settle. The cl_bobmodel toggle is read live (RefreshCvars). Vertical view-bob (cl_bob/cl_bob2/cl_bobfall) is TODO in Base too — not a gap."

  - id: cl-viewmodel.muzzle.flash_particle
    name: "Muzzle flash particle (local firer + remote world-space, m_muzzleeffect)"
    layer: [presentation]
    base_refs:
      - "common/weapons/all.qc:W_MuzzleFlash"
      - "common/weapons/all.qc:NET_HANDLE(w_muzzleflash)"
    port_refs:
      - "game/client/ViewModel.cs:Fire"
      - "game/client/EffectSystem.cs:MuzzleFlashAttached"
      - "game/net/NetGame.cs:MuzzleEffectFor"
      - "game/net/NetGame.cs:UpdateLocalFireFeedback"
    constants:
      - { name: "local emit", base: "pointparticles(m_muzzleeffect) at view-projected movedir_aligned", port: "MuzzleFlashAttached burst at tag_shot socket + flash OmniLight + recoil", match: false }
      - { name: "remote emit", base: "Send_Effect_Except(m_muzzleeffect, shotorg, shotdir*1000, except actor)", port: "server EffectEmitter.Emit('*_MUZZLEFLASH', ..., except: actor)", match: true }
      - { name: "chase_active branch", base: "emit at server sv_shotorg", port: "not handled (gun hidden in chase)", match: false }
    status:
      logic: faithful
      values: partial
      timing: faithful
      presentation: partial
      audio: na
      liveness: live
    intended_divergence: false
    divergence_rationale: null
    gaps:
      - "Local muzzle position is a heuristic at the tag_shot socket rather than Base's view-projected movedir_aligned (not verified to read identically on-screen)."
      - "Secondary fire does not flash (only the primary _attackHeld path calls Fire; UpdateLocalFireFeedback comments 'secondary FX stay networked for now')."
      - "ClientWorld.OnMuzzleFlash wrapper has no caller (NetGame calls _viewModel.Fire() directly) — dead wrapper, but the feature is live by another path."
    verification:
      - { kind: code, ref: "ViewModel.Fire -> MuzzleFlashAttached + OmniLight; live from PredictFireShot / UpdateLocalFireFeedback; remote VERIFIED via EffectEmitter.Emit('*_MUZZLEFLASH', shot.Origin, shot.Dir*1000, except: actor)", result: pass }
    confidence: medium
    notes: "Both local and remote particle flashes are LIVE. Remote emit confirmed faithful to Send_Effect_Except (every weapon passes except: actor). Position is heuristic (tag socket) not the QC view-projection; not runtime-verified for on-screen match. Secondary fire genuinely does not flash."

  - id: cl-viewmodel.muzzle.flash_model
    name: "Muzzle flash MODEL (spinning flash md3 for the 7 m_muzzlemodel weapons: Devastator/MineLayer/OkRPC=flash.md3, Machinegun/Shotgun/OkHMG/OkMachinegun=uziflash.md3)"
    layer: [presentation]
    base_refs:
      - "common/weapons/all.qc:W_MuzzleFlash_Model"
      - "common/weapons/all.qc:W_MuzzleFlash_Model_Think"
      - "common/weapons/all.qc:W_MuzzleFlash (m_muzzlemodel != MDL_Null gate, line 776)"
      - "common/weapons/weapon/devastator.qh:27 / minelayer.qh:29 / machinegun.qh:24 / shotgun.qh:25 (m_muzzlemodel ATTRIB)"
      - "common/mutators/mutator/overkill/{okhmg,okmachinegun,okrpc}.qh (m_muzzlemodel ATTRIB)"
    port_refs:
      - "game/client/ViewModel.cs:SpawnMuzzleFlashModel"
      - "game/client/ViewModel.cs:FlashModelFactory"
      - "game/net/NetGame.cs:MuzzleModelFor"
      - "game/net/NetGame.cs:1386 (FlashModelFactory wiring)"
    constants:
      - { name: "m_muzzlemodel set (Base m_muzzlemodel != MDL_Null)", base: "Devastator/MineLayer/OkRPC=flash.md3, Machinegun/Shotgun/OkHMG/OkMachinegun=uziflash.md3; Vortex/Vaporizer/Arc/all others=MDL_Null (NO model flash — they define a MUZZLEFLASH MODEL but their m_muzzlemodel ATTRIB is MDL_Null)", port: "MuzzleModelFor: devastator/minelayer/okrpc=flash.md3, machinegun/shotgun/okhmg/okmachinegun=uziflash.md3; everything else \"\"", match: true }
      - { name: "flash think", base: "frame+=2, scale*=0.5, alpha-=0.25 every 0.05s; start scale 0.75 alpha 0.75 frame 2; EF_ADDITIVE|EF_FULLBRIGHT", port: "scale*=0.5, alpha-=0.25 over 3x 0.05s ticks via Tween; start scale 0.75 alpha 0.75; Unshaded+Add blend (EF_FULLBRIGHT|EF_ADDITIVE)", match: true }
    status:
      logic: faithful
      values: faithful
      timing: faithful
      presentation: faithful
      audio: na
      liveness: live
    intended_divergence: false
    divergence_rationale: null
    gaps:
      - "Frame is animated in Base via frame+=2 (the md3 flash frame cycle); the port spawns a fresh static node and fades/shrinks it without advancing the md3 frame."
    verification:
      - { kind: code, ref: "ViewModel.SpawnMuzzleFlashModel (gated on FlashModelFactory + non-empty MuzzleModelPath) spawns the flash node at the muzzle marker, scale 0.75 alpha 0.75, EF_ADDITIVE|EF_FULLBRIGHT (ApplyFlashMaterialFx), then a 3-step Tween (scale*=0.5, alpha-=0.25 each 0.05s) matching W_MuzzleFlash_Model_Think; FlashModelFactory wired live at NetGame:1386 to _assets.LoadModel; MuzzleModelFor (NetGame:2216) maps devastator/minelayer/okrpc->flash.md3, machinegun/shotgun/okhmg/okmachinegun->uziflash.md3; Base re-verified: only those 7 weapons set m_muzzlemodel != MDL_Null (Vortex/Vaporizer/Arc are MDL_Null despite defining a nexflash/flash MODEL macro), so the port set now exactly matches Base", result: pass }
    confidence: high
    notes: "FIX + reclassification since baseline (was values:partial). Re-audit of Base m_muzzlemodel ATTRIBs: the seven weapons that attach a model flash are Devastator/MineLayer/OkRPC (flash.md3) and Machinegun/Shotgun/OkHMG/OkMachinegun (uziflash.md3). Vortex/Vaporizer/Arc DEFINE a MUZZLEFLASH MODEL constant (nexflash.md3 / flash.md3) but their m_muzzlemodel ATTRIB is MDL_Null, so they attach NO model flash — the prior baseline gap listing nexflash/Overkill as missing was partly a real gap (Overkill, now mapped: okrpc/okhmg/okmachinegun added to MuzzleModelFor) and partly a registry error (Vortex/Vaporizer never set m_muzzlemodel). The port now covers the full Base set. Sole residual gap: the md3 flash frame is not cycled (frame+=2)."

  - id: cl-viewmodel.casings.brass_eject
    name: "Brass / shell casings (eject, tumble, bounce, fade, bounce sounds)"
    layer: [presentation]
    base_refs:
      - "common/effects/qc/casings.qc:SpawnCasing"
      - "common/effects/qc/casings.qc:NET_HANDLE(casings)"
      - "common/effects/qc/casings.qc:Casing_Touch"
      - "common/weapons/weapon/machinegun.qc:111 (SpawnCasing caller)"
      - "common/weapons/weapon/shotgun.qc:78-83 (SpawnCasing caller, g_casings>=1)"
      - "common/weapons/weapon/rifle.qc:38-42 (SpawnCasing caller, g_casings>=2)"
    port_refs:
      - "src/XonoticGodot.Common/Gameplay/Weapons/WeaponFiring.cs:EjectCasing"
      - "src/XonoticGodot.Common/Gameplay/Weapons/Shotgun.cs:231 (EjectCasing Shell)"
      - "src/XonoticGodot.Common/Gameplay/Weapons/Machinegun.cs:385 (EjectCasing Bullet)"
      - "src/XonoticGodot.Common/Gameplay/Weapons/Rifle.cs:330 (EjectCasing Bullet)"
      - "src/XonoticGodot.Common/Gameplay/Weapons/OkMachinegun.cs:145 (EjectCasing Bullet)"
      - "src/XonoticGodot.Common/Gameplay/Effects/EffectsList.cs (CASING_BULLET/CASING_SHELL register)"
      - "game/client/EffectSystem.cs:455-457 (Spawn -> SpawnCasing routing)"
      - "game/client/ShellCasings.cs:Spawn"
      - "game/client/ClientWorld.cs:334 (SoundHook wiring -> OnSound CH_SHOTS)"
    constants:
      - { name: "cl_casings_bronze_time (bullet)", base: "10", port: "BulletTime 10", match: true }
      - { name: "cl_casings_shell_time (shell)", base: "30", port: "ShellTime 30", match: true }
      - { name: "cl_casings_maxcount", base: "100", port: "MaxCasings 100", match: true }
      - { name: "cl_casings_ticrate", base: "0.03125 (Movetype_Physics_MatchTicrate)", port: "fixed 0.03125 tic accumulator in CasingBody._PhysicsProcess (Ticrate const), catch-up clamped", match: true }
      - { name: "bouncefactor", base: "0.25 shell / 0.5 bullet", port: "0.25 shell / 0.5 bullet", match: true }
      - { name: "eject up-velocity", base: "30-rand*5 (shotgun shell) / 70-rand*5 (bullet)", port: "30-rand*5 shell / 70-rand*5 bullet (per casingtype)", match: true }
      - { name: "g_casings gate", base: ">=1 shotgun shell / >=2 bullet (default 2)", port: ">=1 Shell / >=2 Bullet (EjectCasing)", match: true }
      - { name: "avelocity tumble", base: "'0 10 0' + 100*prandomvec", port: "'0 10 0' + RandRange(-100,100) per-axis (QC magnitude, non-seeded RNG)", match: true }
      - { name: "bounce sound dist", base: "vdist(velocity,>,50) -> brass*/casings* CH_SHOTS ATTEN_LARGE, nextthink+0.2 throttle", port: "vel>50 -> weapons/brass1-3 | casings1-3 via SoundHook (OnSound CH_SHOTS), 0.2s throttle", match: true }
    status:
      logic: faithful
      values: faithful
      timing: faithful
      presentation: faithful
      audio: faithful
      liveness: live
    intended_divergence: false
    divergence_rationale: null
    gaps: []
    verification:
      - { kind: code, ref: "Machinegun/Shotgun/Rifle/OkMachinegun/OkHmg/OkShotgun call WeaponFiring.EjectCasing on the LIVE fire-attack path (verified Machinegun.cs:414 alongside the live impact/muzzle FX), per-casingtype g_casings gate (>=1 shell / >=2 bullet) + QC view-frame eject velocity (deterministic Prandom) -> EmitByEffectInfoName('casing_bullet'/'casing_shell') resolves the REGISTERED CASING_BULLET/CASING_SHELL effect (EffectsList:116-117) -> networks origin+velocity (non-null Effect, not a dropped null-effect) -> ClientWorld.OnEffect -> EffectSystem.Spawn routes CASING_BULLET/CASING_SHELL to SpawnCasing -> ShellCasings.Spawn. ShellCasings BulletTime=10/ShellTime=30/MaxCasings=100 (match Base), bouncefactor 0.25 shell/0.5 bullet, avelocity '0 10 0'+100*prandomvec; CasingBody._PhysicsProcess steps a FIXED cl_casings_ticrate=0.03125 tic accumulator (catch-up clamped) with a LIVE world-only TraceHook (EffectSystem:190, a TraceService over the per-map client collision world wired in SetCollisionWorld) doing full MOVETYPE_BOUNCE brush-face collision + trace_startsolid cull (FloorZ is only the no-tracer fallback); bounce sound brass1-3/casings1-3 via SoundHook (wired live ClientWorld:350 -> OnSound CH_SHOTS) when vel>50, throttled 0.2s", result: pass }
    confidence: high
    notes: "RE-AUDIT UPGRADE (timing partial->faithful, gap cleared): the casing path is now FULLY LIVE and faithful end-to-end. logic/values/presentation/audio were already faithful (lifetimes 10/30, maxcount 100, avelocity '0 10 0'+100*prandomvec, bounce sounds on CH_SHOTS when vdist>50 / 0.2s throttle, all wired live). TIMING is now also faithful: contrary to the prior baseline note, the sim does NOT integrate per-frame with only a FloorZ ground plane — CasingBody._PhysicsProcess advances in whole 0.03125s cl_casings_ticrate tics (Movetype_Physics_MatchTicrate-style, catch-up clamped) AND a live world-only TraceHook (TraceService over the client collision world, wired in EffectSystem.SetCollisionWorld which NetGame calls per map) gives real MOVETYPE_BOUNCE brush collision + the trace_startsolid cull; FloorZ is only the degenerate no-tracer fallback. No residual gap."

  - id: cl-viewmodel.model.wr_viewmodel_override
    name: "Per-weapon view-model override (wr_viewmodel, e.g. Tuba note-driven model swap)"
    layer: [presentation]
    base_refs:
      - "common/weapons/weapon.qh:160 (METHOD wr_viewmodel)"
      - "common/weapons/weapon/tuba.qc:423 (Tuba::wr_viewmodel)"
      - "client/view.qc:viewmodel_draw (newname = wep.wr_viewmodel(wep, this))"
    port_refs:
      - "game/net/NetGame.cs:WeaponViewModelOverride"
      - "game/net/NetGame.cs:EquipNetworkedWeapon (vmOverride rebuild gate)"
    constants:
      - { name: "wr_viewmodel (Tuba)", base: "tuba_instrument 0->v_tuba, 1->v_akordeon, 2->v_kleinbottle", port: "TubaInstrument 0->v_tuba.md3, 1->v_akordeon.md3, 2->v_kleinbottle.md3", match: true }
    status:
      logic: faithful
      values: faithful
      timing: faithful
      presentation: faithful
      audio: na
      liveness: live
    intended_divergence: false
    divergence_rationale: null
    gaps:
      - "Override only resolves on the local player's listen-server Player state (LocalServerPlayer); a pure remote-client (dedicated-server) Tuba instrument is read from server-side player state, so it is correct on the listen-server play path the port runs but unverified on a pure thin client."
    verification:
      - { kind: code, ref: "NetGame.WeaponViewModelOverride (NetGame:1732) returns '' for every weapon except Tuba, for which it maps TubaInstrument (0/1/2) to v_tuba/v_akordeon/v_kleinbottle.md3 — matching Tuba::wr_viewmodel; EquipNetworkedWeapon (NetGame:1622) recomputes vmOverride each frame, rebuilds the gun model when it changes (vmOverride != _equippedVmOverride), and feeds it into ViewModelEquip.Build", result: pass }
    confidence: high
    notes: "FIXED since baseline (was missing, port_refs []): the per-weapon wr_viewmodel override IS now ported and LIVE. WeaponViewModelOverride implements the default (no override) for all weapons and the Tuba per-instrument v_ model swap; EquipNetworkedWeapon rebuilds the viewmodel when the override string changes, faithful to Base viewmodel_draw's newname = wep.wr_viewmodel(wep,this)."
