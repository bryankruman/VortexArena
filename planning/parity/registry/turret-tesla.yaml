unit: turret-tesla
category: turret
base_ref_rev: "v0.8.6-1779-g863cd3e84"
last_audited: "2026-06-27"
summary:
  feature_count: 8
  worst_gaps:
    - "Head coil never visually spins: tr_think computes HeadAVelocity/HeadAngles server-side (logic/values/timing faithful) but no head-bone entity (tesla_head.md3) renders it — grep (2026-06-27) confirms HeadAngles has zero render-node consumer (only TurretAI track-motor/gib + TeslaTurret.TrThink write it), so the menacing charge spin is invisible. The single remaining real gap for this unit."
features:
  - id: turret-tesla.identity.def
    name: "Identity, hitbox, models, spawnflags"
    layer: [authority, presentation]
    base_refs:
      - "common/turrets/turret/tesla.qh:TeslaCoil"
    port_refs:
      - "src/XonoticGodot.Common/Gameplay/Turrets/TeslaTurret.cs:TeslaTurret"
    constants:
      - { name: "mins", base: "'-60 -60 0'", port: "(-60,-60,0)", match: true }
      - { name: "maxs", base: "'60 60 128'", port: "(60,60,128)", match: true }
      - { name: "model", base: "models/turrets/tesla_base.md3", port: "models/turrets/tesla_base.md3", match: true }
      - { name: "head_model", base: "models/turrets/tesla_head.md3", port: "TeslaTurret.cs:52 sets HeadModel = tesla_head.md3 (identity data only); no head-bone entity / render attach consumes it", match: false }
      - { name: "netname", base: "tesla", port: "tesla", match: true }
      - { name: "health", base: "1000 (turrets.cfg:397)", port: "StartHealth 1000", match: true }
      - { name: "spawnflags", base: "TUR_FLAG_HITSCAN|TUR_FLAG_PLAYER|TUR_FLAG_MISSILE", port: "implicit (fixed emplaced, players+missiles selected)", match: true }
    status:
      logic: faithful
      values: faithful
      timing: na
      presentation: partial
      audio: na
      liveness: live
    intended_divergence: false
    divergence_rationale: null
    gaps:
      - "Head model (tesla_head.md3) not instantiated as a separate head/coil entity; no tag_head attachment. TrThink computes head spin as state (HeadAngles) but grep confirms no head-bone node consumes HeadAngles, so the coil never visibly spins (see anim feature)."
    verification:
      - { kind: code_read, ref: "tesla.qh vs TeslaTurret ctor + Spawn (mins/maxs/model/netname/health/spawnflags all confirmed); grep HeadAngles (2026-06-27) -> only TeslaTurret/TurretAI/TurretSpawn (no render consumer); grep tesla_head -> 1 hit (TeslaTurret.cs:52 identity assignment only, no render attach)", result: pass }
    confidence: high
    notes: "Re-verified 2026-06-27: identity/hitbox/base model/health faithful and live; head coil model still not modeled as a separate head-bone entity (no visual spin even though the spin state is computed in TrThink). Unchanged from prior audit."
  - id: turret-tesla.setup.tr_setup
    name: "tr_setup: select/validate flags, ammo flags, custom firecheck, no aim/track"
    layer: [authority]
    base_refs:
      - "common/turrets/turret/tesla.qc:35"
      - "common/turrets/sv_turrets.qc:turret_initialize"
    port_refs:
      - "src/XonoticGodot.Common/Gameplay/Turrets/TeslaTurret.cs:Spawn"
      - "src/XonoticGodot.Common/Gameplay/Turrets/TeslaTurret.cs:Think"
      - "src/XonoticGodot.Common/Gameplay/Turrets/TurretSpawn.cs:Init"
    constants:
      - { name: "solid", base: "SOLID_BBOX", port: "Solid.BBox", match: true }
      - { name: "movetype", base: "MOVETYPE_NOCLIP (fixed)", port: "MoveType.Noclip", match: true }
      - { name: "takedamage", base: "DAMAGE_AIM", port: "DamageMode.Aim", match: true }
      - { name: "select_flags", base: "PLAYERS|MISSILES|RANGELIMITS|TEAMCHECK", port: "SelectPlayers|SelectMissiles|SelectRangeLimits|SelectTeamCheck", match: true }
      - { name: "validate_flags", base: "PLAYERS|MISSILES|RANGELIMITS|TEAMCHECK", port: "Select (same set, used for re-validate)", match: true }
      - { name: "aim_flags", base: "TFL_AIM_NO", port: "no aim (custom Think, no Track call)", match: true }
      - { name: "track_flags", base: "TFL_TRACK_NO", port: "no track (custom Think, no Track call)", match: true }
      - { name: "shoot_flags", base: "TFL_SHOOT_CUSTOM", port: "custom Discharge (not RunCombat)", match: true }
      - { name: "firecheck_flags", base: "TFL_FIRECHECK_REFIRE|TFL_FIRECHECK_AMMO_OWN", port: "Think: AttackFinished + Ammo gates", match: true }
      - { name: "ammo_flags", base: "ENERGY|RECHARGE|RECIEVE", port: "ammo pool regen in Think", match: true }
    status:
      logic: faithful
      values: faithful
      timing: na
      presentation: na
      audio: na
      liveness: live
    intended_divergence: false
    divergence_rationale: null
    gaps: []
    verification:
      - { kind: code_read, ref: "tesla.qc tr_setup vs TeslaTurret.Spawn/Think + TurretSpawn.Init (flags + solid/movetype); custom Discharge path, no RunCombat/Track call confirmed", result: pass }
    confidence: high
    notes: "Re-verified 2026-06-27: omnidirectional no-aim/no-track + custom-fire setup faithfully reproduced (TeslaTurret.Spawn->TurretSpawn.Init solid/movetype/takedamage; custom Think Discharge, no RunCombat/Track call); select/validate flag sets match exactly. Faithful and live."
  - id: turret-tesla.damage.lifecycle
    name: "Damage gating, ammo regen, death blast + respawn"
    layer: [authority]
    base_refs:
      - "common/turrets/sv_turrets.qc:turret_damage"
      - "common/turrets/sv_turrets.qc:turret_die"
      - "common/turrets/sv_turrets.qc:turret_respawn"
    port_refs:
      - "src/XonoticGodot.Common/Gameplay/Turrets/TurretAI.cs:EventDamage"
      - "src/XonoticGodot.Common/Gameplay/Turrets/TurretAI.cs:Damage"
      - "src/XonoticGodot.Common/Gameplay/Turrets/TurretAI.cs:Die"
      - "src/XonoticGodot.Common/Gameplay/Turrets/TurretAI.cs:Respawn"
      - "src/XonoticGodot.Common/Gameplay/Turrets/TeslaTurret.cs:Spawn"
      - "src/XonoticGodot.Common/Gameplay/Turrets/TeslaTurret.cs:Think"
    constants:
      - { name: "respawntime", base: "120 (turrets.cfg:398)", port: "120 (TeslaTurret.RespawnTime const -> Spawn passes respawnTime: RespawnTime to TurretSpawn.Init)", match: true }
      - { name: "ammo regen", base: "ammo += ammo_recharge(15)*frametime, cap 1000", port: "Think: Ammo += AmmoRecharge(15)*frameTime, cap AmmoMax(1000)", match: true }
      - { name: "inactive/dead take no damage", base: "yes", port: "Damage: Dead/!Active -> 0", match: true }
      - { name: "friendlyfire gate", base: "g_friendlyfire (default 0)", port: "g_friendlyfire", match: true }
      - { name: "death blast", base: "RadiusDamage commented out (sv_turrets.qc:182) -> no blast", port: "Die: no blast (matched)", match: true }
      - { name: "fixed (no MOVE shove)", base: "no TUR_FLAG_MOVE", port: "movable=false (default)", match: true }
    status:
      logic: faithful
      values: faithful
      timing: faithful
      presentation: na
      audio: na
      liveness: live
    intended_divergence: false
    divergence_rationale: null
    gaps: []
    verification:
      - { kind: code_read, ref: "turrets.cfg:398 respawntime 120 == TeslaTurret.RespawnTime 120 passed via Spawn(respawnTime:); EventDamage live path (GtEventDamage shim) routes hits -> Damage gate -> health subtract -> Combat.Death -> OnAnyDeath -> Die/Respawn", result: pass }
    confidence: high
    notes: "Re-verified 2026-06-27: Spawn passes respawnTime: RespawnTime (TeslaTurret.cs:36 const 120, :59 Spawn arg), matching g_turrets_unit_tesla_respawntime 120 (no longer inheriting the generic 60s default). Damage routed through the live EventDamage seam; chassis damage gating + ammo regen + death/respawn lifecycle all faithful and live."
  - id: turret-tesla.spawn.liveness
    name: "Map spawnfunc wiring + per-frame think (turret_tesla placed on map)"
    layer: [authority]
    base_refs:
      - "common/turrets/turret/tesla.qc:5"
      - "common/turrets/sv_turrets.qc:turret_link"
    port_refs:
      - "src/XonoticGodot.Common/Gameplay/Turrets/TurretSpawnFuncs.cs:Tesla"
      - "src/XonoticGodot.Common/Gameplay/MapObjects/MapObjectsRegistry.cs:237"
      - "src/XonoticGodot.Common/GameInit.cs:21"
    constants:
      - { name: "g_turrets master switch", base: "1 (default on)", port: "MasterSwitchEnabled default-on", match: true }
    status:
      logic: faithful
      values: faithful
      timing: faithful
      presentation: na
      audio: na
      liveness: live
    intended_divergence: false
    divergence_rationale: null
    gaps: []
    verification:
      - { kind: code_read, ref: "SpawnFuncs.Register(\"turret_tesla\", TurretSpawnFuncs.Tesla) at MapObjectsRegistry.cs:237 -> Tesla -> Spawn(e,\"tesla\") -> def.Spawn(e) + per-frame driver self=>{self.NextThink=Now; def.Think(self);} re-armed each frame (TurretSpawnFuncs.cs:55-57); GameInit.cs:21 calls MapObjectsRegistry.RegisterAll at boot", result: pass }
    confidence: high
    notes: "Re-verified 2026-06-27: spawnfunc registered (MapObjectsRegistry.cs:237 — line ref refreshed from 227), RegisterAll runs at boot (GameInit.cs:21), Think driver re-armed every frame; [Turret] descriptor resolves by name. Fully faithful and live."
  - id: turret-tesla.firecheck.custom
    name: "Custom firecheck: rescan throttle + re-validate + cooldown + ammo + target gate"
    layer: [authority]
    base_refs:
      - "common/turrets/turret/tesla.qc:51"
      - "common/turrets/sv_turrets.qc:turret_select_target"
      - "common/turrets/sv_turrets.qc:turret_firecheck"
    port_refs:
      - "src/XonoticGodot.Common/Gameplay/Turrets/TeslaTurret.cs:Think"
      - "src/XonoticGodot.Common/Gameplay/Turrets/TurretAI.cs:SelectTarget"
    constants:
      - { name: "targetscan_mindelay", base: "0.1 (autocvar_g_turrets_targetscan_mindelay)", port: "TurretAI.Cvar(\"g_turrets_targetscan_mindelay\", 0.1f); now < TargetSelectTime+mindelay -> suppress scan (Think:102)", match: true }
      - { name: "targetscan_maxdelay", base: "1 (autocvar_g_turrets_targetscan_maxdelay)", port: "TurretAI.Cvar(\"g_turrets_targetscan_maxdelay\", 1f); now > TargetSelectTime+maxdelay -> force scan (Think:89)", match: true }
      - { name: "target re-validate window", base: "0.5 (target_validate_time = time + 0.5)", port: "TargetValidateTime gate: when held enemy invalid, clear + set now+0.5f (Think:93-99)", match: true }
      - { name: "scan only when forced/invalid, then stamp time", base: "this.enemy = turret_select_target; this.target_select_time = time", port: "if doTargetScan: e.Enemy = SelectTarget; st.TargetSelectTime = now (Think:106-110)", match: true }
      - { name: "refire gate (attack_finished)", base: "yes", port: "AttackFinished > now -> return", match: true }
      - { name: "own-ammo gate (ammo >= shot_dmg)", base: "yes", port: "Ammo < ShotDamage -> return", match: true }
      - { name: "requires enemy != NULL", base: "yes", port: "Enemy is null -> return", match: true }
    status:
      logic: faithful
      values: faithful
      timing: faithful
      presentation: na
      audio: na
      liveness: live
    intended_divergence: false
    divergence_rationale: null
    gaps: []
    verification:
      - { kind: code_read, ref: "tesla.qc turret_tesla_firecheck (do_target_scan via maxdelay OR invalid-target, suppressed by mindelay, then turret_firecheck refire+ammo, enemy!=NULL) vs TeslaTurret.Think:83-114 — full scan-throttle now ported: cvar-backed mindelay/maxdelay (TurretAI.Cvar defaults 0.1/1), TargetSelectTime stamp, 0.5s re-validate, then AttackFinished/Ammo/enemy gates. Matches Base structure line-for-line.", result: pass }
    confidence: high
    notes: "Re-verified 2026-06-27: the scan-throttle remains fully ported — TeslaTurret.Think:84-111 replicates turret_tesla_firecheck's do_target_scan logic with cvar-backed mindelay(0.1)/maxdelay(1) gating via TargetSelectTime, only re-selecting on maxdelay-elapsed OR held-target-invalid and suppressing under mindelay; then enemy!=null + AttackFinished cooldown + Ammo>=ShotDamage gates (Think:113-115) + 0.5s re-validate. Faithful and live."
  - id: turret-tesla.weapon.chain
    name: "Chain-lightning discharge (toast loop): nearest-LOS first hop, 10-hop decay"
    layer: [authority]
    base_refs:
      - "common/turrets/turret/tesla_weapon.qc:7"
      - "common/turrets/turret/tesla_weapon.qc:53"
    port_refs:
      - "src/XonoticGodot.Common/Gameplay/Turrets/TeslaTurret.cs:Discharge"
      - "src/XonoticGodot.Common/Gameplay/Turrets/TeslaTurret.cs:Toast"
      - "src/XonoticGodot.Common/Gameplay/Damage/DeathTypes.cs:333"
    constants:
      - { name: "shot_dmg (first hop)", base: "200 (turrets.cfg:400)", port: "ShotDamage 200", match: true }
      - { name: "target_range (first hop)", base: "1000 (turrets.cfg:408)", port: "TargetRange 1000", match: true }
      - { name: "shot_refire", base: "1.5 (turrets.cfg:401)", port: "ShotRefire 1.5", match: true }
      - { name: "max chain hops", base: "10", port: "MaxChainHops 10", match: true }
      - { name: "chain damage falloff", base: "0.75/hop", port: "ChainDamageFalloff 0.75", match: true }
      - { name: "chain range falloff", base: "0.85/hop", port: "ChainRangeFalloff 0.85", match: true }
      - { name: "chain drops RANGELIMITS", base: "yes (validate flags lose RANGELIMITS after first toast)", port: "ChainSelect drops SelectRangeLimits", match: true }
      - { name: "no self/repeat hit (g_railgunhit)", base: "yes (.railgunhit set + cleared per discharge)", port: "HashSet<Entity> hitAlready", match: true }
      - { name: "LOS trace", base: "MOVE_WORLDONLY traceline to 0.5*(absmin+absmax), fraction==1.0", port: "Trace WorldOnly to TargetCenter, fraction<1 (unless Ent==target) reject", match: true }
      - { name: "hop distance metric", base: "vlen(it.origin - from.origin)", port: "(e.Origin - from).Length()", match: true }
      - { name: "deathtype", base: "DEATH_TURRET_TESLA.m_id", port: "DeathTypes.TurretTesla (\"turret_tesla\", registered DEATH_SELF_TURRET_TESLA)", match: true }
      - { name: "shot_force / damage force", base: "'0 0 0' (zero knockback; cfg shot_force 400 never applied by toast)", port: "Vector3.Zero", match: true }
      - { name: "skip realowner (etarget != actor.realowner)", base: "yes (arc + railgunhit drawn, Damage skipped if etarget==realowner)", port: "if (!ReferenceEquals(target, turret.Owner ?? turret)) Damage", match: true }
    status:
      logic: faithful
      values: faithful
      timing: faithful
      presentation: na
      audio: na
      liveness: live
    intended_divergence: false
    divergence_rationale: null
    gaps: []
    verification:
      - { kind: code_read, ref: "tesla_weapon.qc wr_think+toast vs Discharge+Toast: first-hop full range, 0.75/0.85 decay, RANGELIMITS dropped on chain, no-repeat HashSet, WorldOnly LOS, distance metric, deathtype, force Vector3.Zero, realowner-skip all confirmed; turrets.cfg:402 shot_force 400 confirmed never applied (Base toast force '0 0 0')", result: pass }
    confidence: high
    notes: "Re-verified 2026-06-27: damage force is Vector3.Zero (no shove, TeslaTurret.cs:222) and the realowner-skip branch is present (target != Owner ?? turret, :220); for a map-placed turret Owner is null so this reduces to never-skip, matching Base (a map turret's realowner is world/null, never validates as a target). Chain mechanic (first-hop full range, 0.75/0.85 decay over 10 hops, RANGELIMITS dropped on chain via ChainSelect, no-repeat HashSet, WorldOnly LOS, distance metric, DEATH_TURRET_TESLA) all faithful and live."
  - id: turret-tesla.weapon.arc_fx
    name: "Per-hop lightning arc visual + discharge sound"
    layer: [presentation, authority]
    base_refs:
      - "common/turrets/turret/tesla_weapon.qc:77"
      - "common/turrets/turret/tesla_weapon.qc:7"
    port_refs:
      - "src/XonoticGodot.Common/Gameplay/Turrets/TeslaTurret.cs:Toast"
      - "src/XonoticGodot.Common/Gameplay/Turrets/TeslaTurret.cs:Discharge"
      - "src/XonoticGodot.Common/Gameplay/Effects/EffectEmitter.cs:263"
    constants:
      - { name: "per-hop arc", base: "te_csqc_lightningarc(from.origin, etarget.origin)", port: "EffectEmitter.TeCsqcLightningArc(from, target.Origin)", match: true }
      - { name: "discharge fire sound", base: "NONE for turret actor (electro_fire only in isPlayer branch of wr_think via W_SetupShot_Dir)", port: "no sound played in Discharge (no Api.Sound.Play)", match: true }
    status:
      logic: faithful
      values: faithful
      timing: faithful
      presentation: faithful
      audio: faithful
      liveness: live
    intended_divergence: false
    divergence_rationale: null
    gaps: []
    verification:
      - { kind: code_read, ref: "tesla_weapon.qc wr_think isPlayer-gated W_SetupShot_Dir (only player path emits SND_TeslaCoilTurretAttack_FIRE) vs Discharge: no Api.Sound.Play present; EffectEmitter.TeCsqcLightningArc:263 wired and called from Toast each hop", result: pass }
    confidence: high
    notes: "Re-verified 2026-06-27: no discharge sound — Discharge (TeslaTurret.cs:159-181) has no Api.Sound.Play; a Base tesla TURRET is silent server-side (electro_fire is player-only via W_SetupShot_Dir in the isPlayer branch). Per-hop arc visual genuinely wired: TeCsqcLightningArc -> EmitByEffectInfoName(\"arc_lightning\", from, to, 0) (real networked TE_CSQC_ARC beam, EffectEmitter.cs:299-300, NOT a stub), called live each hop in Toast (:216). Fully faithful."
  - id: turret-tesla.anim.tr_think
    name: "tr_think head spin-up + idle random crackle arc"
    layer: [authority, presentation]
    base_refs:
      - "common/turrets/turret/tesla.qc:11"
    port_refs:
      - "src/XonoticGodot.Common/Gameplay/Turrets/TeslaTurret.cs:TrThink"
    constants:
      - { name: "head avelocity (low ammo)", base: "'0 45 0' * (ammo/shot_dmg)", port: "new Vector3(0,45,0) * (Ammo/ShotDamage)", match: true }
      - { name: "head avelocity (charged)", base: "'0 180 0' * (ammo/shot_dmg)", port: "new Vector3(0,180,0) * (Ammo/ShotDamage)", match: true }
      - { name: "head avelocity zeroed when inactive", base: "'0 0 0' if !active", port: "Vector3.Zero if !Active", match: true }
      - { name: "idle arc chance", base: "f=ammo/ammo_max; f*f>random() && random()<0.1", port: "f=Ammo/AmmoMax; f*f>Prandom.Float() && Prandom.Float()<0.1f", match: true }
      - { name: "idle arc gated on cooled down", base: "attack_finished[0] <= time", port: "AttackFinished <= now (else return)", match: true }
      - { name: "idle arc reach", base: "te_csqc_lightningarc(shotorg, shotorg+randomvec()*350)", port: "TeCsqcLightningArc(ShotOrg, ShotOrg + Prandom.Vec()*350f); Prandom.Vec() now rejection-samples the unit ball (Prandom.cs:85-94), matching DP randomvec() #91", match: true }
    status:
      logic: faithful
      values: faithful
      timing: faithful
      presentation: partial
      audio: na
      liveness: live
    intended_divergence: false
    divergence_rationale: null
    gaps:
      - "Head spin not rendered: TrThink computes HeadAVelocity ('0 45 0' low-ammo / '0 180 0' charged, scaled by ammo/shot_dmg, zeroed when inactive) and integrates HeadAngles, but grep (2026-06-27) confirms no head-bone/coil entity (tesla_head.md3) consumes HeadAngles — the only readers are TurretAI track-motor/death-gib logic + TeslaTurret.TrThink itself. The coil never visibly spins; server-side state only. This is the last real gap on this unit."
    verification:
      - { kind: code_read, ref: "tesla.qc tr_think (head avelocity branches + cooled-down idle-arc) vs TeslaTurret.TrThink:127-153 — called every Think; idle arc emits via wired TeCsqcLightningArc -> EmitByEffectInfoName(\"arc_lightning\") (real networked TE_CSQC_ARC beam, not a stub); HeadAngles has no render consumer (grep src/ 2026-06-27); Prandom.Vec() reads the unit-ball reject-sample (x^2+y^2+z^2<=1, Prandom.cs:85-94) — NOT a cube", result: pass }
    confidence: high
    notes: "Re-verified 2026-06-27. Logic/values/timing faithful + live: TrThink reproduces the head spin-up scaling and the probabilistic idle crackle arc, called live each Think; the idle arc visual is genuinely emitted via the wired arc emitter. CLOSED since prior audit: the 'Prandom.Vec returns a cube' gap is STALE — the current Prandom.Vec() (Prandom.cs:85-94) rejection-samples the unit ball, matching QC randomvec() #91, so the idle-arc endpoint shape now matches Base. STILL OPEN on presentation only: the head model is not modeled as a head-bone entity, so the computed spin is never rendered (discharge arc + idle crackle ARE visible)."
