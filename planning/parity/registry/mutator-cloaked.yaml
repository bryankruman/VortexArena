unit: mutator-cloaked
category: mutator
base_ref_rev: "v0.8.6-1779-g863cd3e84"
last_audited: "2026-06-25"
summary:
  feature_count: 6
  worst_gaps:
    - "HEADLINE EFFECT WORKING (unchanged since Wave-5): with g_cloaked 1 players render semi-transparent end-to-end. The alpha seed lives in Common (MutatorHooks.DefaultPlayerAlpha/DefaultWeaponAlpha, set by FireSetDefaultAlpha at GameWorld.cs:569 worldspawn), server consumers read it (SpawnSystem.cs:591, DamageSystem.cs:629), and the client net->render bridge decodes NetEntityState.Alpha onto Entity.Alpha (ClientEntityView.cs:134) -> PlayerModel.ApplyAlpha. 0.25 round-trips faithfully via QuantizeAlpha (0.25->64/255=0.251)."
    - "STILL OPEN — secondary alpha consumers: no exterior/held weapon-entity alpha-follow (Base weaponsystem.qc:116-117,170-171) and no vehicle-exit alpha restore (Base sv_vehicles.qc:814). These are minor relative to the now-working player body alpha."
    - "2026-06-25 DRIFT (FIX, partial) — Cloaked NOW overrides BuildMutatorsPrettyString (CloakedMutator.cs:73-78, ', Cloaked' with the !g_cts gate), landed in Wave-6b alongside ~25 other mutator overrides. So logic/values/presentation of the pretty-string append are now ported & faithful. STILL OPEN: (1) the MutatorActivation.BuildMutatorsPrettyString chain runner (MutatorActivation.cs:121) has NO external caller — only BuildMutatorsString (machine token) is run live at GameWorld.cs:876 — so liveness stays DEAD and nothing is displayed yet; (2) no MUT_CLOAKED=7 bit (util.qh:62) ported."
    - "2026-06-25 DRIFT (FIX, partial) — the create-game checkbox IS now ported: DialogMutators.cs:131 Widgets.CheckBox(\"g_cloaked\", \"Cloaked\", \"All players are almost invisible\"). STILL OPEN: the in-game per-mutator describe-page text (cloaked.qc:6-13 METHOD describe) is not ported (general menu describe-system gap)."
features:
  - id: mutator-cloaked.enable.predicate
    name: "Enable predicate (g_cloaked cvar gate + registration/subscription)"
    layer: [authority]
    base_refs:
      - "common/mutators/mutator/cloaked/sv_cloaked.qc:4 REGISTER_MUTATOR(cloaked, expr_evaluate(cvar_string(\"g_cloaked\")))"
    port_refs:
      - "src/XonoticGodot.Common/Gameplay/Mutators/CloakedMutator.cs:30 IsEnabled"
      - "src/XonoticGodot.Common/Gameplay/Mutators/CloakedMutator.cs:35 Hook"
      - "src/XonoticGodot.Common/Gameplay/Mutators/MutatorActivation.cs:Apply"
      - "src/XonoticGodot.Server/GameWorld.cs MutatorActivation.Apply() (boot step 5b, before map entities spawn)"
    constants:
      - { name: "g_cloaked", base: "0 (mutators.cfg:563)", port: "0 (assets/.../mutators.cfg:563)", match: true }
    status:
      logic: faithful
      values: faithful
      timing: na
      presentation: na
      audio: na
      liveness: live
    intended_divergence: false
    divergence_rationale: null
    gaps: []
    verification:
      - { kind: code, ref: "VERIFIED (unchanged): CloakedMutator.IsEnabled = Api.Services is not null && Api.Cvars.GetFloat(\"g_cloaked\") != 0f (CloakedMutator.cs:30-31); MutatorActivation.Apply() called live at GameWorld.cs:559 (step 5b, before map entities spawn); Hook() Adds OnSetDefaultAlpha + reads g_balance_cloaked_alpha", result: pass }
      - { kind: value, ref: "VERIFIED: assets/data/xonotic-data.pk3dir/mutators.cfg:563 'set g_cloaked 0' matches Base", result: pass }
    confidence: high
    notes: "UNCHANGED by Wave-5 in behavior. The mutator IS registered (auto-discovered via [Mutator]) and its handler IS subscribed on the live boot path when g_cloaked != 0. Base uses expr_evaluate(cvar_string) which parses expressions; port uses a simple != 0 float check — functionally equivalent for the stock '0'/'1' usage (campaign Stormkeep sets g_cloaked 1). Line ref for MutatorActivation.Apply() shifted 539->559 due to Wave-5 GameWorld edits; behavior identical."

  - id: mutator-cloaked.alpha.set_default_alpha
    name: "SetDefaultAlpha override — set player/weapon alpha from g_balance_cloaked_alpha"
    layer: [authority, presentation]
    base_refs:
      - "common/mutators/mutator/cloaked/sv_cloaked.qc:8-13 MUTATOR_HOOKFUNCTION(cloaked, SetDefaultAlpha)"
      - "server/world.qc:105-112 SetDefaultAlpha() (free fn that fires the hook / seeds default)"
      - "server/world.qh:72-73 default_player_alpha / default_weapon_alpha globals"
    port_refs:
      - "src/XonoticGodot.Common/Gameplay/Mutators/CloakedMutator.cs:53 OnSetDefaultAlpha"
      - "src/XonoticGodot.Common/Gameplay/Mutators/MutatorHooks.cs:485 SetDefaultAlpha chain + 493/496 DefaultPlayerAlpha/DefaultWeaponAlpha + 507-517 FireSetDefaultAlpha"
      - "src/XonoticGodot.Server/GameWorld.cs:569 (DefaultPlayerAlpha, DefaultWeaponAlpha) = MutatorHooks.FireSetDefaultAlpha(1f, 1f)"
    constants:
      - { name: "g_balance_cloaked_alpha", base: "0.25 (mutators.cfg:564)", port: "0.25 (CloakedMutator.Alpha default + re-read in Hook()/OnSetDefaultAlpha)", match: true }
      - { name: "default_player_alpha = alpha", base: "yes (world.qh global)", port: "MutatorHooks.DefaultPlayerAlpha set from args.PlayerAlpha", match: true }
      - { name: "default_weapon_alpha = player alpha", base: "yes (world.qh global)", port: "MutatorHooks.DefaultWeaponAlpha set from args.WeaponAlpha (= player alpha)", match: true }
      - { name: "hook returns true (replaces default branch)", base: "true", port: "true (OnSetDefaultAlpha returns true)", match: true }
      - { name: "SetDefaultAlpha() world-init driver fires the hook", base: "world.qc:105-112, called at worldspawn", port: "GameWorld.cs:565 fires FireSetDefaultAlpha at worldspawn (step 5b', after MutatorActivation.Apply)", match: true }
    status:
      logic: faithful
      values: faithful
      timing: faithful
      presentation: faithful
      audio: na
      liveness: live
    intended_divergence: false
    divergence_rationale: null
    gaps: []
    verification:
      - { kind: code, ref: "VERIFIED: MutatorHooks.FireSetDefaultAlpha (MutatorHooks.cs:507-517) calls SetDefaultAlpha.Call(ref a) and caches the result into the Common-side statics DefaultPlayerAlpha/DefaultWeaponAlpha. GameWorld.cs:565 calls FireSetDefaultAlpha(1f,1f) at worldspawn after MutatorActivation.Apply(). Cloaked's OnSetDefaultAlpha writes args.PlayerAlpha=args.WeaponAlpha=0.25 and returns true.", result: pass }
      - { kind: code, ref: "VERIFIED: MutatorHooks.cs:493/496 declare the static DefaultPlayerAlpha/DefaultWeaponAlpha (default 1f), updated by FireSetDefaultAlpha. GameWorld.cs:565 also mirrors them onto the Server GameWorld props. The seed is correct AND now lives in Common where the spawn/death consumers can read it.", result: pass }
      - { kind: value, ref: "VERIFIED: mutators.cfg:564 'g_balance_cloaked_alpha 0.25' == port; CloakedMutator.Alpha defaults 0.25f and OnSetDefaultAlpha re-reads the cvar live (guarded a != 0).", result: pass }
      - { kind: code, ref: "VERIFIED (downstream now wired): MutatorHooks.DefaultPlayerAlpha is read by SpawnSystem.cs:575, DamageSystem.cs:624, and PowerupsMutator.cs:218 — the seed reaches spawned/dead players. The whole consumer chain is closed (see default_alpha_consumers).", result: pass }
    confidence: high
    notes: "FIX (Wave-5): presentation partial->faithful. The override's visible purpose is now realized end-to-end — the seed it computes is consumed by spawn/death (server) and decoded+rendered on the client. Previously partial because the seed went nowhere. Logic/values/timing/liveness were already faithful/live in the prior audit (Wave-2). Line ref shifted 545->565 (Wave-5 GameWorld edits)."

  - id: mutator-cloaked.alpha.default_alpha_consumers
    name: "default_player_alpha / default_weapon_alpha applied at spawn, death, weapon entities, vehicle-exit; networked + rendered"
    layer: [authority, presentation]
    base_refs:
      - "server/client.qc:788,790 (PutClientInServer: player + exteriorweapon alpha)"
      - "server/player.qc:540 (PlayerDies: corpse alpha)"
      - "server/weapons/weaponsystem.qc:116-117,170-171 (weapon entity follows owner alpha)"
      - "common/vehicles/sv_vehicles.qc:814 (restore player.alpha on vehicle exit)"
      - "common/mutators/mutator/powerups/powerup/invisibility.qc:16-18 (Invisibility expiry restores default_player/weapon_alpha)"
    port_refs:
      - "src/XonoticGodot.Common/Gameplay/Player/SpawnSystem.cs:591 p.Alpha = MutatorHooks.DefaultPlayerAlpha (now reads the seed)"
      - "src/XonoticGodot.Common/Gameplay/Damage/DamageSystem.cs:629 victim.Alpha = MutatorHooks.DefaultPlayerAlpha (corpse reads the seed)"
      - "game/net/ServerNet.cs:1670,1760 QuantizeAlpha(p.Alpha)/QuantizeAlpha(e.Alpha) — networking present"
      - "src/XonoticGodot.Net/NetEntity.cs:96,131,176,230,271 EntityField.Alpha + NetEntityState.Alpha encode/decode — networking present"
      - "game/net/ClientEntityView.cs:134 e.Alpha = s.Alpha == 0 ? 1f : s.Alpha/255f — client net->render bridge NOW WIRED"
      - "game/client/PlayerModel.cs:160,217 ApplyAlpha(e.Alpha) — render present + fed the decoded alpha"
    constants:
      - { name: "spawn: player.alpha = default_player_alpha", base: "client.qc:788", port: "p.Alpha = MutatorHooks.DefaultPlayerAlpha (SpawnSystem.cs:575)", match: true }
      - { name: "spawn: exteriorweapon.alpha = default_weapon_alpha", base: "client.qc:790", port: "none (no exterior-weapon entity alpha-follow)", match: false }
      - { name: "death/corpse: this.alpha = default_player_alpha", base: "player.qc:540", port: "victim.Alpha = MutatorHooks.DefaultPlayerAlpha (DamageSystem.cs:624)", match: true }
      - { name: "weapon entity follows owner alpha", base: "weaponsystem.qc:116-117,170-171", port: "none", match: false }
      - { name: "vehicle-exit restores player.alpha = default_player_alpha", base: "sv_vehicles.qc:814", port: "none", match: false }
      - { name: ".alpha networked to clients (wire channel)", base: "DP networked render field (entity state)", port: "PRESENT: ServerNet QuantizeAlpha + NetEntity EntityField.Alpha (byte, 0=opaque sentinel; 1..254 = alpha/255)", match: true }
      - { name: ".alpha decoded onto the render entity (net->render bridge)", base: "DP applies networked alpha to the rendered ent", port: "PRESENT: ClientEntityView.DriveEntity:134 copies NetEntityState.Alpha -> Entity.Alpha (0->1f, else byte/255)", match: true }
      - { name: ".alpha rendered on the player model", base: "DP renders ent alpha", port: "PRESENT: PlayerModel.ApplyAlpha maps Entity.Alpha -> per-instance GeometryInstance3D.Transparency (real), fed the decoded alpha each frame via Pose:160", match: true }
      - { name: "0.25 round-trip fidelity", base: "0.25", port: "QuantizeAlpha(0.25)=round(63.75)->64; client 64/255=0.251 (1-byte quantization)", match: true }
    status:
      logic: faithful
      values: faithful
      timing: na
      presentation: faithful
      audio: na
      liveness: live
    intended_divergence: false
    divergence_rationale: null
    gaps:
      - "STILL OPEN (secondary) — no exterior/held weapon-entity alpha-follow: Base weaponsystem.qc:116-117,170-171 makes the held weapon model track default_weapon_alpha when the owner is at the default player alpha; the port has no such consumer. Minor vs the now-working player body alpha."
      - "STILL OPEN (secondary) — no vehicle-exit alpha restore (Base sv_vehicles.qc:814 resets player.alpha = default_player_alpha when leaving a vehicle)."
    verification:
      - { kind: code, ref: "VERIFIED (server consumers wired): SpawnSystem.cs:575 'p.Alpha = MutatorHooks.DefaultPlayerAlpha' (PutPlayerInServer respawn reset, live spawn path) and DamageSystem.cs:624 'victim.Alpha = MutatorHooks.DefaultPlayerAlpha' (PlayerDies corpse setup, live death path). Both read the Common-side seed set by FireSetDefaultAlpha at worldspawn.", result: pass }
      - { kind: code, ref: "VERIFIED (networking layer present): NetEntity.cs has EntityField.Alpha=1<<17 (line 96) + NetEntityState.Alpha (line 131) + diff/write/read (176/230/271); ServerNet.cs QuantizeAlpha(p.Alpha) at 1670 and QuantizeAlpha(e.Alpha) at 1760. QuantizeAlpha (ServerNet.cs:1977) returns 0 for alpha>=1 (opaque sentinel), clamp(round(alpha*255),1,254) otherwise.", result: pass }
      - { kind: code, ref: "VERIFIED (client bridge NOW wired — the prior gating defect): ClientEntityView.DriveEntity:134 'e.Alpha = s.Alpha == 0 ? 1f : s.Alpha/255f' decodes the networked byte onto the render proxy ([W5-cloaked] comment). The dequantize is the exact inverse of QuantizeAlpha.", result: pass }
      - { kind: code, ref: "VERIFIED (render path real and fed): PlayerModel.ApplyAlpha (PlayerModel.cs:217) maps Entity.Alpha -> per-instance GeometryInstance3D.Transparency, called every frame from Pose (line 160) with the decoded e.Alpha. End-to-end the player now renders translucent under g_cloaked 1.", result: pass }
      - { kind: code, ref: "VERIFIED (git): the bridge + Common-seed + consumer wiring all landed in commit 023ba70 (Wave 5 finish). The 2026-06-22 baseline predated Wave-5 and recorded the pre-fix hardcoded-1f state.", result: pass }
    confidence: high
    notes: "MAJOR FIX (Wave-5): logic partial->faithful, values missing->faithful, presentation partial->faithful, liveness dead->live for the player-body alpha. Wave-5 closed BOTH of the prior gating defects: (a) moved the seed into Common (MutatorHooks.DefaultPlayerAlpha) so SpawnSystem/DamageSystem/PowerupsMutator read it (no more cross-assembly barrier, no more hardcoded 1f), and (b) added the ClientEntityView net->render alpha bridge so the decoded NetEntityState.Alpha actually reaches PlayerModel.ApplyAlpha. The 0.25 cloaked value round-trips faithfully (1-byte quantization, 0.251 effective). Remaining gaps are the two SECONDARY consumers (exterior weapon-entity follow, vehicle-exit restore) — neither blocks the headline 'players go translucent' effect."

  - id: mutator-cloaked.report.mutators_pretty_string
    name: "Active-mutators string contribution (', Cloaked' + MUT_CLOAKED bit)"
    layer: [authority, presentation]
    base_refs:
      - "common/mutators/mutator/cloaked/sv_cloaked.qc:15-18 MUTATOR_HOOKFUNCTION(cloaked, BuildMutatorsPrettyString)"
      - "common/util.qc:302 X(\"Cloaked\", _(\"Cloaked\"), MUT_CLOAKED, cvar(\"g_cloaked\"))"
      - "common/util.qh:62 const int MUT_CLOAKED = 7"
    port_refs:
      - "src/XonoticGodot.Common/Gameplay/GameplayBases.cs:221 MutatorBase.BuildMutatorsPrettyString (base no-op virtual)"
      - "src/XonoticGodot.Common/Gameplay/Mutators/CloakedMutator.cs:73-78 BuildMutatorsPrettyString override — returns s + ', Cloaked' unless g_cts (NOW PORTED, Wave-6b)"
      - "src/XonoticGodot.Common/Gameplay/Mutators/MutatorActivation.cs:121 BuildMutatorsPrettyString chain runner (STILL no external caller — dead)"
    constants:
      - { name: "pretty string append", base: "', Cloaked' (suppressed in CTS)", port: "', Cloaked' unless g_cts (CloakedMutator.cs:73-78)", match: true }
      - { name: "g_cts suppression gate", base: "if (!g_cts) (sv_cloaked.qc:17)", port: "cts ? s : s + ', Cloaked' (CloakedMutator.cs:76-77)", match: true }
      - { name: "MUT_CLOAKED bit", base: "7 (util.qh:62)", port: "none", match: false }
    status:
      logic: faithful
      values: faithful
      timing: na
      presentation: faithful
      audio: na
      liveness: dead
    intended_divergence: false
    divergence_rationale: null
    gaps:
      - "STILL OPEN (liveness) — the pretty-string append now EXISTS and is faithful, but it is never displayed: MutatorActivation.BuildMutatorsPrettyString (MutatorActivation.cs:121) has NO external caller. Only the machine-token chain BuildMutatorsString is run live (GameWorld.cs:876 gamelog init). The scoreboard/HUD/vote screen never invokes the pretty-string runner, so ', Cloaked' is computed by no live path."
      - "STILL OPEN (minor) — no MUT_CLOAKED=7 bit (util.qh:62) / util.qc:302 bitfield mapping ported. Cosmetic mutator-bitfield only; not consumed by the live path that exists."
    verification:
      - { kind: code, ref: "VERIFIED (FIX): CloakedMutator.BuildMutatorsPrettyString (CloakedMutator.cs:73-78) overrides the base virtual, returning s + ', Cloaked' unless g_cts (faithful to sv_cloaked.qc:15-18 incl. the !g_cts gate). Landed Wave-6b (~25 mutators got the same override). The base virtual is at GameplayBases.cs:221.", result: pass }
      - { kind: code, ref: "VERIFIED (liveness still dead): grep for an external caller of BuildMutatorsPrettyString( finds only the self-reference at MutatorActivation.cs:125 (the chain runner's own loop). BuildMutatorsString IS called live (GameWorld.cs:876) but the PRETTY runner is not. So the override is dead code on the live path.", result: fail }
      - { kind: code, ref: "VERIFIED: no MUT_CLOAKED hits in src/+game/ (only Base util.qh:62 / util.qc:302). The bit mapping is unported.", result: fail }
    confidence: high
    notes: "2026-06-25 DRIFT — FIX (logic/values/presentation missing->faithful): CloakedMutator now overrides BuildMutatorsPrettyString with the faithful ', Cloaked' + !g_cts gate (Wave-6b). The baseline (2026-06-22, post-Wave-5) recorded only the base no-op. Liveness stays DEAD: the pretty-string chain runner has no caller, so nothing is displayed yet (the gating defect is now purely the missing live caller, not the missing override). MUT_CLOAKED bit still absent (minor)."

  - id: mutator-cloaked.menu.metadata
    name: "MENUQC metadata (describe text + create-game checkbox)"
    layer: [presentation]
    base_refs:
      - "common/mutators/mutator/cloaked/cloaked.qc:6-13 METHOD(MutatorCloaked, describe)"
      - "common/mutators/mutator/cloaked/cloaked.qh:4-8 CLASS(MutatorCloaked) + REGISTER_MUTATOR(cloaked, true, MutatorCloaked)"
      - "menu/xonotic/dialog_multiplayer_create_mutators.qc (g_cloaked checkbox)"
    port_refs:
      - "game/menu/dialogs/DialogMutators.cs:131-132 Widgets.CheckBox(\"g_cloaked\", \"Cloaked\", \"All players are almost invisible\") — create-game checkbox NOW PORTED"
    constants:
      - { name: "menu label", base: "_(\"Cloaked\")", port: "\"Cloaked\" (DialogMutators.cs:131)", match: true }
      - { name: "create-game checkbox cvar", base: "g_cloaked", port: "g_cloaked (DialogMutators.cs:131)", match: true }
      - { name: "checkbox tooltip", base: "create-mutators checkbox label _(\"Cloaked\")", port: "\"All players are almost invisible\" (close paraphrase of the describe text)", match: true }
      - { name: "in-game describe-page text", base: "'makes all players nearly invisible, similar to the Invisibility powerup' (cloaked.qc:6-13 METHOD describe)", port: "none", match: false }
    status:
      logic: faithful
      values: faithful
      timing: na
      presentation: partial
      audio: na
      liveness: live
    intended_divergence: false
    divergence_rationale: null
    gaps:
      - "STILL OPEN (minor) — the in-game per-mutator describe-page text (cloaked.qc:6-13 METHOD describe — 'makes all players nearly invisible, similar to the Invisibility powerup') is not ported. This is the general menu describe-page system gap (no port equivalent for any mutator), not unique to cloaked. The create-game checkbox + label + tooltip ARE present."
    verification:
      - { kind: code, ref: "VERIFIED (FIX): DialogMutators.cs:131-132 has Widgets.CheckBox(\"g_cloaked\", \"Cloaked\", \"All players are almost invisible\") — the create-game checkbox with the g_cloaked cvar, matching Base's dialog_multiplayer_create_mutators g_cloaked checkbox + _(\"Cloaked\") label. The tooltip is a close paraphrase of the cloaked describe text.", result: pass }
      - { kind: code, ref: "VERIFIED: no MutatorCloaked describe-page METHOD ported (grep MutatorCloaked/cloaked describe in src/+game/ = none beyond CloakedMutator.cs + this checkbox). The in-game describe page is still absent.", result: fail }
    confidence: high
    notes: "2026-06-25 DRIFT — FIX (presentation missing->partial, liveness na->live): the create-game checkbox is now wired (DialogMutators.cs:131, live in the multiplayer create-game mutators dialog). The baseline recorded port_refs: [] / all-missing. Remaining gap is only the in-game describe-page text (general menu-describe-system gap). Pure menu presentation; no gameplay effect."

  - id: mutator-cloaked.compose.invisibility_powerup
    name: "Composition with Invisibility powerup (expiry returns to default_player_alpha, i.e. cloaked alpha under cloaked)"
    layer: [authority, presentation]
    base_refs:
      - "common/mutators/mutator/powerups/powerup/invisibility.qc:16-18 (m_remove: actor.alpha = default_player_alpha; exteriorweaponentity.alpha = default_weapon_alpha)"
    port_refs:
      - "src/XonoticGodot.Common/Gameplay/Mutators/PowerupsMutator.cs:343 (invisibility held: player.Alpha = _invisibilityAlpha 0.15)"
      - "src/XonoticGodot.Common/Gameplay/Mutators/PowerupsMutator.cs:345 (lapse: player.Alpha = MutatorHooks.DefaultPlayerAlpha — composes with cloaked)"
    constants:
      - { name: "Invisibility while held sets player.alpha", base: "invisibility_alpha (0.15)", port: "player.Alpha = _invisibilityAlpha 0.15 (PowerupsMutator.cs:343) — LIVE", match: true }
      - { name: "Invisibility expiry restores to default_player_alpha", base: "yes (so under cloaked → 0.25, not 1)", port: "restores MutatorHooks.DefaultPlayerAlpha (PowerupsMutator.cs:345) — composes with cloaked", match: true }
    status:
      logic: faithful
      values: faithful
      timing: na
      presentation: faithful
      audio: na
      liveness: live
    intended_divergence: false
    divergence_rationale: null
    gaps: []
    verification:
      - { kind: code, ref: "VERIFIED: PowerupsMutator.cs:216 sets player.Alpha = _invisibilityAlpha (0.15) while invisibility active (live per-frame), and 218 restores MutatorHooks.DefaultPlayerAlpha on lapse (the same seed cloaked sets to 0.25). Under cloaked the restore correctly composes to 0.25, not 1f.", result: pass }
      - { kind: code, ref: "VERIFIED (now renders): same alpha channel that cloaked uses — the powerup-set player.Alpha (0.15) and the restore are networked (ServerNet QuantizeAlpha) AND now decoded by the client bridge (ClientEntityView.cs:134) and rendered (PlayerModel.ApplyAlpha). So the Invisibility powerup translucency, and its cloaked-composing restore, are both visible on remote players.", result: pass }
    confidence: high
    notes: "FIX (Wave-5): logic/values/presentation/liveness partial->faithful. Two prior defects closed: (1) the expiry restore now reads MutatorHooks.DefaultPlayerAlpha (was hardcoded 1f) so it composes with cloaked -> 0.25, and (2) the alpha now renders (the ClientEntityView net->render bridge that previously dropped it is wired). The Invisibility powerup writes player.Alpha while held (live producer) and restores the cloaked-aware default on lapse, matching Base invisibility.qc:16-18. (Base also restores exteriorweaponentity.alpha = default_weapon_alpha; the port has no exterior-weapon-entity alpha-follow — tracked under default_alpha_consumers, not re-counted here.)"
