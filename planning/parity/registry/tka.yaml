unit: tka
category: gametype
base_ref_rev: "v0.8.6-1779-g863cd3e84"
last_audited: "2026-06-22"
summary:
  feature_count: 17
  worst_gaps:
    - "tka.bot.role STILL BROKEN (top remaining gap): BotObjectiveRoles.FindBall (BotObjectiveRoles.cs:277) queries class 'keepaway_ball' (underscore), but the live ball is spawned with classname 'keepawayball' (BallEntity.cs:105, no underscore) — so the bot find always fails and bots fall back to DM. BotObjectiveRoles.cs was NOT touched by Wave 5 either. Still no enemy(8000)/sameteam(4000) goalrating split and no Bot_ForbidAttack."
    - "Drop triggers other than death are still unwired: only OnDeath calls DropBall. Base also drops on PlayerUseKey, ClientDisconnect, MakePlayerObserver, DropSpecialItems — none on MutatorHooks yet."
    - "Presentation entirely deferred (Wave 3): no TKA_BALLSTATUS HUD mod-icon (ModIconsPanel has no TKA mode), no STAT(TKA_BALLSTATUS) bit-packing on the server, no team-colored carrier/ball waypoints or radar, no respawn/spark particle effects, no carrier-highspeed (g_tka_ballcarrier_highspeed). Multi-ball chaining (g_tka_ballcarrier_maxballs) also unported."
    - "Re-audited post Wave-5 (commit 66e378f): TeamKeepaway.cs + BallEntity.cs were NOT modified by Wave 5; the only Wave-5 touch near TKA was GameWorld.cs (line-number shifts only) + a new Combat.Heal() in DamageContracts.cs (Onslaught — no TKA impact). All Wave-2 TKA wirings remain live: SpawnBall (GameWorld.cs:639-640), Activate (GameWorld.cs:1687-1688), Tick (GameWorld.cs:2026, ends with CheckPointLimit), winner report (GameWorld.cs:2528-2530), GiveFragsForKill fired (Scores.cs:520), DamageCalculate fired (DamageSystem.cs:256). NO fixes and NO regressions this pass."
features:
  - id: tka.ball.spawn
    name: "Ball spawn at match start (g_tkaball_count balls)"
    layer: [authority]
    base_refs:
      - "common/gametypes/gametype/tka/sv_tka.qc:tka_SpawnBalls"
      - "common/gametypes/gametype/tka/sv_tka.qc:tka_Handler_CheckBall"
    port_refs:
      - "src/XonoticGodot.Common/Gameplay/GameTypes/TeamKeepaway.cs:SpawnBall"
      - "src/XonoticGodot.Common/Gameplay/GameTypes/BallEntity.cs:SpawnForGametype"
      - "src/XonoticGodot.Server/GameWorld.cs:639-640"
    constants:
      - { name: "g_tkaball_count", base: "1", port: "1 (single ball; multi-ball loop not modeled)", match: true }
      - { name: "ball_bbox", base: "-24 -24 -24 .. 24 24 24", port: "-24 -24 -24 .. 24 24 24 (BallKind.KeepawayBall)", match: true }
      - { name: "g_tkaball_damageforcescale", base: "2", port: "2 (BallConfig.DamageForceScale default)", match: true }
      - { name: "g_tkaball_trail_color", base: "254 (glow_color)", port: "254 (TrailColor; glow_trail render deferred)", match: true }
      - { name: "g_tkaball_effects", base: "EF_DIMLIGHT (8) default", port: "8 (EfDimLight; client realization deferred)", match: true }
      - { name: "ball_model", base: "models/orbs/orbblue.md3 (MDL_TKA_BALL)", port: "models/orbs/orbblue.md3 (SetModel on the edict)", match: true }
    status:
      logic: faithful
      values: faithful
      timing: faithful
      presentation: partial
      audio: na
      liveness: live
    intended_divergence: false
    divergence_rationale: null
    gaps:
      - "Single ball only: SpawnBall makes one ball; g_tkaball_count multi-ball chaining (toucher.ballcarried.cnt vs g_tka_ballcarrier_maxballs) not modeled. Default count is 1, so values match the stock server."
      - "Model/EF_DIMLIGHT/trail_color/damageforcescale are set on the sim edict, but the actual glow_trail particle trail + EF_DIMLIGHT light are client/render concerns (Wave 3) — not yet visible."
      - "Spawn is procedural at map-load (GameWorld.cs:631-642 switch on GameType; TKA case at 639-640), replacing QC tka_Handler_CheckBall's per-frame INITPRIO_GAMETYPE think. There is no game_starttime gate / remove-and-respawn-during-warmup loop, but the net effect (one ball once the match is live) matches."
    verification:
      - { kind: codetrace, ref: "GameWorld.cs:631-642 post-spawn switch calls tka.SpawnBall(RandomMapLocation) for TeamKeepaway; BallEntity.SpawnForGametype(KeepawayBall) sets classname/model/bbox/effects/damageforcescale then Relocate()", result: pass }
      - { kind: value, ref: "BallConfig.WithKindDefaults(KeepawayBall): bbox +-24, model orbblue.md3, effects 8, trail 254, damageforcescale 2 — all match Base g_tkaball_* defaults", result: pass }
    confidence: high
    notes: "Wave-2 FIX: SpawnBall is now LIVE via the shared BallEntity framework + a GameWorld round-drive seam. A ball exists in a live match; presentation (trail/light) deferred; multi-ball not modeled."

  - id: tka.ball.respawn
    name: "Loose-ball relocate timer + fall-off respawn"
    layer: [authority]
    base_refs:
      - "common/gametypes/gametype/tka/sv_tka.qc:tka_RespawnBall"
      - "common/gametypes/gametype/tka/sv_tka.qc:tka_TouchEvent (NOIMPACT off-map branch)"
    port_refs:
      - "src/XonoticGodot.Common/Gameplay/GameTypes/TeamKeepaway.cs:RespawnBall"
      - "src/XonoticGodot.Common/Gameplay/GameTypes/BallEntity.cs:Relocate"
      - "src/XonoticGodot.Common/Gameplay/GameTypes/BallEntity.cs:RandomMapLocation"
    constants:
      - { name: "respawntime_cvar", base: "g_tkaball_respawntime (10)", port: "g_tkaball_respawntime (correct cvar), fallback 10", match: true }
      - { name: "respawn_velocity", base: "0 0 200", port: "0 0 200 (RespawnVelocity)", match: true }
    status:
      logic: faithful
      values: faithful
      timing: faithful
      presentation: missing
      audio: partial
      liveness: live
    intended_divergence: false
    divergence_rationale: null
    gaps:
      - "BallEntity.RandomMapLocation uses the QC SelectSpawnPoint FALLBACK path (a random spawnpoint), not the engine MoveToRandomMapLocation world-bounds sampler (rejecting solid/lava/sky). Faithful to the fallback QC itself drops to, but the primary random-world-point sampler is approximated."
      - "Respawn sound (SND_KA_RESPAWN, ATTEN_NONE) IS played in RespawnBall, but the EFFECT_KA_BALL_RESPAWN particles at the old+new origin and the WP_KaBall waypoint (g_tkaball_tracking) are deferred to presentation (Wave 3)."
      - "No NOIMPACT/fall-off respawn branch (tka_TouchEvent off-map Q3SURFACEFLAG_NOIMPACT handling not ported — the world-touch goes to the spark/sfx branch instead)."
    verification:
      - { kind: codetrace, ref: "RespawnBall -> BallEntity.Relocate (MoveToRandomMapLocation via RandomMapLocation) + RespawnVelocity + arm + SND_KA_RESPAWN; reads CvarRespawnTime=g_tkaball_respawntime; reachable from the now-live loose ball", result: pass }
    confidence: high
    notes: "Wave-2 FIX: now relocates to a random map location with the correct g_tkaball_respawntime cvar and is reachable on the live ball. Respawn particles/waypoint + the NOIMPACT off-map branch remain unported."

  - id: tka.ball.pickup
    name: "Ball pickup (attach to carrier, VIP, pickups++, orbit anim)"
    layer: [authority]
    base_refs:
      - "common/gametypes/gametype/tka/sv_tka.qc:tka_TouchEvent"
      - "common/gametypes/gametype/tka/sv_tka.qc:tka_BallThink_Carried"
    port_refs:
      - "src/XonoticGodot.Common/Gameplay/GameTypes/TeamKeepaway.cs:GiveBall"
      - "src/XonoticGodot.Common/Gameplay/GameTypes/TeamKeepaway.cs:BallTouchEntity"
      - "src/XonoticGodot.Common/Gameplay/GameTypes/BallEntity.cs:AttachToCarrier"
    constants:
      - { name: "carried_scale", base: "12/16 = 0.75", port: "none (entity scale not set while carried)", match: false }
      - { name: "self_recapture_lockout_wait", base: "0.5", port: "0.5 (BallEntity.SelfRecaptureLockout)", match: true }
      - { name: "orbit_xyspeed", base: "BALL_XYSPEED 100", port: "100 (BallEntity.BallOrbitSpeed) — driven from now-live Tick", match: true }
      - { name: "orbit_xydist", base: "BALL_XYDIST 24", port: "24 (BallEntity.BallOrbitDist) — driven from now-live Tick", match: true }
    status:
      logic: faithful
      values: partial
      timing: faithful
      presentation: missing
      audio: faithful
      liveness: live
    intended_divergence: false
    divergence_rationale: null
    gaps:
      - "GiveBall is LIVE: BallTouchEntity is the ball's touch handler (registered in the live SpawnBall), so a player touching the loose ball becomes the carrier, with SND_KA_PICKEDUP, INFO/CENTER_KEEPAWAY_PICKUP[_SELF] notifications, the 0.5s self-recapture lockout, AttachToCarrier, and TKA_PICKUPS+1."
      - "FIXED this pass: the carried-ball orbit anim (Tick -> BallEntity.CarryOrbit, BALL_XYSPEED 100 / XYDIST 24) now runs — GameWorld.cs:2026 calls tka.Tick(Simulation.FrameTime) every frame (was CheckPointLimit-only). The ball orbits the carrier."
      - "No scale 0.75 while carried, no GameRules_scoring_vip equivalent, no alpha-sync invisibility, no multi-ball chaining (this.ballcarried insert + cnt offset)."
      - "World-touch non-player branch plays SND_KA_TOUCH but omits the EFFECT_BALL_SPARKS particle (presentation, Wave 3)."
    verification:
      - { kind: codetrace, ref: "BallTouchEntity (live touch) -> GiveBall: Carrier set, AddCol TKA_PICKUPS, AttachToCarrier, SND_KA_PICKEDUP, 3 notifications, IsRecaptureLocked honored", result: pass }
      - { kind: codetrace, ref: "Orbit: GameWorld.cs:2026 case TeamKeepaway tka: tka.Tick(Simulation.FrameTime) -> Tick calls BallEntity.CarryOrbit + GametypeEntities.SetOrigin each frame; Tick now has a live caller", result: pass }
    confidence: high
    notes: "FIX this pass: orbit anim is now live (the tka.Tick wiring at GameWorld.cs:2026). values:partial kept only because scale 0.75 while carried is still unmodeled. Residual: scale/VIP/sparks/multi-ball unported."

  - id: tka.ball.drop
    name: "Ball drop on death / use-key / disconnect / observe"
    layer: [authority]
    base_refs:
      - "common/gametypes/gametype/tka/sv_tka.qc:tka_DropEvent"
      - "common/gametypes/gametype/tka/sv_tka.qc:tka_PlayerReset"
      - "common/gametypes/gametype/tka/sv_tka.qc:PlayerUseKey / ClientDisconnect / MakePlayerObserver / DropSpecialItems"
    port_refs:
      - "src/XonoticGodot.Common/Gameplay/GameTypes/TeamKeepaway.cs:DropBall"
      - "src/XonoticGodot.Common/Gameplay/GameTypes/TeamKeepaway.cs:OnDeath"
      - "src/XonoticGodot.Common/Gameplay/GameTypes/BallEntity.cs:DropFromCarrier"
    constants:
      - { name: "drop_offset_z", base: "10", port: "10 (DropFromCarrier dropOrigin + '0 0 10')", match: true }
      - { name: "drop_velocity", base: "0 0 200 + crandom xy scatter ('0 100 0'*crandom + '100 0 0'*crandom)", port: "0 0 200 + Prandom.Signed xy scatter (DropOffsetVelocity)", match: true }
      - { name: "self_recapture_lockout_wait", base: "0.5 (previous_owner/wait)", port: "0.5 (GtNextTakeTime + Owner=previous_owner)", match: true }
    status:
      logic: faithful
      values: faithful
      timing: faithful
      presentation: missing
      audio: faithful
      liveness: partial
    intended_divergence: false
    divergence_rationale: null
    gaps:
      - "DropBall is invoked from OnDeath (LIVE death hook) when the victim carried — now reachable because a carrier can exist. It detaches, drops at the feet with the crandom xy scatter, arms the 0.5s self-recapture lockout, re-arms the relocate think, plays SND_KA_DROPPED, and sends INFO/CENTER_KEEPAWAY_DROPPED."
      - "The use-key (PlayerUseKey), ClientDisconnect, MakePlayerObserver, and DropSpecialItems drop paths are STILL NOT wired (those MutatorHooks don't exist yet) — only the death path drops the ball."
      - "No WP_KaBall waypoint respawn (SPRITERULE_DEFAULT), no nudgeoutofsolid_OrFallback, no multi-ball chain advance / tka_PlayerReset VIP clear."
    verification:
      - { kind: codetrace, ref: "OnDeath victimWasCarrier -> DropBall -> BallEntity.DropFromCarrier (scatter + lockout + re-arm) + SND_KA_DROPPED + 2 notifications; reachable now that GiveBall sets Carrier", result: pass }
      - { kind: codetrace, ref: "Grep DropBall callers in TeamKeepaway -> only OnDeath; no PlayerUseKey/ClientDisconnect/MakePlayerObserver/DropSpecialItems hooks", result: fail }
    confidence: high
    notes: "Wave-2 FIX: death-drop is now LIVE + faithful (scatter, lockout, sound, notifs). The other four drop triggers remain absent pending the missing MutatorHooks."

  - id: tka.score.teamkill
    name: "Team kill scoring (killac to team while team holds ball)"
    layer: [authority]
    base_refs:
      - "common/gametypes/gametype/tka/sv_tka.qc:PlayerDies (GameRules_scoring_add_team SCORE, g_tka_score_killac)"
    port_refs:
      - "src/XonoticGodot.Common/Gameplay/GameTypes/TeamKeepaway.cs:OnDeath"
      - "src/XonoticGodot.Common/Gameplay/GameTypes/TeamKeepaway.cs:AddTeamScore"
    constants:
      - { name: "score_killac_cvar", base: "g_tka_score_killac (default 1)", port: "g_tka_score_killac (correct cvar), fallback 1", match: true }
    status:
      logic: faithful
      values: faithful
      timing: faithful
      presentation: na
      audio: na
      liveness: live
    intended_divergence: false
    divergence_rationale: null
    gaps:
      - "OnDeath is on the LIVE Combat.Death bus and credits the attacker's TEAM (AddTeamScore -> GS.AddToTeam slot 0). It now adds killac when the attacker personally carries OR (g_tka_score_team && the team holds the ball) — matching Base PlayerDies. With a live ball + carrier path, this actually fires."
      - "The Base DIFF_TEAM(attacker,target) team-kill exclusion is now enforced (scoringKill = attacker!=victim && !SameTeam), so team-kills no longer score."
      - "Correct cvar family now: reads g_tka_score_killac (fallback 1 = Base default), not the old g_keepaway_score_killac/0."
    verification:
      - { kind: codetrace, ref: "OnDeath: scoringKill gated on !SameTeam; AddTeamScore(team, ScoreKillAc) when attackerCarries || (ScoreTeam && TeamHasBall); ScoreKillAc reads g_tka_score_killac fallback 1", result: pass }
      - { kind: value, ref: "CvarScoreKillAc=g_tka_score_killac + DefaultScoreKillAc=1 matches Base gametypes-server.cfg g_tka_score_killac 1", result: pass }
    confidence: high
    notes: "Wave-2 FIX: correct cvar family + DIFF_TEAM exclusion + team-has-ball gating, and live via the now-real carry path."

  - id: tka.score.scoreteam
    name: "g_tka_score_team: any teammate's kill scores while team holds ball"
    layer: [authority]
    base_refs:
      - "common/gametypes/gametype/tka/sv_tka.qc:PlayerDies (team_has_ball scan + g_tka_score_team)"
    port_refs:
      - "src/XonoticGodot.Common/Gameplay/GameTypes/TeamKeepaway.cs:OnDeath"
      - "src/XonoticGodot.Common/Gameplay/GameTypes/TeamKeepaway.cs:TeamHasBall"
      - "src/XonoticGodot.Common/Gameplay/GameTypes/TeamKeepaway.cs:ScoreTeam"
    constants:
      - { name: "g_tka_score_team", base: "1", port: "g_tka_score_team (correct cvar), fallback true", match: true }
    status:
      logic: faithful
      values: faithful
      timing: faithful
      presentation: na
      audio: na
      liveness: live
    intended_divergence: false
    divergence_rationale: null
    gaps:
      - "The DISTINGUISHING TKA rule is now modeled: TeamHasBall(team) checks whether the single ball's carrier is on the attacker's team, and when ScoreTeam (g_tka_score_team, default 1) is set ANY teammate's cross-team kill scores killac for the team (OnDeath: attackerCarries || teamHasBall)."
      - "The no-ball-kill warning suppression is also modeled: the noncarrier warn (CENTER_KEEPAWAY_WARN) fires only when !attackerCarries && !teamHasBall && g_tka_noncarrier_warn."
      - "Single-ball simplification: TeamHasBall is BallTeam==team rather than IL_EACH(g_tkaballs, ...); correct for the one-ball case (the only case the port spawns)."
    verification:
      - { kind: codetrace, ref: "OnDeath: teamHasBall = ScoreTeam && TeamHasBall(team); killac added on attackerCarries||teamHasBall; warn suppressed when teamHasBall; ScoreTeam reads g_tka_score_team fallback true", result: pass }
    confidence: high
    notes: "Wave-2 FIX (missing->faithful): the team-scoring rule that makes TKA a TEAM mode is now implemented and live."

  - id: tka.score.bckill
    name: "Ball-carrier-kill bonus (bckill to team) + carrierkills column"
    layer: [authority]
    base_refs:
      - "common/gametypes/gametype/tka/sv_tka.qc:PlayerDies (TKA_CARRIERKILLS, g_tka_score_bckill)"
    port_refs:
      - "src/XonoticGodot.Common/Gameplay/GameTypes/TeamKeepaway.cs:OnDeath"
    constants:
      - { name: "score_bckill_cvar", base: "g_tka_score_bckill (default 1)", port: "g_tka_score_bckill (correct cvar), fallback 1", match: true }
    status:
      logic: faithful
      values: faithful
      timing: faithful
      presentation: na
      audio: na
      liveness: live
    intended_divergence: false
    divergence_rationale: null
    gaps:
      - "OnDeath now awards the bckill team bonus whenever the victim was the carrier (victimWasCarrier branch), INDEPENDENT of whether the attacker carries — matching Base (any cross-team kill of the carrier). It also credits TKA_CARRIERKILLS to the killer."
      - "Reachable now that a carrier can exist (live ball/pickup path)."
      - "Correct cvar: g_tka_score_bckill, fallback 1 (= Base default), replacing the old g_keepaway_score_bckill/0."
    verification:
      - { kind: codetrace, ref: "OnDeath: if (victimWasCarrier) { AddCol TKA_CARRIERKILLS +1; AddTeamScore(team, ScoreBcKill); } — outside the attackerCarries gate; ScoreBcKill reads g_tka_score_bckill fallback 1", result: pass }
      - { kind: value, ref: "CvarScoreBcKill=g_tka_score_bckill + DefaultScoreBcKill=1 matches Base g_tka_score_bckill 1", result: pass }
    confidence: high
    notes: "Wave-2 FIX: bckill is now correctly un-gated from attacker-carry, reads the right cvar, and is live."

  - id: tka.score.bctime
    name: "Ball-carry-time secondary column (TKA_BCTIME += frametime)"
    layer: [authority]
    base_refs:
      - "common/gametypes/gametype/tka/sv_tka.qc:tka_BallThink_Carried (TKA_BCTIME)"
      - "common/gametypes/gametype/tka/sv_tka.qc:tka_Initialize (GameRules_scoring fields)"
    port_refs:
      - "src/XonoticGodot.Common/Gameplay/GameTypes/TeamKeepaway.cs:Activate"
      - "src/XonoticGodot.Common/Gameplay/GameTypes/TeamKeepaway.cs:Tick"
    constants:
      - { name: "bctime_sort_prio", base: "SFL_SORT_PRIO_SECONDARY", port: "SetSortKeys(Score, TKA_BCTIME)", match: true }
    status:
      logic: faithful
      values: faithful
      timing: faithful
      presentation: na
      audio: na
      liveness: live
    intended_divergence: false
    divergence_rationale: null
    gaps:
      - "FIXED this pass (dead->live): the TKA_BCTIME column is DECLARED in Activate (sorts as secondary) and Tick() accrues frametime to TKA_BCTIME (banks whole seconds, carries the fractional remainder, resets the remainder on each new pickup in GiveBall). Tick now has a LIVE caller: GameWorld.cs:2026 `case TeamKeepaway tka: tka.Tick(Simulation.FrameTime); break;` (was CheckPointLimit-only). So with a carrier, TKA_BCTIME accrues each frame."
    verification:
      - { kind: codetrace, ref: "TeamKeepaway.Tick (TeamKeepaway.cs:516-522) accrues _bcTimeRemainder + AddCol TKA_BCTIME wholeSecs; live caller GameWorld.cs:2026 case TeamKeepaway tka: tka.Tick(Simulation.FrameTime)", result: pass }
    confidence: high
    notes: "FIX this pass: the single-line round-drive wiring (CheckPointLimit -> Tick) at GameWorld.cs:2026 makes the bctime accrual live. Tick() ends with CheckPointLimit() so the limit check is preserved."

  - id: tka.score.timepoints
    name: "Timed possession points to TEAM score (g_tka_score_timepoints)"
    layer: [authority]
    base_refs:
      - "common/gametypes/gametype/tka/sv_tka.qc:tka_BallThink_Carried (GameRules_scoring_add_team_float2int SCORE)"
    port_refs:
      - "src/XonoticGodot.Common/Gameplay/GameTypes/TeamKeepaway.cs:Tick"
      - "src/XonoticGodot.Common/Gameplay/GameTypes/TeamKeepaway.cs:ScoreTimePoints"
    constants:
      - { name: "g_tka_score_timepoints", base: "0 (off by default)", port: "g_tka_score_timepoints (correct cvar), fallback 0", match: true }
    status:
      logic: faithful
      values: faithful
      timing: faithful
      presentation: na
      audio: na
      liveness: live
    intended_divergence: false
    divergence_rationale: null
    gaps:
      - "FIXED this pass (dead->live): Tick() accrues g_tka_score_timepoints*dt to the carrier's TEAM score with a fractional remainder (QC float2int_decimal_fld analogue), and Tick is now CALLED every frame on the live path (GameWorld.cs:2026 case TeamKeepaway tka: tka.Tick(Simulation.FrameTime); was CheckPointLimit-only). Default is 0 (off), so still no impact on stock servers, but the feature is now connected rather than dead-wired."
      - "Residual: Scores_CountFragsRemaining suppression tied to timepoints (announce remaining frags only when timepoints off) is still unported."
    verification:
      - { kind: codetrace, ref: "TeamKeepaway.Tick (TeamKeepaway.cs:502-512) has the timepoints accrual via ScoreTimePoints (g_tka_score_timepoints) -> AddTeamScore; live caller GameWorld.cs:2026 tka.Tick(Simulation.FrameTime)", result: pass }
    confidence: high
    notes: "FIX this pass: same single-line root cause as bctime now resolved (CheckPointLimit -> Tick at GameWorld.cs:2026). Cvar/remainder logic correct and live. Only the Scores_CountFragsRemaining suppression remains."

  - id: tka.score.nofrags
    name: "No DM frags (GiveFragsForKill zeroed) + reset_map_global"
    layer: [authority]
    base_refs:
      - "common/gametypes/gametype/tka/sv_tka.qc:GiveFragsForKill"
      - "common/gametypes/gametype/tka/sv_tka.qc:reset_map_global"
    port_refs:
      - "src/XonoticGodot.Common/Gameplay/GameTypes/TeamKeepaway.cs:OnGiveFragsForKill"
      - "src/XonoticGodot.Server/Scores.cs:520"
    constants:
      - { name: "frag_delta", base: "0 (GiveFragsForKill sets M_ARGV(2)=0)", port: "0 (OnGiveFragsForKill sets FragScore=0, returns true)", match: true }
    status:
      logic: faithful
      values: faithful
      timing: na
      presentation: na
      audio: na
      liveness: live
    intended_divergence: false
    divergence_rationale: null
    gaps:
      - "TKA now subscribes MutatorHooks.GiveFragsForKill (Activate) and zeroes the per-kill frag delta (returns true), and the hook is now actually FIRED on the live scoring path at Scores.cs:520 (the ENEMY-FRAG branch reads the adjusted FragScore back). So DM frags are suppressed in TKA, matching Base."
      - "reset_map_global (clear float2int_decimal_fld per client) has no port analogue; moot while timepoints is dead (and the remainder is a class field reset in Activate)."
    verification:
      - { kind: codetrace, ref: "Activate -> MutatorHooks.GiveFragsForKill.Add(OnGiveFragsForKill); Scores.cs:520 GiveFragsForKill.Call -> frag=(int)gfk.FragScore before GiveFrags", result: pass }
    confidence: high
    notes: "Wave-1/2 FIX: the GiveFragsForKill chain is now wired at the Scores.Obituary call site (previously the dormant cross-boundary gap), and TKA zeroes the frag."

  - id: tka.damage.matrix
    name: "Possession damage/force scaling matrix (Damage_Calculate)"
    layer: [authority]
    base_refs:
      - "common/gametypes/gametype/tka/sv_tka.qc:Damage_Calculate"
    port_refs:
      - "src/XonoticGodot.Common/Gameplay/GameTypes/TeamKeepaway.cs:DamageScale"
      - "src/XonoticGodot.Common/Gameplay/GameTypes/TeamKeepaway.cs:DamageForceScale"
      - "src/XonoticGodot.Common/Gameplay/GameTypes/TeamKeepaway.cs:OnDamageCalculate"
      - "src/XonoticGodot.Common/Gameplay/Damage/DamageSystem.cs:256"
    constants:
      - { name: "ballcarrier_damage_cvar", base: "g_tka_ballcarrier_damage (vector 1 1 1)", port: "g_tka_ballcarrier_damage parsed as a vector, fallback 1", match: true }
      - { name: "ballcarrier_force", base: "g_tka_ballcarrier_force (vector 1 1 1)", port: "g_tka_ballcarrier_force (DamageForceScale getter), fallback 1", match: true }
      - { name: "noncarrier_damage_cvar", base: "g_tka_noncarrier_damage (vector 1 1 1)", port: "g_tka_noncarrier_damage parsed as a vector, fallback 1", match: true }
      - { name: "noncarrier_force", base: "g_tka_noncarrier_force (vector 1 1 1)", port: "g_tka_noncarrier_force, fallback 1", match: true }
    status:
      logic: faithful
      values: faithful
      timing: na
      presentation: na
      audio: na
      liveness: live
    intended_divergence: false
    divergence_rationale: null
    gaps:
      - "TeamKeepaway now subscribes MutatorHooks.DamageCalculate (Activate), and that hook is fired by DamageSystem.cs:256 on every player-vs-player hit, so the possession damage+force matrix actually affects combat (previously zero callers)."
      - "Both damage AND force are scaled: OnDamageCalculate applies DamageScale to args.Damage and DamageForceScale to args.Force (the force getter was previously missing entirely)."
      - "Correct cvar family + form: reads the VECTOR cvars g_tka_ballcarrier/noncarrier_damage/force and indexes the x/y/z component (self / other-carrier / noncarrier) chosen by whether the ATTACKER carries — matching Base. Default 1 1 1, so no scaling on stock servers, but it is now connected."
    verification:
      - { kind: codetrace, ref: "Activate -> MutatorHooks.DamageCalculate.Add(OnDamageCalculate); DamageSystem.cs:254-256 builds DamageCalculateArgs + Call; ScaleFactor parses vector cvar component by attacker-carries", result: pass }
    confidence: high
    notes: "Wave-2 FIX (dead->live): the matrix is now subscribed to the live DamageCalculate channel, reads the correct g_tka vector cvars, and scales both damage and force."

  - id: tka.carrier.highspeed
    name: "Carrier speed multiplier (MOVEVARS_HIGHSPEED *= g_tka_ballcarrier_highspeed)"
    layer: [authority]
    base_refs:
      - "common/gametypes/gametype/tka/sv_tka.qc:PlayerPhysics_UpdateStats"
    port_refs: []
    constants:
      - { name: "g_tka_ballcarrier_highspeed", base: "1", port: "none", match: false }
    status:
      logic: missing
      values: missing
      timing: missing
      presentation: na
      audio: na
      liveness: na
    intended_divergence: false
    divergence_rationale: null
    gaps:
      - "No PlayerPhysics hook applies a carrier highspeed multiplier (explicitly deferred in the class comment as needing a PlayerPhysics_UpdateStats seam)."
      - "Default 1 (no effect by default); absent feature with low stock impact."
    verification:
      - { kind: codetrace, ref: "No tka reference in PlayerPhysics.cs / MovementParameters; class comment lists carrier-highspeed as deferred", result: fail }
    confidence: high
    notes: "Unchanged: no carrier movement modifier. Default 1 so no stock impact."

  - id: tka.win.pointlimit
    name: "Team point-limit + lead-limit win condition"
    layer: [authority]
    base_refs:
      - "common/gametypes/gametype/tka/sv_tka.qh:GameRules_limit_score / GameRules_limit_lead"
      - "common/gametypes/gametype/tka/tka.qh:gametype_init pointlimit=50 leadlimit=0"
    port_refs:
      - "src/XonoticGodot.Common/Gameplay/GameTypes/TeamKeepaway.cs:CheckPointLimit"
      - "src/XonoticGodot.Server/GameWorld.cs:2026"
      - "src/XonoticGodot.Server/GameWorld.cs:2530"
    constants:
      - { name: "pointlimit_default", base: "50 (mapinfo; g_tka_point_limit=-1->mapinfo)", port: "50 (DefaultPointLimit)", match: true }
      - { name: "leadlimit_default", base: "0 (g_tka_point_leadlimit -> mapinfo 0)", port: "0 (LeadLimit; now modeled in CheckPointLimit)", match: true }
      - { name: "timelimit_default", base: "15", port: "via gametype_init/cvars", match: true }
    status:
      logic: faithful
      values: faithful
      timing: na
      presentation: na
      audio: na
      liveness: live
    intended_divergence: false
    divergence_rationale: null
    gaps:
      - "CheckPointLimit is LIVE (now reached via tka.Tick at GameWorld.cs:2026, which ends with CheckPointLimit()) and latches WinningTeam at the point limit via the ST_SCORE team leader; reported at GameWorld.cs:2528-2530 on MatchEnded. With the now-working scoring path it can actually trigger in play."
      - "Lead limit (g_tka_point_leadlimit / GameRules_limit_lead) is modeled: LeadLimit getter + the leader-vs-second check in CheckPointLimit."
      - "Port PointLimit fallback is literal 50; Base g_tka_point_limit default is -1 (use mapinfo 50). Resolution order (g_tka_point_limit -> fraglimit -> 50) is reasonable."
    verification:
      - { kind: codetrace, ref: "GameWorld.cs:2528-2530 reports tka.WinningTeam when MatchEnded; CheckPointLimit polled each frame via tka.Tick at :2026 (Tick ends with CheckPointLimit); lead-limit branch present", result: pass }
    confidence: high
    notes: "Was already live; the limit check is preserved through the Tick wiring change (Tick calls CheckPointLimit). Lead-limit modeling + the live scoring path make the limit actually reachable."

  - id: tka.team.framing
    name: "Team framing (teams 2..4, team spawns) + tie -> overtime"
    layer: [authority]
    base_refs:
      - "common/gametypes/gametype/tka/sv_tka.qh:GameRules_teams / GameRules_spawning_teams"
      - "common/gametypes/gametype/tka/sv_tka.qc:tka_Initialize (tka_teams clamp 2..4)"
      - "common/gametypes/gametype/tka/tka.qh:gametype_init GAMETYPE_FLAG_TEAMPLAY teams=2"
    port_refs:
      - "src/XonoticGodot.Common/Gameplay/GameTypes/TeamKeepaway.cs:TeamCount"
      - "src/XonoticGodot.Common/Gameplay/GameTypes/TeamKeepaway.cs:RequestsTeamSpawns"
      - "src/XonoticGodot.Common/Gameplay/GameTypes/TeamKeepaway.cs:ReportsTie"
    constants:
      - { name: "teamplay", base: "true (GAMETYPE_FLAG_TEAMPLAY)", port: "TeamGame=true", match: true }
      - { name: "teams_default", base: "2 (clamp 2..4; g_tka_teams_override>=2 else g_tka_teams)", port: "2 (clamp 2..4; same override logic)", match: true }
      - { name: "g_tka_team_spawns", base: "0 (no team spawnpoints by default)", port: "RequestsTeamSpawns reads g_tka_team_spawns, default false", match: true }
    status:
      logic: faithful
      values: faithful
      timing: na
      presentation: na
      audio: na
      liveness: live
    intended_divergence: false
    divergence_rationale: null
    gaps:
      - "Team-count clamp (2..4), the override logic (g_tka_teams_override>=2 else g_tka_teams), team-spawn gating (g_tka_team_spawns), and tie->overtime (TeamTie.TopTwoTied on ST_SCORE) are faithfully modeled. Reachable via GameWorld.cs:1687-1688 tka.Activate()."
    verification:
      - { kind: codetrace, ref: "tka.Activate() at GameWorld.cs:1687-1688; TeamGame=true; TeamCount clamp 2..4 + override; RequestsTeamSpawns gates g_tka_team_spawns; ReportsTie=TeamTie.TopTwoTied", result: pass }
    confidence: high
    notes: "Unchanged: correctly modeled as a team mode, with the correct g_tka_* cvars."

  - id: tka.hud.modicon
    name: "TKA HUD mod-icon + TKA_BALLSTATUS stat (team ball-taken icons)"
    layer: [presentation, authority]
    base_refs:
      - "common/gametypes/gametype/tka/cl_tka.qc:HUD_Mod_TeamKeepaway"
      - "common/gametypes/gametype/tka/sv_tka.qc:PlayerPreThink (STAT(TKA_BALLSTATUS) bits)"
      - "common/gametypes/gametype/tka/sv_tka.qc:SpectateCopy"
    port_refs: []
    constants:
      - { name: "blink", base: "blink(0.85,0.15,5)", port: "none", match: false }
      - { name: "TKA_BALL_CARRYING", base: "BIT(4)", port: "never set", match: false }
      - { name: "TKA_BALL_TAKEN_RED/BLUE/YELLOW/PINK", base: "BIT(0..3)", port: "never set", match: false }
      - { name: "TKA_BALL_DROPPED", base: "BIT(5)", port: "never set", match: false }
    status:
      logic: missing
      values: missing
      timing: missing
      presentation: missing
      audio: na
      liveness: na
    intended_divergence: false
    divergence_rationale: null
    gaps:
      - "ModIconsPanel still has no Keepaway/TeamKeepaway mode or DrawTeamKeepaway (grep for tka/ballstatus in ModIconsPanel.cs -> no hits); NetGame mode-select never sets it."
      - "Server never produces STAT(TKA_BALLSTATUS) (PlayerPreThink bit-packing of carrying/dropped/per-team taken absent), so the team ball-taken icons (tka_taken_red/blue/yellow/pink) + carrying icon with expand/blink transition are absent on both server (stat) and client (renderer)."
      - "SpectateCopy mirroring of the ball status is also unported."
    verification:
      - { kind: codetrace, ref: "Grep TKA_BALLSTATUS/HUD_Mod_TeamKeepaway -> no port hits; ModIconsPanel has no tka mode (grep result empty)", result: fail }
    confidence: high
    notes: "Unchanged: presentation deferred to Wave 3. TKA's per-team-taken mod-icon remains entirely absent."

  - id: tka.waypoints.tracking
    name: "Team-colored carrier / ball waypoints + radar tracking"
    layer: [presentation, authority]
    base_refs:
      - "common/gametypes/gametype/tka/sv_tka.qc:tka_ballcarrier_waypointsprite_visible_for_player"
      - "common/gametypes/gametype/tka/sv_tka.qc:tka_TouchEvent (WP_TkaBallCarrier{Red,Blue,Yellow,Pink})"
      - "common/gametypes/gametype/tka/sv_tka.qc:tka_RespawnBall / tka_DropEvent (WP_KaBall)"
    port_refs: []
    constants:
      - { name: "g_tkaball_tracking", base: "1 (0=none/1=tracking/warmup)", port: "none", match: false }
    status:
      logic: missing
      values: missing
      timing: na
      presentation: missing
      audio: na
      liveness: na
    intended_divergence: false
    divergence_rationale: null
    gaps:
      - "No WP_KaBall / per-team WP_TkaBallCarrier{Red,Blue,Yellow,Pink} sprites; no SPRITERULE_TEAMPLAY carrier colormod (colormapPaletteColor by team); no RADARICON_FLAGCARRIER."
      - "No tracking-mode handling (g_tkaball_tracking), no spectator/invisibility visibility rules, no same-team always-visible rule (tka_ballcarrier_waypointsprite_visible_for_player)."
    verification:
      - { kind: codetrace, ref: "No tka waypoint code in src/** or game/**", result: fail }
    confidence: high
    notes: "Unchanged: presentation deferred to Wave 3. TKA's team-colored carrier sprites all absent."

  - id: tka.bot.role
    name: "Bot TKA roles (carrier/collector, team-aware goalrating, ForbidAttack)"
    layer: [authority]
    base_refs:
      - "common/gametypes/gametype/tka/sv_tka.qc:havocbot_role_tka_carrier / havocbot_role_tka_collector"
      - "common/gametypes/gametype/tka/sv_tka.qc:havocbot_goalrating_tkaball"
      - "common/gametypes/gametype/tka/sv_tka.qc:Bot_ForbidAttack"
    port_refs:
      - "src/XonoticGodot.Server/Bot/BotObjectiveRoles.cs:RoleKeepaway"
      - "src/XonoticGodot.Server/Bot/BotRoles.cs:99"
    constants:
      - { name: "ball_goalrating_scale", base: "8000 (enemy) / 4000 (sameteam) ratingscale, distance 2000", port: "RatingBall 8000, no sameteam split", match: false }
      - { name: "ball_class", base: "new(keepawayball) -> classname keepawayball", port: "FindBall queries 'keepaway_ball' (UNDERSCORE mismatch vs the live 'keepawayball')", match: false }
    status:
      logic: partial
      values: partial
      timing: na
      presentation: na
      audio: na
      liveness: dead
    intended_divergence: false
    divergence_rationale: null
    gaps:
      - "STILL BROKEN (unchanged this pass — BotObjectiveRoles.cs was not edited on this branch): RoleKeepaway is dispatched for 'tka' (BotRoles.cs:99, LIVE dispatch) but FindBall() (BotObjectiveRoles.cs:277) searches class 'keepaway_ball' while the now-live ball is spawned with classname 'keepawayball' (BallEntity.cs:105, no underscore) -> bot always finds no ball and falls back to DM behavior. A ball now exists, but the class-name mismatch keeps the role inert (affects both Keepaway and TKA)."
      - "Shared KA role is NOT team-aware: no enemy(8000)/same-team(4000) goalrating split (havocbot_goalrating_tkaball with DIFF_TEAM/SAME_TEAM), so even if the classname matched bots would mis-rate same-team carriers."
      - "No Bot_ForbidAttack equivalent (forbid attacking when neither player holds the ball and a ball is held, with the score_team team-has-ball exception)."
      - "Collector/carrier split collapsed into one role; ratings approximate."
    verification:
      - { kind: codetrace, ref: "BotRoles.cs:99 'tka' -> RoleKeepaway; BotObjectiveRoles.cs:277 FindByClass('keepaway_ball') vs BallEntity.cs:105 ClassName 'keepawayball'; no sameteam rating split; no ForbidAttack", result: fail }
    confidence: high
    notes: "STILL OPEN: the ball is now live but the bot ball-find classname is wrong (keepaway_ball vs keepawayball), so bots remain inert; also not team-aware. Trivial-looking but real dead-wire."
