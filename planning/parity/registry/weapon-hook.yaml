unit: weapon-hook
category: weapon
base_ref_rev: "v0.8.6-1779-g863cd3e84"
last_audited: "2026-06-27"
summary:
  feature_count: 12
  worst_gaps:
    - "No hook ROPE line is ever drawn: there is still no GrapplingHookSend/ENT_CLIENT_HOOK rope stream and no Draw_CylindricLine/Draw_GrapplingHook renderer. The chain HEAD is networked as a generic projectile and rendered as a red Hookbomb spike WHILE IN FLIGHT, but the connecting rope between player and latch point is absent."
    - "Latched hook still goes INVISIBLE: GrapplingHook_Stop sets MoveType.None, which is not a projectile movetype, so ServerNet.Classify (IsProjectileMoveType, ServerNet.cs:2319-2320/2339) drops the model-less chain from the snapshot (Generic with no alias model) — the spike vanishes the moment the hook latches. Base keeps the latched hook + rope drawn via CSQC."
    - "FIXED since the 2026-06-25 audit: a central RemoveGrapplingHooks(pl) reset now EXISTS (Hook.cs:479) and is wired LIVE into observer/disconnect (ClientManager.cs:549/805), freezetag/status-effect Frozen apply (StatusEffects.cs:155), teleport (Teleporters.cs:251/273), vehicle-enter (VehicleBoarding.cs:229), and wr_resetplayer (Hook.cs:592, dispatched per slot by SpawnSystem.cs:905). The offhand +hook is now SAME-TICK (published in OnClientMove before PlayerPreThink, GameWorld.cs:2143-2148) with a vehicle gate."
features:
  - id: weapon-hook.identity.attributes
    name: "Weapon identity, models, color, flags, ammo type"
    layer: [shared, presentation]
    base_refs:
      - "common/weapons/weapon/hook.qh:CLASS(Hook)"
    port_refs:
      - "src/XonoticGodot.Common/Gameplay/Weapons/Hook.cs:Hook()"
    constants:
      - { name: "ammo_type", base: "RES_FUEL", port: "ResourceType.Fuel", match: true }
      - { name: "m_color", base: "'0.471 0.817 0.392'", port: "(0.471,0.817,0.392)", match: true }
      - { name: "spawnflags", base: "CANCLIMB|TYPE_SPLASH|NOTRUEAIM", port: "CanClimb|TypeSplash|NoTrueAim", match: true }
      - { name: "view/world/item model", base: "h_hookgun.iqm / v_hookgun.md3 / g_hookgun.md3", port: "same", match: true }
      - { name: "crosshair", base: "gfx/crosshairhook size 0.5", port: "gfx/crosshairhook size 0.5 (Hook.cs:75-76)", match: true }
    status:
      logic: faithful
      values: faithful
      timing: na
      presentation: faithful
      audio: na
      liveness: live
    intended_divergence: false
    divergence_rationale: null
    gaps: []
    verification:
      - { kind: code, ref: "Hook.cs ctor vs hook.qh CLASS(Hook)", result: pass }
      - { kind: code, ref: "Hook in WeaponOrder.cs registry list; ItemSpawnFuncs.cs registers weapon_hook map pickup", result: pass }
      - { kind: code, ref: "Crosshair override (Hook.cs:75-76 'gfx/crosshairhook'/0.5) vs hook.qh:31-32 ATTRIB w_crosshair/w_crosshair_size; consumed LIVE by CrosshairPanel.cs (weapon.Crosshair + weapon.CrosshairSize)", result: pass }
    confidence: high
    notes: "Unchanged since 2026-06-25. Per-weapon crosshair carried on the port weapon (Hook.cs:75-76) and consumed live by CrosshairPanel. Auto-registered via [Weapon]; obtainable + selectable on the normal match path."

  - id: weapon-hook.primary.state_machine
    name: "Primary grapple lifecycle (fire/reel/remove hook_state machine)"
    layer: [authority]
    base_refs:
      - "common/weapons/weapon/hook.qc:Hook.wr_think"
    port_refs:
      - "src/XonoticGodot.Common/Gameplay/Weapons/Hook.cs:WrThink"
    constants:
      - { name: "pri.refire", base: "0.2 s", port: "0.2 s", match: true }
      - { name: "pri.ammo", base: "5 fuel", port: "5 fuel", match: true }
      - { name: "pri.animtime", base: "0.3 s", port: "0.3 s", match: true }
      - { name: "W_WeaponRateFactor on refire", base: "applied", port: "applied (Hook.cs:200,247)", match: true }
      - { name: "pauseregen 1s regen inhibit", base: "yes (g_balance_pause_fuel_regen)", port: "yes (Hook.cs:204-206, PauseRegenFinished)", match: true }
    status:
      logic: faithful
      values: faithful
      timing: faithful
      presentation: na
      audio: na
      liveness: live
    intended_divergence: false
    divergence_rationale: null
    gaps:
      - "No offhand best-weapon switch (W_SwitchWeapon_Force/w_getbestweapon) when fuel runs out mid-reel (hook.qc fuel-out branch: if m_weapon != WEP_Null switch to best weapon)."
    verification:
      - { kind: code, ref: "Hook.WrThink Primary branch vs wr_think; WeaponRateFactor(actor) applied on HookRefire (Hook.cs:200,247); PauseRegenFinished set 1s w/ IT_UNLIMITED_AMMO guard (Hook.cs:204-206)", result: pass }
      - { kind: code, ref: "WeaponFireDriver.Frame calls WrThink(Primary) every tick for the active slot-0 weapon", result: pass }
    confidence: high
    notes: "Unchanged. Press/release latch, WAITING_FOR_RELEASE, hooked_time_max, HOOK_PULLING, FIRING/REMOVING tail all faithful; latched .state==1 modelled as hook.Health>0; W_WeaponRateFactor refire scaling + 1s pauseregen inhibit ported. Only residual gap is the fuel-out best-weapon auto-switch (offhand)."

  - id: weapon-hook.primary.hooked_fuel_drain
    name: "Hooked-state fuel drain + free grace period"
    layer: [authority]
    base_refs:
      - "common/weapons/weapon/hook.qc:Hook.wr_think"
    port_refs:
      - "src/XonoticGodot.Common/Gameplay/Weapons/Hook.cs:WrThink"
    constants:
      - { name: "pri.hooked_ammo", base: "5 fuel/s", port: "5 fuel/s", match: true }
      - { name: "pri.hooked_time_free", base: "2 s", port: "2 s", match: true }
      - { name: "pri.hooked_time_max", base: "0 (infinite)", port: "0 (infinite)", match: true }
    status:
      logic: faithful
      values: faithful
      timing: faithful
      presentation: na
      audio: na
      liveness: live
    intended_divergence: false
    divergence_rationale: null
    gaps: []
    verification:
      - { kind: code, ref: "WrThink hooked-fuel block vs QC fuel drain; IT_UNLIMITED_AMMO guard present (Hook.cs:215, !unlimitedAmmo)", result: pass }
    confidence: high
    notes: "Unchanged. The drain block skips entirely under IT_UNLIMITED_AMMO. Free for hooked_time_free seconds, then drains hooked_ammo/s; fuel-out forces remove. Offhand path tops fuel up when useammo off (so the drain is a no-op)."

  - id: weapon-hook.primary.fire_grapplinghook
    name: "FireGrapplingHook: launch the chain projectile"
    layer: [authority, presentation]
    base_refs:
      - "server/hook.qc:FireGrapplingHook"
    port_refs:
      - "src/XonoticGodot.Common/Gameplay/Weapons/Hook.cs:FireGrapplingHook"
    constants:
      - { name: "grapplehook_speed_fly", base: "1800 qu/s", port: "Bal(g_balance_grapplehook_speed_fly,1800)", match: true }
      - { name: "grapplehook_health", base: "50 hp", port: "Bal(g_balance_grapplehook_health,50)", match: true }
      - { name: "chain bbox", base: "'-3 -3 -3'..'3 3 3'", port: "same", match: true }
      - { name: "movetype", base: "FLY (TOSS if grapplehook_gravity)", port: "FLY (TOSS if GrappleGravity, Hook.cs:271)", match: true }
      - { name: "muzzle flash", base: "W_MuzzleFlash (EFFECT_HOOK_MUZZLEFLASH)", port: "EffectEmitter.Emit(HOOK_MUZZLEFLASH) (Hook.cs:263)", match: true }
    status:
      logic: partial
      values: faithful
      timing: faithful
      presentation: partial
      audio: faithful
      liveness: live
    intended_divergence: false
    divergence_rationale: null
    gaps:
      - "No dedicated hook entity stream (GrapplingHookSend / ENT_CLIENT_HOOK with hook_start/hook_end). The chain HEAD IS networked via the generic projectile feed (ServerNet.Classify -> Projectile: FL_PROJECTILE/Item + Owner + MoveType.Fly) and renders as a red Hookbomb spike — but with no rope-endpoint stream there is no rope line (see weapon-hook.presentation.rope_line)."
      - "No WarpZone refsys tracking (WarpZone_RefSys_SpawnSameRefSys)."
      - "weaponLocked(actor)/actor.vehicle early-return guard at the top of FireGrapplingHook not ported (the offhand path now gates the vehicle case upstream at GameWorld.cs:2147, but the direct weapon-fire path lacks the weaponLocked guard)."
    verification:
      - { kind: code, ref: "Hook.FireGrapplingHook vs server/hook.qc FireGrapplingHook", result: pass }
      - { kind: code, ref: "ServerNet.Classify: grapplinghook (Item+Owner+Fly) -> Projectile (ServerNet.cs:2339); ProjectileCatalog grapplinghook/hook -> Hookbomb", result: pass }
      - { kind: code, ref: "W_MuzzleFlash ported: EffectEmitter.Emit(HOOK_MUZZLEFLASH) at shot origin (Hook.cs:263); gravity-TOSS movetype honoured (Hook.cs:271-272)", result: pass }
    confidence: high
    notes: "Unchanged classification. Hook muzzle flash emitted (HOOK_MUZZLEFLASH = grapple_muzzleflash) and g_balance_grapplehook_gravity TOSS-mode movetype honoured. Logic stays partial for the absent WarpZone refsys + the unported weaponLocked guard; presentation stays partial because the dedicated rope-endpoint stream is still absent (tracked in rope_line). Plays hook_fire sound; shootable HP set; EditProjectile mutator hook called."

  - id: weapon-hook.primary.latch
    name: "GrapplingHookTouch / GrapplingHook_Stop: latch onto what it hits"
    layer: [authority, presentation]
    base_refs:
      - "server/hook.qc:GrapplingHookTouch"
      - "server/hook.qc:GrapplingHook_Stop"
    port_refs:
      - "src/XonoticGodot.Common/Gameplay/Weapons/Hook.cs:GrapplingHookTouch"
    constants: []
    status:
      logic: faithful
      values: na
      timing: faithful
      presentation: partial
      audio: faithful
      liveness: live
    intended_divergence: false
    divergence_rationale: null
    gaps:
      - "Latched hook becomes INVISIBLE: GrapplingHook_Stop sets MoveType.None (Hook.cs:318), which is NOT a projectile movetype, so ServerNet.Classify (IsProjectileMoveType, ServerNet.cs:2319-2320/2339) no longer classifies it as Projectile; with no model it falls to Generic and is dropped from the render. The rendered spike disappears on latch. Base keeps the latched hook drawn via CSQC."
    verification:
      - { kind: code, ref: "GrapplingHookTouch vs QC touch+Stop+SetMovetypeFollow", result: pass }
      - { kind: code, ref: "EFFECT_HOOK_IMPACT emitted: EffectEmitter.Emit(\"HOOK_IMPACT\", origin) (Hook.cs:315); hook_impact sound played", result: pass }
      - { kind: code, ref: "ServerNet.Classify: MoveType.None model-less entity -> Generic (no alias model) -> dropped (IsProjectileMoveType excludes None, ServerNet.cs:2319-2320)", result: fail }
    confidence: high
    notes: "Unchanged. Emits the EFFECT_HOOK_IMPACT particle on latch AND ports SetMovetypeFollow — records the hit entity as hook.Aiment, clamps the follow-offset into the player bbox, and tracks a moving aiment each think (GrapplingHookThink re-origins the chain to follow.Origin+ViewOfs). This is the precondition vampirehook drains off. STILL OPEN: the latched head is invisible (MoveType.None drops it from the snapshot)."

  - id: weapon-hook.primary.reel_pull
    name: "GrapplingHookThink: reel the firer toward the latch"
    layer: [authority]
    base_refs:
      - "server/hook.qc:GrapplingHookThink"
    port_refs:
      - "src/XonoticGodot.Common/Gameplay/Weapons/Hook.cs:GrapplingHookThink"
    constants:
      - { name: "g_grappling_hook_tarzan", base: "2 (rubber-band, can pull players)", port: "Bal(g_grappling_hook_tarzan,2) -> tarzan branch", match: true }
      - { name: "grapplehook_speed_pull", base: "2000 qu/s", port: "Bal(...,2000)", match: true }
      - { name: "grapplehook_force_rubber", base: "2000", port: "Bal(...,2000)", match: true }
      - { name: "grapplehook_stretch", base: "50 qu", port: "Bal(...,50)", match: true }
      - { name: "grapplehook_airfriction", base: "0.2", port: "Bal(...,0.2)", match: true }
      - { name: "grapplehook_force_rubber_overstretch", base: "1000", port: "Bal(...,1000)", match: true }
      - { name: "grapplehook_length_min", base: "50 qu", port: "Bal(...,50)", match: true }
      - { name: "reel myorg origin", base: "origin+view_ofs+hook_shotorigin('8 8 -12')", port: "origin+ViewOfs (no gun offset, Hook.cs:374)", match: false }
    status:
      logic: faithful
      values: faithful
      timing: faithful
      presentation: na
      audio: na
      liveness: live
    intended_divergence: false
    divergence_rationale: null
    gaps:
      - "Reel origin omits the gun shot-origin offset (hook_shotorigin '8 8 -12'), so latch/length geometry is slightly offset from Base."
      - "Base applies the rubber-band velocity to pull_entity only `if (!frozen_pulling && !(aiment.flags & FL_PROJECTILE))`; the port always writes actor.Velocity. Differs only when the aiment is a projectile/frozen (edge case)."
      - "Mutator tarzan/pull_entity/velocity_multiplier rewrite from GrappleHookThink not honoured (port's GrappleHookThinkArgs carries no such args); pull_frozen (g_balance_grapplehook_pull_frozen) and the frozen-pull early-remove are not ported; no WarpZone refsys transforms."
      - "pusher/pushltime/istypefrag frag attribution on the PULLED aiment (tarzan>=2 reel-to-victim) not set (the shoot-down frag credit IS set, but the reel-pull credit window is separate; out of scope)."
    verification:
      - { kind: code, ref: "GrapplingHookThink tarzan branch (newlength/overstretch/spring/airfriction + tarzan>=2 aiment pull) vs server/hook.qc tarzan branch", result: pass }
      - { kind: value, ref: "balance-xonotic.cfg g_grappling_hook_tarzan 2; speed_pull/force_rubber/stretch/airfriction all match Base", result: pass }
    confidence: high
    notes: "Unchanged. The elastic tarzan rubber-band reel (hook_length shorten toward minlength, overstretch spring, airfriction, spring-to-latch) AND the tarzan>=2 half-impulse pull of a hooked player/nade — a near-bit transcription of Base's tarzan branch; the old straight tarzan-0 reel is kept as the else branch. Residual divergences are minor (gun-offset, projectile-aiment velocity guard, mutator velocity_multiplier, pull_frozen, warpzone). Fires the GrappleHookThink mutator hook (feeds vampirehook)."

  - id: weapon-hook.primary.remove_reset
    name: "RemoveHook / RemoveGrapplingHooks / reset + shoot-down"
    layer: [authority]
    base_refs:
      - "server/hook.qc:RemoveHook"
      - "server/hook.qc:RemoveGrapplingHooks"
      - "server/hook.qc:GrapplingHook_Damage"
      - "common/weapons/weapon/hook.qc:Hook.wr_resetplayer"
    port_refs:
      - "src/XonoticGodot.Common/Gameplay/Weapons/Hook.cs:RemoveHook"
      - "src/XonoticGodot.Common/Gameplay/Weapons/Hook.cs:RemoveGrapplingHooks"
      - "src/XonoticGodot.Common/Gameplay/Weapons/Hook.cs:WrResetPlayer"
    constants: []
    status:
      logic: faithful
      values: na
      timing: faithful
      presentation: na
      audio: na
      liveness: live
    intended_divergence: false
    divergence_rationale: null
    gaps:
      - "Shoot-down frag attribution sets pusher/pushltime + istypefrag=actor.ButtonChat (Hook.cs:296), which is now faithful to Base's PHYS_INPUT_BUTTON_CHAT classification (prior hardcoded-false gap resolved)."
    verification:
      - { kind: code, ref: "Hook.RemoveHook + ProjectileDamage callback vs server/hook.qc RemoveHook/GrapplingHook_Damage; FLY->WALK restore in RemoveHook (Hook.cs:464-465); pusher/pushltime/istypefrag credit set (Hook.cs:290-297)", result: pass }
      - { kind: code, ref: "RemoveGrapplingHooks(pl) static (Hook.cs:479) wired LIVE: ClientManager.cs:549 (PutObserverInServer) + :805 (ClientDisconnect); StatusEffects.cs:155 (Frozen OnApply); Teleporters.cs:251/273; VehicleBoarding.cs:229 (vehicles_enter)", result: pass }
      - { kind: code, ref: "WrResetPlayer (Hook.cs:590) calls RemoveGrapplingHooks + resets hook timers; dispatched per populated slot by SpawnSystem.cs:905 (and NixMutator.cs:287)", result: pass }
    confidence: high
    notes: "FIX since 2026-06-25: the central RemoveGrapplingHooks reset now EXISTS (Hook.cs:479-492, FLY->WALK + clears every slot's .hook) and is wired LIVE into observer/disconnect, freezetag/Frozen-status apply, teleport, vehicle-enter, and wr_resetplayer (live via SpawnSystem) — the long-standing 'no central reset' worst-gap is resolved (logic partial->faithful). RemoveHook restores FLY->WALK; shoot-down credits pusher/pushltime + istypefrag=ButtonChat. The top-of-think guard drops a hook whose followed entity vanished / is a non-nade projectile. The only residual divergence from GrapplingHook_Damage is that the port's callback re-applies damage via the projectile-HP path rather than the QC TakeResource ordering (behaviourally equivalent shoot-down)."

  - id: weapon-hook.secondary.gravity_bomb
    name: "Secondary: lob the gravity bomb (W_Hook_Attack2)"
    layer: [authority, presentation]
    base_refs:
      - "common/weapons/weapon/hook.qc:W_Hook_Attack2"
    port_refs:
      - "src/XonoticGodot.Common/Gameplay/Weapons/Hook.cs:AttackGravityBomb"
    constants:
      - { name: "sec.refire", base: "3 s", port: "3 s (via refire gate)", match: true }
      - { name: "sec.gravity", base: "5", port: "5", match: true }
      - { name: "sec.speed", base: "0 qu/s", port: "0", match: true }
      - { name: "sec.health", base: "15 hp", port: "15", match: true }
      - { name: "sec.lifetime", base: "5 s", port: "5 s", match: true }
      - { name: "setup recoil", base: "4", port: "4", match: true }
      - { name: "newton_style velocity add", base: "if g_projectiles_newton_style", port: "not modelled", match: false }
    status:
      logic: faithful
      values: faithful
      timing: faithful
      presentation: faithful
      audio: faithful
      liveness: live
    intended_divergence: true
    divergence_rationale: "Base gates the secondary via weapon_prepareattack on sec.refire; the ammo-free port bomb is routed through Weapon.RefireFor(Secondary)=sec.refire so holding ATK2 can't spawn one bomb per tick. Net 3 s cadence matches Base."
    gaps:
      - "No Newton-style owner-velocity add to the bomb (g_projectiles_newton_style)."
      - "No bot_dodge/bot_dodgerating, missile_flags (MIF_SPLASH|MIF_ARC), or damagedbycontents on the bomb (bot AI + lava/contents-detonation; out of scope here)."
    verification:
      - { kind: code, ref: "AttackGravityBomb vs W_Hook_Attack2", result: pass }
      - { kind: code, ref: "hookbomb (Item+Owner+Toss) -> ServerNet Projectile; ProjectileCatalog -> Hookbomb render; HOOK_MUZZLEFLASH emitted (Hook.cs:531)", result: pass }
    confidence: high
    notes: "Unchanged. Toss launch, lifetime+touch+shoot-down detonation, shootable HP all faithful; bomb networked + rendered as a Hookbomb projectile; hookbomb_fire sound + HOOK_MUZZLEFLASH effect emitted."

  - id: weapon-hook.secondary.blast_curve
    name: "Secondary blast: duration-spread power-curve pull (W_Hook_ExplodeThink)"
    layer: [authority]
    base_refs:
      - "common/weapons/weapon/hook.qc:W_Hook_Explode2"
      - "common/weapons/weapon/hook.qc:W_Hook_ExplodeThink"
    port_refs:
      - "src/XonoticGodot.Common/Gameplay/Weapons/Hook.cs:StartBlast"
      - "src/XonoticGodot.Common/Gameplay/Weapons/Hook.cs:ExplodeThink"
    constants:
      - { name: "sec.damage", base: "25", port: "25", match: true }
      - { name: "sec.edgedamage", base: "5", port: "5", match: true }
      - { name: "sec.radius", base: "500 qu", port: "500", match: true }
      - { name: "sec.force", base: "-2000 (pull inward)", port: "-2000", match: true }
      - { name: "sec.duration", base: "1.5 s", port: "1.5 s", match: true }
      - { name: "sec.power", base: "3 (falloff exponent)", port: "3", match: true }
      - { name: "explode tick", base: "0.05 s", port: "0.05 s", match: true }
    status:
      logic: faithful
      values: faithful
      timing: faithful
      presentation: faithful
      audio: faithful
      liveness: live
    intended_divergence: false
    divergence_rationale: null
    gaps: []
    verification:
      - { kind: code, ref: "ExplodeThink power-curve delta vs W_Hook_ExplodeThink", result: pass }
      - { kind: code, ref: "EF_NODRAW set on blast start (Hook.cs:541, self.Effects |= EffectFlags.NoDraw); HITTYPE_SPAM OR'd into deathtype after the first tick (Hook.cs:573, DeathTypes.WithHitType(..., Spam))", result: pass }
    confidence: high
    notes: "FIX since 2026-06-25: the two prior presentation/deathtype gaps are resolved — EF_NODRAW is now set on the bomb at blast start (Hook.cs:541, hides the model while the pull ticks) and HITTYPE_SPAM is now OR'd into the deathtype after the first blast tick (Hook.cs:573), matching W_Hook_ExplodeThink. The centrepiece power-curve (f = prev_remaining - clamp(1-dt/duration,0,1)^power applied each 0.05s tick so the cumulative blast == one full RadiusDamage) is faithful. EFFECT_HOOK_EXPLODE emitted at blast start."

  - id: weapon-hook.offhand.mutator
    name: "Grappling Hook offhand mutator (g_grappling_hook)"
    layer: [authority]
    base_refs:
      - "common/mutators/mutator/hook/sv_hook.qc:REGISTER_MUTATOR(hook)"
      - "common/mutators/mutator/hook/sv_hook.qc:OFFHAND_HOOK.offhand_think"
    port_refs:
      - "src/XonoticGodot.Common/Gameplay/Mutators/HookMutator.cs"
    constants:
      - { name: "g_grappling_hook", base: "0 (string; instahook=1)", port: "ExprEvaluate(g_grappling_hook)", match: true }
      - { name: "g_grappling_hook_useammo", base: "0", port: "read", match: true }
      - { name: "fuel grant (useammo)", base: "FuelRegen + g_balance_fuel_rotstable", port: "same", match: true }
    status:
      logic: faithful
      values: faithful
      timing: faithful
      presentation: na
      audio: na
      liveness: live
    intended_divergence: false
    divergence_rationale: null
    gaps:
      - "No useammo ammo_factor toggle on the Hook weapon (handled via fuel top-up instead) — behaviourally equivalent; only matters with g_grappling_hook_useammo on."
      - "weaponUseForbidden side of the QC gate (weaponsystem.qc:609-611) is not modelled (WeaponUseForbidden is a stub returning false); the vehicle case IS now gated (GameWorld.cs:2147 `&& p.Vehicle is null`)."
    verification:
      - { kind: code, ref: "HookMutator OnPlayerPreThink (HookMutator.cs:94-112); MutatorHooks.PlayerPreThink.Call live in GameWorld.cs:2169-2171", result: pass }
      - { kind: code, ref: "Full input chain traced LIVE: BindInput -> BindTable (_hook/HookHeld) -> NetGame (buttons|=InputButtons.Hook) -> InputCommand (Hook=BIT(6)) -> ServerNet.cs:1617 (ButtonHook) -> GameWorld.cs:2147 (OffhandFirePressed = hookPressed && Vehicle is null) -> HookMutator.cs:103 (st.ButtonAttack=OffhandFirePressed) -> Hook.WrThink fires FireGrapplingHook", result: pass }
      - { kind: code, ref: "ordering: OffhandFirePressed published in OnClientMove (GameWorld.cs:2143-2148) BEFORE PlayerPreThink (line 2169) -> SAME-TICK offhand-think, no read/publish latency (bots keep the PostThink publish in WeaponFireDriver.cs:69)", result: pass }
    confidence: high
    notes: "FIX since 2026-06-25: the prior 1-tick read/publish latency is RESOLVED (timing partial->faithful) — the human offhand button is now published in OnClientMove BEFORE the PlayerPreThink hook (GameWorld.cs:2143-2148), so the grapple reacts same-tick like QC's W_WeaponFrame; bots (who never bind +hook) keep the PostThink publish. A vehicle gate was also added (GameWorld.cs:2147 zeroes the key in a vehicle, QC weaponsystem.qc:609-611). Offhand assign + filter-item suppress + start-fuel grant remain faithful. Residual: useammo ammo_factor toggle + the weaponUseForbidden half of the QC gate."

  - id: weapon-hook.mutator.vampirehook
    name: "Vampire Hook (drain HP while hooked to an enemy)"
    layer: [authority]
    base_refs:
      - "common/mutators/mutator/vampirehook/sv_vampirehook.qc:MUTATOR_HOOKFUNCTION(vh, GrappleHookThink)"
    port_refs:
      - "src/XonoticGodot.Common/Gameplay/Mutators/VampireHookMutator.cs:OnGrappleHookThink"
    constants:
      - { name: "g_vampirehook_damagerate", base: "0.1 s (mutators.cfg 0.2)", port: "0.2 s default", match: true }
      - { name: "g_vampirehook_damage / health_steal / teamheal", base: "cvars", port: "read", match: true }
    status:
      logic: faithful
      values: faithful
      timing: faithful
      presentation: na
      audio: na
      liveness: live
    intended_divergence: false
    divergence_rationale: null
    gaps:
      - "hitsound_damage_dealt HUD/stats accumulator on the drain is omitted (cosmetic; port doesn't carry it)."
      - "Drains only on a DIRECT hook hit onto an enemy player; with tarzan reel-to-victim a hooked player can also be pulled, which works, but the drain still requires the chain's aiment to be that player (matches Base, since Base also keys on thehook.aiment)."
    verification:
      - { kind: code, ref: "VampireHookMutator.OnGrappleHookThink vs sv_vampirehook.qc (last_dmg gate, time<game_starttime block, team/enemy/frozen/alive guards, Damage(WEP_HOOK)+Heal, team-heal self-drain)", result: pass }
      - { kind: code, ref: "Hook.GrapplingHookTouch sets self.Aiment=other (SetMovetypeFollow port); thehook.RealOwner=>Owner=firing actor; GrappleHookThink.Call invoked each think (Hook.cs:363-364)", result: pass }
    confidence: high
    notes: "Unchanged. The latch records the hit entity as hook.Aiment (SetMovetypeFollow port in GrapplingHookTouch), so a direct hook hit on an enemy player satisfies the IS_PLAYER(aiment) guard and the per-damagerate drain/heal fires on the live match path (when g_vampirehook is set). The GrappleHookThink mutator hook is dispatched each think by Hook.GrapplingHookThink. Body is a faithful transcription of the QC guards + Damage/Heal."

  - id: weapon-hook.presentation.rope_line
    name: "CSQC rope line rendering (Draw_GrapplingHook)"
    layer: [presentation]
    base_refs:
      - "common/weapons/weapon/hook.qc:Draw_GrapplingHook"
      - "common/weapons/weapon/hook.qc:NET_HANDLE(ENT_CLIENT_HOOK)"
      - "server/hook.qc:GrapplingHookSend"
    port_refs: []
    constants:
      - { name: "rope thickness", base: "8", port: "n/a", match: false }
      - { name: "team textures", base: "particles/hook_red/blue/yellow/pink/white", port: "n/a", match: false }
      - { name: "cl_grapplehook_alpha", base: "1", port: "n/a", match: false }
    status:
      logic: missing
      values: missing
      timing: missing
      presentation: missing
      audio: na
      liveness: na
    intended_divergence: false
    divergence_rationale: null
    gaps:
      - "No hook ROPE line is drawn. There is no GrapplingHookSend/ENT_CLIENT_HOOK stream (with hook_start/hook_end), no Draw_CylindricLine/Draw_GrapplingHook, and no team-coloured rope texture/WarpZone-traced segment in game/ (all Draw_CylindricLine hits in game/ are unrelated beam/laser/porto renderers). The chain HEAD is networked + rendered as a Hookbomb spike while in flight (and disappears once latched), but the connecting rope between the firer's gun and the latch point is entirely absent."
    verification:
      - { kind: code, ref: "no Draw_GrapplingHook/ENT_CLIENT_HOOK/GrapplingHookSend in game/ or src/ (only this registry + spec docs); chain head only classified as ProjectileType.Hookbomb (a spike), no rope", result: fail }
    confidence: high
    notes: "STILL OPEN. Distinct from the chain-head spike (rendered while flying). The rope line itself + the dedicated hook entity stream (with a DrawGrapplingHook mutator hook for team colour) are unimplemented presentation."
