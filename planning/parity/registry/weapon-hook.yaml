unit: weapon-hook
category: weapon
base_ref_rev: "v0.8.6-1779-g863cd3e84"
last_audited: "2026-06-22"
summary:
  feature_count: 12
  worst_gaps:
    - "No hook ROPE line is ever drawn: there is still no GrapplingHookSend/ENT_CLIENT_HOOK rope stream and no Draw_CylindricLine renderer. The chain HEAD is networked as a generic projectile and rendered as a red Hookbomb spike WHILE IN FLIGHT, but the connecting rope between player and latch point is absent."
    - "Latched hook still goes INVISIBLE: GrapplingHook_Stop sets MoveType.None, which is not a projectile movetype, so ServerNet.Classify drops the model-less chain from the snapshot (Generic with no alias model) — the spike vanishes the moment the hook latches. Base keeps the latched hook + rope drawn via CSQC."
    - "Offhand hook is STILL dead: the +hook offhand-fire button never reaches the server — InputButtons has no hook bit and Entity.OffhandFirePressed is never assigned anywhere in src/ or game/, so OnPlayerPreThink always sees the button released. Instahook/campaign offhand hook can't fire (same missing-input-wire as nade-throw / offhand-blaster)."
    - "Primary hook fire has no muzzle flash (Base W_MuzzleFlash) and ignores g_balance_grapplehook_gravity TOSS-mode movetype (always FLY)."
features:
  - id: weapon-hook.identity.attributes
    name: "Weapon identity, models, color, flags, ammo type"
    layer: [shared, presentation]
    base_refs:
      - "common/weapons/weapon/hook.qh:CLASS(Hook)"
    port_refs:
      - "src/XonoticGodot.Common/Gameplay/Weapons/Hook.cs:Hook()"
    constants:
      - { name: "ammo_type", base: "RES_FUEL", port: "ResourceType.Fuel", match: true }
      - { name: "m_color", base: "'0.471 0.817 0.392'", port: "(0.471,0.817,0.392)", match: true }
      - { name: "spawnflags", base: "CANCLIMB|TYPE_SPLASH|NOTRUEAIM", port: "CanClimb|TypeSplash|NoTrueAim", match: true }
      - { name: "view/world/item model", base: "h_hookgun.iqm / v_hookgun.md3 / g_hookgun.md3", port: "same", match: true }
      - { name: "crosshair", base: "gfx/crosshairhook size 0.5", port: "n/a (not set on Hook.cs)", match: false }
    status:
      logic: faithful
      values: faithful
      timing: na
      presentation: partial
      audio: na
      liveness: live
    intended_divergence: false
    divergence_rationale: null
    gaps:
      - "Crosshair string/size not carried on the port weapon (cosmetic)."
    verification:
      - { kind: code, ref: "Hook.cs ctor vs hook.qh CLASS(Hook)", result: pass }
      - { kind: code, ref: "Hook in WeaponOrder.cs registry list; ItemSpawnFuncs.cs registers weapon_hook map pickup", result: pass }
    confidence: high
    notes: "Unchanged by Wave 1/2. Auto-registered via [Weapon]; obtainable + selectable on the normal match path."

  - id: weapon-hook.primary.state_machine
    name: "Primary grapple lifecycle (fire/reel/remove hook_state machine)"
    layer: [authority]
    base_refs:
      - "common/weapons/weapon/hook.qc:Hook.wr_think"
    port_refs:
      - "src/XonoticGodot.Common/Gameplay/Weapons/Hook.cs:WrThink"
    constants:
      - { name: "pri.refire", base: "0.2 s", port: "0.2 s", match: true }
      - { name: "pri.ammo", base: "5 fuel", port: "5 fuel", match: true }
      - { name: "pri.animtime", base: "0.3 s", port: "0.3 s", match: true }
      - { name: "W_WeaponRateFactor on refire", base: "applied", port: "not applied", match: false }
      - { name: "pauseregen 1s regen inhibit", base: "yes (g_balance_pause_fuel_regen)", port: "not modelled", match: false }
    status:
      logic: faithful
      values: partial
      timing: faithful
      presentation: na
      audio: na
      liveness: live
    intended_divergence: false
    divergence_rationale: null
    gaps:
      - "No W_WeaponRateFactor scaling on hook_refire."
      - "No 1-second health-regen inhibit (pauseregen_finished) while hooked."
      - "No offhand best-weapon switch (W_SwitchWeapon_Force/w_getbestweapon) when fuel runs out mid-reel."
    verification:
      - { kind: code, ref: "Hook.WrThink Primary branch vs wr_think", result: pass }
      - { kind: code, ref: "WeaponFireDriver.Frame calls WrThink(Primary) every tick for the active slot-0 weapon", result: pass }
    confidence: high
    notes: "Unchanged by Wave 1/2. Press/release latch, WAITING_FOR_RELEASE, hooked_time_max, HOOK_PULLING, FIRING/REMOVING tail all faithful; latched .state==1 modelled as hook.Health>0. Dispatched per-tick by WeaponFireDriver when the Hook is active."

  - id: weapon-hook.primary.hooked_fuel_drain
    name: "Hooked-state fuel drain + free grace period"
    layer: [authority]
    base_refs:
      - "common/weapons/weapon/hook.qc:Hook.wr_think"
    port_refs:
      - "src/XonoticGodot.Common/Gameplay/Weapons/Hook.cs:WrThink"
    constants:
      - { name: "pri.hooked_ammo", base: "5 fuel/s", port: "5 fuel/s", match: true }
      - { name: "pri.hooked_time_free", base: "2 s", port: "2 s", match: true }
      - { name: "pri.hooked_time_max", base: "0 (infinite)", port: "0 (infinite)", match: true }
    status:
      logic: faithful
      values: faithful
      timing: faithful
      presentation: na
      audio: na
      liveness: live
    intended_divergence: false
    divergence_rationale: null
    gaps:
      - "QC also skips the drain entirely under IT_UNLIMITED_AMMO; the port has no IT_UNLIMITED_AMMO guard in the drain block (minor — unlimited-ammo modes)."
    verification:
      - { kind: code, ref: "WrThink hooked-fuel block vs QC fuel drain", result: pass }
    confidence: high
    notes: "Unchanged by Wave 1/2. Faithful: free for hooked_time_free seconds, then drains hooked_ammo/s; fuel-out forces remove. Offhand path tops fuel up when useammo off (so the drain is a no-op)."

  - id: weapon-hook.primary.fire_grapplinghook
    name: "FireGrapplingHook: launch the chain projectile"
    layer: [authority, presentation]
    base_refs:
      - "server/hook.qc:FireGrapplingHook"
    port_refs:
      - "src/XonoticGodot.Common/Gameplay/Weapons/Hook.cs:FireGrapplingHook"
    constants:
      - { name: "grapplehook_speed_fly", base: "1800 qu/s", port: "Bal(g_balance_grapplehook_speed_fly,1800)", match: true }
      - { name: "grapplehook_health", base: "50 hp", port: "Bal(g_balance_grapplehook_health,50)", match: true }
      - { name: "chain bbox", base: "'-3 -3 -3'..'3 3 3'", port: "same", match: true }
      - { name: "movetype", base: "FLY (TOSS if grapplehook_gravity)", port: "Fly only", match: false }
      - { name: "muzzle flash", base: "W_MuzzleFlash", port: "none", match: false }
    status:
      logic: partial
      values: faithful
      timing: faithful
      presentation: partial
      audio: faithful
      liveness: live
    intended_divergence: false
    divergence_rationale: null
    gaps:
      - "No muzzle flash on hook fire (Base calls W_MuzzleFlash here; port emits none)."
      - "No dedicated hook entity stream (GrapplingHookSend / ENT_CLIENT_HOOK with hook_start/hook_end). The chain HEAD IS networked via the generic projectile feed (ServerNet.Classify -> Projectile: FL_PROJECTILE/Item + Owner + MoveType.Fly) and renders as a red Hookbomb spike — but with no rope-endpoint stream there is no rope line (see weapon-hook.presentation.rope_line)."
      - "grapplehook_gravity TOSS-mode movetype not honoured (always FLY)."
      - "No WarpZone refsys tracking (WarpZone_RefSys_SpawnSameRefSys)."
    verification:
      - { kind: code, ref: "Hook.FireGrapplingHook vs server/hook.qc FireGrapplingHook", result: pass }
      - { kind: code, ref: "ServerNet.Classify: grapplinghook (Item+Owner+Fly) -> Projectile; ProjectileCatalog grapplinghook/hook -> Hookbomb", result: pass }
      - { kind: code, ref: "Base FireGrapplingHook does W_MuzzleFlash + set_movetype(gravity?TOSS:FLY) + Net_LinkEntity(GrapplingHookSend); none ported", result: fail }
    confidence: high
    notes: "Wave 1/2 swapped the hardcoded speed_fly/health to Bal() cvar reads (values still match). Logic/presentation gaps unchanged: chain head IS networked + rendered (Hookbomb spike) while in flight; missing the dedicated rope-endpoint stream, muzzle flash, and gravity-TOSS movetype. Plays hook_fire sound; shootable HP set; EditProjectile mutator hook called."

  - id: weapon-hook.primary.latch
    name: "GrapplingHookTouch / GrapplingHook_Stop: latch onto what it hits"
    layer: [authority, presentation]
    base_refs:
      - "server/hook.qc:GrapplingHookTouch"
      - "server/hook.qc:GrapplingHook_Stop"
    port_refs:
      - "src/XonoticGodot.Common/Gameplay/Weapons/Hook.cs:GrapplingHookTouch"
    constants: []
    status:
      logic: faithful
      values: na
      timing: faithful
      presentation: partial
      audio: faithful
      liveness: live
    intended_divergence: false
    divergence_rationale: null
    gaps:
      - "Latched hook becomes INVISIBLE: GrapplingHook_Stop sets MoveType.None, which is NOT a projectile movetype, so ServerNet.Classify (line 1942) no longer classifies it as Projectile; with no model it falls to Generic and is dropped from the render. The rendered spike disappears on latch. Base keeps the latched hook drawn via CSQC."
    verification:
      - { kind: code, ref: "GrapplingHookTouch vs QC touch+Stop+SetMovetypeFollow", result: pass }
      - { kind: code, ref: "EFFECT_HOOK_IMPACT now emitted: EffectEmitter.Emit(\"HOOK_IMPACT\", origin); HOOK_IMPACT registered EffectsList.cs:113", result: pass }
      - { kind: code, ref: "ServerNet.Classify: MoveType.None model-less entity -> Generic (no alias model) -> dropped", result: fail }
    confidence: high
    notes: "FIX (Wave 1/2): now emits the EFFECT_HOOK_IMPACT particle on latch (was sound-only) AND ports SetMovetypeFollow — records the hit entity as hook.Aiment, clamps the follow-offset into the player bbox, and tracks a moving aiment each think (GrapplingHookThink re-origins the chain to follow.Origin+ViewOfs). This is the precondition vampirehook drains off. STILL OPEN: the latched head is invisible (MoveType.None drops it from the snapshot). Presentation upgraded missing->partial (impact effect + follow tracking land; head visibility still broken). Logic upgraded partial->faithful."

  - id: weapon-hook.primary.reel_pull
    name: "GrapplingHookThink: reel the firer toward the latch"
    layer: [authority]
    base_refs:
      - "server/hook.qc:GrapplingHookThink"
    port_refs:
      - "src/XonoticGodot.Common/Gameplay/Weapons/Hook.cs:GrapplingHookThink"
    constants:
      - { name: "g_grappling_hook_tarzan", base: "2 (rubber-band, can pull players)", port: "Bal(g_grappling_hook_tarzan,2) -> tarzan branch", match: true }
      - { name: "grapplehook_speed_pull", base: "2000 qu/s", port: "Bal(...,2000)", match: true }
      - { name: "grapplehook_force_rubber", base: "2000", port: "Bal(...,2000)", match: true }
      - { name: "grapplehook_stretch", base: "50 qu", port: "Bal(...,50)", match: true }
      - { name: "grapplehook_airfriction", base: "0.2", port: "Bal(...,0.2)", match: true }
      - { name: "grapplehook_force_rubber_overstretch", base: "1000", port: "Bal(...,1000)", match: true }
      - { name: "grapplehook_length_min", base: "50 qu", port: "Bal(...,50)", match: true }
      - { name: "reel myorg origin", base: "origin+view_ofs+hook_shotorigin('8 8 -12')", port: "origin+ViewOfs (no gun offset)", match: false }
    status:
      logic: faithful
      values: faithful
      timing: faithful
      presentation: na
      audio: na
      liveness: live
    intended_divergence: false
    divergence_rationale: null
    gaps:
      - "Reel origin omits the gun shot-origin offset (hook_shotorigin), so latch/length geometry is slightly offset from Base."
      - "Base applies the rubber-band velocity to pull_entity only `if (!frozen_pulling && !(aiment.flags & FL_PROJECTILE))`; the port always writes actor.Velocity. Differs only when the aiment is a projectile/frozen (edge case)."
      - "Mutator velocity_multiplier / pull_entity rewrite from GrappleHookThink not honoured (port's GrappleHookThinkArgs carries no such args); pull_frozen (g_balance_grapplehook_pull_frozen) and the frozen-pull early-remove are not ported; no WarpZone refsys transforms."
      - "pusher/pushltime/istypefrag frag attribution on the pulled aiment not set (out of scope, shoot-down credit area)."
    verification:
      - { kind: code, ref: "GrapplingHookThink tarzan branch (newlength/overstretch/spring/airfriction + tarzan>=2 aiment pull) vs server/hook.qc tarzan branch", result: pass }
      - { kind: value, ref: "balance-xonotic.cfg:253 g_grappling_hook_tarzan 2; speed_pull/force_rubber/stretch/airfriction all match Base", result: pass }
    confidence: high
    notes: "MAJOR FIX (Wave 1/2): the elastic tarzan rubber-band reel is now implemented (hook_length shorten toward minlength, overstretch spring, airfriction, spring-to-latch) AND the tarzan>=2 half-impulse pull of a hooked player/nade — a near-bit transcription of Base's tarzan branch; the old straight tarzan-0 reel is kept as the else branch. All grapplehook_* and tarzan constants now read from the balance cfg (defaults match). HookState.Releasing wired. Residual divergences are minor (gun-offset, projectile-aiment velocity guard, mutator velocity_multiplier, pull_frozen, warpzone). Fires the GrappleHookThink mutator hook (feeds vampirehook)."

  - id: weapon-hook.primary.remove_reset
    name: "RemoveHook / RemoveGrapplingHooks / reset + shoot-down"
    layer: [authority]
    base_refs:
      - "server/hook.qc:RemoveHook"
      - "server/hook.qc:RemoveGrapplingHooks"
      - "server/hook.qc:GrapplingHook_Damage"
      - "common/weapons/weapon/hook.qc:Hook.wr_resetplayer"
    port_refs:
      - "src/XonoticGodot.Common/Gameplay/Weapons/Hook.cs:RemoveHook"
    constants: []
    status:
      logic: partial
      values: na
      timing: faithful
      presentation: na
      audio: na
      liveness: live
    intended_divergence: false
    divergence_rationale: null
    gaps:
      - "No central RemoveGrapplingHooks reset on freezetag-freeze / teleport / vehicle-enter / wr_resetplayer; the hook is cleared only via the weapon's own state machine."
      - "Shoot-down credits no pusher/pushltime/istypefrag frag attribution (QC GrapplingHook_Damage sets realowner.pusher=attacker)."
      - "RemoveHook does not explicitly restore the owner from MOVETYPE_FLY to MOVETYPE_WALK (Base does); deferred to the movement system. (The tarzan branch now does flip FLY->WALK each think while reeling, partially covering this for the elastic path.)"
    verification:
      - { kind: code, ref: "Hook.RemoveHook + ProjectileDamage callback vs server/hook.qc RemoveHook/GrapplingHook_Damage", result: pass }
    confidence: medium
    notes: "Largely unchanged by Wave 1/2 (the new top-of-think guard now drops a hook whose followed entity vanished / is a non-nade projectile, closer to Base's LostMovetypeFollow check). Owner movetype restoration still deferred to the movement system. Live (reached via the weapon state machine each tick)."

  - id: weapon-hook.secondary.gravity_bomb
    name: "Secondary: lob the gravity bomb (W_Hook_Attack2)"
    layer: [authority, presentation]
    base_refs:
      - "common/weapons/weapon/hook.qc:W_Hook_Attack2"
    port_refs:
      - "src/XonoticGodot.Common/Gameplay/Weapons/Hook.cs:AttackGravityBomb"
    constants:
      - { name: "sec.refire", base: "3 s", port: "3 s (via refire gate)", match: true }
      - { name: "sec.gravity", base: "5", port: "5", match: true }
      - { name: "sec.speed", base: "0 qu/s", port: "0", match: true }
      - { name: "sec.health", base: "15 hp", port: "15", match: true }
      - { name: "sec.lifetime", base: "5 s", port: "5 s", match: true }
      - { name: "setup recoil", base: "4", port: "4", match: true }
      - { name: "newton_style velocity add", base: "if g_projectiles_newton_style", port: "not modelled", match: false }
    status:
      logic: faithful
      values: faithful
      timing: faithful
      presentation: faithful
      audio: faithful
      liveness: live
    intended_divergence: true
    divergence_rationale: "Base gates the secondary via weapon_prepareattack on sec.refire; the ammo-free port bomb is routed through Weapon.RefireFor(Secondary)=sec.refire so holding ATK2 can't spawn one bomb per tick. Net 3 s cadence matches Base."
    gaps:
      - "No Newton-style owner-velocity add to the bomb (g_projectiles_newton_style)."
      - "No bot_dodge/bot_dodgerating, missile_flags (MIF_SPLASH|MIF_ARC), or damagedbycontents on the bomb (bot AI + lava/contents-detonation; out of scope here)."
    verification:
      - { kind: code, ref: "AttackGravityBomb vs W_Hook_Attack2", result: pass }
      - { kind: code, ref: "hookbomb (Item+Owner+Toss) -> ServerNet Projectile; ProjectileCatalog -> Hookbomb render; HOOK_MUZZLEFLASH emitted", result: pass }
    confidence: high
    notes: "Unchanged by Wave 1/2. Toss launch, lifetime+touch+shoot-down detonation, shootable HP all faithful; bomb networked + rendered as a Hookbomb projectile; hookbomb_fire sound + HOOK_MUZZLEFLASH effect emitted."

  - id: weapon-hook.secondary.blast_curve
    name: "Secondary blast: duration-spread power-curve pull (W_Hook_ExplodeThink)"
    layer: [authority]
    base_refs:
      - "common/weapons/weapon/hook.qc:W_Hook_Explode2"
      - "common/weapons/weapon/hook.qc:W_Hook_ExplodeThink"
    port_refs:
      - "src/XonoticGodot.Common/Gameplay/Weapons/Hook.cs:StartBlast"
      - "src/XonoticGodot.Common/Gameplay/Weapons/Hook.cs:ExplodeThink"
    constants:
      - { name: "sec.damage", base: "25", port: "25", match: true }
      - { name: "sec.edgedamage", base: "5", port: "5", match: true }
      - { name: "sec.radius", base: "500 qu", port: "500", match: true }
      - { name: "sec.force", base: "-2000 (pull inward)", port: "-2000", match: true }
      - { name: "sec.duration", base: "1.5 s", port: "1.5 s", match: true }
      - { name: "sec.power", base: "3 (falloff exponent)", port: "3", match: true }
      - { name: "explode tick", base: "0.05 s", port: "0.05 s", match: true }
    status:
      logic: faithful
      values: faithful
      timing: faithful
      presentation: partial
      audio: faithful
      liveness: live
    intended_divergence: false
    divergence_rationale: null
    gaps:
      - "HITTYPE_SPAM flag not OR'd into the deathtype after the first blast tick (cosmetic deathtype/obituary detail)."
      - "EF_NODRAW is not set on the bomb at blast start (Base hides the model while the pull ticks); minor visual."
    verification:
      - { kind: code, ref: "ExplodeThink power-curve delta vs W_Hook_ExplodeThink", result: pass }
    confidence: high
    notes: "Unchanged by Wave 1/2. The centrepiece: f = prev_remaining - clamp(1-dt/duration,0,1)^power applied each 0.05s tick so the cumulative blast == one full RadiusDamage, spread over duration. EFFECT_HOOK_EXPLODE emitted at blast start."

  - id: weapon-hook.offhand.mutator
    name: "Grappling Hook offhand mutator (g_grappling_hook)"
    layer: [authority]
    base_refs:
      - "common/mutators/mutator/hook/sv_hook.qc:REGISTER_MUTATOR(hook)"
      - "common/mutators/mutator/hook/sv_hook.qc:OFFHAND_HOOK.offhand_think"
    port_refs:
      - "src/XonoticGodot.Common/Gameplay/Mutators/HookMutator.cs"
    constants:
      - { name: "g_grappling_hook", base: "0 (string; instahook=1)", port: "ExprEvaluate(g_grappling_hook)", match: true }
      - { name: "g_grappling_hook_useammo", base: "0", port: "read", match: true }
      - { name: "fuel grant (useammo)", base: "FuelRegen + g_balance_fuel_rotstable", port: "same", match: true }
    status:
      logic: faithful
      values: faithful
      timing: faithful
      presentation: na
      audio: na
      liveness: dead
    intended_divergence: false
    divergence_rationale: null
    gaps:
      - "DEAD on the live path: the +hook offhand-fire button is never delivered to the server. InputButtons has no hook bit and Entity.OffhandFirePressed is never assigned anywhere in src/ or game/ (verified: only READS — HookMutator st.ButtonAttack=player.OffhandFirePressed, OffhandBlaster/Nades reads — NO `OffhandFirePressed =`), so OnPlayerPreThink always sees the button released and the offhand hook can never fire (affects instahook ruleset + grappling-hook campaign specials). Same missing-input-wire as nade-throw / offhand-blaster (TODO.md T11)."
      - "No useammo ammo_factor toggle on the Hook weapon (handled via fuel top-up instead) — behaviourally equivalent but only matters once the button is wired."
    verification:
      - { kind: code, ref: "HookMutator OnPlayerPreThink; PlayerPreThink.Call live in GameWorld", result: pass }
      - { kind: code, ref: "grep `OffhandFirePressed =` over src/ + game/ -> no assignment; InputButtons has no hook bit", result: fail }
    confidence: high
    notes: "STILL OPEN (no change in Wave 1/2). Offhand assign + filter-item suppress + start-fuel grant are faithful and PlayerPreThink fires live, but the input button is the missing link (the mutator body is otherwise reachable code)."

  - id: weapon-hook.mutator.vampirehook
    name: "Vampire Hook (drain HP while hooked to an enemy)"
    layer: [authority]
    base_refs:
      - "common/mutators/mutator/vampirehook/sv_vampirehook.qc:MUTATOR_HOOKFUNCTION(vh, GrappleHookThink)"
    port_refs:
      - "src/XonoticGodot.Common/Gameplay/Mutators/VampireHookMutator.cs:OnGrappleHookThink"
    constants:
      - { name: "g_vampirehook_damagerate", base: "0.1 s", port: "0.1 s", match: true }
      - { name: "g_vampirehook_damage / health_steal / teamheal", base: "cvars", port: "read", match: true }
    status:
      logic: faithful
      values: faithful
      timing: faithful
      presentation: na
      audio: na
      liveness: live
    intended_divergence: false
    divergence_rationale: null
    gaps:
      - "hitsound_damage_dealt HUD/stats accumulator on the drain is omitted (cosmetic; port doesn't carry it)."
      - "Drains only on a DIRECT hook hit onto an enemy player; with tarzan reel-to-victim a hooked player can also be pulled, which now works, but the drain still requires the chain's aiment to be that player (matches Base, since Base also keys on thehook.aiment)."
    verification:
      - { kind: code, ref: "VampireHookMutator.OnGrappleHookThink vs sv_vampirehook.qc (last_dmg gate, team/enemy/frozen/alive guards, Damage(WEP_HOOK)+Heal)", result: pass }
      - { kind: code, ref: "Hook.GrapplingHookTouch now sets self.Aiment=other (SetMovetypeFollow port); thehook.RealOwner=>Owner=firing actor; GrappleHookThink.Call invoked each think", result: pass }
    confidence: high
    notes: "FIX (Wave 1/2): liveness dead->live. The latch now records the hit entity as hook.Aiment (SetMovetypeFollow port in GrapplingHookTouch), so a direct hook hit on an enemy player satisfies the IS_PLAYER(aiment) guard and the per-0.1s drain/heal actually fires on the live match path (when g_vampirehook is set). Previously aiment was always null and every guard short-circuited. The GrappleHookThink mutator hook is dispatched each think by Hook.GrapplingHookThink. Body is a faithful transcription of the QC guards + Damage/Heal."

  - id: weapon-hook.presentation.rope_line
    name: "CSQC rope line rendering (Draw_GrapplingHook)"
    layer: [presentation]
    base_refs:
      - "common/weapons/weapon/hook.qc:Draw_GrapplingHook"
      - "common/weapons/weapon/hook.qc:NET_HANDLE(ENT_CLIENT_HOOK)"
      - "server/hook.qc:GrapplingHookSend"
    port_refs: []
    constants:
      - { name: "rope thickness", base: "8", port: "n/a", match: false }
      - { name: "team textures", base: "particles/hook_red/blue/yellow/pink/white", port: "n/a", match: false }
      - { name: "cl_grapplehook_alpha", base: "1", port: "n/a", match: false }
    status:
      logic: missing
      values: missing
      timing: missing
      presentation: missing
      audio: na
      liveness: na
    intended_divergence: false
    divergence_rationale: null
    gaps:
      - "No hook ROPE line is drawn. There is no GrapplingHookSend/ENT_CLIENT_HOOK stream (with hook_start/hook_end), no Draw_CylindricLine, and no team-coloured rope texture/WarpZone-traced segment in game/. The chain HEAD is networked + rendered as a Hookbomb spike while in flight (and disappears once latched), but the connecting rope between the firer's gun and the latch point is entirely absent."
    verification:
      - { kind: code, ref: "no Draw_GrapplingHook/CylindricLine/ENT_CLIENT_HOOK in game/; chain head only classified as ProjectileType.Hookbomb (a spike), no rope", result: fail }
    confidence: high
    notes: "STILL OPEN (no change in Wave 1/2). Distinct from the chain-head spike (rendered while flying). The rope line itself + the dedicated hook entity stream are unimplemented presentation."
