unit: weapon-vaporizer
category: weapon
base_ref_rev: "v0.8.6-1779-g863cd3e84"
last_audited: "2026-06-22"
summary:
  feature_count: 12
  worst_gaps:
    - "Achievements still absent: no YODA (airshot), IMPRESSIVE (every-2nd headshot), or ELECTROBITCH (RM-laser airshot) announce; vaporizer_lasthit gate not ported."
    - "Bots cannot use the Vaporizer: wr_aim (bot_aim primary/secondary toggle) is still not ported on this class."
    - "Rocket-Minsta laser is fan-only: the hold-to-rapid ladder (mode-1, g_rm_laser_rapid/rapid_delay/rapid_refire) is absent, and the bolts skip W_CalculateProjectileVelocity (shooter-velocity inheritance) + the CSQCProjectile visual."
  verifier_notes: "Re-audit 2026-06-22 AFTER Wave-1/2 (branch claude/parity-port-waves, commit ec788f7 rewrote Vaporizer.cs). 7 baseline gaps genuinely CLOSED, all verified LIVE against the real match path (EffectEmitter.Sink = EffectNetSink wired at ServerNet.cs:287 -> beam/muzzle/impact networked to clients; WeaponFireDriver.cs:157 calls WrThink(Secondary)). FIXES: (1) primary accuracy 'fired' credit now passed (SetupShot wep:this,maxDamage); (2) fire-sound 0.8 volume scale applied; (3) rail BEAM visual + muzzle flash now emitted (VAPORIZER_BEAM/_HIT + VORTEX_MUZZLEFLASH); (4) impact effect + neximpact sound now emitted at endpoint (VORTEX_IMPACT); (5) RM-explosion constants raised to Base 70/38/140/400 + deathtype now Devastator|SPLASH (devTag) + sky/noimpact guard added; (6) RM-laser constants raised to Base (count 3/dmg 80/radius 150/force 400/speed 6000/lifetime 30/spread 0.05) + deathtype now Electro + ELECTRO muzzle + crylink_fire sound; (7) secondary blaster refire now applies W_WeaponRateFactor; wr_checkammo2 added. NO regressions found. STILL OPEN: achievements, wr_aim, RM-laser rapid ladder + shooter-velocity + CSQC visual, RM-explosion presentation effect, instagib hit-confirm/downgrade-centerprint, Instagib-hardcoded-true default loadout. Confirmed Base values vs mutators.cfg + vaporizer.qc directly."
features:
  - id: weapon-vaporizer.primary.rail_beam
    name: "Primary rail beam (piercing hitscan + knockback + falloff)"
    layer: [authority, shared]
    base_refs:
      - "common/weapons/weapon/vaporizer.qc:W_Vaporizer_Attack"
      - "server/weapons/tracing.qc:FireRailgunBullet"
    port_refs:
      - "src/XonoticGodot.Common/Gameplay/Weapons/Vaporizer.cs:Attack"
      - "src/XonoticGodot.Common/Gameplay/Weapons/WeaponFiring.cs:FireRailgunBullet"
    constants:
      - { name: "primary_damage", base: "150 (<=0 => 10000 instakill)", port: "150 (<=0 => 10000)", match: true }
      - { name: "primary_force", base: "800", port: "800", match: true }
      - { name: "primary_refire", base: "1", port: "1", match: true }
      - { name: "primary_animtime", base: "0.3", port: "0.3", match: true }
      - { name: "damagefalloff_mindist/maxdist/halflife/forcehalflife", base: "0/0/0/0", port: "not passed (defaults 0)", match: true }
      - { name: "max_shot_distance", base: "32768", port: "32768 (MaxShotDistance)", match: true }
      - { name: "recoil", base: "0", port: "0 (recoil arg omitted)", match: true }
    status:
      logic: faithful
      values: faithful
      timing: faithful
      presentation: na
      audio: na
      liveness: live
    intended_divergence: false
    divergence_rationale: null
    gaps: []
    verification:
      - { kind: code, ref: "Vaporizer.cs:125 SetupShot(actor, forward, wep: this, maxDamage: damage) — the accuracy 'fired' denominator is now credited (baseline gap CLOSED); WeaponFiring.cs:FireRailgunBullet pierce/falloff/antilag/warpzone vs vaporizer.qc:131 + tracing.qc:347", result: pass }
    confidence: high
    notes: "FIX vs baseline: Attack now passes wep/maxDamage to SetupShot, crediting the Vaporizer's accuracy 'fired' denominator (was the sole gap on this row). FireRailgunBullet remains a thorough headless port (pierce loop, restore solidity, exp falloff, antilag, warpzone sweep, accuracy min(bdamage,totaldmg)). Falloff all-0 in Base so harmless that the args are omitted."

  - id: weapon-vaporizer.primary.headshot
    name: "Headshot head-AABB test + ANNCE_HEADSHOT"
    layer: [shared]
    base_refs:
      - "server/weapons/tracing.qc:Headshot"
      - "server/weapons/tracing.qc:FireRailgunBullet"
    port_refs:
      - "src/XonoticGodot.Common/Gameplay/Weapons/WeaponFiring.cs:Headshot"
      - "src/XonoticGodot.Common/Gameplay/Weapons/WeaponFiring.cs:TraceHitsBox"
    constants:
      - { name: "head_box_horizontal", base: "0.6 * body box", port: "0.6 * mins/maxs", match: true }
      - { name: "head_box_vertical", base: "1.3*view_ofs_z - 0.3*maxs_z .. maxs_z", port: "same", match: true }
    status:
      logic: faithful
      values: faithful
      timing: na
      presentation: na
      audio: faithful
      liveness: live
    intended_divergence: false
    divergence_rationale: null
    gaps: []
    status_note: null
    verification:
      - { kind: code, ref: "WeaponFiring.cs:Headshot/TraceHitsBox vs tracing.qc Headshot + util.qc trace_hits_box", result: pass }
      - { kind: code, ref: "Vaporizer.cs:131 FireRailgunBullet(headshotNotify:true) -> WeaponFiring.cs:398 Headshot test -> NotificationSystem.Announce(actor,'HEADSHOT'); 'HEADSHOT' ANNCE w/ sound 'headshot' (NotificationsList.cs); Send/Annce dispatches sub.Sound to the live Sink", result: pass }
    confidence: high
    notes: "Unchanged by the waves and still faithful+live. The HEADSHOT announce is wired end-to-end (Vortex passes headshotNotify:false; Vaporizer true). Living/unfrozen/damageable-player gate matches Base."

  - id: weapon-vaporizer.primary.fire_sound
    name: "Primary fire sound (minstanexfire)"
    layer: [presentation]
    base_refs:
      - "common/weapons/weapon/vaporizer.qc:W_Vaporizer_Attack"
    port_refs:
      - "src/XonoticGodot.Common/Gameplay/Weapons/Vaporizer.cs:Attack"
    constants:
      - { name: "fire_sound", base: "weapons/minstanexfire.wav", port: "weapons/minstanexfire.wav", match: true }
      - { name: "fire_volume", base: "VOL_BASE * 0.8", port: "SoundLevels.VolBase * 0.8f", match: true }
    status:
      logic: faithful
      values: faithful
      timing: faithful
      presentation: na
      audio: faithful
      liveness: live
    intended_divergence: false
    divergence_rationale: null
    gaps: []
    verification:
      - { kind: code, ref: "Vaporizer.cs:134 Api.Sound.Play(actor, CH_WEAPON, 'weapons/minstanexfire.wav', VolBase * 0.8f) vs vaporizer.qc:139 sound(.., SND_VAPORIZER_FIRE, VOL_BASE*0.8, ATTEN_NORM)", result: pass }
    confidence: high
    notes: "FIX vs baseline: the dropped VOL_BASE*0.8 volume scale is now applied directly in Attack (the sole gap on this row). The sound is played separately (bypassing any strength-fire sound) exactly as Base intends."

  - id: weapon-vaporizer.primary.beam_particle
    name: "Rail beam visual (cylindric beam) + muzzle flash"
    layer: [presentation]
    base_refs:
      - "common/weapons/weapon/vaporizer.qc:SendCSQCVaporizerBeamParticle"
      - "common/weapons/weapon/vaporizer.qc:VaporizerBeam_Draw"
      - "common/weapons/weapon/vaporizer.qc:W_Vaporizer_Attack (W_MuzzleFlash thiswep)"
    port_refs:
      - "src/XonoticGodot.Common/Gameplay/Weapons/Vaporizer.cs:Attack (EffectEmitter.Emit VAPORIZER_BEAM/_HIT + VORTEX_MUZZLEFLASH)"
      - "src/XonoticGodot.Common/Gameplay/Effects/EffectsList.cs:91 (VAPORIZER_BEAM/_HIT trails)"
    constants:
      - { name: "beam_tex", base: "particles/gauntletbeam (hit) / particles/lgbeam", port: "VAPORIZER_BEAM_HIT (TE_TEI_G3_HIT) / VAPORIZER_BEAM (TE_TEI_G3)", match: false }
      - { name: "cl_vaporizerbeam_lifetime", base: "0.8", port: "engine trail default", match: false }
      - { name: "cl_vaporizerbeam_teamcolor", base: "1", port: "none (no team-color tint)", match: false }
      - { name: "cl_vaporizerbeam_colorboost", base: "0.7", port: "none", match: false }
      - { name: "muzzle_effect", base: "EFFECT_VORTEX_MUZZLEFLASH (W_MuzzleFlash thiswep — VAPORIZER's own)", port: "VORTEX_MUZZLEFLASH (reused)", match: true }
    status:
      logic: faithful
      values: partial
      timing: faithful
      presentation: partial
      audio: na
      liveness: live
    intended_divergence: false
    divergence_rationale: null
    gaps:
      - "Beam is a generic networked trail effect (TE_TEI_G3 / _HIT), not the Base cylindric gauntletbeam/lgbeam beam; no team-color (cl_vaporizerbeam_teamcolor) or colorboost tint, no 0.8 beam lifetime cvar."
    verification:
      - { kind: code, ref: "Vaporizer.cs:146-147 EffectEmitter.Emit(VAPORIZER_BEAM/_HIT, origin->endpos, 0) + Emit(VORTEX_MUZZLEFLASH); EffectsList.cs:91 registers the trails; EffectEmitter.Sink = EffectNetSink (ServerNet.cs:287) networks them to clients", result: pass }
    confidence: high
    notes: "FIX vs baseline (was fully missing): a rail beam IS now drawn on fire and a muzzle flash IS emitted, server-side and networked to all clients. Residual: a plain swept trail rather than the bespoke team-coloured cylindric VaporizerBeam, and the muzzle reuses the VORTEX muzzleflash effect (Base W_MuzzleFlash uses the VAPORIZER weapon's own muzzle, visually near-identical). The CSQC team-color/colorboost path is not modeled."

  - id: weapon-vaporizer.primary.impact_effect
    name: "Beam impact effect + sound (VORTEX_IMPACT / neximpact)"
    layer: [presentation]
    base_refs:
      - "common/weapons/weapon/vaporizer.qc:wr_impacteffect"
    port_refs:
      - "src/XonoticGodot.Common/Gameplay/Weapons/Vaporizer.cs:Attack (EffectEmitter.Emit VORTEX_IMPACT + WeaponSplash.ImpactSoundAt neximpact)"
    constants:
      - { name: "impact_effect", base: "EFFECT_VORTEX_IMPACT", port: "VORTEX_IMPACT (nex_impact)", match: true }
      - { name: "impact_sound", base: "weapons/neximpact.wav (SND_VAPORIZER_IMPACT) VOL_BASE ATTN_NORM", port: "weapons/neximpact.wav VolBase AttenNorm", match: true }
    status:
      logic: faithful
      values: faithful
      timing: na
      presentation: faithful
      audio: faithful
      liveness: live
    intended_divergence: false
    divergence_rationale: null
    gaps:
      - "Impact is emitted server-side at the railgun world-trace endpoint rather than via the CSQC wr_impacteffect (w_org + w_backoff*2 offset) path — visually equivalent, minor positional nuance; w_issilent gate not modeled (port always plays the sound)."
    verification:
      - { kind: code, ref: "Vaporizer.cs:151-152 EffectEmitter.Emit(VORTEX_IMPACT, endpos) + WeaponSplash.ImpactSoundAt(endpos, 'weapons/neximpact.wav') vs vaporizer.qc:407-413 wr_impacteffect; networked via EffectNetSink (ServerNet.cs:287)", result: pass }
    confidence: high
    notes: "FIX vs baseline (was fully missing): the beam now produces an impact spark (VORTEX_IMPACT) AND the neximpact sound at the endpoint, mirroring the sister Vortex. The endpoint comes from a fresh world-only trace after the pierce so it is the wall the beam actually stopped at."

  - id: weapon-vaporizer.primary.achievements
    name: "Yoda (airshot) + Impressive (every-2nd headshot) announcer achievements"
    layer: [authority, presentation]
    base_refs:
      - "common/weapons/weapon/vaporizer.qc:W_Vaporizer_Attack (yoda/impressive_hits, vaporizer_lasthit)"
      - "common/weapons/weapon/vaporizer.qc:wr_setup/wr_resetplayer"
    port_refs: []
    constants:
      - { name: "impressive_cadence", base: "every 2nd qualifying hit (vaporizer_lasthit toggle)", port: "none", match: false }
    status:
      logic: missing
      values: missing
      timing: na
      presentation: missing
      audio: missing
      liveness: na
    intended_divergence: false
    divergence_rationale: null
    gaps:
      - "Never announces YODA (airshot on a flying enemy) or IMPRESSIVE; the every-2nd-hit vaporizer_lasthit gate (wr_setup/wr_resetplayer reset) is absent. (ELECTROBITCH airshot announce on the RM laser is likewise absent — see rm.laser_barrage.)"
    verification:
      - { kind: code, ref: "Vaporizer.cs:164 comment 'Deferred (client render): yoda/impressive achievements'; no yoda/impressive_hits/vaporizer_lasthit field; only the ANNCE strings exist in NotificationsList", result: fail }
    confidence: high
    notes: "STILL OPEN — unchanged by the waves. yoda/impressive ANNCE entries + sounds exist in the registries but no weapon emits them. The vaporizer_lasthit field / wr_setup / wr_resetplayer are not ported."

  - id: weapon-vaporizer.secondary.blaster_laser
    name: "Secondary Blaster knockback laser (plain instagib)"
    layer: [authority, shared]
    base_refs:
      - "common/weapons/weapon/vaporizer.qc:wr_think (fire&2 -> W_Blaster_Attack)"
    port_refs:
      - "src/XonoticGodot.Common/Gameplay/Weapons/Vaporizer.cs:WrThink"
      - "src/XonoticGodot.Common/Gameplay/Weapons/Vaporizer.cs:BlasterSecondary"
    constants:
      - { name: "blaster_refire", base: "0.7 (WEP_CVAR_PRI(BLASTER,refire)) * W_WeaponRateFactor", port: "0.7 * WeaponRateFactor(actor)", match: true }
      - { name: "blaster_ammo_decrement", base: "WEP_CVAR_PRI(BLASTER,ammo) if set (default 0)", port: "not decremented (harmless at default 0)", match: true }
    status:
      logic: faithful
      values: faithful
      timing: faithful
      presentation: na
      audio: na
      liveness: partial
    intended_divergence: false
    divergence_rationale: null
    gaps:
      - "Optional Blaster-ammo decrement (W_DecreaseAmmo if WEP_CVAR_PRI(BLASTER,ammo) set) is omitted — harmless at the default Blaster ammo 0."
    verification:
      - { kind: code, ref: "Vaporizer.cs:105 jump_interval = time + 0.7f * WeaponRateFactor(actor) (rate-factor gap CLOSED) + BlasterSecondary -> Blaster.FirePrimaryDirect vs vaporizer.qc:337-351; WeaponFireDriver.cs:157 calls WrThink(Secondary) when ATK2 held", result: pass }
    confidence: high
    notes: "FIX vs baseline: the secondary refire now multiplies in W_WeaponRateFactor(actor) (was hardcoded 0.7, diverging when sv_weaponrate != 1) — timing now faithful. Path reachable: driver calls WrThink(Secondary) every tick ATK2 held; manual jump_interval gate fires BlasterSecondary -> Blaster.FirePrimaryDirect (Blaster is registered). liveness kept partial because the bolt's visual/sound belongs to the Blaster weapon's own parity and the RM-laser sibling branch is the un-rapid path; the plain-instagib blaster bolt itself is live."

  - id: weapon-vaporizer.rm.primary_explosion
    name: "Rocket-Minsta: explosion at the rail endpoint"
    layer: [authority]
    base_refs:
      - "common/weapons/weapon/vaporizer.qc:W_RocketMinsta_Explosion"
    port_refs:
      - "src/XonoticGodot.Common/Gameplay/Weapons/Vaporizer.cs:RocketMinstaExplosion"
      - "src/XonoticGodot.Common/Gameplay/Weapons/WeaponSplash.cs:RadiusDamage"
    constants:
      - { name: "g_rm_damage", base: "70", port: "fallback 70", match: true }
      - { name: "g_rm_edgedamage", base: "38", port: "fallback 38", match: true }
      - { name: "g_rm_radius", base: "140", port: "fallback 140", match: true }
      - { name: "g_rm_force", base: "400", port: "fallback 400", match: true }
      - { name: "deathtype", base: "WEP_DEVASTATOR.m_id | HITTYPE_SPLASH", port: "Devastator death tag (devTag) + RadiusDamage ORs HITTYPE_SPLASH; accuracyWeapon=devastator", match: true }
      - { name: "sky/noimpact guard", base: "!(trace_dphitq3surfaceflags & (SKY|NOIMPACT))", port: "(DpHitQ3SurfaceFlags & (Sky|NoImpact)) == 0 guard applied", match: true }
    status:
      logic: faithful
      values: faithful
      timing: na
      presentation: missing
      audio: na
      liveness: unknown
    intended_divergence: false
    divergence_rationale: null
    gaps:
      - "No explosion presentation: Base's RadiusDamage emits its own splash effect path, but the port does not emit a Devastator-style explosion effect at the RM endpoint (verify whether RadiusDamage emits it centrally; if not, the visual is absent)."
      - "Liveness of g_rm in a shipped/normal match (a map or mutator that sets g_rm) not traced end-to-end."
    verification:
      - { kind: code, ref: "Vaporizer.cs:156-158 sky/noimpact guard + RocketMinstaExplosion; :175-180 RadiusDamage(g_rm_* 70/38/140/400, accuracyWeapon=devastator, deathTag=devastator) vs vaporizer.qc:108-129 + mutators.cfg g_rm_*", result: pass }
    confidence: high
    notes: "FIXES vs baseline: (1) cvar fallbacks raised from half-strength 35/15/90/200 to the correct Base 70/38/140/400; (2) blast now tagged as a Devastator death + HITTYPE_SPLASH (devTag) with accuracyWeapon=devastator, so the rocketminsta self-damage-nullify/gib hooks (keyed on devastator) and accuracy/obituary attribution match Base; (3) the sky/NOIMPACT endpoint guard from Base is now applied before exploding. Residual: explosion presentation effect + g_rm live-path reachability."

  - id: weapon-vaporizer.rm.laser_barrage
    name: "Rocket-Minsta: secondary bouncing-laser fan + rapid-fire ladder"
    layer: [authority]
    base_refs:
      - "common/weapons/weapon/vaporizer.qc:W_RocketMinsta_Attack"
      - "common/weapons/weapon/vaporizer.qc:wr_think (rapid ladder jump_interval2)"
    port_refs:
      - "src/XonoticGodot.Common/Gameplay/Weapons/Vaporizer.cs:RocketMinstaLaserBarrage"
      - "src/XonoticGodot.Common/Gameplay/Weapons/Vaporizer.cs:WrThink"
    constants:
      - { name: "g_rm_laser_count", base: "3", port: "fallback 3", match: true }
      - { name: "g_rm_laser_damage", base: "80", port: "fallback 80", match: true }
      - { name: "g_rm_laser_radius", base: "150", port: "fallback 150", match: true }
      - { name: "g_rm_laser_force", base: "400", port: "fallback 400", match: true }
      - { name: "g_rm_laser_speed", base: "6000", port: "fallback 6000", match: true }
      - { name: "g_rm_laser_lifetime", base: "30", port: "fallback 30", match: true }
      - { name: "g_rm_laser_refire", base: "0.7", port: "fallback 0.7", match: true }
      - { name: "g_rm_laser_spread/zspread", base: "0.05 / 0", port: "fallback 0.05 / 0", match: true }
      - { name: "rapid ladder", base: "g_rm_laser_rapid 1 / rapid_delay 0.6 / rapid_refire 0.35 / rapid_animtime 0.3", port: "not modeled", match: false }
      - { name: "laser_deathtype", base: "WEP_ELECTRO.m_id (proj.projectiledeathtype)", port: "electro.RegistryId (fallback Vaporizer)", match: true }
      - { name: "fire_sound (mode-0 fan)", base: "SND_CRYLINK_FIRE", port: "weapons/crylink_fire.wav", match: true }
      - { name: "muzzle", base: "W_MuzzleFlash(WEP_ELECTRO)", port: "ELECTRO_MUZZLEFLASH", match: true }
      - { name: "shooter-velocity inherit", base: "W_CalculateProjectileVelocity(actor, actor.velocity, vel, true)", port: "not applied", match: false }
      - { name: "csqc_projectile", base: "PROJECTILE_ROCKETMINSTA_LASER", port: "none (no per-bolt visual)", match: false }
    status:
      logic: partial
      values: faithful
      timing: partial
      presentation: partial
      audio: faithful
      liveness: unknown
    intended_divergence: false
    divergence_rationale: null
    gaps:
      - "Hold-to-rapid acceleration (jump_interval2 / g_rm_laser_rapid / rapid_delay / rapid_refire) and the mode-1 single rapid bolt are absent — Vaporizer.cs always fires the mode-0 fan (no hagar_load/jump_interval2 state)."
      - "Bolts skip W_CalculateProjectileVelocity (shooter-velocity inheritance) — port sets velocity = (shotdir + lateral*spread)*speed only."
      - "No CSQCProjectile(PROJECTILE_ROCKETMINSTA_LASER) per-bolt visual; no weapon_thinkf animtime."
      - "ELECTROBITCH airshot announce (W_RocketMinsta_Laser_Explode notif) not emitted on a flying-enemy laser kill."
    verification:
      - { kind: code, ref: "Vaporizer.cs:195-257 RocketMinstaLaserBarrage (count 3/dmg 80/radius 150/force 400/speed 6000/lifetime 30/spread 0.05 + electro deathtype + ELECTRO_MUZZLEFLASH + crylink_fire) vs vaporizer.qc:222-276 + mutators.cfg:g_rm_laser_*; WrThink only fires mode-0, never mode-1 rapid", result: pass }
    confidence: high
    notes: "FIXES vs baseline: cvar fallbacks raised to correct Base values (was count 1/dmg 25/radius 80/force 300/speed 5000/lifetime 0.3/spread 0); bolt deathtype now WEP_ELECTRO (was Vaporizer — fixes kill feed, accuracy weapon, electro-keyed mutator); muzzle now ELECTRO_MUZZLEFLASH; mode-0 fan sound now crylink_fire (was electro_fire2). Bolt physics (BounceMissile, per-bolt EditProjectile hook, touch+think splash, lateral spread, /count damage&force split) remain structurally faithful. STILL OPEN: the rapid ladder, shooter-velocity inheritance, the CSQC bolt visual, and the ELECTROBITCH announce."

  - id: weapon-vaporizer.ammo.consumption
    name: "Ammo consumption + checkammo + reload"
    layer: [authority]
    base_refs:
      - "common/weapons/weapon/vaporizer.qc:W_Vaporizer_Attack (W_DecreaseAmmo)"
      - "common/weapons/weapon/vaporizer.qc:wr_checkammo1/wr_checkammo2/wr_reload"
    port_refs:
      - "src/XonoticGodot.Common/Gameplay/Weapons/Vaporizer.cs:Attack"
      - "src/XonoticGodot.Common/Gameplay/Weapons/Vaporizer.cs:CheckAmmoPrimary"
      - "src/XonoticGodot.Common/Gameplay/Weapons/Vaporizer.cs:CheckAmmoSecondary"
    constants:
      - { name: "primary_ammo (non-instagib)", base: "10", port: "10 (Instagib hardcoded true on the class default)", match: false }
      - { name: "instagib_per_shot", base: "1 (g_instagib ? 1 : ammo)", port: "1 (InstagibActive re-reads g_instagib in Attack)", match: true }
      - { name: "reload_ammo / reload_time", base: "0 / 0", port: "not ported (wr_reload absent — latent at default 0)", match: true }
    status:
      logic: partial
      values: partial
      timing: na
      presentation: na
      audio: na
      liveness: partial
    intended_divergence: false
    divergence_rationale: null
    gaps:
      - "Instagib flag is hardcoded true on the class default; in non-instagib play the default loadout/CheckAmmoPrimary treat it as instagib (1 cell). Attack/CheckAmmoPrimary DO re-read g_instagib via InstagibActive for the actual decrement/check, so under the mutator it is correct — but the bare class default is wrong."
      - "wr_reload + the wr_think forced-reload branch are not ported (latent: reload_ammo/reload_time default 0)."
      - "wr_aim (bot aim toggle primary/secondary) not ported on this class — bots cannot aim/fire the Vaporizer."
    verification:
      - { kind: code, ref: "Vaporizer.cs:162 TakeResource(InstagibActive?1:Primary.Ammo); :269 CheckAmmoPrimary; :274 CheckAmmoSecondary (wr_checkammo2 NOW ported) vs vaporizer.qc:354/362-392", result: pass }
    confidence: high
    notes: "FIX vs baseline: wr_checkammo2 (secondary blaster ammo: free at default Blaster ammo 0, else needs WEP_CVAR_PRI(BLASTER,ammo)) is now ported as CheckAmmoSecondary. STILL OPEN: wr_aim (no bot support), wr_reload, and the Instagib-hardcoded-true class default. reload_ammo/reload_time default 0 so the missing reload path is latent. Non-instagib equip-path liveness unverified."

  - id: weapon-vaporizer.instagib.integration
    name: "InstaGib integration (start loadout, armor-as-lives, gib, blaster nullify, bleed-out)"
    layer: [authority]
    base_refs:
      - "common/mutators/mutator/instagib/sv_instagib.qc:Damage_Calculate"
      - "common/mutators/mutator/instagib/sv_instagib.qc:PlayerDies"
      - "common/mutators/mutator/instagib/sv_instagib.qc:SetStartItems"
      - "common/mutators/mutator/instagib/sv_instagib.qc:instagib_ammocheck"
    port_refs:
      - "src/XonoticGodot.Common/Gameplay/Mutators/InstagibMutator.cs"
    constants:
      - { name: "g_instagib_ammo_start", base: "10", port: "10", match: true }
      - { name: "g_instagib_extralives", base: "1", port: "1", match: true }
      - { name: "start_health/armor", base: "100 / 0", port: "100 / 0", match: true }
      - { name: "vaporizer_death_gib_damage", base: "1000", port: "1000", match: true }
      - { name: "bleed_out_dmg", base: "5 (hp<=10) / 10", port: "5 / 10", match: true }
    status:
      logic: faithful
      values: faithful
      timing: faithful
      presentation: partial
      audio: partial
      liveness: live
    intended_divergence: false
    divergence_rationale: null
    gaps:
      - "Armor-as-lives matches but the +hitsound_damage_dealt feedback on both attacker and target (a hit-confirm cue) is not raised."
      - "instagib_ammocheck downgrade-to-laser branch (g_rm && g_rm_laser shows CENTER_INSTAGIB_DOWNGRADE instead of bleeding) not modeled — port always bleeds a cell-less player."
    verification:
      - { kind: code, ref: "InstagibMutator.cs vs sv_instagib.qc (start items, Damage_Calculate, PlayerDies, ammocheck)", result: pass }
    confidence: high
    notes: "Unchanged by the waves; still faithful+live on PlayerSpawn/PreThink/Damage/Dies — this is what makes the body-shot one-shot work. Residual: hit-confirm cue + the downgrade centerprint."

  - id: weapon-vaporizer.messages.kill_suicide
    name: "Kill / suicide death messages (WEAPON_VAPORIZER_MURDER / WEAPON_THINKING_WITH_PORTALS)"
    layer: [authority]
    base_refs:
      - "common/weapons/weapon/vaporizer.qc:wr_killmessage"
      - "common/weapons/weapon/vaporizer.qc:wr_suicidemessage"
    port_refs:
      - "src/XonoticGodot.Common/Gameplay/Notifications/DeathMessages.cs:SelectKillMessage"
      - "src/XonoticGodot.Common/Gameplay/Notifications/DeathMessages.cs:SelectSuicideMessage"
    constants:
      - { name: "kill_message", base: "WEAPON_VAPORIZER_MURDER", port: "WEAPON_VAPORIZER_MURDER", match: true }
      - { name: "suicide_message", base: "WEAPON_THINKING_WITH_PORTALS", port: "WEAPON_THINKING_WITH_PORTALS", match: true }
    status:
      logic: faithful
      values: faithful
      timing: na
      presentation: na
      audio: na
      liveness: unknown
    intended_divergence: false
    divergence_rationale: null
    gaps: []
    verification:
      - { kind: code, ref: "DeathMessages.cs case 'vaporizer'->WEAPON_VAPORIZER_MURDER; SelectSuicideMessage vaporizer default -> WEAPON_THINKING_WITH_PORTALS, vs vaporizer.qc:394-402", result: pass }
    confidence: medium
    notes: "Unchanged by the waves. The Vaporizer wr_killmessage/wr_suicidemessage ARE ported, centralized in DeathMessages.cs (a string lookup keyed by weapon NetName). Liveness still unknown: the SelectKillMessage/SelectSuicideMessage call site in the kill-feed path was not traced end-to-end in this audit."
