unit: mutator-vampire
category: mutator
base_ref_rev: "v0.8.6-1779-g863cd3e84"
last_audited: "2026-06-22"
summary:
  feature_count: 5
  worst_gaps:
    - "BuildMutatorsPrettyString chain is ported on the mutator (VampireMutator.BuildMutatorsPrettyString -> ', Vampire') AND aggregated by MutatorActivation.BuildMutatorsPrettyString, but that aggregator has NO live caller — Base calls it at server/client.qc:1107 to build the server-info 'modifications' string (scoreboard / server browser). So 'Vampire' still never reaches the pretty/status string. (The machine ':Vampire' string IS now live via the sv_eventlog gamelog path.)"
    - "The MENUQC 2-paragraph describe info page is still unported (only the DialogMutators checkbox + tooltip exists)."
features:
  - id: mutator-vampire.enable.gate
    name: "Enable predicate (g_vampire + instagib exclusion)"
    layer: [authority]
    base_refs:
      - "common/mutators/mutator/vampire/sv_vampire.qc:7 (REGISTER_MUTATOR vampire = expr_evaluate(g_vampire) && !MUTATOR_IS_ENABLED(mutator_instagib))"
      - "common/mutators/mutator/instagib/sv_instagib.qh:35 (REGISTER_MUTATOR mutator_instagib = autocvar_g_instagib && !MapInfo_LoadedGametype.m_weaponarena)"
    port_refs:
      - "src/XonoticGodot.Common/Gameplay/Mutators/VampireMutator.cs:24 (IsEnabled: g_vampire!=0 && g_instagib==0)"
      - "src/XonoticGodot.Server/GameWorld.cs:539 (MutatorActivation.Apply)"
      - "src/XonoticGodot.Common/Gameplay/Mutators/MutatorActivation.cs:60 (Apply -> mut.IsEnabled -> Add/Remove)"
    constants:
      - { name: "g_vampire", base: "0 (string)", port: "0 (mutators.cfg)", match: true }
      - { name: "instagib exclusion term", base: "!MUTATOR_IS_ENABLED(mutator_instagib)", port: "g_instagib == 0", match: true }
    status:
      logic: faithful
      values: faithful
      timing: na
      presentation: na
      audio: na
      liveness: live
    intended_divergence: false
    divergence_rationale: null
    gaps:
      - "Minor edge divergence: Base excludes vampire when the instagib MUTATOR is enabled, and instagib itself is gated on `g_instagib && !m_weaponarena`. The port's `g_instagib == 0` term keys on the cvar, not the resolved mutator state — so on a weaponarena gametype with g_instagib 1 (instagib mutator OFF) Base would permit vampire but the port suppresses it. The common `g_vampire 1; g_instagib 1` double-activation (the actual baseline defect) is correctly prevented."
    verification:
      - { kind: code, ref: "re-audit 2026-06-22: VampireMutator.IsEnabled (VampireMutator.cs:24-27) = `Api.Services is not null && GetFloat(g_vampire) != 0f && GetFloat(g_instagib) == 0f`; instagib term present. Unchanged at current HEAD (Wave 5 touched only the report row)", result: pass }
      - { kind: code, ref: "live: MutatorActivation.Apply (MutatorActivation.cs:59-69) iterates Mutators.All and Add/Removes per mut.IsEnabled; invoked from GameWorld.cs:539 at server boot", result: pass }
      - { kind: code, ref: "Base instagib register (sv_instagib.qh:35) also requires !m_weaponarena; port keys on g_instagib cvar only -> narrow weaponarena edge case (unchanged)", result: pass }
    confidence: high
    notes: "Re-verified 2026-06-22 against current HEAD: instagib exclusion present, activation path live. Residual is only the weaponarena edge (same approximation across NIX/new_toys/instagib-family ports). No change vs baseline."

  - id: mutator-vampire.heal.on_damage
    name: "Heal attacker by damage dealt (PlayerDamage_SplitHealthArmor hook)"
    layer: [authority]
    base_refs:
      - "common/mutators/mutator/vampire/sv_vampire.qc:9 (MUTATOR_HOOKFUNCTION vampire, PlayerDamage_SplitHealthArmor)"
      - "server/player.qc:322 (MUTATOR_CALLHOOK fired unconditionally per damage event)"
    port_refs:
      - "src/XonoticGodot.Common/Gameplay/Mutators/VampireMutator.cs:45 (OnPlayerDamage)"
      - "src/XonoticGodot.Common/Gameplay/Damage/DamageSystem.cs:438 (PlayerDamageSplitHealthArmor.Call)"
    constants:
      - { name: "g_vampire_factor (default)", base: "1.0", port: "1.0", match: true }
      - { name: "g_vampire_factor == 0 honored", base: "yes (heals 0)", port: "yes (read live, no if(f!=0) swallow)", match: true }
      - { name: "g_vampire_use_total_damage", base: "0 (false)", port: "false", match: true }
      - { name: "guard: !spawnshield(target)", base: "present", port: "present (target.SpawnShieldExpire > now)", match: true }
      - { name: "guard: attacker != target", base: "present", port: "present", match: true }
      - { name: "guard: IS_PLAYER(attacker)", base: "present", port: "EntFlags.Client", match: true }
      - { name: "guard: !IS_DEAD(target)", base: "present", port: "DeadFlag.No", match: true }
      - { name: "guard: !FROZEN(target)", base: "present", port: "present (StatusEffectsCatalog.Frozen)", match: true }
      - { name: "damage_take clamp bound(0,take,curHP)", base: "present", port: "present (QMath.Bound)", match: true }
      - { name: "cvar read cadence", base: "autocvar read live each damage event", port: "read live each damage event (OnPlayerDamage)", match: true }
    status:
      logic: faithful
      values: faithful
      timing: faithful
      presentation: na
      audio: na
      liveness: live
    intended_divergence: false
    divergence_rationale: null
    gaps: []
    verification:
      - { kind: code, ref: "re-audit 2026-06-22: OnPlayerDamage (VampireMutator.cs:45-79) heal math + total-vs-health/alive/frozen/self/player/spawnshield guards vs sv_vampire.qc:9 — all match. Unchanged by Wave 5", result: pass }
      - { kind: code, ref: "spawn-shield guard present: `targetSpawnShielded = target.SpawnShieldExpire > now` (VampireMutator.cs:63-64) skips heal when shielded; mirrors DamageSystem.HasSpawnShield. SpawnShieldExpire is set on the LIVE player spawn path (SpawnSystem.cs `p.SpawnShieldExpire = now + g_spawnshieldtime`)", result: pass }
      - { kind: code, ref: "g_vampire_factor / g_vampire_use_total_damage read live per event (VampireMutator.cs:52-53); factor 0 no longer swallowed (the Wave-2 snapshot in Hook() is gone) and the spurious `&& damageTake > 0f` guard remains dropped, matching Base", result: pass }
      - { kind: code, ref: "live: DamageSystem.cs:438 fires PlayerDamageSplitHealthArmor for every player-damage event; VampireMutator subscribes via Hook() (line 35)", result: pass }
    confidence: high
    notes: "Re-verified 2026-06-22: the Wave-2 fixes (spawn-shield guard, factor==0 honored, per-event cvar cadence) all hold at current HEAD; Wave 5 did not touch this row. No dedicated vampire unit test; the hook chain is exercised structurally by Mayhem/buffs/globalforces tests that drive PlayerDamageSplitHealthArmor, and discovery is asserted in IntegrationSmokeTests. No change vs baseline."

  - id: mutator-vampire.health.cap
    name: "Heal clamped to health limit (GiveResource -> GetResourceLimit)"
    layer: [authority]
    base_refs:
      - "common/resources/sv_resources.qc:12 (GetResourceLimit RES_HEALTH = autocvar_g_balance_health_limit)"
      - "common/mutators/mutator/vampire/vampire.qc:11 (MENUQC describe: 'health can go way above 200' — STALE text)"
    port_refs:
      - "src/XonoticGodot.Common/Gameplay/Items/Resources.cs:105 (GiveResource -> SetResource clamps to GetResourceLimit)"
      - "src/XonoticGodot.Common/Gameplay/Items/Resources.cs:84 (GetResourceLimit RES_HEALTH default 200)"
    constants:
      - { name: "g_balance_health_limit (balance-xonotic)", base: "200", port: "200", match: true }
    status:
      logic: faithful
      values: faithful
      timing: na
      presentation: na
      audio: na
      liveness: live
    intended_divergence: false
    divergence_rationale: null
    gaps: []
    verification:
      - { kind: code, ref: "re-audit 2026-06-22: current Base GetResourceLimit (sv_resources.qc) has NO vampire-specific override; heal caps at g_balance_health_limit. Port matches (Resources.cs:84 `Cvar(g_balance_health_limit, 200f)`, 105)", result: pass }
      - { kind: value, ref: "g_balance_health_limit 200; port default 200 (Resources.cs:84)", result: pass }
    confidence: high
    notes: "Faithful (unchanged). Both Base's MENUQC describe text and the port comment claiming health exceeds 200 are stale documentation; the modern resource system caps at the balance limit in both. No change vs baseline."

  - id: mutator-vampire.buff.exclusion
    name: "g_vampire suppresses the BUFF_VAMPIRE pickup"
    layer: [authority]
    base_refs:
      - "common/mutators/mutator/buffs/sv_buffs.qc:259 (buff_Available: BUFF_VAMPIRE && g_vampire -> false)"
    port_refs:
      - "src/XonoticGodot.Common/Gameplay/Mutators/BuffsMutator.cs:191 (BuffAvailable)"
    constants: []
    status:
      logic: faithful
      values: faithful
      timing: na
      presentation: na
      audio: na
      liveness: live
    intended_divergence: false
    divergence_rationale: null
    gaps: []
    verification:
      - { kind: code, ref: "re-audit 2026-06-22: BuffsMutator.cs:191 `if (n == \"vampire\" && Api.Cvars.GetFloat(\"g_vampire\") != 0f) return false;` mirrors buff_Available", result: pass }
    confidence: high
    notes: "Faithful port (unchanged); exercised on the buff-spawn path when g_buffs is enabled. No change vs baseline."

  - id: mutator-vampire.report.mutator_strings
    name: "Mutator-list strings + menu description (':Vampire' / ', Vampire' / MENUQC describe)"
    layer: [presentation]
    base_refs:
      - "common/mutators/mutator/vampire/sv_vampire.qc:25 (BuildMutatorsString -> ':Vampire'; aggregated at server/gamelog.qc:50, gated by sv_eventlog)"
      - "common/mutators/mutator/vampire/sv_vampire.qc:30 (BuildMutatorsPrettyString -> ', Vampire'; aggregated at server/client.qc:1107 for the server-info 'modifications' string)"
      - "common/mutators/mutator/vampire/vampire.qc:6 (MENUQC METHOD describe — 2-paragraph info page)"
    port_refs:
      - "src/XonoticGodot.Common/Gameplay/Mutators/VampireMutator.cs:82 (BuildMutatorsString -> ':Vampire')"
      - "src/XonoticGodot.Common/Gameplay/Mutators/VampireMutator.cs:85 (BuildMutatorsPrettyString -> ', Vampire')"
      - "src/XonoticGodot.Common/Gameplay/Mutators/MutatorActivation.cs:88 (BuildMutatorsString aggregator — LIVE) / :101 (BuildMutatorsPrettyString aggregator — DEAD, no caller)"
      - "src/XonoticGodot.Server/GameWorld.cs:865 (MutatorActivation.BuildMutatorsString live under sv_eventlog -> GameLog.Init)"
      - "game/menu/dialogs/DialogMutators.cs:140 (g_vampire checkbox + tooltip only; no describe page)"
    constants: []
    status:
      logic: na
      values: na
      timing: na
      presentation: partial
      audio: na
      liveness: partial
    intended_divergence: false
    divergence_rationale: null
    gaps:
      - "BuildMutatorsPrettyString (', Vampire') is now implemented on the mutator and an aggregator MutatorActivation.BuildMutatorsPrettyString exists, but that aggregator has NO live caller. Base invokes it at server/client.qc:1107 to assemble the server-info 'modifications' string (scoreboard / server browser / status). So 'Vampire' still never reaches the human-readable status string. Cross-cutting across all mutators; the pretty-string consumer was not wired in Wave 5."
      - "MENUQC describe (the 2-paragraph mutator info page) still not ported; only the checkbox tooltip 'Damage done to your enemy gets added to your own health' exists."
    verification:
      - { kind: code, ref: "FIX re-audit 2026-06-22: Wave 5 added VampireMutator.BuildMutatorsString -> ':Vampire' (VampireMutator.cs:82) and MutatorActivation.BuildMutatorsString (MutatorActivation.cs:88-94) iterates added mutators. LIVE caller: GameWorld.cs:865 calls it under the sv_eventlog gate and feeds GameLog.Init — same gate as Base server/gamelog.qc:50. The machine ':Vampire' token is therefore genuinely produced on the live event-log path", result: pass }
      - { kind: code, ref: "STILL OPEN: MutatorActivation.BuildMutatorsPrettyString (MutatorActivation.cs:101-107) is defined but grep across src/+game/ finds zero call sites; Base's consumer is server/client.qc:1107. Pretty/status string remains dead", result: fail }
      - { kind: code, ref: "DialogMutators.cs:140 vampire checkbox + tooltip present; no describe page ported", result: fail }
    confidence: high
    notes: "Wave 5 closed the machine-string half of this row: ':Vampire' is now ported AND live (sv_eventlog gamelog path, GameWorld.cs:865). The pretty-string half (', Vampire' for the server-info modifications string at Base client.qc:1107) is ported on the mutator but its aggregator has no caller -> still dead; and the MENUQC describe page is still missing. Net: missing/dead -> partial. The unwired pretty-string consumer is shared across all mutators (Midair has the same shape)."
