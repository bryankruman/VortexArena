unit: sv-anticheat
category: server
base_ref_rev: "v0.8.6-1779-g863cd3e84"
last_audited: "2026-06-22"
summary:
  feature_count: 15
  worst_gaps:
    - "div0_evade reseed broken: anticheat_prethink (zeroes evade_offset every PlayerPreThink) has no live caller, so the evade detector never re-arms per frame and reads differently from Base."
    - "anticheat_report_to_eventlog never invoked: no :anticheat: event-log verdict lines on disconnect — an admin cannot read a live verdict."
    - "No sv_cmd anticheat admin command: the on-demand per-client verdict report (Base sv_cmd.qc:248) is absent."
    - "Teleporter fixangle suppression not wired: after a teleport, snap-aim/strafebot_new is not suppressed, so a legit teleport view-snap can inflate the cheat signal."
    - "PingProvider hardcoded to 0: fixangle suppression window is always 0.2s instead of min(0.4,ping*0.001)+0.2, too short for laggy remote clients."
features:
  - id: sv-anticheat.core.power_mean
    name: "Power-mean accumulator (MEAN_DECLARE/accumulate/evaluate)"
    layer: [authority]
    base_refs:
      - "lib/math.qh:mean_accumulate"
      - "lib/math.qh:mean_evaluate"
    port_refs:
      - "src/XonoticGodot.Server/AntiCheat.cs:Mean"
    constants:
      - { name: "geometric_branch (m==0): acc*=value**weight, acc0=1", base: "yes", port: "yes", match: true }
      - { name: "additive_branch (m!=0): acc+=value**m*weight, acc0=0", base: "yes", port: "yes", match: true }
      - { name: "evaluate: m==0 -> acc**(1/count), else (acc/count)**(1/m); count==0 -> 0", base: "yes", port: "yes", match: true }
      - { name: "internal precision", base: "float", port: "double", match: false }
    status:
      logic: faithful
      values: faithful
      timing: na
      presentation: na
      audio: na
      liveness: live
    intended_divergence: true
    divergence_rationale: "Mean uses double internally vs QC float; verdicts are threshold-coarse so precision is immaterial. Deliberate."
    gaps: []
    verification:
      - { kind: test, ref: "tests/XonoticGodot.Tests/ServerInfraTests.cs:AntiCheat_Mean_PowerMean", result: pass }
      - { kind: code, ref: "AntiCheat.cs:25-39 matches lib/math.qh mean_accumulate/mean_evaluate bodies branch-for-branch (verified)", result: pass }
    confidence: high
    notes: "Exponents 1/2/3/4/5/7/10 all match; every anticheat statistic is non-zero exponent so the additive path is live."

  - id: sv-anticheat.detect.movement_oddity
    name: "movement_oddity strafebot turn-reversal score"
    layer: [authority]
    base_refs:
      - "server/anticheat.qc:movement_oddity"
    port_refs:
      - "src/XonoticGodot.Server/AntiCheat.cs:MovementOddity"
    constants:
      - { name: "0.5 - 0.5*cos(cosangle^2 * 4*PI)", base: "yes", port: "yes", match: true }
      - { name: "normalize(0)=0 (Quake semantics)", base: "yes", port: "QMath.Normalize->zero", match: true }
      - { name: "returns 0 when cosangle>=0", base: "yes", port: "yes", match: true }
    status:
      logic: faithful
      values: faithful
      timing: na
      presentation: na
      audio: na
      liveness: live
    intended_divergence: false
    divergence_rationale: null
    gaps: []
    verification:
      - { kind: test, ref: "tests/XonoticGodot.Tests/ServerInfraTests.cs:AntiCheat_MovementOddity_BotlikeReversalScoresHigh", result: pass }
      - { kind: code, ref: "AntiCheat.cs:296-301 == anticheat.qc:58-65 (verified)", result: pass }
    confidence: high
    notes: "Fed by anticheat_physics strafebot_old; uses QMath.Pi = M_PI."

  - id: sv-anticheat.detect.div0_evade
    name: "div0_evade server-driven dodge-prediction probe"
    layer: [authority]
    base_refs:
      - "server/anticheat.qc:anticheat_physics"
      - "server/anticheat.qc:anticheat_prethink"
      - "server/client.qc:2880"
    port_refs:
      - "src/XonoticGodot.Server/AntiCheat.cs:Physics"
      - "src/XonoticGodot.Server/AntiCheat.cs:PreThink"
    constants:
      - { name: "triangle wave f = |delta-floor-0.5|*2", base: "yes", port: "yes", match: true }
      - { name: "evade_offset = servertime + sys_frametime*(3f-1)", base: "yes", port: "yes", match: true }
      - { name: "accumulate 0.5-0.5*(forward_initial·v_forward), m=5", base: "yes", port: "yes", match: true }
      - { name: "evade_offset zeroed every PlayerPreThink (re-arm)", base: "every frame (anticheat_prethink, client.qc:2880)", port: "never (PreThink has zero callers)", match: false }
    status:
      logic: partial
      values: faithful
      timing: partial
      presentation: na
      audio: na
      liveness: partial
    intended_divergence: false
    divergence_rationale: null
    gaps:
      - "anticheat_prethink (zeroes evade_offset) has no live caller, so the div0_evade reseed branch never re-arms per frame; the detector accumulates against a single stale phase sample and the div0_evade verdict diverges from Base for identical player behavior."
    verification:
      - { kind: code, ref: "repo-wide grep: AntiCheat.PreThink has zero callers (only the definition AntiCheat.cs:165)", result: fail }
      - { kind: code, ref: "Physics() evade branch AntiCheat.cs:207-220 matches anticheat.qc:73-86", result: pass }
    confidence: high
    notes: "Within-frame evade math is faithful AND live (Physics runs per usercmd); the per-frame re-arm that Base relies on (PreThink) is dead. After the first Physics call EvadeOffset!=0 forever, so only the else-branch ever runs."

  - id: sv-anticheat.detect.strafebot_new_snapaim
    name: "strafebot_new turn-angle + idle snap-aim signal/noise + snapback"
    layer: [authority]
    base_refs:
      - "server/anticheat.qc:anticheat_physics"
    port_refs:
      - "src/XonoticGodot.Server/AntiCheat.cs:Physics"
    constants:
      - { name: "gated by forward_prev!=0 AND time>fixangle_endtime", base: "yes", port: "yes", match: true }
      - { name: "angle=acos(clamp(cos,-1,1)); accumulate angle/PI m=5", base: "yes", port: "yes", match: true }
      - { name: "slowmo gate; dt=max(0.001,frametime)/slowmo", base: "autocvar_slowmo default 1", port: "Simulation.TimeScale", match: true }
      - { name: "snapaim signal m=5, noise m=1, debug m2/m3/m4/m7/m10", base: "yes", port: "yes", match: true }
      - { name: "snapback horizon f=bound(0,dt*4,1) ~0.25s", base: "yes", port: "yes", match: true }
      - { name: "snapback_prev = snapback_prev*(1-f) + aim_move*f", base: "yes", port: "yes", match: true }
    status:
      logic: faithful
      values: faithful
      timing: faithful
      presentation: na
      audio: na
      liveness: live
    intended_divergence: false
    divergence_rationale: null
    gaps: []
    verification:
      - { kind: code, ref: "Physics() AntiCheat.cs:227-256 match anticheat.qc:92-129 step-for-step (verified)", result: pass }
    confidence: high
    notes: "Detector math is faithful+live. Suppression-window correctness (the time>fixangle_endtime gate) depends on the fixangle feature below, which is partial — so this signal can be inflated after teleports."

  - id: sv-anticheat.detect.speedhack_old
    name: "Generic speedhack (old movetime/servertime correlation)"
    layer: [authority]
    base_refs:
      - "server/anticheat.qc:anticheat_physics"
    port_refs:
      - "src/XonoticGodot.Server/AntiCheat.cs:Physics"
    constants:
      - { name: "movetime accum via frametime floor/frac", base: "yes", port: "yes", match: true }
      - { name: "accumulate max(0, delta-offset) m=5", base: "yes", port: "yes", match: true }
      - { name: "offset adapt += (delta-offset)*frametime*0.1", base: "0.1", port: "0.1f", match: true }
    status:
      logic: faithful
      values: faithful
      timing: faithful
      presentation: na
      audio: na
      liveness: live
    intended_divergence: false
    divergence_rationale: null
    gaps: []
    verification:
      - { kind: test, ref: "ServerInfraTests.cs:AntiCheat_Physics_AccumulatesWithoutThrowing (asserts SpeedhackOffset set)", result: pass }
      - { kind: code, ref: "Physics() AntiCheat.cs:259-272 == anticheat.qc:131-144 (verified)", result: pass }
    confidence: high
    notes: "QC comment says this one 'seems broken'; the port reproduces it faithfully (threshold 240,0,9999)."

  - id: sv-anticheat.detect.speedhack_new
    name: "Generic speedhack (new decaying accumulator m1..m5)"
    layer: [authority]
    base_refs:
      - "server/anticheat.qc:anticheat_physics"
    port_refs:
      - "src/XonoticGodot.Server/AntiCheat.cs:Physics"
    constants:
      - { name: "falloff", base: "0.2", port: "0.2f", match: true }
      - { name: "accu *= exp(-dt*falloff); accu += frametime*falloff", base: "yes", port: "yes", match: true }
      - { name: "accumulate accu (w=frametime) into m1..m5", base: "yes", port: "yes", match: true }
      - { name: "first-sample seed: accu=1; lasttime=servertime", base: "yes", port: "yes", match: true }
    status:
      logic: faithful
      values: faithful
      timing: faithful
      presentation: na
      audio: na
      liveness: live
    intended_divergence: false
    divergence_rationale: null
    gaps: []
    verification:
      - { kind: code, ref: "Physics() AntiCheat.cs:274-292 match anticheat.qc:146-163 (verified)", result: pass }
    confidence: high
    notes: "Per-usercmd dt drive (GameWorld batch, GameWorld.cs:1084-1093) keeps servertime/dt faithful vs a single per-tick step."

  - id: sv-anticheat.frame.evasion_phase_walk
    name: "Global div0_evade evasion-delta phase walk (start+end frame)"
    layer: [authority]
    base_refs:
      - "server/anticheat.qc:anticheat_startframe"
      - "server/anticheat.qc:anticheat_endframe"
      - "server/main.qc:373"
      - "server/world.qc:2505"
    port_refs:
      - "src/XonoticGodot.Server/AntiCheat.cs:StartFrame"
      - "src/XonoticGodot.Server/AntiCheat.cs:EndFrame"
      - "src/XonoticGodot.Server/GameWorld.cs:917"
      - "src/XonoticGodot.Server/GameWorld.cs:1213"
    constants:
      - { name: "evasion_delta += frametime*(0.5+random()) twice per frame", base: "yes (startframe + endframe)", port: "yes (StartFrame + EndFrame)", match: true }
    status:
      logic: faithful
      values: faithful
      timing: faithful
      presentation: na
      audio: na
      liveness: live
    intended_divergence: true
    divergence_rationale: "RNG is a seeded deterministic Random(0x4317) vs Base unseeded random(); the phase walk is intentionally noisy so the exact sequence is irrelevant to verdicts. Deterministic seed chosen for reproducible headless sims."
    gaps: []
    verification:
      - { kind: code, ref: "StartFrame called GameWorld.cs:917, EndFrame GameWorld.cs:1213, both advance _evasionDelta", result: pass }
    confidence: high
    notes: "Base uses unseeded random(); the port seeds the RNG for determinism. The walk is intentionally noisy so seed parity is immaterial — recorded as intended_divergence rather than a gap."

  - id: sv-anticheat.frame.fixangle_window
    name: "Snap-aim suppression window after a forced view change"
    layer: [authority]
    base_refs:
      - "server/anticheat.qc:anticheat_fixangle"
      - "server/anticheat.qc:anticheat_endframe"
      - "common/mapobjects/teleporters.qc:310"
    port_refs:
      - "src/XonoticGodot.Server/AntiCheat.cs:FixAngle"
      - "src/XonoticGodot.Server/AntiCheat.cs:EndFrame"
      - "src/XonoticGodot.Server/GameWorld.cs:748"
      - "src/XonoticGodot.Server/GameWorld.cs:1213"
    constants:
      - { name: "fixangle_endtime = servertime + ANTILAG_LATENCY + 0.2 (formula)", base: "min(0.4,ping*0.001)+0.2", port: "min(0.4,ping*0.001)+0.2", match: true }
      - { name: "applied on spawn (PutPlayerInServer)", base: "yes", port: "yes (GameWorld.cs:748)", match: true }
      - { name: "applied on teleport (teleporters.qc:310)", base: "yes", port: "no (Teleporters sets player.FixAngle but never calls AntiCheat.FixAngle)", match: false }
      - { name: "applied via endframe FOREACH_CLIENT(it.fixangle)", base: "yes (world.qc:2505 -> anticheat_endframe)", port: "no (EndFrame called at GameWorld.cs:1213 with no fixAngleClients arg)", match: false }
      - { name: "ping fed to window", base: "real per-client ping", port: "AntiCheat.PingProvider hardcoded 0f (GameWorld.cs:667)", match: false }
    status:
      logic: partial
      values: partial
      timing: partial
      presentation: na
      audio: na
      liveness: partial
    intended_divergence: false
    divergence_rationale: null
    gaps:
      - "Teleporter fixangle suppression not wired: after a teleport, snap-aim/strafebot_new is not suppressed, so a legit teleport view-snap can inflate the strafebot_new / idle_snapaim signal (false positive)."
      - "EndFrame's FOREACH_CLIENT(.fixangle) propagation is dead: GameWorld.cs:1213 always calls EndFrame with no fixAngleClients, so any non-spawn forced view change never opens the suppression window."
      - "PingProvider hardcoded to 0: fixangle suppression window is always 0.2s instead of min(0.4,ping*0.001)+0.2, too short for laggy remote clients on a dedicated host."
    verification:
      - { kind: code, ref: "FixAngle formula AntiCheat.cs:168-169 matches anticheat.qc:246 + ANTILAG_LATENCY", result: pass }
      - { kind: code, ref: "Teleporters sets player.FixAngle but no AntiCheat.FixAngle call (repo grep)", result: fail }
      - { kind: code, ref: "GameWorld.cs:1213 EndFrame() called without fixAngleClients; GameWorld.cs:667 PingProvider=0f", result: fail }
    confidence: high
    notes: "Spawn path is faithful+live (GameWorld.cs:748). Teleport + endframe propagation + real ping are the gaps. EndFrame supports a fixAngleClients arg but the live caller passes none. Listen-server/bot ping is 0 anyway, so the ping gap only bites remote clients on a dedicated host."

  - id: sv-anticheat.lifecycle.init
    name: "anticheat_init (jointime stamp + speedhack baseline clear) on connect"
    layer: [authority]
    base_refs:
      - "server/anticheat.qc:anticheat_init"
      - "common/state.qc:59"
    port_refs:
      - "src/XonoticGodot.Server/AntiCheat.cs:Init"
      - "src/XonoticGodot.Server/GameWorld.cs:720"
    constants:
      - { name: "speedhack_offset = 0; jointime = servertime", base: "yes", port: "yes", match: true }
    status:
      logic: faithful
      values: faithful
      timing: faithful
      presentation: na
      audio: na
      liveness: live
    intended_divergence: false
    divergence_rationale: null
    gaps: []
    verification:
      - { kind: code, ref: "InfraClientConnect calls AntiCheat.Init(p, Time) GameWorld.cs:720; Remove on disconnect GameWorld.cs:776", result: pass }
    confidence: high
    notes: "Remove() on disconnect (GameWorld.cs:776) frees the per-player state, analogous to QC edict teardown (state.qc detach)."

  - id: sv-anticheat.report.eventlog
    name: "anticheat_report_to_eventlog (:anticheat: verdict lines on disconnect)"
    layer: [authority]
    base_refs:
      - "server/anticheat.qc:anticheat_report_to_eventlog"
      - "common/state.qc:88"
    port_refs:
      - "src/XonoticGodot.Server/AntiCheat.cs:ReportToEventLog"
    constants:
      - { name: "gated by autocvar_sv_eventlog", base: "default 0", port: "method exists, ungated (gate is the dead call site, not the method)", match: false }
      - { name: ":anticheat:_time:<playerid>:<elapsed> then per-detector line", base: "yes", port: "yes (format faithful)", match: true }
    status:
      logic: faithful
      values: faithful
      timing: na
      presentation: na
      audio: na
      liveness: dead
    intended_divergence: false
    divergence_rationale: null
    gaps:
      - "anticheat_report_to_eventlog has no live caller: Base calls it on client disconnect (state.qc:88, ClientState_detach). The port's InfraClientDisconnect (GameWorld.cs:776) calls AntiCheat.Remove but never ReportToEventLog, so no :anticheat: verdict lines reach the event log on disconnect — only the end-of-match XonStat feed carries values."
    verification:
      - { kind: code, ref: "repo-wide grep: ReportToEventLog has zero callers (only the definition AntiCheat.cs:319); InfraClientDisconnect calls Remove not ReportToEventLog", result: fail }
    confidence: high
    notes: "The format/logic of the method is faithful (sv_eventlog gating lives at the QC call site, which is absent here); it is purely a wiring gap. The GameLog/sv_eventlog plumbing exists (GameWorld.cs:767/GameLog.Part) but is not used for anticheat."

  - id: sv-anticheat.report.sv_cmd
    name: "sv_cmd anticheat admin on-demand verdict report"
    layer: [authority]
    base_refs:
      - "server/command/sv_cmd.qc:248"
      - "server/anticheat.qc:anticheat_report_to_eventlog"
    port_refs: []
    constants: []
    status:
      logic: missing
      values: missing
      timing: na
      presentation: missing
      audio: na
      liveness: na
    intended_divergence: false
    divergence_rationale: null
    gaps:
      - "No sv_cmd anticheat admin command: Base sv_cmd.qc:248 lets an admin dump anticheat_report_to_eventlog for a chosen client on demand. The port's server command table has no anticheat verb, so an admin cannot pull a live per-client verdict at all."
    verification:
      - { kind: code, ref: "no anticheat verb in the port server command table; AntiCheat exposes no command handler", result: fail }
    confidence: medium
    notes: "Distinct from the disconnect eventlog path; both are dead in the port. The underlying ReportToEventLog method exists and is faithful, so wiring is the only work."

  - id: sv-anticheat.report.playerstats
    name: "anticheat_report_to_playerstats (XonStat end-of-match feed)"
    layer: [authority]
    base_refs:
      - "server/anticheat.qc:anticheat_report_to_playerstats"
      - "common/playerstats.qc:206"
    port_refs:
      - "src/XonoticGodot.Server/AntiCheat.cs:ReportToPlayerStats"
      - "src/XonoticGodot.Server/GameWorld.cs:677"
    constants:
      - { name: "anticheat-_time + anticheat-<name> per detector", base: "PLAYERSTATS_ANTICHEAT prefix 'anticheat-'", port: "'anticheat-' + name", match: true }
    status:
      logic: faithful
      values: faithful
      timing: na
      presentation: na
      audio: na
      liveness: live
    intended_divergence: false
    divergence_rationale: null
    gaps: []
    verification:
      - { kind: code, ref: "PlayerStats.AnticheatReporter wired to AntiCheat.ReportToPlayerStats GameWorld.cs:677; invoked in FinalizePlayer PlayerStats.cs:238", result: pass }
    confidence: medium
    notes: "Wired into PlayerStats and invoked at FinalizePlayer; not behaviorally observed end-to-end (no playerstats round-trip test for the anticheat rows). Event-id strings match PLAYERSTATS_ANTICHEAT."

  - id: sv-anticheat.report.register_playerstats
    name: "anticheat_register_to_playerstats (pre-register the anticheat event slots)"
    layer: [authority]
    base_refs:
      - "server/anticheat.qc:anticheat_register_to_playerstats"
      - "common/playerstats.qc:339"
    port_refs: []
    constants:
      - { name: "AddEvent('anticheat-_time') + AddEvent('anticheat-<name>') per detector at GameReport init", base: "yes", port: "no (no AddEvent calls for anticheat slots; PlayerStats.Init registers score/accuracy slots only)", match: false }
    status:
      logic: missing
      values: na
      timing: na
      presentation: na
      audio: na
      liveness: na
    intended_divergence: false
    divergence_rationale: null
    gaps:
      - "anticheat_register_to_playerstats is not ported: Base pre-registers each anticheat event id via PlayerStats_GameReport_AddEvent (playerstats.qc:339) so unregistered events are dropped. The port's PlayerStats.Event() (PlayerStats.cs:177-184) auto-accepts any key, so the anticheat feed still works WITHOUT registration — but the registration step itself is absent, a structural divergence from Base's register-then-emit contract."
    verification:
      - { kind: code, ref: "PlayerStats.Init (PlayerStats.cs:98-114) registers score/accuracy/achievement slots but no anticheat slots; Event() accepts unregistered keys so the omission is masked", result: fail }
    confidence: high
    notes: "Low impact because the port's Event() does not gate on registration (unlike QC, which drops unregistered events). Listed for completeness; the playerstats feed is live regardless."

  - id: sv-anticheat.report.display_verdict
    name: "anticheat_display N/Y/- verdict formatting + ANTICHEATS table"
    layer: [authority]
    base_refs:
      - "server/anticheat.qc:anticheat_display"
      - "server/anticheat.qc:ANTICHEATS"
    port_refs:
      - "src/XonoticGodot.Server/AntiCheat.cs:Display"
      - "src/XonoticGodot.Server/AntiCheat.cs:Detectors"
    constants:
      - { name: ":N if f<=mi, :Y if f>=ma, else :- (and only when t>=tmin)", base: "yes", port: "yes", match: true }
      - { name: "18 detector rows with (tmin,mi,ma)", base: "speedhack(240,0,9999), m1..m5(240,1.01,1.25), strafebot_old(120,0.15,0.4), strafebot_new(120,0.25,0.8), evade(120,0.2,0.5), snapaim*(120,0,9999), snapback(120,0,9999)", port: "identical", match: true }
      - { name: "idle_snapaim = signal - noise", base: "yes", port: "yes", match: true }
    status:
      logic: faithful
      values: faithful
      timing: na
      presentation: faithful
      audio: na
      liveness: partial
    intended_divergence: false
    divergence_rationale: null
    gaps:
      - "Display/Detectors are correct but only reachable via ReportToPlayerStats (live); the event-log + sv_cmd paths that surface these verdict strings to an admin are dead (see sv-anticheat.report.eventlog and sv-anticheat.report.sv_cmd)."
    verification:
      - { kind: test, ref: "ServerInfraTests.cs:AntiCheat_Display_Verdicts", result: pass }
      - { kind: code, ref: "Detectors[] (AntiCheat.cs:121-141) == ANTICHEATS macro (anticheat.qc:190-208) name/tmin/mi/ma for all 18 rows (verified)", result: pass }
    confidence: high
    notes: "All 18 names + thresholds match the ANTICHEATS macro exactly. ReportToPlayerStats feeds the raw values (live); the human-readable verdict only surfaces via the dead eventlog/sv_cmd paths."

  - id: sv-anticheat.spectate.evade_angle_copy
    name: "anticheat_spectatecopy (spectator body follows spectatee evade angle)"
    layer: [authority]
    base_refs:
      - "server/anticheat.qc:anticheat_spectatecopy"
      - "server/client.qc:1837"
    port_refs:
      - "src/XonoticGodot.Server/AntiCheat.cs:SpectateCopy"
    constants:
      - { name: "viewer.angles = spectatee.anticheat_div0_evade_v_angle", base: "yes", port: "yes (logic)", match: true }
    status:
      logic: faithful
      values: faithful
      timing: na
      presentation: stub
      audio: na
      liveness: dead
    intended_divergence: false
    divergence_rationale: null
    gaps:
      - "anticheat_spectatecopy has no live caller: a spectator's body angles do not inherit the spectatee's evade-tracked angle. The port's own ClientManager.SpectateCopy (ClientManager.cs:340) is a separate mirror that omits the anticheat angle-copy. Cosmetic (spectator body yaw) but a divergence from Base."
    verification:
      - { kind: code, ref: "repo grep: AntiCheat.SpectateCopy has zero callers; ClientManager.SpectateCopy (ClientManager.cs:340) is a separate mirror that omits it", result: fail }
    confidence: high
    notes: "Low gameplay impact; listed for completeness of the div0_evade SPECTATORS path (Base server/client.qc:1837)."
