unit: mutator-rocketminsta
category: mutator
base_ref_rev: "v0.8.6-1779-g863cd3e84"
last_audited: "2026-06-22"
summary:
  feature_count: 9
  worst_gaps:
    - "Laser barrage still has no rapid-fire ramp: Base's hold-to-stream (hagar_load/jump_interval2 + g_rm_laser_rapid 1 / _rapid_delay 0.6 / _rapid_refire 0.35 + the mode-1 single-bolt stream) is absent. The port Vaporizer still has no hagar_load/JumpInterval2 state — it fires only the mode-0 fan on the 0.7s g_rm_laser_refire gate."
    - "RM laser bolts are still not networked/rendered as projectiles: no CSQCProjectile PROJECTILE_ROCKETMINSTA_LASER, no models/elaser.mdl, no EFFECT_ROCKETMINSTA_LASER trail attached, no team-color colormod. The bolts deal radius damage but are visually invisible (only the trail effect string is registered at EffectsList.cs:250, with no emitter)."
    - "Electrobitch achievement (RM laser timeout-explode landing on a flying enemy of another team) is still not implemented; the port collapses the Base touch path (no achievement) and the timeout .use path (achievement) into one RadiusDamage with no ANNCE_ACHIEVEMENT_ELECTROBITCH."
    - "Owner velocity not added to laser bolts: Base applies W_CalculateProjectileVelocity(actor, actor.velocity, vel, true); the port still omits it (aim divergence while moving). g_rm_laser==2 standalone-laser mode and the forced-reload-for-laser branch are still unmodeled (dormant at defaults)."
features:
  - id: mutator-rocketminsta.enable.registration
    name: "Registration + enable (rides on g_instagib, hooks re-gate on g_rm live toggle)"
    layer: [authority]
    base_refs:
      - "common/mutators/mutator/rocketminsta/sv_rocketminsta.qc:6 (REGISTER_MUTATOR(rm, autocvar_g_instagib))"
      - "common/mutators/mutator/rocketminsta/sv_rocketminsta.qc:11 (if(!autocvar_g_rm) return)"
      - "mutators.cfg:441 (set g_rm 0)"
    port_refs:
      - "src/XonoticGodot.Common/Gameplay/Mutators/RocketMinstaMutator.cs:32 (IsEnabled)"
      - "src/XonoticGodot.Common/Gameplay/Mutators/RocketMinstaMutator.cs:53 (RmActive)"
      - "src/XonoticGodot.Server/GameWorld.cs:511 (MutatorActivation.Apply)"
    constants:
      - { name: "g_rm default", base: "0 (disabled)", port: "0 via mutators.cfg:441", match: true }
      - { name: "enable predicate", base: "REGISTER_MUTATOR cond autocvar_g_instagib", port: "GetFloat(\"g_instagib\") != 0f", match: true }
      - { name: "per-hook live gate", base: "if(!autocvar_g_rm) return", port: "RmActive(): GetFloat(\"g_rm\") != 0f", match: true }
    status:
      logic: faithful
      values: faithful
      timing: na
      presentation: na
      audio: na
      liveness: live
    intended_divergence: false
    divergence_rationale: null
    gaps: []
    verification:
      - { kind: code, ref: "RocketMinstaMutator unchanged by Wave 1-2; MutatorActivation.Apply at GameWorld.cs:511 subscribes Hook() on live boot; IsEnabled gates on g_instagib, each hook re-gates on g_rm. Faithful + live.", result: pass }
    confidence: high
    notes: "RM is a sub-mode of instagib: enabled when g_instagib set, then each hook body re-checks g_rm so it can be flipped mid-match (mirrors QC). Both g_instagib and g_rm default 0. Unchanged + verified faithful."

  - id: mutator-rocketminsta.damage.zero_self_nade
    name: "Damage_Calculate: zero Devastator self/nade damage"
    layer: [authority]
    base_refs:
      - "common/mutators/mutator/rocketminsta/sv_rocketminsta.qc:18-20 (MUTATOR_HOOKFUNCTION rm Damage_Calculate, WEP_DEVASTATOR branch)"
    port_refs:
      - "src/XonoticGodot.Common/Gameplay/Mutators/RocketMinstaMutator.cs:63-67 (OnDamageCalculate devastator branch)"
      - "src/XonoticGodot.Common/Gameplay/Damage/DamageSystem.cs:254-256 (DamageCalculate.Call with string deathType)"
      - "src/XonoticGodot.Common/Gameplay/Weapons/Vaporizer.cs:175-180 (explosion now passes deathTag=FromWeapon(\"devastator\"))"
    constants:
      - { name: "match weapon", base: "DEATH_ISWEAPON(WEP_DEVASTATOR)", port: "WeaponNetNameOf == \"devastator\"", match: true }
      - { name: "condition", base: "attacker==target || target.classname==\"nade\"", port: "ReferenceEquals(attacker,target) || target.ClassName==\"nade\"", match: true }
      - { name: "result", base: "frag_damage = 0", port: "args.Damage = 0f", match: true }
    status:
      logic: faithful
      values: faithful
      timing: na
      presentation: na
      audio: na
      liveness: live
    intended_divergence: false
    divergence_rationale: null
    gaps: []
    verification:
      - { kind: code, ref: "Wave-2 FIX: RM explosion (Vaporizer.cs:177-180) now passes deathTag = FromWeapon(\"devastator\") to RadiusDamage. Explosion victims (firer included — directHit not passed, so all are indirect) carry tag 'weapon-devastator|splash'; WeaponNetNameOf strips the splash suffix → 'devastator'. DamageCalculate.Call (DamageSystem.cs:254) passes that string deathType to the hook → branch matches → self/nade damage zeroed.", result: pass }
    confidence: high
    notes: "FIXED. Previously the explosion mislabeled itself 'vaporizer' so this branch never matched real RM-explosion damage. Now rocket-jumping with the RM explosion no longer hurts the firer (and nade damage is nullified). The hook body was always faithful; the upstream deathtype label is now correct (devastator)."

  - id: mutator-rocketminsta.damage.zero_laser_self_round
    name: "Damage_Calculate: zero Electro laser self / pre-round damage (g_rm_laser)"
    layer: [authority]
    base_refs:
      - "common/mutators/mutator/rocketminsta/sv_rocketminsta.qc:22-25 (WEP_ELECTRO branch, g_rm_laser gate)"
    port_refs:
      - "src/XonoticGodot.Common/Gameplay/Mutators/RocketMinstaMutator.cs:71-75 (OnDamageCalculate electro branch)"
      - "src/XonoticGodot.Common/Gameplay/Damage/DamageSystem.cs:254-256"
      - "src/XonoticGodot.Common/Gameplay/Weapons/Vaporizer.cs:212-213 (laser deathType = electro.RegistryId)"
    constants:
      - { name: "gate", base: "autocvar_g_rm_laser", port: "GetFloat(\"g_rm_laser\") != 0f", match: true }
      - { name: "match weapon", base: "DEATH_ISWEAPON(WEP_ELECTRO)", port: "WeaponNetNameOf == \"electro\"", match: true }
      - { name: "round condition", base: "round_handler_IsActive() && !round_handler_IsRoundStarted()", port: "VehicleCommon.GameStopped", match: false }
      - { name: "self condition", base: "frag_attacker == frag_target", port: "ReferenceEquals(attacker,target)", match: true }
    status:
      logic: partial
      values: faithful
      timing: na
      presentation: na
      audio: na
      liveness: live
    intended_divergence: true
    divergence_rationale: "Round-not-started branch: a mutator can't reach the gametype-owned round handler, so the port approximates round_handler_IsActive()&&!IsRoundStarted() with game_stopped (GameStopped) — a faithful superset for the not-yet-live-round case. The self-damage (attacker==target) branch is exact. Documented in the class comment."
    gaps:
      - "Round gate approximated by game_stopped (intended divergence); may diverge from the exact round-handler edge in round-based modes mid-round."
    verification:
      - { kind: code, ref: "Wave-2 FIX: RM laser bolts now tag damage with electro.RegistryId (Vaporizer.cs:212-213). Direct touch victim → ApplyDamage(int electro id) → FromWeapon('electro')='weapon-electro'; indirect victims → 'weapon-electro|splash'. WeaponNetNameOf → 'electro' → branch matches; self-damage (attacker==target) zeroed when g_rm_laser set.", result: pass }
    confidence: high
    notes: "FIXED on the dominant axis (the weapon-id mislabel): laser self-damage and pre-round laser damage are now nullified. logic:partial remains ONLY because of the intended round-gate approximation (game_stopped vs the exact round handler). liveness upgraded partial→live."

  - id: mutator-rocketminsta.kill.force_gib
    name: "PlayerDies: force gib on Devastator/Electro kills (corpse damage = 1000)"
    layer: [authority]
    base_refs:
      - "common/mutators/mutator/rocketminsta/sv_rocketminsta.qc:37-38 (MUTATOR_HOOKFUNCTION rm PlayerDies)"
    port_refs:
      - "src/XonoticGodot.Common/Gameplay/Mutators/RocketMinstaMutator.cs:81-90 (OnPlayerDies)"
      - "src/XonoticGodot.Common/Gameplay/Damage/DamageSystem.cs:588-589 (PlayerDies.Call with string deathType)"
    constants:
      - { name: "match weapons", base: "WEP_DEVASTATOR || WEP_ELECTRO", port: "\"devastator\" || \"electro\"", match: true }
      - { name: "forced corpse damage", base: "M_ARGV(4) = 1000", port: "args.Damage = 1000f", match: true }
    status:
      logic: faithful
      values: faithful
      timing: na
      presentation: na
      audio: na
      liveness: live
    intended_divergence: false
    divergence_rationale: null
    gaps: []
    verification:
      - { kind: code, ref: "PlayerDies.Call live at DamageSystem.cs:588; RM explosion kills now tagged 'weapon-devastator|splash' and RM laser kills 'weapon-electro'(+|splash). WeaponNetNameOf strips the splash suffix → 'devastator'/'electro' → both branches match → forced gib (Damage=1000) fires on real RM kills.", result: pass }
    confidence: high
    notes: "FIXED. Faithful hook body, now LIVE in practice because both RM damage sources carry the correct devastator/electro deathtype. RM kills over-gib as in Base."

  - id: mutator-rocketminsta.primary.explosion
    name: "Primary rail beam detonates a Devastator-style explosion at the endpoint"
    layer: [authority]
    base_refs:
      - "common/weapons/weapon/vaporizer.qc:108 (W_RocketMinsta_Explosion)"
      - "common/weapons/weapon/vaporizer.qc:169-171 (W_Vaporizer_Attack g_rm + surface-flag gate)"
      - "mutators.cfg:442-445"
    port_refs:
      - "src/XonoticGodot.Common/Gameplay/Weapons/Vaporizer.cs:168-181 (RocketMinstaExplosion)"
      - "src/XonoticGodot.Common/Gameplay/Weapons/Vaporizer.cs:156-158 (Attack g_rm + surface-flag gate)"
    constants:
      - { name: "g_rm_damage", base: "70", port: "cvar (cfg 70); C# fallback 70", match: true }
      - { name: "g_rm_edgedamage", base: "38", port: "cvar (cfg 38); C# fallback 38", match: true }
      - { name: "g_rm_radius", base: "140", port: "cvar (cfg 140); C# fallback 140", match: true }
      - { name: "g_rm_force", base: "400", port: "cvar (cfg 400); C# fallback 400", match: true }
      - { name: "deathtype", base: "WEP_DEVASTATOR.m_id | HITTYPE_SPLASH", port: "deathTag=FromWeapon(\"devastator\"); HITTYPE_SPLASH OR'd on indirect victims via SplashDeathType", match: true }
      - { name: "sky/noimpact skip", base: "skip on Q3SURFACEFLAG_SKY(0x4)|NOIMPACT(0x1)", port: "Vaporizer.cs:156-158 gates on (DpHitQ3SurfaceFlags & (Sky|NoImpact))==0", match: true }
      - { name: "accuracy_add", base: "accuracy_add(WEP_DEVASTATOR, g_rm_damage)", port: "accuracyWeapon: devastator passed to RadiusDamage → WeaponAccuracyEvents.Hit", match: true }
    status:
      logic: faithful
      values: faithful
      timing: faithful
      presentation: na
      audio: missing
      liveness: live
    intended_divergence: false
    divergence_rationale: null
    gaps:
      - "No Devastator explosion sound emitted on the explosion path (Base routes the explode SFX via the deathtype/effect; the port plays only the rail fire sound minstanexfire.wav + the Vortex impact neximpact.wav, no rocket explosion sound at the blast)."
    verification:
      - { kind: code, ref: "RocketMinstaExplosion called from Attack under g_rm AND the new sky/noimpact surface-flag gate (Vaporizer.cs:156-158, Sky=0x4 NoImpact=0x1). RadiusDamage now passes deathTag=FromWeapon('devastator') (→ HITTYPE_SPLASH on indirect via SplashDeathType) and accuracyWeapon=devastator. cfg + C# fallbacks both ship Base values 70/38/140/400.", result: pass }
    confidence: high
    notes: "Largely FIXED. The deathtype mislabel (was 'vaporizer'), the missing sky/noimpact gate, the missing HITTYPE_SPLASH, and the missing accuracy_add are all now correct — closing the root cause that disabled the three RM mutator hooks for the explosion. Only audio remains missing: there is no Devastator explosion SFX emitted at the blast point."

  - id: mutator-rocketminsta.secondary.laser_barrage
    name: "Secondary / out-of-cells primary: bouncing RM laser fan"
    layer: [authority]
    base_refs:
      - "common/weapons/weapon/vaporizer.qc:222 (W_RocketMinsta_Attack)"
      - "common/weapons/weapon/vaporizer.qc:176 (W_RocketMinsta_Laser_Damage)"
      - "common/weapons/weapon/vaporizer.qc:286-355 (wr_think laser branch + rapid ramp)"
      - "mutators.cfg:446-460"
    port_refs:
      - "src/XonoticGodot.Common/Gameplay/Weapons/Vaporizer.cs:195-258 (RocketMinstaLaserBarrage)"
      - "src/XonoticGodot.Common/Gameplay/Weapons/Vaporizer.cs:69-109 (WrThink primaryFallback + secondary branch)"
    constants:
      - { name: "g_rm_laser_count", base: "3", port: "cvar (cfg 3); C# fallback 3", match: true }
      - { name: "g_rm_laser_damage (÷count)", base: "80", port: "cvar (cfg 80); fallback 80", match: true }
      - { name: "g_rm_laser_force (÷count)", base: "400", port: "cvar (cfg 400); fallback 400", match: true }
      - { name: "g_rm_laser_radius", base: "150", port: "cvar (cfg 150); fallback 150", match: true }
      - { name: "g_rm_laser_speed", base: "6000", port: "cvar (cfg 6000); fallback 6000", match: true }
      - { name: "g_rm_laser_lifetime", base: "30 s", port: "cvar (cfg 30); fallback 30", match: true }
      - { name: "g_rm_laser_spread", base: "0.05", port: "cvar (cfg 0.05); fallback 0.05", match: true }
      - { name: "g_rm_laser_refire", base: "0.7 s", port: "cvar (cfg 0.7); fallback 0.7", match: true }
      - { name: "g_rm_laser_zspread", base: "zspread * (random()-0.5) → [-0.5,0.5]", port: "zspread * (Prandom.Float()-0.5f) → [-0.5,0.5]", match: true }
      - { name: "g_rm_laser_spread_random", base: "0 (random multiplier on spread)", port: "not modeled (spread used as-is; dormant at default 0)", match: false }
      - { name: "deathtype", base: "WEP_ELECTRO.m_id", port: "electro.RegistryId", match: true }
      - { name: "muzzle flash", base: "W_MuzzleFlash(WEP_ELECTRO)", port: "EffectEmitter.Emit(\"ELECTRO_MUZZLEFLASH\")", match: true }
      - { name: "owner velocity add", base: "W_CalculateProjectileVelocity(...,true)", port: "not applied", match: false }
      - { name: "fan velocity formula", base: "(shotdir + ((i+0.5)/n*2-1)*right*spread)*speed; z+=zspread*(random-0.5)", port: "same formula", match: true }
    status:
      logic: partial
      values: faithful
      timing: partial
      presentation: missing
      audio: faithful
      liveness: live
    intended_divergence: false
    divergence_rationale: null
    gaps:
      - "No rapid-fire ramp: Base's hold-to-stream (hagar_load/jump_interval2, g_rm_laser_rapid 1 / _rapid_delay 0.6 / _rapid_refire 0.35, mode-1 single-bolt stream) is absent. The port Vaporizer has no hagar_load/JumpInterval2 state — it fires only the mode-0 fan on the 0.7s refire."
      - "No W_CalculateProjectileVelocity: shooter motion not added to bolt velocity (aim divergence while moving)."
      - "g_rm_laser==2 standalone-laser mode (RM laser without g_rm) and the forced-reload-for-laser branch (wr_think:290-303, which resets hagar_load) are not modeled."
      - "g_rm_laser_spread_random multiplier not modeled (dormant at default 0)."
    verification:
      - { kind: code, ref: "RocketMinstaLaserBarrage spawns count bolts (BounceMissile, MakeTrigger), per-bolt RadiusDamage(deathType=electro.RegistryId, damage/force ÷count) on Touch (directHit:other) and Think (timeout), ELECTRO_MUZZLEFLASH, crylink_fire.wav, per-bolt EditProjectile hook. WrThink still has no rapid/hagar_load path. cfg + fallbacks ship Base values.", result: pass }
    confidence: high
    notes: "Mostly FIXED. Wave-2 corrected: deathtype now WEP_ELECTRO (was vaporizer), mode-0 sound now SND_CRYLINK_FIRE crylink_fire.wav (was electro_fire2 → audio:partial→faithful), zspread now (random-0.5) ([-0.5,0.5], was 2x), and the electro muzzle flash is emitted. C# fallbacks updated to the Base cfg defaults so values:partial→faithful. Still OPEN: the rapid-fire ramp (logic/timing:partial), the missing owner-velocity add, and the unmodeled g_rm_laser==2/forced-reload/spread_random paths. presentation:missing tracked separately in laser.projectile_render."

  - id: mutator-rocketminsta.laser.projectile_render
    name: "RM laser projectile networking, model, trail, and team color"
    layer: [presentation]
    base_refs:
      - "common/weapons/weapon/vaporizer.qc:272 (CSQCProjectile PROJECTILE_ROCKETMINSTA_LASER)"
      - "common/weapons/projectiles.qh (PROJECTILE_ROCKETMINSTA_LASER)"
      - "common/models/all.inc (models/elaser.mdl)"
      - "client/weapons/projectile.qc (colormod = team palette when colormap>0)"
      - "common/effects/all.inc (EFFECT_ROCKETMINSTA_LASER rocketminsta_laser_neutral)"
    port_refs:
      - "src/XonoticGodot.Common/Gameplay/Effects/EffectsList.cs:250 (ROCKETMINSTA_LASER trail registered)"
    constants:
      - { name: "projectile id", base: "PROJECTILE_ROCKETMINSTA_LASER", port: "none (not networked as a projectile)", match: false }
      - { name: "model", base: "models/elaser.mdl", port: "none", match: false }
      - { name: "trail effect", base: "EFFECT_ROCKETMINSTA_LASER", port: "registered name only (EffectsList.cs:250); not emitted by a bolt", match: false }
      - { name: "team colormod", base: "colormapPaletteColor(colormap & 0x0F)", port: "none", match: false }
    status:
      logic: na
      values: na
      timing: na
      presentation: missing
      audio: na
      liveness: dead
    intended_divergence: false
    divergence_rationale: null
    gaps:
      - "RM laser bolts are invisible: no CSQCProjectile/entity networking, no models/elaser.mdl, no EFFECT_ROCKETMINSTA_LASER trail attached, no team-color colormod. The trail effect string is registered (EffectsList.cs:241) but nothing emits it for these bolts."
    verification:
      - { kind: code, ref: "Vaporizer.cs:223-254 RocketMinstaLaserBarrage spawns a headless damage entity (plasma_prim, no SetModel, no CSQCProjectile, no trail emit); only the effect string is registered (EffectsList.cs:250). The ELECTRO_MUZZLEFLASH at the muzzle (Vaporizer.cs:219) is the firing-point flash, not the per-bolt model/trail; the bolts themselves remain unrendered. Re-verified 2026-06-22: no RM laser projectile networking/model/trail/colormod added in Wave 5 (grep CSQCProjectile/PROJECTILE_ROCKETMINSTA/elaser — none on the RM path).", result: fail }
    confidence: high
    notes: "STILL OPEN. liveness:dead — the registered trail effect has no emitter on the RM-laser path. Base networks each bolt as PROJECTILE_ROCKETMINSTA_LASER and tints it to the firer's team. (The new muzzle flash is the firing-point flash, not the per-bolt model/trail.)"

  - id: mutator-rocketminsta.laser.electrobitch
    name: "Electrobitch achievement on flying-enemy laser timeout-explode"
    layer: [authority]
    base_refs:
      - "common/weapons/weapon/vaporizer.qc:194-199 (W_RocketMinsta_Laser_Explode → ANNCE_ACHIEVEMENT_ELECTROBITCH)"
    port_refs: []
    constants:
      - { name: "trigger", base: "timeout-explode hits DAMAGE_AIM player on diff team, not dead, IsFlying → ANNCE_ACHIEVEMENT_ELECTROBITCH to realowner", port: "none", match: false }
    status:
      logic: missing
      values: na
      timing: na
      presentation: na
      audio: missing
      liveness: na
    intended_divergence: false
    divergence_rationale: null
    gaps:
      - "The Electrobitch announcer achievement (RM laser timeout-explode landing on a flying enemy of another team) is not implemented. The port's laser Touch/Think paths just deal RadiusDamage and delete — no achievement check, and the touch/timeout paths are not distinguished (Base only awards on the timeout .use→W_RocketMinsta_Laser_Explode path, not the direct W_RocketMinsta_Laser_Touch)."
    verification:
      - { kind: code, ref: "Vaporizer.cs:241-249 RocketMinstaLaserBarrage Touch/Think both call RadiusDamage + Remove with no ANNCE_ACHIEVEMENT_ELECTROBITCH / Send_Notification equivalent", result: fail }
    confidence: high
    notes: "STILL OPEN. Minor achievement parity gap, unchanged by Wave 1-2. Base distinguishes touch (no achievement) from timeout (achievement); the port collapses both into the same RadiusDamage."

  - id: mutator-rocketminsta.instagib.downgrade
    name: "Out-of-ammo downgrade (no instagib death countdown) when g_rm + g_rm_laser"
    layer: [authority]
    base_refs:
      - "common/mutators/mutator/instagib/sv_instagib.qc:79-92 (instagib_ammocheck g_rm&&g_rm_laser branch)"
    port_refs:
      - "src/XonoticGodot.Common/Gameplay/Mutators/InstagibMutator.cs:267-299 (AmmoCheck rocketMinsta branch)"
      - "src/XonoticGodot.Common/Gameplay/Mutators/InstagibMutator.cs:260-264 (OnPlayerPreThink)"
      - "src/XonoticGodot.Server/GameWorld.cs:1037 (PlayerPreThink.Call)"
    constants:
      - { name: "branch predicate", base: "autocvar_g_rm && autocvar_g_rm_laser", port: "GetFloat(\"g_rm\")!=0 && GetFloat(\"g_rm_laser\")!=0", match: true }
      - { name: "behavior", base: "skip countdown; CENTER_INSTAGIB_DOWNGRADE once (instagib_needammo); keep fighting with laser", port: "no Countdown; NotificationSystem.Center(\"INSTAGIB_DOWNGRADE\") gated on !InstagibNeedAmmo", match: true }
      - { name: "branch order", base: "dead/stopped → cells/unlimited/godmode → rm&&rm_laser → else countdown", port: "same order (InstagibMutator.cs:280-297)", match: true }
    status:
      logic: faithful
      values: na
      timing: faithful
      presentation: faithful
      audio: na
      liveness: live
    intended_divergence: false
    divergence_rationale: null
    gaps: []
    verification:
      - { kind: code, ref: "Wave-2 FIX: InstagibMutator.AmmoCheck now has the `else if (rocketMinsta)` branch (line 284-292) between the cells>0 check and Countdown, matching Base sv_instagib.qc:79 exactly (same order, same g_rm&&g_rm_laser predicate, same CENTER_INSTAGIB_DOWNGRADE centerprint gated on !needammo). AmmoCheck runs from OnPlayerPreThink, dispatched live at GameWorld.cs:1037; InstagibMutator subscribes PlayerPreThink and gates on g_instagib.", result: pass }
    confidence: high
    notes: "FIXED. A cell-less player in RM-laser mode is now downgraded (no instagib death countdown, INSTAGIB_DOWNGRADE centerprint shown once) instead of bleeding out, so they keep fighting with lasers — matching Base. The branch is live on the real PlayerPreThink path."
