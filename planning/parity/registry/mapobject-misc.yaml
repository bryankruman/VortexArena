unit: mapobject-misc
category: mapobject
base_ref_rev: "v0.8.6-1779-g863cd3e84"
last_audited: "2026-06-22"
summary:
  feature_count: 12
  worst_gaps:
    - "dynlight renders no light: server state machine runs (incl. full path-travel) but there is still no realtime dynamic-light consumer, so authored map lights are invisible"
    - "func_clientwall distance-fade not rendered: fade_start/fade_end/alpha_max/alpha_min/fade_vertical_offset + PVS alpha-cull (Ent_Wall_PreDraw) are stored on the entity but no renderer applies them, and the antiwall solid-toggle relay (g_clientmodel_use) is unported"
    - "bgmscript ADSR not ported: func_clientwall/func_pointparticles never animate alpha/position/emission to the map music"
    - "item_key pickup centerprint is silently dropped: KeySetup/KeyTouch grant the keys (live) but the CenterprintHandler sink is never wired, so the 'You've picked up the X key!' message no-ops; MF_ROTATE key spin is also dropped (no Entity.ModelFlags)"
features:
  - id: mapobject-misc.laser.server_logic
    name: "misc_laser damage + detector think (aim, trace, team gate, enter/exit latch)"
    layer: [authority]
    base_refs:
      - "common/mapobjects/misc/laser.qc:misc_laser_think"
      - "common/mapobjects/misc/laser.qc:misc_laser_aim"
      - "common/mapobjects/misc/laser.qc:spawnfunc(misc_laser)"
    port_refs:
      - "src/XonoticGodot.Common/Gameplay/MapObjects/Laser.cs:LaserThink"
      - "src/XonoticGodot.Common/Gameplay/MapObjects/Laser.cs:LaserAim"
      - "src/XonoticGodot.Common/Gameplay/MapObjects/Laser.cs:LaserSetup"
    constants:
      - { name: "LASER_BEAM_MAXLENGTH", base: "32768", port: "32768", match: true }
      - { name: "LASER_BEAM_MAXWORLDSIZE", base: "1048576", port: "1048576", match: true }
      - { name: "LASER_FINITE", base: "BIT(1)", port: "1<<1", match: true }
      - { name: "LASER_NOTRACE", base: "BIT(2)", port: "1<<2", match: true }
      - { name: "LASER_INVERT_TEAM", base: "BIT(3)", port: "1<<3", match: true }
      - { name: "dmg<0 instakill amount", base: "100000", port: "100000", match: true }
      - { name: "team_damage_gate", base: "((sf&INVERT)==0)==(team!=hit.team)", port: "verbatim", match: true }
    status:
      logic: faithful
      values: faithful
      timing: faithful
      presentation: na
      audio: na
      liveness: live
    intended_divergence: true
    divergence_rationale: "End effect is resolved by NAME (Entity.LaserEndEffect) instead of QC's particle-effect integer .cnt, and the detector latch uses a dedicated Entity.LaserHitLatch bool instead of QC's .count field (a mapper-set count no longer pre-latches). No shipped map uses a numeric cnt key. Net_LinkEntity/laser_SendEntity is dropped (the client reads the shared entity off the ambient facade). Documented in Laser.cs."
    gaps: []
    verification:
      - { kind: code, ref: "Laser.cs unchanged by Wave-1/2 (git diff main..HEAD = empty); LASER_BEAM_MAXLENGTH=32768 / MAXWORLDSIZE=1048576 confirmed vs laser.qh:32,34 — aim, endpoint, trace gate, detector enter/exit latch, INVERT team truth-table, dmg<0 instakill, dmg*frametime, DEATH_HURTTRIGGER->Void all match", result: pass }
      - { kind: code, ref: "MapObjectsRegistry.cs:135 registers misc_laser -> Laser.LaserSetup", result: pass }
    confidence: high
    notes: "VERIFIED unchanged by Wave-1/2 (Laser.cs has no diff vs main). Counterintuitive team gate (non-inverted team laser damages SAME-team touchers) mirrored verbatim. INITPRIO_FINDTARGET enemy lookup deferred to first active think rather than a spawn pass — same net effect."

  - id: mapobject-misc.laser.beam_render
    name: "misc_laser beam + end effect + dynamic light (Draw_Laser)"
    layer: [presentation]
    base_refs:
      - "common/mapobjects/misc/laser.qc:Draw_Laser"
    port_refs:
      - "game/client/LaserRenderer.cs:LaserRenderer"
    constants:
      - { name: "beam texture", base: "particles/laserbeam", port: "particles/laserbeam.tga", match: true }
      - { name: "sliding texcoord", base: "time*3", port: "Uv1Offset = -now*3 (animated, tiled along beam)", match: true }
      - { name: "end fx SKY/NOIMPACT suppress", base: "Draw_Laser:333 sky||noimpact => no particles/dlight", port: "hitSurface gated on !sky && !noImpact", match: true }
      - { name: "end fx rate", base: "drawframetime*1000 every frame", port: "throttled ~10 Hz (EndFxInterval=0.1)", match: false }
      - { name: "additive when no alpha", base: "alpha 0.5 DRAWFLAG_ADDITIVE", port: "approximated (AlphaKey>0 normal else additive 0.5)", match: false }
    status:
      logic: faithful
      values: partial
      timing: partial
      presentation: partial
      audio: na
      liveness: live
    intended_divergence: false
    divergence_rationale: null
    gaps:
      - "End-point particle bursts are still throttled to ~10 Hz per laser (EndFxInterval=0.1f) rather than emitted every frame (drawframetime*1000)."
      - "Client trace runs at the server tick origin (no InterpolateOrigin_Do) — negligible since stock-map lasers are static."
    verification:
      - { kind: code, ref: "LaserRenderer.cs:256-261 Uv1Scale tiling + Uv1Offset=-now*TexScrollRate(3); LaserRenderer.cs:216-221 hitSurface gated on Q3SURFACEFLAG_SKY|NOIMPACT; EndFxInterval=0.1f still present at line 43", result: pass }
    confidence: medium
    notes: "PARTIAL FIX (Wave-2), re-confirmed this pass. The two baseline gaps that were code-fixable are closed: the beam texcoord scrolls (time*3 via per-frame Uv1Offset, tiled along the ribbon) and the end fx are suppressed on SKY *and* NOIMPACT faces (Draw_Laser:333). Residual partials: end-fx still ~10Hz not per-frame, no InterpolateOrigin. Live on listen-server/demo via ambient-facade scan; idles on a pure --connect client. Not observed in-game this pass (medium confidence on the actual visual)."

  - id: mapobject-misc.follow.attach_follow
    name: "misc_follow attach / MOVETYPE_FOLLOW ride at spawn"
    layer: [authority]
    base_refs:
      - "common/mapobjects/misc/follow.qc:follow_init"
      - "common/mapobjects/misc/follow.qc:spawnfunc(misc_follow)"
      - "lib/util.qc:attach_sameorigin"
    port_refs:
      - "src/XonoticGodot.Common/Gameplay/MapObjects/Follow.cs:FollowInit"
      - "src/XonoticGodot.Common/Gameplay/MapObjects/Follow.cs:FollowSetup"
      - "src/XonoticGodot.Common/Gameplay/MapObjects/Follow.cs:AttachSameOrigin"
    constants:
      - { name: "FOLLOW_ATTACH", base: "BIT(0)", port: "1<<0", match: true }
      - { name: "FOLLOW_LOCAL", base: "BIT(1)", port: "1<<1", match: true }
      - { name: "tagscale = vlen(v_forward)^-2", base: "1/(flen*flen)", port: "1f/(flen*flen)", match: true }
      - { name: "t_left = v_right * -tagscale", base: "yes", port: "vRight * -tagscale", match: true }
    status:
      logic: faithful
      values: faithful
      timing: faithful
      presentation: faithful
      audio: na
      liveness: live
    intended_divergence: false
    divergence_rationale: null
    gaps: []
    verification:
      - { kind: code, ref: "Follow.cs:80-133 vs follow.qc:5-64 — jointtype-persist, !src&&!dst guard, FOLLOW_ATTACH/LOCAL branches, dst.solid=SOLID_NOT, follow_sameorigin relative view_ofs/v_angle all match", result: pass }
      - { kind: code, ref: "Follow.cs:151-196 AttachSameOrigin vs util.qc:2057-2095 — gettaginfo tag pose, tagscale, tag-local origin dots, fixedmakevectors pitch-flip + FixedVecToAngles2 round trip; headless fallback to bare parent link when no model service", result: pass }
      - { kind: code, ref: "MapObjectsRegistry.cs:192 misc_follow -> Follow.FollowSetup; RunPostSpawn (line 323) drains Follow.RunDeferredInit; RunPostSpawn called on the live server path at GameWorld.cs:575", result: pass }
    confidence: high
    notes: "FIXED (Wave-2), re-confirmed. The baseline gap (tag-relative origin/angle bake deferred) is closed: AttachSameOrigin bakes the entity into the parent tag's local space via Api.Models.TryGetTag + QMath.FixedVecToAngles2, with a documented headless fallback to the bare parent link. Deferred to INITPRIO_FINDTARGET via the pending-init queue drained in RunPostSpawn (live on the real match path)."

  - id: mapobject-misc.dynlight.server_state
    name: "dynlight static/follow/tag/path modes + toggle + reset (server)"
    layer: [authority]
    base_refs:
      - "common/mapobjects/misc/dynlight.qc:spawnfunc(dynlight)"
      - "common/mapobjects/misc/dynlight.qc:dynlight_find_aiment"
      - "common/mapobjects/misc/dynlight.qc:dynlight_find_target"
      - "common/mapobjects/misc/dynlight.qc:dynlight_find_path"
      - "common/mapobjects/misc/dynlight.qc:dynlight_use"
    port_refs:
      - "src/XonoticGodot.Common/Gameplay/MapObjects/DynamicLight.cs:DynlightSetup"
      - "src/XonoticGodot.Common/Gameplay/MapObjects/DynamicLight.cs:FindAiment"
      - "src/XonoticGodot.Common/Gameplay/MapObjects/DynamicLight.cs:FindTarget"
      - "src/XonoticGodot.Common/Gameplay/MapObjects/DynamicLight.cs:FindPath"
      - "src/XonoticGodot.Common/Gameplay/MapObjects/DynamicLight.cs:PathNext"
      - "src/XonoticGodot.Common/Gameplay/MapObjects/DynamicLight.cs:PathWait"
      - "src/XonoticGodot.Common/Gameplay/MapObjects/DynamicLight.cs:DynlightUse"
    constants:
      - { name: "light_lev default", base: "200", port: "200", match: true }
      - { name: "color default", base: "'1 1 1'", port: "(1,1,1)", match: true }
      - { name: "path speed default", base: "100", port: "100", match: true }
      - { name: "think interval", base: "0.1s", port: "0.1s", match: true }
      - { name: "START_OFF / NOSHADOW / FOLLOW", base: "BIT(0)/BIT(1)/BIT(2)", port: "1<<0/1<<1/1<<2", match: true }
      - { name: "path-follow corner walk", base: "setthink(train_next) — travels chain", port: "PathNext/PathWait walk corner chain via CalcMove(Linear)", match: true }
    status:
      logic: faithful
      values: faithful
      timing: faithful
      presentation: na
      audio: na
      liveness: live
    intended_divergence: true
    divergence_rationale: "The path-following branch reimplements a dynlight-specialised train_next/train_wait inline (PathNext/PathWait) on the public MapMover.CalcMove seam rather than calling MovingBrushes.TrainNext, because a dynlight is a zero-size point light that exposes none of the bbox/TRAIN_CURVE/TRAIN_TURN/NEEDACTIVATION keys the full func_train pather handles. Net behavior (travel corner chain at .speed, fire each corner's targets, honour per-corner .wait/.speed) matches QC's train_next reuse. Documented in DynamicLight.cs header."
    gaps: []
    verification:
      - { kind: code, ref: "DynamicLight.cs:69-291 vs dynlight.qc:39-156 — defaults (light_lev=200, color='1 1 1', speed=100, nextthink time+0.1 all confirmed in Base), four-mode dispatch, FindAiment/FindTarget MOVETYPE_FOLLOW+owner+view_ofs, DynlightUse/SetActive toggle, DynlightReset all match", result: pass }
      - { kind: code, ref: "DynamicLight.cs:177-237 FindPath/PathNext/PathWait vs dynlight.qc:62-73 (setthink train_next) — FindFirstByTargetName lookahead, target chain advance, per-corner wait/speed, UseTargets on arrival; toggle (.Use) + spin (avelocity) preserved independent of think", result: pass }
      - { kind: code, ref: "MapObjectsRegistry.cs:187 dynlight registered; RunPostSpawn (line 321) drains DynamicLight.RunDeferredInit; RunPostSpawn is called on the live path at GameWorld.cs:575", result: pass }
    confidence: high
    notes: "FIXED (Wave-2), re-confirmed. The baseline park-on-first-corner gap is closed: a path-following dynlight now travels the full path_corner chain (PathNext/PathWait built on CalcMove). Static/FOLLOW/tag-attach/toggle/reset/spin remain faithful and live. The render half is a separate feature (dynlight.render) and is still missing."

  - id: mapobject-misc.dynlight.render
    name: "dynlight realtime light (radius/color/style render)"
    layer: [presentation]
    base_refs:
      - "common/mapobjects/misc/dynlight.qc:18 (QUAKED: light_lev/color/style realtime light)"
    port_refs: []
    constants: []
    status:
      logic: na
      values: na
      timing: na
      presentation: missing
      audio: na
      liveness: na
    intended_divergence: false
    divergence_rationale: null
    gaps:
      - "No DP-style realtime dynamic-light consumer: a dynlight now ticks/toggles/follows/TRAVELS on the server but still emits no visible light. Where a map authored a moving or pulsing light, the player sees nothing."
    verification:
      - { kind: code, ref: "DynamicLight.cs:18-21 header still states 'There is NO dlight RENDER consumer yet'; grep OmniLight3D across game/client/: only LaserRenderer end-fx light + csqc-fx/projectile/viewmodel/warm-fx lights, no dynlight spawn", result: fail }
    confidence: high
    notes: "STILL OPEN. Deferred to T4 realtime-light territory per the port header; unchanged by Wave-1/2. Base's pflags lines are commented out, but the DP engine still drives a realtime light from light_lev/color/style — the port has neither."

  - id: mapobject-misc.keys.item_key
    name: "item_key* pickup keys (spawn, touch, grant itemkeys, model rotate)"
    layer: [authority]
    base_refs:
      - "common/mapobjects/misc/keys.qc:spawnfunc(item_key)"
      - "common/mapobjects/misc/keys.qc:item_key_touch"
      - "common/mapobjects/misc/keys.qc:spawn_item_key"
    port_refs:
      - "src/XonoticGodot.Common/Gameplay/MapObjects/MapObjectsRegistry.cs:KeySetup"
      - "src/XonoticGodot.Common/Gameplay/MapObjects/MapObjectsRegistry.cs:SpawnItemKey"
      - "src/XonoticGodot.Common/Gameplay/MapObjects/MapObjectsRegistry.cs:KeyTouch"
    constants:
      - { name: "default model", base: "models/keys/key.md3", port: "models/keys/key.md3", match: true }
      - { name: "GOLD/SILVER/BRONZE ids", base: "BIT(0)/BIT(1)/BIT(2)", port: "1<<0/1<<1/1<<2", match: true }
      - { name: "RED/BLUE/GREEN keycards", base: "BIT(3)/BIT(4)/BIT(5)", port: "1<<3/1<<4/1<<5", match: true }
      - { name: "MASTER key", base: "0xffffff", port: "0xffffff", match: true }
      - { name: "colormod GOLD/SILVER/BRONZE", base: "'1 .9 0'/'.9 .9 .9'/'.6 .25 0'", port: "(1,.9,0)/(.9,.9,.9)/(.6,.25,0)", match: true }
      - { name: "colormod RED/BLUE/GREEN/MASTER", base: "'.9 0 0'/'0 0 .9'/'0 .9 0'/'1 .25 .25'", port: "verbatim", match: true }
      - { name: "key bbox", base: "'-16 -16 -56'..'16 16 0' +32z", port: "(-16,-16,-56)..(16,16,0) +32z", match: true }
      - { name: "item_key1->SILVER / item_key2->GOLD (legacy swap)", base: "BIT(1)/BIT(0)", port: "1<<1/1<<0", match: true }
      - { name: "modelflags MF_ROTATE", base: "set", port: "dropped (no Entity.ModelFlags)", match: false }
      - { name: "EF_LOWPRECISION", base: "set", port: "EfLowPrecision (4194304)", match: true }
      - { name: "FLOATING spawnflag -> noalign -> MOVETYPE_NONE else TOSS", base: "yes", port: "yes", match: true }
      - { name: "pickup sound", base: "play2(toucher, SND(ITEMPICKUP))", port: "MapMover.Sound(toucher, TriggerAuto, .Noise)", match: true }
      - { name: "pickup centerprint", base: "centerprint(toucher, message)", port: "MapMover.Centerprint (handler unwired -> no-op)", match: false }
    status:
      logic: faithful
      values: faithful
      timing: faithful
      presentation: partial
      audio: faithful
      liveness: live
    intended_divergence: false
    divergence_rationale: null
    gaps:
      - "item_key pickup centerprint ('You've picked up the X key!') is silently dropped: MapMover.Centerprint(toucher, this.Message) IS called (MapObjectsRegistry.cs:441) but MapObjectsCommon.CenterprintHandler (declared MapObjectsCommon.cs:782) is never assigned anywhere in game/ or src/ (grep = 0 assignments), so Centerprint no-ops on the live path."
      - "MF_ROTATE key spin is dropped — the port's Entity carries no ModelFlags field (grep for ModelFlags/MF_ROTATE on Entity = none), so the key model sits static instead of rotating (documented at SpawnItemKey, MapObjectsRegistry.cs:413)."
      - "item_keys_names[lowestbit] save (for keylist display strings) and the Q3COMPAT_COMMON QL +8z origin nudge are omitted (no Q3-compat global; the latter documented)."
    verification:
      - { kind: code, ref: "MapObjectsRegistry.cs:154-159 registers item_key/item_key1/item_key2/item_key_gold/silver/master with correct BIT pre-stamps; KeySetup/SpawnItemKey/KeyTouch (lines 349-448) vs keys.qc:77-264 — multi-bit reject (excl. master), per-id netname/colormod, bbox, TOSS/NONE, touch grant + fire-target-once-with-blanked-message all match", result: pass }
      - { kind: code, ref: "consumer chain consistent: KeyTouch grants toucher.ItemKeys (line 439); Doors.cs and Triggers.cs read player/toucher.ItemKeys (same Entity.ItemKeys field) — a granted key now actually opens a keylock/key-gated door", result: pass }
      - { kind: grep, ref: "CenterprintHandler: only declaration at MapObjectsCommon.cs:782, NO assignment anywhere outside planning/ -> Centerprint no-ops; ModelFlags/MF_ROTATE: no Entity field", result: fail }
    confidence: high
    notes: "MOSTLY FIXED (Wave-2), re-confirmed. The baseline worst-gap (keys never spawn, key-gated areas permanently locked) is closed: all six key classnames are registered, the grant logic + pickup sound are live, and the consumer chain (Doors/Triggers reading the same Entity.ItemKeys) means a picked-up key actually unlocks. Residual presentation gaps: the centerprint sink is unwired (host never assigns CenterprintHandler) and MF_ROTATE spin is dropped. Touch uses toucher.ItemKeys where QC uses PS(toucher).itemkeys, but the port consolidates both onto Entity.ItemKeys consistently across producer and consumer."

  - id: mapobject-misc.teleport_dest.marker
    name: "info_teleport_destination / misc_teleporter_dest endpoint"
    layer: [authority]
    base_refs:
      - "common/mapobjects/misc/teleport_dest.qc:spawnfunc(info_teleport_destination)"
      - "common/mapobjects/misc/teleport_dest.qc:spawnfunc(misc_teleporter_dest)"
    port_refs:
      - "src/XonoticGodot.Common/Gameplay/MapObjects/Teleporters.cs:TeleportDestSetup"
    constants:
      - { name: "facing stored in mangle, angles cleared", base: "yes", port: "yes", match: true }
    status:
      logic: faithful
      values: faithful
      timing: na
      presentation: na
      audio: na
      liveness: live
    intended_divergence: false
    divergence_rationale: null
    gaps: []
    verification:
      - { kind: code, ref: "Teleporters.cs TeleportDestSetup unchanged by the Wave-2 Teleporters diff (diff does not touch TeleportDestSetup) vs teleport_dest.qc:29-49 — mangle=angles, angles cleared, setorigin, index", result: pass }
      - { kind: code, ref: "MapObjectsRegistry.cs:116-117 registers both classnames -> TeleportDestSetup", result: pass }
    confidence: high
    notes: "VERIFIED unchanged by Wave-1/2 (the Teleporters.cs Wave-2 diff does not touch TeleportDestSetup). Passive findable edict (indexed by targetname); the reorient+reproject is consumed by the trigger_teleport path. CSQC net-link unnecessary (shared-entity seam)."

  - id: mapobject-misc.corner.path_corner
    name: "path_corner waypoint + per-corner platmovetype override (set_platmovetype)"
    layer: [authority]
    base_refs:
      - "common/mapobjects/misc/corner.qc:spawnfunc(path_corner)"
      - "common/mapobjects/platforms.qc:set_platmovetype"
    port_refs:
      - "src/XonoticGodot.Common/Gameplay/MapObjects/MovingBrushes.cs:PathCornerSetup"
      - "src/XonoticGodot.Common/Gameplay/MapObjects/MapObjectsCommon.cs:SetPlatMoveType"
    constants:
      - { name: "platmovetype string parse -> platmovetype_start/end", base: "set_platmovetype(this, platmovetype) tokenizes up to 2 floats + optional 'force'", port: "MapMover.SetPlatMoveType(this, Platmovetype) — same tokenizer", match: true }
      - { name: "one-token sets both ends", base: "n==1 -> end=start", port: "PlatMoveEnd = n>1 ? argv[1] : PlatMoveStart", match: true }
      - { name: "cubic_speedfunc_is_sane reject", base: "lib/math.qh:138", port: "CubicSpeedFuncIsSane (verbatim ellipse condition)", match: true }
    status:
      logic: faithful
      values: faithful
      timing: faithful
      presentation: na
      audio: na
      liveness: live
    intended_divergence: false
    divergence_rationale: null
    gaps: []
    verification:
      - { kind: code, ref: "MovingBrushes.cs:246-252 PathCornerSetup now calls MapMover.SetPlatMoveType(this, Platmovetype) before origin/index; func_train TrainNext (line 363-372) restores the corner's PlatMoveStart/End or re-parses the train's own key — matches corner.qc:44-51 + train_next", result: pass }
      - { kind: code, ref: "MapObjectsCommon.cs:909-940 SetPlatMoveType vs platforms.qc:209-235 — stof tokenizer, n==0/1/2 branches, 'force' skip, CubicSpeedFuncIsSane gate (false return = reject mover); Platmovetype promoted onto the edict BEFORE the spawnfunc runs", result: pass }
    confidence: high
    notes: "FIXED (Wave-2), re-confirmed. The baseline gap (set_platmovetype unported, corner platmovetype string ignored) is closed: PathCornerSetup now parses the corner's per-corner ease/turn string, and func_train's TrainNext applies the corner override or restores the train default. SetPlatMoveType is also wired into func_plat and func_train spawnfuncs."

  - id: mapobject-misc.subs.calcmove
    name: "SUB_CalcMove / SUB_CalcAngleMove mover driver (linear + bezier + cubic_speedfunc)"
    layer: [shared]
    base_refs:
      - "common/mapobjects/subs.qc:SUB_CalcMove"
      - "common/mapobjects/subs.qc:SUB_CalcMove_Bezier"
      - "common/mapobjects/subs.qc:SUB_CalcMove_controller_think"
      - "common/mapobjects/subs.qc:SUB_CalcAngleMove"
    port_refs:
      - "src/XonoticGodot.Common/Gameplay/MapObjects/MapObjectsCommon.cs:CalcMove"
      - "src/XonoticGodot.Common/Gameplay/MapObjects/MapObjectsCommon.cs:CalcMoveBezier"
      - "src/XonoticGodot.Common/Gameplay/MapObjects/MapObjectsCommon.cs:CalcMoveControllerThink"
      - "src/XonoticGodot.Common/Gameplay/MapObjects/MapObjectsCommon.cs:CalcAngleMove"
    constants:
      - { name: "linear cutoff", base: "traveltime < 0.15", port: "< 0.15f", match: true }
      - { name: "platmovetype_start/end DEFAULT", base: "0/0 (QC float default) => smoothstep ease cubic_speedfunc(0,0,t) = -2t^3+3t^2", port: "PlatMoveStart=0/PlatMoveEnd=0 (field default) => smoothstep ease", match: true }
      - { name: "explicit-linear platmovetype", base: "start==1 && end==1", port: "PlatMoveStart==1 && PlatMoveEnd==1", match: true }
      - { name: "non-positive traveltime fallback", base: "0.001", port: "0.001f", match: true }
      - { name: "angle short cutoff", base: "traveltime < 0.1", port: "< 0.1f", match: true }
      - { name: "controller overshoot", base: "+ '0 0 0.125'", port: "+ (0,0,0.125)", match: true }
      - { name: "cubic_speedfunc", base: "(((s+e-2)x -2s-e+3)x +s)x", port: "verbatim", match: true }
    status:
      logic: faithful
      values: faithful
      timing: faithful
      presentation: na
      audio: na
      liveness: live
    intended_divergence: false
    divergence_rationale: null
    gaps: []
    verification:
      - { kind: code, ref: "MapObjectsCommon.cs:125-126 PlatMoveStart/End default 0 (uninitialized, matching QC float default) — the (start==1 && end==1) linear shortcut (line 548) FAILS by default, so long movers run the bezier branch with cubic_speedfunc(0,0,t) = -2t^3+3t^2 (smoothstep ease), exactly as Base", result: pass }
      - { kind: code, ref: "MapObjectsCommon.cs:527-707 CalcMove/CalcMoveBezier/CalcMoveControllerThink/CalcAngleMove vs subs.qc:114-389 — branch split, controller sub-entity, cubic_speedfunc, .ltime scheduling, SUB_CalcMoveDone snap+think1 all faithful; SetPlatMoveType now feeds non-default curves from a mapper platmovetype key", result: pass }
    confidence: high
    notes: "FIXED (Wave-2), re-confirmed. The baseline default-easing divergence is closed: PlatMoveStart/End default to 0/0 (matching QC's uninitialized float fields), so every stock door/plat/train >= 0.15s move runs the smoothstep S-curve instead of forced linear. A mapper-set platmovetype key flows through SetPlatMoveType (see corner.path_corner). Used by every door/plat/train/button — the most-live feature in the unit, now timing/values-faithful. Curve correctness verified by code; in-game feel not separately observed (severity was cosmetic anyway)."

  - id: mapobject-misc.bgmscript.adsr
    name: "bgmscript map-music ADSR animation (func_clientwall / func_pointparticles)"
    layer: [presentation]
    base_refs:
      - "client/bgmscript.qc:doBGMScript"
      - "client/bgmscript.qc:BGMScript_InitEntity"
      - "common/mapobjects/models.qc:Ent_Wall_Draw"
    port_refs: []
    constants:
      - { name: "bgmscriptsustain default", base: "1", port: "n/a", match: false }
      - { name: "alpha drive (lip<0)", base: "alpha = 1 + lip*f", port: "n/a", match: false }
      - { name: "alpha drive (lip>0)", base: "alpha = 1 - lip*(1-f)", port: "n/a", match: false }
      - { name: "position/angle drive", base: "(fld) += movedir*f", port: "n/a", match: false }
      - { name: "gate", base: "autocvar_bgmvolume>0 && bgmtime>=0", port: "n/a", match: false }
    status:
      logic: missing
      values: missing
      timing: missing
      presentation: missing
      audio: na
      liveness: na
    intended_divergence: false
    divergence_rationale: null
    gaps:
      - "No bgmscript ADSR envelope: a func_clientwall / func_pointparticles carrying a .bgmscript key does not animate its alpha / origin / angles / emission to the map's .bgs music script — the prop is static instead of pulsing to the soundtrack. doBGMScript / BGMScript_InitEntity / the .bgs loader are absent."
    verification:
      - { kind: code, ref: "grep bgmscript/doBGMScript/Ent_Wall_PreDraw across game/ + src/: only documented as out-of-scope (MapModels.cs:11-12, MapParticleEmitters.cs:19, PointParticles.cs:14); no consumer added by Wave-1/2", result: fail }
    confidence: high
    notes: "STILL OPEN. Explicitly scoped out in the MapModels.cs / PointParticles.cs / MapParticleEmitters.cs headers as a documented residual ('bgmscript ADSR is out of scope — no shipped stock map depends on it'). Unchanged by Wave-1/2."

  - id: mapobject-misc.models.props_walls
    name: "models.qc decoration props + static walls (spawn/solid/drop/colormap)"
    layer: [authority, presentation]
    base_refs:
      - "common/mapobjects/models.qc:g_model_init"
      - "common/mapobjects/models.qc:g_model_dropbyspawnflags"
      - "common/mapobjects/models.qc:g_model_setcolormaptoactivator"
      - "common/mapobjects/models.qc:spawnfunc(func_wall) etc."
    port_refs:
      - "src/XonoticGodot.Common/Gameplay/MapObjects/MapModels.cs:GModelInit"
      - "src/XonoticGodot.Common/Gameplay/MapObjects/MapModels.cs:DropBySpawnflags"
      - "src/XonoticGodot.Common/Gameplay/MapObjects/MapModels.cs:SetColormapToActivator"
    constants:
      - { name: "ALIGN_ORIGIN/BOTTOM spawnflags&3", base: "1/2/3", port: "1/2/3", match: true }
      - { name: "drop trace distance", base: "4096", port: "4096f", match: true }
      - { name: "misc_* pitch flip", base: "angles_x = -angles_x", port: "FlipPitch", match: true }
      - { name: "colormap teamplay", base: "(team-1)*0x11 | BIT(10)", port: "(team-1)*0x11 | 1<<10", match: true }
      - { name: "solid<0 -> SOLID_NOT", base: "yes", port: "SolidOverride<0 -> Solid.Not", match: true }
    status:
      logic: faithful
      values: faithful
      timing: faithful
      presentation: partial
      audio: na
      liveness: live
    intended_divergence: true
    divergence_rationale: "The g_clientmodel_* Net_LinkEntity (ENT_CLIENT_WALL) stream is omitted: the listen-server/demo client reads the shared entity off the ambient facade (the seam every client fx uses). No shipped stock map depends on the antiwall relay or bgmscript on these. Documented in MapModels.cs."
    gaps:
      - "Colormap value is stored on Entity.ColorMapOverride but render consumption (team-tinting the prop) is a follow-up seam — a colormapped prop is not actually recolored on screen yet (grep ColorMapOverride: only stored at MapModels.cs/MapObjectFieldsExtra.cs, no renderer consumer)."
    verification:
      - { kind: code, ref: "MapModels.cs unchanged by Wave-1/2 (git diff main..HEAD = empty) vs models.qc:17-218; all classnames registered in MapObjectsRegistry.cs; solid/SetModel order, DropBySpawnflags 3 branches, colormap teamplay path all match", result: pass }
    confidence: high
    notes: "VERIFIED unchanged by Wave-1/2 (MapModels.cs has no diff vs main). func_wall/func_clientwall/func_static get Solid.Bsp + inline brush via SetModel. DropToFloor runs inline. The colormap-render/clientwall-fade/bgmscript pieces are separate presentation features."

  - id: mapobject-misc.models.clientwall_fade
    name: "func_clientwall distance-fade + PVS alpha-cull + antiwall solid-toggle (Ent_Wall_PreDraw / g_clientmodel_use)"
    layer: [presentation, authority]
    base_refs:
      - "common/mapobjects/models.qc:Ent_Wall_PreDraw"
      - "common/mapobjects/models.qc:g_clientmodel_use"
    port_refs:
      - "src/XonoticGodot.Common/Gameplay/MapObjects/MapObjectFieldsExtra.cs:FadeStartDist"
    constants:
      - { name: "alpha = (alpha_min + alpha_max * bound(0,(fade_end-dist)/(fade_end-fade_start),1)) / 100", base: "yes", port: "fields stored, formula NOT applied", match: false }
      - { name: "single-distance cut (fade_end==fade_start)", base: "alpha=0 past fade_start", port: "not applied", match: false }
      - { name: "PVS cull -> alpha 0", base: "!checkpvs => alph=0", port: "not applied", match: false }
      - { name: "inactive (antiwall) -> alpha 0 / solid toggle", base: "antiwall_flag 1=deactivate 2=activate", port: "not applied", match: false }
    status:
      logic: missing
      values: missing
      timing: na
      presentation: missing
      audio: na
      liveness: na
    intended_divergence: false
    divergence_rationale: null
    gaps:
      - "func_clientwall distance-fade is not rendered: fade_start/fade_end/alpha_max/alpha_min/fade_vertical_offset are parsed onto the Entity (MapObjectFieldsExtra.cs) but NO renderer applies Ent_Wall_PreDraw's distance-fade alpha formula or its PVS alpha-cull — a clientwall meant to fade out with distance stays fully opaque. (The only FadeEndDist consumer is WeatherSystem.cs:166 for rain/snow draw distance, an unrelated coincidental field name.)"
      - "The antiwall relay (g_clientmodel_use: trigger sets antiwall_flag to toggle a func_clientwall/func_clientillusionary between its default_solid and SOLID_NOT, with the .inactive flag hiding it) is not ported — a map that toggles a clientwall solid/visible via a trigger has no effect."
    verification:
      - { kind: code, ref: "MapObjectFieldsExtra.cs stores FadeStartDist/FadeEndDist/AlphaMax/AlphaMin/FadeVerticalOffset; grep across game/ + src/: no wall renderer consumes them (only WeatherSystem rain/snow drawdist matches FadeEndDist)", result: fail }
      - { kind: grep, ref: "antiwall / g_clientmodel_use / antiwall_flag across game/ + src/ (excl. planning): no port", result: fail }
    confidence: high
    notes: "STILL OPEN. Unchanged by Wave-1/2. No shipped stock map is known to depend on these; severity low for stock content but a correctness hole for custom maps."
