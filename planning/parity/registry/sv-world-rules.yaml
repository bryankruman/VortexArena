unit: sv-world-rules
category: server
base_ref_rev: "v0.8.6-1779-g863cd3e84"
last_audited: "2026-06-25"
summary:
  feature_count: 18
  worst_gaps:
    - "RandomSeed / PingPLReport broadcast entities and RedirectionThink are still absent (no shared client RNG seed, no scoreboard ping/PL feed, no server-redirect command)."
    - "DumpStats (final/periodic :scores:/:status: eventlog+file scores dump on every match end via NextLevel) has no port analogue — the eventlog scores feed is missing."
    - "GameWorld.Shutdown now does slowmo-reset + ban-save + unfinished PlayerStats_GameReport(false), but ServerProgsDB persistence, bot_endgame, CheatShutdown and WeaponStats_Shutdown on teardown are still absent."
    - "World-layer leadlimit / leadlimit_and_fraglimit WIN semantics, WinningCondition_RanOutOfSpawns, and the CheckRules_World mutator hook are still not evaluated in the checkrules cascade (leadlimit math only feeds the frags-remaining announcer)."
    - "serverflags + animated-lightstyle table are now installed server-side (authority faithful+live) but the CLIENT consumers are still unported: no fullbright/pickup-timer gate reads the serverflags bits, and no render consumer animates the lightstyle table (presentation still missing on both)."
features:
  - id: sv-world-rules.boot.worldspawn
    name: "worldspawn map-init bookkeeping"
    layer: [authority]
    base_refs:
      - "server/world.qc:spawnfunc(worldspawn)"
      - "server/world.qc:InitGameplayMode"
    port_refs:
      - "src/XonoticGodot.Server/GameWorld.cs:Boot"
      - "src/XonoticGodot.Server/GameWorld.cs:ApplyWorldspawn"
    constants:
      - { name: "matchid_format", base: "<counter>.<unixtime>.<rand6> (sprintf %d.%s.%06d)", port: "string.Format \"{0}.{1}.{2:D6}\" (sv_eventlog_files_counter . unix . rand6) GameWorld.cs:1018", match: true }
    status:
      logic: faithful
      values: faithful
      timing: na
      presentation: na
      audio: na
      liveness: live
    intended_divergence: false
    divergence_rationale: null
    gaps:
      - "No ServerProgsDB load and no full support-entity spawn pass at boot (RandomSeed/PingPLReport/lightstyles covered as separate rows; DB persistence covered under misc.shutdown)."
    verification:
      - { kind: code, ref: "GameWorld.Boot + ApplyWorldspawn + GameLog.MatchId now = string.Format(\"{0}.{1}.{2:D6}\", Cvars.Int(sv_eventlog_files_counter), unixTime, rand6) (GameWorld.cs:1016-1019) vs world.qc:949/763. Boot handles gravity/fog/music keys + reply precompute + max_shot_distance + warpzones.", result: pass }
    confidence: high
    notes: "FIXED since 2026-06-22: the matchid is now the QC <counter>.<unixtime>.<rand6> correlation id (GameWorld.cs:1018), not the bare map name — values partial->faithful, logic partial->faithful. Boot handles gravity/fog/music worldspawn keys, matchid, and reply precompute; the heavy QC init list (DB, mapinfo enumerate, q3 compat) is host/engine-side. Map_MarkAsRecent IS ported (MapRotation.MarkAsRecent)."
  - id: sv-world-rules.cvars.changes_log
    name: "cvar_changes / cvar_purechanges server-list log"
    layer: [authority]
    base_refs:
      - "server/world.qc:cvar_changes_init"
    port_refs:
      - "src/XonoticGodot.Server/CommandReplies.cs:BuildCvarChanges"
    constants:
      - { name: "header_default_server", base: "// this server runs at default server settings", port: "same", match: true }
      - { name: "header_default_gameplay", base: "// this server runs at default gameplay settings", port: "same", match: true }
    status:
      logic: faithful
      values: faithful
      timing: na
      presentation: na
      audio: na
      liveness: live
    intended_divergence: false
    divergence_rationale: null
    gaps: []
    verification:
      - { kind: code, ref: "BuildCvarChanges vs cvar_changes_init: value!=default scan, _-skip, both exclude passes, header strings", result: pass }
    confidence: high
    notes: "Unchanged; still faithful. Drives the server browser pure/impure flag, including the gameplay-irrelevant second exclude pass."
  - id: sv-world-rules.warmup.stage
    name: "Warmup stage entry + limit resolution"
    layer: [authority]
    base_refs:
      - "server/world.qc:readlevelcvars"
      - "server/command/vote.qc:ReadyCount"
    port_refs:
      - "src/XonoticGodot.Server/WarmupController.cs:Begin"
      - "src/XonoticGodot.Server/WarmupController.cs:ResolveWarmupLimit"
      - "src/XonoticGodot.Server/WarmupController.cs:ResolveMinPlayers"
    constants:
      - { name: "g_warmup", base: "0", port: "0 (Cvars.cs:123)", match: true }
      - { name: "g_warmup_limit", base: "180 (xonotic-server.cfg)", port: "180 (Cvars.cs:124)", match: true }
      - { name: "g_warmup_majority_factor", base: "0.8", port: "0.8 (Cvars.cs:126)", match: true }
      - { name: "map_minplayers", base: "per-map worldspawn key (0 default)", port: "0 (Cvars.cs:131)", match: true }
    status:
      logic: faithful
      values: faithful
      timing: faithful
      presentation: na
      audio: na
      liveness: live
    intended_divergence: false
    divergence_rationale: null
    gaps: []
    verification:
      - { kind: code, ref: "WarmupController.Begin (campaign forces off; g_warmup<0||>1 -> infinite limit -1; ==1 -> ResolveWarmupLimit) + ResolveMinPlayers vs readlevelcvars:2198-2213. g_warmup_limit default 180 == xonotic-server.cfg:37. MapMinPlayers wired to Cvars.Int(map_minplayers).", result: pass }
    confidence: high
    notes: "Unchanged; faithful. g_warmup_limit default 180 matches Base, the g_warmup<0 map-minplayers lower bound is live. The Begin() readlevelcvars mirror (campaign-off / infinite-vs-timed limit) is faithful."
  - id: sv-world-rules.warmup.serverflags
    name: "readlevelcvars serverflags (fullbright / pickuptimer)"
    layer: [authority, presentation]
    base_refs:
      - "server/world.qc:readlevelcvars"
    port_refs:
      - "src/XonoticGodot.Common/Gameplay/ServerFlags.cs"
      - "src/XonoticGodot.Server/GameWorld.cs:ReadLevelCvars"
      - "src/XonoticGodot.Server/GameWorld.cs:Boot"
    constants:
      - { name: "SERVERFLAG_ALLOW_FULLBRIGHT", base: "BIT(0) from sv_allow_fullbright", port: "1<<0 from sv_allow_fullbright (ServerFlags.cs:24)", match: true }
      - { name: "SERVERFLAG_FORBID_PICKUPTIMER", base: "BIT(4) from sv_forbid_pickuptimer", port: "1<<4 from sv_forbid_pickuptimer (ServerFlags.cs:36)", match: true }
      - { name: "sv_allow_fullbright", base: "1 (xonotic-server.cfg:611)", port: "1 (Cvars.cs:402)", match: true }
      - { name: "sv_forbid_pickuptimer", base: "0 (xonotic-server.cfg:614)", port: "0 (Cvars.cs:403)", match: true }
    status:
      logic: faithful
      values: faithful
      timing: na
      presentation: missing
      audio: na
      liveness: live
    intended_divergence: false
    divergence_rationale: null
    gaps:
      - "Client-side consumer not ported: nothing reads the published serverflags bits to actually gate fullbright player rendering (Base client/view.qc:1115) or hide the HUD item-pickup timer (Base client/hud/panel/pickup.qc:91). The server publishes ServerFlags.Value + the `serverflags` cvar mirror, but no port render/HUD code consumes it — so the two bits have no player-observable effect yet (presentation: missing)."
    verification:
      - { kind: code, ref: "ServerFlags.cs (full SERVERFLAG_* bit table, AllowFullbright=1<<0/ForbidPickupTimer=1<<4) + GameWorld.ReadLevelCvars (cs:1545-1558: clears+sets both bits from sv_allow_fullbright/sv_forbid_pickuptimer, writes ServerFlags.Value and mirrors to the `serverflags` cvar). LIVE: called at GameWorld.Boot (cs:934) and re-run on either source cvar change via OnServerCvarChanged (cs:1531-1532). vs readlevelcvars:2189-2195. No client consumer of the bits exists (grep: no fullbright/pickuptimer gate reads serverflags).", result: partial }
    confidence: high
    notes: "FIX since 2026-06-25: the server-side authority half is now PORTED and LIVE — ServerFlags.cs holds the SERVERFLAG_* bit table, and GameWorld.ReadLevelCvars derives the two world-rules bits from sv_allow_fullbright/sv_forbid_pickuptimer at Boot + re-derives on cvar change, publishing ServerFlags.Value and mirroring to the `serverflags` cvar for the shared-store client. logic+values missing->faithful, liveness na->live. STILL OPEN: the client consumer (the actual fullbright/pickup-timer gate) is unported, so presentation stays missing and the bits have no on-screen effect yet."
  - id: sv-world-rules.warmup.ready_count
    name: "ReadyCount ready-up majority + countdown abort"
    layer: [authority]
    base_refs:
      - "server/command/vote.qc:ReadyCount"
    port_refs:
      - "src/XonoticGodot.Server/WarmupController.cs:ReadyCountCheck"
      - "src/XonoticGodot.Server/WarmupController.cs:ToggleReady"
    constants:
      - { name: "majority_count_basis", base: "humans only (human_players)", port: "humans only (IsBot skip)", match: true }
      - { name: "majority_rounding", base: "rint(human_players * bound(0.5, factor, 1))", port: "Round(humans * clamp(factor,0.5,1), ToEven)", match: true }
      - { name: "sv_teamnagger", base: "2 (xonotic-server.cfg)", port: "2 (Cvars.cs:150)", match: true }
    status:
      logic: faithful
      values: faithful
      timing: faithful
      presentation: faithful
      audio: faithful
      liveness: live
    intended_divergence: false
    divergence_rationale: null
    gaps:
      - "No GiveWarmupResources re-give on countdown abort (loadout re-give deferred to SpawnSystem); the abort itself fires, but the per-player warmup loadout is not re-given when reset_map already ran at countdown start."
    verification:
      - { kind: code, ref: "ReadyCountCheck (human-only humansReady/humanPlayers, rint via Round(...,ToEven), clamp(factor,0.5,1), minplayers/badteams abort, infinite->timed switch) vs ReadyCount:553-616. LIVE callers: ToggleReady via `ready` cmd + BotPopulation. Seams WIRED at GameWorld.cs (MapMinPlayers, BadTeams->Teamplay.TeamsUnbalancedForNag, TimeoutActive->Timeout.Active, OnCountdownStop->NotificationSystem.Send). Notifications defined NotificationsList.cs.", result: pass }
    confidence: high
    notes: "Faithful. The minplayers / sv_teamnagger badteams countdown-abort and the timeout ready-count guard are LIVE; the COUNTDOWN_STOP_MINPLAYERS/BADTEAMS notifications are defined + broadcast."
  - id: sv-world-rules.warmup.ready_restart
    name: "ReadyRestart / ReadyRestart_force match-restart flow"
    layer: [authority]
    base_refs:
      - "server/command/vote.qc:ReadyRestart"
      - "server/command/vote.qc:ReadyRestart_force"
    port_refs:
      - "src/XonoticGodot.Server/WarmupController.cs:ReadyRestart"
      - "src/XonoticGodot.Server/GameWorld.cs:RestartMatch"
    constants:
      - { name: "RESTART_COUNTDOWN", base: "10 (vote.qh:66)", port: "10 (WarmupController.RestartCountdown)", match: true }
      - { name: "campaign_countdown", base: "3", port: "3 (WarmupController.CampaignCountdown), LIVE", match: true }
      - { name: "teamplay_lockonrestart", base: "0 (xonotic-server.cfg:29)", port: "0 (Cvars.cs:152)", match: true }
      - { name: "sv_ready_restart_after_countdown", base: "0 (xonotic-server.cfg:24)", port: "0 (Cvars.cs:128), now implemented (restart_timer deferral)", match: true }
    status:
      logic: faithful
      values: faithful
      timing: faithful
      presentation: na
      audio: na
      liveness: live
    intended_divergence: false
    divergence_rationale: null
    gaps:
      - "Per-player restart policy still not ported: no sv_spectate==2 demotion, no re-join of queued (wants_join) players, no per-player allowed_timeouts reset, no alivetime_start clear in ReadyRestart_force."
    verification:
      - { kind: code, ref: "WarmupController.ReadyRestart vs ReadyRestart_force:441-524; RestartCountdown=10 == RESTART_COUNTDOWN (vote.qh:66); CampaignCountdown=3 LIVE; OnLockTeams WIRED -> TeamsLocked. sv_ready_restart_after_countdown NOW IMPLEMENTED: when !warmup && cvar!=0, ReadyRestart arms a deferred reset (WarmupController.cs:377-380 _pendingResetAt=GameStartTime) that Think() fires once at game_starttime (cs:298-302, restart_timer think); default 0 path still runs reset_map(false) immediately. Paired client/spawn half: SpawnSystem extends rot/regen-pause + spawn-shield by (game_starttime-time) only when !sv_ready_restart_after_countdown (cs:770-791, QC client.qc:665-669).", result: partial }
    confidence: high
    notes: "FIX since 2026-06-25: sv_ready_restart_after_countdown is now IMPLEMENTED — ReadyRestart arms a restart_timer-style deferral (_pendingResetAt at game_starttime) when the cvar is on and outside warmup, dispatched once by Think(); the default (0) keeps the immediate reset_map(false) at countdown start. The SpawnSystem countdown-freeze extension also honors the cvar (only extends pause/shield timers when after-countdown is OFF). logic partial->faithful, constant match false->true. Campaign 3s countdown + teamplay_lockonrestart team-lock remain LIVE. STILL OPEN: the per-player restart policy (spectate demotion, requeue, timeout/alivetime reset) is deferred."
  - id: sv-world-rules.warmup.reset_map
    name: "reset_map: full map/player reset on restart"
    layer: [authority]
    base_refs:
      - "server/command/vote.qc:reset_map"
    port_refs:
      - "src/XonoticGodot.Server/GameWorld.cs:ResetMap"
      - "src/XonoticGodot.Server/GameWorld.cs:ResetMapObjects"
    constants: []
    status:
      logic: partial
      values: faithful
      timing: faithful
      presentation: na
      audio: na
      liveness: live
    intended_divergence: false
    divergence_rationale: null
    gaps:
      - "No per-client accuracy_reset / Inventory_clear pass on reset_map."
      - "The waypoint/assault .reset2 second pass (FOREACH_ENTITY_FLOAT_ORDERED two-stage ordering) is not separately driven — only the single .reset callback per entity fires."
    verification:
      - { kind: code, ref: "ResetMap (GameWorld.cs:3497-3551: Score_ClearAll on real restart, Mayhem/TeamMayhem/CTS/LMS reset_map_players, round reset, per-player zero-vel + StatusEffectsCatalog.RemoveAll + PlayerStates.OnSpawn + Clients.Spawn, minigames+centerprint kill) / ResetMapObjects (3558-3578: projectile delete THEN e.Reset?.Invoke(e) for every non-client entity) vs reset_map:351-430", result: partial }
    confidence: high
    notes: "FIXED since 2026-06-22: ResetMapObjects now FIRES every non-client map entity's .reset callback (e.Reset?.Invoke, GameWorld.cs:3576), returning movers/items to spawn — no longer projectile-only-delete. Also added LMS reset_map_players. Score clear, round reset, per-player zero-velocity + Spawn, status-effects RemoveAll, and projectile deletion remain present and gated on time<=game_starttime. STILL OPEN: per-client accuracy/inventory reset and the .reset2 second pass."
  - id: sv-world-rules.checkrules.cascade
    name: "CheckRules_World per-frame win cascade"
    layer: [authority]
    base_refs:
      - "server/world.qc:CheckRules_World"
    port_refs:
      - "src/XonoticGodot.Server/GameWorld.cs:RunCheckRulesWorld"
      - "src/XonoticGodot.Server/GameWorld.cs:CheckRulesAndIntermission"
      - "src/XonoticGodot.Common/Gameplay/Scoring/GameScores.cs:CountFragsRemaining"
    constants:
      - { name: "warmup/pre-start limit zeroing", base: "warmup_stage || time<=game_starttime", port: "same", match: true }
      - { name: "timelimit offset", base: "timelimit += game_starttime", port: "same", match: true }
    status:
      logic: partial
      values: faithful
      timing: faithful
      presentation: na
      audio: partial
      liveness: live
    intended_divergence: false
    divergence_rationale: null
    gaps:
      - "WinningCondition_RanOutOfSpawns (team spawn-exhaustion draw/win, g_spawn_useallspawns) is still not ported (confirmed absent, GameWorld.cs:2884 comment)."
      - "leadlimit / leadlimit_and_fraglimit WIN semantics are still not evaluated in the world layer (the win comes from the gametype MatchEnded latch + tie report). The leadlimit math exists in GameScores.CountFragsRemaining but only feeds the frags-remaining announcer, not the end-of-match decision."
      - "The CheckRules_World mutator hook (M_ARGV status override) is still not invoked."
    verification:
      - { kind: code, ref: "RunCheckRulesWorld (GameWorld.cs:2833+: timelimit zeroing + offset, overtime/suddendeath cascade) vs CheckRules_World:1725-1861. ANNCE_REMAINING_FRAG_{1,2,3}: GameScores.CountFragsRemaining (leadlimit + leadlimit_and_fraglimit) LIVE-called per-frame from the gametype scoring layer; REMAINING_FRAG notifs defined NotificationsList.cs. RanOutOfSpawns/mutator hook not ported (GameWorld.cs:2884).", result: partial }
    confidence: high
    notes: "Unchanged. The frags-remaining announcer (ANNCE_REMAINING_FRAG_{1,2,3}) is implemented + LIVE in the gametype scoring layer with leadlimit math (audio partial). STILL OPEN: the world-layer leadlimit WIN condition, WinningCondition_RanOutOfSpawns, and the CheckRules_World mutator hook. Overtime/suddendeath ordering + warmup/pre-start gating are faithful."
  - id: sv-world-rules.checkrules.overtime
    name: "Overtime / sudden-death (InitiateSuddenDeath/Overtime/GetWinningCode)"
    layer: [authority]
    base_refs:
      - "server/world.qc:InitiateSuddenDeath"
      - "server/world.qc:InitiateOvertime"
      - "server/world.qc:GetWinningCode"
    port_refs:
      - "src/XonoticGodot.Server/OverTimeManager.cs"
    constants:
      - { name: "timelimit_overtime", base: "2 (min)", port: "2 (Cvars.cs:114)", match: true }
      - { name: "timelimit_overtimes", base: "0", port: "0 (Cvars.cs:115)", match: true }
      - { name: "timelimit_suddendeath", base: "5 (min)", port: "5 (Cvars.cs:116)", match: true }
      - { name: "OVERTIME_SUDDENDEATH", base: "BITS(24)=16777216", port: "1<<24", match: true }
    status:
      logic: faithful
      values: faithful
      timing: faithful
      presentation: partial
      audio: partial
      liveness: live
    intended_divergence: false
    divergence_rationale: null
    gaps:
      - "STAT(OVERTIMES) is not networked to clients (the HUD OVERTIME banner has no server feed) — deferred per T42."
      - "race/qualifying race_StartCompleting branch deliberately omitted (those modes latch their own winner)."
    verification:
      - { kind: code, ref: "OverTimeManager vs world.qc:1467-1536; constants vs Cvars.cs:114-116 (2/0/5) + xonotic-server.cfg", result: pass }
    confidence: high
    notes: "Unchanged; still faithful. Decision logic, constants, sentinel handling (overtimesadded=-1), and the just-begun-suddendeath revert are all faithful; only the client networking of the overtime stat is deferred."
  - id: sv-world-rules.nextlevel.intermission
    name: "NextLevel + intermission entry / map-change timer"
    layer: [authority]
    base_refs:
      - "server/world.qc:NextLevel"
    port_refs:
      - "src/XonoticGodot.Server/GameWorld.cs:NextLevel"
      - "src/XonoticGodot.Server/Intermission.cs"
    constants:
      - { name: "sv_mapchange_delay", base: "5", port: "5 (Cvars.cs:137)", match: true }
      - { name: "empty_server_exittime", base: "-1", port: "-1", match: true }
    status:
      logic: faithful
      values: faithful
      timing: faithful
      presentation: partial
      audio: na
      liveness: live
    intended_divergence: false
    divergence_rationale: null
    gaps:
      - "DumpStats(true) final scores dump at NextLevel is not called (see sv-world-rules.eventlog.dumpstats); target_music_kill and the MatchEnd_BeforeScores/MatchEnd mutator hooks are also not invoked here."
      - "MatchEnd_RestoreSpectatorStatus (move match-joiners back to spectator at match end) is not called here (see new_unmapped)."
      - "The bprint team/player 'wins the match' broadcast lines are handled by the notification layer, not this path (verify coverage in sv-notification)."
    verification:
      - { kind: code, ref: "GameWorld.NextLevel (GameWorld.cs:2950+) + Intermission.Begin vs world.qc:1404-1464", result: pass }
    confidence: high
    notes: "Unchanged; still faithful on the core path. Winner marking, :gameover eventlog, playerstats report, campaign pre-intermission, demo stop, and exittime = time + sv_mapchange_delay (or -1 empty) are all present. DumpStats(true) and MatchEnd_RestoreSpectatorStatus from this same QC path are now tracked as separate gaps."
  - id: sv-world-rules.entity.randomseed
    name: "RandomSeed shared-RNG broadcast entity"
    layer: [authority, presentation]
    base_refs:
      - "server/world.qc:RandomSeed_Spawn"
      - "server/world.qc:RandomSeed_Think"
    port_refs: []
    constants:
      - { name: "reroll_period_s", base: "5", port: "none", match: false }
    status:
      logic: missing
      values: missing
      timing: missing
      presentation: missing
      audio: na
      liveness: na
    intended_divergence: false
    divergence_rationale: null
    gaps:
      - "No shared server->client RNG seed: client-side random effects cannot be kept deterministic with the server."
    verification:
      - { kind: code, ref: "grep RandomSeed across src/ — no match; world.qc:608/601", result: fail }
    confidence: high
    notes: "STILL OPEN — unchanged. Networked entity (ENT_CLIENT_RANDOMSEED, RandomSeed_Send) rerolled every 5s; absent in the port."
  - id: sv-world-rules.entity.pingplreport
    name: "PingPLReport ping/packet-loss client report"
    layer: [authority, presentation]
    base_refs:
      - "server/world.qc:PingPLReport_Spawn"
      - "server/world.qc:PingPLReport_Think"
    port_refs: []
    constants:
      - { name: "LATENCY_THINKRATE", base: "10 (s)", port: "none", match: false }
      - { name: "report_period_s", base: "3 / maxclients", port: "none", match: false }
    status:
      logic: missing
      values: missing
      timing: missing
      presentation: missing
      audio: na
      liveness: na
    intended_divergence: false
    divergence_rationale: null
    gaps:
      - "No server->client ping / packet-loss / movement-loss report; scoreboard ping column and playerstats latency have no feed."
    verification:
      - { kind: code, ref: "grep PingPLReport/ping_packetloss/PINGPLREPORT across src/ — no match; world.qc:96/58", result: fail }
    confidence: high
    notes: "STILL OPEN — unchanged. TE_CSQC_PINGPLREPORT round-robins one client per think (3/maxclients s); records latency for playerstats every LATENCY_THINKRATE=10 s; absent in the port."
  - id: sv-world-rules.boot.lightstyles
    name: "Animated lightstyle table install"
    layer: [authority, presentation]
    base_refs:
      - "server/world.qc:spawnfunc(worldspawn)"
    port_refs:
      - "src/XonoticGodot.Common/Gameplay/LightStyles.cs"
      - "src/XonoticGodot.Server/GameWorld.cs:Boot"
    constants:
      - { name: "lightstyle_count", base: "12 named (0-11) + style 63", port: "12 named (0-11) + style 63 (LightStyles.cs:46-59)", match: true }
      - { name: "frame_strings", base: "world.qc:882-920 verbatim", port: "verbatim identical (LightStyles.cs:46-59)", match: true }
      - { name: "anim_rate_fps", base: "10 (Quake lightstyle convention)", port: "10 (LightStyles.FramesPerSecond)", match: true }
    status:
      logic: faithful
      values: faithful
      timing: faithful
      presentation: missing
      audio: na
      liveness: live
    intended_divergence: false
    divergence_rationale: null
    gaps:
      - "Client render consumer not ported: nothing samples LightStyles.Sample to modulate map-light/dynlight brightness per frame, so a map light driven by a style index still does not visibly animate. The server-side table install is present, but the presentation (the actual light animation) is unconsumed (DynamicLightRenderer renders a steady radius)."
    verification:
      - { kind: code, ref: "LightStyles.cs: 12 named styles (0-11) + style 63, frame strings byte-for-byte identical to world.qc:882-920, 10fps Quake decode ('a'=0.0..'m'=1.0). InstallWorldspawnTable() called LIVE at GameWorld.Boot (cs:928). No render caller of LightStyles.Sample (grep). vs spawnfunc(worldspawn) lightstyle(0..11)+lightstyle(63).", result: partial }
    confidence: high
    notes: "FIX since 2026-06-25: the server-side worldspawn lightstyle table is now PORTED and installed LIVE at Boot — LightStyles.InstallWorldspawnTable() loads the 12 named styles + style 63 with frame strings verbatim from world.qc:882-920, plus a Sample() decoder at the Quake 10fps convention. logic+values+timing missing->faithful (authority half), liveness na->live. STILL OPEN: the client render consumer that actually animates a styled light is unported, so presentation stays missing."
  - id: sv-world-rules.misc.start_delay
    name: "g_start_delay pre-match join window"
    layer: [authority]
    base_refs:
      - "server/world.qc:readlevelcvars"
    port_refs:
      - "src/XonoticGodot.Server/WarmupController.cs:Begin"
    constants:
      - { name: "g_start_delay", base: "0 (listen) / 15 (dedicated) (xonotic-common.cfg:185-186)", port: "0 (Cvars.cs:134); registered", match: true }
    status:
      logic: faithful
      values: faithful
      timing: faithful
      presentation: na
      audio: na
      liveness: live
    intended_divergence: false
    divergence_rationale: null
    gaps:
      - "The dedicated-server 15s default comes from an `if_dedicated` clause in xonotic-common.cfg that the port's config layer does not replicate — an explicit `set g_start_delay 15` works, but the stock dedicated 15s join window is not auto-applied. Config-layer divergence only, not a code gap."
    verification:
      - { kind: code, ref: "WarmupController.Begin: startDelay = CampaignActive ? 0 : Cvars.FloatOr(g_start_delay,0); GameStartTime = Now + startDelay — vs readlevelcvars:2220-2221. g_start_delay registered (Cvars.cs:134, default 0).", result: pass }
    confidence: high
    notes: "Unchanged; faithful. The g_start_delay cvar is registered with the Base listen default 0, read-and-apply on the live no-warmup path is faithful (campaign => 0). The only residual is the dedicated-server 15s if_dedicated config default (cfg-layer, not a code defect)."
  - id: sv-world-rules.misc.max_shot_distance
    name: "max_shot_distance from world bounds"
    layer: [authority, shared]
    base_refs:
      - "server/world.qc:InitGameplayMode"
    port_refs:
      - "src/XonoticGodot.Common/Gameplay/Weapons/WeaponFiring.cs:SetMaxShotDistanceFromWorldBounds"
      - "src/XonoticGodot.Common/Gameplay/Weapons/WeaponFiring.cs:CurrentMaxShotDistance"
      - "src/XonoticGodot.Server/GameWorld.cs:Boot"
    constants:
      - { name: "max_shot_distance", base: "min(230000, vlen(world.maxs-world.mins)) per-map (default 32768 in constants.qh)", port: "min(230000f, VLen(maxs-mins)) per-map at Boot; 32768f constant fallback (WeaponFiring.cs:60)", match: true }
    status:
      logic: faithful
      values: faithful
      timing: na
      presentation: na
      audio: na
      liveness: live
    intended_divergence: false
    divergence_rationale: null
    gaps: []
    verification:
      - { kind: code, ref: "WeaponFiring.SetMaxShotDistanceFromWorldBounds (cs:59-60: min(230000f, VLen(worldMaxs-worldMins))) called per map at GameWorld.cs:734-739 from the built collision bounds (degenerate fixture keeps the 32768f fallback). All hitscan/turret/vehicle sites read WeaponFiring.CurrentMaxShotDistance / TurretAI.CurrentMaxShotDistance. vs world.qc:731 max_shot_distance = min(230000, vlen(world.maxs-world.mins)).", result: pass }
    confidence: high
    notes: "FIXED since 2026-06-22: the global hitscan/trueaim/turret clamp is now recomputed per-map from the world bounds (min(230000, vlen(maxs-mins))) at Boot (GameWorld.cs:734) and exposed as WeaponFiring.CurrentMaxShotDistance, read by every SetupShot/FireBullet/turret/vehicle site. Was a fixed 32768qu constant. logic+values partial->faithful. The 32768f constant survives only as the pre-map/degenerate-fixture fallback (matches the QC constants.qh default)."
  - id: sv-world-rules.misc.redirection
    name: "RedirectionThink server redirect"
    layer: [authority]
    base_refs:
      - "server/world.qc:RedirectionThink"
    port_refs: []
    constants: []
    status:
      logic: missing
      values: missing
      timing: missing
      presentation: na
      audio: na
      liveness: na
    intended_divergence: false
    divergence_rationale: null
    gaps:
      - "No server-redirect / mass-reconnect command (sv_redirect_target / quit_and_redirect)."
    verification:
      - { kind: code, ref: "grep RedirectionThink/sv_redirect across src/ — no match; world.qc:2560", result: fail }
    confidence: high
    notes: "STILL OPEN — unchanged. Admin convenience feature; low gameplay impact."
  - id: sv-world-rules.misc.shutdown
    name: "Shutdown: persist DB/bans + slowmo/playerstats/bot teardown"
    layer: [authority]
    base_refs:
      - "server/world.qc:Shutdown"
    port_refs:
      - "src/XonoticGodot.Server/GameWorld.cs:Shutdown"
      - "src/XonoticGodot.Server/TimeoutController.cs:ResetSlowmoOnShutdown"
    constants: []
    status:
      logic: partial
      values: na
      timing: na
      presentation: na
      audio: na
      liveness: live
    intended_divergence: false
    divergence_rationale: null
    gaps:
      - "ServerProgsDB persistence on shutdown (db_save/db_dump of server.db<sessionid>) has no port analogue."
      - "bot_endgame, CheatShutdown, WeaponStats_Shutdown and MapInfo_Shutdown are not performed on world teardown."
    verification:
      - { kind: code, ref: "GameWorld.Shutdown (cs:1377-1398): Bans.Save() + Timeout.ResetSlowmoOnShutdown() + (if !Intermission.Running) PlayerStats.GameReport(finished:false) + cvar-changed unsubscribe — vs world.qc:2618-2670 (slowmo reset, Ban_SaveBans, PlayerStats_GameReport(false), db_save, bot_endgame).", result: partial }
    confidence: high
    notes: "FIXED since 2026-06-22: GameWorld.Shutdown is no longer a near-no-op — it now does the slowmo-reset-on-active-timeout (Timeout.ResetSlowmoOnShutdown, TimeoutController.cs:243), the Ban_SaveBans re-persist, AND the unfinished-match PlayerStats_GameReport(false) (only when intermission never ran, to avoid double-report). liveness partial->live. STILL OPEN: ServerProgsDB persistence, bot_endgame, CheatShutdown, WeaponStats_Shutdown — so logic stays partial."
  - id: sv-world-rules.eventlog.dumpstats
    name: "DumpStats final/periodic scores dump (:scores: / :status:)"
    layer: [authority]
    base_refs:
      - "server/world.qc:DumpStats"
    port_refs: []
    constants:
      - { name: "final_prefix", base: ":scores:", port: "none", match: false }
      - { name: "status_prefix", base: ":status:", port: "none", match: false }
      - { name: "sv_logscores_console / sv_logscores_file", base: "console/file score-line sinks", port: "none", match: false }
    status:
      logic: missing
      values: missing
      timing: missing
      presentation: na
      audio: na
      liveness: na
    intended_divergence: false
    divergence_rationale: null
    gaps:
      - "DumpStats(final) emits a :scores:<gametype>_<map>:<time> (final, at NextLevel) or :status:... (periodic, via the printstats/status console cmd) block — a per-player score/ping/team/handicap line dump — to console (sv_logscores_console), the eventlog (sv_eventlog), and a log file (sv_logscores_file). The port has no analogue: no final scores block is written to the eventlog at match end, and there is no printstats/status server command."
    verification:
      - { kind: code, ref: "grep DumpStats/printstats/:scores:/:status:/logscores across src/ — no match. Base callers: world.qc:1429 (NextLevel, final=true) + sv_cmd.qc:1249 (printstats cmd, final=false). world.qc:1264.", result: fail }
    confidence: high
    notes: "NEW ROW (registry coverage gap, 2026-06-25). World-rules authority feature: the end-of-match eventlog/file scores feed that external stats parsers (e.g. server score trackers) consume. Missing entirely. Distinct from PlayerStats_GameReport (the xonstat upload, which IS ported); DumpStats is the local console/eventlog/file score-line dump."
