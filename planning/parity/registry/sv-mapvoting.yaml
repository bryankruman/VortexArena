unit: sv-mapvoting
category: server
base_ref_rev: "v0.8.6-1779-g863cd3e84"
last_audited: "2026-06-22"
summary:
  feature_count: 15
  worst_gaps:
    - "Players STILL cannot vote: CastVote/Abstain have no caller and DispatchImpulse still early-returns during intermission (Commands.cs:1357), so the vote always finishes with zero votes — the central liveness failure is unchanged after Wave 1/2."
    - "Client vote screen STILL never appears: MapVotePanel.SetVote/SetVotes/SetWinner have zero callers anywhere in the repo and there is no ENT_CLIENT_MAPVOTE net sync; the panel is present-but-unfed."
    - "No mapvote net entity: ballot, mask, live tallies, own-vote and winner are never sent to clients."
    - "Suggestion seeding is now CODED (MapVoting.Start 4-arg overload) but DEAD on the live path: GameWorld.DriveEndOfMatchMapFlow calls the 3-arg Start (GameWorld.cs:2108) with the rotation ballot only — Commands.MapSuggestions is never passed in."
    - "1 s winner→changelevel delay property (ReadyToChangeLevel) exists but is BYPASSED: GameWorld applies the map on MapVote.Finished (GameWorld.cs:2127), never consulting ReadyToChangeLevel."
    - "Reduce is now implemented + live in MapVoting.CheckRulesDecide, but g_maplist_votable_reduce_count is unregistered (reads 0 → keepExactly=false, wrong mode vs Base default 2); the misnamed g_maplist_votable_keeptwotime=15 still pollutes Cvars.cs:397."
    - "No server-side gametype vote at all (GameTypeVote_*): no ballot, no MapInfo_SwitchGameType, no maplist reset; sv_vote_gametype_options still ships 'dm tdm ctf' vs Base 'dm tdm ca ctf'."
features:
  - id: sv-mapvoting.lifecycle.think_loop
    name: "Vote lifecycle / think loop (start, throttle, winner delay)"
    layer: [authority]
    base_refs:
      - "server/mapvoting.qc:MapVote_Start"
      - "server/mapvoting.qc:MapVote_Think"
    port_refs:
      - "src/XonoticGodot.Server/GameWorld.cs:DriveEndOfMatchMapFlow"
      - "src/XonoticGodot.Server/MapVoting.cs:Tick"
    constants:
      - { name: "think_cadence_s", base: "0.5", port: "0.5 (ThinkCadenceSeconds, self-throttled in Tick)", match: true }
      - { name: "winner_to_changelevel_delay_s", base: "1", port: "1 (WinnerToChangeLevelDelaySeconds — modeled but NOT consulted by GameWorld)", match: false }
    status:
      logic: faithful
      values: partial
      timing: partial
      presentation: na
      audio: na
      liveness: live
    intended_divergence: false
    divergence_rationale: null
    gaps:
      - "The 1 s winner→changelevel delay is now modeled as MapVoting.ReadyToChangeLevel (winner_time + 1, plus the timeout-snap nextthink clamp), but DriveEndOfMatchMapFlow applies the map on MapVote.Finished (GameWorld.cs:2127-2128) and never reads ReadyToChangeLevel — so on the live path the map still changes immediately on finish."
    verification:
      - { kind: code, ref: "MapVoting.cs:71 ThinkCadenceSeconds=0.5 enforced in Tick (lines 293-298, incl. the timeout-snap matching mapvoting.qc:789-791); ReadyToChangeLevel at 101 unused by GameWorld.cs:2127", result: pass }
    confidence: high
    notes: "FIX: 0.5 s think throttle is now genuinely implemented + live (was 'every frame'). PARTIAL: winner-delay logic exists but is bypassed on the live path, so the visible timing is unchanged."

  - id: sv-mapvoting.ballot.build
    name: "Build the ballot from rotation + suggestions + abstain"
    layer: [authority]
    base_refs:
      - "server/mapvoting.qc:MapVote_Init"
      - "server/mapvoting.qc:MapVote_AddVotableMaps"
      - "server/mapvoting.qc:MapVote_AddVotable"
    port_refs:
      - "src/XonoticGodot.Server/MapVoting.cs:Start"
      - "src/XonoticGodot.Server/MapVoting.cs:AddVotable"
      - "src/XonoticGodot.Server/GameWorld.cs:DriveEndOfMatchMapFlow"
    constants:
      - { name: "g_maplist_votable", base: "6", port: "6", match: true }
      - { name: "MAPVOTE_COUNT", base: "20", port: "30 (MaxCandidates, panel only); server nmax = g_maplist_votable", match: false }
      - { name: "g_maplist_votable_abstain", base: "0", port: "0", match: true }
    status:
      logic: partial
      values: faithful
      timing: na
      presentation: na
      audio: na
      liveness: partial
    intended_divergence: false
    divergence_rationale: null
    gaps:
      - "Suggestion seeding is now CODED (Start's 4-arg overload seeds min(suggestCap, nmax) suggested maps first, deduped, then fills from the rotation — matching MapVote_AddVotableMaps' smax seed) but is DEAD: GameWorld.cs:2108 calls the 3-arg Start with the rotation ballot only and never passes Commands.MapSuggestions."
      - "The abstain slot append (Start lines 188-189) and per-candidate Rng tiebreak (AddVotable line 221) are now implemented; abstain is off by default (g_maplist_votable_abstain 0) so the slot isn't added in a stock match."
    verification:
      - { kind: code, ref: "MapVoting.Start (4-arg) seeds suggestions+abstain+Rng; GameWorld.cs:2108 invokes the 3-arg Start(ballot, PlayerCount, timeout) with no suggestions", result: pass }
    confidence: high
    notes: "FIX (code): the build machinery (suggestions/abstain/Rng) is now present and faithful. STILL OPEN (liveness): on the live match path only the rotation maps reach the ballot; suggestions dead-end."

  - id: sv-mapvoting.override_chain
    name: "Pre-vote override chain (DoNextMapOverride: campaign / queued nextmap / samelevel) + silent GotoNextMap"
    layer: [authority]
    base_refs:
      - "server/mapvoting.qc:MapVote_Think"
      - "server/world.qc:DoNextMapOverride"
      - "server/world.qc:GotoNextMap"
    port_refs:
      - "src/XonoticGodot.Server/GameWorld.cs:DriveEndOfMatchMapFlow"
    constants:
      - { name: "samelevel", base: "0", port: "read via Cvars.Bool(\"samelevel\")", match: true }
    status:
      logic: faithful
      values: faithful
      timing: na
      presentation: na
      audio: na
      liveness: live
    intended_divergence: false
    divergence_rationale: null
    gaps:
      - "Faithful priority order (campaign PostIntermission -> QueuedNextMap -> samelevel -> vote -> silent rotation), folded into one method; Base's DoNextMapOverride also handles g_campaign quit and the lastlevel/'endmatch' nextmap-cvar cases which the port routes through QueuedNextMap/Campaign instead."
    verification:
      - { kind: code, ref: "GameWorld.cs:2078-2118 implements the override priority before MapVote.Start; matches MapVote_Think's pre-MapVote_Init branches", result: pass }
    confidence: medium
    notes: "Unchanged by Wave 1/2; still live and largely faithful."

  - id: sv-mapvoting.vote_scoreboard_hide
    name: "During-vote client state: 2342 sentinel health + SVC_FINALE (hide scoreboard, zero impulse each tick)"
    layer: [authority]
    base_refs:
      - "server/mapvoting.qc:MapVote_Tick"
    port_refs: []
    constants:
      - { name: "vote_sentinel_health", base: "2342", port: "none", match: false }
    status:
      logic: missing
      values: missing
      timing: missing
      presentation: missing
      audio: na
      liveness: na
    intended_divergence: false
    divergence_rationale: null
    gaps:
      - "Base MapVote_Tick forces RES_HEALTH=2342 on every client (incl. bots) and sends SVC_FINALE \"\" once, to hide the scoreboard during the vote and zero CS(it).impulse each tick; the port models none of this."
      - "No per-tick impulse-zeroing on the vote path (the port's vote can't read impulses at all — DispatchImpulse still drops them during intermission)."
    verification:
      - { kind: code, ref: "grep: no 2342 sentinel / SVC_FINALE on the vote path in src/ (the only -2342 is an unrelated 'observing' marker in Chat.cs)", result: pass }
    confidence: high
    notes: "Unchanged by Wave 1/2. Still moot while the vote screen never appears, but a real missing piece of MapVote_Tick's per-client loop."

  - id: sv-mapvoting.cast.vote
    name: "Cast a vote (impulse -> .mapvote)"
    layer: [authority]
    base_refs:
      - "server/mapvoting.qc:MapVote_Tick"
    port_refs:
      - "src/XonoticGodot.Server/MapVoting.cs:CastVote"
      - "src/XonoticGodot.Server/Commands.cs:DispatchImpulse"
    constants: []
    status:
      logic: faithful
      values: na
      timing: na
      presentation: na
      audio: na
      liveness: dead
    intended_divergence: false
    divergence_rationale: null
    gaps:
      - "CastVote is now a complete, faithful method (range/availability guard, abstain re-route, prior-vote move, recount) but STILL has no caller; DispatchImpulse still early-returns `if (_world.Intermission.Running) return true;` (Commands.cs:1357), so impulses never reach the vote."
      - "No path turns a client key press / impulse into a server vote, so every vote finishes with zero votes."
    verification:
      - { kind: code, ref: "grep: CastVote has zero callers outside MapVoting.cs; Commands.cs:1357 DispatchImpulse early-returns on Intermission.Running (unchanged)", result: pass }
    confidence: high
    notes: "STILL OPEN — the central liveness failure is unchanged after Wave 1/2. The cast LOGIC was fleshed out but remains uncastable."

  - id: sv-mapvoting.cast.abstain
    name: "Abstain (Don't care) option"
    layer: [authority]
    base_refs:
      - "server/mapvoting.qc:MapVote_AddVotable"
      - "server/mapvoting.qc:MapVote_CheckRules_decide"
    port_refs:
      - "src/XonoticGodot.Server/MapVoting.cs:Abstain"
    constants:
      - { name: "g_maplist_votable_abstain", base: "0", port: "0", match: true }
    status:
      logic: faithful
      values: faithful
      timing: na
      presentation: na
      audio: na
      liveness: dead
    intended_divergence: false
    divergence_rationale: null
    gaps:
      - "Abstain() is now faithful (clears any map choice, marks abstained, excluded from voters_real, still counts toward 'everyone voted') and the abstain ballot slot is appended in Start, but Abstain() has no caller — abstainers still can't be expressed on the live path."
    verification:
      - { kind: code, ref: "grep: Abstain has zero callers outside MapVoting.cs / CastVote's internal re-route; Start appends the abstain slot only when g_maplist_votable_abstain (default 0)", result: pass }
    confidence: high
    notes: "FIX (code): abstain mechanics + voters_real accounting now match Base. STILL OPEN (liveness): dead, same as CastVote. Default abstain=0 so off in a stock match anyway."

  - id: sv-mapvoting.decide.tally_rank
    name: "Tally votes + rank candidates"
    layer: [authority]
    base_refs:
      - "server/mapvoting.qc:MapVote_CheckRules_count"
      - "server/mapvoting.qc:MapVote_ranked_cmp"
    port_refs:
      - "src/XonoticGodot.Server/MapVoting.cs:RecountFromBallots"
      - "src/XonoticGodot.Server/MapVoting.cs:RankCandidates"
      - "src/XonoticGodot.Server/MapVoting.cs:Finish"
    constants: []
    status:
      logic: faithful
      values: na
      timing: na
      presentation: na
      audio: na
      liveness: partial
    intended_divergence: false
    divergence_rationale: null
    gaps:
      - "Ranking now uses a stable descending sort by votes then the per-candidate Rng (RankCandidates), faithfully mirroring MapVote_ranked_cmp + the mapvote_rng tiebreak assigned at build (was a per-Finish RNG reservoir in the baseline). Heapsort vs stable Sort is an implementation detail with identical ordering semantics."
      - "The running-gametype 0-vote tiebreak (current gametype index wins among 0-vote options) is still absent, but it only applies to the gametype vote, which is wholly unported."
    verification:
      - { kind: code, ref: "RankCandidates (MapVoting.cs:390-408) sorts available-first, votes-desc, Rng-desc; Finish/CheckRulesDecide use the ranked top; RecountFromBallots counts only available candidates", result: pass }
    confidence: high
    notes: "FIX: the reservoir tiebreak is replaced by a per-candidate Rng matching Base. Logic runs live during the vote, but since no votes are cast it only ever sees the zero-vote case (liveness partial)."

  - id: sv-mapvoting.decide.early_finish
    name: "Early finish: timeout / leader unbeatable / all voted"
    layer: [authority]
    base_refs:
      - "server/mapvoting.qc:MapVote_CheckRules_decide"
    port_refs:
      - "src/XonoticGodot.Server/MapVoting.cs:CheckRulesDecide"
      - "src/XonoticGodot.Server/MapVoting.cs:LeaderIsUnbeatable"
    constants:
      - { name: "g_maplist_votable_timeout", base: "30", port: "30", match: true }
    status:
      logic: faithful
      values: faithful
      timing: faithful
      presentation: na
      audio: na
      liveness: partial
    intended_divergence: false
    divergence_rationale: null
    gaps:
      - "With no votes ever cast (CastVote dead) the only branch that fires in a real match is the 30 s timeout."
    verification:
      - { kind: code, ref: "CheckRulesDecide (MapVoting.cs:309-341) now uses Base's exact inequality `(votersReal - votesRunningTotal) < votesRecent` plus the `votersReal == 0` all-abstained branch and the all-voted branch (ballots >= ExpectedVoters); matches mapvoting.qc:634-651", result: pass }
    confidence: high
    notes: "FIX: the unbeatable-leader check is now bit-faithful to Base (was a reformulated `(best-second) > (ExpectedVoters-ballotsCast)`), and the all-abstained branch is present with proper voters_real accounting. Still effectively a 30 s timer because the cast path is dead."

  - id: sv-mapvoting.decide.reduce
    name: "Ballot reduce / keep-two (mid-vote option pruning)"
    layer: [authority]
    base_refs:
      - "server/mapvoting.qc:MapVote_CheckRules_decide"
    port_refs:
      - "src/XonoticGodot.Server/MapVoting.cs:CheckRulesDecide"
      - "src/XonoticGodot.Server/MapVoting.cs:ReduceRemoveThis"
    constants:
      - { name: "g_maplist_votable_reduce_time", base: "15", port: "15 (via MapVoting hardcoded FloatOr fallback; cvar UNREGISTERED)", match: false }
      - { name: "g_maplist_votable_reduce_count", base: "2", port: "0 (cvar UNREGISTERED → Cvars.Int reads 0 → keepExactly=false, WRONG mode)", match: false }
    status:
      logic: partial
      values: partial
      timing: faithful
      presentation: missing
      audio: na
      liveness: partial
    intended_divergence: false
    divergence_rationale: null
    gaps:
      - "Reduce logic is now implemented + live in CheckRulesDecide (the keep_exactly/REDUCE_REMOVE_THIS loop, the time>reduce_time && (keep_exactly||ri>=2) trigger, MapVote_TouchMask GTV_AVAILABLE stripping, and the <3-real-options / reduce_time<=now disable), faithfully mirroring mapvoting.qc:654-680 + 310-314."
      - "BUT the cvars that drive it are unregistered: g_maplist_votable_reduce_count reads 0 (Cvars.Int) → keepExactly=false, so the port reduces to 'any map with a vote' instead of Base's default 'keep exactly top 2'. g_maplist_votable_reduce_time is unregistered so MapVoting falls back to its hardcoded 15 f (coincidentally Base's default)."
      - "The misnamed g_maplist_votable_keeptwotime=15 is STILL registered (Cvars.cs:397) and STILL unread — dead registry noise."
      - "No client presentation of the reduce (options being dimmed/removed) because the whole client panel is unfed; and with no votes cast the reduce only ever sees the zero-vote ballot."
    verification:
      - { kind: code, ref: "MapVoting.cs:198-202 arms _reduceCount=Cvars.Int(reduce_count)[=0] / reduceDelay=FloatOr(reduce_time,15); CheckRulesDecide:345-371 runs the prune. Cvars.cs:397 keeptwotime=15 unread; no reduce_time/reduce_count registration", result: pass }
    confidence: high
    notes: "FIX (logic): reduce is now coded + live (was wholly missing). STILL OPEN (values): the two reduce cvars are unregistered, so reduce_count defaults to the wrong mode; misnamed keeptwotime persists."

  - id: sv-mapvoting.finish.winner
    name: "Finish + pick winner + apply map change"
    layer: [authority]
    base_refs:
      - "server/mapvoting.qc:MapVote_Finished"
      - "server/mapvoting.qc:MapVote_Winner"
    port_refs:
      - "src/XonoticGodot.Server/MapVoting.cs:Finish"
      - "src/XonoticGodot.Server/GameWorld.cs:ApplyMapChange"
    constants: []
    status:
      logic: faithful
      values: na
      timing: partial
      presentation: na
      audio: na
      liveness: live
    intended_divergence: false
    divergence_rationale: null
    gaps:
      - "No event-log (:vote:finished) emitted."
      - "No 1 s winner-reveal delay before changelevel on the live path: MapVoting.ReadyToChangeLevel models it (winner_time + 1) but GameWorld.cs:2127 applies the map on MapVote.Finished and never consults it (timing)."
      - "FixClientCvars-on-finish is not modeled."
    verification:
      - { kind: code, ref: "Finish (MapVoting.cs:435-452) sets WinningMap from the ranked top available candidate; GameWorld.cs:2127-2128 applies via ApplyMapChange -> ChangeLevelHandler on Finished (not ReadyToChangeLevel)", result: pass }
    confidence: high
    notes: "Winner selection now uses the proper Rng-tiebroken ranked top. The winner still drives the real changelevel; only the 1 s reveal delay and the votes feeding it are missing."

  - id: sv-mapvoting.gametype_vote
    name: "Gametype vote (pre-map gametype ballot + switch)"
    layer: [authority]
    base_refs:
      - "server/mapvoting.qc:GameTypeVote_Start"
      - "server/mapvoting.qc:GameTypeVote_Finished"
      - "server/mapvoting.qc:GameTypeVote_SetGametype"
      - "server/mapvoting.qc:GameTypeVote_AddVotable"
    port_refs: []
    constants:
      - { name: "sv_vote_gametype", base: "0", port: "0", match: true }
      - { name: "sv_vote_gametype_timeout", base: "20", port: "20", match: true }
      - { name: "sv_vote_gametype_options", base: "dm tdm ca ctf", port: "dm tdm ctf", match: false }
      - { name: "sv_vote_gametype_reduce_time", base: "10", port: "none", match: false }
      - { name: "sv_vote_gametype_reduce_count", base: "2", port: "none", match: false }
      - { name: "sv_vote_gametype_detail", base: "1", port: "none", match: false }
      - { name: "sv_vote_gametype_default_current", base: "1", port: "none", match: false }
      - { name: "sv_vote_gametype_maplist_reset", base: "1", port: "none", match: false }
    status:
      logic: missing
      values: partial
      timing: missing
      presentation: missing
      audio: na
      liveness: na
    intended_divergence: false
    divergence_rationale: null
    gaps:
      - "No server-side gametype vote at all: no gametype ballot, no custom gametypes, no hook execution, no MapInfo_SwitchGameType, no maplist reset."
      - "sv_vote_gametype_options still ships 'dm tdm ctf' (Cvars.cs:402) vs Base 'dm tdm ca ctf' (missing ca)."
      - "sv_vote_gametype_reduce_time/_reduce_count/_detail/_default_current/_maplist_reset cvars are unregistered."
    verification:
      - { kind: code, ref: "no GameTypeVote_* counterpart in src/; only the client panel can draw GametypeVote (unfed)", result: pass }
    confidence: high
    notes: "Unchanged by Wave 1/2. The panel's GametypeVote draw path is unreachable because nothing drives it."

  - id: sv-mapvoting.suggestions
    name: "Player map suggestions (suggestmap)"
    layer: [authority]
    base_refs:
      - "server/mapvoting.qc:MapVote_Suggest"
    port_refs:
      - "src/XonoticGodot.Server/Commands.cs:CmdSuggestMap"
      - "src/XonoticGodot.Server/MapVoting.cs:Start"
    constants:
      - { name: "g_maplist_votable_suggestions", base: "2", port: "2", match: true }
      - { name: "g_maplist_votable_suggestions_override_mostrecent", base: "0", port: "0", match: true }
      - { name: "g_maplist_votable_show_suggester", base: "1", port: "none (read via FloatOr default 1; cvar UNREGISTERED)", match: false }
    status:
      logic: partial
      values: partial
      timing: na
      presentation: na
      audio: na
      liveness: partial
    intended_divergence: false
    divergence_rationale: null
    gaps:
      - "CmdSuggestMap still only stores into Commands._mapSuggestions (existence + dedup + suggestions==0 disable); the new Start overload that would seed them is NOT called by GameWorld (still the 3-arg Start), so suggestions never reach a ballot."
      - "Recency check (Map_IsRecent / override_mostrecent) and gametype-support check are still not applied in CmdSuggestMap."
      - "g_maplist_votable_show_suggester (default 1) is still unregistered; MapVoting.Start reads it via FloatOr(default 1) so the suggester WOULD be surfaced if the seed path were wired — but it isn't."
    verification:
      - { kind: code, ref: "CmdSuggestMap (Commands.cs:1779-1813) unchanged; MapVoting.Start can consume (map,suggester) pairs but GameWorld.cs:2108 calls the 3-arg overload with no suggestions", result: pass }
    confidence: high
    notes: "FIX (code): a real seeding path now exists in MapVoting.Start (deduped, suggester-tagged). STILL OPEN (liveness): the command's output dead-ends because GameWorld doesn't pass MapSuggestions into Start."

  - id: sv-mapvoting.net.sync
    name: "Vote state networking (ENT_CLIENT_MAPVOTE + screenshot transfer)"
    layer: [authority, presentation]
    base_refs:
      - "server/mapvoting.qc:MapVote_SendEntity"
      - "server/mapvoting.qc:MapVote_SendPicture"
      - "common/net_linked.qh:ENT_CLIENT_MAPVOTE"
      - "common/net_linked.qh:TE_CSQC_PICTURE"
    port_refs: []
    constants: []
    status:
      logic: missing
      values: na
      timing: na
      presentation: missing
      audio: na
      liveness: na
    intended_divergence: false
    divergence_rationale: null
    gaps:
      - "No mapvote net entity: the ballot, mask, live tallies, own-vote and winner are never sent to clients."
      - "No screenshot transfer (TE_CSQC_PICTURE / mv_getpicture); the client can only load level-shots from its own VFS."
    verification:
      - { kind: code, ref: "grep: no ENT_CLIENT_MAPVOTE / mapvote net handler in src/ or game/; mv_getpicture is registered only as a server-handled-but-unimplemented client command", result: pass }
    confidence: high
    notes: "Unchanged by Wave 1/2. Without this sync the client panel has nothing to show even if it were activated."

  - id: sv-mapvoting.client.draw
    name: "Client vote-screen panel (grid, thumbnails, counts, winner reveal)"
    layer: [presentation]
    base_refs:
      - "client/mapvoting.qc:MapVote_Draw"
      - "client/mapvoting.qc:MapVote_DrawMapItem"
      - "client/mapvoting.qc:GameTypeVote_DrawGameTypeItem"
      - "client/mapvoting.qc:MapVote_DrawAbstain"
    port_refs:
      - "game/hud/MapVotePanel.cs:DrawPanel"
      - "game/hud/MapVotePanel.cs:DrawCandidate"
    constants:
      - { name: "hud_panel_mapvote_highlight_border", base: "1", port: "1 (MapVotePanel.RegisterCvars:200)", match: true }
      - { name: "MV_FADETIME", base: "0.2", port: "0.2 (FadeTime, MapVotePanel.cs:123 — modeled but unfed)", match: true }
    status:
      logic: faithful
      values: faithful
      timing: partial
      presentation: faithful
      audio: na
      liveness: dead
    intended_divergence: false
    divergence_rationale: null
    gaps:
      - "Panel is STILL never fed: SetVote/SetVotes/SetWinner have ZERO callers anywhere in the repo (HudManager only does Get<MapVotePanel>()), so it never becomes Active in a real match -> the player never sees the vote screen."
      - "MV_FADETIME selection-pulse easing and mv_winner_alpha (max(0.2, 1-sqrt(time-winner_time))) grow/fade are now modeled in the panel, but with no data feed they never run live."
      - "Suggester line fade-out is drawn but never driven (no data feed)."
    verification:
      - { kind: code, ref: "grep across src/+game/: .SetVote/.SetVotes/.SetWinner have ZERO callers outside MapVotePanel.cs", result: pass }
      - { kind: code, ref: "MapVotePanel.cs:200 registers highlight_border=1 (matches Base hud_*.cfg skins); FadeTime=0.2 + winnerAlpha at lines 364-371 match MV_FADETIME / mv_winner_alpha", result: pass }
    confidence: high
    notes: "FIX (values/timing): highlight_border default corrected 2->1 (now matches every shipped Base skin; the wrong code comment is fixed), and MV_FADETIME + mv_winner_alpha fade are now modeled. STILL OPEN (liveness): the panel is dead — present-but-unfed, exactly as in the baseline."

  - id: sv-mapvoting.client.input
    name: "Client vote input (keyboard/mouse selection + cast)"
    layer: [presentation]
    base_refs:
      - "client/mapvoting.qc:MapVote_InputEvent"
      - "client/mapvoting.qc:MapVote_SendChoice"
      - "client/mapvoting.qc:MapVote_Selection"
    port_refs: []
    constants: []
    status:
      logic: missing
      values: na
      timing: na
      presentation: missing
      audio: na
      liveness: na
    intended_divergence: false
    divergence_rationale: null
    gaps:
      - "No input handler: arrows/enter/space/digits/mouse don't move a selection or cast a vote on the panel (the panel exposes a Selection property but nothing writes it from input)."
      - "No localcmd('impulse N') equivalent to send the chosen option to the server."
    verification:
      - { kind: code, ref: "MapVotePanel has no _Input/_GuiInput/InputEvent handler; no MapVote_SendChoice counterpart; Selection is fed externally only", result: pass }
    confidence: high
    notes: "Unchanged by Wave 1/2. Even if the panel were activated, the player could not interact with it."
