unit: mutator-waypoints
category: mutator
base_ref_rev: "v0.8.6-1779-g863cd3e84"
last_audited: "2026-06-22"
summary:
  feature_count: 13
  worst_gaps:
    - "Player-deployed waypoints (personal/here/danger pings + team HELP-ME) are STILL DEAD: no server impulse dispatch; deploy/attach/helpme API has zero callers (re-verified by repo-wide grep post Wave-1/2)."
    - "Server WaypointSprite_* manager API remains DEAD: Spawn/AttachCarrier/UpdateHealth/HelpMePing/Ping/Disown/Kill all still have zero callers; the gametype producers build WaypointSprite records directly and bypass the manager entirely."
    - "Objective emit still narrow: Wave-2 added Domination control points (now LIVE), but Onslaught/KeyHunt/Assault/Keepaway/Nexball/Race/FreezeTag/LMS/itemstime/monster/turret/weapon/vehicle/buff markers are all still missing."
    - "Client tuning cvars are still inert: WaypointSpriteLayer hardcodes constants; g_waypointsprite_* and cl_hidewaypoints (incl. the menu bindings) have no effect."
    - "Presentation fades still missing: distance-fade, edge-fade, crosshair-fade, per-sprite scale, text/uppercase toggle, radar ping ring all unimplemented."
features:
  - id: waypoints.registry.defs
    name: "Waypoint-kind registry (WP_* defs: text/icon/color/blink)"
    layer: [shared]
    base_refs:
      - "common/mutators/mutator/waypoints/all.inc:REGISTER_WAYPOINT"
      - "common/mutators/mutator/waypoints/all.qh:Waypoint"
    port_refs:
      - "src/XonoticGodot.Common/Gameplay/Waypoints/Waypoints.cs:WaypointRegistry"
    constants:
      - { name: "def_count", base: "63 (all.inc) + Null + ~12 RadarIcons", port: "63 (Waypoints.cs Build) — all REGISTER_WAYPOINT names present", match: true }
      - { name: "WP_FROZEN_COLOR", base: "0.25 0.9 1", port: "0.25 0.9 1", match: true }
      - { name: "OnsCPDefend / OnsCPAttack / Seeker blink", base: "0.5 / 2 / 2", port: "0.5 / 2 / 2", match: true }
      - { name: "FlagReturn blink (spritelookupblinkvalue override)", base: "2", port: "2 (baked into def)", match: true }
      - { name: "blink default", base: "1", port: "1", match: true }
      - { name: "superweapon Weapon / Item blink (spritelookupblinkvalue, wp_extra-dependent)", base: "2 / item.m_waypointblink", port: "Weapon def blink=1; not wp_extra-driven", match: false }
    status:
      logic: faithful
      values: faithful
      timing: na
      presentation: faithful
      audio: na
      liveness: partial
    intended_divergence: false
    divergence_rationale: null
    gaps:
      - "Wave-2 closed the def gap: all 63 REGISTER_WAYPOINT names are now present with matching text/icon/color, and the static per-def blink values (OnsCPDefend 0.5, OnsCPAttack 2, Seeker 2, FlagReturn 2) are correct. KeyCarrierFriend/KeyCarrierFinish added (the prior 25-def deficit is resolved)."
      - "Only residual: the wp_extra-DRIVEN spritelookupblinkvalue overrides (superweapon Weapon=2, Item=item.m_waypointblink) are not modeled — Weapon's def blink is a flat 1 and there is no per-instance wp_extra blink lookup. Harmless while those producers are absent."
      - "liveness: partial — most defs are reachable only once their gametype emits waypoints (only CTF + Domination do), so the bulk of the newly-added defs are not exercised on the live path yet."
    verification:
      - { kind: code-read, ref: "all.inc (63 REGISTER_WAYPOINT) vs WaypointRegistry.Build (63 Add(...)) — names/text/icon/color/blink line-by-line", result: pass }
    confidence: high
    notes: "FIX vs baseline: the registry is now a faithful 1:1 port of all.inc (was 38/63 with blink flattened). Defs that exist are faithful; the only open item is the per-instance wp_extra blink (Weapon/Item), which matters only once those producers emit."

  - id: waypoints.server.manager_api
    name: "Server WaypointSprite_* spawn/update/kill API + manager"
    layer: [authority]
    base_refs:
      - "common/mutators/mutator/waypoints/waypointsprites.qc:WaypointSprite_Spawn"
      - "common/mutators/mutator/waypoints/waypointsprites.qc:WaypointSprite_Kill"
    port_refs:
      - "src/XonoticGodot.Common/Gameplay/Waypoints/Waypoints.cs:WaypointSprites"
    constants: []
    status:
      logic: partial
      values: partial
      timing: faithful
      presentation: na
      audio: na
      liveness: dead
    intended_divergence: false
    divergence_rationale: null
    gaps:
      - "STILL DEAD: Spawn/SpawnFixed/AttachCarrier/UpdateSprites/UpdateHealth/UpdateRule/UpdateTeamRadar/UpdateBuildFinished/UpdateMaxHealth/HelpMePing/Ping/Disown/Kill/DetachCarrier still have ZERO callers; _active is always empty."
      - "Only Reset() (map load) and Think() (per tick) are invoked — both no-op on the empty list."
      - "The live objective producers (Ctf.CollectWaypoints, Domination.CollectWaypoints) construct WaypointSprite records directly into the net scratch list and bypass this manager entirely, so the deploy/attach/lifetime/build-progress lifecycle is unreachable."
      - "sv_waypointsprite_deployed_lifetime/deadlifetime/limitedrange never read (consumers dead); no WaypointSprite_Init analogue."
    verification:
      - { kind: code-read, ref: "repo-wide grep 'WaypointSprites.(Spawn|SpawnFixed|AttachCarrier|UpdateSprites|UpdateHealth|UpdateRule|UpdateTeamRadar|UpdateBuildFinished|UpdateMaxHealth|Ping|HelpMePing|Disown|Kill|DetachCarrier|DeployFixed|DeployPersonal|Attach)' → ZERO matches post Wave-1/2", result: fail }
    confidence: high
    notes: "NO CHANGE vs baseline. Wave-2 added objective producers but they emit WaypointSprite records directly (ServerNet.SendWaypoints merges WaypointSprites.Active with the gametype CollectWaypoints pull); WaypointSprites.Active is still always empty. Classic present-but-dead lifecycle."

  - id: waypoints.gametype.collect
    name: "Per-tick objective waypoint emit (per gametype)"
    layer: [authority]
    base_refs:
      - "common/gamemodes/gamemode/ctf/sv_ctf.qc:ctf_FlagSetup"
      - "common/mutators/mutator/waypoints/waypointsprites.qc:WaypointSprite_SpawnFixed"
    port_refs:
      - "src/XonoticGodot.Common/Gameplay/GameplayBases.cs:CollectWaypoints"
      - "src/XonoticGodot.Common/Gameplay/GameTypes/Ctf.cs:CollectWaypoints"
      - "src/XonoticGodot.Common/Gameplay/GameTypes/Domination.cs:CollectWaypoints"
    constants: []
    status:
      logic: partial
      values: faithful
      timing: faithful
      presentation: na
      audio: na
      liveness: partial
    intended_divergence: false
    divergence_rationale: null
    gaps:
      - "Wave-2 added Domination control-point markers (LIVE via ServerNet.SendWaypoints:924 → GameType.CollectWaypoints): one fixed DomNeut/DomRed/Blue/Yellow/Pink marker per point at origin+'0 0 32', radar tint = team palette color. So CTF + Domination now emit (was CTF-only)."
      - "Still missing: Onslaught control points + generators, Key Hunt keys + carriers, Assault defend/destroy/push, Keepaway/TeamKeepaway/Nexball balls + goals, Race start/finish/checkpoint, LMS leader, FreezeTag frozen/reviving markers (base no-op CollectWaypoints)."
      - "itemstime (item-respawn) spectator waypoints, monster/turret/weapon/vehicle/buff markers: none."
    verification:
      - { kind: code-read, ref: "grep 'override.*CollectWaypoints' → Ctf.cs + Domination.cs; both reached via ServerNet.SendWaypoints per tick", result: partial }
    confidence: high
    notes: "PARTIAL FIX vs baseline: a second live objective producer (Domination) was added. Still a CTF/Dom-first scope; most gametypes' objective markers remain absent."

  - id: waypoints.ctf.flag_markers
    name: "CTF flag waypoint sprites (base/dropped/carrier state machine)"
    layer: [authority, presentation]
    base_refs:
      - "common/gamemodes/gamemode/ctf/sv_ctf.qc:ctf_FlagSetup"
      - "common/mutators/mutator/waypoints/all.inc:REGISTER_WAYPOINT(FlagBaseRed)"
    port_refs:
      - "src/XonoticGodot.Common/Gameplay/GameTypes/Ctf.cs:CollectWaypoints"
    constants:
      - { name: "flag radar tint (red)", base: "colormapPaletteColor(0) ~ 1 .0625 .0625", port: "1 0.0625 0.0625", match: true }
    status:
      logic: faithful
      values: faithful
      timing: faithful
      presentation: partial
      audio: na
      liveness: live
    intended_divergence: false
    divergence_rationale: null
    gaps:
      - "No flag-carrier HEALTH BAR (QC WaypointSprite_AttachCarrier sets one from healtharmor_maxdamage; the port's CollectWaypoints still sets Health=-1 and AttachCarrier is dead). UNCHANGED."
      - "No auto-helpme on carrier hit (g_ctf_flagcarrier_auto_helpme_damage 100) — depends on the dead helpme path. UNCHANGED."
      - "Enemy-flagcarrier waypoint reveal on stalemate not wired: Ctf.Stalemate state was added (Wave-2) but the QC ctf_stalemate enemy-FC waypoint reveal is still a 'presentation follow-on (see todos)' per the code comment — markers are not produced from the stalemate state."
    verification:
      - { kind: code-read, ref: "Ctf.CollectWaypoints (Health=-1, FlagBase/Dropped/CarrierEnemy + suffix) → SendWaypoints → ClientNet → WaypointSpriteLayer/RadarPanel chain", result: pass }
    confidence: medium
    notes: "The original live producer; State→def mapping (AtBase/Dropped/Carried × team suffix) faithful. Carrier health bar + auto-helpme + stalemate enemy-FC reveal still absent. Rendering observed only in code, not runtime."

  - id: waypoints.deploy.personal_here_danger
    name: "Player-deployed pings (waypoint_personal/here/danger impulses)"
    layer: [authority]
    base_refs:
      - "server/impulse.qc:IMPULSE(waypoint_personal_here)"
      - "server/impulse.qc:IMPULSE(waypoint_here_here)"
      - "server/impulse.qc:IMPULSE(waypoint_danger_here)"
    port_refs: []
    constants:
      - { name: "sv_waypointsprite_deployed_lifetime", base: "10", port: "none", match: false }
      - { name: "sv_waypointsprite_limitedrange", base: "5120", port: "none", match: false }
    status:
      logic: missing
      values: missing
      timing: missing
      presentation: missing
      audio: na
      liveness: dead
    intended_divergence: false
    divergence_rationale: null
    gaps:
      - "STILL no server dispatch for waypoint_personal_here/_crosshair/_death, waypoint_here_*, waypoint_danger_*, waypoint_clear*. Strings appear only as QuickMenuPanel say_team macro text (QuickMenuPanel.cs:261-284); the server never routes them to DeployPersonal/DeployFixed."
    verification:
      - { kind: code-read, ref: "grep waypoint_personal/here/danger across repo → only QuickMenuPanel text + spec/registry docs; no server impulse handler", result: fail }
    confidence: high
    notes: "NO CHANGE vs baseline. The impulse handlers were never ported; the (now-richer) registry has the Waypoint/Here/Danger/Helpme defs but nothing deploys them."

  - id: waypoints.deploy.helpme
    name: "Team HELP-ME attach + ping (waypoint_here_follow)"
    layer: [authority]
    base_refs:
      - "server/impulse.qc:IMPULSE(waypoint_here_follow)"
      - "common/mutators/mutator/waypoints/waypointsprites.qc:WaypointSprite_HelpMePing"
    port_refs:
      - "src/XonoticGodot.Common/Gameplay/Waypoints/Waypoints.cs:HelpMePing"
    constants:
      - { name: "SPRITE_HELPME_BLINK", base: "2", port: "0.45 dim (different scheme)", match: false }
    status:
      logic: missing
      values: missing
      timing: missing
      presentation: stub
      audio: na
      liveness: dead
    intended_divergence: false
    divergence_rationale: null
    gaps:
      - "STILL no waypoint_here_follow dispatch; WaypointSprite_Attach(WP_Helpme) + HelpMePing never called. The helpme flash exists in the client layer but no waypoint ever sets Helpme>0 (HelpmeUntil only set by the dead HelpMePing)."
    verification:
      - { kind: code-read, ref: "grep HelpMePing callers across repo → none", result: fail }
    confidence: high
    notes: "NO CHANGE vs baseline. Core teamplay communication feature is still non-functional."

  - id: waypoints.visibility.rules
    name: "Per-peer visibility filter (SPRITERULE_DEFAULT/TEAMPLAY/SPECTATOR)"
    layer: [authority]
    base_refs:
      - "common/mutators/mutator/waypoints/waypointsprites.qc:WaypointSprite_visible_for_player"
      - "common/mutators/mutator/waypoints/waypointsprites.qh:SPRITERULE_DEFAULT"
    port_refs:
      - "game/net/ServerNet.cs:WaypointVisible"
      - "src/XonoticGodot.Common/Gameplay/Waypoints/Waypoints.cs:SpriteRule"
    constants: []
    status:
      logic: partial
      values: na
      timing: na
      presentation: na
      audio: na
      liveness: live
    intended_divergence: false
    divergence_rationale: null
    gaps:
      - "SPRITERULE_DEFAULT: port (ServerNet.WaypointVisible:979) returns visible-to-everyone unconditionally; QC (waypointsprites.qc:996) restricts a team-set Default waypoint to the same team AND requires the viewer be a live player (not a spectator). UNCHANGED."
      - "SPRITERULE_SPECTATOR: port = observer-only (peer.IsObserver). QC gates on sv_itemstime: hides from EVERYONE when sv_itemstime==0, shows to live players in warmup or when sv_itemstime==2. Port ignores sv_itemstime entirely. UNCHANGED."
      - "Personal-waypoint (this.enemy/showto) restriction not modeled (no deployed personals exist anyway). UNCHANGED."
    verification:
      - { kind: code-read, ref: "ServerNet.WaypointVisible (Teamplay/Spectator/Default switch) vs WaypointSprite_visible_for_player", result: partial }
    confidence: high
    notes: "NO CHANGE vs baseline. Correct for CTF flags + Domination points (everyone-visible Default). Latent bug if team-restricted Default waypoints or spectator/itemstime waypoints are added."

  - id: waypoints.visibility.three_image_rule
    name: "TEAMPLAY three-image per-audience swap (netname/2/3)"
    layer: [authority, presentation]
    base_refs:
      - "common/mutators/mutator/waypoints/waypointsprites.qc:Draw_WaypointSprite"
      - "common/mutators/mutator/waypoints/waypointsprites.qc:WaypointSprite_UpdateSprites"
    port_refs: []
    constants: []
    status:
      logic: missing
      values: na
      timing: na
      presentation: missing
      audio: na
      liveness: na
    intended_divergence: false
    divergence_rationale: null
    gaps:
      - "Port still networks a single SpriteName; the own-team/enemy-team/spectator image triple (model1/2/3) and the client-side SPRITERULE_TEAMPLAY image selection are not modeled."
    verification:
      - { kind: code-read, ref: "WaypointNet has one SpriteName; SendWaypoints writes one sprite name + a server-side rule filter only", result: fail }
    confidence: high
    notes: "NO CHANGE vs baseline. No current producer uses TEAMPLAY rule, so no visible impact yet, but the mechanism is absent."

  - id: waypoints.net.serialize
    name: "Waypoint networking (S2C channel + per-peer filter)"
    layer: [authority]
    base_refs:
      - "common/mutators/mutator/waypoints/waypointsprites.qc:WaypointSprite_SendEntity"
      - "common/mutators/mutator/waypoints/waypointsprites.qc:Ent_WaypointSprite"
    port_refs:
      - "game/net/ServerNet.cs:SendWaypoints"
      - "game/net/ClientNet.cs:HandleWaypoints"
      - "game/net/NetProtocol.cs:NetControl.Waypoints"
    constants: []
    status:
      logic: partial
      values: faithful
      timing: faithful
      presentation: na
      audio: na
      liveness: live
    intended_divergence: true
    divergence_rationale: "Port re-serializes the full peer-visible waypoint set each tick as a flat record list (id/origin/team/sprite/radaricon/color/health/fade/helpme/maxdist/hideable) instead of QC's delta-bit SendEntity edicts. Equivalent payload; simpler wire format suited to the port's snapshot net model. Capped at 255/peer."
    gaps:
      - "No SendFlags delta encoding (always sends all fields); QC sends only changed sub-fields. Bandwidth-only difference. UNCHANGED."
      - "wp_extra (item/weapon/buff id used for client text/color/blink lookup) not networked — fine while those producers are absent. UNCHANGED."
    verification:
      - { kind: code-read, ref: "SendWaypoints/HandleWaypoints round-trip; merges WaypointSprites.Active + GameType.CollectWaypoints; called at ServerNet.cs:920 per tick", result: pass }
    confidence: high
    notes: "Live for CTF + Domination. Format diverges deliberately; functionally equivalent for the fields used."

  - id: waypoints.client.draw
    name: "3D in-world sprite draw (icon/text + arrow + project)"
    layer: [presentation]
    base_refs:
      - "common/mutators/mutator/waypoints/waypointsprites.qc:Draw_WaypointSprite"
      - "common/mutators/mutator/waypoints/waypointsprites.qc:drawspritearrow"
    port_refs:
      - "game/client/WaypointSpriteLayer.cs:_Draw"
    constants:
      - { name: "iconsize", base: "32", port: "24 (hardcoded)", match: false }
      - { name: "fontsize", base: "12", port: "13 (hardcoded)", match: false }
      - { name: "edgeoffset_*", base: "0.06 (cvar)", port: "0.06 (hardcoded const)", match: true }
      - { name: "SPRITE_ARROW_SCALE", base: "1", port: "approx (custom arrow)", match: false }
    status:
      logic: partial
      values: partial
      timing: faithful
      presentation: partial
      audio: na
      liveness: live
    intended_divergence: true
    divergence_rationale: "IconSize 24 / FontSize 13 are a deliberate 'gently smaller than QC' tweak (per the code comment). The off-screen arrow uses a simpler triangle than QC's bordered glyph but serves the same purpose."
    gaps:
      - "g_waypointsprite_text / _uppercase / _iconcolor toggles unimplemented (always icon-if-present, mixed-case, white icon). UNCHANGED."
      - "cl_hidewaypoints (0/1/2) and hideflags & 1 (fixed) / & 2 (radar-only) gating not honored (Hideable networked but never consulted). UNCHANGED."
      - "No per-sprite scale (waypointsprite_scale / minscale) at all. UNCHANGED."
    verification:
      - { kind: code-read, ref: "WaypointSpriteLayer._Draw (const EdgeInset/IconSize/FontSize) vs Draw_WaypointSprite; file untouched by Wave-1/2", result: partial }
    confidence: high
    notes: "NO CHANGE vs baseline. Live + renders CTF flags + Domination points; icon/text/arrow present; many cvar-driven knobs hardcoded or absent."

  - id: waypoints.client.fades
    name: "Distance / edge / crosshair / lifetime fades + blink"
    layer: [presentation]
    base_refs:
      - "common/mutators/mutator/waypoints/waypointsprites.qc:Draw_WaypointSprite"
      - "common/mutators/mutator/waypoints/waypointsprites.qc:WaypointSprite_Load"
    port_refs:
      - "game/client/WaypointSpriteLayer.cs:_Draw"
    constants:
      - { name: "distancealphaexponent", base: "2", port: "2 (hardcoded in maxdist ramp)", match: true }
      - { name: "distancefade{alpha,scale,distmult}", base: "1 / 0.7 / 0.5", port: "none", match: false }
      - { name: "edgefade{alpha,dist,scale}", base: "0.5 / 50 / 1", port: "none", match: false }
      - { name: "crosshairfade{alpha,dist,scale}", base: "0.25 / 150 / 1", port: "none", match: false }
      - { name: "SPRITE_HELPME_BLINK", base: "2 (bright-half multiply)", port: "0.45 (dim-half multiply)", match: false }
    status:
      logic: partial
      values: partial
      timing: partial
      presentation: partial
      audio: na
      liveness: live
    intended_divergence: false
    divergence_rationale: null
    gaps:
      - "distance-fade, edge-fade, crosshair-fade (alpha+scale ramps) entirely unimplemented; only a raw max-distance cull + a single distance ramp. UNCHANGED."
      - "Lifetime fade is server-computed (FadeAlpha) and networked rather than client-computed from fadetime/lifetime**timealphaexponent; timealphaexponent (1) not applied. UNCHANGED."
      - "Blink: only helpme dim-flash modeled (0.45 in the off-half), differing from QC's *2 bright-half scheme; per-def blink (now correct in the registry) is NOT applied at draw time. UNCHANGED."
    verification:
      - { kind: code-read, ref: "WaypointSpriteLayer fade block vs Draw_WaypointSprite fade block; file untouched by Wave-1/2", result: partial }
    confidence: high
    notes: "NO CHANGE vs baseline. The registry now carries correct per-def blink, but the client draw layer does not consume it; most of the polish layer (fades/scales) remains missing."

  - id: waypoints.client.cvars_inert
    name: "Client tuning cvars (g_waypointsprite_*, cl_hidewaypoints) wiring"
    layer: [presentation]
    base_refs:
      - "common/mutators/mutator/waypoints/waypointsprites.qc:WaypointSprite_Load"
      - "xonotic-client.cfg:g_waypointsprite_alpha"
    port_refs:
      - "game/menu/dialogs/DialogSettingsGame.cs:228"
      - "assets/data/xonotic-data.pk3dir/xonotic-client.cfg:502"
    constants:
      - { name: "g_waypointsprite_alpha", base: "1", port: "config default present, READ BY NOTHING", match: false }
      - { name: "cl_hidewaypoints", base: "0", port: "config default present, READ BY NOTHING", match: false }
    status:
      logic: missing
      values: stub
      timing: na
      presentation: missing
      audio: na
      liveness: dead
    intended_divergence: false
    divergence_rationale: null
    gaps:
      - "STILL: all g_waypointsprite_* cvars + cl_hidewaypoints exist as config defaults and are bound in DialogSettingsGame, but WaypointSpriteLayer reads none of them (only const EdgeInset/IconSize/FontSize; no Cvars.Get* calls) — the knobs have no effect."
    verification:
      - { kind: code-read, ref: "grep Cvars/g_waypointsprite/cl_hidewaypoints in WaypointSpriteLayer.cs → only const definitions, no reads", result: fail }
    confidence: high
    notes: "NO CHANGE vs baseline. Menu UI is faithful to QC but misleads the player: changing these settings does nothing."

  - id: waypoints.radar.icons
    name: "Team radar icons from waypoint sprites"
    layer: [presentation]
    base_refs:
      - "common/mutators/mutator/waypoints/waypointsprites.qc:Ent_WaypointSprite"
      - "common/mutators/mutator/waypoints/all.qh:REGISTER_RADARICON"
    port_refs:
      - "game/hud/RadarPanel.cs:277"
    constants:
      - { name: "radar icon sprite", base: "gfx/teamradar_icon_<n>", port: "gfx/teamradar_icon_1 / _2", match: true }
    status:
      logic: faithful
      values: faithful
      timing: faithful
      presentation: partial
      audio: na
      liveness: live
    intended_divergence: false
    divergence_rationale: null
    gaps:
      - "Radar PING ring pulse (WaypointSprite_Ping → cnt|BIT(7), the expanding radar circle) is still a no-op in the port (WaypointSprites.Ping is a documented no-op). UNCHANGED."
    verification:
      - { kind: code-read, ref: "RadarPanel iterates net.Waypoints, draws teamradar_icon per RadarIcon>0; file untouched by Wave-1/2", result: pass }
    confidence: medium
    notes: "NO CHANGE vs baseline. Live for CTF flag + Domination control-point radar icons (tinted by team radar color). Ping pulse animation absent."
