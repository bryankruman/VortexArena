unit: mutator-touchexplode
category: mutator
base_ref_rev: "v0.8.6-1779-g863cd3e84"
last_audited: "2026-06-22"
summary:
  feature_count: 7
  worst_gaps:
    - "DEATH_TOUCHEXPLODE obituary still mis-resolves: the blast now PASSES deathTag 'touchexplode' (the kill is tagged + processed), but 'touchexplode' is NOT a registered DeathTypes constant and is absent from both _registry and the SelectSpecial switch (DeathTypes.cs / DeathMessages.cs:209-225) — so SelectSpecial falls through to DEATH_SELF_GENERIC / DEATH_MURDER_FRAG, NOT 'died in an accident'. Needs a DeathTypes TOUCHEXPLODE registry entry + SelectSpecial branch. Same latent defect affects the nade booms that also pass 'touchexplode' (NadeSpawnBoom.cs:62, NadeTranslocateBoom.cs:81)."
    - "active-mutators list entry missing: Base util.qc:315 registers MUT_TOUCHEXPLODE 'Touch explode' for the server-browser/scoreboard mutator string; the port still has no MUT_-style active-mutators string builder (only the create-game menu checkbox). Systemic gap shared by all mutators."
features:
  - id: touchexplode.enable.cvar
    name: "Enable predicate (g_touchexplode)"
    layer: [authority]
    base_refs:
      - "common/mutators/mutator/touchexplode/sv_touchexplode.qc:11"   # REGISTER_MUTATOR(touchexplode, expr_evaluate(autocvar_g_touchexplode))
    port_refs:
      - "src/XonoticGodot.Common/Gameplay/Mutators/TouchExplodeMutator.cs:38"  # IsEnabled => g_touchexplode != 0
      - "game/menu/dialogs/DialogMutators.cs:129"                              # checkbox bound to g_touchexplode
    constants:
      - { name: "g_touchexplode", base: "0 (string, off)", port: "0", match: true }
    status:
      logic: faithful
      values: faithful
      timing: na
      presentation: faithful
      audio: na
      liveness: live
    intended_divergence: false
    divergence_rationale: null
    gaps: []
    verification:
      - { kind: code, ref: "MutatorActivation.Apply (GameWorld.cs:535) adds when IsEnabled; checkbox bound to g_touchexplode (DialogMutators.cs:129); g_touchexplode registered mutators.cfg:162", result: pass }
    confidence: high
    notes: "expr_evaluate(string) vs float != 0 — equivalent for the 0/1 cvar. No mutator/gametype exclusions in Base; none in port. Unchanged by Wave-1/2. Base mutator-info describe/message text has no port menu-info-page analogue, but that is cosmetic menu help text only."

  - id: touchexplode.scan.prethink
    name: "Per-frame pairwise overlap scan (PlayerPreThink)"
    layer: [authority]
    base_refs:
      - "common/mutators/mutator/touchexplode/sv_touchexplode.qc:29"   # MUTATOR_HOOKFUNCTION(touchexplode, PlayerPreThink)
    port_refs:
      - "src/XonoticGodot.Common/Gameplay/Mutators/TouchExplodeMutator.cs:75"   # OnPlayerPreThink
      - "src/XonoticGodot.Server/GameWorld.cs:988"                              # PlayerPreThink.Call per client per tick
    constants:
      - { name: "outer_guard", base: "time>texp_time && !game_stopped && IS_PLAYER && !IS_DEAD && !FROZEN && !IS_INDEPENDENT_PLAYER", port: "same set (now>TouchExplodeTime + GameStopped + IsLivePlayer + IsFrozen + IsIndependentPlayer)", match: true }
      - { name: "overlap_test", base: "boxesoverlap(absmin,absmax)", port: "BoxesOverlap(AbsMin,AbsMax)", match: true }
    status:
      logic: faithful
      values: faithful
      timing: faithful
      presentation: na
      audio: na
      liveness: live
    intended_divergence: false
    divergence_rationale: null
    gaps:
      - "IS_INDEPENDENT_PLAYER guard is a no-op: Entity.IsIndependentPlayer is hardcoded false (DamageEntityState.cs:132); equivalent today since the port has no independent-players mode, but it won't gate if one is added. Unchanged by Wave-1/2."
    verification:
      - { kind: code, ref: "OnClientMove (GameWorld.cs:986-988) builds PlayerPreThinkArgs and fires PlayerPreThink.Call per client each tick; IsFrozen via StatusEffectsCatalog.Frozen; AbsMin/AbsMax kept current in EngineServices.LinkEdict", result: pass }
    confidence: high
    notes: "QC FOREACH_CLIENT inner loop = port foreach(FindByClass('player')); handler invoked once per client so the all-players scan runs per-client just like QC. O(players^2)/frame in both, bounded by the debounce. IsLivePlayer folds in IS_PLAYER(EntFlags.Client) + !IS_DEAD(DeadState) + !IsFreed. Unchanged by Wave-1/2."

  - id: touchexplode.scan.debounce
    name: "0.2s per-pair re-trigger debounce"
    layer: [authority]
    base_refs:
      - "common/mutators/mutator/touchexplode/sv_touchexplode.qc:40"   # player.touchexplode_time = it.touchexplode_time = time + 0.2
    port_refs:
      - "src/XonoticGodot.Common/Gameplay/Mutators/TouchExplodeMutator.cs:98"   # player.TouchExplodeTime = other.TouchExplodeTime = now + 0.2f
      - "src/XonoticGodot.Common/Gameplay/Mutators/EntityMutatorState.cs:41"    # .TouchExplodeTime field
    constants:
      - { name: "debounce_s", base: "0.2", port: "0.2", match: true }
    status:
      logic: faithful
      values: faithful
      timing: faithful
      presentation: na
      audio: na
      liveness: live
    intended_divergence: false
    divergence_rationale: null
    gaps: []
    verification:
      - { kind: code, ref: "symmetric assignment to both entities (TouchExplodeMutator.cs:98) matches QC line 40; field is a plain per-entity server-side float (EntityMutatorState.cs:41), not networked", result: pass }
    confidence: high
    notes: ".touchexplode_time is a plain edict field in QC; port promotes it to Entity.TouchExplodeTime (EntityMutatorState). Unchanged by Wave-1/2."

  - id: touchexplode.blast.radiusdamage
    name: "Midpoint explosion + RadiusDamage"
    layer: [authority]
    base_refs:
      - "common/mutators/mutator/touchexplode/sv_touchexplode.qc:15"   # PlayerTouchExplode: org midpoint + RadiusDamage
    port_refs:
      - "src/XonoticGodot.Common/Gameplay/Mutators/TouchExplodeMutator.cs:104"   # PlayerTouchExplode
      - "src/XonoticGodot.Common/Gameplay/Weapons/WeaponSplash.cs:49"            # RadiusDamage
    constants:
      - { name: "g_touchexplode_radius", base: "50", port: "50 (default field + unconditional read of registered cfg)", match: true }
      - { name: "g_touchexplode_damage", base: "20", port: "20 (default field + unconditional read of registered cfg)", match: true }
      - { name: "g_touchexplode_edgedamage", base: "0", port: "0 (default field + unconditional read; cfg 0 now applied)", match: true }
      - { name: "g_touchexplode_force", base: "300", port: "300 (default field + unconditional read of registered cfg)", match: true }
      - { name: "org_z_offset", base: "(p1.mins.z + p2.mins.z)*0.5 (feet)", port: "same (org.Z += (p1.Mins.Z + p2.Mins.Z)*0.5f)", match: true }
      - { name: "attacker", base: "NULL (no credit)", port: "attacker: null", match: true }
      - { name: "inflictor", base: "spawn()/setorigin/RadiusDamage/delete throwaway entity", port: "Spawn()/SetOrigin/RadiusDamage/Remove (className 'touchexplode')", match: true }
    status:
      logic: faithful
      values: faithful
      timing: na
      presentation: na
      audio: na
      liveness: live
    intended_divergence: false
    divergence_rationale: null
    gaps: []
    verification:
      - { kind: value, ref: "Wave-2 replaced the `if (cvar != 0f)` guard with an unconditional read (TouchExplodeMutator.cs:53-56); class defaults now 50/20/0/300 (TouchExplodeMutator.cs:24-33) matching mutators.cfg:163-166; cfg is exec'd by ConfigLoader.LoadServerConfig (GameWorld.cs:433) BEFORE MutatorActivation.Apply (GameWorld.cs:535) so Hook() reads the registered cfg values on the live path", result: pass }
    confidence: high
    notes: "FIXED in Wave-2. Removing the non-zero override is what closes the edgedamage=0 gap: the legitimate cfg 0 is now read into EdgeDamage. The class-field defaults (50/20/0/300) now also match cfg, so a unit test that loads no cfg would read 0 from GetFloat and over-write them to 0 (radius 0 -> RadiusDamage early-returns) — but that bare-test path is not the live match path; on the live path the cfg is loaded first. Blast geometry (midpoint-at-feet, spawn/setorigin/RadiusDamage/delete inflictor, attacker null, DMG_NOWEP analogue) was already faithful. WeaponSplash.RadiusDamage is the shared headless RadiusDamageForSource port."

  - id: touchexplode.blast.deathtype
    name: "DEATH_TOUCHEXPLODE death type / obituary"
    layer: [authority]
    base_refs:
      - "common/mutators/mutator/touchexplode/sv_touchexplode.qc:25"   # RadiusDamage(... DEATH_TOUCHEXPLODE.m_id ...)
      - "common/deathtypes/all.inc:35"                                 # REGISTER_DEATHTYPE(TOUCHEXPLODE, DEATH_SELF/MURDER_TOUCHEXPLODE)
    port_refs:
      - "src/XonoticGodot.Common/Gameplay/Mutators/TouchExplodeMutator.cs:121"   # RadiusDamage(... deathTag: "touchexplode")
      - "src/XonoticGodot.Common/Gameplay/Weapons/WeaponSplash.cs:208"           # deathTag path -> Combat.Damage(e, ..., tag, ...)
      - "src/XonoticGodot.Common/Gameplay/Notifications/DeathMessages.cs:187"    # SelectSpecial — no TOUCHEXPLODE case (falls to GENERIC/FRAG)
      - "src/XonoticGodot.Common/Gameplay/Damage/DeathTypes.cs:330"              # _registry — no 'touchexplode' entry
    constants:
      - { name: "deathtype", base: "DEATH_TOUCHEXPLODE -> 'died in an accident'", port: "tag 'touchexplode' now passed + flows to Combat.Damage; but SelectSpecial has no TOUCHEXPLODE case -> resolves DEATH_SELF_GENERIC / DEATH_MURDER_FRAG", match: false }
    status:
      logic: partial
      values: na
      timing: na
      presentation: missing
      audio: na
      liveness: partial
    intended_divergence: false
    divergence_rationale: null
    gaps:
      - "Obituary still mis-resolves: Wave-2 added deathTag 'touchexplode' (TouchExplodeMutator.cs:121), and WeaponSplash.RadiusDamage routes a non-null deathTag straight to Combat.Damage(e, ..., tag, ...) (WeaponSplash.cs:208-214), so the kill is now TAGGED + processed through the special-death pipeline. BUT 'touchexplode' is NOT a registered DeathTypes constant and is absent from both DeathTypes._registry (DeathTypes.cs:330) AND the SelectSpecial switch (DeathMessages.cs:209-225), so DeathMessages.SelectSpecial('touchexplode', murder) falls through `_ => GENERIC` -> DEATH_SELF_GENERIC / DEATH_MURDER_FRAG, not 'died in an accident'."
      - "Full fix still needs a DeathTypes TOUCHEXPLODE constant + _registry/SelectSpecial branch (cross-file, deathtype-registry phase). The DEATH_SELF/MURDER_TOUCHEXPLODE notification rows are registered (NotificationsList.cs:163/240/859/1037/1080) but remain unreachable until SelectSpecial maps the tag. Same latent defect affects the nade booms that pass 'touchexplode' (NadeSpawnBoom.cs:62, NadeTranslocateBoom.cs:81)."
    verification:
      - { kind: code, ref: "blast now passes deathTag 'touchexplode' (TouchExplodeMutator.cs:121); WeaponSplash.cs:208-214 forwards it to Combat.Damage; grep of DeathTypes.cs for 'touchexplode' = 0 hits; SelectSpecial (DeathMessages.cs:187-235) has no TOUCHEXPLODE base-name case -> GENERIC/FRAG", result: fail }
    confidence: high
    notes: "PARTIALLY improved by Wave-2: the deathTag now flows (logic missing->partial; liveness dead->partial — the kill IS tagged + damages, but the obituary text is wrong). The remaining work is the 2-part deathtype-registry wiring (DeathTypes constant + SelectSpecial branch); the producer side (passing the tag) is now done. WeaponSplash.RadiusDamage's deathTag param carries the special-death string through to the damage pipeline correctly."

  - id: touchexplode.blast.presentation
    name: "Impact sound + explosion_small particle"
    layer: [authority, presentation]
    base_refs:
      - "common/mutators/mutator/touchexplode/sv_touchexplode.qc:20"   # sound(p1, CH_TRIGGER, SND_TOUCHEXPLODE)
      - "common/mutators/mutator/touchexplode/sv_touchexplode.qc:21"   # Send_Effect(EFFECT_EXPLOSION_SMALL, org)
    port_refs:
      - "src/XonoticGodot.Common/Gameplay/Mutators/TouchExplodeMutator.cs:111"   # Api.Sound.Play(p1, SoundChannel.TriggerAuto, weapons/grenade_impact.wav)
      - "src/XonoticGodot.Common/Gameplay/Mutators/TouchExplodeMutator.cs:112"   # EffectEmitter.Emit("EXPLOSION_SMALL", org)
      - "game/net/ServerNet.cs:287"                                              # EffectEmitter.Sink = new EffectNetSink (live server effect networking)
    constants:
      - { name: "sound_sample", base: "W_Sound('grenade_impact')", port: "weapons/grenade_impact.wav", match: true }
      - { name: "sound_channel", base: "CH_TRIGGER (-3)", port: "SoundChannel.TriggerAuto (-3)", match: true }
      - { name: "particle", base: "EFFECT_EXPLOSION_SMALL ('explosion_small')", port: "EffectEmitter.Emit(\"EXPLOSION_SMALL\") -> 'explosion_small'", match: true }
    status:
      logic: faithful
      values: faithful
      timing: na
      presentation: faithful
      audio: faithful
      liveness: live
    intended_divergence: false
    divergence_rationale: null
    gaps: []
    verification:
      - { kind: code, ref: "PlayerTouchExplode (TouchExplodeMutator.cs:111-112) plays the grenade_impact sample on SoundChannel.TriggerAuto (= CH_TRIGGER -3, Services.cs:112) AND emits EXPLOSION_SMALL; EXPLOSION_SMALL registered (EffectsList.cs:29 -> 'explosion_small'); EffectEmitter.Sink is replaced by the live EffectNetSink on the server path (ServerNet.cs:287) so the effect is networked to clients", result: pass }
    confidence: high
    notes: "FIXED in Wave-2 on both halves. (1) Sound channel corrected Item(3) -> TriggerAuto(-3) = CH_TRIGGER, so channel-stacking/replacement semantics now match Base. (2) The EFFECT_EXPLOSION_SMALL particle is now emitted via EffectEmitter.Emit and is LIVE: the host installs a real EffectNetSink (ServerNet.cs:287) that networks Send_Effect-equivalent requests to clients (RecordingSink only stands in for headless tests). The sample, channel, and effect name all match Base."

  - id: touchexplode.list.activemutators
    name: "Active-mutators list entry ('Touch explode')"
    layer: [presentation]
    base_refs:
      - "common/util.qc:315"   # X("Touch explode", _("Touch explode"), MUT_TOUCHEXPLODE, cvar("g_touchexplode") > 0)
    port_refs: []
    constants:
      - { name: "list_label", base: "'Touch explode' (when g_touchexplode > 0)", port: "none", match: false }
    status:
      logic: missing
      values: na
      timing: na
      presentation: missing
      audio: na
      liveness: na
    intended_divergence: false
    divergence_rationale: null
    gaps:
      - "Base appends 'Touch explode' to the active-mutators string (util.qc:315, MUT_TOUCHEXPLODE) used for the server-browser mutator field and the scoreboard/info mutator list. The port still has no MUT_-style active-mutators string builder, so an active touchexplode game is not advertised in any mutator list. Systemic gap shared by all mutators (the whole MUT_* enumeration is unported); only the create-game menu checkbox (DialogMutators.cs:129) exists. Unchanged by Wave-1/2."
    verification:
      - { kind: code, ref: "grep of src/** and game/** for an active-mutators / MUT_ list builder = none (only DialogMutators.cs); Base util.qc:315 unported", result: fail }
    confidence: high
    notes: "Cosmetic/info presentation only — no gameplay effect. Flagged so the missing active-mutators list is tracked against this unit; likely owned by a future shared server-info/scoreboard parity unit. Unchanged by Wave-1/2."
