unit: weapon-machinegun
category: weapon
base_ref_rev: "v0.8.6-1779-g863cd3e84"
last_audited: "2026-06-22"
summary:
  feature_count: 13
  worst_gaps:
    - "EFFECT_BULLET tracer trail still not drawn for the MachineGun: FireBullet has the tracerEffect seam (Wave-1) and the BULLET effect IS registered (tr_bullet), but Machinegun.FireOne calls FireBullet WITHOUT a tracerEffect, so the MG bullet tracer is absent. Base passes EFFECT_BULLET on every MG fireBullet call (machinegun.qc:86/101/194/244)."
    - "Bot wr_aim distance switch still missing: bots never press the long-range no-spread burst secondary (ButtonAttack2 still forced false in BotBrain.cs:382; comment confirms the per-weapon wr_aim is deferred). Unchanged by the fix-up."
    - "mode-0 paths remain dead (g_balance_machinegun_mode default 1): AttackSingle + its (now-correct) SNIPE secondary are unreachable on the live path, and the W_MachineGun_Attack_Frame held-fire self-continuation loop is not modeled. Harmless at the default mode 1 but the only home of the (non-default) SNIPE obituary."
features:
  - id: machinegun.identity
    name: "MachineGun registration & identity (flags, color, ammo, models, impulse)"
    layer: [shared]
    base_refs:
      - "common/weapons/weapon/machinegun.qh:CLASS(MachineGun)"
    port_refs:
      - "src/XonoticGodot.Common/Gameplay/Weapons/Machinegun.cs:Machinegun (ctor)"
    constants:
      - { name: "netname", base: "machinegun", port: "machinegun", match: true }
      - { name: "ammo_type", base: "RES_BULLETS", port: "ResourceType.Bullets", match: true }
      - { name: "impulse", base: "3", port: "3", match: true }
      - { name: "color", base: "'0.678 0.886 0.267'", port: "(0.678,0.886,0.267)", match: true }
      - { name: "spawnflags", base: "NORMAL|RELOADABLE|TYPE_HITSCAN|PENETRATEWALLS|BLEED", port: "Normal|Reloadable|TypeHitscan|PenetrateWalls|Bleed", match: true }
      - { name: "view_model", base: "h_uzi.iqm", port: "h_uzi.iqm", match: true }
      - { name: "world_model", base: "v_uzi.md3", port: "v_uzi.md3", match: true }
      - { name: "item_model", base: "g_uzi.md3", port: "g_uzi.md3", match: true }
    status:
      logic: faithful
      values: faithful
      timing: na
      presentation: na
      audio: na
      liveness: live
    intended_divergence: false
    divergence_rationale: null
    gaps: []
    verification:
      - { kind: code, ref: "machinegun.qh ATTRIBs vs Machinegun.cs ctor (DisplayName/NetName/Impulse/Color/flags/models)", result: pass }
    confidence: high
    notes: "Unchanged by the fix-up. Pure registration row; actual viewmodel/muzzleflash rendering covered by machinegun.presentation.muzzle_impact. bot_pickupbasevalue 7000 not ported (out of scope)."

  - id: machinegun.firemode.dispatch
    name: "wr_think fire-mode dispatch (mode 0 vs mode 1, primary/secondary routing)"
    layer: [authority]
    base_refs:
      - "common/weapons/weapon/machinegun.qc:wr_think"
    port_refs:
      - "src/XonoticGodot.Common/Gameplay/Weapons/Machinegun.cs:WrThink"
      - "src/XonoticGodot.Server/GameWorld.cs (WeaponFireDriver.Frame -> WrThink)"
    constants:
      - { name: "mode", base: "1", port: "1 (live from cfg)", match: true }
      - { name: "burst_gate", base: "(fire&2) && burst", port: "Secondary && Burst>0", match: true }
      - { name: "first_gate (mode0 sec)", base: "(fire&2) && first", port: "Secondary (first not re-checked)", match: false }
      - { name: "secondary-as-zoom gate", base: "wr_zoom: secondary is zoom when burst==0 (mode1) / first==0 (mode0)", port: "not modeled (Burst>0 gate only)", match: false }
    status:
      logic: faithful
      values: faithful
      timing: faithful
      presentation: na
      audio: na
      liveness: live
    intended_divergence: false
    divergence_rationale: null
    gaps:
      - "mode-0 secondary path does not re-check the `first` cvar gate (Base requires (fire&2) && first); harmless at default first=1."
      - "The secondary-as-zoom behavior (Base wr_zoom: when burst==0 the secondary becomes a zoom, not a fire mode) is not modeled; harmless at default burst=3 but a custom balance with burst 0 would zoom in Base and do nothing here."
    verification:
      - { kind: code, ref: "machinegun.qc wr_think mode 0/1 split vs Machinegun.WrThink 109-178", result: pass }
      - { kind: code, ref: "WeaponFireDriver.Frame -> WrThink(Primary)/WrThink(Secondary) live", result: pass }
    confidence: high
    notes: "Unchanged structurally by the fix-up. Dispatch logic faithful; the zoom + mode-0 first gate remain the two harmless-at-default gaps."

  - id: machinegun.fire.auto
    name: "Sustained automatic primary fire (spread accumulation + per-shot damage/ammo)"
    layer: [authority]
    base_refs:
      - "common/weapons/weapon/machinegun.qc:W_MachineGun_Attack_Auto"
    port_refs:
      - "src/XonoticGodot.Common/Gameplay/Weapons/Machinegun.cs:AttackAuto"
      - "src/XonoticGodot.Common/Gameplay/Weapons/Machinegun.cs:WrThink (MiscBulletCounter=0 reset, :121)"
    constants:
      - { name: "sustained_damage", base: "10", port: "10", match: true }
      - { name: "sustained_ammo", base: "1", port: "1", match: true }
      - { name: "sustained_force", base: "3", port: "3", match: true }
      - { name: "sustained_refire", base: "0.1", port: "0.1", match: true }
      - { name: "spread_accuracy_formula", base: "spread_min < spread_max ? spread_min+accum : spread_min-accum", port: "same", match: true }
      - { name: "spread_crouchmod (C# fallback)", base: "1", port: "1", match: true }
    status:
      logic: faithful
      values: faithful
      timing: faithful
      presentation: na
      audio: na
      liveness: live
    intended_divergence: false
    divergence_rationale: null
    gaps: []
    verification:
      - { kind: code, ref: "W_MachineGun_Attack_Auto vs AttackAuto 192-218 (raw damage credit, heat scales dealt, spread accum persists)", result: pass }
      - { kind: code, ref: "Machinegun.cs:105 spread_crouchmod fallback 1 (matches bal-wep-xonotic.cfg)", result: pass }
      - { kind: code, ref: "WrThink:121 sets st.MiscBulletCounter=0 before AttackAuto (QC machinegun.qc:293)", result: pass }
    confidence: high
    notes: "Faithful and unchanged by the fix-up. Live values all match cfg. Fire sound (uzi_fire) plays per shot in FireOne. (The spread_crouchmod 0.25->1 and counter-reset fixes were already landed at the prior audit.)"

  - id: machinegun.fire.burst
    name: "Burst secondary (3-round no-spread burst, count-up scheduling, cooldown)"
    layer: [authority]
    base_refs:
      - "common/weapons/weapon/machinegun.qc:W_MachineGun_Attack_Burst"
      - "common/weapons/weapon/machinegun.qc:wr_think (burst setup: to_shoot/burst_fraction/to_use)"
    port_refs:
      - "src/XonoticGodot.Common/Gameplay/Weapons/Machinegun.cs:AttackBurst"
      - "src/XonoticGodot.Common/Gameplay/Weapons/Machinegun.cs:WrThink (burst branch 125-162)"
    constants:
      - { name: "burst", base: "3", port: "3", match: true }
      - { name: "burst_ammo", base: "3", port: "3", match: true }
      - { name: "burst_refire", base: "0.06", port: "0.06", match: true }
      - { name: "burst_refire2", base: "0.45", port: "0.45", match: true }
      - { name: "burst_animtime", base: "0.3", port: "0.3", match: true }
      - { name: "burst_spread (C# fallback)", base: "0", port: "0", match: true }
      - { name: "deathtype secondary bit", base: "thiswep.m_id (NO HITTYPE_SECONDARY on burst)", port: "RegistryId (no secondary bit)", match: true }
    status:
      logic: faithful
      values: faithful
      timing: faithful
      presentation: na
      audio: na
      liveness: live
    intended_divergence: false
    divergence_rationale: null
    gaps: []
    verification:
      - { kind: code, ref: "W_MachineGun_Attack_Burst + wr_think burst setup vs AttackBurst 223-261 + WrThink burst branch (count-up, burst_fraction, to_use)", result: pass }
      - { kind: code, ref: "Base burst passes thiswep.m_id (NO HITTYPE_SECONDARY) -> MURDER_SPRAY; port burst passes RegistryId (no secondary bit) -> SPRAY -> MATCH", result: pass }
    confidence: high
    notes: "Faithful and unchanged by the fix-up. Base's burst never ORs HITTYPE_SECONDARY, so both Base and port read MURDER_SPRAY for a burst kill — no SNIPE defect lives here. Scheduling/ammo/count-up logic faithful (misc_bulletcounter=-to_shoot counts up to 0; ammo decremented up front; toShoot==0 edge handled)."

  - id: machinegun.fire.single_mode0
    name: "Single 'first'/sustained shot (mode 0 primary + secondary snipe)"
    layer: [authority]
    base_refs:
      - "common/weapons/weapon/machinegun.qc:W_MachineGun_Attack"
      - "common/weapons/weapon/machinegun.qc:W_MachineGun_Attack_Frame"
    port_refs:
      - "src/XonoticGodot.Common/Gameplay/Weapons/Machinegun.cs:AttackSingle"
      - "src/XonoticGodot.Common/Gameplay/Weapons/Machinegun.cs:FireOne (deathTag = WithHitType(.., Secondary))"
    constants:
      - { name: "first_damage", base: "14", port: "14", match: true }
      - { name: "first_spread", base: "0.03", port: "0.03", match: true }
      - { name: "first_refire", base: "0.125", port: "0.125", match: true }
      - { name: "first_ammo", base: "1", port: "1", match: true }
      - { name: "first_force", base: "3", port: "3", match: true }
      - { name: "secondary deathtype bit", base: "thiswep.m_id | HITTYPE_SECONDARY", port: "FromWeapon(machinegun)|secondary (FIXED)", match: true }
    status:
      logic: partial
      values: faithful
      timing: faithful
      presentation: na
      audio: na
      liveness: dead
    intended_divergence: false
    divergence_rationale: null
    gaps:
      - "mode 0 is not the bundled default (mode 1), so AttackSingle is dead on the live path."
      - "The W_MachineGun_Attack_Frame auto-continuation loop (held-fire repeat for mode-0 primary) is not separately modeled — AttackSingle fires once per gated call rather than via a self-rescheduling frame think."
    verification:
      - { kind: code, ref: "cfg g_balance_machinegun_mode 1 -> mode-0 branch unreached (AttackSingle dead)", result: pass }
      - { kind: code, ref: "FireOne(secondary:true) sets deathTag=WithHitType(FromWeapon(machinegun),Secondary) -> FireBullet(deathTag) -> ApplyDamage(deathTag) -> DeathMessages HasHitType(.,Secondary)=SNIPE — the secondary hittype bit is now carried", result: pass }
    confidence: high
    notes: "FIXED since baseline (logic): the mode-0 secondary snipe now ORs HITTYPE_SECONDARY into the deathtype via the deathTag override (FireOne -> FireBullet -> ApplyDamage), so the SNIPE obituary would be produced if mode 0 were enabled. logic kept partial because the W_MachineGun_Attack_Frame held-fire self-continuation loop is still not modeled and the mode-0 `first` gate is not re-checked. liveness stays dead: mode 0 is non-default, so this whole path is unreachable in normal play (matching Base, where SNIPE is also only reachable via the mode-0 secondary)."

  - id: machinegun.spread.accumulation
    name: "Spread accumulation: time-decay (default) + legacy counter branch"
    layer: [authority]
    base_refs:
      - "common/weapons/weapon/machinegun.qc:MachineGun_Update_Spread"
    port_refs:
      - "src/XonoticGodot.Common/Gameplay/Weapons/Machinegun.cs:UpdateSpread"
    constants:
      - { name: "spread_min", base: "0.02", port: "0.02", match: true }
      - { name: "spread_max", base: "0.05", port: "0.05", match: true }
      - { name: "spread_add", base: "0.012", port: "0.012", match: true }
      - { name: "spread_decay", base: "0.048", port: "0.048", match: true }
    status:
      logic: faithful
      values: faithful
      timing: faithful
      presentation: na
      audio: na
      liveness: live
    intended_divergence: false
    divergence_rationale: null
    gaps: []
    verification:
      - { kind: code, ref: "MachineGun_Update_Spread vs UpdateSpread 317-332 (both branches present, bound/clamp matches)", result: pass }
      - { kind: code, ref: "Machinegun.cs:104 spread_decay fallback 0.048 (matches bal-wep-xonotic.cfg) -> live behavior = time-decay branch", result: pass }
    confidence: high
    notes: "Faithful and unchanged by the fix-up. Both branches ported; live behavior = decay branch (decay fallback fix already landed at the prior audit)."

  - id: machinegun.heat
    name: "Barrel-heat damage multiplier (cold/heat by spread accumulation)"
    layer: [authority]
    base_refs:
      - "common/weapons/weapon/machinegun.qc:MachineGun_Heat"
    port_refs:
      - "src/XonoticGodot.Common/Gameplay/Weapons/Machinegun.cs:Heat"
    constants:
      - { name: "spread_cold_damagemultiplier", base: "1", port: "1 (read live)", match: true }
      - { name: "spread_heat_damagemultiplier", base: "1", port: "1 (read live)", match: true }
      - { name: "unset-multiplier compat", base: "multiplier ? pct*mult : pct (skip when 0)", port: "same (!=0f guard)", match: true }
    status:
      logic: faithful
      values: faithful
      timing: na
      presentation: na
      audio: na
      liveness: live
    intended_divergence: false
    divergence_rationale: null
    gaps: []
    verification:
      - { kind: code, ref: "MachineGun_Heat vs Heat 351-365 (cold/heat pct formula + 0-guard)", result: pass }
      - { kind: code, ref: "bal-wep-xonotic.cfg cold/heat_damagemultiplier both 1 -> 1.0 no-op, faithful", result: pass }
    confidence: high
    notes: "Unchanged by the fix-up; already faithful. Reads the live cold/heat mult cvars; default 1/1 makes it a 1.0 no-op."

  - id: machinegun.fire.bullet_trace
    name: "Shared bullet trace: spread, wall penetration, distance falloff, force, antilag"
    layer: [authority, shared]
    base_refs:
      - "server/weapons/tracing.qc:fireBullet_falloff"
      - "common/weapons/calculations.qc:W_CalculateSpread"
      - "server/weapons/tracing.qc:W_SetupShot_Dir_ProjectileSize_Range"
    port_refs:
      - "src/XonoticGodot.Common/Gameplay/Weapons/WeaponFiring.cs:FireBullet"
      - "src/XonoticGodot.Common/Gameplay/Weapons/WeaponFiring.cs:SetupShot"
      - "src/XonoticGodot.Common/Gameplay/Weapons/WeaponFiring.cs:CalculateSpread"
      - "src/XonoticGodot.Common/Gameplay/Weapons/Machinegun.cs:FireOne"
    constants:
      - { name: "solidpenetration", base: "13.1", port: "13.1", match: true }
      - { name: "max_shot_distance", base: "32768", port: "32768", match: true }
      - { name: "g_ballistics_solidpenetration_exponent", base: "1", port: "1 (const)", match: true }
      - { name: "g_ballistics_mindistance", base: "1", port: "1 (const)", match: true }
      - { name: "hitscan_spread_style", base: "0 (dir + randomvec*spread)", port: "style 0", match: true }
      - { name: "damagefalloff (all)", base: "0 (off)", port: "0", match: true }
      - { name: "EFFECT_BULLET tracer arg", base: "fireBullet_falloff(..., EFFECT_BULLET, true)", port: "FireBullet tracerEffect arg exists but MG passes none", match: false }
    status:
      logic: faithful
      values: faithful
      timing: faithful
      presentation: partial
      audio: na
      liveness: live
    intended_divergence: false
    divergence_rationale: null
    gaps:
      - "g_ballistics_solidpenetration_exponent and g_ballistics_mindistance are hardcoded consts (1,1) in FireBullet rather than live cvars; matches default but won't follow a ruleset that retunes ballistics."
      - "EFFECT_BULLET tracer trail still not drawn for the MachineGun. Wave-1 added a tracerEffect param to FireBullet (open-air >16u + in-solid >4u EmitTrail sweeps) and the BULLET effect IS registered (EffectsList.cs:230 -> tr_bullet), but Machinegun.FireOne (Machinegun.cs:294-295) calls FireBullet WITHOUT a tracerEffect, so the MG bullet tracer is absent. The seam is live for weapons that opt in; the MG does not. Base passes EFFECT_BULLET on every MG fireBullet call."
    verification:
      - { kind: code, ref: "fireBullet_falloff vs WeaponFiring.FireBullet (spread, penetration loop pow(frac,exp), double-hit guard, sky stop, falloff, LagComp, warpzone)", result: pass }
      - { kind: code, ref: "Machinegun.FireOne:294 FireBullet(...) has no tracerEffect arg -> no MG tracer", result: fail }
    confidence: high
    notes: "Antilag (LagComp.Begin/End) and warpzone-aware sweep ported. Headshot multiplier unused by the MG (0). The fix-up added a deathTag passthrough to FireBullet/ApplyDamage (used by the mode-0 snipe) but did NOT wire the MG tracer — presentation remains partial."

  - id: machinegun.recoil
    name: "Firing recoil (punchangle random kick unless g_norecoil)"
    layer: [authority]
    base_refs:
      - "common/weapons/weapon/machinegun.qc:W_MachineGun_Attack_Auto (punchangle, gated on !g_norecoil)"
    port_refs:
      - "src/XonoticGodot.Common/Gameplay/Weapons/Machinegun.cs:Recoil (336-344)"
    constants:
      - { name: "punch_xy", base: "random()-0.5", port: "Prandom.Float()-0.5", match: true }
      - { name: "g_norecoil gate", base: "if (!autocvar_g_norecoil) { ... }", port: "if g_norecoil != 0 return", match: true }
    status:
      logic: faithful
      values: faithful
      timing: na
      presentation: faithful
      audio: na
      liveness: live
    intended_divergence: false
    divergence_rationale: null
    gaps: []
    verification:
      - { kind: code, ref: "machinegun.qc (if !g_norecoil) vs Machinegun.Recoil 336-344 — early-return when g_norecoil != 0", result: pass }
    confidence: high
    notes: "Faithful and unchanged by the fix-up. Uses deterministic PRNG (Prandom) so client prediction agrees. MG calls SetupShot with recoil=0, so the punchangle kick is solely from Recoil()."

  - id: machinegun.presentation.muzzle_impact
    name: "Muzzle flash + bullet-impact particle effects + casing ejection"
    layer: [presentation]
    base_refs:
      - "common/weapons/weapon/machinegun.qc:W_MuzzleFlash (per shot)"
      - "common/weapons/weapon/machinegun.qc:wr_impacteffect (EFFECT_MACHINEGUN_IMPACT)"
      - "common/weapons/weapon/machinegun.qc:W_MachineGun_Attack_Auto/Burst (SpawnCasing, g_casings>=2)"
    port_refs:
      - "src/XonoticGodot.Common/Gameplay/Weapons/Machinegun.cs:FireOne (BulletImpactFx / Emit MUZZLEFLASH / EjectCasing)"
      - "src/XonoticGodot.Common/Gameplay/Weapons/WeaponFiring.cs:BulletImpactFx / EjectCasing"
      - "src/XonoticGodot.Common/Gameplay/Effects/EffectsList.cs (CASING_BULLET/CASING_SHELL register)"
      - "game/client/EffectSystem.cs:454 (Spawn -> SpawnCasing) ; game/client/ShellCasings.cs:Spawn"
    constants:
      - { name: "impact_effect", base: "EFFECT_MACHINEGUN_IMPACT", port: "MACHINEGUN_IMPACT -> machinegun_impact", match: true }
      - { name: "muzzleflash_effect", base: "EFFECT_MACHINEGUN_MUZZLEFLASH", port: "MACHINEGUN_MUZZLEFLASH -> uzi_muzzleflash", match: true }
      - { name: "impact_org2", base: "w_org + w_backoff*2", port: "impactPos + backoff*2 (BulletImpactFx)", match: true }
      - { name: "casings (g_casings>=2)", base: "SpawnCasing per shot (casingtype 3 bullet)", port: "EjectCasing -> CASING_BULLET (networks + client SpawnCasing)", match: true }
      - { name: "casing eject velocity", base: "(rand*50+50)*right - (rand*25+25)*fwd + (70-rand*5)*up", port: "same (Prandom)", match: true }
    status:
      logic: faithful
      values: faithful
      timing: faithful
      presentation: faithful
      audio: na
      liveness: live
    intended_divergence: false
    divergence_rationale: null
    gaps: []
    verification:
      - { kind: code, ref: "BulletImpactFx emits MACHINEGUN_IMPACT at impactPos+backoff*2 (live); muzzleflash registered + emitted in FireOne", result: pass }
      - { kind: code, ref: "EjectCasing -> EmitByEffectInfoName('casing_bullet') resolves CASING_BULLET (NetName casing_bullet); WriteEffect networks (Effect non-null, RegistryId + velocity flag); DecodeEffect -> OnEffectReceived (name=Effect.Name 'CASING_BULLET') -> EffectSystem.Spawn -> SpawnCasing -> ShellCasings.Spawn", result: pass }
      - { kind: code, ref: "g_casings default 2 in xonotic-server.cfg:231 -> bullet-casing gate (>=2) passes; firing player has EntFlags.Client (SpawnSystem.cs:515) -> EjectCasing actor-gate passes", result: pass }
    confidence: high
    notes: "FIXED since baseline (liveness partial->live, presentation partial->faithful). The casing ejection is now genuinely live END-TO-END: CASING_BULLET/CASING_SHELL are registered Effects (stable RegistryId, NetName casing_bullet/casing_shell), so EmitByEffectInfoName resolves a non-null Effect, ServerNet.WriteEffect networks it (no longer dropped), the client decodes by id and routes Effect.Name='CASING_BULLET' (and the effectinfo alias) to EffectSystem.SpawnCasing -> ShellCasings.Spawn (real bouncing brass with ModelLoader/fallback). g_casings 2 is the cfg default and the eject velocity matches QC (per-casingtype up base 70). Muzzleflash + impact puff remain live/faithful."

  - id: machinegun.audio.impact_ricochet
    name: "Bullet-impact random ricochet sound (SND_RIC_RANDOM)"
    layer: [presentation]
    base_refs:
      - "common/weapons/weapon/machinegun.qc:wr_impacteffect (sound CH_SHOTS SND_RIC_RANDOM)"
      - "common/sounds/all.inc (SND_RIC_RANDOM -> ric1/ric2/ric3)"
    port_refs:
      - "src/XonoticGodot.Common/Gameplay/Weapons/WeaponFiring.cs:BulletImpactFx (-> SoundSystem.PlayRic)"
      - "src/XonoticGodot.Common/Gameplay/Weapons/Machinegun.cs:FireOne (BulletImpactFx, silent gate)"
      - "src/XonoticGodot.Common/Gameplay/Sounds/SoundSystem.cs:PlayRic / SoundVariantGroups.Ric"
    constants:
      - { name: "ric_sounds", base: "ric1/ric2/ric3 random", port: "SoundSystem.PlayRic -> RIC group ric1..3 (live caller)", match: true }
      - { name: "channel", base: "CH_SHOTS, VOL_BASE, ATTN_NORM", port: "PlayOn default channel/vol/attn", match: true }
      - { name: "silent gate", base: "if (!w_issilent)", port: "silent = sky surface || trace miss", match: true }
    status:
      logic: faithful
      values: faithful
      timing: faithful
      presentation: faithful
      audio: faithful
      liveness: live
    intended_divergence: false
    divergence_rationale: null
    gaps:
      - "Minor: Base plays the ric client-side in CSQC wr_impacteffect; the port plays it server-side on the actor (broadcast to clients). Audibly equivalent. Base's w_issilent is set per-surface (e.g. nodamage/sky); the port approximates it as sky-surface-hit OR trace-miss only, so a few special non-sky 'silent' surfaces would still ping. Cosmetic."
    verification:
      - { kind: code, ref: "FireOne:306 -> WeaponFiring.BulletImpactFx -> SoundSystem.PlayRic(actor); LIVE per impact", result: pass }
      - { kind: code, ref: "SoundVariantGroups.Ric() picks RIC1..3 (registered) -> ric1/2/3 present in assets", result: pass }
    confidence: high
    notes: "Faithful and unchanged by the fix-up. The shared BulletImpactFx seam invokes SoundSystem.PlayRic on every bullet impact; RIC group resolves to the real ric1..3 samples. Shared with all bullet weapons (rifle/shotgun/okmg also call BulletImpactFx)."

  - id: machinegun.bot.wr_aim
    name: "Bot fire logic (wr_aim distance-based primary/secondary selection)"
    layer: [authority]
    base_refs:
      - "common/weapons/weapon/machinegun.qc:wr_aim"
    port_refs:
      - "src/XonoticGodot.Server/Bot/BotBrain.cs (generic primary-only fire, ButtonAttack2=false at :382)"
    constants:
      - { name: "switch_distance", base: "3000 - bound(0,skill,10)*200", port: "none (generic close/far)", match: false }
      - { name: "secondary_use", base: "ATCK2 burst beyond range", port: "ButtonAttack2 forced false", match: false }
    status:
      logic: missing
      values: missing
      timing: na
      presentation: na
      audio: na
      liveness: dead
    intended_divergence: false
    divergence_rationale: null
    gaps:
      - "Bots never use the MachineGun's long-range no-spread burst secondary: the per-weapon wr_aim (which presses ATCK2 beyond ~3000-skill*200 units) is still not ported; BotBrain fires primary only with ButtonAttack2=false (BotBrain.cs:382; comment at :342-343 confirms it is deferred). Unchanged by the fix-up."
    verification:
      - { kind: code, ref: "BotBrain.cs:342-343 comment 'the deferred per-weapon wr_aim'; ButtonAttack2=false at :382", result: fail }
      - { kind: code, ref: "grep WrAim/wr_aim in Machinegun.cs -> absent", result: fail }
    confidence: high
    notes: "Unchanged by the fix-up. Generic bot fire works (bots shoot the MG primary), so this is a behavioral subtlety, not a dead weapon. Affects bot effectiveness at long range. Shared deferral across all weapons with a wr_aim."

  - id: machinegun.notification.kill_suicide
    name: "Kill/suicide obituary (MURDER_SNIPE vs MURDER_SPRAY, THINKING_WITH_PORTALS suicide)"
    layer: [authority]
    base_refs:
      - "common/weapons/weapon/machinegun.qc:wr_killmessage"
      - "common/weapons/weapon/machinegun.qc:wr_suicidemessage"
    port_refs:
      - "src/XonoticGodot.Common/Gameplay/Notifications/DeathMessages.cs:73 (SelectKillMessage)"
      - "src/XonoticGodot.Common/Gameplay/Notifications/DeathMessages.cs:150 (SelectSuicideMessage)"
      - "src/XonoticGodot.Server/Scores.cs (obituary wiring)"
    constants:
      - { name: "murder_secondary", base: "HITTYPE_SECONDARY ? MURDER_SNIPE : MURDER_SPRAY", port: "sec ? SNIPE : SPRAY (HasHitType secondary)", match: true }
      - { name: "suicide", base: "WEAPON_THINKING_WITH_PORTALS", port: "WEAPON_THINKING_WITH_PORTALS", match: true }
    status:
      logic: faithful
      values: faithful
      timing: na
      presentation: faithful
      audio: na
      liveness: partial
    intended_divergence: false
    divergence_rationale: null
    gaps:
      - "The message-SELECTION code is faithful and wired live (Scores.cs obituary path calls SelectKillMessage/SelectSuicideMessage; DeathMessages keys SNIPE/SPRAY on HasHitType(deathType, Secondary)). The SNIPE branch is reachable in Base ONLY from the mode-0 secondary, which now correctly carries HITTYPE_SECONDARY in the port too (see machinegun.fire.single_mode0) — but mode 0 is non-default and dead, so in default mode-1 play BOTH Base and the port emit MURDER_SPRAY (faithful)."
    verification:
      - { kind: code, ref: "DeathMessages.cs:73 machinegun SNIPE/SPRAY split + suicide THINKING_WITH_PORTALS:150, wired in Scores.cs", result: pass }
      - { kind: code, ref: "Default mode 1: Base burst uses m_id (no secondary) -> SPRAY; port burst uses RegistryId -> SPRAY -> MATCH. SNIPE only via the (dead) mode-0 secondary, which now sets the secondary bit.", result: pass }
    confidence: high
    notes: "Selection table faithful and default-mode-1 behavior matches Base (both SPRAY). liveness kept partial because the SNIPE message itself is only reachable via the (dead, non-default) mode-0 secondary — but the mode-0 secondary's HITTYPE_SECONDARY bit is now FIXED (machinegun.fire.single_mode0), so the residual is purely the dead mode-0 path, not a missing-bit defect. Spray kills + the suicide line are produced correctly."
