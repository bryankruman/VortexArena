unit: weapon-machinegun
category: weapon
base_ref_rev: "v0.8.6-1779-g863cd3e84"
last_audited: "2026-06-22"
summary:
  feature_count: 13
  worst_gaps:
    - "Casing ejection wired but DEAD on the live path: EjectCasing routes through EmitByEffectInfoName('casing_bullet'), which has no registered Effect, and ServerNet.WriteEffect drops every null-Effect request — the casing emission is never networked, so no shells eject (the Wave-2 change compiles but isn't reachable)."
    - "Bot wr_aim distance switch still missing: bots never use the long-range no-spread burst secondary (ButtonAttack2 still forced false in BotBrain.cs:374)."
    - "EFFECT_BULLET tracer trail not wired for the MachineGun: FireBullet gained a tracerEffect arg (Wave-1 seam) but Machinegun.FireOne never passes it, so the bullet tracer is still not drawn."
    - "mode-0 secondary 'snipe' still does not OR HITTYPE_SECONDARY, so the SNIPE obituary is unreachable — but this only matters in non-default mode 0 (Base's default-mode-1 burst ALSO omits the secondary bit and reads SPRAY, so the burst path is faithful; baseline over-stated this)."
features:
  - id: machinegun.identity
    name: "MachineGun registration & identity (flags, color, ammo, models, impulse)"
    layer: [shared]
    base_refs:
      - "common/weapons/weapon/machinegun.qh:CLASS(MachineGun)"
    port_refs:
      - "src/XonoticGodot.Common/Gameplay/Weapons/Machinegun.cs:Machinegun (ctor)"
    constants:
      - { name: "netname", base: "machinegun", port: "machinegun", match: true }
      - { name: "ammo_type", base: "RES_BULLETS", port: "ResourceType.Bullets", match: true }
      - { name: "impulse", base: "3", port: "3", match: true }
      - { name: "color", base: "'0.678 0.886 0.267'", port: "(0.678,0.886,0.267)", match: true }
      - { name: "spawnflags", base: "NORMAL|RELOADABLE|TYPE_HITSCAN|PENETRATEWALLS|BLEED", port: "Normal|Reloadable|TypeHitscan|PenetrateWalls|Bleed", match: true }
      - { name: "view_model", base: "h_uzi.iqm", port: "h_uzi.iqm", match: true }
      - { name: "world_model", base: "v_uzi.md3", port: "v_uzi.md3", match: true }
      - { name: "item_model", base: "g_uzi.md3", port: "g_uzi.md3", match: true }
    status:
      logic: faithful
      values: faithful
      timing: na
      presentation: na
      audio: na
      liveness: live
    intended_divergence: false
    divergence_rationale: null
    gaps: []
    verification:
      - { kind: code, ref: "machinegun.qh ATTRIBs vs Machinegun.cs ctor (DisplayName/NetName/Impulse/Color/flags/models)", result: pass }
    confidence: high
    notes: "Unchanged by Wave-2. Pure registration row; actual viewmodel/muzzleflash rendering covered by machinegun.presentation.muzzle_impact. bot_pickupbasevalue 7000 not ported (out of scope)."

  - id: machinegun.firemode.dispatch
    name: "wr_think fire-mode dispatch (mode 0 vs mode 1, primary/secondary routing)"
    layer: [authority]
    base_refs:
      - "common/weapons/weapon/machinegun.qc:wr_think"
    port_refs:
      - "src/XonoticGodot.Common/Gameplay/Weapons/Machinegun.cs:WrThink"
      - "src/XonoticGodot.Server/GameWorld.cs:1182 (WeaponFireDriver.Frame)"
    constants:
      - { name: "mode", base: "1", port: "1 (live from cfg)", match: true }
      - { name: "burst_gate", base: "(fire&2) && burst", port: "Secondary && Burst>0", match: true }
      - { name: "first_gate (mode0 sec)", base: "(fire&2) && first", port: "Secondary (first not re-checked)", match: false }
      - { name: "secondary-as-zoom gate", base: "wr_zoom: secondary is zoom when burst==0 (mode1) / first==0 (mode0)", port: "not modeled (Burst>0 gate only)", match: false }
    status:
      logic: faithful
      values: faithful
      timing: faithful
      presentation: na
      audio: na
      liveness: live
    intended_divergence: false
    divergence_rationale: null
    gaps:
      - "mode-0 secondary path does not re-check the `first` cvar gate (Base requires (fire&2) && first); harmless at default first=1."
      - "The secondary-as-zoom behavior (Base wr_zoom: when burst==0 the secondary becomes a zoom, not a fire mode) is not modeled; harmless at default burst=3 but a custom balance with burst 0 would zoom in Base and do nothing here."
    verification:
      - { kind: code, ref: "machinegun.qc:276-350 wr_think mode 0/1 split vs Machinegun.WrThink 109-178", result: pass }
      - { kind: code, ref: "WeaponFireDriver.Frame -> WrThink(Primary)/WrThink(Secondary) live", result: pass }
    confidence: high
    notes: "Unchanged structurally by Wave-2 (the only WrThink edit was the MiscBulletCounter=0 reset in the auto branch, tracked under machinegun.fire.auto). Dispatch logic faithful; the zoom + mode-0 first gate remain the two harmless-at-default gaps."

  - id: machinegun.fire.auto
    name: "Sustained automatic primary fire (spread accumulation + per-shot damage/ammo)"
    layer: [authority]
    base_refs:
      - "common/weapons/weapon/machinegun.qc:W_MachineGun_Attack_Auto"
    port_refs:
      - "src/XonoticGodot.Common/Gameplay/Weapons/Machinegun.cs:AttackAuto"
      - "src/XonoticGodot.Common/Gameplay/Weapons/Machinegun.cs:WrThink (MiscBulletCounter=0 reset, :121)"
    constants:
      - { name: "sustained_damage", base: "10", port: "10", match: true }
      - { name: "sustained_ammo", base: "1", port: "1", match: true }
      - { name: "sustained_force", base: "3", port: "3", match: true }
      - { name: "sustained_refire", base: "0.1", port: "0.1", match: true }
      - { name: "spread_accuracy_formula", base: "spread_min < spread_max ? spread_min+accum : spread_min-accum", port: "same", match: true }
      - { name: "spread_crouchmod (C# fallback)", base: "1", port: "1 (FIXED 0.25->1)", match: true }
    status:
      logic: faithful
      values: faithful
      timing: faithful
      presentation: na
      audio: na
      liveness: live
    intended_divergence: false
    divergence_rationale: null
    gaps: []
    verification:
      - { kind: code, ref: "W_MachineGun_Attack_Auto 146-213 vs AttackAuto 192-218 (raw damage credit, heat scales dealt, spread accum persists)", result: pass }
      - { kind: code, ref: "Machinegun.cs:105 spread_crouchmod fallback now 1 (matches bal-wep-xonotic.cfg)", result: pass }
      - { kind: code, ref: "WrThink:121 sets st.MiscBulletCounter=0 before AttackAuto (QC machinegun.qc:293)", result: pass }
    confidence: high
    notes: "FIXED since baseline: (1) the spread_crouchmod C# fallback 0.25->1 (now matches Base on a config-less path); (2) WrThink resets MiscBulletCounter=0 on a fresh auto trigger pull (QC machinegun.qc:293), closing the baseline counter-reset note. Live values all match cfg. Fire sound (uzi_fire) plays per shot in FireOne."

  - id: machinegun.fire.burst
    name: "Burst secondary (3-round no-spread burst, count-up scheduling, cooldown)"
    layer: [authority]
    base_refs:
      - "common/weapons/weapon/machinegun.qc:W_MachineGun_Attack_Burst"
      - "common/weapons/weapon/machinegun.qc:wr_think (burst setup: to_shoot/burst_fraction/to_use)"
    port_refs:
      - "src/XonoticGodot.Common/Gameplay/Weapons/Machinegun.cs:AttackBurst"
      - "src/XonoticGodot.Common/Gameplay/Weapons/Machinegun.cs:WrThink (burst branch 125-162)"
    constants:
      - { name: "burst", base: "3", port: "3", match: true }
      - { name: "burst_ammo", base: "3", port: "3", match: true }
      - { name: "burst_refire", base: "0.06", port: "0.06", match: true }
      - { name: "burst_refire2", base: "0.45", port: "0.45", match: true }
      - { name: "burst_animtime", base: "0.3", port: "0.3", match: true }
      - { name: "burst_spread (C# fallback)", base: "0", port: "0 (FIXED 0.04->0)", match: true }
      - { name: "deathtype secondary bit", base: "thiswep.m_id (NO HITTYPE_SECONDARY on burst)", port: "RegistryId (no secondary bit)", match: true }
    status:
      logic: faithful
      values: faithful
      timing: faithful
      presentation: na
      audio: na
      liveness: live
    intended_divergence: false
    divergence_rationale: null
    gaps: []
    verification:
      - { kind: code, ref: "W_MachineGun_Attack_Burst 215-267 + wr_think 298-329 vs AttackBurst 223-261 + WrThink burst branch (count-up, burst_fraction, to_use)", result: pass }
      - { kind: code, ref: "Base W_MachineGun_Attack_Burst passes thiswep.m_id (NO HITTYPE_SECONDARY) to fireBullet_falloff; port FireOne passes RegistryId (no secondary bit) -> both read MURDER_SPRAY -> MATCH", result: pass }
      - { kind: code, ref: "Machinegun.cs:103 burst_spread fallback now 0 (matches bal-wep-xonotic.cfg)", result: pass }
    confidence: high
    notes: "CORRECTED from baseline + FIXED. Baseline claimed burst kills wrongly read SPRAY-instead-of-SNIPE; adversarial re-read of Base shows W_MachineGun_Attack_Burst passes thiswep.m_id WITHOUT HITTYPE_SECONDARY, so Base burst ALSO reads MURDER_SPRAY — the port is faithful here, no defect. Separately FIXED: burst_spread C# fallback 0.04->0. Scheduling/ammo/count-up logic faithful (misc_bulletcounter=-to_shoot counts up to 0; ammo decremented up front; toShoot==0 edge handled)."

  - id: machinegun.fire.single_mode0
    name: "Single 'first'/sustained shot (mode 0 primary + secondary snipe)"
    layer: [authority]
    base_refs:
      - "common/weapons/weapon/machinegun.qc:W_MachineGun_Attack"
      - "common/weapons/weapon/machinegun.qc:W_MachineGun_Attack_Frame"
    port_refs:
      - "src/XonoticGodot.Common/Gameplay/Weapons/Machinegun.cs:AttackSingle"
    constants:
      - { name: "first_damage", base: "14", port: "14", match: true }
      - { name: "first_spread", base: "0.03", port: "0.03", match: true }
      - { name: "first_refire", base: "0.125", port: "0.125", match: true }
      - { name: "first_ammo", base: "1", port: "1", match: true }
      - { name: "first_force", base: "3", port: "3", match: true }
      - { name: "secondary deathtype bit", base: "thiswep.m_id | HITTYPE_SECONDARY", port: "RegistryId only", match: false }
    status:
      logic: partial
      values: faithful
      timing: faithful
      presentation: na
      audio: na
      liveness: dead
    intended_divergence: false
    divergence_rationale: null
    gaps:
      - "mode 0 is not the bundled default (mode 1), so AttackSingle is dead on the live path."
      - "The W_MachineGun_Attack_Frame auto-continuation loop (held-fire repeat for mode-0 primary) is not separately modeled — AttackSingle fires once per gated call rather than via a self-rescheduling frame think."
      - "The mode-0 secondary 'snipe' (AttackSingle secondary:true) does not OR in HITTYPE_SECONDARY into the bullet deathtype, so even if mode 0 were enabled the snipe kill would read SPRAY not SNIPE. This is the ONLY MachineGun path where Base actually sets the secondary bit (wr_think: m_id | HITTYPE_SECONDARY), so it is the sole genuine source of the dead SNIPE obituary — and it only matters in non-default mode 0."
    verification:
      - { kind: code, ref: "cfg g_balance_machinegun_mode 1 -> mode-0 branch unreached", result: pass }
      - { kind: code, ref: "W_MachineGun_Attack 57-118 vs AttackSingle 268-281; secondary path passes RegistryId, no HITTYPE_SECONDARY", result: fail }
    confidence: high
    notes: "Values/refire faithful; logic partial (frame-loop continuation, the `first` gate, and the missing secondary hittype all differ). Reachable only if a server sets mode 0. Note vs baseline: this row (not the burst) is now the true home of the SNIPE-obituary defect."

  - id: machinegun.spread.accumulation
    name: "Spread accumulation: time-decay (default) + legacy counter branch"
    layer: [authority]
    base_refs:
      - "common/weapons/weapon/machinegun.qc:MachineGun_Update_Spread"
    port_refs:
      - "src/XonoticGodot.Common/Gameplay/Weapons/Machinegun.cs:UpdateSpread"
    constants:
      - { name: "spread_min", base: "0.02", port: "0.02", match: true }
      - { name: "spread_max", base: "0.05", port: "0.05", match: true }
      - { name: "spread_add", base: "0.012", port: "0.012", match: true }
      - { name: "spread_decay", base: "0.048", port: "0.048 (FIXED fallback 0->0.048)", match: true }
    status:
      logic: faithful
      values: faithful
      timing: faithful
      presentation: na
      audio: na
      liveness: live
    intended_divergence: false
    divergence_rationale: null
    gaps: []
    verification:
      - { kind: code, ref: "MachineGun_Update_Spread 8-29 vs UpdateSpread 309-324 (both branches present, bound/clamp matches)", result: pass }
      - { kind: code, ref: "Machinegun.cs:104 spread_decay fallback now 0.048 (matches bal-wep-xonotic.cfg) — config-less path now uses the correct decay branch, not the legacy counter branch", result: pass }
    confidence: high
    notes: "FIXED since baseline: the C# Configure() fallback for spread_decay 0->0.048, so a config-less path no longer silently flips the MG to the legacy Nexuiz counter-spread model. Both branches were already ported faithfully; live behavior = decay branch."

  - id: machinegun.heat
    name: "Barrel-heat damage multiplier (cold/heat by spread accumulation)"
    layer: [authority]
    base_refs:
      - "common/weapons/weapon/machinegun.qc:MachineGun_Heat"
    port_refs:
      - "src/XonoticGodot.Common/Gameplay/Weapons/Machinegun.cs:Heat"
    constants:
      - { name: "spread_cold_damagemultiplier", base: "1", port: "1 (read live)", match: true }
      - { name: "spread_heat_damagemultiplier", base: "1", port: "1 (read live)", match: true }
      - { name: "unset-multiplier compat", base: "multiplier ? pct*mult : pct (skip when 0)", port: "same (!=0f guard)", match: true }
    status:
      logic: faithful
      values: faithful
      timing: na
      presentation: na
      audio: na
      liveness: live
    intended_divergence: false
    divergence_rationale: null
    gaps: []
    verification:
      - { kind: code, ref: "MachineGun_Heat 31-55 vs Heat 343-357 (cold/heat pct formula + 0-guard)", result: pass }
      - { kind: code, ref: "bal-wep-xonotic.cfg cold/heat_damagemultiplier both 1 -> 1.0 no-op, faithful", result: pass }
    confidence: high
    notes: "Unchanged by Wave-2; already faithful. Reads the live cold/heat mult cvars so it stays faithful if a balance set retunes them; default 1/1 makes it a 1.0 no-op."

  - id: machinegun.fire.bullet_trace
    name: "Shared bullet trace: spread, wall penetration, distance falloff, force, antilag"
    layer: [authority, shared]
    base_refs:
      - "server/weapons/tracing.qc:fireBullet_falloff"
      - "common/weapons/calculations.qc:W_CalculateSpread"
      - "server/weapons/tracing.qc:W_SetupShot_Dir_ProjectileSize_Range"
    port_refs:
      - "src/XonoticGodot.Common/Gameplay/Weapons/WeaponFiring.cs:FireBullet"
      - "src/XonoticGodot.Common/Gameplay/Weapons/WeaponFiring.cs:SetupShot"
      - "src/XonoticGodot.Common/Gameplay/Weapons/WeaponFiring.cs:CalculateSpread"
      - "src/XonoticGodot.Common/Gameplay/Weapons/Machinegun.cs:FireOne"
    constants:
      - { name: "solidpenetration", base: "13.1", port: "13.1", match: true }
      - { name: "max_shot_distance", base: "32768", port: "32768", match: true }
      - { name: "g_ballistics_solidpenetration_exponent", base: "1", port: "1 (const)", match: true }
      - { name: "g_ballistics_mindistance", base: "1", port: "1 (const)", match: true }
      - { name: "hitscan_spread_style", base: "0 (dir + randomvec*spread)", port: "style 0", match: true }
      - { name: "damagefalloff (all)", base: "0 (off)", port: "0", match: true }
      - { name: "EFFECT_BULLET tracer arg", base: "fireBullet_falloff(..., EFFECT_BULLET, true)", port: "FireBullet tracerEffect arg exists but MG passes null", match: false }
    status:
      logic: faithful
      values: faithful
      timing: faithful
      presentation: partial
      audio: na
      liveness: live
    intended_divergence: false
    divergence_rationale: null
    gaps:
      - "g_ballistics_solidpenetration_exponent and g_ballistics_mindistance are hardcoded consts (1,1) in FireBullet rather than live cvars; matches default but won't follow a ruleset that retunes ballistics."
      - "EFFECT_BULLET tracer trail not drawn for the MachineGun: Wave-1 added a tracerEffect param to FireBullet (with EmitTrail sweeps for open-air >16u and in-solid >4u legs), but Machinegun.FireOne calls FireBullet WITHOUT a tracerEffect, so the MG bullet tracer is still absent. The seam is live for weapons that pass it (KeyHunt uses EmitTrail) but the MG does not opt in."
    verification:
      - { kind: code, ref: "fireBullet_falloff vs WeaponFiring.FireBullet (spread, penetration loop pow(frac,exp), double-hit guard, sky stop, falloff, LagComp, warpzone)", result: pass }
      - { kind: code, ref: "Machinegun.FireOne:286 FireBullet(...) has no tracerEffect arg -> no MG tracer", result: fail }
    confidence: high
    notes: "Antilag (LagComp.Begin/End) and warpzone-aware sweep ported. Headshot multiplier unused by the MG (0). Wave-1 added the tracer plumbing to the shared FireBullet but the MG was not wired to use EFFECT_BULLET — downgraded presentation to partial to record the still-missing MG tracer (previously bundled in a deferred note)."

  - id: machinegun.recoil
    name: "Firing recoil (punchangle random kick unless g_norecoil)"
    layer: [authority]
    base_refs:
      - "common/weapons/weapon/machinegun.qc:W_MachineGun_Attack_Auto (punchangle, 165-169, gated on !g_norecoil)"
    port_refs:
      - "src/XonoticGodot.Common/Gameplay/Weapons/Machinegun.cs:Recoil (330-336)"
    constants:
      - { name: "punch_xy", base: "random()-0.5", port: "Prandom.Float()-0.5", match: true }
      - { name: "g_norecoil gate", base: "if (!autocvar_g_norecoil) { ... }", port: "if g_norecoil != 0 return (FIXED)", match: true }
    status:
      logic: faithful
      values: faithful
      timing: na
      presentation: faithful
      audio: na
      liveness: live
    intended_divergence: false
    divergence_rationale: null
    gaps: []
    verification:
      - { kind: code, ref: "machinegun.qc:61/165/218 (if !g_norecoil) vs Machinegun.Recoil 328-336 — now returns early when g_norecoil != 0", result: pass }
    confidence: high
    notes: "FIXED since baseline: Recoil now gates on g_norecoil (early-return when set), matching Base's `if (!autocvar_g_norecoil)` wrapper. Uses deterministic PRNG (Prandom) so client prediction agrees. SetupShot's separate view-pitch recoil already gated, but MG calls SetupShot with recoil=0, so the punchangle kick is solely from Recoil()."

  - id: machinegun.presentation.muzzle_impact
    name: "Muzzle flash + bullet-impact particle effects + casing ejection"
    layer: [presentation]
    base_refs:
      - "common/weapons/weapon/machinegun.qc:W_MuzzleFlash (per shot)"
      - "common/weapons/weapon/machinegun.qc:wr_impacteffect (EFFECT_MACHINEGUN_IMPACT)"
      - "common/weapons/weapon/machinegun.qc:W_MachineGun_Attack_Auto/Burst (SpawnCasing, g_casings>=2)"
    port_refs:
      - "src/XonoticGodot.Common/Gameplay/Weapons/Machinegun.cs:FireOne (BulletImpactFx MACHINEGUN_IMPACT / Emit MACHINEGUN_MUZZLEFLASH / EjectCasing)"
      - "src/XonoticGodot.Common/Gameplay/Weapons/WeaponFiring.cs:BulletImpactFx / EjectCasing"
      - "src/XonoticGodot.Common/Gameplay/Effects/EffectsList.cs:61-62"
    constants:
      - { name: "impact_effect", base: "EFFECT_MACHINEGUN_IMPACT", port: "MACHINEGUN_IMPACT -> machinegun_impact", match: true }
      - { name: "muzzleflash_effect", base: "EFFECT_MACHINEGUN_MUZZLEFLASH", port: "MACHINEGUN_MUZZLEFLASH -> uzi_muzzleflash", match: true }
      - { name: "impact_org2", base: "w_org + w_backoff*2", port: "impactPos + backoff*2 (BulletImpactFx)", match: true }
      - { name: "casings (g_casings>=2)", base: "SpawnCasing per shot", port: "EjectCasing wired but emission DROPPED (null Effect)", match: false }
    status:
      logic: faithful
      values: faithful
      timing: faithful
      presentation: partial
      audio: na
      liveness: partial
    intended_divergence: false
    divergence_rationale: null
    gaps:
      - "Brass casing ejection (g_casings>=2) is wired but DEAD on the live path. FireOne calls WeaponFiring.EjectCasing, which computes the QC view-frame eject velocity correctly and routes the casing through EffectEmitter.EmitByEffectInfoName('casing_bullet', ...). But there is NO registered Effect named 'casing_bullet', so the EffectRequest carries Effect=null, and ServerNet.WriteEffect / ln.Encode both drop null-Effect requests (return false/null). The casing emission is therefore never networked, and the client's EffectSystem.SpawnCasing is never called via this path. No shells eject. (Additionally the g_casings cvar appears unregistered in the port, so EjectCasing's internal `g_casings < 2` gate likely also short-circuits to no-op.)"
    verification:
      - { kind: code, ref: "BulletImpactFx emits MACHINEGUN_IMPACT at impactPos+backoff*2 (live, registered effect); muzzleflash registered + emitted in FireOne", result: pass }
      - { kind: code, ref: "EjectCasing -> EmitByEffectInfoName('casing_bullet') -> Effect=null; ServerNet.WriteEffect returns false when r.Effect is null (ServerNet.cs:2226-2228) -> casing never sent", result: fail }
    confidence: high
    notes: "Muzzleflash + impact particle remain live and faithful (and the impact now emits at the more-faithful w_org+backoff*2 with the real surface normal). The Wave-2 casing change is the textbook port failure mode: code added, compiles, but no live caller path reaches the client (the effect sink drops nameless emissions). Casing remains effectively NOT ported."

  - id: machinegun.audio.impact_ricochet
    name: "Bullet-impact random ricochet sound (SND_RIC_RANDOM)"
    layer: [presentation]
    base_refs:
      - "common/weapons/weapon/machinegun.qc:wr_impacteffect (sound CH_SHOTS SND_RIC_RANDOM, 419-420)"
      - "common/sounds/all.inc (SND_RIC_RANDOM -> ric1/ric2/ric3)"
    port_refs:
      - "src/XonoticGodot.Common/Gameplay/Weapons/WeaponFiring.cs:BulletImpactFx (-> SoundSystem.PlayRic)"
      - "src/XonoticGodot.Common/Gameplay/Weapons/Machinegun.cs:FireOne (BulletImpactFx, silent gate)"
      - "src/XonoticGodot.Common/Gameplay/Sounds/SoundSystem.cs:PlayRic / SoundVariantGroups.Ric"
    constants:
      - { name: "ric_sounds", base: "ric1/ric2/ric3 random", port: "SoundSystem.PlayRic -> RIC group ric1..3 (live caller)", match: true }
      - { name: "channel", base: "CH_SHOTS, VOL_BASE, ATTN_NORM", port: "PlayOn default channel/vol/attn", match: true }
      - { name: "silent gate", base: "if (!w_issilent)", port: "silent = sky surface || trace miss", match: true }
    status:
      logic: faithful
      values: faithful
      timing: faithful
      presentation: faithful
      audio: faithful
      liveness: live
    intended_divergence: false
    divergence_rationale: null
    gaps:
      - "Minor: Base plays the ric client-side in CSQC wr_impacteffect; the port plays it server-side on the actor (broadcast to clients). Audibly equivalent. Base's w_issilent is set per-surface (e.g. nodamage/sky); the port approximates it as sky-surface-hit OR trace-miss only, so a few special non-sky 'silent' surfaces would still ping. Cosmetic."
    verification:
      - { kind: code, ref: "FireOne:298 -> WeaponFiring.BulletImpactFx -> SoundSystem.PlayRic(actor) (WeaponFiring.cs:481); LIVE per impact", result: pass }
      - { kind: code, ref: "SoundVariantGroups.Ric() picks RIC1..3 (registered in SoundsList) -> ric1/2/3.ogg present in assets/data/.../sound/weapons/", result: pass }
    confidence: high
    notes: "FIXED since baseline (dead->live, missing->faithful). The previously-zero-caller SoundSystem.PlayRic is now invoked on every bullet impact via the shared BulletImpactFx seam; RIC group resolves to the real ric1..3 samples. Shared with all bullet weapons (rifle/shotgun/okmg also call BulletImpactFx)."

  - id: machinegun.bot.wr_aim
    name: "Bot fire logic (wr_aim distance-based primary/secondary selection)"
    layer: [authority]
    base_refs:
      - "common/weapons/weapon/machinegun.qc:wr_aim (269-275)"
    port_refs:
      - "src/XonoticGodot.Server/Bot/BotBrain.cs (generic primary-only fire, ButtonAttack2=false at :374)"
    constants:
      - { name: "switch_distance", base: "3000 - bound(0,skill,10)*200", port: "none (generic close/far)", match: false }
      - { name: "secondary_use", base: "ATCK2 burst beyond range", port: "ButtonAttack2 forced false", match: false }
    status:
      logic: missing
      values: missing
      timing: na
      presentation: na
      audio: na
      liveness: dead
    intended_divergence: false
    divergence_rationale: null
    gaps:
      - "Bots never use the MachineGun's long-range no-spread burst secondary: the per-weapon wr_aim (which presses ATCK2 beyond ~3000-skill*200 units) is still not ported; BotBrain fires primary only with ButtonAttack2=false. Wave-2 reworked BotBrain (sticky-enemy window, superbot target rating, idle reload) but did NOT add per-weapon wr_aim."
    verification:
      - { kind: code, ref: "BotBrain.cs:335 comment 'the deferred per-weapon wr_aim'; ButtonAttack2=false at :374 (still)", result: fail }
      - { kind: code, ref: "grep WrAim/wr_aim in Machinegun.cs -> absent", result: fail }
    confidence: high
    notes: "Unchanged by Wave-2 for this row. Generic bot fire works (bots shoot the MG primary), so this is a behavioral subtlety, not a dead weapon. Affects bot effectiveness at long range. Shared deferral across all weapons with a wr_aim."

  - id: machinegun.notification.kill_suicide
    name: "Kill/suicide obituary (MURDER_SNIPE vs MURDER_SPRAY, THINKING_WITH_PORTALS suicide)"
    layer: [authority]
    base_refs:
      - "common/weapons/weapon/machinegun.qc:wr_killmessage (398-404)"
      - "common/weapons/weapon/machinegun.qc:wr_suicidemessage (393-396)"
    port_refs:
      - "src/XonoticGodot.Common/Gameplay/Notifications/DeathMessages.cs:73 (SelectKillMessage)"
      - "src/XonoticGodot.Common/Gameplay/Notifications/DeathMessages.cs (SelectSuicideMessage)"
      - "src/XonoticGodot.Server/Scores.cs:643-644 / 565-566 (obituary wiring)"
    constants:
      - { name: "murder_secondary", base: "HITTYPE_SECONDARY ? MURDER_SNIPE : MURDER_SPRAY", port: "sec ? SNIPE : SPRAY (keyed on deathtype secondary bit)", match: true }
      - { name: "suicide", base: "WEAPON_THINKING_WITH_PORTALS", port: "WEAPON_THINKING_WITH_PORTALS", match: true }
    status:
      logic: faithful
      values: faithful
      timing: na
      presentation: faithful
      audio: na
      liveness: partial
    intended_divergence: false
    divergence_rationale: null
    gaps:
      - "The message-SELECTION code is faithful and wired live (Scores.cs obituary path calls SelectKillMessage/SelectSuicideMessage). The SNIPE branch is reachable in Base ONLY from the mode-0 secondary (the sole path that ORs HITTYPE_SECONDARY); the port's mode-0 secondary does not set that bit, so SNIPE stays unreachable — but mode 0 is non-default and dead, so in default mode-1 play BOTH Base and the port emit MURDER_SPRAY (faithful). The residual SNIPE gap lives in machinegun.fire.single_mode0, and is far narrower than the baseline implied."
    verification:
      - { kind: code, ref: "DeathMessages.cs:73 machinegun SNIPE/SPRAY split + suicide THINKING_WITH_PORTALS present, wired in Scores.cs:643", result: pass }
      - { kind: code, ref: "Default mode 1: Base burst uses m_id (no secondary) -> SPRAY; port burst uses RegistryId -> SPRAY -> MATCH", result: pass }
    confidence: high
    notes: "Re-graded logic partial->faithful and re-scoped vs baseline: the selection table is correct AND the default-mode-1 behavior now matches Base (both SPRAY) because Base's burst never carries the secondary bit. liveness kept partial because the SNIPE message itself is only reachable via the (dead, non-default) mode-0 secondary, whose missing hittype bit is tracked under machinegun.fire.single_mode0. Spray kills + the suicide line are produced correctly."
