unit: turret-flac
category: turret
base_ref_rev: "v0.8.6-1779-g863cd3e84"
last_audited: "2026-06-22"
summary:
  feature_count: 10
  worst_gaps:
    - "port turret_die does a min(ammo,50) RadiusDamage death blast that Base does NOT — the Base blast (sv_turrets.qc:182) is commented out; the port adds an unintended on-death explosion"
    - "fuse time uses DistAimPos/shotSpeed (muzzle->aim distance) instead of Base tur_impacttime, which comes from a forward tracebox to trace_endpos (sv_turrets.qc:523) — diverges when geometry stops the shot short"
    - "all presentation missing: PROJECTILE_HAGAR trail, BLASTER muzzleflash, head-frame cycle, TFL_DMG_HEADSHAKE head-shake, head model/bone"
    - "hidden player FLAC weapon (FlacAttack, impulse 5) not implemented"
    - "respawn schedules at now+respawntime; Base nextthink = time + respawntime - 0.2 (minor 0.2s timing offset)"
  notable_fixes_since_last_audit:
    - "Wave-2 closed the four numeric/aim gaps that dominated the prior audit: shot_spread 0.0125->0.02, target_range_optimal 2000->1250, aim_maxpitch 90->35, respawntime 60->90, the target_select_*bias weights (rangebias 0.25 / anglebias 0.5 / playerbias 0) now passed, the track accel_rot 0.7 / blendrate 0.2 now passed, and the unintended zPredict aim flag removed."
features:
  - id: turret-flac.identity.spawn
    name: "FLAC turret identity, hitbox, model, health, map spawn"
    layer: [authority]
    base_refs:
      - "common/turrets/turret/flac.qh:CLASS(Flac)"
      - "common/turrets/turret/flac.qc:spawnfunc(turret_flac)"
      - "common/turrets/sv_turrets.qc:turret_initialize"
    port_refs:
      - "src/XonoticGodot.Common/Gameplay/Turrets/FlacTurret.cs:FlacTurret(),Spawn"
      - "src/XonoticGodot.Common/Gameplay/Turrets/TurretSpawn.cs:Init"
      - "src/XonoticGodot.Common/Gameplay/Turrets/TurretSpawnFuncs.cs:Flac"
      - "src/XonoticGodot.Common/Gameplay/MapObjects/MapObjectsRegistry.cs:209"
    constants:
      - { name: "m_mins", base: "-32 -32 0", port: "-32 -32 0", match: true }
      - { name: "m_maxs", base: "32 32 64", port: "32 32 64", match: true }
      - { name: "health", base: "700", port: "700", match: true }
      - { name: "body model", base: "models/turrets/base.md3", port: "models/turrets/base.md3", match: true }
      - { name: "head model", base: "models/turrets/flac.md3", port: "none (no head bone)", match: false }
      - { name: "team default", base: "FLOAT_MAX", port: "float.MaxValue", match: true }
    status:
      logic: faithful
      values: faithful
      timing: na
      presentation: missing
      audio: na
      liveness: live
    intended_divergence: false
    divergence_rationale: null
    gaps:
      - "head model models/turrets/flac.md3 not set (no head-bone attachment in port)"
    verification:
      - { kind: code, ref: "FlacTurret ctor + TurretSpawn.Init; MapObjectsRegistry.cs:209 registers turret_flac", result: pass }
    confidence: high
    notes: "Identity/hitbox/health/body-model faithful. Head model + bone are presentation, deferred in port. spawnflags TUR_FLAG_SPLASH|FASTPROJ|MISSILE are Base attribs folded into the port's fire/aim params, not stored as a port flag field. (values upgraded partial->faithful: the only constant mismatch left is the head model, which is presentation, not a numeric value.)"

  - id: turret-flac.spawnfunc.liveness
    name: "turret_flac map spawnfunc wired to a live per-frame think"
    layer: [authority]
    base_refs:
      - "common/turrets/turret/flac.qc:spawnfunc(turret_flac)"
      - "common/turrets/sv_turrets.qc:turret_link (setthink turret_think, nextthink=time)"
    port_refs:
      - "src/XonoticGodot.Common/Gameplay/Turrets/TurretSpawnFuncs.cs:Spawn,Flac"
      - "src/XonoticGodot.Common/Gameplay/MapObjects/MapObjectsRegistry.cs:209"
      - "src/XonoticGodot.Common/GameInit.cs:21"
    constants:
      - { name: "g_turrets master switch", base: "1 (on; turrets.cfg)", port: "MasterSwitchEnabled default-on (turrets.cfg sets 1)", match: true }
    status:
      logic: faithful
      values: faithful
      timing: faithful
      presentation: na
      audio: na
      liveness: live
    intended_divergence: false
    divergence_rationale: null
    gaps: []
    verification:
      - { kind: code, ref: "MapObjectsRegistry.RegisterAll registers turret_flac (line 209); called from GameInit.cs:21; TurretSpawnFuncs.Spawn arms NextThink=Now each frame; g_turrets default 1 confirmed in both Base + port turrets.cfg", result: pass }
      - { kind: unverified, ref: "not observed in a running match; no flac-specific test (only generic MLRS turret-projectile obituary test)", result: unknown }
    confidence: medium
    notes: "Wiring complete and on the real BSP entity-lump path. Unexercised by any test; whether stock maps place turret_flac is open. ruleset-XPM sets g_turrets 0; default ruleset leaves it on."

  - id: turret-flac.target.select_flags
    name: "Target filtering: missiles-only, no turrets, team, range (anti-projectile role)"
    layer: [authority]
    base_refs:
      - "common/turrets/turret/flac.qc:METHOD(Flac, tr_setup)"
      - "common/turrets/sv_turrets.qc:turret_validate_target,turret_select_target"
    port_refs:
      - "src/XonoticGodot.Common/Gameplay/Turrets/FlacTurret.cs:Select,ValidTarget"
      - "src/XonoticGodot.Common/Gameplay/Turrets/TurretAI.cs:ValidTarget,SelectTarget"
    constants:
      - { name: "target_select_flags", base: "default + NOTURRETS + MISSILESONLY", port: "RangeLimits|TeamCheck|Missiles|MissilesOnly|NoTurrets", match: true }
      - { name: "target_range", base: "4000", port: "4000", match: true }
      - { name: "target_range_min", base: "500", port: "500", match: true }
    status:
      logic: faithful
      values: faithful
      timing: faithful
      presentation: na
      audio: na
      liveness: live
    intended_divergence: false
    divergence_rationale: null
    gaps: []
    verification:
      - { kind: code, ref: "FlacTurret.Select matches tr_setup flags (verified against flac.qc tr_setup NOTURRETS|MISSILESONLY); range/min match turrets.cfg:69-70", result: pass }
    confidence: high
    notes: "The anti-projectile role (only shoots enemy missiles) is correctly modeled by MISSILESONLY + NOTURRETS + TeamCheck. Base default target_select_flags include LOS; port Select omits LOS, a minor logic narrowing that rarely matters for a missiles-only flak turret."

  - id: turret-flac.target.scoring_biases
    name: "Target-selection scoring biases (range/angle/missile/player/same)"
    layer: [authority]
    base_refs:
      - "common/turrets/sv_turrets.qc:turret_targetscore_generic"
      - "turrets.cfg:73-77 (g_turrets_unit_flac_target_select_*bias)"
    port_refs:
      - "src/XonoticGodot.Common/Gameplay/Turrets/FlacTurret.cs:23-46,77-83 (bias consts -> TurretParams)"
      - "src/XonoticGodot.Common/Gameplay/Turrets/TurretAI.cs:ScoreTarget(300-333),SelectTarget"
    constants:
      - { name: "rangebias", base: "0.25", port: "0.25 (RangeBias const, passed)", match: true }
      - { name: "anglebias", base: "0.5", port: "0.5 (AngleBias const, passed)", match: true }
      - { name: "missilebias", base: "1", port: "1 (MissileBias const, passed)", match: true }
      - { name: "playerbias", base: "0", port: "0 (PlayerBias const, passed; moot — MISSILESONLY rejects players)", match: true }
      - { name: "samebias", base: "1", port: "1 (SameBias const, passed)", match: true }
    status:
      logic: faithful
      values: faithful
      timing: na
      presentation: na
      audio: na
      liveness: live
    intended_divergence: false
    divergence_rationale: null
    gaps: []
    verification:
      - { kind: code, ref: "FlacTurret.Think now passes rangeBias:0.25 sameBias:1 angleBias:0.5 missileBias:1 playerBias:0; TurretAI.ScoreTarget (lines 325-328) applies dScore*RangeBias + aScore*AngleBias + mScore*MissileBias + pScore*PlayerBias, and SelectTarget seeds the sticky enemy with *SameBias (line 352)", result: pass }
    confidence: high
    notes: "FIXED since prior audit. Wave-2 added the five per-unit bias constants and threads them into TurretParams; ScoreTarget/SelectTarget consume all five and the values now match turrets.cfg exactly. playerbias 0 is harmless either way (players already filtered by MISSILESONLY)."

  - id: turret-flac.aim.lead_compensate
    name: "Lead aim + shot-traveltime compensation (intercept a fast missile)"
    layer: [authority]
    base_refs:
      - "common/turrets/turret/flac.qc:tr_setup (aim_flags = LEAD|SHOTTIMECOMPENSATE)"
      - "common/turrets/sv_turrets.qc:turret_aim_generic"
    port_refs:
      - "src/XonoticGodot.Common/Gameplay/Turrets/FlacTurret.cs:Think (lead:true, shotTimeCompensate:true; zPredict NOT set)"
      - "src/XonoticGodot.Common/Gameplay/Turrets/TurretAI.cs:AimPoint"
    constants:
      - { name: "aim_flags", base: "TFL_AIM_LEAD | TFL_AIM_SHOTTIMECOMPENSATE", port: "lead + shotTimeCompensate (zPredict default false)", match: true }
    status:
      logic: faithful
      values: faithful
      timing: faithful
      presentation: na
      audio: na
      liveness: live
    intended_divergence: false
    divergence_rationale: null
    gaps: []
    verification:
      - { kind: code, ref: "Base flac.qc tr_setup aim_flags = TFL_AIM_LEAD | TFL_AIM_SHOTTIMECOMPENSATE (verified); FlacTurret.Think now passes only lead+shotTimeCompensate (zPredict argument dropped, defaults false). TurretParams.ZPredict false => AimPoint skips the gravity-arc branch", result: pass }
    confidence: high
    notes: "FIXED since prior audit. Wave-2 removed the unintended zPredict:true argument, so the port aim flag set is now exactly LEAD|SHOTTIMECOMPENSATE, matching Base. The core intercept math (lead by refire + shot traveltime) was already faithful."

  - id: turret-flac.aim.track_motor
    name: "Head track motor (fluid-inertia) + per-axis aim limits"
    layer: [authority]
    base_refs:
      - "common/turrets/sv_turrets.qc:turret_track"
      - "turrets.cfg:83-90 (aim_speed/maxrot/maxpitch/track_*)"
    port_refs:
      - "src/XonoticGodot.Common/Gameplay/Turrets/FlacTurret.cs:35,48-51,79-83 (aimMaxPitch/track consts -> TurretParams)"
      - "src/XonoticGodot.Common/Gameplay/Turrets/TurretAI.cs:Track(456-522)"
    constants:
      - { name: "aim_speed", base: "200", port: "200", match: true }
      - { name: "aim_maxrot", base: "360", port: "360", match: true }
      - { name: "aim_maxpitch", base: "35", port: "35 (AimMaxPitch const, passed)", match: true }
      - { name: "track_type", base: "3 (fluid-inertia)", port: "TrackFluidInertia (3)", match: true }
      - { name: "track_accel_pitch", base: "0.5", port: "0.5 (TrackAccelPitch const, passed)", match: true }
      - { name: "track_accel_rot", base: "0.7", port: "0.7 (TrackAccelRot const, passed)", match: true }
      - { name: "track_blendrate", base: "0.2", port: "0.2 (TrackBlendRate const, passed)", match: true }
    status:
      logic: faithful
      values: faithful
      timing: faithful
      presentation: na
      audio: na
      liveness: live
    intended_divergence: false
    divergence_rationale: null
    gaps: []
    verification:
      - { kind: code, ref: "FlacTurret.Think now passes aimMaxPitch:35, trackAccelPitch:0.5, trackAccelRot:0.7, trackBlendRate:0.2; TurretAI.Track TrackFluidInertia branch (lines 494-521) consumes TrackAccelPitch/TrackAccelRot/TrackBlendRate and clamps per axis to aimMaxPitch/aimMaxRot. All match turrets.cfg:84-90", result: pass }
    confidence: high
    notes: "FIXED since prior audit. aim_maxpitch (90->35) was the impactful fix (restores the real flak acquisition cone); track accel_rot (0.5->0.7) and blendrate (0.35->0.2) now passed, restoring the head-slew inertia feel. Track motor type/speed/maxrot were already faithful."

  - id: turret-flac.fire.flak_shell
    name: "Flak shell fire: fast splash projectile + timed air-burst fuse"
    layer: [authority]
    base_refs:
      - "common/turrets/turret/flac_weapon.qc:METHOD(FlacAttack, wr_think)"
      - "common/turrets/turret/flac_weapon.qc:turret_flac_projectile_think_explode"
      - "common/turrets/sv_turrets.qc:turret_projectile,turret_fire,turret_do_updates (tur_impacttime sv_turrets.qc:523)"
    port_refs:
      - "src/XonoticGodot.Common/Gameplay/Turrets/FlacTurret.cs:Attack"
      - "src/XonoticGodot.Common/Gameplay/Turrets/TurretSpawn.cs:Projectile"
      - "src/XonoticGodot.Common/Gameplay/Turrets/TurretAI.cs:Fire,RunCombat"
    constants:
      - { name: "shot_dmg", base: "20", port: "20", match: true }
      - { name: "shot_radius", base: "100", port: "100", match: true }
      - { name: "shot_speed", base: "9000", port: "9000", match: true }
      - { name: "shot_force", base: "25", port: "25", match: true }
      - { name: "shot_refire", base: "0.1", port: "0.1", match: true }
      - { name: "shot_spread", base: "0.02", port: "0.02", match: true }
      - { name: "target_range_optimal", base: "1250", port: "1250", match: true }
      - { name: "ammo_max", base: "1000", port: "1000", match: true }
      - { name: "ammo_recharge", base: "100", port: "100", match: true }
      - { name: "aim_firetolerance_dist", base: "150", port: "150", match: true }
      - { name: "proj size", base: "5", port: "5", match: true }
      - { name: "proj health", base: "0 (not shootable, FL_NOTARGET)", port: "0 (FL_NOTARGET)", match: true }
      - { name: "edge damage", base: "shot_dmg (== core)", port: "ShotDamage (== core)", match: true }
      - { name: "snap radius", base: "shot_radius*3", port: "ShotRadius*3", match: true }
      - { name: "lifetime fallback nextthink", base: "time + 9", port: "now + 9", match: true }
      - { name: "fuse jitter", base: "random()*0.01 - random()*0.01", port: "Prandom.Float()*0.01 - Prandom.Float()*0.01", match: true }
      - { name: "fuse base time", base: "tur_impacttime = vlen(tur_shotorg - trace_endpos)/shot_speed (forward tracebox, sv_turrets.qc:523)", port: "DistAimPos / shot_speed (no forward trace)", match: false }
      - { name: "missile_flags", base: "MIF_SPLASH | MIF_PROXY", port: "n/a (no missile_flags field; behaviour folded into the fuse think)", match: false }
    status:
      logic: faithful
      values: faithful
      timing: partial
      presentation: missing
      audio: faithful
      liveness: live
    intended_divergence: false
    divergence_rationale: null
    gaps:
      - "Fuse time uses DistAimPos/shotSpeed (muzzle->aim distance) instead of Base tur_impacttime, which comes from a forward tracebox (sv_turrets.qc:510-523) to trace_endpos; diverges when geometry would stop the shot short of the aim point."
      - "Presentation missing: PROJECTILE_HAGAR shell trail (turret_projectile spawns with className 'turret_projectile', so the client classifier never maps it to a Flac/FLAC_TRAIL projectile type), EFFECT_BLASTER_MUZZLEFLASH, head-frame cycle (++frame wrap 4)."
    verification:
      - { kind: code, ref: "shot_spread now 0.02 (turrets.cfg:64), target_range_optimal now 1250 (turrets.cfg:71); FlacTurret.Attack reproduces snap-to-enemy + edge==core RadiusDamage; shot_dmg/radius/speed/force/refire match turrets.cfg:60-65", result: pass }
      - { kind: code, ref: "fuse base time still DistAimPos/ShotSpeed (FlacTurret.Attack:105) vs Base forward-traced tur_impacttime (sv_turrets.qc:523)", result: fail }
      - { kind: code, ref: "fire sound weapons/hagar_fire.wav played on SoundChannel.Weapon (matches SND_FlacAttack_FIRE = W_Sound(hagar_fire))", result: pass }
    confidence: high
    notes: "FIXED since prior audit: shot_spread (0.0125->0.02) and target_range_optimal (2000->1250) now match Base, so values is faithful. STILL OPEN: the fuse-time derivation (DistAimPos vs forward-traced trace_endpos) and all client FX. The signature flak mechanic (timed fuse + air-burst snap-to-enemy + full-radius damage) and the fire sound were already faithful."

  - id: turret-flac.lifecycle.respawn
    name: "Death + respawn timer (and the port's extra death blast)"
    layer: [authority]
    base_refs:
      - "common/turrets/sv_turrets.qc:turret_die(166),turret_hide,turret_respawn (nextthink = time + respawntime - 0.2, line 162)"
      - "turrets.cfg:58 (g_turrets_unit_flac_respawntime 90)"
    port_refs:
      - "src/XonoticGodot.Common/Gameplay/Turrets/TurretAI.cs:Die(739-771),Respawn,EnsureDeathHook,EventDamage(710-732)"
      - "src/XonoticGodot.Common/Gameplay/Turrets/FlacTurret.cs:Spawn (respawnTime: RespawnTime=90)"
      - "src/XonoticGodot.Common/Gameplay/Turrets/TurretSpawn.cs:Init (st.RespawnTime=respawnTime), GtEventDamage shim"
      - "src/XonoticGodot.Common/Gameplay/Damage/DamageSystem.cs:280,324-334 (EventDamage routes non-player edicts via GtEventDamage)"
    constants:
      - { name: "respawntime", base: "90", port: "90 (FlacTurret.Spawn passes respawnTime: RespawnTime)", match: true }
      - { name: "respawn nextthink offset", base: "time + respawntime - 0.2 (sv_turrets.qc:162)", port: "now + respawntime (no -0.2)", match: false }
      - { name: "death blast", base: "NONE — RadiusDamage(min(ammo,50)...) is commented out (sv_turrets.qc:182)", port: "min(ammo,50) RadiusDamage at 250 radius (DEATH_TURRET)", match: false }
    status:
      logic: partial
      values: partial
      timing: faithful
      presentation: na
      audio: na
      liveness: live
    intended_divergence: false
    divergence_rationale: null
    gaps:
      - "Port TurretAI.Die (lines 751-755) still fires a min(ammo,50) RadiusDamage death blast (250 radius, force min(ammo,50)*5, DEATH_TURRET); Base turret_die has this blast COMMENTED OUT (sv_turrets.qc:182) — Base turrets do not explode on death. The port adds an unintended on-death AoE. STILL OPEN."
      - "Respawn schedules at now + respawntime; Base schedules nextthink = time + respawntime - 0.2 (sv_turrets.qc:162). Minor 0.2s timing offset (turret_hide already runs ~0.2s into the death sequence in Base)."
    verification:
      - { kind: code, ref: "FlacTurret.Spawn now calls TurretSpawn.Init(... respawnTime: RespawnTime) with RespawnTime=90; TurretSpawn.Init sets st.RespawnTime=respawnTime (line 74). Matches turrets.cfg:58 (90)", result: pass }
      - { kind: code, ref: "Base sv_turrets.qc:182 RadiusDamage in turret_die is commented out; port TurretAI.Die:751-755 actively does the blast", result: fail }
      - { kind: code, ref: "death/respawn now driven by a LIVE event_damage path: DamageSystem.Apply(280)->EventDamage(324)->GtEventDamage(331-333)->TurretAI.EventDamage->TakeResource health + Combat.Death->OnAnyDeath(648-654)->Die. The pre-damage gate (TurretAI.Damage) now runs on the live path (Wave-2 wired the GtEventDamage shim in TurretSpawn.Init)", result: pass }
    confidence: high
    notes: "FIXED since prior audit: respawntime now 90 (was generic 60 default) — values mismatch narrowed to the -0.2 offset only. Wave-2 also made the turret damage->death path genuinely LIVE (event_damage shim routed through DamageSystem), so the pre-damage gate + retaliation in TurretAI.Damage now actually run. STILL OPEN: the extra death blast Base does not produce. logic stays partial because of the unintended blast."

  - id: turret-flac.obituary.death_message
    name: "FLAC kill obituary / death notification (DEATH_TURRET_FLAC)"
    layer: [authority, presentation]
    base_refs:
      - "common/turrets/turret/flac_weapon.qc:turret_flac_projectile_think_explode (projectiledeathtype = DEATH_TURRET_FLAC)"
      - "common/notifications/all.inc:DEATH_TURRET_FLAC / DEATH_SELF_TURRET_FLAC"
    port_refs:
      - "src/XonoticGodot.Common/Gameplay/Damage/DeathTypes.cs:266 (TurretFlac=\"turret_flac\"),359 (Reg -> DEATH_SELF_TURRET_FLAC)"
      - "src/XonoticGodot.Common/Gameplay/Notifications/NotificationsList.cs:186 (TURRET_FLAC),1083 (DEATH_SELF_TURRET_FLAC)"
    constants:
      - { name: "deathtype", base: "DEATH_TURRET_FLAC", port: "DeathTypes.TurretFlac (\"turret_flac\")", match: true }
      - { name: "self/suicide line", base: "DEATH_SELF_TURRET_FLAC", port: "DEATH_SELF_TURRET_FLAC", match: true }
      - { name: "obituary text", base: "got caught up in the FLAC turret fire", port: "got caught up in the FLAC turret fire", match: true }
    status:
      logic: faithful
      values: faithful
      timing: na
      presentation: faithful
      audio: na
      liveness: live
    intended_divergence: false
    divergence_rationale: null
    gaps: []
    verification:
      - { kind: code, ref: "FlacTurret.Attack RadiusDamage passes deathTag: DeathTypes.TurretFlac; DeathTypes.Reg(TurretFlac, Turret, DEATH_SELF_TURRET_FLAC); NotificationsList registers TURRET_FLAC + DEATH_SELF_TURRET_FLAC", result: pass }
      - { kind: unverified, ref: "no flac-specific obituary test; generic test (MonsterTurretVehicleObituaryTests.cs:200) exercises TurretMlrs through the same TurretSpawn.Projectile/RadiusDamage path", result: unknown }
    confidence: high
    notes: "Unchanged since prior audit. The FLAC death-tag flows through RadiusDamage as the string deathTag (dual-deathtype model) and resolves to the correct self/murder obituary lines registered in NotificationsList. The generic turret-projectile obituary path is unit-tested with MLRS; FLAC reuses it identically."

  - id: turret-flac.weapon.player_form
    name: "Hidden player FLAC weapon (FlacAttack, impulse 5)"
    layer: [authority]
    base_refs:
      - "common/turrets/turret/flac_weapon.qh:CLASS(FlacAttack)"
      - "common/turrets/turret/flac_weapon.qc:METHOD(FlacAttack, wr_think) (IS_PLAYER branch)"
    port_refs: []
    constants:
      - { name: "impulse", base: "5", port: "n/a", match: false }
      - { name: "spawnflags", base: "WEP_FLAG_SPECIALATTACK | WEP_FLAG_HIDDEN", port: "n/a", match: false }
      - { name: "player fuse tur_impacttime", base: "10 (fixed)", port: "n/a", match: false }
    status:
      logic: missing
      values: missing
      timing: missing
      presentation: missing
      audio: missing
      liveness: na
    intended_divergence: false
    divergence_rationale: null
    gaps:
      - "The hidden/dev player-fired FLAC weapon (impulse 5, WEP_FLAG_HIDDEN, fixed 10s fuse, base class PortoLaunch) is not implemented. Low impact: not used in normal play or default arenas."
    verification:
      - { kind: code, ref: "no FlacAttack / WEP_FLAC player weapon in src (only the turret FlacTurret); flac references in src are all turret/sound/effect/notification", result: fail }
    confidence: high
    notes: "Unchanged since prior audit. Shares wr_think with the turret in Base (branches on IS_PLAYER). Only the turret-AI path is ported. The player form is a hidden developer feature, out of Wave-1/2 scope."
