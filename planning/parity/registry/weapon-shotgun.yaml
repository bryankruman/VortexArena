unit: weapon-shotgun
category: weapon
base_ref_rev: "v0.8.6-1779-g863cd3e84"
last_audited: "2026-06-27"
summary:
  feature_count: 13
  worst_gaps:
    - "Antilag takeback is STILL per-pellet (each FireBullet calls LagComp.Begin) rather than ONCE for the whole volley as Base does outside the loop (shotgun.qc:23-26 antilag_takeback_all); functionally-equivalent hit-reg but redundant rewind work per pellet."
    - "g_ballistics_penetrate_clips (default 1) / weapclip handling still not modeled (no weapclip distinction in FireBullet)."
    - "Muzzleflash uziflash.md3 model attachment still not verified to render (only the SHOTGUN_MUZZLEFLASH particle is emitted)."
    - "SHOTGUN_IMPACT is emitted per pellet server-side (~12/shot) via a second WorldOnly trace (intended divergence — no CSQC layer), not the single CSQC w_org + w_backoff*2 impact; emits on the world hit even when the pellet struck a player."
    - "pickup_ammo (15) not read in Shotgun.cs (item-pickup concern). MENUQC describe() weapon-guide prose has no port."

features:
  - id: weapon-shotgun.primary.attack_fan
    name: "Primary fire: 12-pellet hitscan fan"
    layer: [authority]
    base_refs:
      - "common/weapons/weapon/shotgun.qc:W_Shotgun_Attack"
      - "common/weapons/weapon/shotgun.qc:wr_think"
    port_refs:
      - "src/XonoticGodot.Common/Gameplay/Weapons/Shotgun.cs:Attack"
      - "src/XonoticGodot.Common/Gameplay/Weapons/Shotgun.cs:WrThink"
    constants:
      - { name: "primary_ammo (shells/shot)", base: "1", port: "1", match: true }
      - { name: "primary_bullets (pellets)", base: "12", port: "12", match: true }
      - { name: "primary_damage (per pellet)", base: "4", port: "4", match: true }
      - { name: "primary_force", base: "15", port: "15", match: true }
      - { name: "primary_solidpenetration", base: "3.8", port: "3.8", match: true }
      - { name: "primary_spread", base: "0.12", port: "0.12", match: true }
      - { name: "damagefalloff_halflife/min/max", base: "0/0/0 (no falloff)", port: "0/0/0", match: true }
    status:
      logic: faithful
      values: faithful
      timing: faithful
      presentation: na
      audio: na
      liveness: live
    intended_divergence: false
    divergence_rationale: null
    gaps:
      - "Antilag takeback is still per-pellet (inside FireBullet/LagComp.Begin) rather than ONCE for the whole volley as Base does outside the loop (shotgun.qc:23-26 antilag_takeback_all); functionally-equivalent hit-reg but redundant rewind work per pellet."
    verification:
      - { kind: code, ref: "Shotgun.Attack: TakeResource + SetupShot(maxDamage=damage*bullets) + 12x FireBullet; constants via Bal() match bal-wep-xonotic.cfg:47-58", result: pass }
      - { kind: code, ref: "live path GameWorld -> WeaponFireDriver.Frame -> WrThink(Primary) -> Attack", result: pass }
    confidence: high
    notes: "Unchanged on the core fan; still faithful. WrThink also runs the (live) auto-melee branch and the casing/ricochet seams; the 12-pellet primary blast itself is intact."

  - id: weapon-shotgun.primary.spread_style
    name: "Pellet spread distribution (hitscan spread style)"
    layer: [shared]
    base_refs:
      - "common/weapons/calculations.qc:W_CalculateSpread"
      - "server/weapons/tracing.qc:370 (fireBullet passes autocvar_g_hitscan_spread_style)"
    port_refs:
      - "src/XonoticGodot.Common/Gameplay/Weapons/WeaponFiring.cs:CalculateSpread"
      - "src/XonoticGodot.Common/Gameplay/Weapons/WeaponFiring.cs:HitscanSpreadStyle"
      - "src/XonoticGodot.Common/Gameplay/Weapons/WeaponFiring.cs:FireBullet (passes HitscanSpreadStyle)"
    constants:
      - { name: "g_hitscan_spread_style (default)", base: "4 (gauss-2D plane; balance-xonotic.cfg:209)", port: "4 (HitscanSpreadStyle reads cvar, fallback 4)", match: true }
      - { name: "g_weaponspreadfactor", base: "1", port: "1 (WeaponSpreadFactor)", match: true }
      - { name: "W_SPREAD_GAUSS_MAX_STDEV clamp", base: "4", port: "4 (SpreadGaussMaxStdev)", match: true }
    status:
      logic: faithful
      values: faithful
      timing: na
      presentation: na
      audio: na
      liveness: live
    intended_divergence: false
    divergence_rationale: null
    gaps: []
    verification:
      - { kind: code, ref: "WeaponFiring.CalculateSpread now implements all 8 styles (0-7) matching W_CalculateSpread's per-style sigma factors + density functions; FireBullet:282 passes HitscanSpreadStyle", result: pass }
      - { kind: code, ref: "HitscanSpreadStyle reads autocvar_g_hitscan_spread_style, falls back to 4 headless; Base balance-xonotic.cfg:209 verified = 4; case 4 = gauss-2D cliptoplane onto aim plane", result: pass }
    confidence: high
    notes: "FIX vs baseline (partial/partial -> faithful/faithful). The baseline recorded only style-0 implemented; the port now ports all 8 W_CalculateSpread styles and FireBullet passes the live g_hitscan_spread_style (default 4 = gauss-2D plane). Affects every fireBullet weapon, not just shotgun."

  - id: weapon-shotgun.primary.firebullet_falloff
    name: "Per-pellet trace: falloff + solid penetration + force"
    layer: [authority]
    base_refs:
      - "server/weapons/tracing.qc:fireBullet_falloff (tracing.qc:~378-537)"
      - "lib/math.qh:ExponentialFalloff"
    port_refs:
      - "src/XonoticGodot.Common/Gameplay/Weapons/WeaponFiring.cs:FireBullet"
      - "src/XonoticGodot.Common/Gameplay/Weapons/WeaponFiring.cs:ExponentialFalloff"
    constants:
      - { name: "g_ballistics_solidpenetration_exponent", base: "1 (xonotic-server.cfg:474)", port: "1 (reads cvar, fallback 1, WeaponFiring.cs:300-301)", match: true }
      - { name: "g_ballistics_mindistance", base: "2 (xonotic-server.cfg:470)", port: "2 (reads cvar, fallback 2, WeaponFiring.cs:302-303)", match: true }
      - { name: "g_ballistics_penetrate_clips", base: "1 (xonotic-server.cfg:473)", port: "not modeled (no weapclip distinction)", match: false }
      - { name: "max_shot_distance", base: "32768 / per-map", port: "CurrentMaxShotDistance (per-map from world diagonal)", match: true }
    status:
      logic: faithful
      values: partial
      timing: na
      presentation: na
      audio: na
      liveness: live
    intended_divergence: false
    divergence_rationale: null
    gaps:
      - "g_ballistics_penetrate_clips (default 1) / weapclip handling still not modeled (no weapclip distinction in the penetration loop)."
    verification:
      - { kind: code, ref: "WeaponFiring.FireBullet penetration loop (damageFraction = penFrac^exponent) + ExponentialFalloff vs tracing.qc fireBullet_falloff", result: pass }
      - { kind: code, ref: "ballisticsMinDistance now reads g_ballistics_mindistance (fallback 2) WeaponFiring.cs:302-303; exponent reads g_ballistics_solidpenetration_exponent (fallback 1)", result: pass }
    confidence: high
    notes: "FIX vs baseline (values partial -> still partial but mindistance closed). The baseline recorded g_ballistics_mindistance hardcoded to 1; the port now reads it live with the Base default 2. Only g_ballistics_penetrate_clips remains unmodeled. Penetration math, double-hit/self-hit guard, sky/out-of-world stop, warpzone-aware sweep all faithful. The shotgun passes no tracerEffect (Base shotgun has no tracer), so the tracer arg is inert here."

  - id: weapon-shotgun.primary.setupshot_recoil
    name: "W_SetupShot: trueaim, muzzle offset, recoil punch"
    layer: [authority, presentation]
    base_refs:
      - "server/weapons/tracing.qc:W_SetupShot_Dir_ProjectileSize_Range"
    port_refs:
      - "src/XonoticGodot.Common/Gameplay/Weapons/WeaponFiring.cs:SetupShot"
    constants:
      - { name: "recoil punchangle", base: "5 (deg up)", port: "5", match: true }
      - { name: "g_norecoil (default)", base: "1 — recoil SUPPRESSED by default (xonotic-server.cfg:232)", port: "reads g_norecoil cvar; cfg ships default 1 so suppressed when loaded", match: true }
      - { name: "g_trueaim_minrange", base: "44 (xonotic-server.cfg:204)", port: "44 (reads cvar, fallback 44, WeaponFiring.cs:220-222)", match: true }
    status:
      logic: faithful
      values: faithful
      timing: na
      presentation: faithful
      audio: na
      liveness: live
    intended_divergence: false
    divergence_rationale: null
    gaps:
      - "Antilag takeback at the SetupShot level is bracketed by LagComp.Begin/End around the trueaim + muzzle-nudge traces (g_antilag==2); warpzones in the trueaim are partially handled (direction kept straight on a portal cross). Accuracy FIRED credit IS wired (WeaponAccuracyEvents.Fired)."
    verification:
      - { kind: code, ref: "WeaponFiring.SetupShot vs tracing.qc W_SetupShot_Dir_ProjectileSize_Range; trueaim minrange clamp now applied (WeaponFiring.cs:220-222)", result: pass }
      - { kind: code, ref: "recoil gated on g_norecoil (WeaponFiring.cs:145-147); cfg default 1 suppresses — faithful in practice", result: pass }
    confidence: high
    notes: "FIX vs baseline (values partial -> faithful). The baseline recorded the g_trueaim_minrange=44 clamp as not applied; SetupShot now applies it (un-adjusts the trueaim point when the centered shot lands within minrange of the eye). Recoil punch correctly gated on g_norecoil (default 1 suppresses). Muzzle offset reads the registered per-model tag_shot (MuzzleOffsetFor) with DefaultMuzzleOffset fallback."

  - id: weapon-shotgun.primary.refire_timer
    name: "Primary refire gating via private shotgun_primarytime"
    layer: [authority]
    base_refs:
      - "common/weapons/weapon/shotgun.qc:wr_think (shotgun.qc:289-309)"
      - "server/weapons/weaponsystem.qc:weapon_prepareattack"
    port_refs:
      - "src/XonoticGodot.Common/Gameplay/Weapons/Shotgun.cs:WrThink"
      - "src/XonoticGodot.Common/Gameplay/Weapons/WeaponFireGate.cs:PrepareAttack"
    constants:
      - { name: "primary_refire", base: "0.75 s", port: "0.75", match: true }
      - { name: "primary_animtime", base: "0.2 s", port: "0.2", match: true }
      - { name: "g_weaponratefactor", base: "1", port: "1 (WeaponRateFactor)", match: true }
    status:
      logic: faithful
      values: faithful
      timing: faithful
      presentation: na
      audio: na
      liveness: live
    intended_divergence: false
    divergence_rationale: null
    gaps: []
    verification:
      - { kind: code, ref: "WrThink parks ShotgunPrimaryTime + PrepareAttack(attackTime=animtime); matches QC passing animtime into weapon_prepareattack (shotgun.qc:290) and parking refire in shotgun_primarytime", result: pass }
    confidence: high
    notes: "The private-timer-vs-shared-ATTACK_FINISHED split (refire 0.75 parked privately, shared timer held only animtime 0.2 so a melee can follow a blast immediately) remains faithfully reproduced. Unchanged."

  - id: weapon-shotgun.secondary.melee_swing
    name: "Secondary melee slap (swing-arc damage + multihit + multi-frame think)"
    layer: [authority]
    base_refs:
      - "common/weapons/weapon/shotgun.qc:W_Shotgun_Attack2"
      - "common/weapons/weapon/shotgun.qc:W_Shotgun_Melee_Think"
    port_refs:
      - "src/XonoticGodot.Common/Gameplay/Weapons/Shotgun.cs:Melee"
      - "src/XonoticGodot.Common/Gameplay/Weapons/Shotgun.cs:MeleeThink"
    constants:
      - { name: "secondary_damage (vs player)", base: "70", port: "70", match: true }
      - { name: "melee_nonplayerdamage", base: "40", port: "40", match: true }
      - { name: "secondary_force", base: "200", port: "200", match: true }
      - { name: "melee_range", base: "120", port: "120", match: true }
      - { name: "melee_swing_side / swing_up", base: "120 / 30", port: "120 / 30", match: true }
      - { name: "melee_traces", base: "10", port: "10", match: true }
      - { name: "melee_time", base: "0.15 s", port: "0.15 (spread across the multi-frame think)", match: true }
      - { name: "melee_delay (wind-up)", base: "0.25 s (bal-wep-xonotic.cfg:71)", port: "0.25 (NextThink = time + melee_delay*rate, Shotgun.cs:256)", match: true }
      - { name: "melee_no_doubleslap", base: "1 (bal-wep-xonotic.cfg:73)", port: "true (aborts the swing if the owner died, Shotgun.cs:281-285)", match: true }
      - { name: "melee_multihit", base: "1", port: "1", match: true }
    status:
      logic: faithful
      values: faithful
      timing: faithful
      presentation: na
      audio: na
      liveness: live
    intended_divergence: false
    divergence_rationale: null
    gaps: []
    verification:
      - { kind: code, ref: "Shotgun.Melee spawns a meleetemp think entity (NextThink = time + melee_delay*rate); MeleeThink runs each frame across melee_time, batching swing_prev..f traces, swing_factor=(1-i/traces)*2-1, damage*min(1,swing_factor+1), multihit dedupe vs W_Shotgun_Melee_Think", result: pass }
      - { kind: code, ref: "melee_no_doubleslap: MeleeThink aborts (Api.Entities.Remove) when actor.DeadState != No && Secondary.MeleeNoDoubleslap (shotgun.qc:104-108); strength sound on first think run (FireWPlayStrengthSound)", result: pass }
    confidence: high
    notes: "FIX vs baseline (logic/values/timing partial -> faithful). The baseline recorded the swing collapsed to a single tick with no melee_delay wind-up and no melee_no_doubleslap. The port now spawns a scheduled meleetemp think entity that begins after the 0.25s melee_delay wind-up, spreads its melee_traces across melee_time (0.15s) re-scheduling itself each frame (so a target can move out of a partial swing), aborts on melee_no_doubleslap if the owner dies mid-swing, and routes swing damage through the secondary-tagged Combat.Damage. Swing state held in a closure-captured MeleeSwingState (no scratch-field leak). Melee woosh per trace (WeaponFiring.MeleeWoosh)."

  - id: weapon-shotgun.secondary.melee_routing
    name: "Secondary routing: melee gate + out-of-ammo auto-melee + alt triple-shot"
    layer: [authority]
    base_refs:
      - "common/weapons/weapon/shotgun.qc:wr_think (shotgun.qc:341-351 melee gate; 313-338 triple)"
      - "common/weapons/weapon/shotgun.qc:W_Shotgun_Attack3_Frame1"
      - "common/weapons/weapon/shotgun.qc:W_Shotgun_Attack3_Frame2"
      - "server/weapons/weaponsystem.qc:weapon_prepareattack_checkammo (the empty-primary silent early-out)"
    port_refs:
      - "src/XonoticGodot.Common/Gameplay/Weapons/Shotgun.cs:WrThink"
      - "src/XonoticGodot.Common/Gameplay/Weapons/Shotgun.cs:Attack3Frame1"
      - "src/XonoticGodot.Common/Gameplay/Weapons/Shotgun.cs:Attack3Frame2"
      - "src/XonoticGodot.Common/Gameplay/Weapons/WeaponFireDriver.cs:ScheduleThink (Frame1/Frame2 scheduling; dispatched in WeaponFireDriver.Frame:145-150)"
      - "src/XonoticGodot.Common/Gameplay/Weapons/WeaponFireGate.cs:PrepareAttackImpl (buttonFire gate)"
      - "src/XonoticGodot.Common/Gameplay/Weapons/WeaponFireGate.cs:CheckAmmoWithAutoSwitch (Shotgun melee silent early-out)"
    constants:
      - { name: "secondary (mode)", base: "1 (melee)", port: "1", match: true }
      - { name: "melee_blockedbyfiring", base: "0", port: "0", match: true }
      - { name: "secondary_refire", base: "1.25 s", port: "1.25", match: true }
      - { name: "alt_refire / alt_animtime", base: "1.2 / 0.2 (bal-wep-xonotic.cfg:66,65)", port: "1.2 / 0.2 (Secondary.AltRefire/AltAnimtime, Shotgun.cs:144,147)", match: true }
    status:
      logic: faithful
      values: faithful
      timing: faithful
      presentation: na
      audio: na
      liveness: live
    intended_divergence: false
    divergence_rationale: null
    gaps: []
    verification:
      - { kind: code, ref: "WrThink: emptyPrimaryAutoMelee (fire==Primary && !bot && GetResource<=0 && ClipLoad==0 && !unlimited) -> PrepareAttack(FireMode.Secondary, attackTime NaN, buttonFire: fire)", result: pass }
      - { kind: code, ref: "PrepareAttackImpl: held = buttonFire==Secondary ? ButtonAttack2 : ButtonAttack — empty-primary press passes buttonFire=Primary -> checks ButtonAttack (held) -> proceeds; auto-melee LIVE", result: pass }
      - { kind: code, ref: "CheckAmmoWithAutoSwitch: Shotgun melee-secondary primary early-out returns false silently (no dry-fire click, no auto-switch) so the auto-melee can slap (weaponsystem.qc:254-256)", result: pass }
      - { kind: code, ref: "secondary==2 triple-shot: WrThink fires blast 1 + schedules Attack3Frame1 (Shotgun.cs:150-159) via WeaponFireDriver.ScheduleThink at alt_animtime; Attack3Frame1 fires blast 2 + schedules Attack3Frame2 (Shotgun.cs:248-269); Attack3Frame2 fires blast 3 + returns to ready (Shotgun.cs:274-297). Each Frame guards on CheckAmmoSecondary + SwitchToOtherWeapon when dry — faithful to W_Shotgun_Attack3_Frame1/2 (shotgun.qc:207-263)", result: pass }
      - { kind: code, ref: "scheduled Frames are LIVE: WeaponFireDriver.Frame dispatches st.WeaponThink at WeaponNextThink (WeaponFireDriver.cs:145-150). alt_refire 1.2 / alt_animtime 0.2 parked in ShotgunPrimaryTime (Shotgun.cs:154-156)", result: pass }
    confidence: high
    notes: "FIX vs baseline (logic partial -> faithful, liveness partial -> live). The baseline recorded the alt triple-shot (secondary==2) as a single blast with the staged Frame1->Frame2 sequence dead. The port now fully ports the staged 3-blast sequence: WrThink fires the first blast then schedules Attack3Frame1 via WeaponFireDriver.ScheduleThink (dispatched on the live path in WeaponFireDriver.Frame), Frame1 fires the second and schedules Frame2, Frame2 fires the third and returns to ready — each with a per-blast CheckAmmoSecondary guard + SwitchToOtherWeapon (W_SwitchWeapon_Force) dry-fire path. Out-of-ammo auto-melee remains LIVE; melee gate + alt timing (1.2/0.2) faithful. Triple-shot only runs under the non-default g_balance_shotgun_secondary 2, but the path is wired and reachable, so liveness is live."

  - id: weapon-shotgun.ammo.checkammo_reload
    name: "Ammo checks + reload"
    layer: [authority]
    base_refs:
      - "common/weapons/weapon/shotgun.qc:wr_checkammo1"
      - "common/weapons/weapon/shotgun.qc:wr_checkammo2"
      - "common/weapons/weapon/shotgun.qc:wr_reload"
    port_refs:
      - "src/XonoticGodot.Common/Gameplay/Weapons/Shotgun.cs:CheckAmmoPrimary"
      - "src/XonoticGodot.Common/Gameplay/Weapons/Shotgun.cs:CheckAmmoSecondary"
      - "src/XonoticGodot.Common/Gameplay/Weapons/WeaponFireGate.cs:WrReload/WReload/WReloadedAndReady"
    constants:
      - { name: "ammo_type", base: "RES_SHELLS", port: "ResourceType.Shells", match: true }
      - { name: "pickup_ammo", base: "15 (bal-wep-xonotic.cfg:46)", port: "not read in Shotgun.cs", match: false }
      - { name: "reload_ammo / reload_time", base: "0 / 2", port: "reloadable flag set; generic WReload pipeline in WeaponFireGate (reads g_balance_shotgun_reload_*)", match: true }
    status:
      logic: faithful
      values: partial
      timing: faithful
      presentation: na
      audio: na
      liveness: live
    intended_divergence: false
    divergence_rationale: null
    gaps:
      - "wr_checkammo1/2 in Base also OR the clip load (weapon_load[m_id] >= ammo) (shotgun.qc:357,370); the port's CheckAmmoPrimary/Secondary DO include the GetWeaponLoad OR-term (Shotgun.cs:366-367,374-375), so the clip-load term is now present. reload_ammo defaults 0 so no clip is used by default."
      - "pickup_ammo (15) still not read in Shotgun.cs (item-pickup concern, not the weapon's checkammo)."
    verification:
      - { kind: code, ref: "CheckAmmoPrimary/Secondary vs wr_checkammo1/2 (melee free, triple uses primary ammo); port DOES include the weapon_load OR-term (GetWeaponLoad >= ammo)", result: pass }
      - { kind: code, ref: "WeaponFireGate.WReload/WReloadedAndReady port W_Reload (reload sound, scheduled think, clip transfer) generically", result: pass }
    confidence: medium
    notes: "Re-audit found the clip-load OR-term IS present in CheckAmmoPrimary/Secondary (the baseline note that it was omitted is corrected). Generic shared reload pipeline (WeaponFireGate.WReload) makes reload timing faithful. pickup_ammo (item-pickup) still not read here. values stays partial only for pickup_ammo."

  - id: weapon-shotgun.bot.wr_aim
    name: "Bot aim: melee vs ranged selection"
    layer: [authority]
    base_refs:
      - "common/weapons/weapon/shotgun.qc:wr_aim (shotgun.qc:267-273)"
    port_refs:
      - "src/XonoticGodot.Common/Gameplay/Weapons/Shotgun.cs:BotWantsSecondary (cs:186-187)"
      - "src/XonoticGodot.Common/Gameplay/GameplayBases.cs:Weapon.BotWantsSecondary (virtual)"
      - "src/XonoticGodot.Server/Bot/BotBrain.cs:355 (calls BotWantsSecondary, routes ButtonAttack2)"
    constants:
      - { name: "melee_range switch threshold", base: "120", port: "120 (Secondary.MeleeRange)", match: true }
    status:
      logic: faithful
      values: faithful
      timing: na
      presentation: na
      audio: na
      liveness: live
    intended_divergence: false
    divergence_rationale: null
    gaps: []
    verification:
      - { kind: code, ref: "Shotgun.BotWantsSecondary returns enemyDistance <= Secondary.MeleeRange (120) — exact match for vdist(actor.origin-enemy.origin, <=, melee_range) -> ATCK2 (shotgun.qc:269)", result: pass }
      - { kind: code, ref: "BotBrain.cs:349-359 calls ChosenWeapon.BotWantsSecondary live; on true sets wantAttack=false/wantAttack2=true -> routes the shot onto ButtonAttack2", result: pass }
    confidence: high
    notes: "FIX vs baseline (missing/missing/na -> faithful/faithful/live). The baseline recorded no WrAim override. The port now overrides BotWantsSecondary (Shotgun.cs:186) implementing the melee_range distance switch, and BotBrain dispatches it live and routes the result onto ButtonAttack2 — same pattern as MachineGun/Rifle. The bot_aim ranged-tuning params (1000000,0,0.001) are owned by the generic BotBrain aim, not this hook."

  - id: weapon-shotgun.fx.muzzle_impact
    name: "Muzzleflash + bullet-impact particle"
    layer: [presentation]
    base_refs:
      - "common/weapons/weapon/shotgun.qc:W_MuzzleFlash (via W_Shotgun_Attack)"
      - "common/weapons/weapon/shotgun.qc:wr_impacteffect (CSQC, shotgun.qc:400-410)"
    port_refs:
      - "src/XonoticGodot.Common/Gameplay/Weapons/Shotgun.cs:Attack"
      - "src/XonoticGodot.Common/Gameplay/Weapons/WeaponFiring.cs:BulletImpactFx"
      - "src/XonoticGodot.Common/Gameplay/Effects/EffectEmitter.cs:Emit"
      - "src/XonoticGodot.Common/Gameplay/Effects/EffectsList.cs:43-44 (SHOTGUN_IMPACT/MUZZLEFLASH registered)"
      - "game/net/ServerNet.cs:WriteEffect (EffectNetSink) ; game/client/ClientWorld.cs:OnEffect (RenderSink)"
    constants:
      - { name: "muzzleflash effect", base: "EFFECT_SHOTGUN_MUZZLEFLASH + uziflash.md3 model", port: "SHOTGUN_MUZZLEFLASH particle (uziflash.md3 model attachment not verified)", match: false }
      - { name: "impact effect emission", base: "EFFECT_SHOTGUN_IMPACT in CSQC wr_impacteffect, once per networked impact", port: "SHOTGUN_IMPACT emitted server-side per pellet via an EXTRA WorldOnly trace (~12/shot)", match: false }
    status:
      logic: partial
      values: na
      timing: na
      presentation: partial
      audio: na
      liveness: live
    intended_divergence: true
    divergence_rationale: "Impact effect is emitted server-side per pellet (an extra WorldOnly trace in Shotgun.Attack finds the surface, then WeaponFiring.BulletImpactFx emits SHOTGUN_IMPACT) rather than from a CSQC wr_impacteffect, because the port has no CSQC layer. The effect sink is live on both ends (ServerNet EffectNetSink networks it; ClientWorld RenderSink renders it)."
    gaps:
      - "Muzzleflash uziflash.md3 model attachment still not verified to render (only the SHOTGUN_MUZZLEFLASH particle is emitted)."
      - "SHOTGUN_IMPACT is still emitted per pellet server-side (~12/shot) via a second WorldOnly trace, not the single CSQC w_org + w_backoff*2 impact; it emits on the world hit point even when the pellet struck a player (the extra trace is WorldOnly, ignoring entities)."
    verification:
      - { kind: code, ref: "Shotgun.Attack: extra WorldOnly trace per pellet -> backoff -> WeaponFiring.BulletImpactFx(SHOTGUN_IMPACT); EffectsList.cs:43-44 registered", result: pass }
      - { kind: code, ref: "effect sink live: ServerNet WriteEffect (net) + ClientWorld RenderSink/OnEffect (listen)", result: pass }
    confidence: medium
    notes: "Unchanged from baseline. Behaviorally equivalent. Per-pellet-vs-per-impact count, the WorldOnly-trace mismatch on player hits, and the unverified muzzleflash MODEL remain open."

  - id: weapon-shotgun.fx.impact_ricochet_audio
    name: "Impact ricochet sound + melee woosh + casing eject"
    layer: [presentation]
    base_refs:
      - "common/weapons/weapon/shotgun.qc:wr_impacteffect (SND_RIC_RANDOM, w_random<0.05 / 0.25 s prevric throttle, shotgun.qc:404-409)"
      - "common/weapons/weapon/shotgun.qc:W_Shotgun_Melee_Think (EFFECT_SHOTGUN_WOOSH, shotgun.qc:126)"
      - "common/weapons/weapon/shotgun.qc:W_Shotgun_Attack (SpawnCasing when g_casings>=1, shotgun.qc:78-83)"
    port_refs:
      - "src/XonoticGodot.Common/Gameplay/Weapons/WeaponFiring.cs:BulletImpactFx (calls SoundSystem.PlayRic — LIVE, throttled)"
      - "src/XonoticGodot.Common/Gameplay/Weapons/WeaponFiring.cs:MeleeWoosh (emits SHOTGUN_WOOSH — LIVE)"
      - "src/XonoticGodot.Common/Gameplay/Weapons/WeaponFiring.cs:EjectCasing (emits 'casing_shell', gate>=1 — LIVE both ends)"
      - "src/XonoticGodot.Common/Gameplay/Effects/EffectsList.cs:101 (CASING_SHELL registered Effect)"
      - "src/XonoticGodot.Common/Gameplay/Sounds/SoundSystem.cs:PlayRic (RIC group ric1..ric3 registered)"
      - "game/client/EffectSystem.cs:Spawn -> SpawnCasing(origin, velocity, shell) for casing_shell/CASING_SHELL"
    constants:
      - { name: "ricochet chance / throttle", base: "w_random<0.05 / 0.25 s (prevric)", port: "Prandom.Float()<0.05 / now-actor.PrevRic>0.25 throttle (WeaponFiring.cs:552-558)", match: true }
      - { name: "g_casings shotgun gate", base: ">=1 ejects shotgun casing (shotgun.qc:78); g_casings default 2", port: "EjectCasing(Shell) gates >=1", match: true }
      - { name: "casing eject velocity", base: "(rand*50+50)*v_right - (rand*25+25)*v_forward - (rand*5-30)*v_up (shotgun.qc:82)", port: "(rand*50+50)*right - (rand*25+25)*forward + (30-rand*5)*up (WeaponFiring.cs:592-594)", match: true }
      - { name: "casing renders on client", base: "yes (casings net temp -> CSQC casing entity)", port: "yes — 'casing_shell' is a registered Effect; networks (WriteEffect) + listen RenderSink route to EffectSystem.SpawnCasing -> ShellCasings.Spawn", match: true }
    status:
      logic: faithful
      values: faithful
      timing: faithful
      presentation: faithful
      audio: faithful
      liveness: live
    intended_divergence: false
    divergence_rationale: null
    gaps: []
    verification:
      - { kind: code, ref: "BulletImpactFx now throttles the ricochet: if (!silent && now - actor.PrevRic > 0.25) { if (Prandom.Float() < 0.05) SoundSystem.PlayRic(actor); actor.PrevRic = now; } — matches shotgun.qc:404-409 (prevric reset on every gate pass, 5% roll)", result: pass }
      - { kind: code, ref: "MeleeWoosh -> Emit('SHOTGUN_WOOSH'); EjectCasing -> EmitByEffectInfoName('casing_shell'); EffectsList.cs:101 registers CASING_SHELL so the effect is NON-null", result: pass }
      - { kind: code, ref: "ServerNet.WriteEffect networks non-null Effect (velocity-carrying); EffectSystem.Spawn('casing_shell') -> SpawnCasing(shell) -> ShellCasings.Spawn(origin,velocity)", result: pass }
      - { kind: code, ref: "Base shotgun.qc:78,82 verified: g_casings>=1, full 3D eject velocity with up (30-rand*5); port gate>=1, upBase=30 — MATCH", result: pass }
    confidence: high
    notes: "FIX vs baseline (logic/values partial, audio partial, timing missing -> all faithful). The baseline recorded the ricochet sound un-throttled (played on every pellet impact, ~12/shot). BulletImpactFx now ports the full prevric throttle: at most once per 0.25s per actor, AND only on a 5% roll (w_random<0.05), with prevric reset on every gate pass — matching shotgun.qc:404-409. The .PrevRic per-actor state field now exists (the baseline 'no per-actor prevric state field' latent gap is closed). Casing eject + melee woosh remain live + faithful."

  - id: weapon-shotgun.notify.killmessage
    name: "Obituary: slap vs blast kill message + suicide line"
    layer: [authority]
    base_refs:
      - "common/weapons/weapon/shotgun.qc:wr_killmessage (shotgun.qc:388-394)"
      - "common/weapons/weapon/shotgun.qc:wr_suicidemessage (shotgun.qc:383-386)"
    port_refs:
      - "src/XonoticGodot.Common/Gameplay/Notifications/DeathMessages.cs:91 (SelectKillMessage shotgun branch)"
      - "src/XonoticGodot.Common/Gameplay/Weapons/Shotgun.cs:MeleeThink (slapDeathType via DeathTypes.WithHitType + Combat.Damage)"
      - "src/XonoticGodot.Server/Scores.cs:643 (EmitObituary -> SelectKillMessage with full deathType)"
    constants:
      - { name: "murder slap notif", base: "(HITTYPE_SECONDARY) ? WEAPON_SHOTGUN_MURDER_SLAP : WEAPON_SHOTGUN_MURDER", port: "slap tags |secondary so MURDER_SLAP is reached; blast -> MURDER", match: true }
      - { name: "suicide notif", base: "WEAPON_THINKING_WITH_PORTALS", port: "WEAPON_THINKING_WITH_PORTALS", match: true }
    status:
      logic: faithful
      values: faithful
      timing: na
      presentation: na
      audio: na
      liveness: live
    intended_divergence: false
    divergence_rationale: null
    gaps: []
    verification:
      - { kind: code, ref: "Shotgun.MeleeThink builds slapDeathType = WithHitType(FromWeapon('shotgun'), Secondary) and calls Combat.Damage(... slapDeathType ...) directly", result: pass }
      - { kind: code, ref: "Combat.Damage carries DeathType string -> Scores.EmitObituary -> SelectKillMessage(WeaponNetNameOf, deathType) -> sec true -> WEAPON_SHOTGUN_MURDER_SLAP", result: pass }
    confidence: high
    notes: "The slap tags HITTYPE_SECONDARY (DeathTypes.Secondary suffix) and routes through Combat.Damage with the tagged string, so the |secondary suffix survives to Scores.EmitObituary -> SelectKillMessage -> WEAPON_SHOTGUN_MURDER_SLAP. Blast tags no suffix -> WEAPON_SHOTGUN_MURDER. Suicide line faithful. Faithful + live, unchanged."

  - id: weapon-shotgun.menu.describe
    name: "MENUQC weapon-guide prose (describe)"
    layer: [presentation]
    base_refs:
      - "common/weapons/weapon/shotgun.qc:describe (MENUQC, shotgun.qc:416-428)"
    port_refs: []
    constants: []
    status:
      logic: na
      values: na
      timing: na
      presentation: missing
      audio: na
      liveness: na
    intended_divergence: false
    divergence_rationale: null
    gaps:
      - "The MENUQC describe() weapon-guide prose (primary single round spreading into pellets, secondary slap timing, ammo/falloff blurb + W_Guide_Keybinds/DPS) has no port. Other weapons (Crylink/Electro/Tuba) reference a describe seam but the shotgun text is not ported. Menu-only documentation; no gameplay impact."
    verification:
      - { kind: code, ref: "grep describe/PAGE_TEXT across src/.../Weapons: no Shotgun describe port; Base shotgun.qc:416-428 has the MENUQC describe()", result: fail }
    confidence: high
    notes: "New row (registry coverage gap, not a regression). MENUQC-only in-menu weapon guide prose; cosmetic documentation feature with no gameplay path."
