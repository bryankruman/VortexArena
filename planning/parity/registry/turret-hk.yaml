unit: turret-hk
category: turret
base_ref_rev: "v0.8.6-1779-g863cd3e84"
last_audited: "2026-06-22"
summary:
  feature_count: 11
  worst_gaps:
    - "Guidance accel/decel constants STILL hardcoded & wrong in GuidedProjectile.HkThink (accel 1.05/accel2 1.1/decel 0.85 vs Base 1.025/1.05/0.9) — wrong speed envelope (turnrate WAS fixed to 0.25)"
    - "Fuel-out behavior differs: Base goes inert (MOVETYPE_BOUNCE, stops thinking, drops as a dud) at cnt<time; port still detonates at TTL and conflates the 30s fuel window with the 30s TTL"
    - "No re-seek of nearest valid target when enemy lost (Base scans g_damagedbycontents within 5000u via hk_is_valid_target); port still only nulls a dead enemy and holds heading"
    - "No external target reception (turret_hk_addtarget / TUR_FLAG_RECIEVETARGETS): HK cannot be fed a designated target"
    - "No WEP_HK player special-attack weapon (hidden/dev path) — not ported at all"
    - "Head model (hk.md3) not instantiated as a separate head entity; no tag_head attachment"
    - "No tr_think head-frame animation, no muzzle te_explosion, no PROJECTILE_ROCKET trail (presentation)"
    - "Select/firecheck flag set still diverges (no SelectVehicles/SelectTriggerTarget consts; AFF not a discrete gate)"
features:
  - id: turret-hk.identity.def
    name: "Identity, hitbox, models, spawnflags"
    layer: [authority, presentation]
    base_refs:
      - "common/turrets/turret/hk.qh:HunterKiller"
    port_refs:
      - "src/XonoticGodot.Common/Gameplay/Turrets/HkTurret.cs:HkTurret"
    constants:
      - { name: "mins", base: "'-32 -32 0'", port: "(-32,-32,0)", match: true }
      - { name: "maxs", base: "'32 32 64'", port: "(32,32,64)", match: true }
      - { name: "model", base: "models/turrets/base.md3", port: "models/turrets/base.md3", match: true }
      - { name: "head_model", base: "models/turrets/hk.md3", port: "none (no head bone entity)", match: false }
      - { name: "netname", base: "hk", port: "hk", match: true }
      - { name: "health", base: "500", port: "500", match: true }
      - { name: "spawnflags", base: "TUR_FLAG_SPLASH|MEDPROJ|PLAYER|RECIEVETARGETS", port: "SPLASH via radius damage; PLAYER implicit; RECIEVETARGETS not modeled", match: false }
    status:
      logic: faithful
      values: faithful
      timing: na
      presentation: partial
      audio: na
      liveness: live
    intended_divergence: false
    divergence_rationale: null
    gaps:
      - "Head model (hk.md3) not instantiated as a separate head entity; no tag_head attachment."
      - "TUR_FLAG_RECIEVETARGETS spawnflag not modeled (see turret-hk.target.recieve)."
    verification:
      - { kind: code_read, ref: "hk.qh vs HkTurret ctor + Spawn (unchanged by Wave-2)", result: pass }
    confidence: high
    notes: "Identity/hitbox/base model + health faithful; head bone and receive-targets flag not modeled. Unchanged from baseline."
  - id: turret-hk.setup.tr_setup
    name: "tr_setup: aim/select/firecheck/shoot flags, ammo flags"
    layer: [authority]
    base_refs:
      - "common/turrets/turret/hk.qc:24"
      - "common/turrets/sv_turrets.qc:turret_initialize"
    port_refs:
      - "src/XonoticGodot.Common/Gameplay/Turrets/HkTurret.cs:Think"
      - "src/XonoticGodot.Common/Gameplay/Turrets/TurretSpawn.cs:Init"
    constants:
      - { name: "aim_flags", base: "TFL_AIM_SIMPLE", port: "aimSimple=true", match: true }
      - { name: "shoot_flags", base: "TFL_SHOOT_CLEARTARGET", port: "clearTarget=true", match: true }
      - { name: "ammo_flags", base: "TFL_AMMO_ROCKETS|RECHARGE", port: "ammo pool only (recharge)", match: false }
      - { name: "select_flags", base: "LOS|VEHICLES|TRIGGERTARGET|RANGELIMITS|TEAMCHECK", port: "Los|Players|RangeLimits|TeamCheck|AngleLimits", match: false }
      - { name: "firecheck_flags", base: "DEAD|TEAMCHECK|REFIRE|AFF", port: "framework firecheck (no discrete AFF)", match: false }
      - { name: "target_validate_flags", base: "VEHICLES|TEAMCHECK", port: "framework ValidTarget", match: false }
    status:
      logic: faithful
      values: partial
      timing: na
      presentation: na
      audio: na
      liveness: live
    intended_divergence: false
    divergence_rationale: null
    gaps:
      - "Select flags differ: Base has VEHICLES + TRIGGERTARGET (no discrete ANGLELIMITS); port has Players + AngleLimits and NO SelectVehicles/SelectTriggerTarget flag (those flags do not exist in TurretAI) — vehicle acquirability unverified, trigger-target acquisition absent."
      - "AFF (avoid-friendly-fire trace) firecheck flag not modeled as a discrete gate."
    verification:
      - { kind: code_read, ref: "hk.qc tr_setup vs HkTurret.Think Select const + TurretParams (unchanged by Wave-2)", result: pass }
    confidence: medium
    notes: "Core setup faithful (simple-aim, clear-target after fire); the select/firecheck flag SET still diverges (vehicles, triggertarget, AFF). Unchanged from baseline."
  - id: turret-hk.spawn.liveness
    name: "Map spawnfunc wiring + per-frame think (turret_hk placed on map)"
    layer: [authority]
    base_refs:
      - "common/turrets/turret/hk.qc:9"
      - "common/turrets/sv_turrets.qc:turret_link"
    port_refs:
      - "src/XonoticGodot.Common/Gameplay/Turrets/TurretSpawnFuncs.cs:Hk"
      - "src/XonoticGodot.Common/Gameplay/MapObjects/MapObjectsRegistry.cs:211"
    constants:
      - { name: "g_turrets master switch", base: "1 (default on)", port: "MasterSwitchEnabled default-on", match: true }
    status:
      logic: faithful
      values: faithful
      timing: faithful
      presentation: na
      audio: na
      liveness: live
    intended_divergence: false
    divergence_rationale: null
    gaps: []
    verification:
      - { kind: code_read, ref: "SpawnFuncs.Register(turret_hk, TurretSpawnFuncs.Hk) -> Hk -> Spawn(e,'hk'); GameInit runs RegisterAll; Think re-armed each frame", result: pass }
    confidence: high
    notes: "Confirmed live by code-trace (registered spawnfunc, RegisterAll at boot, Think re-armed). Not yet confirmed that a stock map actually places turret_hk. Unchanged from baseline."
  - id: turret-hk.combat.brain
    name: "Acquire/aim/track/fire combat brain (shared turret framework)"
    layer: [authority]
    base_refs:
      - "common/turrets/sv_turrets.qc:turret_think"
      - "common/turrets/sv_turrets.qc:turret_select_target"
      - "common/turrets/sv_turrets.qc:turret_firecheck"
      - "common/turrets/sv_turrets.qc:turret_fire"
    port_refs:
      - "src/XonoticGodot.Common/Gameplay/Turrets/TurretAI.cs:RunCombat"
      - "src/XonoticGodot.Common/Gameplay/Turrets/TurretAI.cs:ScoreTarget"
      - "src/XonoticGodot.Common/Gameplay/Turrets/TurretAI.cs:SelectTarget"
      - "src/XonoticGodot.Common/Gameplay/Turrets/HkTurret.cs:Think"
    constants:
      - { name: "shot_refire", base: "5", port: "5", match: true }
      - { name: "ammo_max", base: "240", port: "240", match: true }
      - { name: "ammo_recharge", base: "16", port: "16", match: true }
      - { name: "target_range", base: "6000", port: "6000", match: true }
      - { name: "target_range_min", base: "220", port: "220", match: true }
      - { name: "target_range_optimal", base: "5000", port: "5000 (FIXED: was 3000)", match: true }
      - { name: "target_select_rangebias", base: "0.5", port: "0.5 (FIXED: now passed via TurretParams, consumed at TurretAI.cs:325)", match: true }
      - { name: "target_select_samebias", base: "0.01", port: "0.01 (FIXED: now passed; seed score *0.01 at TurretAI.cs:352)", match: true }
      - { name: "target_select_anglebias", base: "0.1", port: "0.1 (FIXED: now passed; consumed at TurretAI.cs:326)", match: true }
      - { name: "target_select_playerbias", base: "1", port: "1.0", match: true }
      - { name: "target_select_missilebias", base: "0", port: "0 (FIXED: now passed; moot since SelectMissiles unset)", match: true }
      - { name: "aim_speed", base: "100", port: "100", match: true }
      - { name: "aim_firetolerance_dist", base: "500", port: "500", match: true }
      - { name: "shot_volly", base: "0", port: "0", match: true }
      - { name: "targetscan_maxdelay", base: "1", port: "1 (FIXED: now Cvar('g_turrets_targetscan_maxdelay',1f) at TurretAI.cs:552; was hardcoded 0.6)", match: true }
    status:
      logic: faithful
      values: faithful
      timing: faithful
      presentation: na
      audio: na
      liveness: live
    intended_divergence: false
    divergence_rationale: null
    gaps: []
    verification:
      - { kind: code_read, ref: "Wave-2: HkTurret.Think now passes rangeBias 0.5/sameBias 0.01/angleBias 0.1/missileBias 0/playerBias 1 + rangeOptimal 5000; ScoreTarget (TurretAI.cs:325-328) + SelectTarget seed (:352) consume them; targetscan_maxdelay Cvar-backed default 1 (:552). turrets.cfg defaults confirmed.", result: pass }
    confidence: high
    notes: "BASELINE GAP CLOSED. Wave-2 threaded all the target-scoring biases (rangebias/samebias/anglebias/missilebias) through TurretParams and they are genuinely consumed in ScoreTarget/SelectTarget; target_range_optimal corrected to 5000; framework targetscan_maxdelay now reads the cvar default 1. samebias 0.01 makes the seed score 100x smaller (Base semantics: low samebias = easy to switch off current target — the baseline's 'too sticky' read was inverted; now matches Base)."
  - id: turret-hk.weapon.fire
    name: "wr_think (turret branch): launch the guided rocket"
    layer: [authority]
    base_refs:
      - "common/turrets/turret/hk_weapon.qc:14"
      - "common/turrets/sv_turrets.qc:turret_projectile"
    port_refs:
      - "src/XonoticGodot.Common/Gameplay/Turrets/HkTurret.cs:Attack"
      - "src/XonoticGodot.Common/Gameplay/Turrets/GuidedProjectile.cs:Launch"
    constants:
      - { name: "launch speed", base: "shot_speed * 0.75 = 375", port: "ShotSpeed*0.75 = 375", match: true }
      - { name: "shot_dmg", base: "120", port: "120", match: true }
      - { name: "shot_radius", base: "200", port: "200", match: true }
      - { name: "shot_force", base: "600", port: "600", match: true }
      - { name: "missile size", base: "6", port: "6", match: true }
      - { name: "missile health (shootable)", base: "10", port: "10", match: true }
      - { name: "movetype", base: "MOVETYPE_BOUNCEMISSILE", port: "MoveType.BounceMissile", match: true }
      - { name: "missile_flags", base: "MIF_SPLASH|MIF_PROXY|MIF_GUIDED_AI", port: "splash via radius damage; proxy/guided implicit", match: true }
      - { name: "deathtype", base: "DEATH_TURRET_HK", port: "DeathTypes.TurretHk (turret_hk)", match: true }
      - { name: "fire sound", base: "SND rocket_fire (W_Sound)", port: "weapons/rocket_fire.wav", match: true }
      - { name: "muzzle te_explosion", base: "yes (hk_weapon.qc:30)", port: "none", match: false }
    status:
      logic: faithful
      values: faithful
      timing: faithful
      presentation: partial
      audio: faithful
      liveness: live
    intended_divergence: false
    divergence_rationale: null
    gaps:
      - "No muzzle te_explosion flash on launch (hk_weapon.qc:30)."
      - "No PROJECTILE_ROCKET client trail/smoke on the rocket."
    verification:
      - { kind: code_read, ref: "hk_weapon.qc wr_think (turret branch) vs HkTurret.Attack + GuidedProjectile.Launch (unchanged by Wave-2)", result: pass }
    confidence: high
    notes: "Launch ballistics/damage/flags faithful; fire sound (rocket_fire) plays; muzzle flash + projectile trail still missing (presentation). Unchanged from baseline."
  - id: turret-hk.weapon.guidance
    name: "turret_hk_missile_think: obstacle-avoiding guided-rocket flight model"
    layer: [authority]
    base_refs:
      - "common/turrets/turret/hk_weapon.qc:47"
      - "common/turrets/turret/hk_weapon.qc:241"
    port_refs:
      - "src/XonoticGodot.Common/Gameplay/Turrets/GuidedProjectile.cs:HkThink"
    constants:
      - { name: "shot_speed (band floor)", base: "500", port: "500", match: true }
      - { name: "shot_speed_max", base: "1000", port: "1000", match: true }
      - { name: "shot_speed_turnrate", base: "0.25", port: "0.25 (FIXED: HkTurret passes ShotTurnRate=0.25, consumed via g.TurnRate at HkThink:272)", match: true }
      - { name: "shot_speed_accel", base: "1.025", port: "1.05 (STILL hardcoded in HkThink:226)", match: false }
      - { name: "shot_speed_accel2", base: "1.05", port: "1.1 (STILL hardcoded at HkThink:263,269)", match: false }
      - { name: "shot_speed_decel", base: "0.9", port: "0.85 (STILL hardcoded at HkThink:224)", match: false }
      - { name: "proximity detonate", base: "shot_radius * 0.25 = 50", port: "Radius * 0.25 = 50", match: true }
      - { name: "lead time clamp", base: "min(dist/speed, 0.5)", port: "min(dist/speed, 0.5)", match: true }
      - { name: "forward trace len", base: "myspeed * 3", port: "myspeed * 3", match: true }
      - { name: "seek trace len", base: "myspeed * 2.95", port: "myspeed * 2.95", match: true }
      - { name: "panic ff threshold", base: "ff < 0.5 -> pt_seek 1", port: "ff < 0.5 -> ptSeek 1", match: true }
      - { name: "clear-path dist", base: "1000", port: "1000", match: true }
      - { name: "fuel window cnt", base: "time + 30", port: "FuelTime now+30 AND ExpireTime now+30 (conflated)", match: false }
      - { name: "fuel-out behavior", base: "MOVETYPE_BOUNCE + nextthink=0 (inert dud)", port: "Detonate at TTL", match: false }
      - { name: "re-seek radius (no enemy)", base: "5000 over g_damagedbycontents (hk_is_valid_target)", port: "keeps current heading; no re-seek scan", match: false }
    status:
      logic: partial
      values: partial
      timing: faithful
      presentation: na
      audio: na
      liveness: live
    intended_divergence: false
    divergence_rationale: null
    gaps:
      - "accel/decel constants STILL hardcoded & wrong in GuidedProjectile.HkThink (1.05/1.1/0.85 vs cfg 1.025/1.05/0.9) — accelerates and brakes faster than Base; whole speed envelope off. (GuidedProjectile.cs was NOT touched by Wave-2.)"
      - "Fuel-out: Base makes the rocket inert (MOVETYPE_BOUNCE, nextthink=0) so it drops as a dud at cnt<time; port still collapses the 30s fuel window into the 30s TTL and DETONATES at expiry instead."
      - "No re-seek of the nearest valid target when the enemy is lost (Base scans g_damagedbycontents within 5000u via hk_is_valid_target; HkThink only nulls a dead enemy and holds heading)."
      - "Minor: angular-offset 'ad' computed as 0 when enemy is null (port guards the null); Base derefs null enemy.origin = '0 0 0' yielding a non-zero ad — edge-case divergence only when re-seek would have run."
    verification:
      - { kind: code_read, ref: "hk_weapon.qc turret_hk_missile_think vs GuidedProjectile.HkThink line-by-line; turrets.cfg consts. turnRate fixed at the HkTurret call site (0.25, consumed); accel/decel literals 1.05/1.1/0.85 unchanged in HkThink; fuel-out still detonates; no re-seek scan.", result: pass }
    confidence: high
    notes: "PARTIAL FIX: shot_speed_turnrate corrected 0.1->0.25 (HkTurret now passes ShotTurnRate=0.25, consumed via g.TurnRate). STILL OPEN: accel/decel constants hardcoded wrong in HkThink, fuel-out dud behavior, target re-seek. Funnel-steering structure remains faithfully ported."
  - id: turret-hk.target.recieve
    name: "External target reception (turret_hk_addtarget / TUR_FLAG_RECIEVETARGETS)"
    layer: [authority]
    base_refs:
      - "common/turrets/turret/hk.qc:36"
      - "common/turrets/turret/hk.qc:23"
    port_refs: []
    constants:
      - { name: "validate flags", base: "VEHICLES|TEAMCHECK", port: "none", match: false }
    status:
      logic: missing
      values: missing
      timing: na
      presentation: na
      audio: na
      liveness: na
    intended_divergence: false
    divergence_rationale: null
    gaps:
      - "turret_hk_addtarget not implemented: the HK cannot receive a designated target from relays / other turrets (TUR_FLAG_RECIEVETARGETS); it can only self-acquire."
    verification:
      - { kind: code_read, ref: "no addtarget / cross-turret target-broadcast counterpart in port (HkTurret.Attack NOTE confirms it's still left out)", result: fail }
    confidence: high
    notes: "Port comment in HkTurret.Attack still explicitly notes the receive-targets hook needs the cross-turret target-broadcast system, which isn't modeled. Unchanged from baseline."
  - id: turret-hk.weapon.player
    name: "WEP_HK player special-attack weapon (hidden, impulse 9)"
    layer: [authority, presentation]
    base_refs:
      - "common/turrets/turret/hk_weapon.qc:14"
      - "common/turrets/turret/hk_weapon.qh:HunterKillerAttack"
    port_refs: []
    constants:
      - { name: "weapon flags", base: "WEP_FLAG_HIDDEN | WEP_FLAG_SPECIALATTACK", port: "none", match: false }
    status:
      logic: missing
      values: missing
      timing: missing
      presentation: missing
      audio: missing
      liveness: na
    intended_divergence: false
    divergence_rationale: null
    gaps:
      - "The hidden player weapon WEP_HK (HunterKillerAttack) — the player branch of wr_think (weapon_prepareattack / turret_initparams / W_SetupShot_Dir / weapon_thinkf WFRAME_FIRE1) that lets a given-the-weapon player fire the same guided rocket — is not ported at all (no WEP_HK / HunterKillerAttack symbol exists in the port)."
    verification:
      - { kind: code_read, ref: "grep WEP_HK|HunterKillerAttack across src/** -> no matches", result: fail }
    confidence: high
    notes: "Hidden/dev-cheat path; only the turret (non-player) branch of wr_think is ported (HkTurret.Attack). Cosmetic/edge gameplay impact. Unchanged from baseline."
  - id: turret-hk.damage.lifecycle
    name: "Damage gating, retaliation, death + respawn"
    layer: [authority]
    base_refs:
      - "common/turrets/sv_turrets.qc:turret_damage"
      - "common/turrets/sv_turrets.qc:turret_die"
      - "common/turrets/sv_turrets.qc:turret_respawn"
    port_refs:
      - "src/XonoticGodot.Common/Gameplay/Turrets/TurretAI.cs:EventDamage"
      - "src/XonoticGodot.Common/Gameplay/Turrets/TurretAI.cs:Damage"
      - "src/XonoticGodot.Common/Gameplay/Turrets/TurretAI.cs:Die"
      - "src/XonoticGodot.Common/Gameplay/Turrets/TurretSpawn.cs:Init"
    constants:
      - { name: "respawntime", base: "90", port: "90 (FIXED: HkTurret.Spawn now passes respawnTime=RespawnTime=90 to TurretSpawn.Init)", match: true }
      - { name: "takedamage", base: "DAMAGE_AIM", port: "DamageMode.Aim", match: true }
      - { name: "inactive/dead take no damage", base: "yes", port: "yes", match: true }
      - { name: "friendlyfire gate", base: "g_friendlyfire", port: "g_friendlyfire", match: true }
    status:
      logic: faithful
      values: faithful
      timing: faithful
      presentation: na
      audio: unknown
      liveness: live
    intended_divergence: false
    divergence_rationale: null
    gaps: []
    verification:
      - { kind: test, ref: "TurretLifecycleTests (shared Damage_Dead/Inactive/SameTeam gates)", result: pass }
      - { kind: code_read, ref: "Wave-2: TurretSpawn.Init wires e.GtEventDamage = TurretAI.EventDamage; DamageSystem.EventDamage (:331-333) routes non-player edicts with GtEventDamage to it -> turret pre-damage gate (TurretAI.Damage) + health subtract + Combat.Death on lethal -> OnAnyDeath -> Die (blast + respawn). HkTurret.Spawn now threads respawnTime=90.", result: pass }
    confidence: high
    notes: "BASELINE GAP CLOSED. respawntime corrected 60->90 (HkTurret.Spawn now passes RespawnTime). Additionally Wave-2 wired the turret's own .event_damage (GtEventDamage) into the live DamageSystem path, so the pre-damage gate in TurretAI.Damage (previously bit-faithful but with no discrete live caller) now runs on the real damage route, mirroring Onslaught objectives."
  - id: turret-hk.anim.head
    name: "tr_think head-frame animation (launcher cycle 0..5)"
    layer: [authority, presentation]
    base_refs:
      - "common/turrets/turret/hk.qc:15"
      - "common/turrets/turret/hk_weapon.qc:40"
    port_refs: []
    constants:
      - { name: "frame cycle", base: "++frame, wrap > 5 -> 0", port: "none", match: false }
    status:
      logic: missing
      values: missing
      timing: missing
      presentation: missing
      audio: na
      liveness: na
    intended_divergence: false
    divergence_rationale: null
    gaps:
      - "No tr_think head-frame cycler (0..5) and no fire-time kick (++tur_head.frame); the launcher head never plays its load/cycle animation."
    verification:
      - { kind: code_read, ref: "no frame handling in HkTurret / TurretSpawn", result: fail }
    confidence: high
    notes: "Cosmetic head animation; entirely absent in port (consistent with other ported turrets' missing frame handling). Unchanged from baseline."
  - id: turret-hk.head.track
    name: "Head rotation: track type + aim clamps"
    layer: [authority]
    base_refs:
      - "common/turrets/sv_turrets.qc:turret_track"
    port_refs:
      - "src/XonoticGodot.Common/Gameplay/Turrets/TurretAI.cs:Track"
      - "src/XonoticGodot.Common/Gameplay/Turrets/HkTurret.cs:Think"
    constants:
      - { name: "aim_speed", base: "100", port: "100", match: true }
      - { name: "aim_maxrot", base: "360", port: "360", match: true }
      - { name: "aim_maxpitch", base: "20", port: "20 (FIXED: was 30)", match: true }
      - { name: "track_type", base: "3 (fluid inertia)", port: "3 (FluidInertia)", match: true }
      - { name: "track_accel_pitch", base: "0.25", port: "0.25 (FIXED: now passed; consumed at TurretAI.cs:497)", match: true }
      - { name: "track_accel_rot", base: "0.6", port: "0.6 (FIXED: now passed; consumed at TurretAI.cs:498)", match: true }
      - { name: "track_blendrate", base: "0.2", port: "0.2 (FIXED: now passed; consumed at TurretAI.cs:499)", match: true }
    status:
      logic: faithful
      values: faithful
      timing: faithful
      presentation: na
      audio: na
      liveness: live
    intended_divergence: false
    divergence_rationale: null
    gaps: []
    verification:
      - { kind: code_read, ref: "Wave-2: HkTurret.Think now passes aimMaxPitch=20 + trackAccelPitch 0.25/trackAccelRot 0.6/trackBlendRate 0.2; Track (TurretAI.cs:497-499) consumes them. turrets.cfg defaults confirmed.", result: pass }
    confidence: high
    notes: "BASELINE GAP CLOSED. aim_maxpitch corrected 30->20; the three head-track motor rates (track_accel_pitch/rot/blendrate) now threaded through TurretParams and genuinely consumed in Track. track_type + aim_speed + aim_maxrot remain matched."
