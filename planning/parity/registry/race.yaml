unit: race
category: gametype
base_ref_rev: "v0.8.6-1779-g863cd3e84"
last_audited: "2026-06-25"
summary:
  feature_count: 19
  worst_gaps:
    - "race_place START GRID ordering + trigger_race_checkpoint_verify map-load validation still missing; no bot race role (havocbot_role_race); no speed awards (race_SpeedAwardFrame). The Race force-keyboard movement quantization (M_SQRT1_2 octant snap) is implemented for CTS (Cts.OnPlayerPhysics) but NOT for Race — Race.cs registers no PlayerPhysics hook; cl_movecliptokeyboard 2 stuffcmd absent for both."
    - "HUD_Mod_Race MOD-ICON (floating PB+server-best+medal flash) has no port code (ModIconsPanel has no Race case). The split-timer (#8) + checkpoints (#27) panels still feed host-side only (no TE_CSQC_RACE net path), so split/anticipation/speed/medal are listen-server only for the split panels."
    - "Lap-limit default (port 7) differs from Base (-1 = consult mapinfo). cl_allow_uid* consent gate on the record DB still absent (RaceRecords only checks for a non-empty uid)."
    - "FIXED since 2026-06-22: the spawnflag-4 wrong-way kill AND the spawnflag-8 timed-finish (start!=finish track) are now BOTH armed from the BSP lump (WireObjectiveSpawns GameWorld.cs:2053-2055 passes punishWrongWay + timed); the wrong-way kill still uses DeathTypes.Void not Base DEATH_HURTTRIGGER (cosmetic deathtype only)."
    - "FIXED since 2026-06-22: the g_race_qualifying_timelimit SOURCE is now wired at Race activation (GameWorld.cs:1986-1997) — g_race_qualifying_timelimit(_override)>0 derives qualifying==2, stashes the real limits, and replaces the active timelimit with the qualifying clock; TransitionQualifyingToRace restores them when it elapses."
    - "Carried from prior audit: respawn-at-last-checkpoint is LIVE (RespawnSpot/ApplyRespawnSpot via Clients.OnPreSpawn → SpawnSystem SpawnPointTarg), and the race/CTS RANKINGS scoreboard column is genuinely NETWORKED (ServerNet snapshots RaceRecords top-15 → ScoreboardBlock wire → ScoreboardPanel.SetRankings) so it works for a pure remote client."
features:
  - id: race.mode.setup_limits
    name: "Mode setup + limits (teams, qualifying, lap/frag/time limits)"
    layer: [authority]
    base_refs:
      - "common/gametypes/gametype/race/sv_race.qc:rc_SetLimits"
      - "common/gametypes/gametype/race/sv_race.qc:race_Initialize"
      - "common/gametypes/gametype/race/race.qh:Race INIT"
    port_refs:
      - "src/XonoticGodot.Common/Gameplay/GameTypes/Race.cs:Activate"
      - "src/XonoticGodot.Common/Gameplay/GameTypes/Race.cs:RaceTeams"
      - "src/XonoticGodot.Common/Gameplay/GameTypes/Race.cs:LapLimit"
    constants:
      - { name: "g_race_laps_limit (default)", base: "-1 (use mapinfo); 0 = unlimited", port: "7 (DefaultLapLimit fallback)", match: false }
      - { name: "g_race_teams (default)", base: "0", port: "0; clamp 2..4 when set", match: true }
      - { name: "g_race_qualifying_timelimit (default)", base: "0; >0 ⇒ qualifying==2 + timelimit swap", port: "wired (GameWorld.cs:1986-1997: reads g_race_qualifying_timelimit(_override), derives qualifying==2, stashes limits, replaces active timelimit)", match: true }
      - { name: "gametype legacy defaults", base: "timelimit=20 qualifying=5 laplimit=7 teamlaplimit=15 leadlimit=0", port: "laplimit=7 only", match: false }
    status:
      logic: partial
      values: partial
      timing: faithful
      presentation: na
      audio: na
      liveness: live
    intended_divergence: false
    divergence_rationale: null
    gaps:
      - "Unset g_race_laps_limit defaults to 7 in the port; Base (-1) consults the mapinfo laplimit."
      - "The remaining legacy gametype_init defaults (teamlaplimit=15, the 20-min timelimit / 5-min qualifying defaults) are not applied as defaults — only laplimit=7 has a port fallback."
    verification:
      - { kind: code, ref: "Race.Activate / RaceTeams / LapLimit (live in GameWorld WireGametype:1972) + qualifying-timelimit source wired (GameWorld.cs:1986-1997: q-timelimit>0 ⇒ set g_race_qualifying=2, SaveRaceLimits, replace timelimit)", result: pass }
    confidence: high
    notes: "FIXED since 2026-06-22 (qualifying-timelimit source wired). Team clamp + lap-limit core wired & tested; the g_race_qualifying_timelimit(_override) source now derives qualifying==2 and stashes/swaps the limits at activation (the SaveRaceLimits caller the prior audit flagged as missing now exists). Only the lap-limit -1=mapinfo semantics and the other legacy-default seeds remain divergent."

  - id: race.score.rules
    name: "Score rules (fastest/laps/time columns + sort keys, no SP_SCORE)"
    layer: [authority]
    base_refs:
      - "common/gametypes/gametype/race/sv_race.qc:race_ScoreRules"
    port_refs:
      - "src/XonoticGodot.Common/Gameplay/GameTypes/Race.cs:DeclareScoreRules"
    constants:
      - { name: "SP_RACE_FASTEST flags", base: "PRIMARY|LOWER|TIME (qualifying)", port: "LowerIsBetter|Time", match: true }
      - { name: "SP_RACE_LAPS / SP_RACE_TIME flags", base: "LAPS PRIMARY; TIME SECONDARY|LOWER|TIME", port: "LAPS primary; TIME secondary lower time", match: true }
      - { name: "score_enabled", base: "false (no SP_SCORE)", port: "scoreEnabled: false", match: true }
    status:
      logic: faithful
      values: faithful
      timing: na
      presentation: na
      audio: na
      liveness: live
    intended_divergence: false
    divergence_rationale: null
    gaps: []
    verification:
      - { kind: code, ref: "Race.DeclareScoreRules vs race_ScoreRules", result: pass }
    confidence: high
    notes: "Faithful column/sort declaration for FFA, qualifying, and team race."

  - id: race.checkpoint.entities
    name: "Checkpoint trigger entities spawned from the map lump"
    layer: [authority]
    base_refs:
      - "server/race.qc:spawnfunc(trigger_race_checkpoint)"
      - "server/race.qc:checkpoint_touch"
    port_refs:
      - "src/XonoticGodot.Common/Gameplay/GameTypes/Race.cs:SpawnCheckpoint"
      - "src/XonoticGodot.Server/GameWorld.cs:WireObjectiveSpawns"
      - "src/XonoticGodot.Common/Gameplay/MapObjects/MapObjectsRegistry.cs:RaceCheckpoint"
    constants:
      - { name: "finish line index", base: "race_timed_checkpoint (cp 0 closed-loop; highest CP via spawnflags & 8)", port: "TimedCheckpoint = 0 default, follows highest CP when spawnflag 8 set; armed from e.SpawnFlags & 8 (GameWorld.cs:2055)", match: true }
    status:
      logic: faithful
      values: faithful
      timing: na
      presentation: na
      audio: na
      liveness: live
    intended_divergence: false
    divergence_rationale: null
    gaps:
      - "Defrag target_checkpoint / target_startTimer / target_stopTimer chain not modeled for Race (CTS-only no-ops)."
    verification:
      - { kind: code, ref: "GameWorld.WireObjectiveSpawns Race arm (GameWorld.cs:2053-2055): SpawnCheckpoint(e.Origin, e.Count, punishWrongWay: (e.SpawnFlags & 4)!=0, timed: (e.SpawnFlags & 8)!=0) — both spawnflag variants now armed from the BSP lump", result: pass }
    confidence: high
    notes: "FIXED since 2026-06-22. The prior audit flagged the spawnflag-4 wrong-way variant and the spawnflag-8 timed-finish as un-armed (WireObjectiveSpawns dropped e.SpawnFlags). The Race arm now passes BOTH punishWrongWay (spawnflag 4) and timed (spawnflag 8), so a start!=finish track scores the lap at its highest timed CP (race_timed_checkpoint) instead of hardcoding CP 0. Checkpoints + penalty zones are genuinely spawned from the BSP entity lump on the live path."

  - id: race.checkpoint.ordered_crossing
    name: "Ordered checkpoint crossing detection + advance"
    layer: [authority]
    base_refs:
      - "server/race.qc:checkpoint_passed"
      - "server/race.qc:race_NextCheckpoint"
    port_refs:
      - "src/XonoticGodot.Common/Gameplay/GameTypes/Race.cs:CheckpointTouch"
      - "src/XonoticGodot.Common/Gameplay/GameTypes/Race.cs:CrossCheckpoint"
    constants:
      - { name: "wrong-CP kill damage (spawnflag 4)", base: "10000 (DEATH_HURTTRIGGER)", port: "10000 (DeathTypes.Void)", match: false }
    status:
      logic: faithful
      values: faithful
      timing: na
      presentation: na
      audio: na
      liveness: live
    intended_divergence: false
    divergence_rationale: null
    gaps:
      - "Uses DeathTypes.Void instead of Base's DEATH_HURTTRIGGER for the wrong-way kill (cosmetic deathtype/obituary only; the 10000 damage matches)."
      - "No SUB_UseTargets / equipment-clear (Portal_ClearAll, porto_forbidden) on crossing."
    verification:
      - { kind: code, ref: "Race.CheckpointTouch expected-next + _punishWrongWay set; now populated from the BSP lump (WireObjectiveSpawns GameWorld.cs:2054 punishWrongWay: (e.SpawnFlags & 4)!=0), so a wrong-order crossing of a spawnflag-4 CP deals 10000 on the live path", result: pass }
    confidence: high
    notes: "FIXED since 2026-06-22 (wrong-way kill now LIVE). Next-CP wrap + start-line lap reset match Base; the punitive spawnflag-4 branch is now armed from the map lump (GameWorld.cs:2054) so a wrong-order crossing actually kills. Residual: the Void-vs-HURTTRIGGER deathtype and the SUB_UseTargets / equipment-clear side branches remain omitted."

  - id: race.lap.timing_scoring
    name: "Lap close: fastest-lap + laps + cumulative-time scoring"
    layer: [authority]
    base_refs:
      - "server/race.qc:race_SendTime"
    port_refs:
      - "src/XonoticGodot.Common/Gameplay/GameTypes/Race.cs:CrossCheckpoint"
    constants:
      - { name: "TIME_FACTOR", base: "100 (hundredths)", port: "TimeEncode hundredths", match: true }
    status:
      logic: faithful
      values: faithful
      timing: partial
      presentation: na
      audio: na
      liveness: live
    intended_divergence: false
    divergence_rationale: null
    gaps:
      - "Lap time uses wall Api.Clock.Time, not the QC race_movetime per-frame fractional accumulator; lap times drift from Base under frame-time jitter or pause."
    verification:
      - { kind: test, ref: "RaceOvertimePenaltyTests (lap close paths)", result: pass }
    confidence: high
    notes: "Unchanged by Wave-2. Fastest/laps/time columns updated faithfully; timing source still differs from Base."

  - id: race.records.db
    name: "Per-map top-99 record ranking DB (read/pos/write/setTime)"
    layer: [authority]
    base_refs:
      - "server/race.qc:race_readTime"
      - "server/race.qc:race_readPos"
      - "server/race.qc:race_writeTime"
      - "server/race.qc:race_setTime"
    port_refs:
      - "src/XonoticGodot.Common/Gameplay/GameTypes/RaceRecords.cs:SetTime"
      - "src/XonoticGodot.Common/Gameplay/GameTypes/RaceRecords.cs:WriteTime"
      - "src/XonoticGodot.Common/Gameplay/GameTypes/RaceRecords.cs:ReadPos"
    constants:
      - { name: "RANKINGS_CNT", base: "99", port: "99", match: true }
      - { name: "record key scheme", base: "strcat(map, record_type, field, pos)", port: "map+type+field+pos", match: true }
      - { name: "UID requirement", base: "needs crypto_idfp + cl_allow_uid* ==1", port: "needs non-empty uid (no cl_allow_uid* gate)", match: false }
    status:
      logic: faithful
      values: faithful
      timing: na
      presentation: na
      audio: na
      liveness: live
    intended_divergence: false
    divergence_rationale: null
    gaps:
      - "No cl_allow_uidtracking / cl_allow_uid2name consent gate (Base refuses to store/name without both)."
    verification:
      - { kind: test, ref: "RaceRecordsTests", result: pass }
    confidence: high
    notes: "Filing is live (CrossCheckpoint -> RecordFinishTime -> SetTime); the classified result is now also consumed (see race.records.notifications, fixed in Wave-2)."

  - id: race.records.notifications
    name: "Record result + finish/abandon notifications (INFO_RACE_*)"
    layer: [authority]
    base_refs:
      - "server/race.qc:race_setTime"
      - "common/gametypes/gametype/race/sv_race.qc:PlayerDies"
      - "server/race.qc:race_AbandonRaceCheck"
    port_refs:
      - "src/XonoticGodot.Common/Gameplay/GameTypes/Race.cs:RecordFinishTime"
      - "src/XonoticGodot.Common/Gameplay/GameTypes/Race.cs:SendRecordNotification"
      - "src/XonoticGodot.Common/Gameplay/GameTypes/Race.cs:FinishRacer"
      - "src/XonoticGodot.Common/Gameplay/GameTypes/Race.cs:AbandonRaceCheck"
      - "src/XonoticGodot.Common/Gameplay/Notifications/NotificationsList.cs:RACE_FINISHED"
      - "src/XonoticGodot.Common/Gameplay/Notifications/NotificationsList.cs:RACE_ABANDONED"
    constants:
      - { name: "INFO_RACE_NEW_* / FAIL_* / MISSING_UID arg counts", base: "decl in notifications.inc", port: "all 8 calls match declared (stringCount,floatCount)", match: true }
    status:
      logic: faithful
      values: faithful
      timing: na
      presentation: faithful
      audio: na
      liveness: live
    intended_divergence: false
    divergence_rationale: null
    gaps:
      - "Notification time tokens are TIME_ENCODE'd ints (matches Base race_diff/race_time HUD tokens); not yet verified visually in a live client, but the dispatch path is the same NotificationSystem.Info used by Ctf/Domination."
    verification:
      - { kind: code, ref: "RecordFinishTime->SendRecordNotification (NEW_IMPROVED/SET/BROKEN/FAIL_RANKED/FAIL_UNRANKED/NEW_MISSING_UID) + FinishRacer RACE_FINISHED + AbandonRaceCheck RACE_ABANDONED; all 8 arg counts cross-checked vs NotificationsList decls", result: pass }
    confidence: high
    notes: "FIXED in Wave-2 (was stub/dead). CrossCheckpoint->RecordFinishTime is live; the classified RaceRecordResult now fires the matching INFO_RACE_* line via NotificationSystem.Info (a live broadcast used by other gametypes). The old race.Events dependency is bypassed (Events stays null but is no longer required)."

  - id: race.penalty.zones
    name: "Penalty zones (freeze in race / accumulate in qualifying)"
    layer: [authority]
    base_refs:
      - "server/race.qc:spawnfunc(trigger_race_penalty)"
      - "server/race.qc:race_ImposePenaltyTime"
      - "common/gametypes/gametype/race/sv_race.qc:PlayerPhysics"
    port_refs:
      - "src/XonoticGodot.Common/Gameplay/GameTypes/Race.cs:SpawnPenaltyZone"
      - "src/XonoticGodot.Common/Gameplay/GameTypes/Race.cs:SetPenalty"
      - "src/XonoticGodot.Common/Gameplay/GameTypes/Race.cs:Tick"
    constants:
      - { name: "default penalty seconds", base: "5", port: "5", match: true }
    status:
      logic: partial
      values: faithful
      timing: faithful
      presentation: na
      audio: na
      liveness: live
    intended_divergence: false
    divergence_rationale: null
    gaps:
      - "Freeze release restores MoveType.Walk unconditionally (Base just clears the freeze; a swimming/flying racer would be forced to Walk)."
      - "disableclientprediction = 2 (Base) is not set during the freeze; the client may mispredict through the freeze."
      - "RACE_NET_PENALTY_* networking (the on-screen penalty line) is not sent — penalty is invisible to the HUD."
    verification:
      - { kind: test, ref: "RaceOvertimePenaltyTests.PenaltyZone_*", result: pass }
    confidence: high
    notes: "Unchanged by Wave-2. Freeze/accumulate behaviour + expiry tested; the prediction/HUD side is missing."

  - id: race.win.condition
    name: "Win condition: everyone-finished + lap-limit + sudden-death run-on"
    layer: [authority]
    base_refs:
      - "common/gametypes/gametype/race/sv_race.qc:WinningCondition_Race"
      - "common/gametypes/gametype/race/sv_race.qc:CheckRules_World"
      - "server/race.qc:race_StartCompleting"
      - "server/race.qc:race_AbandonRaceCheck"
    port_refs:
      - "src/XonoticGodot.Common/Gameplay/GameTypes/Race.cs:CheckWinningCondition"
      - "src/XonoticGodot.Common/Gameplay/GameTypes/Race.cs:FinishRacer"
      - "src/XonoticGodot.Common/Gameplay/GameTypes/Race.cs:StartCompleting"
      - "src/XonoticGodot.Common/Gameplay/GameTypes/Race.cs:AbandonRaceCheck"
      - "src/XonoticGodot.Server/GameWorld.cs:2346 (case Race: race.Tick(); race.CheckWinningCondition())"
    constants: []
    status:
      logic: faithful
      values: faithful
      timing: na
      presentation: na
      audio: na
      liveness: live
    intended_divergence: false
    divergence_rationale: null
    gaps:
      - "Sudden-death is modeled as a Race-local latch (SuddenDeath) gated on the RaceCompleting state; the shared OverTimeManager deliberately excludes the race/qualifying branch, so the standard overtime-window timer does not gate race end (intentional split, documented in OverTimeManager.cs)."
    verification:
      - { kind: test, ref: "RaceOvertimePenaltyTests.Overtime_LapLimitWithUnfinished_StartsSuddenDeath", result: pass }
      - { kind: code, ref: "FinishRacer->StartCompleting (RaceCompleting latch + abandon dead racers) + OnDeath->AbandonRaceCheck; CheckWinningCondition gates sudden-death on RaceCompleting. Live via GameWorld.cs:2346 (race.Tick(); race.CheckWinningCondition()).", result: pass }
    confidence: high
    notes: "FIXED in Wave-2 (was partial). The global race_completing latch is now modeled: the FIRST racer to reach the lap limit trips StartCompleting (abandoning every dead racer + INFO_RACE_ABANDONED), and a dead racer respawning into a completing race is auto-abandoned via OnDeath->AbandonRaceCheck. This is the multi-racer drain-to-(n==c) state machine the baseline VERIFIER flagged as absent."

  - id: race.team.laps
    name: "Team race: members' laps add up into ST_RACE_LAPS"
    layer: [authority]
    base_refs:
      - "common/gametypes/gametype/race/sv_race.qc:rc_SetLimits"
      - "server/race.qc:race_SendTime"
    port_refs:
      - "src/XonoticGodot.Common/Gameplay/GameTypes/Race.cs:RaceTeams"
      - "src/XonoticGodot.Common/Gameplay/GameTypes/Race.cs:CrossCheckpoint"
    constants:
      - { name: "team count clamp", base: "bound(2,n,4)", port: "Clamp(n,2,4)", match: true }
    status:
      logic: faithful
      values: faithful
      timing: na
      presentation: na
      audio: na
      liveness: live
    intended_divergence: false
    divergence_rationale: null
    gaps: []
    verification:
      - { kind: test, ref: "RaceOvertimePenaltyTests.TeamRace_LapsAddUpToTeam", result: pass }
    confidence: high
    notes: "Team-lap accumulation into the secondary team slot (ST_RACE_LAPS) tested."

  - id: race.qualifying.mode
    name: "Qualifying solo time-trial (rank by fastest, retract after each lap)"
    layer: [authority]
    base_refs:
      - "common/gametypes/gametype/race/sv_race.qc:rc_SetLimits"
      - "server/race.qc:race_RetractPlayer"
    port_refs:
      - "src/XonoticGodot.Common/Gameplay/GameTypes/Race.cs:Qualifying"
      - "src/XonoticGodot.Common/Gameplay/GameTypes/Race.cs:RetractPlayer"
      - "src/XonoticGodot.Server/GameWorld.cs:1972-1979 (race.Activate + OnFinishRetract = Clients.Spawn)"
    constants: []
    status:
      logic: partial
      values: partial
      timing: partial
      presentation: na
      audio: na
      liveness: partial
    intended_divergence: false
    divergence_rationale: null
    gaps:
      - "Retract uses Clients.Spawn (a normal spawn), not the Base checkpoint/grid-aware respawn (race_RetractPlayer + spawn_evalfunc by race_place)."
      - "independent_players / MAKE_INDEPENDENT_PLAYER (solo scoring isolation) not modeled."
      - "Qualifying checkpoint records + RACE_NET_CHECKPOINT_*_QUALIFYING packets absent."
    verification:
      - { kind: code, ref: "Race.Qualifying + OnFinishRetract wiring (GameWorld.cs:1975)", result: partial }
    confidence: medium
    notes: "Unchanged by Wave-2. Solo retract-to-start works; the qualifying-specific networking, isolation and grid spawn do not."

  - id: race.qualifying.transition
    name: "Qualifying-then-race transition (g_race_qualifying==2 -> 0)"
    layer: [authority]
    base_refs:
      - "common/gametypes/gametype/race/sv_race.qc:reset_map_global"
      - "common/gametypes/gametype/race/sv_race.qc:rc_SetLimits"
    port_refs:
      - "src/XonoticGodot.Common/Gameplay/GameTypes/Race.cs:TransitionQualifyingToRace"
      - "src/XonoticGodot.Common/Gameplay/GameTypes/Race.cs:SaveRaceLimits"
    constants: []
    status:
      logic: faithful
      values: faithful
      timing: faithful
      presentation: na
      audio: na
      liveness: live
    intended_divergence: false
    divergence_rationale: null
    gaps: []
    verification:
      - { kind: code, ref: "GameWorld.cs:1986-1997 Race activation reads g_race_qualifying_timelimit(_override), derives g_race_qualifying=2 + SaveRaceLimits + replaces the active timelimit with the qualifying clock; RunCheckRulesWorld timelimit-elapsed branch (GameWorld.cs:2545) calls race.TransitionQualifyingToRace() + re-arms GameStartTime", result: pass }
    confidence: high
    notes: "FIXED again since 2026-06-22 (qualifying-timelimit source now wired). The full qualifying==2 chain is live: g_race_qualifying_timelimit(_override)>0 derives qualifying==2 and replaces the active timelimit with the qualifying clock at activation (GameWorld.cs:1986-1997, the prior audit's last open gap), and when that clock elapses TransitionQualifyingToRace converts the session to a plain race (clears g_race_qualifying, restores the stashed fraglimit/leadlimit/timelimit, re-declares score rules, resets every racer) instead of going to intermission."

  - id: race.spawn.grid_respawn
    name: "Spawn grid by race_place + respawn at last checkpoint"
    layer: [authority]
    base_refs:
      - "server/race.qc:trigger_race_checkpoint_spawn_evalfunc"
      - "server/race.qc:spawnfunc(info_player_race)"
      - "server/race.qc:race_RetractPlayer"
    port_refs:
      - "src/XonoticGodot.Common/Gameplay/GameTypes/Race.cs:ApplyRespawnSpot"
      - "src/XonoticGodot.Common/Gameplay/GameTypes/Race.cs:OnDeath (respawn-checkpoint resume)"
      - "src/XonoticGodot.Common/Gameplay/GameTypes/Race.cs:CheckpointTouch (RespawnSpot/RespawnCheckpoint update)"
      - "src/XonoticGodot.Server/GameWorld.cs:1979 (Clients.OnPreSpawn = p => race.ApplyRespawnSpot(p))"
      - "src/XonoticGodot.Common/Gameplay/Player/SpawnSystem.cs:332 (consumes Player.SpawnPointTarg)"
    constants: []
    status:
      logic: partial
      values: partial
      timing: na
      presentation: na
      audio: na
      liveness: partial
    intended_divergence: false
    divergence_rationale: null
    gaps:
      - "race_place START-GRID ordering still missing: racers do not get ordered start positions in a multi-racer race (no race_place read / info_player_race grid)."
      - "trigger_race_checkpoint_verify map-load validation (missing-spawn error) not performed."
    verification:
      - { kind: code, ref: "Race.ApplyRespawnSpot sets Player.SpawnPointTarg to the last-passed checkpoint; wired LIVE via ClientManager.OnPreSpawn (invoked ClientManager.cs:616 before SelectSpawnPoint) → SpawnSystem.cs:332 short-circuits to the forced spot; OnDeath resumes NextCheckpoint after the respawn CP", result: partial }
    confidence: high
    notes: "FIXED (partial) since baseline — the prior audit marked this fully dead/missing. The respawn-at-last-checkpoint half (Base race_respawn_checkpoint / race_respawn_spotref / SPAWN_PRIO_RACE_PREVIOUS_SPAWN) is now implemented and genuinely live: crossing a CP in order re-points RespawnSpot, a death resumes from that CP (or clears the lap at the start line), and the host forces the next spawn there via SpawnPointTarg. Still open: the race_place start-grid ordering and the map-load checkpoint-verify."

  - id: race.movement.quantization
    name: "Force-keyboard movement quantization + race_movetime + FixClientCvars"
    layer: [authority, shared]
    base_refs:
      - "common/gametypes/gametype/race/sv_race.qc:PlayerPhysics"
      - "common/gametypes/gametype/race/sv_race.qc:FixClientCvars"
    port_refs: []
    constants:
      - { name: "M_SQRT1_2 diagonal snap", base: "0.70710678 (45 deg octant)", port: "none", match: false }
      - { name: "cl_movecliptokeyboard", base: "stuffed to 2", port: "none", match: false }
    status:
      logic: missing
      values: missing
      timing: missing
      presentation: na
      audio: na
      liveness: na
    intended_divergence: false
    divergence_rationale: null
    gaps:
      - "Analog (joystick) move input is not snapped to keyboard octants -> an analog precision advantage Base removes for fairness."
      - "race_movetime per-frame fractional accumulator absent (lap timing uses wall clock instead)."
      - "cl_movecliptokeyboard 2 is not stuffed to clients."
    verification:
      - { kind: code, ref: "Race.cs registers no PlayerPhysics hook (only Tick handles the penalty freeze); grep M_SQRT1_2 / movecliptokeyboard / race_movetime hits only Cts.OnPlayerPhysics, a sibling GameType — not shared with Race", result: fail }
    confidence: high
    notes: "Still MISSING for Race (re-confirmed 2026-06-25). NOTE: the identical M_SQRT1_2 octant-snap quantization IS implemented for the sibling CTS gametype (Cts.OnPlayerPhysics, registered Cts.cs:192) — but Race : GameType and Cts : GameType are sealed siblings with no shared physics hook, so Race itself does not quantize. cl_movecliptokeyboard 2 stuffcmd + race_movetime accumulator absent for both. The spec doc-comment in Race.cs explicitly marks this deferred."

  - id: race.hud.split_timer
    name: "Race split timer HUD panel (#8): running clock, splits, anticipation, medals"
    layer: [presentation]
    base_refs:
      - "client/hud/panel/racetimer.qc:HUD_RaceTimer"
      - "client/hud/panel/racetimer.qc:MakeRaceString"
      - "common/gametypes/gametype/race/cl_race.qc:HUD_Mod_Race"
      - "client/main.qc:NET_HANDLE(TE_CSQC_RACE)"
    port_refs:
      - "game/hud/RaceTimerPanel.cs:DrawPanel"
      - "game/hud/RaceTimerPanel.cs:MakeRaceString"
    constants:
      - { name: "TIME_DECIMALS", base: "2", port: "DeltaFmt 0.00", match: true }
      - { name: "panel aspect", base: "4:1", port: "AspectInner 4:1", match: true }
      - { name: "split fade window", base: "2 s", port: "2 s", match: true }
    status:
      logic: faithful
      values: faithful
      timing: faithful
      presentation: faithful
      audio: na
      liveness: live
    intended_divergence: false
    divergence_rationale: null
    gaps:
      - "On a pure (remote) client the feed is still absent — there is no TE_CSQC_RACE net message; the feed is host-side (NetGame.UpdateRacePanels reads the live Race gametype's RaceState + per-checkpoint record store). On a listen server the timer/splits/anticipation/speed/medal all render live."
    verification:
      - { kind: code, ref: "NetGame.UpdateRacePanels feeds RaceCheckpoint/RaceTime/RaceCheckpointTime/RacePreviousBestTime/RaceNextBestTime/RaceCheckpointSpeed/RaceStatus from Race.GetState + Race.CheckpointRecord* (server-side per-checkpoint records added in Race.RecordCheckpointSplit)", result: pass }
    confidence: high
    notes: "FIXED in Wave-5 (was dead). The server Race gametype now keeps per-checkpoint split records (RecordCheckpointSplit, QC race_checkpoint_records[]) and the host feeds the split/anticipation/speed + the record-result medal flash (race_status 0/1/2/3) into the panel each frame. Rendering (MakeRaceString + medal art) was already faithful."

  - id: race.hud.checkpoint_splits
    name: "Checkpoints split list panel (#27)"
    layer: [presentation]
    base_refs:
      - "client/hud/panel/checkpoints.qc:HUD_Checkpoints"
      - "client/hud/panel/racetimer.qc:StoreCheckpointSplits"
    port_refs:
      - "game/hud/CheckpointsPanel.cs:DrawPanel"
      - "game/hud/CheckpointsPanel.cs:StoreSplit"
    constants:
      - { name: "CHECKPOINTS_BASE_SIZE", base: "0.75", port: "0.75", match: true }
      - { name: "CHECKPOINTS_SPACING", base: "0.25", port: "0.25", match: true }
    status:
      logic: faithful
      values: faithful
      timing: na
      presentation: faithful
      audio: na
      liveness: live
    intended_divergence: false
    divergence_rationale: null
    gaps:
      - "Host-side feed only (no TE_CSQC_RACE net path for a pure client). On a listen server StoreSplit is now called per crossing (QC StoreCheckpointSplits) and ClearSplits on a fresh lap, so the panel renders the live split list."
    verification:
      - { kind: code, ref: "NetGame.UpdateRacePanels calls Checkpoints.StoreSplit(cp, label, delta) on each new crossing + ClearSplits on a fresh lap, deduped by the crossing stamp", result: pass }
    confidence: high
    notes: "FIXED in Wave-5 (was dead). StoreSplit is now fed from the live race path (the per-checkpoint record delta), once per crossing; ClearSplits fires on a lap restart. Word-wrap + flip/align/fontscale rendering was already faithful."

  - id: race.bot.role
    name: "Bot race role (navigate the checkpoint track)"
    layer: [authority]
    base_refs:
      - "common/gametypes/gametype/race/sv_race.qc:havocbot_role_race"
    port_refs: []
    constants: []
    status:
      logic: missing
      values: missing
      timing: na
      presentation: na
      audio: na
      liveness: na
    intended_divergence: false
    divergence_rationale: null
    gaps:
      - "Bots have no race role; they do not navigate toward the next checkpoint, so a bot-filled race has idle/wandering bots."
    verification:
      - { kind: code, ref: "grep havocbot_role_race / RaceRole in Server/Bot — none", result: fail }
    confidence: high
    notes: "Unchanged by Wave-2. Race waypoint extension (.race) is referenced in GameWorld bot waypoint load, but no per-bot race goal-rating role exists."

  - id: race.hud.mod_icon_rankings
    name: "Race/CTS mod-icon (PB + server best + medals) and Rankings scoreboard column"
    layer: [presentation]
    base_refs:
      - "common/gametypes/gametype/race/cl_race.qc:HUD_Mod_Race"
      - "server/race.qc:race_send_recordtime"
      - "server/race.qc:getrankings"
    port_refs:
      - "game/net/ServerNet.cs:1943-1964 (snapshot RaceRecords top-15 → _scoreRankings)"
      - "src/XonoticGodot.Net/ScoreboardBlock.cs:114-160 (Serialize/Deserialize rankings block)"
      - "game/net/NetGame.cs:3433 (_scoreboard.SetRankings(sb.Rankings))"
      - "game/hud/ScoreboardPanel.cs:SetRankings / DrawRankings"
    constants:
      - { name: "RACE_NET_SERVER_STATUS medal fade", base: "5 s", port: "none (mod-icon medal not ported)", match: false }
      - { name: "rankings display cap", base: "RANKINGS_DISPLAY_CNT", port: "15 (ServerNet displayCnt)", match: false }
    status:
      logic: partial
      values: partial
      timing: na
      presentation: partial
      audio: na
      liveness: partial
    intended_divergence: false
    divergence_rationale: null
    gaps:
      - "The HUD_Mod_Race MOD-ICON (the floating personal-best + server-best icon + the new-record/new-rank/fail medal flash with rank ordinal, faded over 5 s) is still NOT ported — ModIconsPanel has no Race case. (Note: the split-timer panel #8 has its OWN internal medal flash, fed via NetGame.UpdateRacePanels; that is race.hud.split_timer, not this mod-icon.)"
      - "The rankings DISPLAY cap is a hardcoded 15 rather than Base's RANKINGS_DISPLAY_CNT cvar; ReadName/ReadTime are read straight from the persistent RaceRecords DB (no separate networked record-name stream)."
    verification:
      - { kind: code, ref: "ServerNet.UpdateScoreboardRows fills _scoreRankings from RaceRecords.ReadTime/ReadName for gt rc|cts; ScoreboardBlock.Serialize writes the rankings block on every score-version snapshot (ServerNet.cs:1546); NetGame feeds ScoreboardPanel.SetRankings(sb.Rankings) — works for a PURE remote client", result: partial }
    confidence: high
    notes: "FIXED (partial) since baseline — the prior audit marked this entirely missing/dead. The RANKINGS scoreboard column half is now genuinely networked + live: the server snapshots the per-map RaceRecords top-15 (best-first TIME_ENCODE'd time + holder name) into the scoreboard wire, and the client renders it via the real net path (no longer the unrelated ScorePanel standings). STILL OPEN: the HUD_Mod_Race floating mod-icon itself (PB/server-best icon + ordinal medal flash) has no port code."

  - id: race.speed_awards
    name: "Speed awards (round-best + all-time-best speed at checkpoint)"
    layer: [authority, presentation]
    base_refs:
      - "common/gametypes/gametype/race/sv_race.qc:GetPressedKeys (race_SpeedAwardFrame)"
      - "server/race.qc:race_SpeedAwardFrame"
    port_refs: []
    constants: []
    status:
      logic: missing
      values: missing
      timing: missing
      presentation: missing
      audio: na
      liveness: na
    intended_divergence: false
    divergence_rationale: null
    gaps:
      - "race_SpeedAwardFrame (per-frame top-speed tracking, round + all-time speed awards broadcast to clients, RaceTimerPanel speed-at-CP readout baseline) is not ported. RaceTimerPanel has RaceCheckpointSpeed/BestSpeed fields but they are never fed."
    verification:
      - { kind: code, ref: "grep SpeedAward / race speed award in src+game — none", result: fail }
    confidence: high
    notes: "Unchanged by Wave-2. The speed readout in RaceTimerPanel (cl_race_cptimes_showspeed) is part of this and is dead for lack of a feed."
