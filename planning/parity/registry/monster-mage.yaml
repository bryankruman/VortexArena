unit: monster-mage
category: monster
base_ref_rev: "v0.8.6-1779-g863cd3e84"
last_audited: "2026-06-25"
summary:
  feature_count: 10
  worst_gaps:
    - "CSQC spike projectile model (PROJECTILE_MAGE_SPIKE) still not wired — spike has no client-visible model/trail."
    - "Send_Effect particles for push (TE_EXPLOSION), spike (EXPLOSION_SMALL), teleport (SPAWN), and heal (HEALING/AMMO_REGEN/ARMOR_REPAIR) still not emitted (CSQC presentation)."
    - "mr_death random die1/die2 anim pick and mr_anim concrete frame groups (shoot/melee/duckjump/die) still collapsed into the shared logical-phase enum."
    - "RandomRelocate / mr_think need_help scans remain behavioral approximations of the QC builtins (not bit-exact)."
features:
  - id: monster-mage.spawn.setup
    name: "Spawn + mr_setup (health, speeds, loot, size, attackfunc)"
    layer: [authority]
    base_refs:
      - "common/monsters/monster/mage.qc:spawnfunc(monster_mage)"
      - "common/monsters/monster/mage.qc:METHOD(Mage, mr_setup)"
      - "common/monsters/monster/mage.qh:CLASS(Mage)"
    port_refs:
      - "src/XonoticGodot.Common/Gameplay/Monsters/Mage.cs:Spawn"
      - "src/XonoticGodot.Common/Gameplay/Monsters/MonsterSpawnFuncs.cs:Mage"
      - "src/XonoticGodot.Common/Gameplay/MapObjects/MapObjectsRegistry.cs:198"
    constants:
      - { name: "g_monster_mage_health", base: "400", port: "400 (cfg)", match: true }
      - { name: "g_monster_mage_speed_walk", base: "250", port: "250 (cfg)", match: true }
      - { name: "g_monster_mage_speed_run", base: "400", port: "400 (cfg)", match: true }
      - { name: "g_monster_mage_speed_stop", base: "50", port: "50 (cfg)", match: true }
      - { name: "g_monster_mage_damageforcescale", base: "0.5", port: "0.5 (cfg)", match: true }
      - { name: "g_monster_mage_loot", base: "health_big", port: "health_big (cfg)", match: true }
      - { name: "m_mins", base: "-16 -16 -24", port: "-16 -16 -24", match: true }
      - { name: "m_maxs", base: "16 16 55", port: "16 16 55", match: true }
    status:
      logic: faithful
      values: faithful
      timing: faithful
      presentation: na
      audio: faithful
      liveness: live
    intended_divergence: false
    divergence_rationale: null
    gaps: []
    verification:
      - { kind: code, ref: "MapObjectsRegistry.cs:198 -> MonsterSpawnFuncs.Mage -> Setup -> MonsterSound(spawn) at MonsterAI.cs:312; e.Think wired -> SimulationLoop.RunThink", result: pass }
      - { kind: value-diff, ref: "assets/data/xonotic-data.pk3dir/monsters.cfg g_monster_mage_* == Base monsters.cfg (line-for-line, verified)", result: pass }
    confidence: high
    notes: "Setup values flow through MonsterAI.Cvar from the shipped monsters.cfg, which matches Base exactly. Skin randomized 0..4 like QC; heal switch handles 0/1/2 (skins 3/4 = no per-skin heal, same as Base). Wave-2 added MonsterSound(spawn) at Setup (QC Monster_Spawn_Setup:1364 Monster_Sound(monstersound_spawn) on CH_VOICE) — now audio:faithful for the spawn cue."

  - id: monster-mage.attack.select
    name: "Attack selection (melee push vs ranged teleport/spike)"
    layer: [authority]
    base_refs:
      - "common/monsters/monster/mage.qc:M_Mage_Attack"
    port_refs:
      - "src/XonoticGodot.Common/Gameplay/Monsters/Mage.cs:Attack"
      - "src/XonoticGodot.Common/Gameplay/Monsters/MonsterAI.cs:AttackCheck"
    constants:
      - { name: "g_monster_mage_attack_push_chance", base: "0.7", port: "0.7 (cfg)", match: true }
      - { name: "g_monster_mage_attack_spike_chance", base: "0.45", port: "0.45 (cfg)", match: true }
      - { name: "g_monster_mage_attack_teleport_chance", base: "0.1 (cfg; inline 0.2)", port: "0.1 (cfg); 0.1 inline-fallback", match: true }
      - { name: "g_monsters_attack_range", base: "120", port: "120 (cfg)", match: true }
    status:
      logic: faithful
      values: faithful
      timing: faithful
      presentation: na
      audio: na
      liveness: live
    intended_divergence: true
    divergence_rationale: "Port re-derives melee/ranged from enemy distance inside Mage.Attack and calls Push/Teleport/FireSpike directly, rather than routing through WEP_MAGE_SPIKE.wr_think / OFFHAND_MAGE_TELEPORT.offhand_think. The weapon-entity wrapper is not modeled; the branch chances and gating are behaviorally equivalent."
    gaps: []
    verification:
      - { kind: code, ref: "MonsterAI.AttackCheck:980 (st.Def.Attack) -> Mage.Attack:98 distance split (<=AttackRange push 0.7; else teleport 0.1 / spike !ActiveSpike 0.45)", result: pass }
    confidence: high
    notes: "Wave-2 corrected the inline TeleportChance fallback 0.2->0.1 (now matches cfg). Mage.Attack re-checks st.AttackFinished at its top. Per-attack cooldowns set inside each sub-attack. Spike one-at-a-time via st.ActiveSpike (QC .mage_spike)."

  - id: monster-mage.attack.spike
    name: "Homing electric spike (one in flight, seeker guidance)"
    layer: [authority]
    base_refs:
      - "common/monsters/monster/mage.qc:M_Mage_Attack_Spike"
      - "common/monsters/monster/mage.qc:M_Mage_Attack_Spike_Think"
      - "common/monsters/monster/mage.qc:M_Mage_Attack_Spike_Explode"
    port_refs:
      - "src/XonoticGodot.Common/Gameplay/Monsters/Mage.cs:FireSpike"
      - "src/XonoticGodot.Common/Gameplay/Monsters/MonsterFramework.cs:HomeProjectile"
      - "src/XonoticGodot.Common/Gameplay/Monsters/MonsterAI.cs:SpawnProjectile"
    constants:
      - { name: "g_monster_mage_attack_spike_damage", base: "45", port: "45 (cfg)", match: true }
      - { name: "g_monster_mage_attack_spike_radius", base: "60", port: "60 (cfg)", match: true }
      - { name: "g_monster_mage_attack_spike_delay", base: "2", port: "2 (cfg)", match: true }
      - { name: "g_monster_mage_attack_spike_speed_max", base: "370", port: "370 (cfg)", match: true }
      - { name: "g_monster_mage_attack_spike_accel", base: "480", port: "480 (cfg); 480 inline", match: true }
      - { name: "g_monster_mage_attack_spike_decel", base: "480", port: "480 (cfg); 480 inline", match: true }
      - { name: "g_monster_mage_attack_spike_turnrate", base: "0.65", port: "0.65 (cfg); 0.65 inline", match: true }
      - { name: "g_monster_mage_attack_spike_smart", base: "1", port: "1 (cfg); 1 inline", match: true }
      - { name: "g_monster_mage_attack_spike_smart_mindist", base: "600", port: "600 (cfg); 600 inline", match: true }
      - { name: "g_monster_mage_attack_spike_smart_trace_min", base: "1000", port: "1000 (cfg); 1000 inline", match: true }
      - { name: "g_monster_mage_attack_spike_smart_trace_max", base: "2500", port: "2500 (cfg); 2500 inline", match: true }
      - { name: "launch_speed", base: "400", port: "400", match: true }
      - { name: "ltime", base: "time+7", port: "Now+7", match: true }
      - { name: "avelocity", base: "300 300 300", port: "300 300 300", match: true }
      - { name: "edge_damage", base: "spike_damage*0.5", port: "spike_damage*0.5", match: true }
      - { name: "muzzle_origin", base: "origin + v_forward*14 + '0 0 30' + v_right*-14", port: "origin + fwd*14 + '0 0 30' + right*-14 (FireSpike override of SpawnProjectile)", match: true }
    status:
      logic: faithful
      values: faithful
      timing: faithful
      presentation: missing
      audio: faithful
      liveness: live
    intended_divergence: false
    divergence_rationale: null
    gaps:
      - "CSQC spike projectile model (PROJECTILE_MAGE_SPIKE) still not wired — spike has no client-visible model/trail in flight."
      - "Send_Effect(EFFECT_EXPLOSION_SMALL) on detonation (mage.qc:133) not emitted (CSQC particle)."
    verification:
      - { kind: code, ref: "FireSpike:142 -> SpawnProjectile (think NextThink=Now) -> HomeProjectile per-frame; Explode (MonsterAI.cs:1311) RadiusDamage + onExplode (grenade_impact) + clears ActiveSpike", result: pass }
      - { kind: value-diff, ref: "Mage.cs inline fields (accel 480/decel 480/turnrate 0.65/smart 1/mindist 600/trace 1000/2500) == monsters.cfg == Base", result: pass }
      - { kind: code, ref: "FireSpike:160-161 AngleVectors(e.Angles) muzzle override == M_Mage_Attack_Spike:229 origin+v_forward*14+'0 0 30'+v_right*-14", result: pass }
    confidence: high
    notes: "FIXED in Wave-2: (1) all inline C# constant defaults now match Base (was accel 800/decel 1000/turnrate 0.6/smart 0/mindist 256/trace 100/500); the dangerous smart-default-0 (would have disabled obstacle avoidance) is now 1. (2) Spike muzzle origin now re-derives the +30 up / -14 right offsets via AngleVectors after the shared spawner placed it. (3) Spike impact sound SND_MageSpike_IMPACT (weapons/grenade_impact.wav on CH_SHOTS) now played via the onExplode hook (SpawnProjectile.Explode invokes onExplode at MonsterAI.cs:1320). FIRE sound (electro_fire) still played in FireSpike. Homing math faithful; one-at-a-time via st.ActiveSpike."

  - id: monster-mage.attack.push
    name: "Explosive close-range push (AoE shove)"
    layer: [authority]
    base_refs:
      - "common/monsters/monster/mage.qc:M_Mage_Attack_Push"
    port_refs:
      - "src/XonoticGodot.Common/Gameplay/Monsters/Mage.cs:Push"
    constants:
      - { name: "g_monster_mage_attack_push_damage", base: "25", port: "25 (cfg)", match: true }
      - { name: "g_monster_mage_attack_push_radius", base: "150", port: "150 (cfg)", match: true }
      - { name: "g_monster_mage_attack_push_force", base: "300", port: "300 (cfg)", match: true }
      - { name: "g_monster_mage_attack_push_delay", base: "1", port: "1 (cfg)", match: true }
      - { name: "edge_damage", base: "push_damage (no falloff)", port: "PushDamage (no falloff)", match: true }
    status:
      logic: faithful
      values: faithful
      timing: faithful
      presentation: missing
      audio: faithful
      liveness: live
    intended_divergence: false
    divergence_rationale: null
    gaps:
      - "Send_Effect(EFFECT_TE_EXPLOSION) particle on push not emitted by the port (CSQC)."
    verification:
      - { kind: code, ref: "Push:170 -> WeaponSplash.RadiusDamage(PushDamage, PushDamage, PushRadius, force=PushForce, DeathTypes.MonsterMage), weapons/tagexp1.wav sound", result: pass }
    confidence: high
    notes: "Unchanged by Wave-2 (already faithful). Both core and edge damage = push_damage (full damage at radius edge, matching QC). Push sound (tagexp1) played. Freezes movement via state=AttackMelee, sets anim/cooldown."

  - id: monster-mage.attack.teleport
    name: "Teleport behind / random-relocate near the target"
    layer: [authority]
    base_refs:
      - "common/monsters/monster/mage.qc:M_Mage_Attack_Teleport"
    port_refs:
      - "src/XonoticGodot.Common/Gameplay/Monsters/Mage.cs:Teleport"
      - "src/XonoticGodot.Common/Gameplay/Monsters/Mage.cs:RandomRelocate"
    constants:
      - { name: "g_monster_mage_attack_teleport_delay", base: "5 (cfg; inline 2)", port: "5 (cfg); 5 inline", match: true }
      - { name: "g_monster_mage_attack_teleport_random", base: "0.4", port: "0.4 (cfg); 0.4 inline", match: true }
      - { name: "g_monster_mage_attack_teleport_random_range", base: "1200", port: "1200 (cfg)", match: true }
      - { name: "behind_trace_dist", base: "v_forward * -200", port: "-200 along target forward", match: true }
    status:
      logic: faithful
      values: faithful
      timing: faithful
      presentation: missing
      audio: faithful
      liveness: live
    intended_divergence: false
    divergence_rationale: null
    gaps:
      - "Send_Effect(EFFECT_SPAWN) at both old and new origin (mage.qc:333-334,351-352) not emitted (CSQC)."
      - "RandomRelocate is an approximation of QC's MoveToRandomLocationWithinBounds builtin: 32 biased samples + ground-drop + bbox-fit test, not the engine's content-mask sampler (maxtries 256, jitter/dist params). Behaviorally equivalent (place at a random clear grounded spot within +/-1200) but not bit-exact."
    verification:
      - { kind: code, ref: "Teleport:186 random branch (RandomRelocate, chance teleport_random) + behind-blink branch (range gate, OnGround, tracebox backward NoMonsters, face target, velocity*=0.5, cooldown) == M_Mage_Attack_Teleport both halves", result: pass }
      - { kind: code, ref: "Mage.cs:114 Attack -> Teleport on TeleportChance; live on the real attack path", result: pass }
    confidence: high
    notes: "FIXED in Wave-2: (1) the random-relocation branch (teleport_random 0.4: blink to a random clear grounded spot within a +/-1200 box) is now ported as RandomRelocate — was previously absent. (2) inline TeleportDelay 2->5 and TeleportChance now read cfg. (3) the wrongly-invented monsters/mage_sight.wav voice on teleport was removed (QC plays no sound on teleport). Only the CSQC EFFECT_SPAWN particle remains missing."

  - id: monster-mage.defend.shield
    name: "Self damage-blocking shield"
    layer: [authority]
    base_refs:
      - "common/monsters/monster/mage.qc:M_Mage_Defend_Shield"
      - "common/monsters/sv_monsters.qc:Monster_Damage"
      - "common/util.qc:healtharmor_applydamage"
    port_refs:
      - "src/XonoticGodot.Common/Gameplay/Monsters/Mage.cs:RaiseShield"
      - "src/XonoticGodot.Common/Gameplay/Monsters/MonsterAI.cs:MarkPain"
      - "src/XonoticGodot.Common/Gameplay/Monsters/MonsterAI.cs:RunStatusTimers"
      - "src/XonoticGodot.Common/Gameplay/Monsters/MonsterAI.cs:MonsterEventDamage"
      - "src/XonoticGodot.Common/Gameplay/Monsters/MonsterFramework.cs:Shield"
    constants:
      - { name: "g_monster_mage_shield_time", base: "3", port: "3 (cfg)", match: true }
      - { name: "g_monster_mage_shield_delay", base: "7", port: "7 (cfg)", match: true }
      - { name: "g_monster_mage_shield_blockpercent", base: "0.8", port: "0.8 (cfg/Mage.cs:46)", match: true }
      - { name: "effective_damage_reduction", base: "~0.8% (damage*RES_ARMOR/100, RES_ARMOR=0.8)", port: "~0.8% (MonsterEventDamage:1700 ArmorValue/100, RaiseShield set ArmorValue=0.8) — single application, faithful", match: true }
    status:
      logic: faithful
      values: faithful
      timing: faithful
      presentation: na
      audio: faithful
      liveness: live
    intended_divergence: false
    divergence_rationale: null
    gaps: []
    verification:
      - { kind: code, ref: "MarkPain:1540-1593 — the old take*=(1-blockpercent) shield cut + the 0.9 fallback are GONE; MarkPain now subtracts `take` directly (self.Health -= take) with no shield-specific reduction", result: pass }
      - { kind: value-diff, ref: "MonsterEventDamage:1700-1704 is the SOLE armor handling: armorBlock = ArmorValue/100; save = clamp(damage*armorBlock,0,100); take = damage-save == Monster_Damage:1091 healtharmor_applydamage(100, RES_ARMOR/100). RaiseShield ArmorValue=0.8 => ~0.8% block, single application", result: pass }
      - { kind: code, ref: "RaiseShield:361-375 sets ArmorValue=blockpercent(cfg 0.8); RunStatusTimers:1255-1262 restores ShieldRestoreArmor + removes Shield at ShieldExpire", result: pass }
    confidence: high
    notes: "FIXED since the 2026-06-22 baseline: the #1 worst gap (double-counted ~80% MarkPain shield cut + hardcoded 0.9 fallback) is fully resolved. MarkPain no longer references blockpercent or STATUSEFFECT_Shield at all — the entire `take*=(1-blockpercent)` block and its 0.9 fallback were deleted. The shield now reduces damage exactly once, via the faithful ArmorValue/100 armor split in MonsterEventDamage (RaiseShield sets ArmorValue to the cfg 0.8 => ~0.8% reduction, Base-faithful, near-cosmetic by design). Baseline monster armor (bound(0.2,0.5*SKILLMOD,0.9), Setup:255) is modeled; shield duration/cooldown and armor bump-then-restore are timing-faithful; no invented sound (audio faithful)."

  - id: monster-mage.defend.heal
    name: "Radius heal of self/allies (skin variants: health/ammo/armor)"
    layer: [authority]
    base_refs:
      - "common/monsters/monster/mage.qc:M_Mage_Defend_Heal"
      - "common/monsters/monster/mage.qc:M_Mage_Defend_Heal_Check"
    port_refs:
      - "src/XonoticGodot.Common/Gameplay/Monsters/Mage.cs:HealPulse"
      - "src/XonoticGodot.Common/Gameplay/Monsters/Mage.cs:HealCheck"
    constants:
      - { name: "g_monster_mage_heal_allies", base: "20", port: "20 (cfg)", match: true }
      - { name: "g_monster_mage_heal_range", base: "250", port: "250 (cfg)", match: true }
      - { name: "g_monster_mage_heal_delay", base: "1.5", port: "1.5 (cfg)", match: true }
      - { name: "ammo_topup", base: "cells 1 / rockets 1 / shells 2 / bullets 5", port: "cells 1 / rockets 1 / shells 2 / bullets 5", match: true }
      - { name: "self_heal_eligibility", base: "monster branch: RES_HEALTH < max_health (ALL skins)", port: "monster branch: Health < MaxHealth (ALL skins)", match: true }
    status:
      logic: faithful
      values: faithful
      timing: faithful
      presentation: missing
      audio: faithful
      liveness: live
    intended_divergence: false
    divergence_rationale: null
    gaps:
      - "Send_Effect(EFFECT_HEALING / AMMO_REGEN / ARMOR_REPAIR) particles per skin not emitted (CSQC)."
      - "Waypoint-sprite health update for healed ally monsters (WaypointSprite_UpdateHealth, mage.qc:280-281) not invoked in the port."
    verification:
      - { kind: code, ref: "HealPulse skin switch (player branch) + non-client branch GiveResourceWithLimit(Health,20,MaxHealth) vs M_Mage_Defend_Heal", result: pass }
      - { kind: code, ref: "HealCheck:308-315 now has NO self special-case: a mage (Flags&Monster, !Client) flows through the monster branch `Health < MaxHealth` == M_Mage_Defend_Heal_Check:103-104, so self-heals on EVERY skin toward 400", result: pass }
    confidence: high
    notes: "FIXED in Wave-2: the self-heal eligibility bug is gone. The old HealCheck special-cased targ==self to `skin==0 && health < regenstable(100)` (so skin 1/2 never self-healed, skin 0 stopped above 100 HP); that branch was removed, so a mage now flows through the !IS_PLAYER monster branch and self-heals toward max_health on all skins, matching Base. Also: the wrongly-invented monsters/mage_heal.wav voice was removed (QC plays no dedicated heal sound -> audio faithful). Player skin 0/1/2 branches and per-resource caps match Base. Only CSQC particles + ally waypoint-health remain."

  - id: monster-mage.think.decide
    name: "Per-think heal/shield decision (mr_think)"
    layer: [authority]
    base_refs:
      - "common/monsters/monster/mage.qc:METHOD(Mage, mr_think)"
    port_refs:
      - "src/XonoticGodot.Common/Gameplay/Monsters/Mage.cs:Think"
      - "src/XonoticGodot.Common/Gameplay/Monsters/Mage.cs:NeedsHelpNearby"
    constants:
      - { name: "g_monster_mage_heal_minhealth", base: "250", port: "250 (cfg)", match: true }
      - { name: "heal_random_gate", base: "random()<0.5", port: "MonsterRandom.Next()<0.5", match: true }
      - { name: "shield_random_gate", base: "random()<0.5", port: "MonsterRandom.Next()<0.5", match: true }
    status:
      logic: faithful
      values: faithful
      timing: faithful
      presentation: na
      audio: na
      liveness: live
    intended_divergence: false
    divergence_rationale: null
    gaps:
      - "QC mr_think scans BOTH clients (FOREACH_CLIENT) and monsters (IL_EACH g_monsters) for need_help; port NeedsHelpNearby uses one FindInRadius pass over all entities in heal_range, which conflates the two scans but covers both populations."
    verification:
      - { kind: code, ref: "Mage.Think:75 heal gate (rand<0.5 && Now>=AttackFinished && (health<minhealth || NeedsHelpNearby)) and shield gate (rand<0.5 && enemy && Now>=ShieldDelay && health<MaxHealth && !Shield) vs mr_think", result: pass }
    confidence: high
    notes: "Heal trigger and shield trigger conditions match mr_think exactly. RunThink then runs the shared chase/attack. NeedsHelpNearby reuses HealCheck (which is now self-heal-correct; it explicitly skips self via `if (it == e) continue`)."

  - id: monster-mage.death.pain
    name: "Pain reaction + death anim (mr_pain / mr_death)"
    layer: [authority]
    base_refs:
      - "common/monsters/monster/mage.qc:METHOD(Mage, mr_pain)"
      - "common/monsters/monster/mage.qc:METHOD(Mage, mr_death)"
    port_refs:
      - "src/XonoticGodot.Common/Gameplay/Monsters/MonsterAI.cs:MarkPain"
      - "src/XonoticGodot.Common/Gameplay/Monsters/MonsterAI.cs:MarkDead"
    constants:
      - { name: "mr_pain_return", base: "damage_take (unchanged)", port: "shared MarkPain (no mage override)", match: true }
      - { name: "die_anim_pick", base: "random()>0.5 ? die2 : die1", port: "shared MonsterAnim.Death", match: false }
      - { name: "pain_sound_throttle", base: "Monster_Sound(monstersound_pain, 1.2, true)", port: "MonsterSound(pain, 1.2, true)", match: true }
    status:
      logic: faithful
      values: faithful
      timing: faithful
      presentation: partial
      audio: faithful
      liveness: live
    intended_divergence: false
    divergence_rationale: null
    gaps:
      - "mr_death random die1/die2 anim selection is not modeled per-mage (the port uses the shared death-anim phase MonsterAnim.Death, no mage-specific 50/50 die-anim pick)."
    verification:
      - { kind: code, ref: "mr_pain returns take unchanged (matches shared MarkPain passthrough); no mage-specific override in Mage.cs", result: pass }
      - { kind: code, ref: "MarkPain:1402 pain sound now routed through MonsterSound(pain, 1.2, true) == Monster_Damage:1101 1.2s delaytoo throttle", result: pass }
    confidence: high
    notes: "mr_pain is a passthrough in Base, so the shared MarkPain is faithful. Wave-2 routed the pain/death sounds through the new MonsterSound helper honouring the g_monsters_sounds gate + the 1.2s pain throttle (was an unthrottled direct Play). die1/die2 remains a presentation-only anim choice. Death sound via MarkDead (monsters/<netname>_death.wav)."

  - id: monster-mage.anim.frames
    name: "Animation frame groups (mr_anim)"
    layer: [shared, presentation]
    base_refs:
      - "common/monsters/monster/mage.qc:METHOD(Mage, mr_anim)"
    port_refs:
      - "src/XonoticGodot.Common/Gameplay/Monsters/MonsterAI.cs:MonsterAnim"
    constants:
      - { name: "anim_shoot", base: "2 1 5", port: "logical MonsterAnim.Attack (no frame group)", match: false }
      - { name: "anim_melee", base: "5 1 5", port: "logical MonsterAnim.Attack", match: false }
      - { name: "anim_duckjump", base: "4 1 5", port: "logical MonsterAnim.Attack", match: false }
      - { name: "anim_die1/die2", base: "9/10 1 0.5", port: "logical MonsterAnim.Death", match: false }
    status:
      logic: faithful
      values: missing
      timing: unknown
      presentation: missing
      audio: na
      liveness: partial
    intended_divergence: false
    divergence_rationale: null
    gaps:
      - "Port tracks only logical anim PHASES (MonsterAnim enum) in st.Anim for timing/gating; the concrete nanomage.dpm frame-group playback (idle/walk/run/shoot/duckjump/melee/pain/die frame ranges from mr_anim) is not wired to a real animation driver."
    verification:
      - { kind: code, ref: "MonsterAI.MonsterAnim enum (Idle/Walk/Run/Attack/Pain/Death) vs mr_anim animfixfps frame groups — port collapses shoot/melee/duckjump into one Attack phase", result: fail }
    confidence: medium
    notes: "Unchanged by Wave-2. mr_anim is GAMEQC (shared client+server) for frame-group setup; the port collapses distinct shoot/melee/duckjump frames into one Attack phase. Whether the renderer plays the correct frame ranges is unverified."
