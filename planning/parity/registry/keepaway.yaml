unit: keepaway
category: gametype
base_ref_rev: "v0.8.6-1779-g863cd3e84"
last_audited: "2026-06-22"
summary:
  feature_count: 14
  worst_gaps:
    - "Carrier highspeed multiplier (g_keepaway_ballcarrier_highspeed) is still unimplemented (no PlayerPhysics hook; comment-only deferral in Keepaway.cs:51)."
    - "Ball/carrier WAYPOINT SPRITES + radar tracking (g_keepawayball_tracking) are still not spawned/attached (sprite TYPES are registered in the catalog, but nothing emits them on the ball or carrier and the cvar is never read)."
    - "Particles still missing: EFFECT_KA_BALL_RESPAWN at relocate and EFFECT_BALL_SPARKS on world-touch are not emitted (sounds for these events now play)."
    - "Three of four drop triggers (use-key / disconnect / observe) are still unwired; only the death-drop is live."
    - "Bot ka role's two material defects are now FIXED (FindBall queries 'keepawayball'; RoleKeepaway reads ball.GtCarrier) — it is live + finds/contests the ball; residual is the collapsed carrier/collector split + no Bot_ForbidAttack."
features:
  - id: keepaway.ball.spawn
    name: "Ball spawn at map start (single keepawayball)"
    layer: [authority]
    base_refs:
      - "common/gametypes/gametype/keepaway/sv_keepaway.qc:ka_SpawnBalls"
      - "common/gametypes/gametype/keepaway/sv_keepaway.qc:ka_Handler_CheckBall"
    port_refs:
      - "src/XonoticGodot.Common/Gameplay/GameTypes/Keepaway.cs:SpawnBall"
      - "src/XonoticGodot.Common/Gameplay/GameTypes/Keepaway.cs:AdoptBall"
      - "src/XonoticGodot.Common/Gameplay/GameTypes/BallEntity.cs:SpawnForGametype"
      - "src/XonoticGodot.Server/GameWorld.cs:637"
    constants:
      - { name: "g_keepawayball_count", base: "1", port: "1 (single ball only)", match: true }
      - { name: "ball_bbox", base: "+-24 24 24", port: "+-24 24 24 (BallConfig.WithKindDefaults KeepawayBall)", match: true }
      - { name: "g_keepawayball_damageforcescale", base: "2", port: "2 (BallConfig.DamageForceScale → Entity.DamageForceScale)", match: true }
      - { name: "g_keepawayball_trail_color", base: "254", port: "254 (BallConfig.TrailColor — client glow_trail still deferred)", match: true }
      - { name: "g_keepawayball_effects", base: "8 (EF_DIMLIGHT)", port: "8 (EfDimLight → Entity.Effects)", match: true }
    status:
      logic: faithful
      values: faithful
      timing: faithful
      presentation: partial
      audio: na
      liveness: live
    intended_divergence: false
    divergence_rationale: null
    gaps:
      - "SpawnBall is CALLED on the live match path — GameWorld.Boot (the NetGame match-start path) procedurally spawns the single ball at a RandomMapLocation right after the entity lump (GameWorld.cs:636-637 case Keepaway ka), replacing the missing QC ka_Handler_CheckBall driver."
      - "PRESENTATION residual: the EF_DIMLIGHT bit + glow_color/glow_trail are set as data on the edict, but the actual ball MODEL render + glow trail are client concerns not yet realized (model path models/orbs/orbblue.md3 is configured but the CSQC render of it is deferred)."
    verification:
      - { kind: codetrace, ref: "GameWorld.cs:636-637 case Keepaway ka: ka.SpawnBall(BallEntity.RandomMapLocation(...)); reached from the live Boot/map-load path after MapObjectsRegistry.RunPostSpawn", result: pass }
      - { kind: codetrace, ref: "BallConfig.WithKindDefaults(KeepawayBall) sets classname keepawayball, bbox +-24, EF_DIMLIGHT=8, trail 254, damageforcescale 2 — all == Base ka_SpawnBalls", result: pass }
    confidence: high
    notes: "Entity layer is reachable and wired through the shared BallEntity framework. The only residual is client model/glow presentation. Unchanged by Wave 5 (only line numbers shifted)."

  - id: keepaway.ball.respawn
    name: "Loose-ball relocate timer + fall-off/NEEDKILL respawn"
    layer: [authority]
    base_refs:
      - "common/gametypes/gametype/keepaway/sv_keepaway.qc:ka_RespawnBall"
      - "common/gametypes/gametype/keepaway/sv_keepaway.qc:ka_DamageEvent"
    port_refs:
      - "src/XonoticGodot.Common/Gameplay/GameTypes/Keepaway.cs:RespawnBall"
      - "src/XonoticGodot.Common/Gameplay/GameTypes/BallEntity.cs:Relocate"
      - "src/XonoticGodot.Common/Gameplay/GameTypes/BallEntity.cs:RandomMapLocation"
    constants:
      - { name: "g_keepawayball_respawntime", base: "10", port: "10 (RespawnTime fallback)", match: true }
      - { name: "respawn_velocity", base: "0 0 200", port: "0 0 200 (RespawnVelocity)", match: true }
    status:
      logic: partial
      values: faithful
      timing: faithful
      presentation: missing
      audio: faithful
      liveness: live
    intended_divergence: false
    divergence_rationale: null
    gaps:
      - "The relocate MOVES the ball — BallEntity.Relocate calls RandomMapLocation (a random spawnpoint, falling back to a jitter around home) + the '0 0 200' kick + re-arms NextThink; the loose-ball think (RespawnThink) is live via SpawnForGametype."
      - "SND_KA_RESPAWN plays at relocate (Keepaway.RespawnBall → SoundSystem.PlayOn KA_RESPAWN, ATTEN_NONE)."
      - "STILL OPEN (logic): RandomMapLocation uses the QC FALLBACK path (random info_player_* spawnpoint) only — it does NOT do the full MoveToRandomMapLocation world-bounds sampling with the SOLID/SLIME/LAVA/SKY content rejection. Functionally close (ball relocates to a reachable spawn) but not bit-identical placement."
      - "STILL OPEN (logic): no NOIMPACT fall-off respawn branch (ka_TouchEvent NOIMPACT) and no NEEDKILL ka_DamageEvent respawn path — a ball that takes a NEEDKILL hit or falls off the map is not force-respawned."
      - "STILL OPEN (presentation): no EFFECT_KA_BALL_RESPAWN particle at the old + new origin, and no WP_KaBall waypoint sprite spawn."
    verification:
      - { kind: codetrace, ref: "SpawnForGametype installs RespawnThink → Relocate; RespawnBall plays KA_RESPAWN; both reachable now the ball entity is live", result: pass }
      - { kind: codetrace, ref: "Grep ka_DamageEvent / NOIMPACT respawn — no port equivalent", result: fail }
    confidence: high
    notes: "Random relocation + respawn sound work; world-bounds sampling, NEEDKILL/fall-off respawn, and the respawn particle remain. Unchanged by Wave 5."

  - id: keepaway.ball.pickup
    name: "Ball pickup (attach to carrier, mark VIP, pickups++)"
    layer: [authority]
    base_refs:
      - "common/gametypes/gametype/keepaway/sv_keepaway.qc:ka_TouchEvent"
    port_refs:
      - "src/XonoticGodot.Common/Gameplay/GameTypes/Keepaway.cs:PickUp"
      - "src/XonoticGodot.Common/Gameplay/GameTypes/Keepaway.cs:BallTouchEntity"
      - "src/XonoticGodot.Common/Gameplay/GameTypes/BallEntity.cs:AttachToCarrier"
      - "src/XonoticGodot.Common/Gameplay/GameTypes/BallEntity.cs:IsRecaptureLocked"
    constants:
      - { name: "carried_scale", base: "12/16 = 0.75", port: "none (no entity scale field)", match: false }
      - { name: "self_recapture_lockout_wait", base: "0.5", port: "0.5 (BallEntity.SelfRecaptureLockout)", match: true }
    status:
      logic: faithful
      values: partial
      timing: faithful
      presentation: partial
      audio: faithful
      liveness: live
    intended_divergence: false
    divergence_rationale: null
    gaps:
      - "Pickup is reachable — the live engine trigger-touch pass (TriggerTouch.cs:42-46 dispatches Solid.Trigger entities' Touch=BallTouchEntity) fires BallTouchEntity, which calls PickUp; AttachToCarrier makes the ball SOLID_NOT + MOVETYPE_NONE + sets GtCarrier."
      - "SND_KA_PICKEDUP plays (PlayOn KA_PICKEDUP, ATTEN_NONE); INFO_KEEPAWAY_PICKUP (all) + CENTER_KEEPAWAY_PICKUP (all-except) + CENTER_KEEPAWAY_PICKUP_SELF (one) notifications all sent and registered."
      - "KEEPAWAY_PICKUPS column increment runs (AddCol on PickUp); the 0.5s self-recapture lockout is honored (BallTouchEntity → IsRecaptureLocked)."
      - "The non-player world-touch branch plays SND_KA_TOUCH (ATTEN_NORM)."
      - "STILL OPEN (values/presentation): carried scale 0.75 is not applied (no entity scale field). EFFECT_BALL_SPARKS particle on the non-player touch is not emitted (only the sound is)."
      - "STILL OPEN (logic): GameRules_scoring_vip(toucher,true) has no direct port equivalent (the carrier role is tracked via Ball.Carrier instead); functionally the carrier is identified for the mod-icon, but the QC VIP flag/scoreboard marker is not modeled."
      - "STILL OPEN (logic): multi-ball chaining (g_keepaway_ballcarrier_maxballs, cnt chain) is collapsed to a single ball (cnt=1)."
    verification:
      - { kind: codetrace, ref: "TriggerTouch.cs:42-46 dispatches the ball's Touch=BallTouchEntity → PickUp; sounds + notifs + KEEPAWAY_PICKUPS all fire; SoundsList.cs:99 KA_PICKEDUP; Keepaway.cs:237-239 notifications", result: pass }
      - { kind: value, ref: "SelfRecaptureLockout=0.5 == Base wait+0.5; carried scale 0.75 NOT applied", result: fail }
    confidence: high
    notes: "Core pickup (attach, lockout, scoring, sounds, notifications) is faithful + live. Residuals: carried scale, spark particle, VIP scoreboard flag, multi-ball chain. Unchanged by Wave 5."

  - id: keepaway.ball.drop
    name: "Ball drop on death/use-key/disconnect/observe"
    layer: [authority]
    base_refs:
      - "common/gametypes/gametype/keepaway/sv_keepaway.qc:ka_DropEvent"
      - "common/gametypes/gametype/keepaway/sv_keepaway.qc:ka_PlayerReset"
    port_refs:
      - "src/XonoticGodot.Common/Gameplay/GameTypes/Keepaway.cs:Drop"
      - "src/XonoticGodot.Common/Gameplay/GameTypes/Keepaway.cs:OnDeath"
      - "src/XonoticGodot.Common/Gameplay/GameTypes/BallEntity.cs:DropFromCarrier"
      - "src/XonoticGodot.Common/Gameplay/GameTypes/BallEntity.cs:DropOffsetVelocity"
    constants:
      - { name: "drop_offset_z", base: "10", port: "10 (DropFromCarrier carrier.Origin + '0 0 10')", match: true }
      - { name: "drop_velocity_base", base: "0 0 200 + crandom xy ('0 100 0'*crandom + '100 0 0'*crandom)", port: "0 0 200 + Vec(0,100,0)*Signed + Vec(100,0,0)*Signed", match: true }
      - { name: "self_recapture_lockout_wait", base: "0.5", port: "0.5 (DropFromCarrier sets GtNextTakeTime)", match: true }
    status:
      logic: partial
      values: faithful
      timing: faithful
      presentation: missing
      audio: faithful
      liveness: partial
    intended_divergence: false
    divergence_rationale: null
    gaps:
      - "The drop velocity includes the crandom xy scatter (DropOffsetVelocity == Base '0 0 200' + '0 100 0'*crandom + '100 0 0'*crandom; Prandom.Signed == crandom); the 0.5s previous_owner/wait self-recapture lockout is armed (GtNextTakeTime + Owner=carrier)."
      - "SND_KA_DROPPED plays globally (PlayGlobal KA_DROPPED, ATTEN_NONE); INFO_KEEPAWAY_DROPPED (all) + CENTER_KEEPAWAY_DROPPED (all) notifications sent and registered."
      - "STILL OPEN (liveness): only the DEATH-drop is wired (OnDeath → Drop when victimCarried). The use-key (PlayerUseKey), disconnect (ClientDisconnect), and observe (MakePlayerObserver) drop triggers are NOT wired — those mutator hooks are not yet on MutatorHooks (per the class comment)."
      - "STILL OPEN (logic): no nudgeoutofsolid_OrFallback after placing the dropped ball; no multi-ball chain advance / ka_PlayerReset chain semantics (single ball only)."
      - "STILL OPEN (presentation): no WP_KaBall waypoint sprite re-spawn on drop."
    verification:
      - { kind: codetrace, ref: "OnDeath victimCarried → Drop → DropFromCarrier (live death bus, Combat.Death). DropOffsetVelocity scatter + GtNextTakeTime lockout verified; KA_DROPPED + KEEPAWAY_DROPPED registered (Keepaway.cs:259-261)", result: pass }
      - { kind: codetrace, ref: "Grep for PlayerUseKey/ClientDisconnect/MakePlayerObserver ka drop hooks — none wired", result: fail }
    confidence: high
    notes: "Death-drop is fully faithful + live (scatter, lockout, sound, notifications). The other 3 drop triggers remain unwired pending the mutator hooks. Unchanged by Wave 5."

  - id: keepaway.score.timepoints
    name: "Per-second carry scoring (score_timepoints * frametime)"
    layer: [authority]
    base_refs:
      - "common/gametypes/gametype/keepaway/sv_keepaway.qc:ka_BallThink_Carried"
    port_refs:
      - "src/XonoticGodot.Common/Gameplay/GameTypes/Keepaway.cs:Tick"
      - "src/XonoticGodot.Server/GameWorld.cs:2036"
    constants:
      - { name: "g_keepaway_score_timepoints", base: "0", port: "0 (DefaultScoreTimePoints)", match: true }
    status:
      logic: faithful
      values: faithful
      timing: faithful
      presentation: na
      audio: na
      liveness: live
    intended_divergence: false
    divergence_rationale: null
    gaps:
      - "Tick (GameWorld.cs:2036, live per-frame DriveGametypeFrame) has a real carrier to score because the pickup path is live — the float2int-remainder accrual awards score while the ball is held. Default timepoints=0 so the column is opt-in (matches Base cfg g_keepaway_score_timepoints 0)."
    verification:
      - { kind: codetrace, ref: "GameWorld.cs:2036 case Keepaway ka: ka.Tick(Simulation.FrameTime); carrier set by the now-live PickUp path", result: pass }
    confidence: high
    notes: "Algorithm + default match Base; the upstream carrier is real, so scoring fires when timepoints>0. Unchanged by Wave 5."

  - id: keepaway.score.bctime
    name: "Ball-carry-time secondary column (KEEPAWAY_BCTIME += frametime)"
    layer: [authority]
    base_refs:
      - "common/gametypes/gametype/keepaway/sv_keepaway.qc:ka_BallThink_Carried"
      - "common/gametypes/gametype/keepaway/sv_keepaway.qh:GameRules_scoring"
    port_refs:
      - "src/XonoticGodot.Common/Gameplay/GameTypes/Keepaway.cs:Tick"
      - "src/XonoticGodot.Common/Gameplay/GameTypes/Keepaway.cs:Activate"
    constants:
      - { name: "bctime_sort_prio", base: "SFL_SORT_PRIO_SECONDARY", port: "SetSortKeys(Score, KEEPAWAY_BCTIME)", match: true }
    status:
      logic: faithful
      values: faithful
      timing: faithful
      presentation: na
      audio: na
      liveness: live
    intended_divergence: false
    divergence_rationale: null
    gaps:
      - "BCTIME accrual runs while carrying (the carrier is live); the secondary-sort column is declared in Activate (live, GameWorld.cs:1716 ka.Activate)."
      - "MINOR (intended): the port banks whole seconds (int column) and carries the fractional remainder; QC adds raw frametime to a float scoring field. Observable difference is negligible (scoreboard shows whole seconds)."
    verification:
      - { kind: codetrace, ref: "Tick BCTIME branch banks whole seconds to KEEPAWAY_BCTIME; carrier now set; column declared in Activate (GS.DeclareColumn + SetSortKeys)", result: pass }
    confidence: high
    notes: "Faithful + live; the whole-second banking is a benign display-equivalent simplification. Unchanged by Wave 5."

  - id: keepaway.score.killbonuses
    name: "Kill bonuses (bckill killer bonus + killac carrier bonus + carrierkills)"
    layer: [authority]
    base_refs:
      - "common/gametypes/gametype/keepaway/sv_keepaway.qc:PlayerDies"
      - "common/gametypes/gametype/keepaway/sv_keepaway.qc:GiveFragsForKill"
    port_refs:
      - "src/XonoticGodot.Common/Gameplay/GameTypes/Keepaway.cs:OnDeath"
      - "src/XonoticGodot.Common/Gameplay/GameTypes/Keepaway.cs:OnGiveFragsForKill"
    constants:
      - { name: "g_keepaway_score_bckill", base: "1 (cfg gametypes-server.cfg:437)", port: "1 (DefaultScoreBcKill)", match: true }
      - { name: "g_keepaway_score_killac", base: "1 (cfg gametypes-server.cfg:438)", port: "1 (DefaultScoreKillAc)", match: true }
    status:
      logic: faithful
      values: faithful
      timing: faithful
      presentation: na
      audio: na
      liveness: live
    intended_divergence: false
    divergence_rationale: null
    gaps:
      - "OnDeath bonuses reach a real carrier — killing the carrier awards bckill + KEEPAWAY_CARRIERKILLS; a kill made while carrying awards killac. All gated correctly (attacker != victim, IS_PLAYER)."
      - "The unset fallbacks DefaultScoreBcKill/KillAc are 1, matching the Base cfg defaults (gametypes-server.cfg:437-438 = 1/1), so headless/tests award the right bonus."
      - "GiveFragsForKill is suppressed — OnGiveFragsForKill zeroes FragScore and returns true; the live Scores.cs GiveFragsForKill.Call reads back 0, so the normal +1 FFA kill frag no longer leaks into the ka score (matches QC ka GiveFragsForKill)."
      - "MINOR: Base only adds bckill score under `if(autocvar_g_keepaway_score_bckill)` (nonzero guard); the port adds (int)ScoreBcKill unconditionally. With default=1 these are identical; a server explicitly setting bckill 0 would award 0 anyway (0 added). No observable divergence at any sane cvar value."
    verification:
      - { kind: codetrace, ref: "OnDeath on Combat.Death bus (Activate); bckill/killac/carrierkills awarded; carrier now real", result: pass }
      - { kind: codetrace, ref: "OnGiveFragsForKill on MutatorHooks.GiveFragsForKill; zeroes FragScore + returns true", result: pass }
      - { kind: value, ref: "DefaultScoreBcKill/KillAc=1 == cfg g_keepaway_score_*=1", result: pass }
    confidence: high
    notes: "Kill bonuses, carrier-kill column, frag-suppression all faithful + live. Default fallbacks correct at 1. Unchanged by Wave 5."

  - id: keepaway.win.pointlimit
    name: "Point-limit win condition + leader"
    layer: [authority]
    base_refs:
      - "common/gametypes/gametype/keepaway/sv_keepaway.qh:GameRules_limit_score"
      - "common/gametypes/gametype/keepaway/keepaway.qh:gametype_init pointlimit=30"
    port_refs:
      - "src/XonoticGodot.Common/Gameplay/GameTypes/Keepaway.cs:UpdateLeaderAndCheckLimit"
      - "src/XonoticGodot.Common/Gameplay/GameTypes/Keepaway.cs:RecomputeLeader"
      - "src/XonoticGodot.Server/GameWorld.cs:2486"
    constants:
      - { name: "pointlimit_default", base: "30 (mapinfo; g_keepaway_point_limit=-1 → mapinfo)", port: "30 (DefaultPointLimit literal)", match: true }
      - { name: "g_keepaway_point_limit_cfg", base: "-1 (gametypes-server.cfg:436; -1 = use mapinfo)", port: "returned directly; -1 treated as no-limit (limit>0 guard)", match: false }
      - { name: "timelimit_default", base: "20", port: "via gametype_init/cvars", match: true }
    status:
      logic: faithful
      values: partial
      timing: na
      presentation: na
      audio: na
      liveness: live
    intended_divergence: false
    divergence_rationale: null
    gaps:
      - "STILL OPEN (values): the port reads g_keepaway_point_limit and returns it directly; Base cfg default is -1 (use mapinfo's 30 — gametypes-server.cfg:436). When the cfg is loaded the port reads -1 and (since the `limit > 0f` guard disables the check for limit<=0) plays WITHOUT a point limit until fraglimit/mapinfo supplies one — whereas Base resolves -1 to the mapinfo's 30. Now that scoring is live this divergence is REACHABLE (a player could carry to 30 and Base would end the match; the port wouldn't unless fraglimit is set). The literal-30 fallback only applies when BOTH the ka cvar and fraglimit are unset."
    verification:
      - { kind: codetrace, ref: "GameWorld.cs:2484-2486 case Keepaway: winner = ka.Leader when MatchEnded; UpdateLeaderAndCheckLimit live", result: pass }
      - { kind: value, ref: "g_keepaway_point_limit cfg default -1 (mapinfo) vs port PointLimit returns -1 → no limit; mapinfo's 30 not consulted", result: fail }
    confidence: medium
    notes: "Leader/limit machinery is live + faithful, but the -1→mapinfo resolution is not modeled — now that scoring works this can let a timed ka run past Base's 30-point end. Unchanged by Wave 5."

  - id: keepaway.ffa.framing
    name: "FFA framing (no teams) + tie -> overtime"
    layer: [authority]
    base_refs:
      - "common/gametypes/gametype/keepaway/keepaway.qh:gametype_init GAMETYPE_FLAG_USEPOINTS"
    port_refs:
      - "src/XonoticGodot.Common/Gameplay/GameTypes/Keepaway.cs:TeamGame"
      - "src/XonoticGodot.Common/Gameplay/GameTypes/Keepaway.cs:ReportsTie"
    constants:
      - { name: "teamplay", base: "false (no GAMETYPE_FLAG_TEAMPLAY)", port: "TeamGame=false", match: true }
    status:
      logic: faithful
      values: faithful
      timing: na
      presentation: na
      audio: na
      liveness: live
    intended_divergence: false
    divergence_rationale: null
    gaps: []
    verification:
      - { kind: codetrace, ref: "TeamGame=false (ctor); ReportsTie=FfaTie.TopTwoTied", result: pass }
    confidence: high
    notes: "Correctly modeled as FFA (team variant is separate tka/TeamKeepaway). Unchanged — already faithful."

  - id: keepaway.damage.matrix
    name: "Possession damage/force scaling matrix (Damage_Calculate)"
    layer: [authority]
    base_refs:
      - "common/gametypes/gametype/keepaway/sv_keepaway.qc:Damage_Calculate"
    port_refs:
      - "src/XonoticGodot.Common/Gameplay/GameTypes/Keepaway.cs:OnDamageCalculate"
      - "src/XonoticGodot.Common/Gameplay/GameTypes/Keepaway.cs:DamageScale"
      - "src/XonoticGodot.Common/Gameplay/GameTypes/Keepaway.cs:DamageForceScale"
      - "src/XonoticGodot.Common/Gameplay/GameTypes/Keepaway.cs:ScaleFactor"
    constants:
      - { name: "g_keepaway_ballcarrier_damage", base: "1 1 1 (vector cfg)", port: "1 1 1 (vector string parse, per-component)", match: true }
      - { name: "g_keepaway_ballcarrier_force", base: "1 1 1 (vector cfg)", port: "1 1 1 (vector string parse)", match: true }
      - { name: "g_keepaway_noncarrier_damage", base: "1 1 1 (vector cfg)", port: "1 1 1 (vector string parse)", match: true }
      - { name: "g_keepaway_noncarrier_force", base: "1 1 1 (vector cfg)", port: "1 1 1 (vector string parse)", match: true }
    status:
      logic: faithful
      values: faithful
      timing: na
      presentation: na
      audio: na
      liveness: live
    intended_divergence: false
    divergence_rationale: null
    gaps:
      - "The matrix affects combat — OnDamageCalculate is registered on MutatorHooks.DamageCalculate in Activate, and the live DamageSystem DamageCalculate.Call invokes it on every player-vs-player hit (the IS_PLAYER guard mirrors QC: returns early for non-player attacker/target)."
      - "ScaleFactor/CvarVectorComponent parse the VECTOR cvar STRING and index the component (x=self, y=other-carrier, z=noncarrier), matching Base's single-vector autocvar — the old absent-_x/_y/_z suffix read is gone, so a server-set non-default vector IS honored. Component selection (attackerCarries→carrier vs noncarrier cvar; target==attacker→x, targetCarries→y, else→z) matches the QC branch table."
    verification:
      - { kind: codetrace, ref: "Activate adds OnDamageCalculate to MutatorHooks.DamageCalculate; DamageSystem DamageCalculate.Call invokes it live", result: pass }
      - { kind: codetrace, ref: "ScaleFactor component map (0/1/2) == QC Damage_Calculate .x/.y/.z branches; CvarVectorComponent parses 'a b c' vector string", result: pass }
    confidence: high
    notes: "Wired + reads the real vector cvars. At default 1 1 1 there is no visible change, but a server tuning the matrix is honored faithfully. Unchanged by Wave 5."

  - id: keepaway.carrier.highspeed
    name: "Carrier speed multiplier (MOVEVARS_HIGHSPEED *= ballcarrier_highspeed)"
    layer: [authority]
    base_refs:
      - "common/gametypes/gametype/keepaway/sv_keepaway.qc:PlayerPhysics_UpdateStats"
    port_refs: []
    constants:
      - { name: "g_keepaway_ballcarrier_highspeed", base: "1", port: "none", match: false }
    status:
      logic: missing
      values: missing
      timing: missing
      presentation: na
      audio: na
      liveness: na
    intended_divergence: false
    divergence_rationale: null
    gaps:
      - "STILL OPEN: no PlayerPhysics hook applies a carrier highspeed multiplier; the only 'ballcarrier_highspeed' references in the codebase are comment-only deferral notes in Keepaway.cs:51 and TeamKeepaway.cs. Explicitly deferred."
      - "Default is 1 (no effect) so the visible impact is nil unless a server sets it; still a missing feature."
    verification:
      - { kind: codetrace, ref: "No keepaway/highspeed implementation in any PlayerPhysics code; grep ballcarrier_highspeed → only Keepaway.cs/TeamKeepaway.cs comments", result: fail }
    confidence: high
    notes: "Unchanged from baseline — low gameplay impact at default but absent."

  - id: keepaway.hud.modicon
    name: "Keepaway HUD mod-icon (blinking ball-carrying icon)"
    layer: [presentation, authority]
    base_refs:
      - "common/gametypes/gametype/keepaway/cl_keepaway.qc:HUD_Mod_Keepaway"
      - "common/gametypes/gametype/keepaway/sv_keepaway.qc:PlayerPreThink (OBJECTIVE_STATUS|KA_CARRYING)"
    port_refs:
      - "game/hud/ModIconsPanel.cs:DrawKeepaway"
      - "game/net/NetGame.cs:3518"
      - "src/XonoticGodot.Net/GametypeStatusBlock.cs:110"
    constants:
      - { name: "blink", base: "blink(0.85,0.15,5)", port: "Blink(0.85,0.15,5)", match: true }
      - { name: "expand_transition", base: "bound(0, elapsed*2, 1)", port: "Clamp(elapsed*2,0,1)", match: true }
      - { name: "KA_CARRYING", base: "BIT(0) on STAT(OBJECTIVE_STATUS)", port: "carrier net id (0=nobody) in the Keepaway status block", match: true }
    status:
      logic: faithful
      values: faithful
      timing: faithful
      presentation: faithful
      audio: na
      liveness: live
    intended_divergence: false
    divergence_rationale: null
    gaps:
      - "The whole mod-icon stack is live end-to-end. Server packs ka.Ball.Carrier's net id into the per-recipient Keepaway status block (GametypeStatusBlock.cs:110-114, Kind.Keepaway=7), produced per-peer in the live snapshot writer. The client reads ms.CarrierNetId, sets panel.KeepawayCarrying = (CarrierNetId == LocalNetId) (NetGame.cs:3518-3520), and DrawKeepaway (ModIconsPanel.cs:664) renders the keepawayball_carrying icon with the blink(0.85,0.15,5) pulse + the status-change expand transition — faithful to HUD_Mod_Keepaway."
      - "MINOR (intended): the port transmits the carrier NET ID rather than a per-player KA_CARRYING bit; the client derives the local carrying flag from it (carrier == self). Equivalent result, and it also feeds the carrier waypoint if/when that is spawned."
      - "MINOR (presentation): when the keepawayball_carrying skin texture is missing the panel draws a ball-glyph + 'BALL' stand-in (a deliberate never-invisible fallback, not a Base behavior)."
    verification:
      - { kind: codetrace, ref: "GametypeStatusBlock.cs:114 writes carrier net id; NetGame.cs:3518-3520 sets Mode=Keepaway + KeepawayCarrying; ModIconsPanel.cs:664 DrawKeepaway blink/expand", result: pass }
    confidence: high
    notes: "Was entirely absent on both server and client; now a complete, live, faithful pipeline. Unchanged by Wave 5 (line numbers shifted)."

  - id: keepaway.waypoints.tracking
    name: "Ball / carrier waypoint sprites + radar tracking (g_keepawayball_tracking)"
    layer: [presentation, authority]
    base_refs:
      - "common/gametypes/gametype/keepaway/sv_keepaway.qc:ka_ballcarrier_waypointsprite_visible_for_player"
      - "common/gametypes/gametype/keepaway/sv_keepaway.qc:ka_RespawnBall (WP_KaBall)"
      - "common/gametypes/gametype/keepaway/sv_keepaway.qc:ka_TouchEvent (WP_KaBallCarrier)"
    port_refs:
      - "src/XonoticGodot.Common/Gameplay/Waypoints/Waypoints.cs:137"
    constants:
      - { name: "g_keepawayball_tracking", base: "1 (0=none/1=always/2=dropped-only)", port: "none (cvar never read)", match: false }
    status:
      logic: missing
      values: missing
      timing: na
      presentation: missing
      audio: na
      liveness: na
    intended_divergence: false
    divergence_rationale: null
    gaps:
      - "PARTIAL PROGRESS: the waypoint sprite TYPES are registered in the catalog (Waypoints.cs:137-138 KaBall, KaBallCarrier with radar icons), so the data definitions exist."
      - "STILL OPEN: NOTHING spawns/attaches a KaBall waypoint on the loose ball (ka_RespawnBall WaypointSprite_Spawn) or a KaBallCarrier sprite on the carrier (ka_TouchEvent WaypointSprite_AttachCarrier); the g_keepawayball_tracking cvar (0/1/2) is never read; the spectator/invisibility visibility rules (ka_ballcarrier_waypointsprite_visible_for_player) are not modeled. Players/spectators still get no on-screen or radar cue to the ball or carrier."
    verification:
      - { kind: codetrace, ref: "Waypoints.cs:137-138 registers KaBall/KaBallCarrier types, but no WaypointSprite_Spawn/AttachCarrier call site in Keepaway.cs or the net layer; g_keepawayball_tracking unread anywhere", result: fail }
    confidence: high
    notes: "Sprite types are declared but never instantiated; the tracking feature itself is still missing. Unchanged by Wave 5 (line 127→137)."

  - id: keepaway.bot.role
    name: "Bot ka roles (carrier/collector + ball goal rating + ForbidAttack)"
    layer: [authority]
    base_refs:
      - "common/gametypes/gametype/keepaway/sv_keepaway.qc:havocbot_role_ka_carrier"
      - "common/gametypes/gametype/keepaway/sv_keepaway.qc:havocbot_role_ka_collector"
      - "common/gametypes/gametype/keepaway/sv_keepaway.qc:havocbot_goalrating_ball"
      - "common/gametypes/gametype/keepaway/sv_keepaway.qc:Bot_ForbidAttack"
    port_refs:
      - "src/XonoticGodot.Server/Bot/BotObjectiveRoles.cs:RoleKeepaway"
      - "src/XonoticGodot.Server/Bot/BotObjectiveRoles.cs:FindBall"
      - "src/XonoticGodot.Server/Bot/BotRoles.cs:99"
    constants:
      - { name: "ball_goalrating_scale", base: "8000", port: "RatingBall = 8000f", match: true }
      - { name: "routerating_dist", base: "2000", port: "2000f", match: true }
      - { name: "ball_class", base: "keepawayball", port: "FindBall queries 'keepawayball' (BotObjectiveRoles.cs:292)", match: true }
      - { name: "carrier_field", base: "ball.owner (set on pickup, ka_TouchEvent:147)", port: "Entity.GtCarrier (port split: GtCarrier=carry, Owner=previous_owner/lockout); bot now reads ball.GtCarrier", match: true }
    status:
      logic: partial
      values: faithful
      timing: na
      presentation: na
      audio: na
      liveness: live
    intended_divergence: false
    divergence_rationale: null
    gaps:
      - "FIXED (was the worst gap): RoleKeepaway is live (set on the brain at bot creation — BotPopulation.cs:414 / BotController.cs:101 via BotRoles.ChooseRole; 'ka'/'keepaway'/'tka' → RoleKeepaway, BotRoles.cs:99) and now ACTUALLY engages the ball. FindBall() queries classname 'keepawayball' (BotObjectiveRoles.cs:292), matching the live spawned ball (BallEntity.cs:105 / Base new(keepawayball) sv_keepaway.qc:261) — the old 'keepaway_ball' mismatch is gone. RoleKeepaway reads ball.GtCarrier (BotObjectiveRoles.cs:255,267), which is the field AttachToCarrier actually sets (EntityGametypeState.cs:224, via BallEntity.AttachToCarrier → Keepaway.PickUp) — the old ball.Owner read (Owner is only set on DROP) is gone. So a loose ball is chased and a carried ball's carrier is correctly identified."
      - "STILL OPEN: no Bot_ForbidAttack equivalent (Base sv_keepaway.qc:542 forbids attacking when neither bot nor target carries and a held ball exists)."
      - "STILL OPEN (logic, why status stays partial): the collector/carrier split is collapsed into one RoleKeepaway (Base has two havocbot_role_ka_carrier sv_keepaway.qc:351 / havocbot_role_ka_collector:374 with the carrier ForbidAttack/flee behaviour); a structural simplification — the bot finds + contests the ball but does not switch to the dedicated carrier role."
    verification:
      - { kind: codetrace, ref: "FindBall queries 'keepawayball' (BotObjectiveRoles.cs:292) == live ball classname (BallEntity.cs:105, Base new(keepawayball) sv_keepaway.qc:261) — now matches", result: pass }
      - { kind: codetrace, ref: "RoleKeepaway reads ball.GtCarrier (BotObjectiveRoles.cs:255,267); AttachToCarrier sets GtCarrier (EntityGametypeState.cs:224) on PickUp → carrier seen", result: pass }
      - { kind: codetrace, ref: "Role is live: BotRoles.ChooseRole('ka') → RoleKeepaway assigned to BotBrain.Role at spawn (BotPopulation.cs:414 / BotController.cs:101); brain runs it on its strategy clock", result: pass }
      - { kind: value, ref: "RatingBall=8000f == Base goalrating_ball 8000; routerating 2000f == Base", result: pass }
    confidence: high
    notes: "FIXED post-baseline (Wave 5): both material defects (FindBall classname 'keepaway_ball'→'keepawayball'; carrier read ball.Owner→ball.GtCarrier) are corrected, so the role is genuinely live — bots find and contest the ball instead of playing DM. Rating values stay faithful. Logic remains partial only for the structural carrier/collector split + the missing Bot_ForbidAttack flee behaviour."
