[
 {
  "unit": "mutator-bloodloss",
  "notes": [
   "[logic/values] mutator-bloodloss.presentation.active_mutator_signalling: The serverinfo/scoreboard active-mutators list will not include 'Blood loss', and the CSQC active-mutator HUD icon (mut_set_active(MUT_BLOODLOSS)) never lights up — the port has NO active-mutator signalling mechanism at all (no mut_set_active / MUT_BLOODLOSS / BuildMutatorsString equivalent found across src/ or game/). Re-confirmed unported after Wave-2."
  ]
 },
 {
  "unit": "mutator-breakablehook",
  "notes": [
   "[logic] breakablehook.owner.punish_splash: TEAMPLAY REGRESSION (STILL OPEN): in a team game (CTF/TDM/CA etc.) breaking a TEAMMATE's hook fires the punish (5 splash to the teammate + force-remove), because DiffTeam falls to the FFA branch !ReferenceEquals(attacker, owner) when the static GameScores.Teamplay is false — and it is ALWAYS false on the server (no live writer; ScoreInfoBlock.Apply has zero callers, re-verified 2026-06-22). Base DIFF_TEAM uses team!=team in teamplay and would skip a same-team break. The pre-Wave-2 port (!Teams.SameTeam) handled this correctly, so this is a regression vs both Base and the old port. NOT addresse"
  ]
 },
 {
  "unit": "mutator-buffs",
  "notes": [
   "[logic/values/timing] buffs.item.spawnfunc: Still no item_buff / item_buff_<type> / item_buff_random spawnfunc, no per-team variants, and no Q3/QL/WOP compat classnames mapped via Buff_CompatName as map-spawnable buff items. (GiveItems.cs:342 ports Buff_CompatName only for the `give` console command, not for map spawnfuncs; ItemSpawnFuncs.cs:69-70 only aliases item_buff_speed/_invisibility to POWERUPS, not buffs.) ; buff_Init lifecycle absent: no MDL_BUFF model/size, MOVETYPE_TOSS map placement, PFLAGS_FULLDYNAMIC light, buff_Customize, replace-unavailable-via-buff_NewType, bot_pickup value 1000, or buff_Reset/INITPRIO ordering. ; Conse",
   "[logic/timing] buffs.item.auto_spawn: Default count is 0 so this never runs unless explicitly configured (matches Base default). ; QC defers via InitializeEntity(buffs_DelayedInit, INITPRIO_FINDTARGET) and sets spawnflags|=64 (always randomize/relocate) on each seed; port now ALSO sets BuffAlwaysRelocate=true on each auto-seed (SpawnInitialBuffs:229), runs MaybeRandomize, arms BuffLifetime, and starts the BuffThink lifetime cycle — so the spawnflag-64 always-relocate + random_lifetime behavior is now modelled. Still spawns immediately at Hook() (not via the INITPRIO_FINDTARGET defer) and the item remains stripped (no waypoint/glow",
   "[logic] buffs.item.touch_pickup: cl_buffs_autoreplace is still not honored: a different held buff is always replaced; QC only replaces (STATUSEFFECT_REMOVE_CLEAR) when the per-player cvar is set, else keeps the held buff. ; No INFO_ITEM_BUFF notification to other players; no INFO_ITEM_BUFF_LOST on the replaced buff. ; No EFFECT_ITEM_PICKUP particle; only the pickup sound plays. ; team_forced (teamplay buff items) and toucher.vehicle checks not implemented; lifetime not zeroed on pickup. ; Only reachable when a buff item exists (see buffs.item.spawnfunc / auto_spawn).",
   "[logic/timing] buffs.item.respawn_cooldown: Port respawn is still a single Think after cooldown_respawn (now also clears Owner and runs MaybeRandomize/MaybeRelocate); no buff_activetime countdown, no activate-cooldown arming on round start (Base buff_Reset uses g_buffs_cooldown_activate; port only models cooldown_respawn), no buff_active/buff_activetime state machine, no buff_Customize light pulse. ; No SND_STRENGTH_RESPAWN + EFFECT_ITEM_RESPAWN on reactivation; no waypoint build-finished timer; no g_buffs_pickup_anyway instant-respawn. (KA_RESPAWN now plays on a buff_Respawn RELOCATION via BuffRespawn, but not on a plain cooldown react",
   "[logic/values/timing] buffs.item.random_location: MoveToRandomMapLocation relocation IS now done (BallEntity.RandomMapLocation, which folds in the SelectSpawnPoint fallback), with the '0 0 200' launch + a mins*1.5/maxs*1.5 tracebox unstick + g_buffs_random_lifetime arming + SND_KA_RESPAWN on relocation. The g_buffs_randomize teamplay gate (& (teamplay?2:1)) and g_buffs_random_location/spawnflag-64 relocate gate are now honored (no longer an unconditional re-randomize). ; Still missing: g_buffs_random_location_attempts is not read (RandomMapLocation uses its own fixed sampling rather than the attempts/256/1024 box-content rejection of MoveToRa",
   "[logic] buffs.item.available: QC also excludes ammo when start_items already has IT_UNLIMITED_AMMO; port only checks g_melee_only (and the per-buff cvar + g_vampire).",
   "[logic/values/timing] buffs.medic.regen: QC PlayerRegen scales the existing regen/limit(max)/rot factors (rot_mod=0.2, limit_mod=max_mod=1.5, regen_mod=1.7); port no-ops PlayerRegen (returns false) and runs a separate ad-hoc per-frame heal toward a recomputed medic max in MedicTick. ; Health-rot multiplier (0.2 = slower rot above stable) is still not applied — above-max health rots at the normal rate. ; Regen pacing/ceiling differs from QC's folded multipliers.",
   "[logic/values] buffs.disability: Movement-speed slow (Stunned x0.7 via SpeedMultiplier, OnPlayerPhysics:531-533) is LIVE. ; Weapon fire-time slow (WeaponRateFactor x1.5) is LIVE: OnWeaponRateFactor (544-550) multiplies args.Factor by 1.5 for a stunned player; WeaponRateFactor.Call has a live site at WeaponFireGate.cs:455. ; STILL MISSING: weapon projectile-speed slow (WeaponSpeedFactor x0.7) and monster run/walk slow (MonsterMove x0.7) — BuffsMutator subscribes neither hook (no WeaponSpeedFactor mutator-hook chain exists at all — Devastator/WeaponImpulses read a static g_weaponspeedfactor, not a per-player hook; MonsterMove i",
   "[logic] buffs.ammo: QC sets every weapon slot's clip_load = weapon_load[switchweapon] = clip_size on apply AND each tick (and remembers/restores buff_ammo_prev_clipload), so reload weapons (rifle / OK machinegun) never need reloading; port still has NO clip/weapon_load handling. ; Port AmmoTick floors each resource pool to max(current,1) instead of relying on the IT_UNLIMITED_AMMO flag for ammo — a much smaller top-up; QC's m_tick only touches clips (ammo is the flag).",
   "[logic] buffs.magnet: Port uses center-distance (origin to origin) with a precomputed max reach; QC uses boxesoverlap (absmin/absmax expanded by the range), so the effective pull radius differs for large/off-center items. ; QC selects buff-vs-item range by it.buffdef; port selects by classname == 'item_buff' (equivalent in practice, but tied to the missing item_buff spawnfunc).",
   "[logic] buffs.flight: On removal QC restores gravity from trigger_gravity_check.enemy.gravity when standing in a gravity zone, else the saved old gravity; port always restores the saved old gravity (loses the gravity-trigger interaction).",
   "[logic] buffs.swapper: Core swap is implemented and LIVE: nearest enemy within range (DIFF_TEAM/dead/frozen/vehicle filtered), origin/velocity/angles + oldorigin exchanged, SND_KA_RESPAWN at both ends, buff_RemoveAll consumes the buff. The ForbidThrowCurrentWeapon hook has live Call sites (WeaponThrowing.cs:149/185), so pressing drop-weapon reaches it. ; Divergences from QC: uses center-distance + Api.Entities.FindInRadius instead of WarpZone_FindRadius (no warpzone traversal); omits Drop_Special_Items(closest) (flag/key carriers keep their objective on swap); omits MUTATOR_CALLHOOK(PortalTeleport, player); omits th",
   "[logic/values/timing] buffs.carrier_model: A player holding a buff still has no colored MDL_BUFF glow model attached: no buffs_BuffModel_Spawn/Update/Think/Customize, no removal on observer/disconnect. EF_NOSHADOW IS set on the player while held (ApplyBuff:324), but the glow model itself is absent.",
   "[logic/values/timing] buffs.buff_effect: No per-carrier Send_Effect particle emission throttled by buff_effect_delay; g_buffs_effects (default ON) is never read. EntityMutatorState declares a buff_effect_delay field but nothing emits the effect.",
   "[logic/values/timing] buffs.waypoint: No WP_Buff radar/HUD waypoint on buff items (no buff_Waypoint_Spawn, no RADARICON_Buff, no build-finished timer); the colored buff icon never appears on radar/screen edge, and there is no per-viewer hide-if-already-held logic.",
   "[logic/values] buffs.drop: Still no PlayerUseKey handler: a player cannot drop a held buff even when g_buffs_drop is enabled. No ITEM_BUFF_DROP notification, no INFO_ITEM_BUFF_LOST to others, no SND_BUFF_LOST, no buff_shield delay set on drop.",
   "[logic/values] buffs.replace_powerups: Still no FilterItem hook in BuffsMutator: with g_buffs>=0 and g_buffs_replace_powerups, QC replaces every powerup item with a random buff (buff_SpawnReplacement). The g_buffs>0 'replace' mode is entirely non-functional, removing the second obtain path. (A FilterItemDefinition hook chain exists in MutatorHooks.cs but BuffsMutator does not subscribe it.)",
   "[logic/values] buffs.mutator_string: Buffs is still not appended to the server's mutator string/pretty-string (only reported when g_buffs > 0 in QC); the scoreboard/serverinfo mutator list will not show Buffs.",
   "[logic] buffs.notifications: Only the picker's ITEM_BUFF_GOT centerprint + a single misc/shield_respawn pickup sound fire (BuffTouch:272-275). ; Missing emission: INFO_ITEM_BUFF (to others on pickup) — the token EXISTS in NotificationsList.cs:664 (as 'ITEM_BUFF' Info, '%s got the %s buff!') but BuffTouch never emits it; ITEM_BUFF_LOST/INFO_ITEM_BUFF_LOST on replace/expire; ITEM_BUFF_DROP on drop. ; Missing sounds: SND_BUFF_LOST (replace/drop/expire), SND_STRENGTH_RESPAWN (item reactivate). SND_KA_RESPAWN IS now wired for the swapper swap (BuffsMutator.cs:593-594) but not for item relocation.",
   "[logic] buffs.observer_cleanup: Still no MakePlayerObserver / ClientDisconnect handler to run buffs_RemovePlayer (delete the carrier buff_model). Moot only because the buff_model glow itself is unimplemented (buffs.carrier_model)."
  ]
 },
 {
  "unit": "mutator-bugrigs",
  "notes": [
   "[logic/values] mutator-bugrigs.player_physics.prevangles: player.disableclientprediction = 2 is not set (Base force-disables prediction every tick for a bugrigs player). The port treats bugrigs as server-authoritative so this is LIKELY inert, but that is unverified under the port's net smoothing / cl_movement_errorcompensation layer — could surface as camera/position drift for a bugrigs client.",
   "[logic/values/timing] mutator-bugrigs.clientconnect.chase_camera: No forced 3rd-person chase camera on join: a bugrigs player spawns in 1st person (Base stuffcmds 'cl_cmd settemp chase_active 1' on ClientConnect), which is the wrong/unplayable view for a ground-hugging rig.",
   "[logic/values] mutator-bugrigs.serverinfo.mutator_strings: Mutator not advertised in the active-mutator strings (Base appends ':bugrigs' / ', Bug rigs'): the mode is invisible in the server browser mutator field and scoreboard/endmatch mutator list."
  ]
 },
 {
  "unit": "mutator-campcheck",
  "notes": [
   "[logic/values] campcheck.realclient.gate: Typing players are STILL punished: Base exempts players in chat unless g_campcheck_typecheck 1. The port reads g_campcheck_typecheck into TypeCheck but never consults it in the PreThink gate, and there is no chat-button (PHYS_INPUT_BUTTON_CHAT) seam, so a player typing can still be camp-damaged/killed. Read-but-dead cvar; behavior unchanged from baseline. ; weaponLocked players are STILL camp-checked: Base skips the check when the weapon is locked; the port has no weaponLocked seam/gate (sibling mutators stub WeaponLocked()=>false for the same reason).",
   "[logic/values] campcheck.lifecycle.misc: CopyBody clone's campcheck_prevorigin not copied (no CopyBody hook in the port); practical impact limited because the player edict is reused on respawn and PlayerSpawn re-inits the fields. ; ':CampCheck' not appended to the active-mutators server string (no BuildMutatorsString hook)."
  ]
 },
 {
  "unit": "mutator-cloaked",
  "notes": [
   "[logic/values] mutator-cloaked.report.mutators_pretty_string: STILL OPEN — Cloaked contributes nothing to the active-mutators pretty string. Wave-5 added the GENERAL mechanism (MutatorBase.BuildMutatorsPrettyString virtual + MutatorActivation chain runner, used by MidairMutator -> ', Midair'), but: (1) CloakedMutator does NOT override BuildMutatorsPrettyString (still the base no-op), (2) no MUT_CLOAKED=7 bit exists, and (3) MutatorActivation.BuildMutatorsPrettyString has NO live caller — the scoreboard/HUD never invokes it (grep finds only the self-reference at MutatorActivation.cs:105). So 'Cloaked' is still not displayed anywhere.",
   "[logic/values] mutator-cloaked.menu.metadata: STILL OPEN — no port menu-mutator metadata: no Cloaked describe text or create-game checkbox (general menu-mutator gap across the port, not unique to cloaked). UNCHANGED by Wave-5."
  ]
 },
 {
  "unit": "mutator-damagetext",
  "notes": [
   "[logic] damagetext.sv.first_hit_bitset: No ClientDisconnect hook: a disconnecting attacker's entry is not removed from every victim's DentAttackers set (the mutator only subscribes PlayerDamaged + PlayerSpawn). If a new player reuses the same Entity reference/slot, the STOP_ACCUMULATION first-hit gate can mis-fire. Edge case; MutatorHooks exposes no ClientDisconnect/ClientConnect hook at all (re-grep of MutatorHooks.cs on claude/parity-port-waves still finds none — the +121 Wave-2 lines added other hooks, not these). Wave-2 added only a documentation comment (DamagetextMutator.cs:73-82) acknowledging this gap and noting it keys on t",
   "[values] damagetext.cl.draw_fade_shrink_move: World drift only rises along world-Z (x0.0254 inch->meter hardcoded scale); QC offsets along the view basis (forward/right/up) so the number's view-relative placement is lost (lateral offset_world Y=25 and the X/Y velocity components are dropped). UNCHANGED by Wave-5 (DamageTextLayer.cs:277-278). ; Color codes in cl_damagetext_format render literally (DrawString instead of drawcolorcodedstring2); font is the Godot fallback font, not the Xonotic HUD font; drawfontscale-based crisp scaling (size/size_max cell) replaced by a direct integer pixel size, so glyph metrics/centering differ. UNCHANGED ",
   "[logic/values] damagetext.ui.settings_tab: No port menu tab exposes the cl_damagetext / friendlyfire / size_min/size_max / color / per-weapon / alpha_start / fade-time / accumulate sliders that ui_damagetext.qc builds; the cvars exist and are read live by DamageTextConfig but are not editable from a settings UI."
  ]
 },
 {
  "unit": "mutator-dodging",
  "notes": [
   "[logic] mutator-dodging.anim.jump_action: Base drives ANIMACTION_JUMP on the dodge hop (unconditionally, OUTSIDE the sv_dodging_sound guard); the port plays only the sound, no animation action, so a dodge shows no jump pose. STILL MISSING — DodgingMutator.cs:358-360 is a comment documenting that no animdecide/anim-action seam exists in the port.",
   "[logic] mutator-dodging.lifecycle.reset: STILL only PlayerSpawn is hooked; the MakePlayerObserver reset is unreachable because MutatorHooks STILL has NO MakePlayerObserver hook member (verified: MutatorHooks.cs has no such HookChain; only comment references across the codebase). A player going to spectate mid-dodge keeps stale dodging_* fields. Wave-5 did not add this hook."
  ]
 },
 {
  "unit": "mutator-doublejump",
  "notes": [
   "[logic] doublejump.stat.per_player_doublejump: The per-player stat infrastructure (Physics_ClientOption / g_physics_clientselect) is now implemented and resolves the per-player doublejump value into MovementParameters.DoubleJump (PhysicsPreset.cs:66, MovementParameters.cs:187). BUT the DoublejumpMutator does NOT consume it: IsEnabled (hook subscription) and the OnPlayerJump body both key off sv_doublejump directly. When g_physics_clientselect is on and a client picks a preset that flips doublejump per-client, the mutator's subscription gate won't track that override. Stock play (g_physics_clientselect 0) is unaffected: mp.DoubleJump == sv_"
  ]
 },
 {
  "unit": "mutator-dynamic_handicap",
  "notes": [
   "[logic/timing] dynamic_handicap.triggers.recompute: Missing AddedPlayerScore(SP_SCORE) recompute: Base fires the POST-write AddedPlayerScore hook (scores.qc:377) on every SP_SCORE delta; the port's GameScores.AddToPlayer fires only the PRE-write AddPlayerScoreHook (GameScores.cs:414, veto/rewrite) and NO post-write event — so caps, objective points, KH/Dom/CTF scoring and any non-death score change do not re-handicap until the player's next spawn or death. Re-confirmed post-fixup: GameScores still has only the pre-write hook. ; ClientDisconnect does not recompute: no ClientDisconnect hook chain exists in MutatorHooks (re-confirmed: chain list s",
   "[logic] dynamic_handicap.apply.invalid_value_guard: Port does not error on a non-positive forced handicap; it silently treats forced<=0 as 1 at read time (HandicapTotal:840-841). Base would error() (server fault) on a <=0 SetForcedHandicap. Unreachable in practice: the +1 / 1/(|h|+1) fold keeps dynamic-handicap values strictly positive, and the default min clamp of 0 is never actually hit. Captured as a behavioral divergence, not a live bug.",
   "[logic/values] dynamic_handicap.present.handicap_level: Port never computes or networks handicap_level (no handicap_level/handicapLevel/HANDICAP_MAX_LEVEL symbol anywhere in src/ or game/ — re-grepped post-fixup, still zero hits), so the scoreboard's player_handicap icon has no data and is never colored by handicap strength. Base updates it inside Handicap_SetForcedHandicap and replicates it to clients.",
   "[logic/values] dynamic_handicap.present.mutator_string: Active dynamic_handicap is not advertised in the server's mutator list string / pretty string, so server browsers and the scoreboard mutator line will not show it. The port has a BuildMutatorsString hook concept (used by RocketFlyingMutator) but dynamic_handicap does not subscribe it (re-grepped post-fixup: no ':handicap' token, no BuildMutatorsString subscription in DynamicHandicapMutator)."
  ]
 },
 {
  "unit": "mutator-globalforces",
  "notes": [
   "[logic/values] mutator-globalforces.advertise.mutatorstring: The mutator never appends ':GlobalForces' / ', Global forces' to the server's mutator list, so it is not advertised in server-info / scoreboard. Confirmed STILL OPEN after Wave-2: NO BuildMutatorsString / BuildMutatorsPrettyString hook chain exists anywhere in src/ (grep finds only doc-comments in CloakedMutator.cs:16, PowerupsMutator.cs:24, RocketFlyingMutator.cs:23, SuperSpecMutator.cs:38 explicitly deferring it); no chain, no handler, no ':GlobalForces' literal."
  ]
 },
 {
  "unit": "mutator-hook",
  "notes": [
   "[timing] mutator-hook.offhand.think_driver: Timing: the +hook button is published in PlayerPostThink (WeaponFireDriver.Frame, GameWorld.cs:1333/1368) but read in PlayerPreThink (GameWorld.cs:1174), which runs EARLIER in the same tick — so HookMutator.OnPlayerPreThink reads OffhandFirePressed one tick stale. Press/release lands one server tick (~1/72 s) late vs Base, where offhand_think is dispatched inside W_WeaponFrame same-frame. Functionally castable; minor latency. ; No vehicle / weaponUseForbidden suppression of the offhand key inside this path. WeaponFireDriver.WeaponUseForbidden is a stub returning false; the seated case is exclu",
   "[values] mutator-hook.startitems.fuel: STILL OPEN: only the non-warmup start fuel is bumped (StartLoadout has a single AmmoFuel field, no warmup-fuel field). Base also sets warmup_start_ammo_fuel = max(.., rotstable); SpawnSystem.cs:883 reads a flat g_warmup_start_ammo_fuel cvar but the hook mutator never maxes it against rotstable. Minor: only matters during warmup with useammo on (a rare combination).",
   "[logic/values] mutator-hook.info.mutator_string: STILL MISSING after Wave-5: no ':grappling_hook' server-info token and no ', Hook' pretty token are appended, so server browsers / the votescreen won't show that the grappling-hook mutator is active. The g_grappling_hook entry in CommandReplies.cs:252 remains the cvar_changes PureBadCvars suppression list, NOT the BuildMutatorsString token emission — those tokens still have no port emitter anywhere (grep ':grappling' / ', Hook' / BuildMutatorsString -> no emission).",
   "[logic] mutator-hook.client.gameplay_tip: STILL MISSING after Wave-5: no client gameplay tip is shown when the hook mutator is active; Base cl_hook appends '^3grappling hook^8 is enabled, press ^3<+hook key>^8 to use it'. The port has no BuildGameplayTipsString/gameplay-tips system at all (grep src/ + game/ for BuildGameplayTip/GameplayTip/'grappling hook is enabled' -> none)."
  ]
 },
 {
  "unit": "mutator-instagib",
  "notes": [
   "[logic] mutator-instagib.enable.predicate: Port enable predicate omits the !MapInfo_LoadedGametype.m_weaponarena guard; console `g_instagib 1` could co-enable with a weapon-arena gametype. Mitigated on the live path only by the menu radio-group (DialogMutators.cs:268 _arenaGroup zeroes g_weaponarena/g_nix when InstaGib is picked) — same mitigation pattern as NIX; the console double-activation hole remains. ; ONADD item-flag unblocking of VaporizerCells/Invisibility/Speed and the g_instagib_items IntrusiveList are not ported (tied to the still-missing item economy).",
   "[logic] mutator-instagib.damage.yoda: Instagib's Damage_Calculate yoda branch (target alpha in (0,1) and is a player -> set global yoda=1, used for the humiliation announcer) is still not ported. Vaporizer.cs:164 only notes yoda/impressive as a deferred client-render TODO.",
   "[values] mutator-instagib.items.filter: instagib_replace_item_with_random_powerup deck (even distribution of Invisibility/ExtraLife/Speed for big powerups under g_powerups) not ported — the port DELETES big powerups instead of replacing them (documented cross-file TODO; matches Base only when g_powerups is off). ; Ammo-convert-to-VaporizerCells replacement (g_instagib_ammo_convert_* set -> replace pack with VaporizerCells) is a plain delete in the port (no item-spawn replace seam + no VaporizerCells economy). The DELETE half is now Base-faithful (Base always deletes the original); only the replace half is missing. ; Vaporizer loot-d",
   "[logic/values/timing] mutator-instagib.items.defs: Still no item_vaporizer_cells / item_minst_cells spawnfunc (grep -> none). item_extralife exists only as an LMS extra-life spawn (LastManStanding.cs), not the instagib VaporizerCells economy; the ammo_vaporizercells_init (cells = g_instagib_ammo_drop) seed is absent.",
   "[logic] mutator-instagib.lifecycle.stop_countdown: Port still has no MatchEnd / MakePlayerObserver hook chains and no instagib subscription; the countdown's needammo flag / FINDAMMO centerprint is only reset on the next spawn (OnPlayerSpawn) and by the per-tick stop conditions — not when the match ends or a player becomes a spectator. NOTE: the per-tick game_stopped branch is now functional (reads real VehicleCommon.GameStopped), so MatchEnd is now PARTLY covered (the countdown stops within ~1 tick of the match freezing); the MakePlayerObserver case is still uncovered until the next spawn.",
   "[logic/values] mutator-instagib.misc.strings_monsters: No ':instagib' serverinfo tag, no 'InstaGib' modname, no ', InstaGib' pretty-string. ; MonsterDropItem (vaporizer_cells) and MonsterSpawn (Mage skin 1) not ported. ; RandomItems_GetRandomInstagibItemClassName (weighted instagib item pick over g_instagib_items) not ported."
  ]
 },
 {
  "unit": "mutator-invincibleproj",
  "notes": [
   "[logic/values] mutator-invincibleproj.string.machine: No port chain emits ':InvincibleProjectiles'; the port still has no active-mutators token-string assembly. There is NO BuildMutatorsString HookChain anywhere in src/ (grep for a HookChain<...BuildMutators...> = 0 hits) — only doc-comment mentions in RocketFlyingMutator.cs / SuperSpecMutator.cs / PowerupsMutator.cs.",
   "[logic/values] mutator-invincibleproj.string.pretty: Scoreboard / HUD / server-browser will not display 'Invincible Projectiles' as an active mutator (BuildMutatorsPrettyString hook still not ported; no pretty active-mutators string assembled). The create-game menu checkbox (input side) IS present at game/menu/dialogs/DialogMutators.cs:181.",
   "[logic/values] mutator-invincibleproj.client.active_mut_flag: The Base GetActiveMutators-style scan (util.qc:280-330) walks the active_mutators cvar (or, when empty, the live cvars) and calls mut_set_active(MUT_INVINCIBLE_PROJECTILES) + appends the translated name when g_invincible_projectiles is set, so the menu / server-browser 'active mutators' list and any MUT_ bitmask consumer can light up this mutator. The port still has NO active_mutators / mut_set_active / MUT_ flag surface (grep across src+game = 0 hits)."
  ]
 },
 {
  "unit": "mutator-itemstime",
  "notes": [
   "[logic/values/timing] mutator-itemstime.net.csqc_sync: No itemstime net message exists in XonoticGodot.Net (REGISTER_NET_TEMP(itemstime) / IT_Write / NET_HANDLE not ported). Item times are fed directly from the local server mutator to the local HUD in NetGame.cs:2286-2300, guarded by `if (_server is not null)` (NetGame.cs:2260). A pure remote client (dedicated/separate server) never receives times and the items-time panel stays empty. STILL OPEN after Waves 1/2/5 (grep of src/XonoticGodot.Net for itemstime/IT_Write -> zero matches; the NetGame feed block was rewired in Wave 5 to add the enable gate but is still host-only).",
   "[logic/values] mutator-itemstime.producer.per_player_tiers: The per-client SERVER send gate (only spectators/warmup get times in a live round unless sv_itemstime==2 via SetTimesForAllPlayers), and the MakePlayerObserver / PlayerSpawn / ClientConnect / reset_map_global sync+reset hooks (SetTimesForPlayer / ResetTimesForPlayer / ResetTimes) are not implemented. There is no per-player networked itemstime state in the port. STILL OPEN after Waves 1/2/5: ItemstimeMutator.Hook() still adds ONLY MutatorHooks.SvStartFrame — no observer/spawn/connect/reset hooks. Wave 5 reproduced the OBSERVABLE consequence of the gate client-side (the host's ShouldDraw spectat",
   "[logic] mutator-itemstime.net.itemstime_stat: STAT(ITEMSTIME) is not REGISTERED as a networked client stat. Wave 5 instead exposes ItemstimeMutator.Tier = (int)sv_itemstime and the host enable gate reads it directly. On a listen server this is equivalent to the stat for the local client (value faithful, consumed live), but a pure remote client still has no networked tier. The waypoint-sprite consumer (waypoints.spectator_sprites) is also still unported, so that second consumer of the stat remains unsatisfied.",
   "[logic] mutator-itemstime.hud.feed_host: The times reach the panel only on the listen-host (`if (_server is not null)` at NetGame.cs:2260). The published table itself is faithful (CurrentTimes); the delivery mechanism is a direct local push instead of the per-client networked IT_Write, so a remote client gets nothing (see net.csqc_sync). Wave 5 narrowed the push to fire only when ShouldDraw passes (no longer unconditional when enabled), which is closer to Base's per-player send semantics but still host-only. STILL OPEN (remote-client transport).",
   "[logic/values/timing] mutator-itemstime.waypoints.spectator_sprites: No waypointsprite_itemstime / SPRITERULE_SPECTATOR support in the port: timed (SpectatorOnly) items (mega health/armor) get no spectator/warmup respawn-countdown waypoint sprite (Item_RespawnCountdown sets SPRITERULE_SPECTATOR + WaypointSprite_UpdateBuildFinished at time+ITEM_RESPAWN_TICKS), and the g_waypointsprite_itemstime cvar (default 2) is unregistered. Also depends on the unmodeled networked STAT(ITEMSTIME). STILL OPEN after Waves 1/2/5 (grep across src + game -> no matches)."
  ]
 },
 {
  "unit": "mutator-kick_teamkiller",
  "notes": [
   "[logic/values/timing] mutator-kick_teamkiller.detect.playtime: STILL OPEN (unchanged by Wave 1/2): port has no per-client startplaytime; uses absolute sim clock since level start (Api.Clock.Time). For a mid-match joiner the denominator is larger, so the same teamkill count yields a lower computed rate -> punished later than Base (or escapes). Coincides with Base only for players present since level start. grep for startplaytime/StartPlayTime across src/ still finds no per-client transition timestamp (only the mutator's own comment).",
   "[timing] mutator-kick_teamkiller.detect.threshold: The threshold expression itself is faithful, but its result is skewed by the wrong playtime denominator (see mutator-kick_teamkiller.detect.playtime): mid-match joiners cross the rate condition later than Base. STILL OPEN."
  ]
 },
 {
  "unit": "mutator-melee_only",
  "notes": [
   "[logic/values] melee_only.ui.mutator_string_machine: STILL MISSING for melee_only. The shared BuildMutatorsString infrastructure now EXISTS and is LIVE: GameplayBases.BuildMutatorsString virtual (GameplayBases.cs:198), MutatorActivation.BuildMutatorsString chain (MutatorActivation.cs:88), live caller at GameWorld.cs:865 (GameLogInit -> g_mutatormsg). But ONLY MidairMutator and VampireMutator override BuildMutatorsString; MeleeOnlyMutator does NOT, so melee_only contributes no ':MeleeOnly' machine token. Infrastructure-absence half of the baseline gap is closed; the melee_only handler is still missing.",
   "[logic/values] melee_only.ui.mutator_string_pretty: STILL MISSING for melee_only AND the chain is not displayed. The BuildMutatorsPrettyString infrastructure exists (GameplayBases.cs:206 virtual + MutatorActivation.cs:101 chain) and Midair/Vampire override it, but (a) MeleeOnlyMutator does NOT override it, and (b) MutatorActivation.BuildMutatorsPrettyString has NO live external caller (grep finds none outside the chain itself), so even an override would not reach the active-mutator / mutator-list UI. The label ', Melee only Arena' never shows."
  ]
 },
 {
  "unit": "mutator-midair",
  "notes": [
   "[logic/values] mutator-midair.bot.disable_bunnyhop: Port has no separate bot_moveskill field, so OnPlayerSpawn zeroes the bot's ENTIRE skill (Player.BotSkill = 0). Base only zeroes bot_moveskill and leaves `skill` (aim/reaction) intact; its bunnyhop gate is `skill + bot_moveskill >= offset`, so a Base bot with skill >= 7 STILL bunnyhops even with moveskill 0. The port's gate becomes `0 >= 7` = always false, disabling bunnyhop unconditionally — more aggressive than Base. ; Collateral over-reach: BotBrain keys aim error, reaction interval, dodge cadence, reload-on-purpose (Skill<2/<5 gates) and roles off the same Skill, so zeroing it makes midair",
   "[logic/values] mutator-midair.meta.mutator_strings: PARTIALLY FIXED in Wave 5. Machine string is now LIVE+faithful: MidairMutator.BuildMutatorsString returns `:midair`, MutatorActivation.BuildMutatorsString aggregates all Added mutators, and GameWorld.cs:865-867 feeds the tokens into GameLog.Init's `:gameinfo:mutators:LIST` line. This mirrors Base server/gamelog.qc:50 exactly, including the sv_eventlog gate (default 0 in both Base and port — so the line is only emitted when event logging is enabled, faithful). ; STILL OPEN — pretty/display string: BuildMutatorsPrettyString override (`, Midair`) and its MutatorActivation aggregator exist, but th"
  ]
 },
 {
  "unit": "mutator-multijump",
  "notes": [
   "[logic/values] mutator-multijump.jump.client_gate: Per-client opt-out/cap STILL non-functional, but the dead point moved deeper: ClientMultijumpProvider is now ASSIGNED on the live path (Commands.cs:392, reading GetClientCvarFloat(p,'cl_multijump',-1)) -- the mutator-level seam the baseline flagged is closed. The remaining break is one layer down: 'cl_multijump' is missing from CmdSentCvar's SentCvarAllowlist (Commands.cs:179-198), so a client's `sentcvar cl_multijump 0/2` is silently dropped (Commands.cs:2036-2037) and never lands in _clientCvars. The provider therefore always reads its -1 fallback. Net result identical to baseline: a client ",
   "[logic/values] mutator-multijump.presentation.mutator_list_string: The serverinfo/scoreboard active-mutators list (BuildMutatorsString ':multijump' / BuildMutatorsPrettyString ', Multi jump') is not produced for this mutator. No central active-mutators aggregator exists in the port (confirmed: no BuildMutatorsString / active_mutators / mutators_list producer found in src/)."
  ]
 },
 {
  "unit": "mutator-nades",
  "notes": [
   "[values] mutator-nades.toss.projectile: Newton-style 0 is implemented as pure absolute _velocity rather than W_CalculateProjectileVelocity(e, e.velocity, _velocity, true); for non-default g_projectiles_newton_style this diverges from Base's velocity blending (style 0 is the shipped default, so identical at defaults).",
   "[logic/values] mutator-nades.boom.napalm: g_nades_napalm_ball_damageforcescale (4) is not applied: napalm is modeled as the Burning status effect with no knockback, so the fireball/fountain damage-force push (QC proj.damageforcescale) is missing. ; Water-contents velocity damping (pointcontents CONTENT_WATER -> velocity*0.5 / z=200) and the round-not-started fountain/ball delete guard are omitted; EFFECT_FIREBALL_LASER trail + EF_FLAME ball effect omitted (render).",
   "[logic] mutator-nades.boom.ice: The recently-revived skip (revival_time, time-revival_time < 1.5s) is omitted, so a just-unfrozen player can be immediately re-frozen (confirmed ice.qc:59). ; The round-not-started delete guard is omitted (relevant only in CA/round modes). The muzzleflash/icefield particle gate omitted (render).",
   "[logic] mutator-nades.boom.translocate: move_out_of_solid is a minimal upward-nudge approximation (no builtin), so embedded-in-solid recovery is less robust than Base's full search. ; MUTATOR_CALLHOOK(PortalTeleport) is not fired (no subscribers in port).",
   "[logic] mutator-nades.boom.entrap_veil_darkness: Entrap MonsterMove run/walk slow is omitted (no MonsterMove hook chain in the port). ; Veil tint of enemies (render colormod) omitted; only the teammate alpha-hide is ported. Veil monster-alpha lapse omitted. ; Darkness blind overlay (HUD_DarkBlinking full-screen fill) + SND_BLIND are CSQC-only and missing; only the NadeDarknessTime deadline is set server-side.",
   "[logic/values] mutator-nades.bonus.economy: GameRules_scoring_is_vip is treated as false: the VIP-kill medium bonus + flagcarrier time-score multiplier (g_nades_bonus_score_time_flagcarrier=2, scaled by KH key count) are not applied — the port always uses bonus_score_time. ; MonsterDies bonus is implemented (NadeBonus.OnMonsterDies) but has NO live caller — NadesMutator does not subscribe a MonsterDies hook (no MonsterDies hook chain exists), so killing an enemy monster never banks the minor bonus. ; The bonus notification + SND(NADE_BONUS) on banking a nade are not emitted (presentation/audio).",
   "[logic] mutator-nades.lifecycle.cleanup: STILL MISSING: MakePlayerObserver / ClientDisconnect / reset_map_global (nades_RemovePlayer = nades_Clear + RemoveBonus + delete nade_spawnloc) and PutClientInServer (RemoveBonus) are not ported — their HookChains do not exist in MutatorHooks. nades_Clear/RemoveBonus exist as methods but have no live caller on these paths -> a held nade, banked bonus, or spawn marker leaks across observer/disconnect/round-reset. NOW OBSERVABLE because nades can actually be held/banked (input wired). ; STILL MISSING: DropSpecialItems (the dedicated death-drop toss hook) has no port HookChain. NOTE: the death-to",
   "[logic] mutator-nades.damage.freezetag_revive: The port suppresses the self-nade damage+force but does NOT call freezetag_Unfreeze / set freezetag_revive_nade_health / send EFFECT_ICEORGLASS + INFO_FREEZETAG_REVIVED_NADE/CENTER_FREEZETAG_REVIVE_SELF notifications (documented cross-task seam — FreezeTag owns the unfreeze). So the revive-nade does not actually revive.",
   "[logic/values/timing] mutator-nades.presentation.client: No nade projectile model/trail (nade_customize / nade_spawn CSQCProjectile + Nade_TrailEffect), no orb 3D model (orb_draw), no orb 2D full-screen color flash (orb_draw2d / hud_colorflash_alpha), no darkness blind overlay (HUD_DarkBlinking) + SND_BLIND, no spawn-loc EF_STARDUST render. ; No DrawAmmoNades (cl_nades.qc:86): the ammo-panel bonus-nade count + progress bar + (rainbow) type icon is not drawn. ; The held-nade charge ring (CrosshairPanel.NadeTimer) is wired + networked AND now functional: a nade can finally be primed (input wired in Wave-5), so NadeTimer climbs 0->1 and the ring appear"
  ]
 },
 {
  "unit": "mutator-new_toys",
  "notes": [
   "[logic/values/timing] mutator-new_toys.mapreplace.set_weaponreplace: Map weapon-entity replacement is STILL entirely unported. ItemSpawnFuncs.WeaponSpawn (cs:228) -> StartItem.Spawn (cs:240) spawns the literal map weapon and never fires a SetWeaponreplace hook (no such chain in MutatorHooks — only FilterItemDefinition/SetStartItems/SetWeaponArena), never calls a W_Apply_Weaponreplace equivalent, never reads the per-entity \"new_toys\" map key, and has no random_items early-out. With g_new_toys_autoreplace 1/2 a map's weapon_vortex still spawns a Vortex; a weapon_vortex with '\"new_toys\" \"rifle\"' is ignored. ; Wave-2 only updated the NewToysMutator docstring (cs:23",
   "[logic/values] mutator-new_toys.pickup.roflsound: STILL OPEN — the 'New toys, new toys!' roflsound never plays on new-toy weapon pickup: SND_WEAPONPICKUP_NEW_TOYS is registered in SoundsList (SoundsList.cs:73) but assigned to no pickup, and g_new_toys_use_pickupsound is read NOWHERE in src/ or game/ (verified this audit). New-toy weapons use the normal WEAPONPICKUP sound. No FilterItem-for-weapon-pickup wiring was added by Wave-1/2. (Default cvar 0 so off by default; low impact.)",
   "[logic] mutator-new_toys.menu.describe_page: STILL OPEN — the MENUQC mutator describe() page (a help/info blurb in the mutator picker naming the current new-toy weapons and noting mutual exclusivity with InstaGib/Overkill) is not ported. This is a general port-wide menu-describe gap shared across all mutators, not specific to New Toys. Cosmetic."
  ]
 },
 {
  "unit": "mutator-nix",
  "notes": [
   "[logic] mutator-nix.enable.gate: IsEnabled is `Api.Cvars.GetFloat(\"g_nix\") != 0` only (NixMutator.cs:40-41). QC additionally requires `!MUTATOR_IS_ENABLED(mutator_instagib) && !MUTATOR_IS_ENABLED(ok) && !MapInfo_LoadedGametype.m_weaponarena`. With direct cvar sets, NIX can activate alongside instagib/overkill and the two fight over the per-frame loadout. The stock menu arena radio group hides this in normal play; exposure is console/config only. UNCHANGED post-Wave-5 (NixMutator.cs byte-identical to baseline). No cross-mutator MUTATOR_IS_ENABLED helper exists in the port either — InstagibMutator.IsEnabled is also bare `g_inst",
   "[logic] mutator-nix.add.wrinit: Port resets the rotation globals (nix_weapon/nextweapon/nextchange = 0) and reads cvars, but does NOT run the `FOREACH(Weapons, choosable, it.wr_init(it))` warm-init pass. QC itself comments the per-rotation re-init as 'too slow' and skips it; the ONADD warm is cosmetic/perf and has no observable gameplay effect.",
   "[logic/values] mutator-nix.remove.restore: QC restores start_ammo_shells/nails/rockets/cells/fuel + STAT(WEAPONS)=start_weapons + best-weapon per slot. Port Unhook only clears weapons, gives blaster, and SwitchToBest — it does NOT restore the start_ammo_* resources or the start_weapons set. Players leaving NIX get a blaster-only loadout, not their normal start kit. Unchanged by Wave-1/2.",
   "[logic] mutator-nix.rotation.engine: wpn.wr_resetplayer (weapon think-state reset) is not called on the per-round resync. The full-clip prefill for reloadable weapons (QC: weapon_load[nix_weapon] = wpn.reloading_ammo across slots) is also not explicit; the port relies on its lazy weapon-load seeding. Observable outcome largely preserved but not bit-identical.",
   "[logic] mutator-nix.ammo.refill_trickle: IT_UNLIMITED_AMMO branch not ported: QC fills to g_pickup_<type>_max when the player has IT_UNLIMITED_AMMO, and skips the trickle entirely in that case. Port always uses g_balance_nix_ammo_<type> and always trickles. Outcome unchanged in stock NIX (IT_UNLIMITED_AMMO is not set), so values are faithful for default play.",
   "[logic] mutator-nix.weaponset.force: QC only calls W_SwitchWeapon when `m_switchweapon != wpn` AND the player does NOT already own m_switchweapon (i.e. a switch is forced only when the current target isn't a now-owned weapon). The port re-asserts Inventory.SwitchWeapon every frame to either the kept-active (blaster/nix) or the nix weapon (lines 248-253). Benign in the common case but not bit-identical; with g_nix_with_blaster a manual mid-round switch should be sticky.",
   "[logic] mutator-nix.spawn.loadout: OnPlayerSpawn sets NixLastChangeId=-1 and calls GiveCurrentWeapon (both faithful) but does NOT set `player.items |= IT_UNLIMITED_SUPERWEAPONS` (QC sv_nix.qc:282). The port HAS a live superweapon-expiry pass (PlayerFrameLogic.SuperweaponTimeout, run every frame via PlayerPowerups at GameWorld.cs:1329) that strips superweapons unless IT_UNLIMITED_SUPERWEAPONS is set. MOOT in stock play: superweapons (e.g. Fireball, SpawnFlags = SuperWeapon|TypeSplash|NoDual, NO WEP_FLAG_NORMAL) fail CanChooseWeapon's Normal-flag requirement, so no superweapon ever enters the NIX rotation. Becomes a live bug only",
   "[logic] mutator-nix.entity.target_items: QC's OnEntityPreSpawn hook deletes any `target_items` entity under NIX (they would otherwise change weapons/ammo and fight the rotation). NixMutator has NO OnEntityPreSpawn handler, AND there is no MutatorHooks.OnEntityPreSpawn hook chain or .Call dispatch site in the port at all (the only OnEntityPreSpawn references are QC-comment annotations in GameWorld.cs:2548-2587 map spawn, not a mutator hook). target_items map objects still spawn (MapObjectsRegistry.cs:89 registers target_items -> TargetUtilities.ItemsSetup) and survive under NIX. Low impact (target_items is rare) but a genuine behavior",
   "[logic] mutator-nix.modname.strings: QC appends ':NIX'/', NIX' to the mutators string and returns 'NIX' from SetModname. POST-WAVE-5 the string infrastructure now EXISTS and is partly live: MutatorBase has virtual BuildMutatorsString/BuildMutatorsPrettyString (GameplayBases.cs:198/206), MutatorActivation.BuildMutatorsString/PrettyString iterate the active mutators (MutatorActivation.cs), a LIVE consumer feeds the event log (GameWorld.cs:865, gated by sv_eventlog), and Vampire/Midair override the virtuals. BUT NixMutator does NOT override either virtual, so NIX still appends no token to the mutators string. BuildMutatorsPrettyStri"
  ]
 },
 {
  "unit": "mutator-offhand_blaster",
  "notes": [
   "[timing] offhand_blaster.fire.offhand_think: weaponUseForbidden suppression incomplete: Base (weaponsystem.qc:610-612) clears key_pressed when weaponUseForbidden(actor) (round active-but-not-started OR the ForbidWeaponUse mutator hook). The port DOES zero OffhandFirePressed alongside the fire buttons when WeaponUseForbidden(player) is true (WeaponFireDriver.cs:72-73), BUT WeaponUseForbidden is still a stub returning false (WeaponFireDriver.cs:324) — so the round-grace / ForbidWeaponUse gate is not actually enforced. The vehicle and frozen/dead/game-stopped halves ARE handled (WeaponThink returns while p.Vehicle != null; WeaponLocked cove",
   "[logic/values] offhand_blaster.report.mutators_string: STILL MISSING after Wave-1/2/5: BuildMutatorsString/BuildMutatorsPrettyString not ported. No such hook chain or per-mutator implementation exists in src/ or game/ (grep finds only doc-comment references in Cloaked/Powerups/RocketFlying/SuperSpec mutators). The only mutator-list builder (GameLog.Init's ':gameinfo:mutators:' line) is still fed null at its sole call site (GameWorld.cs:829, line shifted) — so even that list is empty, and it is not the BuildMutatorsString hook.",
   "[logic/values] offhand_blaster.tips.gameplay_hint: STILL MISSING after Wave-1/2/5: gameplay-tips line ('offhand blaster is enabled, press <key>') not shown to the client. No BuildGameplayTipsString / gameplay-tips builder exists in src/ or game/ (grep found none), and the mutator-active bitfield (mut_is_active / MUT_OFFHAND_BLASTER=24) is not networked to the client. (The +hook bind it would reference is now LIVE as of Wave-5, but the tip text itself is still absent.)",
   "[logic/values] offhand_blaster.menu.describe: STILL MISSING after Wave-1/2/5: menu mutator-info describe text not ported. The multi-paragraph description (gives all players a Blaster offhand used with hook, can fire any time but ordinary secondary can't be used, laser-jumping, overrides the hook mutator) has no port equivalent. No 'offhand' entry exists in any menu dialog (grep game/menu/ for 'offhand' — zero matches)."
  ]
 },
 {
  "unit": "mutator-overkill",
  "notes": [
   "[logic] overkill.enable.predicate: Port enables on g_overkill != 0 only; Base also requires !MUTATOR_IS_ENABLED(mutator_instagib), MapInfo_LoadedGametype.m_weaponarena == false, and cvar_string(\"g_mod_balance\") == \"Overkill\". Port can activate Overkill rules without the Overkill balance/guards, and can double-activate alongside instagib or a weapon-arena gametype. UNCHANGED by Wave-2.",
   "[logic] overkill.onadd.precache_and_itemblock: ok_player models are never precached / forced as the default player model (precache_all_playermodels equivalent absent). ; g_overkill_items list (the random-items injection source) is never built. ; Mechanism differs: Base ONADD sets ITEM_FLAG_MUTATORBLOCKED on the four items at add-time; the port instead reads the four filter cvars in Hook() and enforces the block per-spawn in the FilterItem hook (overkill.filter row). End behavior (which items are blocked, with which defaults) now matches — but the ONADD spawnflag-blocking and ONREMOVE clear are not modeled.",
   "[logic] overkill.weapons.unblock: No ok_weapons sub-mutator gating the OK weapons behind (g_overkill_weapons || ok). Whether the OK weapons are WEP_FLAG_MUTATORBLOCKED outside Overkill — and unblocked under it — is not enforced via this hook; the OK weapons exist as direct registrations. UNCHANGED by Wave-2.",
   "[logic/values/timing] overkill.loot.drop_monster: Monsters drop no Overkill loot and their normal drop is not suppressed. No MonsterDropItem hook chain exists in the port (grep MonsterDropItem.Call → zero hits), so there is nothing to subscribe. UNCHANGED by Wave-2.",
   "[logic] overkill.respawn.remember_restore_weapon: Port records/restores only slot 0: OnPlayerDies stores Inventory.CurrentWeapon(target) into OkLastWeapon[0] and nulls slots 1..MAX_WEAPONSLOTS; Base captures every slot's m_switchweapon (ok_lastwep[MAX_WEAPONSLOTS]). Low practical impact — Overkill loadouts occupy slot 0 only — but the per-slot capture is not faithfully reproduced. UNCHANGED by Wave-2.",
   "[logic/timing] overkill.countdown.blaster: During an active-but-not-started round, pressing ATCK2 does not fire the secondary Blaster: Overkill does not subscribe the PlayerPreThink chain (which IS live — PlayerPreThink.Call at GameWorld.cs:1294). Players cannot blaster-jump during the countdown, and the button is not consumed. UNCHANGED; re-verified 2026-06-22 post-impl.",
   "[logic/values] overkill.randomitems.inject_ok_items: When random items are enabled under Overkill, the OK item set (g_overkill_items + weapon_okhmg/okrpc with their g_<prefix>_*_probability weights) is not injected into the random roll. The port's RandomItemsMutator.GetRandomItemClassName does NOT fire a mod-injection hook chain, so there is no chain for Overkill to override. UNCHANGED by Wave-2 (also gated on the missing g_overkill_items list, overkill.onadd row).",
   "[logic/values/timing] overkill.itemwaypoints.respawn: No respawn-timer waypoints are shown for HealthMega/ArmorMedium/ArmorBig/ArmorMega under Overkill. No Item_RespawnCountdown / Item_ScheduleRespawn mutator hook chains exist in the port. UNCHANGED by Wave-2.",
   "[logic/values] overkill.naming.mod_strings: Server browser / scoreboard mutator string does not show ':OK' / ', Overkill': the BuildMutatorsString / BuildMutatorsPrettyString chains now EXIST and are LIVE (MutatorBase virtuals dispatched by MutatorActivation.BuildMutatorsString/PrettyString, called from GameWorld.cs:865; other mutators e.g. Midair/Vampire override them) — but OverkillMutator does NOT override them, so it contributes no ':OK'/', Overkill' token. A one-line override would close this; user-visible behavior is still absent. ; SetModname is not set to 'Overkill': no SetModname hook chain exists anywhere in the port (grep Set"
  ]
 },
 {
  "unit": "mutator-physical_items",
  "notes": [
   "[logic/values/timing] mutator-physical_items.item_spawn.ghost_entity: Item_Spawn ghost-entity creation is entirely unimplemented: no second MOVETYPE_PHYSICS/SOLID_CORPSE entity is spawned, the real item is never hidden (EF_NODRAW/MOVETYPE_FOLLOW/aiment), so items can never become physical under any g_physical_items setting (port always shows classic spinning pickups).",
   "[logic/values/timing] mutator-physical_items.item_callbacks.think_touch_damage: physical_item_think reset logic is missing: a map item awaiting respawn is never snapped home/frozen and a knocked-around item is never returned to its origin (g_physical_items_reset has no effect); ghost is never deleted when the real item is gone. ; physical_item_touch / physical_item_damage hazard-reset is missing: an item that slides into NODROP/SKY or is hit by HURTTRIGGER/SLIME/LAVA/SWAMP is never snapped back to spawn_origin."
  ]
 },
 {
  "unit": "mutator-pinata",
  "notes": [
   "[values] mutator-pinata.death.throw_impulse: STILL OPEN after Waves 1/2 (Prandom.cs:Vec was not modified — re-verified unchanged 2026-06-22). Prandom.Vec() samples a uniform cube ([-1,1)^3, |v| up to ~1.73) with no rejection step, where QC randomvec() (the DarkPlaces engine builtin #91) samples uniformly inside the unit ball (|v|<=1). Even the QC prandomvec fallback (lib/random.qc:120, `while (v*v > 1)`) rejection-samples to the unit ball, so neither QC path matches the port. With the *175 scale, port burst weapons get up to ~303 qu/s horizontal kick vs <=175 in Base and the throw directions cluster toward the 8 cube-corner diagonals ins",
   "[logic/values] mutator-pinata.report.mutators_string: STILL OPEN after Waves 1/2 (re-verified 2026-06-22). The port has NO BuildMutatorsString / BuildMutatorsPrettyString hook chain at all: MutatorHooks.cs (incl. the new Wave-2 Fire* entry points) defines no BuildMutators* HookChain, and the only BuildMutators references in src are doc-comments noting the chains were intentionally skipped (RocketFlyingMutator.cs:23, CloakedMutator.cs:16, PowerupsMutator.cs:24, SuperSpecMutator.cs:38). Pinata's two server-side report hooks (':Pinata' machine token used by the server browser/serverinfo, ', Piñata' pretty string used by the scoreboard/HUD active-mut",
   "[logic/values] mutator-pinata.menu.metadata: STILL OPEN after Waves 1/2 (re-verified 2026-06-22). No MENUQC describe text, no MutatorPinata menu class/label, and no create-game create-mutators checkbox for Pinata: the menu would not offer a Piñata toggle. General menu gap across the port, not unique to pinata."
  ]
 },
 {
  "unit": "mutator-powerups",
  "notes": [
   "[timing] powerups.speed.attack_rate: Per-weapon animtime/refire paths still call the parameterless WeaponRateFactor() which does NOT invoke the WeaponRateFactor hook, so those weapons do not fire faster while Speed is held. Confirmed parameterless callers (UNCHANGED from baseline; re-grepped 2026-06-22): Electro.cs:140,191; Machinegun.cs:153,241; OkHmg.cs:162; OkMachinegun.cs:148; Rifle.cs:180,230; Shotgun.cs:109.",
   "[logic] powerups.invisibility.stealth: Enemy waypoint sprites of an invisible player are NOT team-restricted: a cloaked flag/objective carrier still shows on enemy radar (CustomizeWaypoint unported — no waypoint customize chain exists). ; Monsters still acquire an invisible player as a target (MonsterValidTarget has no mutator hook chain; only the monster's own MonsterAI.ValidTarget exists, which the powerups mutator cannot veto). ; Bots still shoot an invisible player. A Bot_ForbidAttack seam NOW EXISTS and is live (Server/Bot/BotBrain.cs:42 ForbidAttackHook, called at BotBrain.cs:776; wired only by the survival gametype at GameWo",
   "[logic/values/timing] powerups.strength.fire_sound: Firing any weapon while Strength is active never plays the SND_STRENGTH_FIRE cue; the prevstrengthsound/prevstrengthsoundattempt anti-spam timers are not modeled (no W_PlayStrengthSound hook / fired-weapon hook in the port).",
   "[values] powerups.item.defs_spawn_gate: g_jetpack_fuel CvarOr fallback is 100 in the port but the Base default is 8 (fuel/sec); only matters on the .count override path with the cvar unset. ; m_botvalue not set on the port pickups (Base 11000 for strength/shield/speed/invisibility, 3000 for jetpack/fuelregen) — bot item desirability may be miscalibrated.",
   "[logic] powerups.drop.ondeath_onuse: No WP_Item countdown waypoint sprite on the dropped powerup (powerups_DropItem waypoint + powerups_DropItem_Think unported — no reachable WaypointSprite API from this file). ; +use drop (g_powerups_drop, PlayerUseKey hook) is still unported. NOTE: the PlayerUseKey hook is NOW live (MutatorHooks.FirePlayerUseKey fired at VehicleBoarding.cs:106) but PowerupsMutator does not subscribe to it — only drop-on-death is live. So the seam exists; the powerups subscription is the remaining work. ; ItemTouched hook (kills the dropped powerup's waypoint sprite on pickup) unported (moot while there is no wa",
   "[logic] powerups.obituary_mutator_strings: Death log does not append 'S' (Strength) / 'I' (Shield) item codes for a victim who held the powerup (LogDeath_AppendItemCodes unported). ; Server-browser mutator strings do not include ':powerups'/'Powerups' (or the ':no_powerups'/', No powerups' variant when g_powerups==0)."
  ]
 },
 {
  "unit": "mutator-random_gravity",
  "notes": [
   "[logic] random_gravity.enable.cvar_gate: MUTATOR_ONADD cvar_settemp(\"sv_gravity\", ...) is not modelled: the original sv_gravity is not pushed onto a restore stack, so a modified gravity is not guaranteed to be reverted at match end (deferred to the host via an in-code NOTE at RandomGravityMutator.cs:46-47). UNCHANGED by Wave-1/2 — no cvar_settemp/SetTemp restore exists anywhere in src/XonoticGodot.Common.",
   "[logic/timing] random_gravity.roll.round_gate: No round-handler gate in OnStartFrame: in round modes (Clan Arena, Freezetag, round-based Domination, etc.) the port can roll gravity in the inter-round window before the round starts; Base suppresses it. STILL NOT trivially fixable: grep over src/XonoticGodot.Common for any static/global round-state provider (RoundActiveProvider/IsRoundStartedProvider/static Func<...> round) found NONE. RoundHandler (Common/Gameplay/GameTypes/RoundHandler.cs) exposes IsRoundStarted/Phase per instance but is owned per-gametype; DamageSystem documents the same unreachability. Wiring this gate needs a new global",
   "[logic/values] random_gravity.advertise.mutator_string: Random Gravity is not added to the server-info / scoreboard mutator list: neither BuildMutatorsString nor BuildMutatorsPrettyString is implemented for this mutator. grep -rn 'RandomGravity'/'Random gravity' over src found only the mutator class itself (no advertising string). UNCHANGED by Wave-1/2 (other mutators like Cloaked/SuperSpec do have these hooks, but random_gravity still does not)."
  ]
 },
 {
  "unit": "mutator-random_items",
  "notes": [
   "[logic/values] random_items.engine.mod_injection_hook: The RandomItems_GetRandomItemClassName mutator hook seam is still not modeled: GetRandomItemClassName (:177) goes straight to the vanilla pick (the docstring states the mod-injection hook 'isn't modeled'). So Overkill/Instagib cannot substitute their item pools (okhmg/okrpc, vaporizer_cells), and randomitems-overkill.cfg's overkill-specific probabilities are never consulted.",
   "[logic/values/timing] random_items.map.filter_replace: Map items are still never replaced under g_random_items 1: the port has no entity-level FilterItem mutator hook. StartItem.SpawnInternal (StartItem.cs:103) calls only the definition-level FilterItemDefinition hook (a forbid-spawn predicate used by Duel/Mayhem), which cannot replace an item with a different classname. ; The g_random_items_replace_<classname> machinery (literal 'random' vs candidate-list, tokenize + uniform pick, chosen==current -> no-op) is still entirely absent, though the replace cvars are shipped in randomitems-xonotic.cfg.",
   "[logic/values/timing] random_items.map.item_touched_rerandom: Picked-up map items still never re-randomize: there is no ItemTouch mutator-hook chain in the port. ItemPickupRules.ItemTouch:364 still explicitly notes 'the port has no ItemTouch hook chain yet', so a respawning item keeps its original identity.",
   "[values] random_items.loot.spawn_item: g_random_loot_time field default is 20 vs Base 10; the cvar override only applies when non-zero (RandomItemsMutator.cs:90-91). Latent only when the random-items cfg isn't loaded — the shipped randomitems-xonotic.cfg sets 10, and it is exec'd from mutators.cfg:552, so in normal play the value is 10.",
   "[logic/values] random_items.spawn.recursion_guard_ok_flag: None of the spawn-mechanics safety/flagging is ported: no random_items_is_spawning recursion guard (moot until the FilterItem replacement re-enters the spawn path), no ITEM_IS_LOOT skip in a (missing) FilterItem/ItemTouched, and no ok_item flag on spawned/replaced/loot items. The Entity.ItemIsLoot marker now exists (StartItem sets it), but the FilterItem/ItemTouched skips that read it are still absent. ; Because the loot half is now live, the missing ok_item tag is observable: a random-loot item dropped under Overkill is NOT flagged as an Overkill item (OverkillMutator.cs:281-282 documents the",
   "[logic/values] random_items.string.mutator_labels: Neither BuildMutatorsString nor BuildMutatorsPrettyString is ported, and the port still has NO active-mutators string builder at all (no MutatorHooks.BuildMutatorsString chain; NetName is never consumed to assemble a mutator list). So the active-mutators net string and the scoreboard 'Random items' label omit this mutator entirely."
  ]
 },
 {
  "unit": "mutator-rocketflying",
  "notes": [
   "[logic/values] mutator-rocketflying.allowrocketjumping.force_on: AllowRocketJumping not ported: the mutator still does not force the Devastator remote-jump self-boost on, and the port's Devastator still has no remote_jump_* system (no dedicated rocket-jump blast / vertical velocity shaping).",
   "[logic] mutator-rocketflying.advertise.mutator_string: No advertised mutator string / scoreboard indicator: BuildMutatorsString + BuildMutatorsPrettyString + mut_set_active are not ported, so clients never see 'Rocket Flying' listed as an active mutator."
  ]
 },
 {
  "unit": "mutator-rocketminsta",
  "notes": [
   "[logic/timing] mutator-rocketminsta.secondary.laser_barrage: No rapid-fire ramp: Base's hold-to-stream (hagar_load/jump_interval2, g_rm_laser_rapid 1 / _rapid_delay 0.6 / _rapid_refire 0.35, mode-1 single-bolt stream) is absent. The port Vaporizer has no hagar_load/JumpInterval2 state — it fires only the mode-0 fan on the 0.7s refire. ; No W_CalculateProjectileVelocity: shooter motion not added to bolt velocity (aim divergence while moving). ; g_rm_laser==2 standalone-laser mode (RM laser without g_rm) and the forced-reload-for-laser branch (wr_think:290-303, which resets hagar_load) are not modeled. ; g_rm_laser_spread_random multiplier not modeled (do",
   "[logic] mutator-rocketminsta.laser.electrobitch: The Electrobitch announcer achievement (RM laser timeout-explode landing on a flying enemy of another team) is not implemented. The port's laser Touch/Think paths just deal RadiusDamage and delete — no achievement check, and the touch/timeout paths are not distinguished (Base only awards on the timeout .use->W_RocketMinsta_Laser_Explode path, not the direct W_RocketMinsta_Laser_Touch)."
  ]
 },
 {
  "unit": "mutator-sandbox",
  "notes": [
   "[logic] mutator-sandbox.object.think_grab: WIRED + LIVE: OnStartFrame is subscribed to MutatorHooks.SvStartFrame (fired in the server tick) and ticks ObjectThink for every object over the live RealClientsProvider roster (NetGame.WireSandbox:858 — non-bot players); CryptoIdfpProvider(=PersistentId) resolves realowner from UID and clears it on disconnect. ; Sandbox_DragAllowed mutator-hook veto has no port chain; grab is gated on g_sandbox_readonly only (TODO SandboxMutator.cs:345). ; presentation: CSQCMODEL_AUTOUPDATE has no port seam (TODO SandboxMutator.cs:364); .Grab is computed but never consumed (no drag system).",
   "[values] mutator-sandbox.object.edit_properties: WIRED + LIVE: object_edit reachable via CmdSandbox → HandleCommand on the live path. ; values: physics 2 maps to MoveType.Push (engine value 7), but Base MOVETYPE_PHYSICS=32 (re-verified dpextensions.qc:1707; port EntityEnums.cs:11 Push=7) — the numeric movetype byte diverges. Behaviorally moot (no real physics-engine backs either), and the value round-trips internally consistent in save/load, but it is NOT Base-byte-identical. ; The faithful Base solidity→physics fall-through (missing break;) IS preserved (re-verified Base sv_sandbox.qc:685-697 has no break on solidity; port reproduces via go",
   "[logic/values/timing] mutator-sandbox.drag.grab: STILL MISSING: no Drag/Drag_Begin/Drag_Finish system. ObjectThink computes .Grab (e.Grab, written at SandboxMutator.cs:343-347) but nothing consumes it; +button8 does nothing; objects cannot be physically carried. (grep: no Drag_Begin/Drag_Finish/button8 grab consumer anywhere; e.Grab is written but never read.)",
   "[logic] mutator-sandbox.hooks.gating: WIRED + LIVE: HandleCommand is now reachable (CmdSandbox), so the g_sandbox_readonly gate (commands rejected with the read-only notice) fires on the live path; g_sandbox_info gates LOG verbosity faithfully. ; The three Sandbox_* veto hooks (DragAllowed/SaveAllowed/EditAllowed) have NO port hook-chain (TODOs at SandboxMutator.cs:345/534/610) — no other mutator can gate sandbox. Each call site is approximated as 'readonly only'."
  ]
 },
 {
  "unit": "mutator-spawn_near_teammate",
  "notes": [
   "[logic] mutator-spawn_near_teammate.bias.spawn_score: checkpvs(spot.origin, teammate) visibility gate is still skipped (no mutator-reachable PVS query, flagged inline at line 121): spots are biased toward / msnt_lookat set to teammates not visible from the spot — a superset of Base's chosen set. (Unchanged from baseline — still open.)",
   "[logic] mutator-spawn_near_teammate.relocate.eligibility: weaponLocked(teammate) gate is always false in the port (WeaponLocked returns false; no per-player weapon-lock reachable): may relocate beside a mate Base would skip. (Unchanged from baseline — still open, minor.) ; PHYS_INPUT_BUTTON_CHAT (teammate typing) gate is always false in the port (IsChatting returns false; no chat-button input plumbed): may relocate beside a typing mate Base would skip. (Unchanged from baseline — still open, minor.)",
   "[logic] mutator-spawn_near_teammate.relocate.spot_search: No hurt-trigger reject (tracebox_hits_trigger_hurt, QC line 144): the player can still be relocated inside a trigger_hurt and take damage on spawn. (Deferred — no clean mutator-reachable equivalent; flagged inline at line 324.) ; No nade-in-range reject (g_nades, IL_EACH(g_projectiles) within g_nades_nade_radius 300, QC lines 153-163): with nades enabled the player can still be relocated next to a live nade. (Deferred — needs the projectile entity list; flagged inline at line 333.)",
   "[logic] mutator-spawn_near_teammate.relocate.commit_closetodeath: closetodeath (default ON) uses the player's CURRENT origin (player.Origin) instead of player.death_origin — confirmed there is NO .death_origin / DeathOrigin field anywhere in the port (only an unwired Chat.cs '%d' fallback also uses self.Origin), so 'spawn as close to where you died as possible' measures distance from the wrong reference and can select a different teammate than Base among several candidates. This is the DEFAULT relocate behavior. (Unchanged from baseline — still open.)"
  ]
 },
 {
  "unit": "mutator-stale_move_negation",
  "notes": [
   "[logic/values] smneg.mutator_string: BuildMutatorsString/BuildMutatorsPrettyString unported: mutator never appears in the server-browser / mutator-list string. STILL OPEN after Wave-1/2: no BuildMutators/BuildMutatorsPretty HookChain exists in MutatorHooks.cs (the 30 chains there cover damage/spawn/physics/alpha/vehicle/etc. but not the mutator-string event). All BuildMutatorsString references in src/ (Cloaked, Powerups, RocketFlying, SuperSpec) are comments noting the feature is unported."
  ]
 },
 {
  "unit": "mutator-status_effects",
  "notes": [
   "[values] status_effects.framework.registry: StatusEffectDef still drops m_color/m_icon/m_name (HUD reconstructs label/icon/color from a separate PowerupMeta table in PowerupsPanel). m_sound_rm/m_persistent/m_tick methods are carried on the def, and the m_sound_rm names now use the registered SND_* def keys (BURNING_REMOVE/STUNNED_REMOVE/POWEROFF) that all resolve — closing the data-poverty gap except the cosmetic m_color/m_icon/m_name.",
   "[logic] status_effects.spawnshield_stunned_superweapon: Player spawnshield is STILL not the spawnshield status effect — it remains the bespoke Entity.SpawnShieldExpire timer (SpawnSystem:605, read by DamageSystem.HasSpawnShield). The registered spawnshield def is only applied to monsters via MonsterFramework, and its OnTick (SpawnShieldTick) is a no-op, so the EF_ADDITIVE|EF_FULLBRIGHT post-game_starttime shimmer is never produced for any entity. (UNCHANGED.) ; Stunned EF_SHOCK aura still not applied (presentation: missing). StunnedTick self-removes while frozen (logic correct). The removal sound is now correct: RemovalSound='STUNNED_REMOVE' resolv"
  ]
 },
 {
  "unit": "mutator-superspec",
  "notes": [
   "[logic/values] superspec.ui.mutator_strings: No BuildMutatorsString / BuildMutatorsPrettyString hook chains exist in MutatorHooks.cs: superspec never appears in the machine mutator tag or the pretty active-mutator label (server browser / scoreboard / serverinfo). The SuperSpecMutator class summary itself lists this as a remaining todo."
  ]
 },
 {
  "unit": "mutator-touchexplode",
  "notes": [
   "[logic] touchexplode.blast.deathtype: Obituary still mis-resolves: Wave-2 added deathTag 'touchexplode' (TouchExplodeMutator.cs:121), and WeaponSplash.RadiusDamage routes a non-null deathTag straight to Combat.Damage(e, ..., tag, ...) (WeaponSplash.cs:208-214), so the kill is now TAGGED + processed through the special-death pipeline. BUT 'touchexplode' is NOT a registered DeathTypes constant and is absent from both DeathTypes._registry (DeathTypes.cs:330) AND the SelectSpecial switch (DeathMessages.cs:209-225), so DeathMessages.SelectSpecial('touchexplode', murder) falls through `_ => GENERIC` -> DEATH_SELF_GENERIC / DEATH_MURDER_",
   "[logic] touchexplode.list.activemutators: Base appends 'Touch explode' to the active-mutators string (util.qc:315, MUT_TOUCHEXPLODE) used for the server-browser mutator field and the scoreboard/info mutator list. The port still has no MUT_-style active-mutators string builder, so an active touchexplode game is not advertised in any mutator list. Systemic gap shared by all mutators (the whole MUT_* enumeration is unported); only the create-game menu checkbox (DialogMutators.cs:129) exists. Unchanged by Wave-1/2."
  ]
 },
 {
  "unit": "mutator-vampirehook",
  "notes": [
   "[logic/values] mutator-vampirehook.drain.engine: Entry gate `time < game_starttime` (block during pre-match warmup/countdown) is still replaced with `VehicleCommon.GameStopped` (sv_vampirehook.qc:17 vs VampireHookMutator.cs:84). GameStopped is end-of-match/intermission; there is no game_starttime/warmup-start concept in the port. Conditions differ: the port lets the drain run during warmup and blocks it at match end. Not flagged intentional; reads as a mistranslation. ; Damage attribution: Base passes `WEP_HOOK.m_id, DMG_NOWEP` (hook id flagged no-weapon) (sv_vampirehook.qc:30); port passes DeathTypes.FromWeapon(\"hook\") only (VampireHookMuta",
   "[logic/values] mutator-vampirehook.drain.heal_indirection: Port substitutes raw GiveResourceWithLimit(Health, steal, 5) for Base Heal(), bypassing the event_heal/PlayerHeal indirection AND Heal's own game_stopped/spectator/FROZEN/IS_DEAD guards (damage.qc:951-953). ; Dead-target floor diverges: PlayerHeal (player.qc:618) no-ops when health <= 0 (also when health >= limit); the port's GiveResourceWithLimit has no health<=0 floor, so it would heal a dead target up to the cap. (When health >= limit, GiveResourceWithLimit computes amount = limit-current < 0 and GiveResource's `amount<=0` guard makes it a harmless no-op, matching Base there.)"
  ]
 },
 {
  "unit": "mutator-walljump",
  "notes": [
   "[logic] mutator-walljump.predict.shared_impulse: The wall-jump velocity impulse is dispatched only via the static MutatorHooks.PlayerJump chain, which is subscribed by MutatorActivation.Apply() — called ONLY in GameWorld.cs:539 (server). Client prediction (EntityMovementStep.Step -> Movement.Move -> PlayerPhysics.PlayerJump -> PlayerJump.Call) shares the same static chain, so in a single-process / listen-server build the chain is populated and the wall jump IS predicted; but on a separate (dedicated-server) client process the chain is empty, so the wall jump is applied by the server only and the client mispredicts (rubberbands) on every wall",
   "[logic] mutator-walljump.fx.anim_and_oldvel: animdecide_setaction(player, ANIMACTION_JUMP, true) not triggered on a wall jump (no anim-action seam in the port; WalljumpMutator.cs:116 is a comment only). UNCHANGED. ; oldvelocity is mis-stashed: port writes player.OldOrigin = player.Origin (wrong field AND wrong value); Base writes player.oldvelocity = player.velocity (the POST-impulse velocity). The shared Entity (Framework/Entity.cs) STILL has no OldVelocity field — only Origin, OldOrigin, Velocity, Angles, AVelocity — so the Base value is unrepresentable here. (OldVelocity exists on ServerPlayerState and VehicleCommon, but not on the sh"
  ]
 },
 {
  "unit": "mutator-waypoints",
  "notes": [
   "[logic] waypoints.server.manager_api: The DEPLOY + CLEAR + HELPME slice is now LIVE: DeployFixed/DeployPersonal/Attach/ClearPersonal/ClearOwned/HelpMePing are called from Commands.cs (the waypoint_* client commands), Reset() on map load (GameWorld.cs:524) and Think() per tick (GameWorld.cs:1420) run, and SendWaypoints merges WaypointSprites.Active per peer. The deploy/attach/lifetime/fade lifecycle is exercised. ; STILL DEAD (objective path): SpawnFixed/AttachCarrier/UpdateSprites/UpdateHealth/UpdateRule/UpdateTeamRadar/UpdateBuildFinished/UpdateMaxHealth/Ping/Disown/Kill/DetachCarrier have zero live callers — the gametype produce",
   "[logic] waypoints.gametype.collect: Three live objective producers now: CTF (flag base/dropped/carrier), Domination (control-point markers), and FreezeTag (Frozen/Reviving overhead sprites with thaw-progress health bars) — all reached via ServerNet.SendWaypoints:924 → GameType.CollectWaypoints per tick (was CTF-only at baseline, CTF+Dom mid-wave). ; Still missing: Onslaught control points + generators, Key Hunt keys + carriers, Assault defend/destroy/push, Keepaway/TeamKeepaway/Nexball balls + goals, Race start/finish/checkpoint, LMS leader (base no-op CollectWaypoints). ; itemstime (item-respawn) spectator waypoints, monster/tu",
   "[logic] waypoints.visibility.rules: Personal-waypoint (showto = owner) restriction is now modeled: DeployPersonal sets VisibleForPlayer = owner-only and WaypointVisible (ServerNet.cs:974) honors the predicate before the rule switch. NEW since baseline. ; SPRITERULE_DEFAULT: port (ServerNet.WaypointVisible:979) returns visible-to-everyone unconditionally; QC restricts a team-set Default waypoint to the same team AND requires the viewer be a live player. UNCHANGED. ; SPRITERULE_SPECTATOR: port = observer-only (peer.IsObserver). QC gates on sv_itemstime (hide from everyone when ==0, show in warmup or when ==2). Port ignores sv_item",
   "[logic] waypoints.visibility.three_image_rule: Port still networks a single SpriteName; the own-team/enemy-team/spectator image triple (model1/2/3) and the client-side SPRITERULE_TEAMPLAY image selection are not modeled.",
   "[logic/values/timing] waypoints.client.fades: distance-fade, edge-fade, crosshair-fade (alpha+scale ramps) still entirely unimplemented; only a raw max-distance cull + a single distance ramp (exponent 2) + the live g_waypointsprite_alpha multiply. UNCHANGED. ; Lifetime fade is server-computed (FadeAlpha) and networked rather than client-computed from fadetime/lifetime**timealphaexponent; timealphaexponent (1) not applied. UNCHANGED. ; Blink: only helpme dim-flash modeled (0.45 in the off-half), differing from QC's *2 bright-half scheme; per-def blink (now correct in the registry) is NOT applied at draw time. UNCHANGED."
  ]
 }
]