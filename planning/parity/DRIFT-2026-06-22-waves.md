# Parity DRIFT — Waves 1-2 verification (FULL: 133/134 units)

_Wave-4 adversarial re-audit (parity-diff update mode), branch claude/parity-port-waves._

**Headline:** ~545 baseline gaps verified CLOSED · 11 regressions · ~885 still open, across 133 re-audited units.

## Gaps confirmed closed (by unit, most first)

### `weapon-crylink` — 14 closed / 6 open
- Impact-SOUND bug closed: primary now plays weapons/crylink_impact.wav, secondary crylink_impact2.wav (was hardcoded impact2 for both modes in OnTouch + LinkExplode). Both .ogg samples verified present.
- Convergence join-explode fully implemented (was missing/dead): ScheduleLinkJoinEffect spawns the meeting-time linkjoineffect think, evaluates the >=2-converged (n/shots)-scaled joinexplode RadiusDamage, and emits EFFECT_CRYLINK_JOINEXPLODE at the CONVERGENCE point. joinexplode_* cvars now read in Configure.
- Wrong-trigger particle removed: CRYLINK_JOINEXPLODE no longer emitted from LinkExplode (chain detonation); it now fires only from the convergence think, exactly as Base.
- WouldHitFriendly friendly-gate now ported and gates linkexplode==1: secondary chains refrain near teammates (was unconditional like Base's ==2). Reachable at stock balance.
- Chain-detonate ordering fixed: now checked FIRST on any damaging touch before the finalhit branch, matching QC's totaldamage-first ordering (was only inside the finalHit branch).

### `mutator-sandbox` — 13 closed / 13 open
- core.registration: missing→faithful AND na→LIVE. SandboxMutator.cs is [Mutator]-attributed; MutatorActivation.Apply() (live at GameWorld.cs:535) activates it on g_sandbox 1, and its SvStartFrame think/autosave tick is genuinely pumped (MutatorHooks.FireStartFrame in the GameWorld loop). The only end-to-end-live feature.
- All 13 gameplay features (spawn/think/touch/edit_get/edit_properties/scale/attach/remove/duplicate/claim_info/storage/hooks) went logic missing→faithful: a single self-contained SandboxMutator.cs faithfully ports every handler, the textual ObjectPortSave/Load serializer (%.9v vectors, ftos, slot layout, db columns), RoundPerfectVector (#2742), and the Base solidity→physics break-fallthrough quirk.
- Every sandbox cvar default (15 + cl_sandbox_clipboard) verified byte-identical to Base mutators.cfg:95-110.
- storage.persist autosave TICK is on the live SvStartFrame chain with autosave_time seeded faithfully (na→partial liveness).
- readonly + g_sandbox_info cvars are now actually read and gate the (reachable-portions-of) command/grab/log logic (missing→partial).

### `vehicle-bumblebee` — 12 closed / 3 open
- death: DamageVehicle/Death now LIVE on the real combat path — Wave-2 added `vehic.GtEventDamage = VehicleCommon.EventDamage` in SpawnVehicle (VehicleCommon.cs:367); DamageSystem.EventDamage (DamageSystem.cs:331) routes any shot non-player edict to it, so a shot bumblebee runs shield/health split + eject + blowup + respawn. Blast 500/100/500/force600, diethink 2+random*8 all faithful.
- board.usekey: corrected a BASELINE AUDIT ERROR — the +use rising-edge -> VehicleBoarding.UseKey dispatch lives in game/net/ServerNet.cs:1256 and :1362 (both input paths) and was already on main; the baseline grep was scoped to src/ and missed game/net/. The whole boarding chain is live.
- All seated behaviors flipped dead->live as a consequence: pilot.flight, pilot.raygun, gunner.frame, gunner.cannon, regen, multiseat.enter, multiseat.exit (boarding live + GameWorld.cs:1097 seated-drive gate feeds VehInput to the vehicle Think).
- spawn.swim_liquidmask: implemented + live — Bumblebee.cs:139 sets DpHitContentsMask |= WATER|SLIME|LAVA when g_vehicle_bumblebee_swim==0 (was missing).
- spawn.vr_spawn: the '0 0 25' spawn lift is now applied (Bumblebee.cs:148) — server state fully faithful (cosmetic sub-entities + scale 1.5 still missing).

### `keyhunt` — 11 closed / 9 open
- notify.center_info (the biggest baseline gap): KeyHunt.cs now sends the full notification storm on live paths - START/SCAN/ROUNDSTART center-prints, INTERFERE/MEET/HELP deferred (time+0.2) volley, PICKUP/DROP/LOST/DESTROYED/CAPTURE info lines - plus the NEXUIZPLASMA capture-trail VFX + KH_CAPTURE jingle. All 36 KEYHUNT_* notifs + KH_* sounds were verified registered.
- Every wrong default fixed: maxdist 4000->150 (capture meet-up restored), score_collect 1->3, score_carrierfrag 1->2, delay_return 15->60, key bbox -10..10 -> -25..25 (matches current Base const). Confirmed against Base gametypes-server.cfg + sv_keyhunt.qc consts.
- alarm.siren: SND_KH_ALARM now plays every 2.5s on the anchor carrier while one team owns all keys (was totally silent).
- loss.timeout_return + loser_team_push_destroy: a timed-out key now routes through LoserTeam = destroy split + INFO_KEYHUNT_DESTROYED + SND_KH_DESTROY + TE_EXPLOSION + FinishRound, matching Base kh_LoserTeam (was a silent reset).
- drop.on_death: keys now fling with dropvelocity 300 via the makevectors(-1 0 0*(45+45rand)+0 360 0*rand) physics, plus SND_KH_DROP + INFO_KEYHUNT_LOST (was placed at origin, silent).

### `invasion` — 10 closed / 9 open
- invasion.rules.no_pvp_damage / no_regen / start_items: all three co-op rule hooks (Damage_Calculate PvP-cancel, PlayerRegen disable, ROUND 200/200 SetStartItems) are now subscribed in Activate AND have verified LIVE dispatch sites (DamageSystem.cs:256, PlayerFrameLogic.cs:46, SpawnSystem.cs:711) — missing→faithful+live.
- invasion.bots.targeting + invasion.spawn.chosen_monster monster_attack: OnPlayerSpawn flags players EntFlags.NoTarget (live via ClientManager.cs:570), honored by BOTH MonsterAI.ValidTarget:562 and BotBrain:760 — one mechanism makes monsters AND bots ignore players (flagged intended_divergence, behaviorally equivalent to bot_attack=false + Bot_ForbidAttack).
- invasion.round.handler_wiring: dead→live. Invasion now owns its own RoundHandler (Activate:323) wired with the REAL CheckPlayers/CheckWinner/RoundStart callbacks + warmup=10/round_timelimit=120/delay=5, ticked on the live path (inv.Tick→Handler.Tick from DriveGametypeFrame:1749). Self-drive pattern like Survival; the stale GameWorld bare-EnableRounds default handler is now an inert sidecar, not the driver.
- invasion.round.timeout: round time limit now genuinely fires — CheckWinner wipes monsters + re-arms on RoundEndTime expiry (logic/timing missing→faithful+live); only the ROUND_OVER notification SEND remains dead.
- invasion.round.fill_and_win_round_mode: spawn-delay default corrected 2.0→0.25; per-round winner now computed (TopKillsPlayer) + announced; end-on-maxrounds corrected (round counter is a limiter, point limit ends ROUND).

### `monster-framework` — 10 closed / 9 open
- spawn voice cue (monstersound_spawn) now emitted at Setup, live via all 5 monsters' Spawn->MonsterAI.Setup
- miniboss EF_RED red glow set at setup (MonsterAI.cs:254) AND re-applied on respawn (1683); verified networked end-to-end (ServerNet.cs:1727 -> client EF_RED=128 dynamic light CsqcModelEffects.cs:120)
- pain voice now honours the 1.2s msound_delay throttle (delaytoo) instead of an ungated machine-gunnable Sound.Play
- all voice cues now routed through the MonsterSound helper which applies the g_monsters_sounds master gate + throttle (sight/idle/melee/death also converted)
- melee-success voice cue now fires on a connecting melee hit (live via Spider/Zombie/Mage combo callers)

### `nexball` — 10 closed / 4 open
- ball.lifecycle: the full QC timed state machine is now ported and LIVE — delay_start release (InitBall), the 4-step velocity-glide home (ResetBall cnt 2->4), idle reset (delay_idle+3), driven each tick by SimulationLoop.RunThink/MoveTypePhysics.RunEntity (missing->faithful, na->live).
- ball.football_kick: footballs are now kickable toward a goal — pusher/team set, mode-2 yaw-only boost (boost_forward 100 / boost_up 200) + -250 backspin + 0.1 s bounce-sound throttle, fired via the ball's TouchAreaGrid (missing->faithful, na->live). This was the headline 'footballs unplayable' gap.
- ball.basketball_pickup: delay_collect (0.5 s) dropper cooldown, the ball.cnt!=0 guard, and the carrier-already-holding -> football bump are all present now (partial->faithful).
- goal.touch_scoring: delay_goal (3 s, 0 for OUT), ball.cnt guard, basketball->football control conversion during reset, GOAL_TOUCHPLAYER carrier-drop, and a goal sound are now wired; the free-ball goal route is reachable because the ball can finally be propelled (timing partial->faithful, liveness partial->live).
- goal.limit_win: GameRules_limit_lead (goalleadlimit) branch is now implemented in CheckGoalLimit (was absent).

### `turret-mlrs` — 10 closed / 6 open
- Target-select biases now match Base exactly (rangebias 0.25 / samebias 0.5 / anglebias 0.5 / missilebias 0 / playerbias 1) — passed into TurretParams and consumed in ScoreTarget; previously all fell back to 1.0
- target_range_optimal 1500 -> 500 (ideal-kill-range scoring now Base-correct)
- TFL_AIM_ZPREDICT removed (zPredict:false) so aim_flags = LEAD|SHOTTIMECOMPENSATE + auto-SPLASH, matching Base mlrs
- aim_maxpitch 30 -> 20; track_accel_rot 0.7 and track_blendrate 0.2 now explicitly passed (were framework defaults 0.5/0.35) — FluidInertia head slew now matches mlrs cfg
- shot_spread 0.0125 -> 0.05 (rockets no longer ~4x tighter than Base)

### `clanarena` — 9 closed / 6 open
- loadout.start_items: CA's defining 200/200 + full ammo (60/320/160/180/0) now applies on every spawn via the LIVE SetStartItems hook (ComputeStartItems fires it; ApplyStartLoadout on the non-warmup spawn path) with values from the shipped balance-xonotic.cfg. dead->live, faithful.
- loadout.weapon_arena: the 'most' weapon arena is now LIVE via the SetWeaponArena hook; ExpandWeaponArena('most') grants the full WEP_FLAG_NORMAL arsenal. missing->live/faithful.
- round.resolution + round.timelimit_stalemate: the LIVE Server RoundHandler now uses ca.CheckTeams (canStart) and ca.CheckWinner (canEnd) instead of the generic defaults, so round wins, the round-timelimit branch, and PreventStalemate (survivor/health bits) all run on the real match path. RoundEndTime is mirrored into ca.Handler each frame. dead->live/faithful.
- round.checkteams_gate: canRoundStart is now CA's faithful CheckTeams (every active team must have a live player), faithful for 3-4 teams, not just 2. partial->faithful.
- round.lifecycle: countdown is now 10s (g_ca_warmup), end-delay 0 (g_ca_round_enddelay), timelimit 180 (g_ca_round_timelimit) - CA's own cvars reach the live handler via EnableRounds + the loaded cfg tree, replacing the generic 5/5/180.

### `keepaway` — 9 closed / 4 open
- ball.spawn: SpawnBall is now LIVE — GameWorld.Boot (the NetGame match-start path) procedurally spawns the single 'keepawayball' at a random map location via the shared BallEntity framework; bbox/EF_DIMLIGHT/trail-color/damageforcescale all match Base.
- ball.pickup: the live engine trigger-touch pass dispatches the ball's touch -> PickUp now actually fires; attach (SOLID_NOT/MOVETYPE_NONE), KEEPAWAY_PICKUPS column, SND_KA_PICKEDUP, all three pickup notifications, and the 0.5s self-recapture lockout are faithful + live.
- ball.respawn: loose ball now RELOCATES to a random spawnpoint (+'0 0 200' kick) on its 10s timer and plays SND_KA_RESPAWN; the old re-bounce-in-place is gone.
- ball.drop (death): drop velocity now includes the crandom xy scatter, the previous_owner/0.5s lockout is armed, SND_KA_DROPPED + INFO/CENTER_KEEPAWAY_DROPPED all fire on the live death bus.
- score.timepoints + score.bctime: both now have a real carrier (pickup is live), so the per-second SCORE accrual and the BCTIME secondary column actually award; Tick is on the live DriveGametypeFrame path.

### `tka` — 9 closed / 6 open
- tka.ball.spawn: SpawnBall is now LIVE - GameWorld.cs:589 calls tka.SpawnBall(RandomMapLocation) at map load via the new shared BallEntity framework (BallEntity.cs); a real keepawayball edict now exists with orbblue.md3, EF_DIMLIGHT(8), trail 254, damageforcescale 2, bbox +-24 - all matching Base g_tkaball_* defaults.
- tka.ball.respawn: now relocates to a random map location (BallEntity.Relocate/RandomMapLocation) on the CORRECT cvar g_tkaball_respawntime (was reading g_keepawayball_respawntime), plays SND_KA_RESPAWN, and is reachable on the live ball.
- tka.ball.pickup: GiveBall is now live via the ball's BallTouchEntity touch handler - pickup sound, INFO/CENTER_KEEPAWAY_PICKUP[_SELF] notifications, TKA_PICKUPS+1, 0.5s self-recapture lockout, and carrier attach all fire.
- tka.ball.drop: death-drop is now live+faithful - OnDeath->DropBall->BallEntity.DropFromCarrier with the crandom xy scatter, 0.5s lockout, SND_KA_DROPPED + drop notifications.
- tka.score.teamkill + tka.score.bckill: now read the correct g_tka_* cvars (defaults 1, was g_keepaway_*/0), enforce the DIFF_TEAM team-kill exclusion, and award bckill independent of attacker-carry - and are live now that a carrier can exist.

### `weapon-electro` — 9 closed / 2 open
- orb shoot-down-into-combo is now LIVE: DamageSystem.EventDamage dispatches a non-player victim's GtEventDamage, and Projectiles.MakeShootable(orb, exception:1) wires the W_CheckProjectileDamage gate + HP-subtract + ProjectileDamage explode (was dead, only BreakablehookMutator ever called it)
- in-flight bolt + orb are NO LONGER invisible: ProjectileCatalog/ProjectileRenderer (game/client/, on main since 2026-06-12) render the elaser/ebomb glow body + TR_NEXUIZPLASMA blue plasma trail + dynamic light; orb gets the electro_fly looping sound. The prior 'fabricated ProjectileCatalog' claim only searched src/ and was wrong
- orb bounce VALUES are now live: MoveTypePhysics Bounce case reads ent.BounceFactor/BounceStop, so the orb's 0.3/0.05 apply instead of the hardcoded engine 0.5 / 0.075 (cross-cutting engine fix)
- all electro detonations now play sound via WeaponSplash.ImpactSound: bolt+orb electro_impact, combo electro_impact_combo (every blast was silent before)
- combo_speed now seeded 2000 (was permanently 1000 because Configure never set ComboSpeed) -> chain ripple at correct speed; combo_comboradius_thruwall LOS abort added

### `domination` — 8 closed / 6 open
- capture.audio (dead->live): DOM_CLAIM now plays positionally on the capturer via AnnounceCapture; verified Base default dom_team capsound=SND_DOM_CLAIM and noise1="" (no narration sound), so the port is fully faithful including correctly omitting play2all.
- capture.notification (dead->live): INFO_DOMINATION_CAPTURE_TIME now broadcast on capture with matching args (netname, point .message, points, rate) and matching 2-string/2-float registration.
- hud.modicons_pps (dead->live): full chain wired -- server WriteDominationPps + Kind.Domination=6, per-peer GametypeStatusBlock.Capture on change, NetGame case Domination -> SetDominationPps -> DrawDomItem. Panel now shows (residual: pps numerator uses global amt/rate not per-point, so default magnitudes ~2.5x high).
- waypoint.sprite (dead->live): CollectWaypoints emits a fixed sprite per point (DomNeut/cyan -> DomRed/Blue/Yellow/Pink + team radar color, RADARICON_DOMPOINT, +32z), fed live through ServerNet.SendWaypoints like CTF flags.
- roundbased.win: cumulative 5-cap match win now enforced -- CheckRoundWinner latches MatchEnded/LeaderTeam off ST_DOM_CAPS at g_domination_roundbased_point_limit; GameWorld reads them.

### `monster-mage` — 8 closed / 8 open
- attack.spike values: all 7 inline constant defaults now match Base (accel 800->480, decel 1000->480, turnrate 0.6->0.65, smart 0->1, mindist 256->600, trace 100/500->1000/2500); the dangerous smart=0 default that would have disabled obstacle avoidance is fixed
- attack.spike audio: SND_MageSpike_IMPACT (weapons/grenade_impact.wav, CH_SHOTS) now played on detonation via a live onExplode hook (SpawnProjectile.Explode invokes it) — was silent
- attack.spike muzzle origin: FireSpike now re-derives origin+fwd*14+'0 0 30'+right*-14 via AngleVectors, matching Base exactly (was missing the +30 up / -14 right offsets)
- attack.teleport logic+values: the random-relocate branch (teleport_random 0.4 -> blink to a random clear grounded spot within +/-1200) is now ported as RandomRelocate; inline delay 2->5 and chance now read cfg — was deterministic-behind-blink-only
- attack.teleport audio: the wrongly-invented monsters/mage_sight.wav voice on teleport removed (QC plays no sound)

### `mapobject-teleporters-portals` — 8 closed / 9 open
- telefrag_teamplay: the inverted+inert teammate-spare bug is genuinely fixed and live — g_telefrags_teamplay registered (Cvars.cs:198, default 1) and Telefrag uses `spareTeammates = GameScores.Teamplay && cvar!=0` then `if(spareTeammates && head.Team==player.Team) continue`, the exact Base tdeath condition (git diff ec788f7 confirms the prior code was Cvar(...,0f)+!teamTelefrags).
- telefrag_cvars: g_telefrags / g_telefrags_teamplay / g_telefrags_avoid all REGISTERED (Cvars.cs:197-199); g_telefrags is now read live in the tdeath gate so a `set g_telefrags 0` applies.
- active_gate: TeleportActive now applies the team-ownership gate + INVERT_TEAMS (Teleporters.cs:122-129), so a team-claimed teleporter relocates only matching-team players server-side (was previously ungated).
- team_claim: trigger_teleport_use is now teamplay-guarded (`if(GameScores.Teamplay) self.Team=actor.Team`) AND actually enforced via the new TeleportActive team gate.
- fx.flash: exit flash now offset to+toForward*32 and BOTH end-flashes emitted inside TeleportPlayer under the 0.2s pushltime debounce, gated by FlagParticles — matches teleporters.qc:100-105.

### `turret-walker` — 8 closed / 7 open
- setup.tr_setup: the spurious SelectAngleLimits was removed from the walker's Select flags (now Players|RangeLimits|TeamCheck|Los), exactly matching Base tr_setup.
- combat.brain: targetscan_maxdelay is now cvar-backed defaulting to 1 (TurretAI.cs:553), was hardcoded 0.6 — the framework-wide rescan cadence now matches Base.
- drive.chase: body-yaw turn is now raw degrees per think via bound(-turny, ShortAngle, turny) with NO frameTime factor (ApplyGait); the old turny*frameTime was removed. At 60fps the chasing body no longer turns ~60x too slow.
- drive.idle_roam: the entire no-enemy IDLE ROAM/WANDER + LAST-SEEN PURSUIT subtree (LOS-probe wander heading, 0.9/0.1 moveto blend, chase last spot ≤10s/within 128u, stop-idle 1+rand*5) is now ported and live in IdleSelect.
- drive.gaits_extra: the SWIM gait logic + constants (waterlevel anim select, pitch clamp bound(-10..10), sin(t*4)*8 bob) are now ported faithfully.

### `weapon-arc` — 8 closed / 9 open
- checkammo: primary beam gate fixed cells>=6 -> (beam_ammo==0 || cells>0), so a player with 1-5 cells now keeps firing instead of auto-switching (the confirmed bug); secondary burst-beam branch now overheat_max>0 && (burst_ammo==0||cells>0). Live-dispatched at WeaponFireGate.cs:493. Row -> faithful.
- bolt.explode_bounce: shoot-down now subtracts damage from RES_HEALTH and only detonates at HP<=0 via Projectiles.MakeShootable/ShootDown (live through DamageSystem non-player GtEventDamage dispatch); BoltTouch now keys solely on TakeDamage==Aim. Both baseline gaps closed; row -> faithful.
- beam.heal_teammates: health heal now uses GiveResourceWithLimit (clamps to hmax / LimitNone) instead of DROPPING the heal past the cap; armor heal now refreshes PauseRotArmorFinished. Both baseline gaps closed; logic -> faithful.
- beam.heat_overheat: overheat-jam duration now time + heat/cooldown_speed (not fixed overheat_max); CooldownSpeed now picks per-state cooldown / heat/beam_refire / 0-while-bursting matching arc.qc:225-231. timing -> faithful (2 of 4 gaps closed).

### `bot-ai` — 7 closed / 15 open
- weapon.reload: idle-reload is now implemented AND genuinely wired (missing/dead -> faithful/live). Verified ThinkProduce calls IdleReload on the real match path (BotPopulation.cs:221 -> GameWorld.cs:1066); skill>=2 reloads a partial held clip via impulse 20, which reaches WeaponImpulses.ReloadHandle -> W_Reload (not a no-op), and skill>=5 pre-rotates to an under-loaded owned Reloadable weapon.
- skill.modifiers: autoskill/skill_auto is ported line-for-line vs Base bot.qc:569-613 and driven every 5s from ServerFrame when skill_auto!=0 -- a genuine live closure of the residual gap. Band (+/-2), caps (17/0), middle-band counter retention, and no-human/no-bot reset all match Base. (Single-knob skill remains the intended divergence.)
- aim.aimdir: the aimskill constants (offset/think/mouse/fixedrate/blendrate + 5 mix + 5 filter coeffs) are now read each frame from the bot_ai_aimskill_* cvars via Cvars.FloatOr, so server-side aim retuning works -- closes the 'hardcoded, cvars dead' gap. Defaults still equal stock.
- target.chooseenemy: 2 of 3 gaps closed -- the sticky-enemy window (bot_ai_enemydetectioninterval_stickingtoenemy, +0.5s LOS-trace hold within 1000qu) and the SUPERBOT health+armor target rating bound(50,hp+armor,250)*dist^2 are both ported and live (Base vlen2 = squared distance matches the port's LengthSquared). Transparent/breakable second scan still open.
- population.namemodel: bots.txt forced-team column (argv5, 1..4) is now parsed and applied via SetBotForcedTeam + AssignBestTeam on the live bot-add path (teamplay only, not under bot_vs_human/2-team). Skin/skill columns still unparsed.

### `mapobject-func` — 7 closed / 9 open
- func_button shootable rewrite VERIFIED LIVE+FAITHFUL: the old accumulate-then-die Combat.Death workaround is gone; ButtonDamage (Buttons.cs:205) is now a per-hit event_damage shim installed via MapMover.InstallEventDamage and dispatched on the real Damage() path (DamageSystem.cs:280->331-335). DONTACCUMULATEDMG single-hit, NOSPLASH splash-immunity, and health==-1 fire-on-any are all bit-faithful to button.qc button_damage (logic partial->faithful).
- Mover soundpacks now LIVE: .sounds/.sound1/.sound2 are promoted by MapObjectFieldsExtra.Apply (called at GameWorld.cs:2402 inside ApplyDictFields, BEFORE SpawnFuncs.TrySpawn at 2263), and ApplyDoorSounds/ApplyPlatSounds/ApplySecretSounds gate on Entity.Sounds. door.slide + door.rotating + plat + door_secret audio all partial->faithful.
- func_door NOSPLASH early-out present + faithful (Doors.cs:659-664): a non-special splash hit resuscitates the door owner to MaxHealthMover and leaves it closed, matching door_damage (door logic->faithful). EF_LOWPRECISION set (Doors.cs:65).
- func_train TRAIN_NEEDACTIVATION intermediate re-arm ported + faithful (MovingBrushes.cs:424-431): a NEEDACTIVATION next corner pauses + re-installs TrainUse, think=null, nextthink=0 — bit-faithful to train_wait (train logic partial->faithful).
- func_breakable NOSPLASH guard + team friendly-fire guard now honored inside OnDeath (Breakable.cs:249-266) before the break, matching func_breakable_damage:271-276; debristimejitter==0 -> DebrisTime=2.5 reproduces the QC bug (logic+values partial->faithful). Still an intended_divergence (Combat.Death hook, no killing-hit force, but debrisdamageforcescale defaults 0).

### `mutator-status_effects` — 7 closed / 7 open
- framework.tick: the collapsed generic tick is replaced by a faithful per-effect dispatch — per-tick PERSISTENT recompute (BITSET via def.PersistentCheck), per-effect OnTick, and TIMEOUT expiry via Remove(); the burn is no longer strength*0.05/frame.
- burning: Fire_AddDamage stacking LEMMA + FireApplyDamage = dps*min(frametime,fireend-now) makes burn damage frame-rate-INDEPENDENT and Base-faithful; deathtype is now the registered DeathTypes.Fire->'FIRE' with FireOwner/FireHitSound attribution; water/frozen self-extinguish and lava-persistence (cvar-gated, default off) implemented.
- framework.persistent: the m_persistent mechanism is now LIVE — Tick recomputes PersistentCheck per tick and sets the networked PERSISTENT bit (dead->live). Superweapon (IT_UNLIMITED_SUPERWEAPONS) and burning (lava) persistence both wired.
- framework.gettime: a faithful GetTime() with the still-active-this-frame clamp now exists and feeds the fire-DOT stacking + per-tick clamp (the exact Base consumer the baseline flagged as missing).
- framework.remove: Remove() now takes a removal_type (NORMAL/TIMEOUT/CLEAR) and plays def.RemovalSound on a NORMAL removal of an active non-persistent effect; the enum dispatch (framework.flags_enums) is now exercised on the live path.

### `turret-ewheel` — 7 closed / 4 open
- drive.enemy: the biggest fix — all four drive speeds corrected (fast/slow/slower/stop 500/150/50/25 vs old 700/500/320/100), turnrate 200, target_range_optimal 900, kite threshold 450, AND the aim-error speed gating now implemented (body yaw error = Base tur_head.spawnshieldtime gates chase fast<1/slow<2/slower-else instead of always going fast). logic+values partial->faithful, verified against turrets.cfg.
- head.track: aim_maxrot 360->20, aim_maxpitch 20->45, track_type FluidInertia(3)->StepMotor(1) — all match turrets.cfg; STEPMOTOR Track case verified to apply hard per-frame clamps. logic+values partial->faithful.
- combat.brain: targetscan_maxdelay corrected 0.6->1 (now Cvar-backed, falls back to Base default when turret cvars are unseeded), g_turrets_nofire master-fire gate added, and aim_firetolerance_dist 150 now threaded through ValidTarget/OnTarget (was a hardcoded 64). values partial->faithful.
- damage.lifecycle: respawntime 60->30 (matches g_turrets_unit_ewheel_respawntime). Plus Wave-1 wired the live damage path — TurretSpawn.Init installs TurretAI.EventDamage as GtEventDamage, DamageSystem.EventDamage routes non-player edicts to it (gate + RES_HEALTH subtract + Combat.Death on lethal). values partial->faithful.
- weapon.fire: shot_spread 0.0125->0.025 (matches Base). values partial->faithful; ballistics/damage/deathtype already faithful.

### `turret-hellion` — 7 closed / 5 open
- Blast radius 500->80 (missile.detonate): HellionTurret.Attack now passes radius:ShotRadius(80) — splash no longer 6.25x too large; previously-dead ShotRadius=80f const is now live.
- Missile proximity/stray basis fixed (missile.guidance): g.Radius seeded from 80, so proximity g.Radius*0.2=16u and stray-out g.Radius*5=400u now match Base owner.shot_radius=80.
- shot_spread 0.08 now applied at launch (weapon.launch): dir = Normalize(dir + Prandom.Vec()*0.08), matching Base normalize(dir+randomvec()*0.08).
- target_range_optimal 3000->4500 + all four target-select biases now passed (combat.brain): rangeBias 0.7, sameBias 0.01, angleBias 0.01, missileBias 0 (missiles get zero score), playerBias 1; ScoreTarget applies all four.
- targetscan_maxdelay hardcoded 0.6 -> cvar-backed default 1 (combat.brain): TurretAI.Cvar('g_turrets_targetscan_maxdelay',1); mindelay/aimidle also cvar-backed.

### `turret-plasma` — 7 closed / 6 open
- damage.gate_retaliate: turret_damage is now LIVE (was dead). TurretSpawn.Init wires e.GtEventDamage = TurretAI.EventDamage; DamageSystem.EventDamage (DamageSystem.cs:331) routes any non-player edict with a GtEventDamage to it on the real damage path - the same seam monsters/Onslaught use. Inactive invuln, FF scaling, MOVE shove, and retaliation now all run.
- attack.instagib_railgun: values fixed - now 1e10 damage / 800 force (was a misread 800 dmg / 0 force). Verified against Base FireRailgunBullet(it,...,1e10,false,800,...,DEATH_TURRET_PLASMA). Also removed the spurious weapons/electro_fire.wav (Base's instagib rail branch passes no fire sound) -> audio now faithful.
- setup.flags + aim.lead_splash: ZPredict bug fixed - MakeParams now zPredict:false, matching Base plasma aim_flags (LEAD|SHOTTIMECOMPENSATE|SPLASH, NO TFL_AIM_ZPREDICT); airborne targets no longer mis-led down a gravity arc.
- balance.cvars: per-unit select biases now passed and consumed (single 0.5/0.01/0.25, dual 0.2/0.4/0.4 in ScoreTarget/SelectTarget) and shot_spread const corrected 0.0125->0.015. Bonus: dual now uses its own target_range_optimal 1000 (was inheriting single's 500).
- track.fluid_inertia (values): track_accel_rot 0.7 and track_blendrate 0.2 now passed via MakeParams/Think and consumed in TurretAI.Track (were struct defaults 0.5/0.35).

### `vehicle-framework` — 7 closed / 7 open
- combat.damage: DEAD->LIVE. SpawnVehicle now installs vehic.GtEventDamage = EventDamage; DamageSystem.EventDamage (DamageSystem.cs:331) routes non-player victims with a GtEventDamage to it, so a shot vehicle runs DamageVehicle (per-weapon rate / shield-then-health / force / death-eject -> Exit/Death -> SetReturn) instead of armor-split PLAYER damage. All 4 descriptors call SpawnVehicle, so every live vehicle gets the shim. Rates match sv_vehicles.qh. This was the unit's single biggest liveness defect.
- combat.regen owner-% mirror: now produced live. RegenResource writes owner.VehicleHealth=(hp/max)*100 and descriptor Frame mirrors scalar shield/energy pools onto the pilot. (HUD still doesn't read them, but the data is now correct.)
- combat.crush: missing -> implemented + live. VehicleCommon.Touch (Crushable + minspeed gate + DEATH_VH_CRUSH Combat.Damage) installed via SpawnVehicle and dispatched by MoveTypePhysics.PushEntity; constants 70/50/100 match. Live on Racer/Raptor/Bumblebee (NOT spiderbot, see regression notes).
- combat.impact: missing -> implemented. VehicleCommon.Impact (min(speedfac*delta, maxpain) DEATH_FALL, 0.25s debounce) wired live for the Racer (bouncepain 200/0.15/150). Raptor override 1/4/1000 exists.
- hooks.VehicleTouch: DEAD -> LIVE. FireVehicleTouch is now dispatched (suppress-on-true) from VehicleCommon.Touch:744 on the wired touch path.

### `weapon-fireball` — 7 closed / 8 open
- primary.chargeup: the 5-frame prefire windup is now fully implemented AND live — WrThink->PrepareAttack->FireballPrefireFrame(0), chained via WeaponFireDriver.ScheduleThink (the same per-tick pending-think mechanism every weapon uses), Frame0 plays SND_FIREBALL_PREFIRE2, Frames0..3 emit EFFECT_FIREBALL_PRE_MUZZLEFLASH at the exact QC f_diff corners (x->v_up, y->v_right), Frame4 launches + returns to ready. animtime*WeaponRateFactor matches QC weapon_thinkf scaling.
- burn.model: Fire_AddDamage + Fire_ApplyDamage are now a faithful port — the QC overlap-combine LEMMA (totaldamage/totaltime/mindps cap), the d<=0/IS_DEAD -> -1 return contract, and a frametime-scaled per-tick burn (dps*min(frametime,fireend-now)) instead of the old frame-rate-coupled Strength*0.05. LIVE on the real per-player loop (GameWorld:1187 Tick -> BurningTick -> FireApplyDamage). Both the laser scorch and firemine ignite route through it.
- primary.bfg: the BFG sweep is now faithful on all the previously-broken fronts — !cnt gate is real (fed by lifetime cnt=1), the owner-survived gate is the QC AFTER-blast health+armor compare (RadiusDamage mutates resources synchronously, so the re-read is correct), DIFF_TEAM same-team skip added, the second LOS trace (shooter must be visible) added, plus accuracy_add hit credit and the FIREBALL_BFGDAMAGE per-victim effect.
- primary.laserscorch: the full QC skip filter (frozen-stat/frozen-status/owner/independent/!DAMAGE_AIM/same-team) is now ported, the FIREBALL_LASER effect is emitted, and the burn routes through the now-faithful FireAddDamage with the distance-scaled rate.
- secondary.firemine.lifecycle: the make-it-hot owner decouple (3 ticks beyond laserradius from the owner eye), the SILENT firemine impact (no sound, matching wr_impacteffect), and the bounce-on-fail behavior (Fire_AddDamage>=0 gate -> delete else keep bouncing) are all now ported.

### `weapon-minelayer` — 7 closed / 6 open
- Proximity team-filter (the material baseline bug) is FIXED: mine.Team = actor.Team is now set in Attack, so the OnThink `self.Team!=0 && head.Team==self.Team` exclusion (== DIFF_TEAM on the owner's team) actually fires — a teammate no longer arms a placed mine in team modes. FROZEN head is also now skipped.
- Mine shoot-down is now LIVE (dead->live): MakeShootable installs GtEventDamage, and DamageSystem.EventDamage:331 dispatches a non-player TakeDamage=Yes target through it (closing the old mis-route into PlayerDamage/PlayerCorpseDamage). A shot mine now depletes its 15 hp and detonates; the g_projectiles_damage gate is honored via exception:1.
- Over-limit fire feedback FIXED: NotifyOverLimit sends the WEAPON_MINELAYER_LIMIT MSG_MULTI center-print + plays the UNAVAILABLE cue (weapons/unavailable.ogg) — presentation+audio missing->faithful.
- Placed mine is now visible: Attack SetModel's 'models/mine.md3' (present in assets) — presentation missing->partial.
- Owner-death auto-detonate now checks the frozen owner (IsFrozen) — logic partial->faithful.

### `weapon-seeker` — 7 closed / 4 open
- Shoot-down now LIVE (cross-weapon root-cause fixed): new Projectiles.MakeShootable installs GtEventDamage, which DamageSystem.EventDamage (DamageSystem.cs:331) already dispatches for non-player victims; RadiusDamage enumerates TakeDamage!=No targets, so missiles AND tag darts are genuinely shootable on the real match path now (baseline: logic:stub/liveness:dead). ShootDown runs the g_projectiles_damage gate, drains HP, and detonates only at hp<=0.
- missile_homing: smart world-avoidance (missile_smart, default ON) ported in MissileThink — adaptive traceline ahead, bound(trace_min, hit-dist, trace_max) kept in waitCell, and the trace_plane_normal*(1-frac) blend into desireddir, matching seeker.qc:66-79. The dominant observable baseline divergence (missiles clipping walls/corners) is closed.
- missile_explode + flac_explode: wrong impact cue fixed — now plays SeekerExpSample() = weapons/seekerexp{1..3}.wav (the wr_impacteffect no-bounce else branch SND_SEEKEREXP_RANDOM) instead of the hardcoded tag_impact.wav; assets confirmed present.
- flac_fire muzzleflash fixed: now HAGAR_MUZZLEFLASH ('uses hagar effects', W_MuzzleFlash(WEP_HAGAR)) instead of SEEKER.
- tag_fire dedupe fixed: TagTouch calls FindTaggedInfo and refreshes the existing tracker instead of spawning a 2nd volley controller (ports W_Seeker_Tagged_Info) — a repeat tag no longer doubles the barrage; also now plays the tag_impact strike cue.

### `weapon-vaporizer` — 7 closed / 6 open
- primary.rail_beam: accuracy 'fired' credit now passed — SetupShot(wep:this, maxDamage:damage) at Vaporizer.cs:125 (sole gap on the row, CLOSED).
- primary.fire_sound: VOL_BASE*0.8 volume scale now applied (Vaporizer.cs:134) — values+audio now faithful.
- primary.beam_particle: rail BEAM (VAPORIZER_BEAM/_HIT) + muzzle flash (VORTEX_MUZZLEFLASH) now EMITTED and networked to clients via the live EffectNetSink (ServerNet.cs:287). Was fully missing.
- primary.impact_effect: VORTEX_IMPACT spark + neximpact sound now emitted at the rail endpoint (Vaporizer.cs:151-152). Was fully missing.
- rm.primary_explosion: cvar fallbacks raised from half-strength (35/15/90/200) to correct Base 70/38/140/400; deathtype now Devastator|HITTYPE_SPLASH via devTag (accuracyWeapon=devastator); sky/NOIMPACT endpoint guard added.

### `ctf` — 6 closed / 5 open
- ctf.hud.modicons: dead->live. GametypeStatusBlock now has a case Ctf -> PackCtfStatus (recipient-relative 2-bit-per-team taken/lost/carrying, bit layout verified against ctf.qh: base*1/*2/*3, neutral 256, shielded 4096). Capture is called per-peer every snapshot (ServerNet:1536) and NetGame.UpdateModIcons:2898 now selects ModIconsMode.Ctf and feeds ObjectiveStatus. Flag-status icons + carrying blink now actually appear in a CTF match.
- ctf.flag.return: presentation fail->faithful. The hardcoded CTF_FLAGRETURN_TIMEOUT_NEUTRAL is replaced by team-colored CTF_RETURN_<team> (Info kill-feed + the returner's Center) on player return and CTF_FLAGRETURN_TIMEOUT_<team> on timeout; all per-team notif keys are registered in NotificationsList.cs.
- ctf.flag.drop_on_death: toss velocity now applied (g_ctf_drop_velocity_up/side 200/100 + crandom() sideways kick + carrier velocity) AND genuinely integrated -- MoveType.Toss runs through the new MoveTypePhysics.PhysicsToss (gravity + ClipVelocity bounce), invoked for every non-client entity in SimulationLoop:243. The flag now tumbles away and settles.
- ctf.flag.pickup: dropped-pickup interpolated score now ported (DroppedPickupScore = floor(early*frac + late*(1-frac) + 0.5) over remaining auto-return time), and pickup_base now also adds team SCORE. logic partial->faithful.
- ctf.flag.capture: capture-assist to the previous dropper (carried.Dropper, the flag's last ctf_dropper) is now awarded (g_ctf_score_capture_assist, player + team SCORE). logic partial->faithful.

### `freezetag` — 6 closed / 10 open
- freezetag.revive.time_to_score: the Base-default time-to-score model (revive_time_to_score=1.5, revive_speed_t2s=0.25, progress never cleared, SCORE accrued over time) is now fully implemented in ReviveTick + cvar-backed with correct defaults, and is the live path. missing -> faithful/live.
- freezetag.revive.manual: now branches on t2s and uses speed_t2s 0.25 with no-clear hold (and revive_speed*(1-base) + clearspeed decay only when t2s off) -> matches Base default. partial -> faithful.
- freezetag.revive.score: SCORE now accrues per-reviver over time under the default t2s (matching Base) instead of a flat +1 on completion. partial -> faithful (only the nade bonus remains).
- freezetag.revive.auto_timeout: continuous base_progress ramp (revive_auto_progress) + the revive_auto on/off gate are now implemented; Freeze arms FrozenTimeout only when revive_auto && maxtime>0. partial -> faithful.
- freezetag.revive.spawnshield: Unfreeze now sets Player.SpawnShieldExpire = now + g_freezetag_revive_spawnshield (1s), and the live DamageSystem.HasSpawnShield honors it. missing -> faithful/live.

### `lms` — 6 closed / 17 open
- lms.regen.disabled: missing→faithful+live (FULLY closed). OnPlayerRegen subscribed in Activate(), fired live from PlayerFrameLogic.cs:46; both g_lms_regenerate/g_lms_rot default 0 so regen+rot fully off, matching Base's `return (!regenerate && !rot)`.
- lms.damage.dynamic_vampire: missing→faithful+live. OnDamageCalculate fired live from DamageSystem.cs:256; all five constants verified exact vs Base + gametypes-server.cfg (min_lives_diff 2, factor base/increase 0.1, max 0.5, heal clamped at start_health 200). Enabled by default, so it is active behavior in a real match.
- lms.loadout.start_items: missing→faithful+live. OnSetStartItems fired live from SpawnSystem.ComputeStartItems (line 711) into ApplyStartLoadout (line 808, real spawn path line 544); 200/200/60/320/160/180/0 match balance-xonotic.cfg exactly. Only the PlayerSpawn least-healthy late-join clone (depends on dead forfeit lifecycle) remains.
- lms.loadout.weapon_arena: missing→faithful+live. OnSetWeaponArena defaults arena to g_lms_weaponarena 'most_available' (exact Base match), live via ComputeStartItems line 679 + ExpandWeaponArena. Residual is the port-wide '_available' map-intersection stand-in, not LMS-specific.
- lms.items.no_pickups: missing→faithful+live for item suppression. OnFilterItemDefinition fired live from StartItem.Spawn (StartItem.cs:103); returns filter-out unless g_lms_items/g_pickup_items>0. The HealthMega→extralife swap half is still missing but dormant under default g_lms_extra_lives=0.

### `mapobject-misc` — 6 closed / 6 open
- follow.attach_follow: tag-relative origin/angle bake (AttachSameOrigin via Api.Models.TryGetTag + FixedVecToAngles2) now closed; deferred-init drained on the live path (GameWorld.cs:575 -> RunPostSpawn).
- dynlight.server_state: park-on-first-corner gap closed — a path-following dynlight now walks the full path_corner chain (PathNext/PathWait on CalcMove), firing per-corner targets and honoring per-corner .wait/.speed; static/follow/tag/toggle/reset/spin all faithful + live.
- corner.path_corner: set_platmovetype now ported (SetPlatMoveType tokenizer + CubicSpeedFuncIsSane gate); per-corner ease string applied by PathCornerSetup and restored/overridden in func_train TrainNext.
- subs.calcmove: default-easing divergence closed — PlatMoveStart/End default 0/0 (QC float default) so every >=0.15s door/plat/train runs the smoothstep S-curve instead of forced linear; most-live feature in the unit.
- keys.item_key: the worst baseline gap (keys never spawned -> key-gated areas permanently locked) is closed; all six key classnames registered with correct BIT pre-stamps, grant logic + pickup sound live, consumer chain (Doors/Triggers read same Entity.ItemKeys) means a picked-up key actually unlocks.

### `mutator-dodging` — 6 closed / 5 open
- force.scaling: PHYS_FROZEN -> horiz_force_frozen (200) branch added to DetermineForce, and the swapped hardcoded fallbacks corrected (speed_min 400->200, force_slowest 200->400) — logic+values missing/partial->faithful.
- move.ramp_impulse: the PM_dodging swimming/dead abort now includes BOTH the clientselect term AND the frozen_dodging carve-out, exactly matching Base sv_dodging.qc:223-224 — logic partial->faithful.
- clientselect.opt_in: the sv_dodging_clientselect gate is now implemented (cvar read + ClientDodgingEnabled + the PMDodging abort term) — logic+values missing->faithful (per-client REPLICATE still absent, so presentation stays partial).
- frozen.dodge: the entire frozen-dodging mechanic is now ported across all three Base sites (detector frozen_no_doubletap single-tap branch, DetermineForce frozen force, PMDodging frozen carve-out) with all three cvars read and matching constants (0/0/200) — logic+values missing->faithful.
- input.double_tap: the frozen single-tap branch was added to all four direction tests and the spurious crouch-exclusion on the +dodge button was removed (now checked unconditionally, matching Base PHYS_INPUT_BUTTON_DODGE) — detection SHAPE now fully faithful.

### `mutator-instagib` — 6 closed / 10 open
- regen.split_health_armor (the #1 worst baseline gap): now subscribes GameHooks.PlayerDamageSplitHealthArmor (InstagibMutator.cs:103) which is dispatched LIVE+unconditionally at DamageSystem.cs:438 and overwrites take=full/save=0 before the subtract — armor no longer 70%-absorbs damage, the armor-as-lives model and the raw 10/5 no-ammo bleed are restored. Verified faithful + live.
- damage.friendlyfire: missing→faithful/live — Wave-2 added the QC first branch (g_friendlyfire==0 && SAME_TEAM && both players → Damage=0) at lines 151-156; Teams.SameTeam matches QC SAME_TEAM.
- damage.contents: missing→faithful/live — DamagedByContents (cfg default 1, shipped) gates Drown/Slime/Lava via DeathTypes.BaseOf; cvar-0 override now zeroes contents damage (lines 166-171).
- damage.friendlypush: missing→faithful/live — !FriendlyPush && SAME_TEAM zeroes Vaporizer force on teammates (lines 180-181); cvar default 1.
- countdown.announce: partial→faithful/live — per-number ladder Announce(NUM_{ceil(hp/10)}) (NUM_1..10 registered+enabled), the >90 MULTI_INSTAGIB_FINDAMMO variant, and the rocketminsta INSTAGIB_DOWNGRADE branch are all wired; all notification ids exist. Only the StopCountdown Kill_Notification clear remains.

### `mutator-rocketminsta` — 6 closed / 4 open
- damage.zero_self_nade: RM explosion now passes deathTag=FromWeapon("devastator") to RadiusDamage (Vaporizer.cs:177-180), so the devastator-keyed no-self/no-nade-damage hook finally matches — rocket-jumping with the RM blast no longer hurts the firer. (partial/dead -> faithful/live)
- damage.zero_laser_self_round: RM laser bolts now tag damage with electro.RegistryId (Vaporizer.cs:212-213); the electro-keyed self/pre-round nullify hook now matches (live). logic:partial remains only from the intended game_stopped round-gate approximation.
- kill.force_gib: RM kills now carry devastator/electro deathtypes, so PlayerDies bumps corpse damage to 1000 and RM kills over-gib as in Base (dead-in-practice -> live).
- primary.explosion: added the sky/noimpact surface-flag gate (Vaporizer.cs:156-158, Sky=0x4|NoImpact=0x1), HITTYPE_SPLASH on indirect victims, and accuracyWeapon=devastator credit; deathtype corrected vaporizer->devastator. logic/values/timing now faithful (audio still missing).
- secondary.laser_barrage: deathtype vaporizer->electro; mode-0 sound electro_fire2->crylink_fire.wav (SND_CRYLINK_FIRE, audio:partial->faithful); zspread fixed to (random-0.5) ([-0.5,0.5], was 2x); ELECTRO_MUZZLEFLASH now emitted; C# fallbacks updated to Base cfg defaults (values:partial->faithful).

### `sv-commands-votes` — 6 closed / 11 open
- cointoss: replaced the deterministic clock-LSB flip with Common.Math.Prandom.Float() > 0.5 (unbiased PCG PRNG = QC random()) AND corrected the default labels to QC's ^1HEADS/^4TAILS with ^7-prefixed custom options — the whole row is now faithful (predictable/biased defect gone).
- extend/reducematchtime: now read timelimit_increment/decrement (default 5 min) instead of a fixed 60s, and ChangeMatchTime replicates the full QC changematchtime clamp (mi>=10, +current-time offset, [min,max]*60 bounds, unlimited/min/max bails).
- playban: now runs the real ClientManager.PutObserverInServer transition (not just the FRAGS_SPECTATOR sentinel) and parts the player from any minigame when g_playban_minigames; the QC Join_Try playban gate was also added to CmdJoin. ban.prefix_lists is now gap-free.
- vote.parse_whitelist values: timelimit vote bound fallback corrected 0/9999 -> 5/60 (now matches Base, no wiring needed); kick/kickban now emit the QC 'kick # <index> ...' form (kickban appends g_ban_default_bantime/masksize/~); movetoauto/red/blue/yellow/pink/spec special-parse cases added.
- vote.call values: VoteController.DefaultTimeout const 30 -> 24 now matches Base sv_vote_timeout default.

### `sv-mapvoting` — 6 closed / 12 open
- think_loop: the 0.5 s MapVote_Think throttle is now genuinely implemented and live (MapVoting.Tick self-throttles + snaps to the timeout, mapvoting.qc:789-791) — was 'ticks every frame'.
- decide.early_finish: the unbeatable-leader check is now bit-faithful to Base's `(voters_real - votes_running_total) < votes_recent`, plus the `voters_real == 0` all-abstained branch with proper abstainer accounting (was a reformulated inequality).
- decide.tally_rank: ranking now uses a per-candidate mapvote_rng tiebreak assigned at ballot build (RankCandidates mirrors MapVote_ranked_cmp), replacing the per-Finish RNG reservoir.
- decide.reduce: the mid-vote ballot reduce / keep-two prune (REDUCE_REMOVE_THIS loop + MapVote_TouchMask GTV_AVAILABLE stripping + the <3-options/reduce_time disable) is now implemented and runs live in CheckRulesDecide — was wholly missing.
- client.draw: hud_panel_mapvote_highlight_border default corrected 2 -> 1 (now matches every shipped Base HUD skin) and the wrong code comment was fixed.

### `turret-plasma_dual` — 6 closed / 7 open
- attack.plasma_ball shot_spread 0.0125->0.015 now matches plasma_dual.cfg (both single and dual cfg are 0.015; old value was the QC global initparams fallback). Dual now re-emits its own projectile with DualShotSpread instead of delegating to base.Attack.
- attack.instagib_rail corrected: damage 800->1e10 (instakill) and force 0->800 now match Base FireRailgunBullet; the spurious weapons/electro_fire.wav fire sound was removed (Base plays none). NOTE: the baseline itself had the Base constants wrong (recorded 800 dmg / 0 force).
- values.engagement_envelope: target_range_optimal 500->1000, track_accel_rot 0.5->0.7, track_blendrate 0.35->0.2 now all fed — PlasmaDualTurret.Think builds its own TurretParams ctor directly instead of the single-plasma MakeParams.
- target.scoring_biases: rangebias/samebias/anglebias 1/1/1 -> 0.2/0.4/0.4, missilebias 0, all now passed explicitly. Bias values faithful.
- track.head_motor: FLUIDINERTIA accel_rot/blendrate now match cfg (0.7/0.2 vs prior ctor-default 0.5/0.35).

### `turret-tesla` — 6 closed / 3 open
- damage.lifecycle respawntime 60->120: TeslaTurret.Spawn now passes respawnTime: RespawnTime(120) to TurretSpawn.Init, matching turrets.cfg g_turrets_unit_tesla_respawntime 120 (verified). values+timing now faithful.
- weapon.chain shot_force: knockback now Vector3.Zero (was dir*400), matching Base toast() force '0 0 0' (tesla deals no shove).
- weapon.chain realowner-skip: added 'if (!ReferenceEquals(target, turret.Owner ?? turret))' before Damage, reproducing Base's etarget!=actor.realowner gate (arc still drawn, damage skipped for owner). For map turrets Owner is null so it reduces to never-skip, matching Base.
- arc_fx discharge sound: the over-emitted weapons/electro_fire.wav was removed; Base tesla TURRET is silent server-side (electro_fire is player-only via W_SetupShot_Dir). audio partial->faithful; whole arc_fx row now fully faithful.
- anim.tr_think head spin: new TrThink reproduces head avelocity scaling ('0 45 0' low / '0 180 0' charged * ammo/shot_dmg, zeroed inactive) integrated into HeadAngles each Think. logic/values/timing missing->faithful.

### `vehicle-spiderbot` — 6 closed / 6 open
- frame.controller TIMING: the prior DOUBLE timing defect (invoke once/0.1s but step by FrameTime ~0.0167 => ~6x too slow) is fixed. Frame now takes dt=ThinkRate=0.1 and steps slew/turn/gravity by dt, matching QC's aggregate per-second rate (QC runs every movement frame * PHYS_INPUT_FRAMETIME). Verified the Think still reschedules at +0.1 via RunThink, so dt=0.1 is the true elapsed time. timing partial->faithful.
- enter CTF FLAG REATTACH (the validation anchor): Spiderbot.Enter now reattaches a carried flag to tur_head at (-20,0,120), faithfully porting spiderbot.qc:552-556. Verified LIVE: player.GtCarried is set by GametypeEntities.AttachToCarrier, called from Ctf.cs:433/778 on flag pickup; Api.Models.SetAttachment + Api.Entities.SetOrigin exist. logic partial->faithful.
- frame.head_aim: muzzle aim now averages tag_hardpoint01 + tag_hardpoint02 origins (values partial->faithful) AND slew steps by dt (timing partial->faithful), matching QC lines 76-96.
- frame.jump: land sound (vehicles/spiderbot_land.wav) + land frame transition (4->5) + the airborne jump frame (4) are now all played/set server-side, matching spiderbot.qc:121-126. presentation+audio partial->faithful; the whole row is now fully faithful.
- death.blowup AUDIO: the ~10%-per-0.1s-tick burn SND_ROCKET_IMPACT (weapons/rocket_impact.wav, CH_SHOTS) is now played server-side during the ~3.4s burn. audio partial->faithful (gib entities + small-explosion visual still presentation-partial).

### `weapon-devastator` — 6 closed / 6 open
- rocket.shootdown DEAD->LIVE+faithful (the headline fix): Wave-2 Projectiles.MakeShootable installs the shoot-down on GtEventDamage — the channel DamageSystem.EventDamage actually dispatches for non-player damageables — wraps W_CheckProjectileDamage + incremental RES_HEALTH subtraction, and only detonates at hp<=0 with HITTYPE_BOUNCE. Correctly inert under stock g_projectiles_damage -2, exactly like Base.
- flight.acceleration values partial->faithful: W_WeaponSpeedFactor (g_weaponspeedfactor) now applied to BOTH speed and speedaccel*frametime in OnThink (was dropped on both).
- laser_guide presentation partial->faithful: EFFECT_ROCKET_GUIDE particle now emitted on the first guide tick (was a dead emit), AND the !PHYS_INPUT_BUTTON_ATCK2 guide-suppression (holding secondary suspends guiding) is now in the gate.
- Airshot achievement now ported+live (new explode.airshot row): mid-air direct enemy kill triggers ANNCE_ACHIEVEMENT_AIRSHOT via NotificationSystem.Announce; DAMAGE_AIM/Client/DIFF_TEAM/!IS_DEAD/IsFlying test + 24u clearance trace all match Base.
- wr_killmessage split now fully wired end-to-end: Devastator sets HITTYPE_BOUNCE on timeout/remote/shootdown, RadiusDamage tags indirect victims HITTYPE_SPLASH, DeathMessages classifies MURDER_SPLASH vs MURDER_DIRECT on the live obituary path (both notifications registered).

### `weapon-mortar` — 6 closed / 3 open
- Shoot-down now LIVE + faithful (was dead/partial): Wave-1 wired DamageSystem.EventDamage to dispatch a non-player victim's GtEventDamage, and Projectiles.MakeShootable installs the W_CheckProjectileDamage gate + RES_HEALTH subtraction, firing Mortar.Explode only at hp<=0 (graze no longer detonates). Under stock g_projectiles_damage -2 the grenade is correctly NOT shootable — Base-faithful, becomes live when a server raises the cvar.
- BounceFactor/BounceStop no longer dead: MoveTypePhysics now reads per-entity ent.BounceFactor/ent.BounceStop instead of hardcoding 0.5/0.075, so g_balance_mortar_bounce* overrides take effect (defaults still match). Bounce math also moved to signed normal·velocity (grenadebouncedownslopes=1), more Base-faithful.
- EFFECT_HAGAR_BOUNCE particle now emitted on a type-1 grenade bounce (was sound-only).
- Airshot achievement now LIVE + faithful (was missing/dead): Explode tests a directly-struck flying, different-team, alive player and sends ANNCE_ACHIEVEMENT_AIRSHOT to the owner; IsFlying ported (onground/swimming/24u clearance).
- Explosion now passes directHit through to RadiusDamage, so the direct-hit target skips the blast LOS reduction as in Base.

### `weapon-rifle` — 6 closed / 6 open
- rifle.reload (the baseline's #1 worst gap) is fully closed: clip seeded to 80 on raise, drained via the clip-aware DecreaseAmmo, and once it drops below min(10,10) the now-present top-of-WrThink forced-reload branch fires the generic WrReload live every tick -> 2s downtime, weapons/reload.wav, refill-from-pool, 80-round cap. Secondary `reload` flag consulted (0 stock); CheckAmmo now ORs the GetWeaponLoad clip term.
- rifle.ammo.decrease: FireBullets now calls the clip-aware DecreaseAmmo (was actor.TakeResource draining the shared pool and pinning the clip at 80). This is the lynchpin that makes the reload fire.
- rifle.fire.bullet accuracy FIRED credit closed: SetupShot now passes wep:this, maxDamage:bal.Damage*shots, so WeaponAccuracyEvents.Fired grows the accuracy% denominator (same path Vortex uses).
- rifle.fx.tracer closed: FireBullet gained a tracerEffect param + per-segment trailparticles; rifle passes RIFLE/RIFLE_WEAK (tracer!=0 ternary, exactly QC); thresholds 16u open-air / 4u in-solid match Base. Effects are REGISTERED so they actually network+render.
- rifle.audio.fire secondary sound fixed: now branches secondary ? campingrifle_fire2 : campingrifle_fire (was hardcoded primary). campingrifle_fire2.ogg present.

### `cts` — 5 closed / 13 open
- cts.combat.selfdamage: was missing → now FAITHFUL + LIVE. Cts.OnDamageCalculate subscribes MutatorHooks.DamageCalculate (live dispatch DamageSystem.cs:256); logic matches Base exactly (if target==attacker || deathtype==FALL, and !g_cts_selfdamage → damage=0); default selfdamage=1 matches gametypes-server.cfg:364.
- cts.weapon.no_drop_throw: was missing → now FAITHFUL + LIVE. Cts.OnForbidThrowCurrentWeapon returns true; the chain is consulted on BOTH the drop path (WeaponThrowing.IsWeaponThrowable:149) and the throw path (ThrowWeapon:185), so both QC hooks (ForbidThrow + ForbidDrop) are covered.
- cts.weapon.shotgun_only: was missing → core gap CLOSED + LIVE. Runners spawn shotgun-only via OnPlayerSpawn→ForceShotgunOnly (PlayerSpawn hook live ClientManager.cs:570). NEW sub-divergence: OnPlayerPreThink re-wipes OwnedWeaponSet to shotgun EVERY frame, so mid-run map-weapon pickups are stripped (Base WantWeapon only governs the start loadout) → logic marked partial.
- cts.items.loot_filter: was missing → now LIVE (logic partial). Cts.OnFilterItemDefinition subscribes MutatorHooks.FilterItemDefinition (live StartItem.cs:103); deletes dropped loot unless monster-drop && g_cts_drop_monster_items; default drop_monster_items=0 matches. Logic is a classname-tag approximation of ITEM_IS_LOOT/.monster_item (no item-class flag registry).
- cts.finish.kill_delay_retract: values partial → FAITHFUL. DefaultFinishKillDelay=2f (Cts.cs:47); ScheduleRetract falls back to 2s when the cvar is unset, matching Base (was fallback-to-0 instant). Retract live via OnFinishRetract + per-frame cts.Tick (GameWorld:1478/1753).

### `monster-golem` — 5 closed / 13 open
- EF_RED miniboss glow (=128, verified vs csprogsdefs.qc:311) now applied at miniboss setup (MonsterAI.Setup:254) AND re-applied on respawn (:1681) — matches Monster_Miniboss_Setup:527/542; live on the real spawn path
- EnemyCheck now re-traces LOS to the held enemy (MoveFilter.NoMonsters, drops it if something solid other than the enemy blocks) + drops a FROZEN enemy — matches Monster_Enemy_Check:1247; live in RunThink
- MatchHalted (game_stopped || time<game_starttime) gate wired into ValidTarget:558 + Move:760 via the SAME live host seams Domination/DamageSystem use (GameScores.GameStopped + StartItem.GameStartTimeProvider) — matches Monster_ValidTarget:108 / Monster_Move:826
- Fabricated weapons/electro_fire2.wav lightning launch cue removed (Golem.cs:165 now a comment); QC M_Golem_Attack_Lightning emits no launch sound — lightning audio now genuinely faithful
- Monster_Sound throttle framework ported (g_monsters_sounds master gate + msound_delay: pain 1.2s/idle 7s) and idle/sight/melee/spawn/pain/death cues wired live where most previously never fired — liveness improved (framework only; sample paths still broken, see regression_notes)

### `mutator-buffs` — 5 closed / 23 open
- buffs.inferno: now fully implemented and LIVE (missing/na -> faithful/live). Burn-on-hit at 0.3x over a log-curve burn time whose formula is bit-identical to Base buff_Inferno_CalculateTime (offset_x=0), DEATH_FIRE immunity + DEATH_LAVA half-damage as carrier-target, DEATH_BUFF_INFERNO death type, recurrence guard. Burn ticks via MonsterFramework.AddFireDamage + the shared Burning status effect. Constants all match.
- buffs.swapper: now implemented and LIVE (missing/na -> partial-logic/live). Drop-weapon swaps origin/velocity/angles with the nearest DIFF_TEAM enemy within g_buffs_swapper_range=1500, plays KA_RESPAWN at both ends, consumes the buff. Reached via live ForbidThrowCurrentWeapon.Call (WeaponThrowing.cs:149/185).
- buffs.medic.survive: the !ITEM_DAMAGE_NEEDKILL guard was added (BuffsMutator.cs:384 `!needKill`), matching Base sv_buffs.qc:561 -> logic partial->faithful. A fatal lava/slime/swamp/void hit is no longer wrongly survived.
- buffs.vengeance: the !ITEM_DAMAGE_NEEDKILL guard was added (line 397 `!needKill`), matching Base sv_buffs.qc:570 -> logic partial->faithful. A lava/slime/void kill no longer reflects; 0.4 mult + 0.1s delay confirmed.
- buffs.disability: the WeaponRateFactor x1.5 fire-rate slow is now wired and LIVE (OnWeaponRateFactor; WeaponRateFactor.Call live at WeaponFireGate.cs:429 + OffhandBlasterMutator.cs:98), closing one of the three missing slow vectors. Row remains partial (projectile-speed + monster-move slows still absent).

### `sv-spawnpoints` — 5 closed / 9 open
- spawn_near_teammate: the relocation branch now applies the sky-surface reject (Q3SURFACEFLAG_SKY, verified =0x4) — previously deferred.
- spawn_near_teammate: now applies the lava/slime/water pointcontents reject (verified SUPERCONTENTS liquids mask = Water|Slime|Lava = 0x70) — previously deferred.
- spawn_near_teammate: both look-at and relocation facing now latch into the networked FixAngle/FixAngleAngles channel (FaceAngles), and the PlayerSpawn hook fires AFTER PutPlayerInServer (ClientManager.cs:570 > :550) so the teammate-facing genuinely reaches the client instead of being overwritten by next-tick input angles.
- spawn_near_teammate: IsEnabled now uses a faithful port of QC expr_evaluate (lib/cvar.qh:48) — boolean cvar-expression interpreter — so ruleset values like '!g_assault !g_freezetag' resolve correctly instead of always reading enabled. Operator-order divergence verified behaviorally equivalent.
- spawnpoint_idle_glow: draw-distance cull now reads the cl_spawn_point_dist_max cvar (Base default 1200, 0 => no cull) instead of the hardcoded 2000 qu — matches Base Spawn_Draw's 'if(dodraw && autocvar_cl_spawn_point_dist_max)' gate.

### `turret-flac` — 5 closed / 5 open
- target.scoring_biases: rangebias 0.25 / anglebias 0.5 / playerbias 0 / missilebias 1 / samebias 1 now defined as consts and passed into TurretParams; ScoreTarget+SelectTarget consume all five (values partial->faithful, row now fully faithful)
- aim.lead_compensate: the unintended zPredict:true argument removed; port aim flags now exactly LEAD|SHOTTIMECOMPENSATE matching Base flac.qc tr_setup (logic+values partial->faithful)
- aim.track_motor: aim_maxpitch 90->35 (restores the real flak acquisition cone), track_accel_rot 0.5->0.7, track_blendrate 0.35->0.2 all now passed and consumed in TurretAI.Track (values partial->faithful)
- fire.flak_shell: shot_spread 0.0125->0.02 and target_range_optimal 2000->1250 now match turrets.cfg (values partial->faithful)
- lifecycle.respawn: respawntime 60->90 (FlacTurret.Spawn now passes respawnTime: RespawnTime); ALSO the turret damage->death path is now genuinely LIVE — Wave-2 wired a GtEventDamage shim so DamageSystem routes turret edicts through TurretAI.EventDamage (pre-damage gate + retaliation + health subtract + Combat.Death), where before the gate code had no live caller

### `turret-framework` — 5 closed / 8 open
- lifecycle.use_damage (the #1 worst gap): TurretAI.Damage went dead->LIVE. Wave-2 installs e.GtEventDamage = TurretAI.EventDamage in TurretSpawn.Init; DamageSystem.EventDamage (DamageSystem.cs:331) routes any non-player edict carrying GtEventDamage to it and returns (so no PlayerDamage). Verified live: IsPlayer(turret)=false, turret TakeDamage=Aim, and WeaponSplash.RadiusDamage (line 82 filters TakeDamage!=No) / ApplyDamage reach turrets in blast radius. The bit-faithful inactive-immunity / friendly-fire / MOVE-shove / retaliate gate + health subtract + death trigger now genuinely run.
- brain.think: g_turrets_targetscan_maxdelay drift 0.6->1 FIXED. Now Cvar("g_turrets_targetscan_maxdelay",1f); turrets.cfg seeds 1 live via ConfigLoader.LoadServerConfig (GameWorld.cs:433), and the fallback is also 1. mindelay/aimidle likewise cvar-backed with Base-matching defaults.
- brain.think/fire.gate: g_turrets_nofire master fire-disable now modeled (Fire() short-circuits when the cvar is set) and wired live.
- acquire.target: LOS reject distance no longer hardcoded 64u; ValidTarget now threads the per-unit aim_firetolerance_dist (p.FireToleranceDist, real per-turret consts e.g. flac 150 / hk 500 / machinegun 25), falling back to 64 only when unset.
- spawn.init: ewheel per-unit respawntime drift corrected (60f -> 30f to match turrets.cfg:13); test updated to assert 30.

### `turret-machinegun` — 5 closed / 9 open
- setup.flags + target.validate angle-gate: SelectAngleLimits dropped from MachinegunTurret.Select (:45) — the machinegun's target-select flag set now matches Base (PLAYERS|RANGELIMITS|TEAMCHECK) and the validate_target:780 angle gate no longer fires when Base would skip it.
- aim.lead: zPredict turned off (MachinegunTurret.cs:68) to match aim_flags = LEAD|SHOTTIMECOMPENSATE — the machinegun no longer leads the gravity arc of airborne targets.
- weapon.firebullet audio: the spurious uzi_fire.wav Play was removed — the turret now fires SILENTLY, which is exactly Base behavior (W_SetupShot_Dir sound is gated behind `if (isPlayer)` and a turret actor is not a player). audio dimension missing-of-the-cue -> faithful.
- think.loop values: targetscan_maxdelay 0.6 -> 1.0 and all three scan delays are now cvar-backed (TurretAI.Cvar with turrets.cfg fallbacks); the g_turrets_nofire master fire-disable gate was added (Fire:608).
- lifecycle.damage liveness (dead->live): TurretAI.Damage now runs on the real damage path via a GtEventDamage shim (TurretSpawn.Init:89) that DamageSystem.EventDamage:331 dispatches for non-player edicts — inactive-immunity, friendly-fire scaling, and the turret's own health subtract are finally reached. This was the single clear present-but-dead defect for the unit. Verified the full live chain hit->Apply->EventDamage->Damage->Combat.Death.

### `weapon-machinegun` — 5 closed / 6 open
- machinegun.audio.impact_ricochet: dead -> LIVE+faithful. FireOne now calls WeaponFiring.BulletImpactFx -> SoundSystem.PlayRic on every bullet impact; the previously-zero-caller PlayRic resolves to the real RIC group (ric1..3.ogg verified present). Silent-gated on sky/miss. This was the baseline's #2 worst gap.
- machinegun.recoil: g_norecoil gate added. Recoil() now early-returns when g_norecoil!=0, matching Base's `if (!autocvar_g_norecoil)` wrapper. Row logic partial -> faithful.
- machinegun.spread.accumulation: C# Configure() fallback spread_decay 0 -> 0.048, so a config-less path no longer flips the MG to the legacy Nexuiz counter-spread model. Values partial -> faithful.
- machinegun.fire.auto: two fixes — spread_crouchmod fallback 0.25 -> 1, AND WrThink now resets MiscBulletCounter=0 on a fresh auto trigger pull (QC machinegun.qc:293). Row -> fully faithful.
- machinegun.fire.burst: burst_spread fallback 0.04 -> 0 (values faithful), and adversarial re-read of Base showed W_MachineGun_Attack_Burst passes thiswep.m_id WITHOUT HITTYPE_SECONDARY — so the baseline's 'burst kills wrongly read SPRAY not SNIPE' was a FALSE gap; the port's burst is faithful. Row logic partial -> faithful.

### `mapobject-movers-platforms` — 4 closed / 4 open
- plat.spawn_setup: message2 "was squished by" default + the sounds-selector (sounds==1->plat1/2, ==2->medplat) and legacy sound1/sound2 overrides now applied via MapMover.ApplyPlatSounds, with .sounds/.sound1/.sound2 promoted onto the edict in MapObjectFieldsExtra.Apply on the live host map-spawn path (verified runs at GameWorld.cs:2402 BEFORE SpawnFuncs.TrySpawn at 2263). values+audio faithful.
- subs.platmovetype_parse: set_platmovetype + cubic_speedfunc_is_sane fully ported AND wired live (PlatSetup:68 + func_train/path_corner; .platmovetype key promoted MapObjectFieldsExtra:151); the train path honors the insane-curve false return. logic/values missing->faithful, liveness na->live.
- subs.calcmove: Entity.PlatMoveStart/End default corrected 1->0, so long movers now run Base's bezier smoothstep ease (cubic_speedfunc 0,0) instead of the wrong constant-velocity linear branch. A genuine easing-fidelity fix, verified against the default fields (MapObjectsCommon.cs:125-126).
- plat.trigger_use: PlatUse now returns only when state != STATE_UP (matches plat_use objerror guard), where the prior port also accepted STATE_TOP. logic partial->faithful. Also center_trigger degenerate-box now deletes the trigger (matches QC delete+objerror) instead of registering an inverted volume.

### `monster-wyvern` — 4 closed / 5 open
- wyvern.fireball.explode_effect: EFFECT_FIREBALL_EXPLODE blast particle now emitted in onExplode and confirmed live (registered EffectsList.cs:122, invoked from SpawnProjectile.Explode on Touch+lifetime, networked via EffectNetSink->RenderSink). presentation missing->faithful.
- wyvern.death: mr_death scatter (400r-200/400r-200/100r+100) now applied on the live corpse and survives MarkDead (flyer velocity preserved, MoveType.Toss, gravity restored). logic+values missing/partial->faithful, with the Think wrapper confirmed running post-kill (non-gibbed).
- wyvern.deadthink: mr_deadthink on-ground die2 hook now genuinely driven on the corpse-fall path (EntFlags.OnGround detect, ds.Landed guard). logic+liveness dead/absent->live/faithful.
- wyvern.pain (audio only): pain cue routed through throttled MonsterSound (CH_PAIN/Body, 1.2s delaytoo self-spam guard) matching QC Monster_Damage:1101. audio->faithful. Also spawn/death/idle/sight/melee voice cues now route through the gated MonsterSound path.

### `mutator-midair` — 4 closed / 2 open
- damage.multiplier: now a live-read property (no Hook-time cache, no `!=0` guard) -> configured 0 means zero airborne damage and mid-match changes apply immediately; values+timing partial->faithful.
- damage.forcescale: now read live each frame (cache-at-Hook staleness gone), still behind the `!=0` truthiness guard matching Base; timing partial->faithful.
- ground.shield_and_glow (shieldtime): ShieldTime is now a live-read property with fallback default 0.3 (was field default 0.5 behind a `!=0` Hook guard) -> default now agrees with Base 0.3 and configured 0 = zero window; values partial->faithful.
- bot.disable_bunnyhop: was missing/na, now LIVE -- PlayerSpawn hook (MidairMutator.cs:104) is subscribed, fired on the real spawn path (ClientManager.cs:570), and the per-frame BotPopulation resync (148-152) genuinely propagates BotSkill=0 into the bunnyhop gate (BotNavigation.cs:337), so midair bots stop bunnyhopping.

### `mutator-powerups` — 4 closed / 6 open
- powerups.glow.effects: Strength/Shield glow now LIVE+faithful. OnPlayerPreThink (live via GameWorld.cs:1037) ORs StrengthGlow=(EF_BLUE|EF_ADDITIVE|EF_FULLBRIGHT) / ShieldGlow=(EF_RED|...) into player.Effects; verified end-to-end network path (ServerNet:1727 -> NetEntity EntityField.Effects -> ClientEntityView:129 -> CsqcModelEffects.Apply:1095 emits blue/red dynamic light). Constants 64/128/32/512 all match Base. Strength=blue, Shield=red (correct per strength.qc:31). Residual: render is a dynamic-light approximation + additive/fullbright only on per-instance materials.
- powerups.notifications: pickup INFO+CENTER on rising edge (with !g_cts + IS_PLAYER gates) and CENTER powerdown on falling edge now fire via NotifyEdge; POWERUP_/POWERDOWN_ notifs registered, edge tracked by _prevActive bitmap (no re-notify while active, matching QC !wasactive). Live on PlayerPreThink.
- powerups.countdown_beep: PlayCountdown plays misc/poweroff each integer-second crossed in the last 6s, math bit-faithful to server/client.qc:1532 (timeLeft<6 && floor(timeLeft-frametime)!=floor(timeLeft)). Live, poweroff.ogg present.
- powerups.drop.ondeath_onuse: drop-on-death now LIVE+faithful (OnPlayerDies via DamageSystem.cs:589). Drops each active powerup; continue-timer (mode 1) stores absolute finish + ItemIsExpiring and ItemTouch subtracts time on pickup (verified round-trip), freeze (mode 2) stores remaining + 20s lifetime. g_powerups_drop_ondeath=1/g_items_dropped_lifetime=20 match Base.

### `mutator-random_items` — 4 closed / 5 open
- engine.weapon_prob_cvar (the headline bug): the weight-lookup cvar name now includes the 'weapon_' segment — port emits g_{prefix}_weapon_{netname}_probability = g_random_items_weapon_vortex_probability, matching Base's sprintf(m_canonical_spawnfunc) and the shipped randomitems-xonotic.cfg (verified exec'd from mutators.cfg:552). Now faithful/live.
- engine.item_prob_by_property: health/armor/resource/powerup picks are now implemented (GetRandomItemClassNameWithProperty) by enumerating Items.All, filtering by type + Normal spawnflag + IsAllowed, weighting by g_{prefix}_{CanonicalSpawnFunc}_probability — exactly the 'PORTABLE NOW' fix the baseline predicted. Was fully missing.
- engine.type_weighted_pick: all five item types now resolve to a concrete classname (was weapon-only-and-broken) AND the only live caller (SpawnLootItem on the genuinely-live PlayerDies path) now actually returns items. Was partial/dead -> faithful/live.
- loot.spawn_item: dropped loot is now a real touch-pickupable item — SpawnLootItem resolves the chosen classname to its Pickup and spawns through StartItem.SpawnLoot (MoveType.Toss, Touch=ItemTouch, despawn timer, NODROP kill), the exact path the baseline named as the fix. Was an inert bare entity (partial/dead) -> faithful/live (values still partial only for the latent loot_time field default, which the shipped cfg overrides to 10).

### `mutator-spawn_near_teammate` — 4 closed / 5 open
- registration.cvar_gate: ExprEvaluate was a stub (any non-''/'0'/'false' string read as enabled); now a genuine port of Base expr_evaluate (lib/cvar.qh:48) — tokenizes, runs the BINOP AND-chain, leading +/- handling, and (ftos(f)==k ? f : cvar(k)) bare-token truthiness with '!' invert. The overkill ruleset value "!g_assault !g_freezetag" now correctly disables the mutator during assault/freezetag. logic partial->faithful.
- lookat_facing: the look-at branch now routes through FaceAngles which latches FixAngle+FixAngleAngles (the networked channel that actually reaches the client), and ClientManager.Spawn calls PutPlayerInServer (550) BEFORE PlayerSpawn.Call (570) so the mutator overwrites the spawnpoint latch on the same respawn edge — exactly Base's 'fixangle is already set' contract. presentation missing->faithful, liveness dead->live.
- commit_closetodeath: the relocate facing (Place: mate.Angles) now also routes through FaceAngles->FixAngle, so the relocation orientation reaches the client. presentation missing->faithful.
- relocate.spot_search: 2 of the 4 baseline-missing rejects are now wired — the sky-surface reject (trace.DpHitQ3SurfaceFlags & Q3SurfaceFlagSky=0x4, matches Base) and the lava/slime/water reject (Api.Trace.PointContents & liquids mask). Infra (DpHitQ3SurfaceFlags, PointContents, MutatorConstants.Q3SurfaceFlagSky) all exist and are used identically by other ported features.

### `mutator-touchexplode` — 4 closed / 3 open
- blast.radiusdamage values partial->faithful: Wave-2 replaced the `if(cvar!=0)` override with an unconditional cvar read AND set the class defaults to 50/20/0/300; mutators.cfg (163-166) registers those values and is exec'd (ConfigLoader, GameWorld.cs:433) BEFORE MutatorActivation.Apply (GameWorld.cs:535), so on the live path edgedamage now reads the legitimate 0 (was pinned at 20) and all four constants now match Base.
- blast.presentation audio partial->faithful: impact sound channel corrected from SoundChannel.Item (3) to SoundChannel.TriggerAuto (-3) = CH_TRIGGER (TouchExplodeMutator.cs:111).
- blast.presentation particle missing->faithful + liveness partial->live: EffectEmitter.Emit("EXPLOSION_SMALL", org) now fires the explosion_small effect (registered EffectsList.cs:29), and the host installs a live EffectNetSink (ServerNet.cs:287) that networks it to clients.
- blast.deathtype partial improvement (logic missing->partial, liveness dead->partial): deathTag "touchexplode" is now passed (TouchExplodeMutator.cs:121) and WeaponSplash.RadiusDamage routes it to Combat.Damage (WeaponSplash.cs:208-214), so the kill is now tagged + processed through the special-death pipeline (producer side done).

### `mutator-vampire` — 4 closed / 1 open
- enable.gate: instagib exclusion added (g_instagib==0 term in VampireMutator.IsEnabled:24-27) -> logic partial->faithful; verified live via MutatorActivation.Apply:60 called from GameWorld.cs:535. The g_vampire 1; g_instagib 1 double-activation defect is now prevented.
- heal.on_damage: spawn-shield guard restored (target.SpawnShieldExpire > now, VampireMutator.cs:64). Verified the entity tracks SpawnShieldExpire on the live spawn path (SpawnSystem.cs:605), the hook fires (DamageSystem.cs:438) BEFORE the full shield block, and with default g_spawnshield_blockdamage 1 DamageTake is unblocked at hook time -> matches Base. Attacker no longer heals off a freshly-spawned shielded victim.
- heal.on_damage: g_vampire_factor==0 now honored - the snapshot `if(f!=0) Factor=f` in Hook() was removed; factor is read live per event (VampireMutator.cs:52), so factor 0 = heal-for-nothing as in Base.
- heal.on_damage: cvar read cadence now per-event - g_vampire_factor and g_vampire_use_total_damage are read live inside OnPlayerDamage each damage event (timing partial->faithful).

### `onslaught` — 4 closed / 13 open
- onslaught.gen.damage_destroy: gen_health hardcoded fallback corrected 2000->2500 (matches gametypes-server.cfg:576). values partial->faithful.
- onslaught.cp.touch_build: cp_health hardcoded fallback corrected 200->1000 (matches cfg:578). values partial->faithful.
- onslaught.round.handler timing: first-round end-delay corrected 7->5 AND added the 5->7 bump on the first decided round via CanRoundEndOns, now matching QC's two-stage round_handler_Init(5,...) then (7,...). timing partial->faithful.
- onslaught.round.handler values: round_timelimit hardcoded fallback corrected 0->500 (matches cfg:570). values partial->faithful.

### `physics-movetypes` — 4 closed / 5 open
- toss.bounce: Entity.BounceFactor/BounceStop fields ADDED (EntityWeaponState.cs:46,49); PhysicsToss now reads them with Base's `==0?default:value` semantics; the settle-threshold `d` switched from abs() to the SIGNED Dot (matches grenadebouncedownslopes=1). Three producers verified live-wired: mortar 0.5/0.075, electro-secondary 0.3/0.05, raptor 0.2/0.
- checkwater: CheckWater return fixed from `WaterLevel>=1` (wetfeet) to `>1` (swimming), matching Base. This closes the WalkMove gravity-gate-too-early bug — a Walk/Step entity with only its feet wet now correctly keeps gravity.
- walk.stepmove: tunables brought to full parity — sv_stepheight 18->31, sv_gameplayfix_stepdown 1->2 (now SET_ONGROUND+groundentity glue on descending-stair landing), and the sv_gameplayfix_stepdown_maxspeed 400 skip gate added.
- step.physics: MOVETYPE_STEP now routes through WalkMove (full slide + explicit stair stepping) instead of the freefall-only PhysicsStep, matching Base step.qc's 1-line _Movetype_Physics_Walk alias; the old hardcoded misc/hitground.wav land cue is gone from the live path.

### `sv-impulse` — 4 closed / 8 open
- action.reload: weaponLocked FROZEN gate now modeled (FrozenStat + STATUSEFFECT_Frozen) so a frozen player can no longer reload, and the previously-dead Tuba.Reload instrument-cycle (Tuba->Accordion->Klein Bottle, %3, 0.5s anim, EFFECT_TELEPORT) is wired for the non-Reloadable wr_reload repurpose - logic partial->faithful, verified live + constants match tuba.qc.
- dispatch.commands_gate: round-not-started gate now LIVE (Commands.cs:1365 blocks impulses 17/20/21 during CA/freezetag pre-round warmup) via GameWorld.Rounds (set by EnableRounds on the live ActivateGameType path) + RoundHandler.IsRoundStarted - 2 of 3 baseline gaps closed.
- cheat.impulses: GIVE_ALL impulse 99 now routed from DispatchImpulse to Cheats.GiveAll, which runs the sv_cheats Allowed() gate and counts the cheat - liveness dead->partial (give-all key works on a cheats-enabled server).
- use.usekey: impulse 21 now genuinely routed (WeaponImpulses case Use=21 -> UseHandle -> VehicleBoarding.UseKey) - the 'not routed' baseline gap closed, though the vehicle-only half is all that fires.
- byid: misleading in-code comment corrected (Base registers cl_weapon_switch_fallback_to_impulse as 1/default-on) - code-doc defect from baseline resolved; behavior was and is faithful.

### `sv-world-rules` — 4 closed / 13 open
- warmup.ready_restart: RESTART_COUNTDOWN corrected 5s->10s (WarmupController.RestartCountdown=10f, matches vote.qh:66) and is live on the real ready/restart path (ToggleReady->ReadyCountCheck->ReadyRestart).
- warmup.ready_count: ready-up majority is now Base-faithful — counts humans only (IsBot skip), uses rint via Round(human_players * bound(0.5, g_warmup_majority_factor, 1), ToEven) with the humans_ready>0 guard. Replaces the old whole-roster ceil(); logic+values partial->faithful. LIVE.
- warmup.stage: Begin() now mirrors readlevelcvars faithfully — campaign forces warmup off, g_warmup<0||>1 => infinite limit (-1), ==1 => timed limit; the g_warmup>1 'minimum N players' branch (ResolveMinPlayers) is live. logic partial->faithful.
- misc.start_delay: missing->partial — Begin() now reads g_start_delay and sets GameStartTime = Now + g_start_delay on the no-warmup path (campaign=>0), matching readlevelcvars:2220-2221. Logic now live.

### `tdm` — 4 closed / 4 open
- tdm.scoring.player_frag (baseline worst gap, was missing/na): Tdm.OnDeath now credits attacker/victim Player.ScoreFrags (+1 enemy frag, -1 teamkill/suicide) on the LIVE death bus; ScoreFrags routes to GameScores.SetPlayer(Score) = the networked scoreboard Score column. Per-player TDM standings no longer read 0. GtKillCount spree bump also wired (NadeBonus accrues). The old Scores.cs/GameWorld.cs comment contradiction is resolved.
- tdm.rules.point_limit (was partial/fail): Tdm.OnInit->SeedLimit resolves the shipped g_tdm_point_limit -1 sentinel to the gametype default 50 and writes fraglimit=50 at boot (mirrors GameRules_limit_score's 'skip cvar_set when limit<0'). UpdateLeaderAndCheckLimit (per-frame at GameWorld:1719 + on death) now ends a stock TDM at 50 frags, not only on the timelimit.
- tdm.identity.registration timelimit value (was partial): SeedTimeLimit seeds TDM's 15-min timelimit at OnInit (guarded on the generic Cvars.cs default 20, so host overrides still win). All in-match defaults (15/50/2/0) now correct.
- tdm.presentation.remaining_frags (was missing/na): GameScores.CountFragsRemaining is a line-faithful port of world.qc:1592-1621 (fragsleft min/max, leadlimit_and_fraglimit, fragsleft_last latch, ANNCE_REMAINING_FRAG_1/2/3 once), called live from Tdm.UpdateLeaderAndCheckLimit; latch re-armed via Activate->ResetFragsRemaining; assets + Annce broadcast verified.

### `vehicle-racer` — 4 closed / 8 open
- impact.bouncepain (major): vr_impact + vehicles_impact now ported AND live — VehicleCommon.Touch is wired as the vehicle's .touch in SpawnVehicle and fired by the MOVETYPE_TOSS/BOUNCE integrator (MoveTypePhysics.PushEntity(doTouch:true) -> FlyMove.Impact -> e1.Touch) on the real SimulationLoop path; a racer ramming at >200 speed-change takes min(0.15*delta,150) DEATH_FALL self-damage, 0.25s debounced. Was entirely absent (missing/na -> faithful/live).
- spawnfunc.liveness: Racer.LoadCvars now reads every g_vehicle_racer_* tunable from the cvar store on Spawn (names verified against Base racer.qc), closing the 'numeric cvars hardcoded as fields' gap. One minor caveat: the value!=0 guard can't honour a tunable explicitly set to 0.
- weapon.rocket: the owner-death / 15s-timeout detonation now passes deathTag: DeathTypes.VhWakiRocket to RadiusDamage (WeaponSplash overrides the int weapon id), so a timeout kill is attributed to DEATH_VH_WAKI_ROCKET instead of a generic weapon — matches the touch-detonation path.
- regen.resources: the VEHICLE_UPDATE_PLAYER %-mirror (player.VehicleHealth/Shield/Energy 0..100% each tick) + the vr_enter one-shot seed are now produced on the authority side (Racer.Think + Enter, and VehicleCommon.RegenResource for RES_HEALTH).
- Secondary: touch-mode board (g_vehicles_enter 0) is now reachable via the wired vehicles_touch handler; and the vehicle death path now runs LIVE via GtEventDamage->DamageVehicle (DamageSystem.EventDamage routes shot vehicles) + SetReturn respawn schedule, not test-only.

### `weapon-shotgun` — 4 closed / 9 open
- Slap obituary now reachable: Shotgun.Melee builds slapDeathType = WithHitType(FromWeapon("shotgun"), Secondary) and routes the swing through Combat.Damage directly, so the |secondary suffix survives end-to-end to Scores.EmitObituary -> SelectKillMessage -> WEAPON_SHOTGUN_MURDER_SLAP (was unreachable; notify.killmessage logic partial->faithful, liveness partial->live).
- Melee woosh now emits: Shotgun.Melee calls WeaponFiring.MeleeWoosh per swing trace, emitting the registered SHOTGUN_WOOSH effect via the live RenderSink (was registered-but-dead, zero emitters).
- Impact ricochet sound now plays: WeaponFiring.BulletImpactFx (called per pellet from Shotgun.Attack) invokes the previously-zero-caller SoundSystem.PlayRic, which resolves to the registered RIC group (ric1..ric3).
- Reload timing now ported: the generic WeaponFireGate.WReload/WReloadedAndReady pipeline (reload sound, scheduled think, clip transfer) replaces the baseline 'deferred to driver / unknown' state (checkammo_reload timing unknown->faithful).

### `weapon-tuba` — 4 closed / 6 open
- fire.note_blast: blast now carries HITTYPE_SOUND via the Wave-1 string deathTag channel (Tuba.cs:124 -> WeaponSplash.RadiusDamage deathTag -> Combat.Damage); DamageSystem.Apply:133 same-team sound rule returns 0 dmg to teammates -> the tuba's 'never hurts teammates' rule is now LIVE and faithful.
- deathtype.instrument_bits: NoteOn now forms a STRING deathtype with Secondary/Bounce bits per TubaInstrument; the always-indirect blast (directHit=null) always routes through the string pipeline; DeathMessages 'tuba' case reads the bits -> Accordeon/Klein Bottle obituaries now selectable (LIVE).
- reload.instrument_cycle: WAS dead+stub, now LIVE+faithful. WeaponImpulses.ReloadHandle:217 dispatches Tuba.Reload for the non-Reloadable tuba; Reload rewritten with the WS_READY guard, %3 cycle, Send_Effect(TELEPORT) (not the old misc/teleport.wav), and the WS_INUSE 0.5s ScheduleThink anim lock -> matches wr_reload.
- setup.instrument_reset: Tuba now overrides WrSetup (Tuba.cs:68) to reset TubaInstrument=0 on equip, called by WeaponFireDriver.cs:273 -> LIVE+faithful (was missing).
- fx.smoke_ring: EFFECT_SMOKE_RING now emitted every 0.25s with bit-exact per-instrument vertical offsets and up*100 velocity (was missing); only the ring origin uses the eye vs the weapon tag, and Base's own tag read is FIXME-flagged.

### `bot-waypoints` — 3 closed / 13 open
- graph.link_cost (logic): TravelCost rewritten to a line-for-line port of waypoint_gettravelcost — the JUMP-source fall cost (jumpheight_time + sqrt((height+jh)/(g/2))) is now genuinely implemented (fromIsJump was dead before), both-submerged/both-crouched early-returns added, and the single-submerged/single-crouch (c+slow)/2 half-path averaging replaces the old whole-segment multiplier. logic upgraded partial->faithful.
- graph.link_cost (water/crouch values): LinearCostUnderwater (0.7x) and LinearCostCrouched (0.5x) added as faithful helpers, so the underwater/crouch cost factors now match Base exactly.
- steer.checkdanger: trigger_hurt detection upgraded from a loose swept-AABB overlap to Base's exact tracebox_hits_box -> trace_hits_box -> trace_hits_box_1d slab math (TraceHitsBox/HitsBox1D, util.qc:2197-2219), closing the AABB-corner false-positive gap. Confirmed still LIVE via BotBrain.Think -> BotDanger.CheckDanger.

### `monster-zombie` — 3 closed / 8 open
- block row: armor-restore fallback corrected 0.6 -> 0.5 to match g_monsters_armor_blockpercent default (Zombie.cs:161); values now faithful.
- block row: the spurious monsters/zombie_melee.wav play in DefendBlock was removed (Base M_Zombie_Defend_Block is silent); block audio now faithful.
- pain row (partial): Monster_Sound was ported with the g_monsters_sounds master gate AND the 1.2s msound_delay throttle (delaytoo), closing the 'no gate / no throttle' sub-gap shared with the sound-delivery worst-gap. Death sound also now g_monsters_sounds-gated.

### `monster-spider` — 3 closed / 9 open
- setup.mr_setup: g_monster_spider_health is now cvar-read into StartHealth BEFORE Setup (Spider.cs:49), so a server health override is honoured (was ctor-fallback-only). All 6 setup constants verified against monsters.cfg (health 180 / walk 400 / run 500 / stop 100 / forcescale 0.6 / loot health_medium).
- attack.web_projectile: web.NetName overridden to 'electro_orb' after spawn (Spider.cs:133) so ProjectileCatalog.cs:247 classifies it as ProjectileType.Electro -> the QC PROJECTILE_ELECTRO orb visual + plasma trail + electro_fly loop. Verified the rename does NOT break the Webbed self-exclusion (keys off victim NetName) or the deathtype (carried via deathType arg).
- web.electro_impact_fx: onExplode now calls EffectEmitter.Emit('ELECTRO_IMPACT', p.Origin) (Spider.cs:114); effect registered at EffectsList.cs:71 -> effectinfo 'electro_impact', matching QC Send_Effect(EFFECT_ELECTRO_IMPACT) at the impact origin.

### `mutator-melee_only` — 3 closed / 4 open
- enable.predicate: the !instagib && !overkill && !arena exclusion is now present and live (IsEnabled gates Hook() via MutatorActivation.cs:64; InstagibMutator/OverkillMutator NetNames resolve). logic partial->faithful.
- enable.predicate: cvar is now read as a string expr via ExprEvaluate(GetString("g_melee_only")) matching Base expr_evaluate(autocvar_g_melee_only), no longer GetFloat. values partial->faithful.
- arena.force_off: SetWeaponArena.Call is now LIVE on the loadout path (SpawnSystem.cs:679 + :892), so melee_only's 'off' correctly prevents an arena loadout from replacing the shotgun set. liveness dead->live.

### `mutator-multijump` — 3 closed / 3 open
- rejump_grant: the previously-omitted client_multijump truthiness factor is now present in the ready gate (`&& clientMultijump != 0`, MultijumpMutator.cs:118) -- closes one of the row's two sub-gaps.
- rejump_grant: maxspeed gate changed from 2D Horiz2D to full 3D Velocity.Length() (line 126), now matching Base vdist -- closes the second sub-gap. Row goes logic/values partial->faithful, confidence medium->high.
- client_gate: the g_multijump_client SERVER DEFAULT is now read from the cvar store (ClientDefault, line 82, default 1 from mutators.cfg:475) and honored in the -1 substitution; the gate logic (>1 -> return, -1 -> default) is coded. Row goes from all-missing to partial (server-default portion now live+faithful).

### `mutator-overkill` — 3 closed / 9 open
- overkill.filter.items_and_powerup_replace: missing/dead -> faithful/live. Wave-2 subscribed FilterItemDefinition (live at StartItem.cs:103). Now blocks HealthMega/ArmorMedium/ArmorBig/ArmorMega per the four filter cvars (medium/big default 1, matching Base) and replaces item_strength->okhmg / item_shield->okrpc superweapon pickups (gated on g_powerups && g_overkill_powerups_replace, effective default 1 from mutators.cfg). ClassName matches verified via ItemSpawnFuncs NetName->classname map.
- overkill.loot.drop_player: partial (non-collectible) -> faithful/live. DropItem now routes through StartItem.SpawnLoot (the real MOVETYPE_TOSS loot pipeline) so the dropped loot has a model, a Touch=ItemPickupRules.ItemTouch handler, despawn timer, and anti-instant-pick. Launch math/lifetime defaults already matched. Only the rarely-used 'random' loot keyword remains a fallback stub.
- overkill.set_weapon_arena_off: stub/dead -> faithful/live. SetWeaponArena.Call is now invoked directly on the live start-items path (SpawnSystem.cs:679) and the warmup path (SpawnSystem.cs:892); Overkill's force-'off' is honored, suppressing any configured g_weaponarena.
- overkill.onadd.precache_and_itemblock: PARTIAL fix. The item-block half is now faithful/live (four g_overkill_filter_* cvars read in Hook() with Base-correct defaults, enforced via the FilterItem hook). Still missing: precache_all_playermodels(ok_player) and the g_overkill_items list build -> row remains partial overall.

### `mutator-weaponarena_random` — 3 closed / 0 open
- arena_weaponset_dep (the headline gap): missing->faithful + na->live. Wave-2 added SpawnSystem.ExpandWeaponArena (g_weaponarena string -> WepSet) and wired the previously-dead MutatorHooks.SetWeaponArena .Call site in ComputeStartItems. weapons_all/most/devall flag filters match Base world.qc:1914-1942 exactly; arena grants IT_UNLIMITED_AMMO|IT_UNLIMITED_SUPERWEAPONS via StartItemFlagBits.
- playerspawn: values partial->faithful. The 'starved pool' defect is gone — ComputeStartItems now seeds OwnedWeaponSet from the full arena set, so drawing N yields a genuine random subset instead of just {blaster}.
- givefragsforkill: logic partial->faithful (the missing warmup-vs-normal pool branch is now present: pool = WarmupStage ? WarmupArenaWepSet() : ArenaWepSet(), matching QC 'if(warmup_stage)') AND values/timing partial->faithful (swap pool no longer starved; a fresh weapon is actually swapped in on a real-match frag). WarmupStage fed live per-tick from Warmup.WarmupStage (GameWorld.cs:946).

### `physics-player` — 3 closed / 8 open
- physics-player.movement_sounds: autocvar_g_footsteps server-side master gate now ported and faithful (GetString("g_footsteps")=="0" => return; unset treated as stock 1, matching xonotic-server.cfg:306). LIVE on the prediction-gated server tick.
- physics-player.movement_sounds: footstep cadence jitter `time + 0.3 + random()*0.1` now ported, plus the nextstep re-arm window `(time>nextstep)||(time<nextstep-10)` — replacing the old fixed-0.3s metronomic interval. Constants now match Base byte-for-byte.
- physics-player.movement_sounds: the ladder leg of PM_check_hitground's bail is now reproduced via player.LadderEntity (a genuinely live field, set/cleared by func_ladder touch in MapVolumes.cs). Row upgraded logic/values/audio -> faithful.

### `resources` — 3 closed / 5 open
- regen.loop: fuel regen now gated on ITEM_FuelRegen ownership AND the entire fuel block skipped under IT_UNLIMITED_AMMO — bit-faithful to client.qc:1744-1753 (UnlimitedAmmo/FuelRegen ItemFlags verified). Previously fuel regen ran unconditionally and rotted even under unlimited ammo.
- regen.loop: vehicle eject (VehicleBoarding.Exit) now runs before the DEATH_ROT damage, matching client.qc:1738 vehicles_exit(VHEF_RELEASE). Symbols verified (p.Vehicle on Entity, Exit takes the player).
- api.take: TakeResourceWithLimit is now ported with the SVQC clamp formula bit-faithful to sv_resources.qc:205 (current-amount<-limit => amount=current-limit). Closes the 'not ported at all' sub-gap — but the new method has ZERO callers (dead), and the live caller GameWorld.cs:1183 wiring of Regen was re-verified.

### `survival` — 3 closed / 5 open
- survival.death.punish_teamkill: missing→faithful, dead→LIVE. Mirror-kill (Combat.Damage self 100000 MirrorDamage), same-status frag penalty (-1 punish / -2 no-punish), NEEDKILL exclusion (Void/Slime/Lava/Swamp) and the round-started gate all ported. Verified genuinely live: Combat.System is the real DamageSystem installed in GameInit.Boot, which runs on the server (GameWorld.cs:408), so the mirror death actually applies. Constants 100000/-1/-2/MIRRORDAMAGE match Base. (No dedicated test yet.)
- survival.scoring.bank_validkills: timing partial→faithful. OnDeath now gates banking on roundStarted (RoleAssigned/Handler.IsRoundStarted), closing the prior warmup-banking divergence, and applies the Base teamkill ±1/±2 frag delta.
- survival.round.handler: values partial→faithful. Activate fallbacks corrected to Base (warmup 10, round_timelimit 120) and Handler.Init now hardcodes the 5s end-delay arg instead of wrongly reading g_survival_round_enddelay. (Logic/liveness still partial — dual-handler architecture unchanged, no _roundSync mirror for Survival.)
- survival.win.side_wipe: tie branch added (WinningSide=None when both sides reach zero same frame) — logic now matches Base hunters/prey/tie resolution (tie *notification* still dead).

### `sv-client-lifecycle` — 3 closed / 14 open
- frame.player_regen: fuel REGEN now gated on IT_FUEL_REGEN ownership AND the entire fuel block (regen+rot) skipped under IT_UNLIMITED_AMMO — verified line-for-line vs Base client.qc:1744-1753. The FuelRegen bit is genuinely fed (GiveItems/FuelRegenItem pickups + HookMutator SetStartItems FUEL_REGEN tag), so it is a real fix not an over-correction. Also ported vehicle-exit (VHEF_RELEASE) before DEATH_ROT. Live via GameWorld.cs:1183. logic partial->faithful, fuel-gate constant match false->true.
- observer.put_observer_in_server: Wave-2 added the despawn EFFECT_SPAWN puff when demoting a live (health>=1) player (same EffectEmitter.Emit('SPAWN') as the verified disconnect path) and set RES_ARMOR to g_balance_armor_start (Cvars.FloatOr) instead of 0, matching QC client.qc:366. values/presentation partial->faithful, liveness->live, armor-start constant match false->true.
- join.join_player: the join tail now fires INFO_JOIN_PLAY (FFA) / CENTER_JOIN_PLAY_TEAM_<col> (team) via NotificationSystem on the live join path (cmd join -> Clients.Join); notifications registered in NotificationsList.cs. presentation missing->partial. QUEUECONFLICT/ANNCE_BEGIN still deferred.

### `sv-ipban` — 3 closed / 9 open
- join_gate: CmdJoin (Commands.cs:1108) now consults g_playban_list and refuses an observer/spectator's join, mirroring Base joinAllowed() (!INGAME && PlayerInList). Was entirely missing on the live join path; now logic faithful + liveness live. The `join` command is registered and live.
- playban force-spectate: CmdPlayBan now calls the REAL observer transition _world.Clients.PutObserverInServer(target) (sets Solid.Not, TakeDamage.No, strips weapons/model) instead of only flipping the FRAGS_SPECTATOR scoreboard sentinel. The play-banned player is now genuinely non-solid/non-shootable this tick.
- playban active minigame removal: CmdPlayBan now calls _world.Minigames.Part(target) when g_playban_minigames, faithfully kicking the banned player out of an in-progress minigame (Base part_minigame). Minigames is live (Tick'd).

### `turret-hk` — 3 closed / 7 open
- combat.brain FULLY CLOSED: HkTurret.Think now threads all target-selection biases (rangebias 0.5, samebias 0.01, anglebias 0.1, missilebias 0, playerbias 1) + range_optimal 5000 through TurretParams, and they are GENUINELY consumed in TurretAI.ScoreTarget (lines 325-328) and the SelectTarget seed (line 352). Verified against turrets.cfg defaults. The baseline's 'samebias 100x too sticky' read was inverted — low samebias = easy to switch targets, now bit-faithful.
- combat.brain: framework targetscan_maxdelay now reads the cvar (default 1) instead of the hardcoded 0.6 (TurretAI.cs:552) — rescan cadence matches Base.
- head.track FULLY CLOSED: aim_maxpitch corrected 30->20; the three head-track motor rates (track_accel_pitch 0.25 / track_accel_rot 0.6 / track_blendrate 0.2) now threaded and consumed in TurretAI.Track (lines 497-499).
- damage.lifecycle FULLY CLOSED: respawntime corrected 60->90 (HkTurret.Spawn now passes respawnTime=RespawnTime). Additionally Wave-2 wired the turret's own .event_damage via Entity.GtEventDamage = TurretAI.EventDamage, and DamageSystem.EventDamage (lines 331-333) routes non-player edicts there — so the pre-damage gate (TurretAI.Damage), previously bit-faithful but lacking a discrete live caller, now runs on the real damage route (liveness improvement, mirrors Onslaught objectives).
- weapon.guidance PARTIAL: shot_speed_turnrate corrected 0.1->0.25 at the HkTurret call site and consumed via g.TurnRate (GuidedProjectile.HkThink:272). The rocket now banks at Base rate.

### `vehicle-raptor` — 3 closed / 1 open
- raptor.bombs: the bomblet_alt (750) clear-fall altitude test is now ported and matches QC bit-for-bit (traceline origin->origin+norm_vel*bomblet_alt; defer burst if trace_fraction==1 or within bomblet_radius of owner), with the cnt=time+10 clear-fall window and the bomblet_alt-gated nextthink. Timing/logic/values upgraded partial->faithful.
- raptor.bombs: bomblet touch detonation is no longer instant — it now reschedules Boom for time + random()*bomblet_explode_delay (0.4s), so the cluster staggers its blasts exactly as QC raptor_bomblet_touch does.
- raptor.spawn.setup: bouncefactor 0.2 (genuinely consumed by MoveTypePhysics for MOVETYPE_BOUNCEMISSILE), the DPCONTENTS_LIQUIDSMASK water-sink mask (read by TraceService), bouncestop, and a colormod reset are now set. logic/values upgraded partial->faithful.
- raptor.death: the dying colormod '-0.5 -0.5 -0.5' wreck tint is now applied (constant was a prior mismatch, now matches Base).

### `weapon-hagar` — 3 closed / 4 open
- hagar.damage.shootdown: logic partial->faithful. Wave-2 added Projectiles.MakeShootable(exception:1) to all 3 rocket spawn paths (Hagar.cs:167/196/255). MakeShootable installs a faithful port of W_CheckProjectileDamage (g_projectiles_damage ladder) + RES_HEALTH subtraction and only fires the explode callback at hp<=0 (W_PrepareExplosionByDamage). Previously any incoming damage detonated immediately. Verified LIVE: missile.TakeDamage=Yes + GtEventDamage installed -> dispatched by DamageSystem.EventDamage (DamageSystem.cs:331) from the live Damage() apply path (DamageSystem.cs:280).
- hagar.secondary.bounce: presentation partial->faithful. BounceTouch (Hagar.cs:207-220) now (a) emits EFFECT_HAGAR_BOUNCE via the live EffectEmitter Sink, (b) keys ONLY on toucher.takedamage==DAMAGE_AIM (removed the spurious EntFlags.Client OR), (c) sets owner=NULL so a bounced rocket can hurt its firer -- all bit-faithful to Base W_Hagar_Touch2. NOTE the path is still DEAD at stock (load=1).
- hagar.fx.muzzle_explode_trail: the prior open gap 'EFFECT_HAGAR_BOUNCE not emitted on bounce' is now closed (emitted in BounceTouch); HAGAR_BOUNCE is registered (EffectsList.cs:101) and resolves through the same live Sink as HAGAR_EXPLODE/MUZZLEFLASH.

### `weapon-hlac` — 3 closed / 5 open
- primary.attack (baseline WORST gap): misc_bulletcounter is now reset to 0 on a fresh primary press in WrThink(Primary) (Hlac.cs:126), gated by PrepareAttack's READY-state requirement (matches QC weapon_prepareattack). The new ScheduleAttackFrame faithfully models W_HLAC_Attack_Frame (overwrites the w_ready think per 'last weapon_thinkf wins', re-fires + increments counter while ATCK held, settles to READY on release WITHOUT resetting) -> release+re-tap now restores accuracy, the signature feather-the-trigger mechanic. Verified LIVE: WeaponFireDriver.cs:155 calls WrThink(Primary) every tick; double-fire-safe (PrepareAttack requires Ready, sets InUse). logic partial->faithful.
- recoil.punchangle: Recoil now early-returns when g_norecoil != 0 (Hlac.cs:269), suppressing the punchangle kick under g_norecoil 1, matching Base's !autocvar_g_norecoil gate. g_norecoil defaults 0 (recoil active). logic partial->faithful, presentation partial->faithful.
- bolt.impact (HITTYPE_SECONDARY): secondary bolts now carry deathTag = WithHitType(FromWeapon(hlac), Secondary) ('weapon_hlac|secondary'), consumed by RadiusDamage (WeaponSplash.cs:208-212), so HITTYPE_SECONDARY now rides the deathtype as QC hlac.qc:113 does; primary correctly keeps the plain id. Lone gap on the row closed.

### `weapon-hook` — 3 closed / 4 open
- reel_pull: elastic tarzan-2 rubber-band reel now implemented (near-bit transcription of Base's tarzan branch: hook_length shorten, overstretch spring, airfriction, spring-to-latch, tarzan>=2 half-impulse pull of a hooked player/nade); all grapplehook_* + tarzan cvars now read from balance cfg and match Base (tarzan=2, speed_pull 2000, force_rubber 2000, stretch 50, airfriction 0.2). logic/values partial->faithful.
- latch: EFFECT_HOOK_IMPACT particle now emitted on latch (was sound-only) and SetMovetypeFollow ported (records hit entity as hook.Aiment, bbox-clamps offset, tracks moving aiment each think). logic partial->faithful, presentation missing->partial.
- vampirehook: liveness dead->live -- the latch now sets hook.Aiment=other, so a direct hook hit on an enemy player satisfies the IS_PLAYER(aiment) guard and the per-0.1s drain/heal actually fires on the live path (RealOwner=>Owner resolves to firer; GrappleHookThink.Call dispatched each think). Previously aiment was always null and every guard short-circuited.

### `weapon-porto` — 3 closed / 8 open
- strength_boost FULLY CLOSED: the lookup was changed from the bogus ByName("buff_strength") (returned null -> dead) to ByName("strength") (Porto.cs:155), matching the real registration (StatusEffects.cs:161) and the canonical query used by Ctf/ItemPickupRules. AND StrengthForce is now seeded live from Bal("g_balance_powerup_strength_force", 3f) (Porto.cs:74) = xonotic default 3, replacing the hardcoded 4 (nexuiz25). Verified live via WeaponFireDriver->WrThink->Attack.
- touch.placement_tree audio CLOSED: porto/create now plays exactly once on the success branch (PlacePortal no longer plays it too), and the fail/slick branches play unsupported/bounce respectively. The old double-create + unconditional-create-on-every-touch defect is gone. Creature-ignore and not-owner-delete branches were also added (headless-approximated).
- lifecycle.lifetime_self_destruct audio CLOSED: PortoThink is now SILENT on a pure lifetime expiry (matching Base W_Porto_Fail, which plays no sound), removing the spurious porto/unsupported-on-every-expiry. The per-slot porto_current latch is now cleared via the carried firing slot (the old hardcoded new WeaponSlot(0) is gone).

### `assault` — 2 closed / 10 open
- assault.objective.inactive_sentinel: ASSAULT_VALUE_INACTIVE now 1000f (Assault.cs:50), exactly matching Base sv_assault.qh (was 1_000_000). values partial -> faithful.
- assault.notify.role_and_destroyed: NOW LIVE. Assault.Activate() subscribes OnPlayerSpawn to MutatorHooks.PlayerSpawn (Assault.cs:401), which is fired on every spawn/respawn in ClientManager.cs:570, so the 'You are attacking!'/'You are defending!' role centerprint now actually fires keyed on the live attacker team. ASSAULT_OBJ_DESTROYED ('Objective destroyed in Xs!') is also sent on the live round-1 destruction branch (Assault.cs:477). Text/args match Base exactly. logic missing/presentation stub/dead -> faithful+live.

### `damage-pipeline` — 2 closed / 6 open
- radius.splash: HITTYPE_SPLASH is now WIRED and LIVE - the unit's #1 worst-gap is closed. RadiusDamage takes a real directHit param, and every indirect non-special victim is routed through the new DamageSystem.SplashDeathType seam (idempotent OR of HITTYPE_SPLASH, no-op for IsSpecial deathtypes); direct hits keep the plain tag. Since RadiusDamage is the single central splash path for all blast weapons, splash-vs-direct kill/effect classification is restored. Verified live consumers (DeathMessages, Devastator MURDER_SPLASH, Breakable/Buttons/Doors/Triggers HasHitType gates). logic upgraded partial->faithful.
- fire.burning (fireball path only): the QC overlap LEMMA merge + a frametime-correct per-tick (dps*min(frametime,remaining), replacing the old Strength*0.05/frame) + the steam_burst removal sound + PERSISTENT machinery (lava-stick / superweapon) were added. The fireball weapon now routes through the new StatusEffectsCatalog.FireAddDamage (sets FireDamagePerSec=raw dps + carries deathtype/owner), making the fireball burn genuinely QC-faithful where it was previously inconsistent raw-dps. Napalm (raw Apply passing dps) also comes out correct via the Strength fallback.

### `deathmatch` — 2 closed / 2 open
- deathmatch.winconditions.leadlimit: was logic/values/timing=missing, liveness=na. Wave-2 added LeadLimit + LeadAndFrag and a best/second-place scan in RecomputeLeader; the leadlimit_reached formula (top-second >= leadlimit) and the leadlimit_and_fraglimit AND/OR gate now match world.qc:1625-1633 exactly, and RecomputeLeader runs every tick on the live OnEndFrame -> DriveGametypeFrame path. Now faithful/live.
- deathmatch.announce.frags_remaining: was all-missing. Now wired END-TO-END: RecomputeLeader (live every tick) scans top/second non-spectator primary scores -> GameScores.CountFragsRemaining (faithful port of world.qc:1592-1622 incl. the min/max leadlimit_and_fraglimit branch and the fragsleft_last anti-repeat latch) -> NotificationSystem.Send(Annce, REMAINING_FRAG_1/2/3) -> networked -> HudNotifications.PlayAnnouncer plays 1fragleft/2fragsleft/3fragsleft (registered+enabled, same announcer path the minutes cue already used). Activate() calls ResetFragsRemaining() to re-arm per match.
- deathmatch.gametype.identity: the prior default_leadlimit constant mismatch (DM ignored leadlimit) is resolved as a side effect of the leadlimit fix.

### `mutator-breakablehook` — 2 closed / 1 open
- breakablehook.cvar.live_reread (feature 5): Wave-2 made Breakable/BreakableOwner expression-bodied properties that read Api.Cvars.GetFloat live each Damage_Calculate, removing the Hook()-time snapshot. Mid-match toggling of g_breakablehook_owner now takes effect immediately as in Base. logic+timing upgraded partial->faithful.
- breakablehook.owner.punish_splash FFA edge: the baseline's worst gap (shooting your OWN hook in FFA wrongly dealt 5 self-splash + removal) is closed. The new DiffTeam falls to the FFA branch !ReferenceEquals(owner,owner)=false, so self-breaks no longer punish, matching Base DIFF_TEAM(owner,owner)=false.

### `mutator-campcheck` — 2 closed / 5 open
- campcheck.realclient.gate — IS_REAL_CLIENT bot exemption is now implemented AND live: the PreThink gate added `player is not Player { IsBot: true }`, and Player.IsBot is assigned on the live client path (ClientManager.cs:53) with PreThink fired at GameWorld.cs:988. Camping bots on a listen server are no longer camp-damaged/killed. This was the worst gap of that row.
- campcheck.prematch.reset — the `time < game_starttime` pre-match re-grace (nextcheck = time + interval*2; traveled = 0) is now implemented and live-wired via StartItem.GameStartTimeProvider (host wires it at GameWorld.cs:550). Partially closes the missing reset block.
- campcheck.mutator.registration — g_campcheck_typecheck is now READ into a TypeCheck field in Hook() (cvar is no longer wholly unread), so registration/cvar-load is fully faithful (though the typecheck BEHAVIOR is still not applied — see still-open).

### `mutator-hook` — 2 closed / 6 open
- enable.predicate: the truthiness-only ExprEvaluate was replaced (HookMutator.cs:146-194) with a faithful port of QC expr_evaluate (lib/cvar.qh:48) — +/- prefix, full AND-chain, all 8 BINOP comparison tokens (>= <= > < == != === !==), and the ftos-literal-or-cvar bare-token with '!' invert. logic partial->faithful, values now match. Genuine Wave-2 code fix.
- precedence.blaster_override: resolved unknown->faithful (confidence low->high) by tracing the ordering. Mutators.All is Registry<MutatorBase>.All sorted ordinal by RegistryName=NetName (Registry.cs:50-53, GameplayBases.cs:142); 'grappling_hook' < 'offhand_blaster' so blaster's PlayerSpawn runs LAST and wins the OffhandWeapon assignment, matching Base 'overridden by offhand_blaster'. No code change needed — the baseline just hadn't traced it.

### `mutator-kick_teamkiller` — 2 closed / 3 open
- detect.gates: the missing warmup_stage gate (`if (warmup_stage) return;`) is now ported AND genuinely live — GameWorld.cs:946 mirrors NotificationSystem.WarmupStage = Warmup.WarmupStage every server tick, so warmup teamkills no longer count toward the threshold. logic partial->faithful.
- notify.broadcast: the severity branch was added, so the default (play-ban) path now sends the CORRECT QUIT_PLAYBAN_TEAMKILL broadcast (kill-feed 'forced to spectate', not the old wrong 'kicked') plus the offender center-print QUIT_PLAYBAN_TEAMKILL via NotifBroadcast.One. All three notifications match Base and are live on the threshold-trip path. logic/values/presentation partial->faithful, liveness partial->live.
- action.punish (PARTIAL only): the severity switch, RESPAWN_SILENT on every branch (RespawnFlag.Silent), and the g_playban_list cons(netaddress+crypto_idfp) write are now real code on the live PlayerDies path — up from baseline stub/dead to partial/partial-live.

### `mutator-walljump` — 2 closed / 4 open
- fx.smokering CLOSED: WalljumpMutator.cs:108 now calls EffectEmitter.Emit("SMOKE_RING", hitPos, planeNormal, 5) — count/origin/normal match Base Send_Effect; effect registered (EffectsList.cs:219, in effectinfo.txt) and the sink is LIVE on the server (EffectNetSink, ServerNet.cs:287) so it networks + renders. missing/dead -> faithful/live.
- impulse.velocity cvar-override-zero gap CLOSED: Hook() now uses `if (IsSet(name)) Field = GetFloat(name)` with IsSet = !IsNullOrEmpty(GetString(name)), so an admin explicitly setting any g_walljump* tunable to 0 (legal in Base, e.g. velocity_z_factor 0) is honored instead of masked by the C# default. Previously `if (x != 0f)` silently dropped it.

### `mutator-waypoints` — 2 closed / 11 open
- waypoints.registry.defs: Wave-2 rebuilt the WP_* registry as a faithful 1:1 port of all.inc — all 63 REGISTER_WAYPOINT defs now present (was 38/63) with matching text/icon/color, and the static per-def blink values are now correct (OnsCPDefend 0.5, OnsCPAttack 2, Seeker 2, FlagReturn 2 baked from spritelookupblinkvalue). Texts corrected (DANGER, Frozen!, White/Red base, Flag carrier, etc.); KeyCarrierFriend/KeyCarrierFinish added. Verified line-by-line against Base all.inc. logic/values/presentation now faithful.
- waypoints.gametype.collect: a second LIVE objective producer was added — Domination.CollectWaypoints emits one fixed control-point marker per point (DomNeut/Red/Blue/Yellow/Pink at origin+'0 0 32', team radar tint). Reached on the real match path via ServerNet.SendWaypoints:924 -> GameType.CollectWaypoints each tick (was CTF-only). Still partial (most gametypes absent), but one genuine live-producer gain.

### `overkill-weapons` — 2 closed / 9 open
- okmachinegun.primary_auto: muzzle flash / brass-casing eject (EjectCasing, g_casings>=2 gate) / punchangle recoil PRNG (random()-0.5, gated !g_norecoil) / EFFECT_RIFLE tracer trail / fireBullet_falloff with the damagefalloff cvars are now wired LIVE on the player path, gated EntFlags.Client — line-for-line with Base okmachinegun.qc:24-58. Presentation went partial->mostly-faithful (recoil/casing/tracer), at parity with the base Machinegun port's bullet presentation.
- presentation.fx: the shared Wave-2 FX seam (EjectCasing/tracer/recoil via WeaponFiring + EffectEmitter live Sink) is now exercised by an OK weapon (okmachinegun) for the first time; the recoil/casing/tracer portion is no longer fully missing.

### `race` — 2 closed / 17 open
- race.records.notifications: was stub/dead -> now faithful/live. Wave-2 added RecordFinishTime -> SendRecordNotification, which fires the correct INFO_RACE_NEW_IMPROVED/NEW_SET/NEW_BROKEN/FAIL_RANKED/FAIL_UNRANKED/NEW_MISSING_UID line plus FinishRacer->RACE_FINISHED and AbandonRaceCheck->RACE_ABANDONED. Verified via NotificationSystem.Info (the same live broadcast Ctf/Domination use) and cross-checked all 8 call sites' arg counts against the (stringCount,floatCount) declarations in NotificationsList.cs — all match. Live path: CrossCheckpoint->RecordFinishTime. The old dead race.Events dependency is bypassed.
- race.win.condition: was partial -> now faithful/live. Wave-2 modeled the global race_completing latch: the first racer to hit the lap limit trips StartCompleting() (sets RaceCompleting, abandons every currently-dead racer + INFO_RACE_ABANDONED), and OnDeath->AbandonRaceCheck auto-completes a dead racer respawning into a completing race. Sudden-death now gates on RaceCompleting. This is the multi-racer drain-to-(n==c) state machine the baseline VERIFIER flagged as absent. Live via GameWorld.cs:1752 race.Tick/CheckWinningCondition; existing overtime test still green.
- Partial improvement (NOT counted as closed): race.checkpoint.ordered_crossing gained a coded spawnflag-4 wrong-way kill (Combat.Damage 10000), but it is DEAD — WireObjectiveSpawns (GameWorld.cs:1519) calls SpawnCheckpoint without punishWrongWay:true, so no live checkpoint arms it; it also uses DeathTypes.Void instead of Base DEATH_HURTTRIGGER. Row stays partial.

### `scoring` — 2 closed / 3 open
- scoring.helper.float2int_decimal_carry: was DEAD + truncating, now genuinely LIVE and faithful. Wave-2 deleted the unwired ClanArena.AddDamageScore and replaced it with OnSplitHealthArmor subscribed to GameHooks.PlayerDamageSplitHealthArmor — a hook that DamageSystem.cs:438 fires unconditionally on every damage application; CA.Activate (which subscribes) is invoked live at GameWorld.cs:1378. The new AddScoreFloat2Int does the real QC per-player decimal carry (carry+=value*factor/100; whole=floor(carry+0.5); AddToPlayer; carry-=whole) instead of the old (int)(dmg*d2s/100) truncation. g_ca_damage2score default 100 matches Base. logic/values/timing partial->faithful, liveness dead->live.
- scoring.winningcondition.remaining_frags_announce: was fully MISSING (no port caller for the 'N frags left' cue), now implemented + LIVE for Deathmatch and TDM. GameScores.CountFragsRemaining mirrors world.qc:1592-1622 (fragsleft formula, leadlimit_and_fraglimit MAX/MIN, _fragsLeftLast once-gate) and broadcasts ANNCE REMAINING_FRAG_{1,2,3}; reached every tick via OnEndFrame -> DriveGametypeFrame -> DM.RecomputeLeader (GameWorld:1706) / Tdm.UpdateLeaderAndCheckLimit (GameWorld:1719); latch reset in each Activate. logic/values/timing/audio missing->faithful.

### `sv-cheats` — 2 closed / 11 open
- GIVE_ALL impulse 99 is now genuinely LIVE (was dead/stub): CmdImpulse (Commands.cs:1379) routes imp==99 -> _world.Cheats.GiveAll. Full live path verified: client `impulse 99` -> ClientCommandRegistry allows 'impulse' -> CmdImpulse -> GiveAll runs the sv_cheats gate + counts. _world.Cheats is a live instance with Log wired to ChatBroadcast. Now logic/values/timing faithful + live; it is the first impulse-side cheat that is actually reachable and produces cheat counts in a real match.
- Refusal-attempt logging added (Cheats.cs:Refuse, wired Cheats.Log -> ChatBroadcast): closes the gating row's baseline 'missing bprintf attempt logging' gap on the live cmd path (god/notarget/noclip/fly/give + GiveAll now broadcast the QC "Player ... tried to use cheat '...'" notice on refusal).

### `sv-teamplay` — 2 closed / 12 open
- sv-teamplay.bot.bot_forced_team: missing->faithful, dead->live. bots.txt argv5 forced-team column is now parsed (PickNameAndModel) and honored on the live bot-add path: SpawnBot pins via Teamplay.SetBotForcedTeam and re-runs AssignBestTeam, which early-outs on GetBotForcedTeam and keeps that team with no auto-balance (faithful to TeamBalance_JoinBestTeam bot_forced_team early-out).
- sv-teamplay.bot_vs_human.team_split: logic partial->faithful, liveness partial->live. The team-segregation half is now implemented (Teamplay.AllowedTeams, QC TeamBalance_CheckAllowedTeams) and LIVE: AssignBestTeam filters candidates by AllowedTeams on the connect join, so a bot_vs_human 2-team match actually bans bots to one side and humans to the other (positive ratio -> bots=blue/humans=red, matching Base for standard TDM). Count sizing was already faithful.

### `warpzones` — 2 closed / 9 open
- warpzones.teleport.player: logic partial->faithful. Wave-2 replaced the velocity-direction gate (Dot(velocity,InForward)>0->skip) with Base's exact plane-side test Dot((origin+view_ofs)-InOrigin, InForward) >= 0 -> skip (= WarpZone_PlaneDist, server.qc:193/common.qc:476), AND added the warpzone_teleport_finishtime same-frame re-teleport guard (_teleportFinishTime keyed per entity, one-frame hold). Verified live via trigger .Touch -> Teleport on the per-tick touch pass. Two baseline gate gaps closed.
- warpzones.teleport.usetargets: logic partial->faithful. Added wz.Partner (QC this.enemy, set in Link/LinkPair) and now fires UseTargetsEx on BOTH this (SkipTarget1|SkipTarget3) and this.enemy (SkipTarget1|SkipTarget2), matching SUB_UseTargets_SkipTargets server.qc:218-219 with the correct BIT(1)/BIT(2)/BIT(3) masks. Partner-zone target relay now works.

### `weapon-blaster` — 2 closed / 5 open
- bolt.touch_radiusdamage: g_projectiles_interact==1 knockback hack now faithfully ported and live (default cvar 1, reachable predicate)
- offhand.fire: refire interval now scaled by W_WeaponRateFactor via the real mutator hook (values/timing partial->faithful)
- bot.aim_dodge: generic ShotLead now leads the blaster bolt at speed 6000 (aim half closed; row missing->partial)

### `weapon-vortex` — 2 closed / 4 open
- weapon-vortex.achievements (missing/dead -> faithful/live): yoda + impressive announces are now genuinely implemented and LIVE. Vortex.Attack captures flying=IsFlying(actor) BEFORE the trace (vortex.qc:122), FireRailgunBullet was changed to RETURN the first pierced victim, and the new Vortex.Announce re-derives QC's yoda/impressive_hits locally and fires NotificationSystem.Announce(ACHIEVEMENT_YODA/IMPRESSIVE). vortex_lasthit lives in a ConditionalWeakTable, reset on WrSetup. Verified team gate (!Teams.SameTeam + self-exclusion) is equivalent to QC DIFF_TEAM in both FFA and teamplay. Driver calls WrThink every tick, so the path is live.
- weapon-vortex.attack.overcharge_sound (audio partial -> faithful): the overcharge nexcharge zap now plays at the charge-proportional volume VOL_BASE*(charge-0.5*animlimit)/(1-0.5*animlimit) with VOL_BASE=0.7 inlined, passed via Api.Sound.Play(..., volume: vol). Bit-faithful to vortex.qc:139; the volume arg exists on ISoundService.Play so it is genuinely wired.

### `duel` — 1 closed / 6 open
- duel.items.powerup_filter (stub/dead -> faithful/live): Wave 2 added Duel.OnFilterItemDefinition + subscribed it via MutatorHooks.FilterItemDefinition.Add in Duel.Activate (Duel.cs:132-133). Verified Duel.Activate is dispatched live at GameWorld.cs:1466 in ActivateGameType (run from match start, GameWorld.cs:521), and the chain is consumed on the live item-spawn path at StartItem.SpawnInternal (StartItem.cs:103). Returns !WithPowerups for powerup classnames (item_strength/item_shield/item_invincible), matching the real powerup spawnfuncs and Base instanceOfPowerup; default g_duel_with_powerups 0 filters powerups out. This was the #2 worst-gap on the baseline -- genuinely closed.

### `items-pickups` — 1 closed / 8 open
- FuelRegen/Jetpack pickup center-prints (CENTER_ITEM_FUELREGEN_GOT / _JETPACK_GOT) are now GENUINELY LIVE + faithful: ItemGiveTo (ItemPickupRules.cs:106-116) calls NotificationSystem.Center on the live give path, gated on Pickup.IsPowerup AND the newly-granted flag (newFlags & FuelRegen/Jetpack) exactly like QC items.qc:581-587, placed before the IT_PICKUPMASK transfer so the flag is still un-held. Notification names + ItemFlag bits all exist; Center is a real client dispatch. Closes one of the two match:false constants in items-pickups.give.item_giveto.

### `mayhem` — 1 closed / 4 open
- mayhem.loadout.weapon_arena (the baseline WORST gap) is now CLOSED and LIVE. Wave-2 wired MutatorHooks.SetWeaponArena.Call into SpawnSystem.ComputeStartItems and added ExpandWeaponArena + StartItemFlagBits. Traced the full live path: PutPlayerInServer:544 -> ApplyStartLoadout:806 -> ComputeStartItems:662 -> SetWeaponArena.Call:679. Mayhem.OnSetWeaponArena sets 'most_available', ExpandWeaponArena OR's every WEP_FLAG_NORMAL non-hidden non-blocked weapon (verified Blaster/Crylink/Devastator/... carry WeaponFlags.Normal), and the arena adds UNLIMITED_AMMO+UNLIMITED_SUPERWEAPONS which now reach Entity.Items. dead->live, logic partial->faithful. (Same seam also fixes CA/LMS/Instagib/Melee/Overkill.)
- Verified the new SetWeaponArena->SetStartItems interaction is faithful to QC ordering: the arena ORs in IT_UNLIMITED_SUPERWEAPONS, then Mayhem.OnSetStartItems strips both flags and re-adds UNLIMITED_AMMO conditionally (matches world.qc @2089 arena OR then @2161 SetStartItems clear+reset). Superweapons do NOT get unlimited duration, correct.

### `mutator-bloodloss` — 1 closed / 3 open
- threshold.live_reread: Wave-2 (commit ec788f7) replaced the static `Threshold = 25f` field (cached once in Hook()) with an expression-bodied property `Threshold => Api.Cvars.GetFloat("g_bloodloss")` that reads the cvar live on every hook call — now bit-faithful to Base, which reads autocvar_g_bloodloss directly in each HOOKFUNCTION. Status logic/values: partial→faithful. The dead 25f fallback and the Hook()-time caching block were both removed.

### `mutator-cloaked` — 1 closed / 4 open
- set_default_alpha row (mutator-cloaked.alpha.set_default_alpha): the SetDefaultAlpha hook is now genuinely LIVE — MutatorHooks.FireSetDefaultAlpha (real .Call site) is invoked at worldspawn from GameWorld.cs:541 after MutatorActivation.Apply, and seeds GameWorld.DefaultPlayerAlpha/DefaultWeaponAlpha = 0.25. This closes the row's own scope: logic stub->faithful, timing missing->faithful, liveness dead->live, value 0.25 faithful.
- Partial infrastructure on default_alpha_consumers: a real per-entity alpha NETWORK channel was added (NetEntity EntityField.Alpha 1<<17 + NetEntityState.Alpha encode/decode; ServerNet QuantizeAlpha of p.Alpha/e.Alpha), and a real player-model RENDER apply (PlayerModel.ApplyAlpha -> per-instance GeometryInstance3D.Transparency, called each frame from Pose).
- Invisibility powerup now has a live alpha producer: PowerupsMutator.cs:216 sets player.Alpha = _invisibilityAlpha (0.15) while held (first live writer of the entity alpha channel).

### `mutator-globalforces` — 1 closed / 1 open
- spread.hook: the dead-player exclusion gap is genuinely CLOSED. Wave-2 (commit ec788f7) replaced IsLivePlayer (Client flag AND DeadState==No) with IsPlayer (Client flag only, no DeadState test) at GlobalForcesMutator.cs:68. Verified adversarially that the port's death path (DamageSystem.cs:628-632) leaves the corpse with EntFlags.Client set and ClassName unchanged (still "player", set once at Player.cs:35), so FindByClass("player") + the Client-flag IsPlayer now BOTH accept the corpse — exactly mirroring Base's classname-only IS_PLAYER (utils.qh:9). Corpses near a blast are now shoved like in Base; status logic partial -> faithful.

### `mutator-invincibleproj` — 1 closed / 3 open
- core.editprojectile_zero_health: liveness partial -> live. Wave-1 added Projectiles.MakeShootable (Projectiles.cs:71-99), which installs e.GtEventDamage = ShootDown. The pre-existing DamageSystem.EventDamage (DamageSystem.cs:331-335) already dispatches a non-player victim's GtEventDamage on the live damage path, so the projectile shoot-down mechanic the mutator defends is now genuinely produced. ShootDown's first line `if (self.Health <= 0f) return;` means a projectile whose Health the mutator zeroed never reaches the gate/detonation -> enabling g_invincible_projectiles now has the real observable effect (projectiles cannot be shot down). MakeShootable is called live from Devastator/Electro/Hagar/Mortar/Minelayer/Seeker/Arc fire paths; EditProjectile.Call fires per-projectile in those same paths; GetResource(Health)/SetResourceExplicit and ShootDown all read the same Entity.Health field (Resources.cs:44,60 / Projectiles.cs:85,90).

### `mutator-nades` — 1 closed / 20 open
- mutator-nades.lifecycle.cleanup: Wave-2 (commit ec788f7) added a VehicleEnter hook to NadesMutator that tosses the held nade on boarding (Toss(player,true,(0,0,100),max(NadeWait,Time+0.05))) — bit-faithful to sv_nades.qc:667. VehicleEnter.Call has live sites in VehicleBoarding.cs:192/223, so this hook is genuinely LIVE on the boarding path. Closes 1 of the 4 bullets in this row; status upgraded logic missing->partial, liveness dead->partial.

### `mutator-new_toys` — 1 closed / 3 open
- startitems.rearrange RANDOM mode FIXED: port previously picked exactly ONE replacement token via Prandom.RangeInt (coin-flip); Wave-2 rewrote OnSetStartItems to OR BOTH the core and new-toy tokens into the resolved set then rebuild Weapons from the union — now bit-faithful to QC's nt_GetReplacement bit-OR into newdefault. The stale Prandom import was removed. logic+values upgraded partial->faithful for the main spawn path.
- NewToysMutator docstring corrected (no longer claims 'MapObjectsRegistry registers no weapon_* spawnfuncs'); it now accurately states ItemSpawnFuncs.WeaponSpawn DOES register one weapon_<netname> spawnfunc per weapon and that the missing piece is the SetWeaponreplace/FilterItem hook chain. Documentation accuracy only — no behavior change.

### `mutator-offhand_blaster` — 1 closed / 5 open
- fire.offhand_think VALUES gap closed: Wave-2 added W_WeaponRateFactor scaling to the offhand refire (FireOffhand now does OffhandNextThink = now + refire * WeaponRateFactor(player); the WeaponRateFactor helper applies 1/g_weaponratefactor then calls MutatorHooks.WeaponRateFactor, which has LIVE producers in BuffsMutator and PowerupsMutator). This matches Base jump_interval = time + refire*W_WeaponRateFactor(actor). Baseline had this as match:false/partial.
- fire.blaster_attack: Wave-2 added the g_projectiles_interact==1 deflect hack to Blaster.OnTouch (temporarily sets DamageForceScale=1 on a touched projectile so the blast can knock it around, then restores) — a small fidelity improvement to the shared Blaster touch (Base default g_projectiles_interact=1). Already-faithful row stays faithful; not a liveness change.

### `mutator-pinata` — 1 closed / 4 open
- enable.predicate: Wave-2 replaced the float read GetFloat("g_pinata")!=0 with a string read evaluated by ExprEvaluate(GetString("g_pinata")), closing the baseline 'reads as float not expr_evaluate' gap for the realistic 0/1 values and aligning with the sibling expr_evaluate idiom. Still live and registered; faithful for stock values.

### `mutator-random_gravity` — 1 closed / 3 open
- random_gravity.roll.gamestart_gate: the `time < game_starttime` pre-match suppression gate is now ported (RandomGravityMutator.cs:68) and live-wired via StartItem.GameStartTimeProvider (GameWorld.cs:550 -> GameStartTime, the real countdown-end synced from Warmup). Was logic:missing/timing:missing/liveness:na -> now faithful/faithful/live. Gravity no longer re-rolls during the warmup countdown on the live server path. Verified the provider is a shared, exercised seam (also consumed live by DamageSystem/Domination/MonsterAI), not dead.

### `mutator-running_guns` — 1 closed / 3 open
- SetDefaultAlpha hook chain now has a LIVE invoker: GameWorld.cs:541 calls the new MutatorHooks.FireSetDefaultAlpha(1f,1f) at worldspawn (the world.qc:SetDefaultAlpha() equivalent), on the same real map-load path as MutatorActivation.Apply. Baseline verification row 2 ('no .Call site anywhere — chain invoked zero times', fail) is genuinely closed — the handler runs and GameWorld.DefaultPlayerAlpha becomes -1 when g_running_guns!=0.
- A port equivalent of world.qc SetDefaultAlpha() now exists (GameWorld.cs:537-541) and seeds DefaultPlayerAlpha/DefaultWeaponAlpha globals (GameWorld.cs:126/129).

### `mutator-spawn_unique` — 1 closed / 0 open
- enable.cvar: Wave-2 (commit ec788f7) replaced the simplified ExprEvaluate bool parser (""/"0"/"false"->false) with a full, line-faithful port of Base expr_evaluate (lib/cvar.qh:48-103). The row went logic: partial -> faithful.
- Empty-string divergence closed: traced "" through both impls -> Base returns TRUE (0 tokens, ret=!false), port now returns TRUE via TokenizeConsole("")->empty. Previously port returned FALSE.
- Full grammar now present and verified: leading +/- sign handling, tokenize_console with quoted spans, all 8 BINOPs (>=,<=,>,<,==,!=,===,!==) in Base's exact strstrofs order (incl. the == before === quirk), and the bare-term boolean((ftos(f)==k)?f:cvar(k))==b path. Cvar/CvarString of unknown names return 0/"" matching QC.
- Liveness re-confirmed: MutatorActivation.Apply (GameWorld.cs:535) calls Hook() when IsEnabled; both SpawnScore.Call (SpawnSystem.cs:434) and PlayerSpawn.Call (ClientManager.cs:570) live with correct (player, spot) arg order.

### `mutator-stale_move_negation` — 1 closed / 1 open
- smneg.tunables: the explicit-0 edge case is fixed. Wave-2 replaced the `if (a != 0f) BonusAsymptote = a;` / `if (cf != 0f) CooldownFactor = cf;` zero-guards in Hook() with a new ReadFloat(name, fallback) helper that returns the seeded default only when the raw cvar string is empty and otherwise honors the literal value, including an explicit 0. This now matches Base reading autocvar_g_smneg_bonus_asymptote / autocvar_g_smneg_cooldown_factor directly. Baseline gap (the only one on this row) closed; row is fully faithful with no remaining gaps.

### `mutator-superspec` — 1 closed / 10 open
- enable.predicate FULLY CLOSED: SuperSpecMutator.cs now exists, carries [Mutator] (source-gen -> Mutators.All), IsEnabled = expr_evaluate(g_superspectate) reading the real cvar (declared mutators.cfg:172 default 0), and is added on the live boot path (MutatorActivation.Apply at GameWorld.cs:535). Faithful + live.
- playerdies.followkiller PARTIALLY fixed: the hook is genuinely ported AND subscribed to the LIVE PlayerDies chain (Hook -> MutatorHooks.PlayerDies.Add; fired DamageSystem.cs:589), condition mirrors Base. NOT fully closed because its body is unreachable (see regression_notes: ASF_FOLLOWKILLER can never be set).
- state.flag_bitfields PARTIAL: ASF_*/SSF_*/ASF_ALL now bit-identical to Base 8-25 and a per-client Options store exists (SSF_VERBOSE/empty-filter connect defaults faithful); intended_divergence flagged (side-table not Player .fields). Still dead because nothing writes it.
- spectate.transfer, msg.helper, itemfilter.match PARTIAL: SpectateTransfer (faithful follow-cam set, intended divergence vs engine Spectate), Msg (silent/verbose/spamlevel gating now faithful, output still a no-op stub), and FilterItem (empty=all + classname-token allowlist, faithful) are all ported but have no reachable live caller.

### `mutator-vampirehook` — 1 closed / 5 open
- latch.aiment_follow CLOSED: GrapplingHookTouch (Hook.cs:284) now sets self.Aiment = other unconditionally on every latch and clamps the follow offset into the player bbox with the same +4/-4 inset as Base SetMovetypeFollow (util.qc:2140-2142). This was the baseline root cause that made the entire mutator inert.
- drain.engine is now LIVE (was 'dead'): with aiment non-null on a player latch, the owner/aiment guard (VampireHookMutator.cs:88) no longer short-circuits; GrappleHookThink.Call dispatches each think (Hook.cs:322, NextThink=time) and the per-hook last_dmg debounce gates the drain. Verified reachable: hook is MoveType.Fly + Solid.Corpse with SuperContentsBody, so engine FlyMove dispatches Touch onto players on the live path.
- drain.heal_indirection liveness flipped dead->live for the same reason (drain body now reachable).

### `sv-antilag` — 1 closed / 5 open
- sv-antilag.arc_beam CLOSED: Arc.cs:BeamTick now wraps its per-frame damage trace in the shared LagComp.Begin(actor)/End() bracket (Arc.cs:301-307). Verified genuinely live, not just compiled: LagComp.Provider is installed at ServerNet.cs:298, BeamTick is on the live primary/burst-beam fire path (Arc.cs:176), and the bracket routes to the same verified BeginLagComp (gated g_antilag==2, honors cl_noantilag, rewinds players+monsters+nades at ANTILAG_LATENCY) that FireBullet uses. Row upgraded partial/na -> logic faithful + liveness live, constant match true, gap removed; removed from unit worst_gaps.

### `sv-intermission` — 1 closed / 13 open
- sv-intermission.timer.exittime: the IntermissionThink +10s post-hold auto-advance grace is now LIVE and faithful. Intermission.Think (Intermission.cs:100-123) introduces InputGracePeriod=10f and flips ReadyToChangeLevel at now>=ExitTime+10s; it is on the real match path (GameWorld.CheckRulesAndIntermission line 1899, driven per-tick). This corrects the baseline's false 'input grace = 0' constant (now base 10 == port 10) and moves values partial->faithful and timing partial->faithful. Verified against Base server/intermission.qc:497-505 (time<exittime return; then time<exittime+10 && !buttons return).

### `tmayhem` — 1 closed / 6 open
- tmayhem.loadout.weapon_arena: the single biggest baseline gap is GENUINELY CLOSED. Was liveness:dead / logic:partial (SetWeaponArena had no .Call site anywhere, so players spawned Blaster-only). Wave-2 added MutatorHooks.SetWeaponArena.Call at SpawnSystem.cs:679 (inside ComputeStartItems, reached on the real spawn path via ApplyStartLoadout<-SpawnSystem.cs:544) and :892 (warmup). TeamMayhem subscribes OnSetWeaponArena in Activate (runs at boot before any spawn); it fills 'most_available', new SpawnSystem.ExpandWeaponArena resolves it to weapons_most + grants UNLIMITED_AMMO/SUPERWEAPONS, and ComputeStartItems replaces the Blaster-only DefaultLoadout with the full arsenal. Verified all referenced APIs exist (Weapons.All, WepSet.Has, ExpandWeaponArena, ItemFlag.Unlimited*). Now liveness:live / logic:faithful / values:faithful.
- tmayhem.loadout.start_items: re-verified the now-adjacent hook ordering matches QC's readplayerstartcvars exactly (arena ORs in both UNLIMITED flags at world.qc:2089, then SetStartItems at world.qc:2161 -- same function -- clears both and re-adds only UNLIMITED_AMMO). The port reproduces this sequence, so an active arena nets UNLIMITED_AMMO only. No bug introduced by the new arena wiring; added a constant row documenting the clear-then-re-add.

## Regressions & wired-but-dead traps (SECOND-PASS BACKLOG)

Genuine regressions, plus Wave-2 code that compiles but isn't live. These are the second-pass fix targets.

### `monster-zombie`  (2 regressions)
- REGRESSION (audio na -> partial) on setup.mr_setup: Wave-2 added MonsterSound(e,st,'spawn',...) in Setup (MonsterAI.cs:312). The zombie 'spawn' cue is COMMENTED OUT in zombie.dpm_0.sounds (Base spawn is silent), and no monsters/zombie_spawn asset exists (sound/monsters/zombie/ ships only death/idle/sight.ogg). The port now attempts a sound Base never plays.
- REGRESSION (audio na -> partial) on attack.melee: Wave-2 added MonsterSound(self,st,'melee',...) on a connecting swing (MonsterAI.cs:1165). The zombie 'melee' cue is also commented out in Base (silent); the port attempts monsters/zombie_melee.wav, which doesn't exist. Net: the zombie_melee.wav was deleted from the block path but re-introduced on the melee-connect path for a cue Base silences.
- Root cause of both regressions + the unchanged pain-sound defect: the port's MonsterSound plays a literal monsters/<name>_<cue>.wav for EVERY cue, with no per-cue check of whether that cue is DEFINED (uncommented) in the model's .sounds file. Base's Monster_Sound reads this.(samplefield); a commented-out cue yields an empty sample and sound7 plays nothing. The port needs the same defined/commented gate so zombie pain/melee/spawn/ranged stay silent.

### `damage-pipeline`  (1 regressions)
- fire.burning REGRESSION (wyvern + Inferno buff): the Wave-2 burn-tick rewrite changed the per-tick formula from Strength*0.05 to dps*frametime (treating Strength as raw dps via fallback). But MonsterFramework.AddFireDamage (MonsterFramework.cs:96) was NOT updated - it still pre-scales Strength = dps/0.05*FrameTime for the OLD convention and never sets FireDamagePerSec. The new tick then multiplies that already-scaled value by frametime AGAIN, so wyvern fireball (Wyvern.cs:164) and the Inferno buff (BuffsMutator.cs:437) now deal ~dps*FrameTime^2/0.05 (about 0.4% of intended at 72fps). These two sources burned roughly correctly under the old tick; the rewrite broke them. Fix: route AddFireDamage through StatusEffectsCatalog.FireAddDamage (pass raw dps). Flagged as a spawn task (task_a32750e7).

### `mutator-breakablehook`  (1 regressions)
- TEAMPLAY REGRESSION introduced by Wave-2 in breakablehook.owner.punish_splash: the new DiffTeam(a,b) helper keys off the STATIC bool GameScores.Teamplay, but that static is never assigned on the server gameplay path. Its only writer is ScoreInfoBlock.Apply (the CLIENT network deserialise), and ScoreInfoBlock.Apply/Serialize/Deserialize have ZERO live callers in the entire src tree (the scoreboard layout block is not wired into the snapshot composer). So server-side the static stays false every match, and DiffTeam always takes the FFA branch !ReferenceEquals(a,b). Consequence: in a TEAM game (CTF/TDM/CA), breaking a TEAMMATE's hook now wrongly fires the punish (5 splash to the teammate + force-removes the chain); Base DIFF_TEAM uses team!=team and skips same-team. The pre-Wave-2 port used !Teams.SameTeam, which handled teamplay correctly, so this is a true regression vs BOTH Base and the old port. Same dead-static dependency exists in NadeOrbHelper.DiffTeam (NadeOrbHelper.cs:33) and Teleporters (Teleporters.cs:124,366) -- a shared bug class. Spawned a follow-up task to wire GameScores.Teamplay (or route through the authoritative world.Teamplay.IsTeamGame). The existing test (MutatorBatchT51Tests.cs:519) is an FFA self-hit and does not exercise the teamplay path, so it stays green and hides the regression.

### `physics-movetypes`  (1 regressions)
- step.physics REGRESSION (Wave-2): routing MOVETYPE_STEP through WalkMove exposed an applyGravity gate that only checks `ent.MoveType == MoveType.Walk` (MoveTypePhysics.cs:331). Base walk.qc:12 gates on `WALK || STEP`. So a MOVETYPE_STEP body now gets applyGravity=false and gets NO gravity in FlyMove — it slides and stair-steps but never free-falls. Before Wave-2 the freefall PhysicsStep DID apply gravity to STEP bodies, so the alias change traded the stepping gap for a gravity gap on the monster corpse/body path. One-line fix (add `|| ent.MoveType == MoveType.Step`); flagged as a spawn_task.

### `survival`  (1 regressions)
- survival.win.timeout is now LIVE but DIVERGES from stock Base. Wave-2 wired RoundTimedOut()→EndRoundTimedOut() (survivors win) into CheckWinningCondition on the live path (GameWorld.cs:1745), so a hiding-prey round now ends at g_survival_round_timelimit (120s). But it OMITS Base's gate `autocvar_g_survival_round_enddelay == -1`. The shipped default is enddelay=0 (gametypes-server.cfg:704), so in stock Base Surv_CheckWinner returns 0 when both sides are alive at the timer, and round_handler_Think never force-ends on round_endtime — stock Base does NOT auto-resolve a timed-out round. The port now ends + awards (survivor +1 reward) a round that stock Base would leave running. This is a real new behavioral divergence, not a clean fix. NOTE: the baseline's framing ('the headline survive-until-time rule is dead') was itself partly mistaken — in stock config Base intentionally does not resolve on the timer.
- Secondary: the new timeout reads surv.Handler.RoundEndTime, which is armed by the OWNED handler's own countdown timeline, not the live GameWorld.Rounds map-reset timeline (Survival, unlike CA/FreezeTag, wires neither _roundPrep nor _roundSync at GameWorld.cs:1468), so the 120s clock can desync from the map-reset/respawn handler.
- Interaction risk: with teamkill punishment now live but Bot_ForbidAttack still unported, an ally-attacking bot now mirror-kills (suicides) itself.

### `sv-commands-votes`  (1 regressions)
- REGRESSION (live, real): vote.parse_whitelist — Wave-2 added an 'allready' case that rejects unless VoteController.WarmupStage() is true, but WarmupStage is NEVER wired in GameWorld.WireCommandsWarmupVoting (defaults static () => false). 'allready' IS in the default sv_vote_commands whitelist, so an allready vote is now ALWAYS rejected with 'Game already started. Use the resetmatch command...' even during warmup — previously it fell through and was votable. Net regression: warmup allready voting is broken. Root cause is the same unwired-hook pattern as the dead vote guards; wiring Voting.WarmupStage = () => Warmup.WarmupStage in GameWorld would fix it.

### `sv-spawnpoints`  (1 regressions)
- spawnpoint_idle_glow per-team tint is BROKEN by the Wave-2 change. SpawnPointParticles.TeamColorRgb switches on team == 1/2/3/4, but a spawnpoint's e.Team carries the port's CSQC team encoding Teams.Red/Blue/Yellow/Pink = 4/13/12/9 (Teams.cs:12-15). Result: a RED spot (Team 4) hits case 4 and is tinted PINK; blue(13)/yellow(12)/pink(9) fall through to white/untinted. The baseline 'untinted' gap is not closed — it is replaced with an incorrect-color defect (red is now objectively wrong). Correct table must switch on Teams.Red/Blue/Yellow/Pink as Ctf.cs:379-386 (TeamRadarColor) does. RGB values themselves are correct. Flagged as a one-line-fix background task (task_274cf96e).

### `turret-machinegun`  (1 regressions)
- lifecycle.damage / setup.flags: turret retaliation is now an ACTIVE divergence. TurretAI.Damage:690 sets `turret.Enemy = attacker`, and Wave-2 made Damage() live via the GtEventDamage shim. But Base's turret_damage (sv_turrets.qc:207-250) has NO such line, and TFL_DMG_RETALIATE — while set in the machinegun's inherited default damage_flags (sv_turrets.qc:1274) — is never READ by any Base code (grep confirms only the BIT def + the unread default-set). So the port turret now turns to face/acquire whoever shot it from anywhere, bypassing the normal scan/score acquisition; Base never does this. Previously the baseline correctly recorded this code as dead (inert); Wave-2 activated it, turning a moot extra into a live behavioral defect. Low gameplay impact but a genuine fidelity regression.

### `turret-plasma_dual`  (1 regressions)
- aim.lead_splash_predict (zPredict): PlasmaDualTurret.Think passes zPredict:true, but Base plasma tr_setup aim_flags = LEAD|SHOTTIMECOMPENSATE|SPLASH and NEVER sets TFL_AIM_ZPREDICT (and the dual has no tr_setup override, so it inherits plasma's flags). turret_aim_generic gates the gravity-arc z-prediction purely on TFL_AIM_ZPREDICT, so the dual mis-leads airborne targets down an arc the Base plasma never predicts. Wave-2 CORRECTED the single plasma (MakeParams now zPredict:false) but left the dual at true — an asymmetric defect. Baseline had mis-graded this row faithful (it claimed Base sets zPredict on the gravity branch, which is false), so it is a long-standing inaccuracy now precise rather than a strict status downgrade, but the single-vs-dual asymmetry is genuinely new.
- Not a code regression but worth flagging: the death_blast divergence (port Die() actively does an ammo-scaled RadiusDamage that Base turret_die has COMMENTED OUT) remains UNCHANGED by Wave-2 and is still a port-added blast Base does not deal.

### `vehicle-spiderbot`  (1 regressions)
- REAL REGRESSION in frame.locomotion (values): the same Wave-2 change that fixed the gravity-step TIMING also replaced the hardcoded factor 0.5 with GravityTicrateFactor(), which reads autocvar_sv_gameplayfix_gravityunaffectedbyticrate via Api.Cvars.GetFloat. That cvar is NOT registered/seeded anywhere in the port (grep confirms only references). EngineServices.GetFloat returns 0f for unregistered cvars (EngineServices.cs:370), so the factor resolves to 1.0 instead of the QC stock 0.5 -> the walk/strafe manual gravity sub-step (g*dt*sv_gravity at velz<=20) is now 2x too strong vs Base default. The port's own engine treats this fix as default-true (FlyMove.cs:61 GravityUnaffectedByTicrate=true), so the cvar read is both wrong-vs-default and engine-inconsistent. Before Wave-2 the factor was hardcoded 0.5 (correct). Flagged a spawn task (task_bdddbe58) to fix it. Net for the row: timing partial->faithful (a gain) but values faithful->partial (the regression), so the row stays values:partial overall.

### `assault`  (dead-wiring)
- CAUTION (not a regression, but a wired-but-dead trap): DriveFrame, TimeLimitReached, StartSecondRound, CanRoundStart, CanRoundEnd grew real bodies this wave (full timelimit check, role swap) but have ZERO live callers (verified by grep across src/ tests/ game/). They compile and look done but are not invoked. GameWorld.cs:1461 still calls bare EnableRounds() for Assault with NO round callbacks, while CA and FreezeTag got full CheckTeams/CheckWinner round wiring in the same wave - Assault was skipped.
- CAUTION: the campaign-end fix in DestroyFinalObjective (Assault.cs:465, campaign = TargetUtilities.IsCampaign?.Invoke()) is DEAD: TargetUtilities.IsCampaign is declared at TargetUtilities.cs:376 but is NEVER assigned anywhere in the entire repo (src/, game/, engine/, net/), so the delegate is null and the campaign branch can never fire. Single-round campaign Assault still never ends.

### `bot-ai`  (dead-wiring)
- TWO DEAD WIRINGS (not regressions, but the new code has no behavioral effect -- flagged as still-open and spun off as task_9efd5856): (1) move.danger -- Wave-2 added a Waypoint.Danger field and made A* add nb.Danger to path cost, but NO producer ever sets Danger (no botframe_updatedangerousobjects analogue; BotPopulation.cs:201 admits it's unported), so Danger is always 0 and routing is unchanged. The baseline 'per-waypoint danger-cost' gap is therefore NOT genuinely closed. (2) The bunnyhop 1.25x LinearCost branch reads WaypointNetwork.Skill, which is never assigned anywhere (defaults 0 -> branch always false), so high-skill route costs are not actually scaled -- identical to pre-Wave behavior.

### `bot-waypoints`  (dead-wiring)
- Two Wave-2 additions are dead scaffolding, NOT closed gaps: (1) WaypointNetwork.Skill + the skill>=7 1.25x bunnyhop-speed cost branch in LinearCost — Network.Skill is never assigned anywhere (only the SEPARATE BotNavigation.Skill/BotBrain.Skill are set), so it stays 0 and LinearCost always uses base speed exactly as before. (2) Waypoint.Danger + the A* term gScore+link.Cost+nb.Danger — nothing ever writes Danger (botframe_updatedangerousobjects still explicitly unported, BotPopulation.cs:201), so the term is always +0. These compile and read plausibly but produce no behavioral change on the live match path.

### `clanarena`  (dead-wiring)
- CRITICAL NEAR-MISS (not a regression - was dead in baseline, still effectively dead, but now for a subtler reason worth flagging): clanarena.scoring.damage2score is now fully implemented (decimal-carry, excess subtraction, friendly-fire debit, environmental self-penalty) AND subscribed on the live damage hook (DamageSystem.cs:438) - BUT its QC round-phase gate reads ca.Handler.IsRoundStarted, and ca.Handler (the owned Common RoundHandler) is NEVER ticked: ClanArena.Tick() has zero callers, and MirrorRoundTiming copies only RoundEndTime/RoundsPlayed, not the phase. So IsRoundStarted is permanently false and OnSplitHealthArmor always returns early at ClanArena.cs:299 -> the SCORE column stays 0 in every match. Fix: tick ca.Handler, OR mirror IsRoundStarted/GameStartTime in MirrorRoundTiming, OR read the live GameWorld.Rounds phase. The SAME un-ticked-handler bug would silence NotifyLastPlayerForTeam's IsRoundStarted gate if it had a caller (it doesn't).
- DriveGametypeFrame case ClanArena is now a bare `break` (CheckRound removed to avoid double-award now that the live handler drives CheckWinner) - this is correct, not a regression: CheckRound is now dead-but-harmless.

### `cts`  (dead-wiring)
- Watch item (not a regression, inside still-partial row): cts.death.respawn_rules — OnDeath now sets RespawnFlag.Force, but it is DEAD: RespawnTiming.Calculate (DeadPlayerThink death edge, GameWorld:1819) does p.RespawnFlags = flags (RespawnTiming.cs:75), clobbering it; and the respawn delay still reads generic g_respawn_delay_* (~2s) not CTS g_cts_respawn_delay -1. So the Force set compiles but isn't wired through — classic dead-on-the-live-path. removeprojectiles still unmodeled.

### `duel`  (dead-wiring)
- Non-blocking test-staleness (NOT a code regression, out of scope to edit): tests/XonoticGodot.Tests/FilterItemHookTests.cs:108 (DuelFilterItem_NotRegistered_PowerupStillSpawns) still asserts/documents the OLD dead behavior ('Duel.FilterItem is NEVER registered ... must NOT fire'). It still passes only because it boots a plain server without activating the Duel gametype, so the now-live subscription never registers. The assertion contradicts the now-fixed behavior and should be updated to activate Duel and assert the powerup IS filtered.

### `items-pickups`  (dead-wiring)
- No regressions. But three Wave-2 additions are DEAD (compile, never fire) and must NOT be mistaken for fixes: (1) cl_autoswitch/CTS autoswitch re-selection logic was ported faithfully into ItemGiveTo head+tail (ItemPickupRules.cs:68-83/135-155) but gates on AutoswitchProvider/CtsAutoswitchProvider/CtsActiveProvider (ItemPickupRules.cs:175-189) which are DECLARED-ONLY — never assigned anywhere. Commands.cs:324 wires Inventory.PriorityProvider at server boot but skips these; the replicated cl_autoswitch value lives in Commands._autoswitch/GetAutoswitch with no bridge. AutoswitchEnabled() therefore always returns false -> picking up a better weapon STILL never auto-switches in a real match (baseline worst-gap NOT closed, only reframed missing->dead). (2) q3compat item origin offset (z += -15 - mins.z) added to StartItem.cs:228-241 but gated on Q3CompatProvider (StartItem.cs:50) which is never assigned -> no-op; Q3/QL-map items still sit ~15qu low. (3) Resources.TakeResourceWithLimit added (faithful to sv_resources.qc:205) but has ZERO callers — GiveAmmoTo still uses TakeResource(res,-amount) at ItemPickupRules.cs:241-242. No unit test was added for autoswitch or center-prints, so the FuelRegen/Jetpack fix is code-read-verified, not test-verified.

### `keyhunt`  (dead-wiring)
- CAUTION (not a regression, but a liveness trap to flag): drop.voluntary_use (DropOneKey) is now FULLY coded - throwvelocity 400, delay_collect gate, DROP notif+sound - but is DEAD. The +use key (WeaponImpulses) routes only to VehicleBoarding.UseKey; the PlayerUseKey mutator hook that should reach kh.DropOneKey is not wired. Baseline had this as plain missing; it is now missing-on-the-live-path-but-coded. A reader could mistake the implementation for a live feature.
- CAUTION: the PUSH branch of LoserTeam (score_push + KH_PUSHES + INFO_KEYHUNT_PUSHED) is implemented but unreachable - nothing ever sets key.Pusher (no off-map/push tracking). ProtectTime is set on drop but only feeds this dead branch, so it has no observable effect. Only the destroy branch is live.

### `monster-framework`  (dead-wiring)
- NOT a regression but a re-verified-still-open defect worth surfacing: MonsterAI.MatchHalted (MonsterAI.cs:1741) reads Scoring.GameScores.GameStopped, which has NO live writer anywhere (only the public setter exists). The server actually drives match-stopped into VehicleCommon.GameStopped (GameWorld.cs:1243 = Intermission.Running||MatchEnded||Timeout.IsPaused) — the global vehicle/mutator ports read. So the game_stopped half of the new prematch/halt guard is dead on the live path; monsters keep targeting/roaming through intermission and timeout. Trivial one-line fix (read VehicleCommon.GameStopped or the g_game_stopped cvar). Registry rows target.validtarget + move.brain + worst_gaps updated with this concrete root cause.

### `mutator-buffs`  (dead-wiring)
- No regressions found. The highest-risk row to re-check was buffs.jump.velocity: I confirmed it is correctly STILL DEAD, not falsely upgraded. JumpVelocityOverride is written only by BuffsMutator (519/524) + declared at EntityMutatorState.cs:158; PlayerPhysics.cs:822 still reads mp.JumpVelocity[Crouch] (cvar-built), and the PlayerJump hook (826-829) that could override mjumpheight is NOT subscribed by BuffsMutator (it subscribes PlayerPhysics, which has no reader). The Wave-2 PlayerPhysics.cs edits were footstep-only (g_footsteps gate + jittered cadence + ladder/lastground gating), unrelated to jump. A Jump-buffed player still jumps at normal height.

### `mutator-dynamic_handicap`  (dead-wiring)
- enable.predicate: NOT a true behavioral regression, but the ONE Wave-2 change for this unit is a 'compiles-but-dead' non-fix. Wave-2 added `&& !HandicapDisabled` to DynamicHandicapMutator.IsEnabled, where HandicapDisabled = `g_cts != 0 || g_race != 0` (DynamicHandicapMutator.cs:51-53), intending to port Base HANDICAP_DISABLED() == IS_GAMETYPE(CTS)||IS_GAMETYPE(RACE). But the port NEVER sets the bare g_cts/g_race cvars to 1: gametypes-server.cfg defaults them to 0 ('script-ignore'), there is no Cvars.Set('g_cts'/'g_race') anywhere in src/ or game/, and gametype selection (GameWorld.ResolveGameType) picks a C# object without stamping a select cvar. The codebase actually detects CTS/RACE via `GameType is Cts`/`GameType is Race` (GameWorld.cs:496 CompatRemaps.IsCtsActive, GameWorld.cs:2579). So in a real CTS/RACE match HandicapDisabled stays false and the mutator still activates — the exact gap the guard was meant to close. I dropped liveness on this row to `dead` rather than calling it a regression because the guard is inert/harmless in the common non-CTS/RACE case and the pre-Wave state also activated in CTS/RACE. The bundled doc-comment fix (correctly re-identifying HANDICAP_DISABLED as the CTS/RACE gate, replacing the old 'sv_cheats lock' mislabel) is a genuine improvement. Recommended real fix: gate on the same `GameType is Cts/Race` provider, not the unset cvars. NOTE: PowerupsMutator.cs:233 has the identical g_cts-cvar reliance but is latent there (0 yields the correct non-CTS default).

### `mutator-kick_teamkiller`  (dead-wiring)
- CAUTION (not a regression, but the action.punish 'fix' must not be over-trusted): enforcement is still incomplete on all three severity branches. (1) Default play-ban force-spectate sets attacker.IsObserver=true DIRECTLY instead of calling ClientManager.PutObserverInServer (ClientManager.cs:400), so a live spawned offender keeps MoveType.Walk/Solid.SlideBox/DamageMode.Aim/FragsPlayer — body stays solid, damageable, effectively still in the match. (2) g_playban_list is written but NEVER enforced at join: ClientManager.cs:274 explicitly defers the playban gate and Bans.IsPlayBanned is only consulted by MinigameSessionManager, so the offender can press fire / autojoin and rejoin. (3) Severity 1 (kick) and 2 (IP-ban) remain dead: both only set LastAction + broadcast, and LastAction has ZERO source-code observers (grep src/**/*.cs) — Bans.DropClient/KickBanClient are never reached. The doc-comment's claim that 'the host observes LastAction to perform the kick/ban' is aspirational; that wiring does not exist.

### `mutator-multijump`  (dead-wiring)
- No genuine regressions found. The Wave-2 changes are strict improvements or no-ops on the default path. Caveat (not a regression, but important): the client_gate fix is INCOMPLETE -- ClientMultijumpProvider is declared and read but NEVER assigned by any host code (grep over src/ shows only the in-mutator declaration+read; the analogous Inventory.PriorityProvider IS wired at Commands.cs:324). So the per-client cl_multijump opt-out/cap still cannot fire; it always resolves to ClientDefault=-1->1. This is a dead seam ('compiles but isn't wired'), so client_gate is partial, not faithful. The rejump_grant ready-gate truthiness factor consumes this same dead value, so on the default path it can never gate false -- but it still matches Base default behavior, so rejump_grant itself is faithful.

### `mutator-powerups`  (dead-wiring)
- OUT-OF-UNIT latent bug (not a powerups regression, flagged as a spawned task): the helper CsqcModelEffectFlags.RoleGlowFlags / CsqcModelAppearance.ComposeForcedAppearance maps the powerup glow colors BACKWARDS (strength->EF_RED, shield->EF_BLUE) vs Base. It has NO live caller, so it does NOT affect the live powerup glow (which uses the correct PowerupsMutator->player.Effects path). It belongs to the cl-csqcmodel unit; if the Wave-3 forced-appearance path is ever wired it would invert the colors.

### `mutator-sandbox`  (dead-wiring)
- No genuine regressions. The one near-miss to flag: menu.tools_dialog stays faithful/dead exactly as in the baseline — Wave-2 added a server-side SandboxMutator backend but did NOT route the menu/console `sandbox …` commands to its HandleCommand (no SV_ParseClientCommand mutator-hook seam, `g_sandbox` absent from ClientCommandRegistry), so the buttons remain inert end-to-end and there is still no normal-match entry point (dev --menu-screen sandbox only). Status unchanged, gap text updated; not a downgrade.

### `mutator-status_effects`  (dead-wiring)
- No genuine regressions found: every baseline-faithful/live row (apply, net.entity, hud.powerups_feed) is unchanged or improved. Two NON-regression caveats worth flagging as new partial-defects introduced by the Wave-2 sound wiring (they were 'audio: missing' in the baseline too, so not a regression — but the new code LOOKS wired yet silently no-ops): (1) burning RemovalSound='steam_burst' is set on the def but no 'steam_burst'/desertfactory sound is registered in SoundsList -> Sounds.ByName returns null -> silent; (2) stunned RemovalSound='ons_spark1' is the wrong CASE for the Ordinal registry key 'ONS_SPARK1' (Registry.cs uses StringComparer.Ordinal) -> null -> silent. Only superweapon 'POWEROFF' actually resolves. These are cheap one-line fixes (register the sound / fix the case).
- framework.removeall_lifecycle advanced from 'missing' to 'stub' but is STILL DEAD: RemoveAll/ClearAll are now written (with per-effect m_remove + sound) but grep confirms ZERO host callers — they are not wired to PlayerDies/MonsterDies/ClientDisconnect/MakePlayerObserver/reset_map_global/PutClientInServer. Stale timers can still survive death/respawn/map reset. This is the single highest-value remaining wire-up.

### `mutator-superspec`  (dead-wiring)
- No true regressions (no baseline-faithful/live row got worse). IMPORTANT liveness caveat to flag: the Wave-2 change is largely COMPILED-BUT-NOT-WIRED. The followkiller PlayerDies hook is subscribed to the live chain, but its guarded body can NEVER execute in a real match because ASF_FOLLOWKILLER (and every autospec/superspec flag) is only settable via the autospec/superspec console commands, which require an SV_ParseClientCommand mutator hook chain that does NOT exist in MutatorHooks.cs. The ClientConnect hook (which sets the SSF_VERBOSE default + loads options) is also absent. Net: the option store is created with defaults but is read-only-reachable. So the 5 'ported' helpers (followkiller, SpectateTransfer, Msg, FilterItem, the flag store) are all effectively dead on the live path until the SV_ParseClientCommand + ItemTouch + ClientConnect hook chains land. Also note: msg.helper's sprint/centerprint output is stubbed (`_ = ...` discards) — even if reached it prints nothing; and FilterItemHookTests.cs covers the UNRELATED StartItem/nix/melee_only FilterItem chain, NOT superspec.FilterItem (no test coverage for this unit).

### `mutator-walljump`  (dead-wiring)
- No genuine regressions. The fx.sound row LOOKS improved (Wave-2 switched from the hardcoded raw path "player/jump.wav" to the correct per-model API SoundSystem.PlayPlayerSound) and logic is now faithful, but the cue is STILL silent because it passes null for modelSoundDir, which resolves to sound/player/default.sounds/jump — and `default.sounds` is a TEXT MANIFEST, not a directory; the port never parses .sounds manifests, so AssetLoader probes a non-existent file. Net audibility is the same as the baseline (silent), so this is neither a fix nor a regression — but worth flagging as a not-actually-closed change. The same unresolved-default-dir gap afflicts DamageSystem pain/death voices (ModelSoundDir is a `return null` stub).

### `net-entity-state`  (dead-wiring)
- No true status regression. But adversarial verification downgraded replicate.client_cvars liveness faithful->partial: Wave-2 (ec788f7) expanded the CLIENT send list (ClientNet.cs ReplicatedCvars) to mirror the full Base REPLICATE set, which LOOKS like a closure, but the server-side SentCvarAllowlist (Commands.cs:179-190) was NOT touched, so cl_autoscreenshot/cl_clippedspectating/cl_handicap(+given/taken)/g_xonoticversion are dropped at the gate (Commands.cs:1900). The send-side widening with no consumer is a compiles-but-dead change.
- cl_autoswitch_cts is now sent AND allowlisted AND stored via SetClientCvar, yet its sole consumer ItemPickupRules.CtsAutoswitchProvider is never assigned anywhere in src/ or game/ -> CtsAutoswitchCvar always returns the -1 null default and CTS autoswitch never fires. End-to-end dead.
- appearance_hook: Wave-2 added 4 new helpers (RespawnGhostClearsColors, BaseGlowmod, GuardGlowmodZero, ApplyHdrGlowClamp) but they have ZERO callers outside CsqcModelAppearance.cs -> dead code; the respawn-ghost decoloring + glowmod-palette/zero-guard/HDR-clamp fidelity is still not applied on the live render path (ModelTint does not call them).

### `overkill-weapons`  (dead-wiring)
- No genuine REGRESSION (no baseline-faithful/live row was made worse by the waves). BUT a serious baseline OVER-CLAIM was corrected on okrpc.primary_chainsaw_missile: the baseline marked it logic:faithful, liveness:live and asserted 'Shootable (takedamage=Yes, health 25 -> ProjectileDamage->Explode)'. In reality the missile shoot-down is DEAD and always has been — OkRpc.cs (unchanged by the waves) sets missile.TakeDamage=Yes + missile.ProjectileDamage=Explode but never calls Projectiles.MakeShootable, the only thing that installs the GtEventDamage shim the damage pipeline dispatches. DamageSystem.EventDamage (DamageSystem.cs:331) only routes a non-player victim through GtEventDamage; a missile with no GtEventDamage falls into the player-resource PlayerDamage path and NEVER runs Explode. So you cannot shoot the chainsaw out of the air. I downgraded that row logic faithful->partial, liveness live->partial, and result fail. This is a baseline-audit correction, not a wave-induced regression. The ironic part: the waves ADDED MakeShootable (now used by Mortar/Devastator/Minelayer/etc.) but did not wire it into okrpc — one-line fix: Projectiles.MakeShootable(missile) after seeding health+ProjectileDamage.

### `resources`  (dead-wiring)
- No genuine regressions. One nuance worth flagging, not a regression: the newly-ported TakeResourceWithLimit (api.take) has zero callers in the port, so liveness for that row was set to 'partial' (TakeResource live, TakeResourceWithLimit dead). Base SVQC TakeResourceWithLimit is likewise uncalled (only the CSQC odd form is used), so this is parity-neutral, not a behavior regression.

### `sv-cheats`  (dead-wiring)
- No true regressions. One CAVEAT on the new refusal-logging (not a regression vs baseline, since baseline had NO logging at all): the port's Allowed() logs on the dead-player guard AND the observer guard, but Base CheatsAllowed returns 0 SILENTLY on those two guards and only logs at the FINAL sv_cheats==0 fall-through. So the port over-logs cheat attempts by dead players / observers. Also the message uses p.NetName instead of Base's team-colored playername(netname, team, false). Recorded as gaps on sv-cheats.gating.cheats_allowed (presentation downgraded to partial); the gating logic itself remains faithful + live.

### `sv-mapvoting`  (dead-wiring)
- No genuine regressions. Every baseline-faithful/live row stayed at least as good: override_chain (faithful/live), finish.winner (live), lifecycle (live) are unchanged or improved. No row went live->dead or faithful->partial.
- Caveat (not a regression, but a Wave-2 trap to flag): much of the new MapVoting.cs logic COMPILES but is not wired on the live match path. CastVote/Abstain still have zero callers; DispatchImpulse still early-returns during intermission (Commands.cs:1357); GameWorld.DriveEndOfMatchMapFlow still calls the 3-arg Start (no suggestions) and applies the map on MapVote.Finished without consulting the new ReadyToChangeLevel 1 s delay. The client panel's SetVote/SetVotes/SetWinner still have zero callers and there is still no ENT_CLIENT_MAPVOTE net sync, so the vote screen never appears. The reduce cvars g_maplist_votable_reduce_time/_reduce_count remain unregistered (reduce_count -> 0 -> wrong keep mode) and the misnamed g_maplist_votable_keeptwotime=15 still sits unread in Cvars.cs:397.

### `sv-world-rules`  (dead-wiring)
- CAUTION (not a regression, but a correctness caveat on the 'fixes'): a large part of the new Wave-2 warmup machinery is CODED-BUT-DEAD. The host never wires CampaignActive, MapMinPlayers, OnCountdownStop, OnLockTeams, or TimeoutActive (verified by grep: no setters exist), and teamplay_lockonrestart + g_start_delay are not registered cvars (FloatOr falls back to 0). Consequences: the minplayers/badteams countdown-abort + COUNTDOWN_STOP notifications never fire (MinPlayers=0, badteams hardcoded false), the campaign 3s countdown never applies (CampaignActive=false => 10s path), team-lock-on-restart never fires, and the dedicated 15s g_start_delay join window collapses to 0. These rows are recorded as partial/dead-seam, not as fully closed.

### `tka`  (dead-wiring)
- Watch item (not a regression, but a notable dead-wire): tka.score.bctime and tka.score.timepoints had a Tick(dt) method WRITTEN in Wave-2 (correct accrual + carried-ball orbit + remainder handling), but it is NEVER CALLED - GameWorld.cs:1738 still does `case TeamKeepaway tka: tka.CheckPointLimit(); break;` whereas the sibling `case Keepaway ka: ka.Tick(...)` at :1740 calls Tick. So bctime/timepoints/orbit remain dead. These were already not-closed in the baseline (no Tick existed at all), so net liveness is unchanged (still dead) - not a regression, but the single most fixable remaining gap (one-line wiring).

### `tmayhem`  (dead-wiring)
- Caveat (not a regression, pre-existing): Mayhem.cs exposed DefaultTimeLimitMinutes=15 'for the host to apply on select', but GameWorld never reads it (dead scaffolding), and tmayhem has no equivalent. So the timelimit=20 gametype-default gap is STILL OPEN, just as in the baseline -- still mitigated because the global timelimit cvar default is already 20.

### `turret-flac`  (dead-wiring)
- No regressions found. Every baseline-faithful/live row stayed faithful/live; all five changed rows moved strictly toward Base. The new live event_damage path is an improvement (it wires previously-dead pre-damage gating), not a regression.

### `turret-plasma`  (dead-wiring)
- No regressions found. Every baseline-faithful/live row is still faithful/live, and the Wave-2 edits only tightened parity. Verified no double-death (EventDamage fires Combat.Death only when DeadState==No; OnAnyDeath guards DeadState!=Respawning) and that all newly-passed TurretParams fields are genuinely consumed (ScoreTarget/SelectTarget/Track/AimPoint), not dead-wired.

### `turret-tesla`  (dead-wiring)
- No regressions found. All five baseline-faithful/live rows (identity, tr_setup, lifecycle framework, spawn.liveness, weapon.chain mechanic) remain intact. The shot_force->0 and sound-removal changes are correct toward Base, not regressions: Base toast() uses force '0 0 0' and the turret actor never reaches the electro_fire emission. Adversarial check on realowner: for a map-placed turret turret.Owner is null so 'turret.Owner ?? turret' = turret, and a turret never validates as its own player/missile target, so damage always applies -- exactly Base's net effect; the branch is now present and correct.

### `turret-walker`  (dead-wiring)
- No genuine regressions found. One caveat worth flagging (not a regression): the newly-ported SWIM gait is DEAD on the live path — Entity.WaterLevel is only computed by PlayerPhysics, and MOVETYPE_STEP turrets never get it set, so e.WaterLevel stays 0 and the WETFEET/SWIMMING/SUBMERGED branches of WalkerMoveTo never fire. The swim logic compiles and is correct but is unreachable; recorded as liveness: dead on drive.gaits_extra rather than a regression (baseline had swim missing).

### `vehicle-bumblebee`  (dead-wiring)
- NOTABLE NEW DEFECT (still_open, not a regression since it was `missing` before): impact.bouncepain — Wave-2 added a Bumblebee bouncepain impl with the CORRECT constants ('1 100 200', 0.25s gate) BUT the WRONG signature `public void Impact(Entity vehicle, Vector3 oldVelocity)` (Bumblebee.cs:380). It is NOT declared `override` of the base virtual `Vehicle.Impact(Entity)`; the dispatcher VehicleCommon.Touch -> `vehic.VehicleDef.Impact(vehic)` (single-arg, VehicleCommon.cs:767) therefore calls the empty base no-op. Result: implemented-but-DEAD (compiles, never invoked). Racer/Raptor override correctly; only the bumblebee mismatched. Fix = make it `override Impact(Entity)` and snapshot OldVelocity in Think like Racer/Raptor do via VehicleCommon.Impact.
- Latent (unchanged): the symbolic SoundsList VEH_BUMBLEBEE_FIRE -> flacexp3 mapping is wrong, but the cannon plays the correct literal 'vehicles/bumblebee_fire.wav', so it's a dead/unused mapping, not a live audio defect.

### `vehicle-framework`  (dead-wiring)
- NOT a regression of a previously-faithful row, but a NEW DEFECT inside the new crush feature: Spiderbot.Spawn (Spiderbot.cs:119) sets vehicle.Touch = null AFTER VehicleCommon.SpawnVehicle installs the crush/touch handler. Base spiderbot vr_spawn does NOT reset touch (settouch func_null appears only in vr_death). Net effect: the ground walker most likely to run a player over never crushes; crush is live only on 3/4 vehicles. combat.crush logic=partial, liveness=partial.
- Dead-code additions that did NOT close their gap: VehicleCommon.PainFrame (faithful body, ZERO callers -> combat.painframe still dead), VehicleCommon.InitVehicle (wraps the real FireVehicleInit hook but has ZERO callers -> VehicleInit hook still dead). These compile but are not wired on the live path.

### `vehicle-raptor`  (dead-wiring)
- CAUTION — a NEW feature was added that is DEAD, not a fix: raptor.impact (vr_impact) got a correct Impact override + VehicleCommon.Impact body in Wave-2, BUT it has no live caller. Raptor.Spawn line 141 sets vehicle.Touch = null, which wipes the VehicleCommon.Touch dispatcher (installed by SpawnVehicle) that is the only thing that calls VehicleDef.Impact; AND Raptor.Think never snapshots vehicle.OldVelocity (Racer.Think does, Racer.cs:296), so the impact delta would always be the full velocity. Net behavior vs Base is unchanged (raptor still takes no crash damage). Recorded as liveness: dead, verification fail. This is the recurring 'compiles but unwired' pattern — adversarially confirmed against the live Racer (whose identical Impact IS live). Spawned a fix task (task_5a2f582b).
- Minor: raptor.spawn.setup mass=1 is set but INERT — the port's MoveTypePhysics never reads .Mass, so the QC bounce-missile mass tuning has no behavioral effect (value set for fidelity only).

### `warpzones`  (dead-wiring)
- No true regressions. BUT one Wave-2 change is dead/inconsistent and is recorded as STILL OPEN, not a fix: warpzones.client.prediction. TriggerTouch.PredictWarpzonesAmbient was added (TriggerTouch.cs:192) but has ZERO callers -- game/net/EntityMovementStep.cs invokes only PredictJumppadsAmbient and PredictTeleportsAmbient, never the new warpzone predictor -- so a warpzone crossing still rubber-bands. Worse, the new predictor still uses the OLD velocity-direction gate that Wave-2 deliberately removed from the authoritative Teleport, and its docstring falsely claims the gate matches authority EXACTLY; if it were wired it would mis-predict vs the server. Row downgraded liveness na->dead, logic missing->stub to reflect the unwired/inconsistent code (still effectively a no-op, so the feature outcome is unchanged from baseline).

### `weapon-arc`  (dead-wiring)
- No genuine regressions. IMPORTANT non-fix to flag: Wave-2 added a LagComp.Begin/End antilag bracket around the beam damage trace (Arc.cs:301-307), but LagComp.Provider is never assigned anywhere in src/ (no ILagCompensation implementation is registered; Net/AntilagBuffer.cs's LagCompensation class does NOT implement the Common interface). So the bracket is a no-op on every path -- it produces NO actual lag compensation. The dps_core antilag gap is therefore only seam-present, not behaviorally closed. Not a regression (Base was already identical without antilag) but it should not be mistaken for a working fix. Same dead LagComp.Provider applies to WeaponFiring.cs:216/364, affecting all hitscan weapons.

### `weapon-fireball`  (dead-wiring)
- One NEW defect introduced by the Wave-2 firemine work (tracked inside the existing secondary.firemine.lifecycle row, not a new row): the make-it-hot decouple sets Entity.Owner=null, but the port's Entity.RealOwner is a COMPUTED ALIAS of Owner (DamageEntityState.RealOwner => Owner). In Base, the decouple nulls only .owner and keeps .realowner=firer, so a post-decouple burn is still credited to the firer; in the port, nulling Owner also nulls RealOwner, so the firemine's burn/obituary after decouple is attributed to the mine itself (FireAddDamage owner = self.RealOwner ?? self = self) rather than the original firer. The scorch-own-firer behavior works; only the damage/kill credit is wrong.

### `weapon-machinegun`  (dead-wiring)
- No genuine regressions. No baseline-faithful/live row became worse. All Wave-2 edits either closed gaps or (in one case) added dead code without breaking an existing path.
- NOTE (not a regression, a correction): the baseline's #1 worst gap — 'burst secondary kills wrongly obituary as SPRAY instead of SNIPE' — was largely WRONG. Base's W_MachineGun_Attack_Burst fires with thiswep.m_id and NO HITTYPE_SECONDARY, so Base burst ALSO reads MURDER_SPRAY. The SNIPE obituary is only reachable from Base's mode-0 secondary (m_id | HITTYPE_SECONDARY), which is dead by default (mode 1). So in default play the port matches Base; the genuine SNIPE defect is narrow and confined to the non-default mode-0 secondary (machinegun.fire.single_mode0).
- CAUTION (still-open, dead code): the Wave-2 'casing eject' (machinegun.presentation.muzzle_impact) compiles but is DEAD. WeaponFiring.EjectCasing routes through EmitByEffectInfoName('casing_bullet'), which has no registered Effect, so the EffectRequest carries Effect=null — and ServerNet.WriteEffect / ln.Encode both DROP null-Effect requests. The casing is never networked to clients; EffectSystem.SpawnCasing is never reached. Casings remain effectively NOT ported. Same null-Effect drop will silently kill any other weapon that relies on EjectCasing (rifle/shotgun/okmg) — worth a shared follow-up: register casing_bullet/casing_shell effects or add a dedicated casing net-temp.

### `weapon-minelayer`  (dead-wiring)
- No genuine regressions. One residual gap to flag (not a regression): the W_MineLayer_Damage knock-loose-while-alive branch (damageforcescale bounce + .wait re-stick + avelocity spin on a non-fatal hit) is structurally unreachable, because the shared Projectiles.ShootDown shim only invokes the per-weapon ProjectileDamage callback once hp<=0. So OnMineDamage's hp>0 branch never runs — a shot mine that survives is not detached, it only explodes when its 15 hp is depleted. This is a shared-shim limitation affecting any weapon needing a per-hit (hp>0) side effect; worth a cross-weapon fix. Net it is still an improvement over the baseline (where shoot-down was fully dead/mis-routed).

### `weapon-porto`  (dead-wiring)
- No genuine regressions found. One thing to watch (NOT a regression vs baseline, both were broken): the slick/clip-reflect and noimpact-fail branches were ADDED to OnTouch with correct sounds, but IsSlickOrClip/IsNoImpact are hardcoded `return false` (Porto.cs:288,293), so those branches are unreachable headless. Net behavior (always places on a world hit) is the same as baseline; the registry now records them as branch-present-but-dead rather than missing.
- PortalClearWithId is declared (Porto.cs:331) and invoked on cnt<0 fail/timeout (Porto.cs:245,357) but the host hook is NEVER assigned in GameWorld.cs (only PortalSpawner at line 412 is wired) -> the combined-shot in-portal cleanup is a silent no-op. This is a new dormant seam, not a regression.

### `weapon-rifle`  (dead-wiring)
- Caveat (NOT a regression, but an incompletely-closed change to flag): rifle.casings now has live-looking code (EjectCasing called with the correct g_casings>=2 gate and exact QC eject velocity) but is DEAD on the networked path. It emits via EmitByEffectInfoName('casing_bullet') which makes an EffectRequest with Effect=null; ServerNet.WriteEffect / EffectNetProtocol.Encode drop any null-Effect request, so it never networks, and even client-side Spawn('casing_bullet') routes to the generic particle path, not EffectSystem.SpawnCasing (the real brass-shell spawner). Status kept liveness:dead / presentation:missing. Same dead path now also affects MachineGun and Shotgun casings.

### `weapon-shotgun`  (dead-wiring)
- No true behavioral regression vs baseline, but ONE latent correctness trap introduced: the out-of-ammo auto-melee fallback is now PRESENT in WrThink (emptyPrimaryAutoMelee branch) yet DEAD — it gates on PrepareAttack(actor, slot, FireMode.Secondary), which checks st.ButtonAttack2 (WeaponFireGate.cs:60). On an empty PRIMARY press ATK2 is not held, so PrepareAttack returns false and the slap never fires. Player-observable behavior matches baseline (empty primary press still does nothing), but the code now looks wired when it isn't. Fix: gate the auto-melee on the primary button / a private path that doesn't require ATK2.
- Casing eject is only half-fixed: the server seam (Shotgun.Attack -> EjectCasing, gated + deterministic velocity) is live, but the client never renders a casing because the emitted effect-name 'casing_shell' is not mapped to EffectSystem.SpawnCasing (still zero callers); it falls into the generic particle path. Also the eject up-velocity uses the machinegun value (70 - rand*5) not the shotgun's (30 - rand*5), and the gate is g_casings>=2 vs Base shotgun's >=1.
