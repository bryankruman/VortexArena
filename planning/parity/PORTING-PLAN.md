# Parity Porting Plan — wave-based, maximally parallel

_Generated by `tools/parity-plan-gen.py` from `_plan-analysis.json` (385 work-items over 185 port files). Regenerate after any `parity-diff --update`._

Execution plan for closing the gaps in [PARITY-GAPS.md](PARITY-GAPS.md), in **4 waves**. The ordering is forced by one fact: a small set of **shared seams** living in cross-cutting *hot files* gates most gaps. Once those exist, the rest is overwhelmingly independent and runs in two big parallel pushes, then a verification gate.

## The parallelism rule (read first)

**One agent owns one file.** Two agents must never edit the same file in the same wave — that is the entire reason for the wave split. The cross-cutting **hot files** are owned by **Wave 1**; after Wave 1, every Wave-2 file is touched by exactly one agent, so 100+ agents run with zero edit conflicts. Run a wave's agents in parallel (use `isolation: worktree` when they touch sibling files), then **barrier**: build + tests + `parity-diff` before the next wave. Each row below names the **file** an agent owns and the **shard(s)** (`registry/<shard>.yaml` + `specs/<shard>.md`) that hold its exact gaps/constants.

## Wave map at a glance

| Wave | What | Parallel agents | Depends on |
|---|---|---|---|
| **1 — Seams** | 18 shared-infra capabilities in 8 hot files | ~18 | — |
| **2 — Gameplay leaves** | every gameplay unit's own-file gaps | ~164 (1 per file) | Wave 1 |
| **3 — Presentation** | client/HUD/FX/audio that renders Wave-2 state | ~13 (1 per file) | Wave 2 |
| **4 — Verify** | build, in-game checks, `parity-diff --update` re-baseline | sequential gate | 1–3 |

122 of 385 work-items are fully self-contained leaves (no shared dependency); ~255 need ≥1 seam — which is why Wave 1, though small, is the only real bottleneck.

## Wave 1 — Shared seams (the only bottleneck)

Independent capabilities, each **owning its hot file(s)** — they run in parallel. `fan-out` = distinct units unblocked once it lands. Do `critical`/`high` first; `low` rows are cosmetic and can be deferred.

| seam | pri | fan-out | what to build | owns (files) |
|---|---|---|---|---|
| **W1-round-handler** — Round-handler tick + per-gametype callback dispatch | critical | 14 | Wire the dead Common RoundHandler so it actually ticks and invokes each round-based gametype's CanRoundStart/CanRoundEnd/CheckWinner, round-start+end timing, grace/warmup countdown, and round timelimit/stalemate. Owns the round + gametype drive in GameWorld.cs. | GameWorld.cs, RoundHandler.cs |
| **W1-projectile-net** — Networked projectile entity (model + trail + shoot-down) | critical | 31 | Assign NetEntityKind.Projectile and network a real projectile entity with model/skin + smoke trail, and route RadiusDamage onto projectiles so they can be shot down. Unblocks every projectile weapon's visuals. | NetEntity.cs, Projectiles.cs |
| **W1-weaponfire-fx** — Shared weapon-fire FX/audio hooks (tracer, ricochet, casings, woosh) | critical | 51 | Add the shared FireBullet/fire-path emission seam: bullet tracer trailparticles, impact ricochet PlayRic dispatch, shell-casing eject, melee woosh. Unblocks every hitscan weapon's signature FX/audio at once. | WeaponFiring.cs, EffectSystem, SoundSystem |
| **W1-input-impulse** — Player input / impulse routing | critical | 31 | Route currently-unbound impulses to handlers: flag throw/pass/request-pass, key voluntary +use drop, objective +use, weapon reload routing for non-Reloadable weapons (tuba), cheat impulses. | WeaponImpulses.cs / impulse routing |
| **W1-damage-channel** — Damage deathtype/hittype channel + per-entity damage scale | high | 18 | Carry the full string deathtype/hittype through the int-keyed damage path (monster/turret/vehicle/special deaths) and let gametypes/entities consult per-entity DamageScale/DamageForceScale. | DamageSystem.cs |
| **W1-mod-icons** — Gametype mod-icon HUD feed (server status -> client render) | high | 8 | Establish the gametype status-block feed end-to-end: server status send, NetGame Kind dispatch, ModIconsPanel mode framework. Wave-2 gametypes push state; Wave-3 adds per-mode render. | GametypeStatusBlock.cs, NetGame.cs, ModIconsPanel.cs |
| **W1-startitems** — Gametype start-loadout / weapon-arena wiring | high | 4 | Wire SetStartItems gametype subscription + the dead SetWeaponArena .Call site so modes can set spawn loadouts and arenas (CA 200/200 'most', etc.). Owns the start-items path in SpawnSystem.cs. | SpawnSystem.cs |
| **W1-notify-driver** — Notification driver edges (frags-left, leader-visible, lead-change) | high | 56 | Add the shared per-frame notification computations in the match/frame driver: 'N frags left' fragsleft, LMS leader-visibility edges, lead-change. (Per-event Send calls stay with each Wave-2 unit.) | match/frame driver, notification helpers |
| **W1-mutator-hooks** — Mutator hook-dispatch wiring (revive dead .Call chains) | high | 0 | Many mutator hooks are subscribed but their .Call dispatch site is missing, so the mutator does nothing live. Wire the dispatch sites so subscribed hooks fire on the live path. | MutatorHooks.cs + dispatch sites |
| **W1-ball-frame** — Ball entity framework (spawn/think/touch/carry) | high | 3 | Shared ball lifecycle: the host-side procedural ball spawner (SpawnBall has zero callers today, so no ball ever exists), think/idle-reset glide, touch/carry, respawn. Unblocks Keepaway, Team Keepaway, Nexball. | shared ball entity + GameWorld spawn |
| **W1-objective-csqc** — Objective entity networking + CSQC (generators / control points / objectives) | medium | 5 | Networked objective world models + icons + progressive damage-model swaps + death-cam for Onslaught generators/control-points and Assault objectives; plus onslaught_link graph-edge resolution. | Onslaught/Assault objective net+render |
| **W1-alpha-net** — Entity alpha networking (default_player_alpha + per-entity Alpha) | medium | 8 | Seed default_player_alpha at worldspawn, add a per-entity Alpha network field, render transparency. Unblocks the Cloaked mutator and any fade/invisibility. | NetEntity.Alpha, world init, PlayerModel render |
| **W1-csqcmodel** — Player-model animation-decide + effect/color pipeline | medium | 6 | Shared player-model anim-decide + effect-flag/forced-color/glow pipeline that client presentation consumes (frozen tint, role colors, forced skins). | Csqc model pipeline |
| **W1-entity-frame** — Turret/Vehicle entity framework (cvars, networking, damage, touch) | medium | 16 | Shared turret+vehicle base: cvar table, CSQC model/aim networking, event-damage routing, touch/enter-exit dispatch every individual turret/vehicle depends on. (Niche modes — lower priority.) | Turrets/ + Vehicles/ shared base |
| **W1-mapvote-net** — Mapvote / end-match vote networking | medium | 10 | Networked end-match map/gametype vote entity + client vote-screen feed. | MapVoting.cs + net + vote screen |
| **W1-mapobject-seam** — Map-object host seam (centerprint-net, sounds, event-damage, movetype parse) | medium | 18 | Shared map-object plumbing several func_/trigger_/target_ entities need: centerprint networking, sounds key, event-damage, platform movetype parse. | MapObjects/ shared + MapLoader |
| **W1-bot-aim** — Per-weapon bot-aim framework | low | 8 | The bot weapon-aim (wraim) framework per-weapon bot tuning hangs off. Framework only — per-weapon values are Wave-2 weapon work. | Bot/ aim framework |
| **W1-active-mutators** — Active-mutators advertisement (BuildMutatorsString + MUT_ HUD icons) | low | 22 | BuildMutatorsString/PrettyString + the MUT_* active-mutator HUD-icon registry. Broad but purely cosmetic — defer if trimming Wave 1. | mutator string builder + HUD |

Wave 1 also **absorbs every work-item that lands in a hot file** (one owner per file). Hot files and their work-item counts:

| hot file | work-items | top shards feeding it |
|---|---|---|
| `src/XonoticGodot.Server/GameWorld.cs` | 21 | mapobject-func, scoring, sv-client-lifecycle, sv-intermission, sv-ipban, sv-world-rules |
| `src/XonoticGodot.Common/Gameplay/Player/SpawnSystem.cs` | 10 | resources, sv-client-lifecycle, sv-spawnpoints |
| `src/XonoticGodot.Net/NetEntity.cs` | 4 | net-entity-state |
| `src/XonoticGodot.Common/Gameplay/Weapons/WeaponFiring.cs` | 4 | sv-antilag, sv-world-rules |
| `src/XonoticGodot.Common/Gameplay/Damage/DamageSystem.cs` | 3 | damage-pipeline |
| `game/net/ServerNet.cs` | 3 | net-entity-state, sv-antilag |
| `game/client/NetGame.cs` | 2 | warpzones |
| `game/net/NetGame.cs` | 2 | cl-hud, sv-intermission |

**Exit:** builds; tests green; each seam has a live caller proven by a smoke test or by `parity-diff` showing the dependent rows are now reachable.

## Wave 2 — Gameplay leaves (the big parallel push)

One agent per file (164 files), each editing only that file. Given the Wave-1 seams these are mutually independent. Grouped by subsystem; within a group do **H** impact first. `seams` = the Wave-1 items this file consumes; `shards` = which registry shards hold its gaps.

### gametype — 19 files

| file | imp | seams | shards | what to port (own-file work) |
|---|---|---|---|---|
| `src/XonoticGodot.Common/Gameplay/GameTypes/Assault.cs` | H | S11, S2, S4, +MutatorHooks.Pla | assault | Make Assault actually END the match: wire the second-round role swap (StartSecondRound/FirstRoundDestroyTime), defender timelimit win (TimeLimitReached), and campaign single-round end via the round handler; then send attacking/defending/objective-destroyed notifications, objective waypoint sprites … (+1 gaps total — see shards) |
| `src/XonoticGodot.Common/Gameplay/GameTypes/Ctf.cs` | H | S1, S2, S3, S4, S5, S9 | ctf | Wire CTF flag-status mod-icon (server OBJECTIVE_STATUS + client ModIconsMode.Ctf), bind throw/pass/request-pass input, drop-on-disconnect/observe/portal (ctf_RemovePlayer), stalemate detection, flag toss velocity + capture-assist/leaderboard/return team-color notifications, and flag model wave/glow… (+1 gaps total — see shards) |
| `src/XonoticGodot.Common/Gameplay/GameTypes/Domination.cs` | H | S2, S3, S4, S9 | domination | Wire Domination pps mod-icon end-to-end (server set_dom_state -> GametypeStatusBlock Domination Kind + DOM_PPS fields -> NetGame case -> SetDominationPps), play DOM_CLAIM capture sound + INFO_DOMINATION_CAPTURE_TIME, enforce roundbased 5-cap match limit, add control-point waypoint sprites + model, … (+1 gaps total — see shards) |
| `src/XonoticGodot.Common/Gameplay/GameTypes/FreezeTag.cs` | H | S10, S2, S4, +client copy of d | freezetag | Make frozen players damage-immune (frag_damage=0 + g_frozen_force) — chip damage currently re-kills the HP=1 body; add revive_auto_reducible (shoot frozen enemy speeds thaw), the time-to-score revive model (speed_t2s 0.25 / no-clear), spawnshield + soft-kill/nade-revive, ApplyStartItems wiring, FT-… (+1 gaps total — see shards) |
| `src/XonoticGodot.Common/Gameplay/GameTypes/Invasion.cs` | H | S2, S4, +HUD monsters_tot, +monster_attack=f | invasion | Fix Invasion's co-op character: stop monsters attacking players (monster_attack=false / clear bot_attack / Bot_ForbidAttack), cancel PvP damage, disable regen, ROUND 200/200 start items; wire the round handler with Invasion predicates (warmup + 120s round timelimit) instead of dead CA defaults; add… (+1 gaps total — see shards) |
| `src/XonoticGodot.Common/Gameplay/GameTypes/Keepaway.cs` | H | S12, S3, S4, S5, S9, +per-entity Damag | keepaway | Make Keepaway playable: add the host-side procedural ball spawner (SpawnBall has zero callers — no ball exists, so no scoring ever fires), wire use-key/disconnect/observe drop paths, MoveToRandomMapLocation respawn, pickup/drop/touch sounds + notifications, the KA_CARRYING mod-icon + carrier waypoi… (+1 gaps total — see shards) |
| `src/XonoticGodot.Common/Gameplay/GameTypes/KeyHunt.cs` | H | S2, S3, S4, S5, S8 | keyhunt | Emit KeyHunt's 36 notifications + 5 sounds (alarm siren, capture jingle, START/SCAN/MEET/HELP/INTERFERE, PICKUP/DROP/LOST/PUSHED/DESTROYED/CAPTURE), implement kh_LoserTeam push/destroy scoring + lava/trigger destroy + voluntary +use drop, fix maxdist=150 and balance defaults, add carried-key orbit,… (+1 gaps total — see shards) |
| `src/XonoticGodot.Common/Gameplay/GameTypes/LastManStanding.cs` | H | S4, +MatchController., +SetStartItems ga, +SetWeaponArena . | lms | Convert LMS from the wrong round-elimination model to Base's continuous per-lives match (EnableRounds with CA predicates is degenerate in FFA): drive respawn/elimination from lives (RESPAWN_SILENT at 0) not the round gate; add the LMS loadout (200/200 + most weapon-arena + no regen + no item pickup… (+1 gaps total — see shards) |
| `src/XonoticGodot.Common/Gameplay/GameTypes/Nexball.cs` | H | S3, S4, S5, S9, +WEP_NEXBALL/Ball, +ball lifecycle T | nexball | Implement the core Nexball interactions: the BallStealer ball-launcher weapon (primary launch + power meter + secondary tackle/safe-pass) so a basketball carrier can shoot, football kick physics (footballs are currently unmovable), ball lifecycle thinks (delay_start release / idle reset / glide Res… (+1 gaps total — see shards) |
| `src/XonoticGodot.Common/Gameplay/GameTypes/Onslaught.cs` | H | S11, S3, S4, S8, +onslaught_link m | onslaught | Resolve onslaught_link map entities into graph edges (the mode is unplayable without this — every node stays shielded), then emit capture/build/under-attack/destroy sounds+notifications, CaptureShield push entity, proximity-decap, spawn_choose/teleport, generator/control-point CSQC models + damage … (+1 gaps total — see shards) |
| `src/XonoticGodot.Common/Gameplay/GameTypes/Race.cs` | H | S4, +TE_CSQC_RACE-equ, +race_completing , +race_movetime pe | race | Feed the fully-built Race HUD (split timer panel #8, checkpoint splits #27, PB/server-best mod-icon, rankings column are all dead for lack of a net feed) and fire RACE_FINISHED/ABANDONED/NEW_RECORD notifications (LastRecord unconsumed); add the global race_completing/abandon win latch, qualifying-t… (+1 gaps total — see shards) |
| `src/XonoticGodot.Common/Gameplay/GameTypes/Survival.cs` | H | S2, S4, +RoundHandler InP, +Survival entry i | survival | Wire Survival's headline timeout win: EndRoundTimedOut has no caller and the round handler never tests RoundEndTime, so 'survive until time runs out' never fires (a hiding prey leaves the round unending); also add the dual-handler de-desync, teamkill punishment (mirror-kill + frag dock), Bot_Forbid… (+1 gaps total — see shards) |
| `src/XonoticGodot.Common/Gameplay/GameTypes/TeamKeepaway.cs` | H | S12, S3, S4, S5, S9, +per-entity Damag | tka | Make Team Keepaway playable AND a true team mode: add the host-side ball spawner (SpawnBall dead — no ball, no scoring), implement g_tka_score_team (any teammate's kill scores while team holds the ball — the distinguishing TKA rule) + DIFF_TEAM team-kill exclusion + a per-frame Tick for bctime/time… (+1 gaps total — see shards) |
| `src/XonoticGodot.Common/Gameplay/GameTypes/Cts.cs` | M | S4, +race-record/stat, +race_movetime pe | cts | Add CTS combat rules (suppress self/fall damage, restrict to Shotgun via WantWeapon, forbid weapon throw/drop), feed the dead Race timer HUD + fire RACE_NEW_SET/IMPROVED/BROKEN/FINISHED notifications (LastRecord unconsumed) + race_SendStatus medal trigger, force-keyboard movement quantization + det… (+1 gaps total — see shards) |
| `src/XonoticGodot.Common/Gameplay/GameTypes/Duel.cs` | M | +Announcer_Duel c, +BotPopulation du, +join free-slot/p | duel | Enforce the 1v1 cap (Duel.PlayerLimit=2 has no reader — a 3rd client can join AND bot-fill reads g_maxplayers), wire the powerup FilterItem into the MutatorHooks chain (dead — powerups still spawn at default), and call the duel 'A vs B' centerprint title (SetDuelTitle exists, no caller). |
| `src/XonoticGodot.Common/Gameplay/GameTypes/Mayhem.cs` | M | +SetWeaponArena . | mayhem | Wire the dead SetWeaponArena chain so Mayhem players spawn with the full arsenal (most_available) instead of only the Blaster (no .Call site anywhere), and apply the timelimit=15 gametype default on select. |
| `src/XonoticGodot.Common/Gameplay/GameTypes/Tdm.cs` | M | S4 | tdm | Credit per-player SP_SCORE in TDM (every player's scoreboard Score reads 0 today — Tdm.OnDeath only adds the team total, unlike Deathmatch/Ctf/CA), resolve the g_tdm_point_limit -1 'use mapinfo 50' sentinel (stock TDM never ends on frags), seed the TDM timelimit=15 default, restore the GtKillCount … (+1 gaps total — see shards) |
| `src/XonoticGodot.Common/Gameplay/GameTypes/TeamMayhem.cs` | M | +SetWeaponArena . | tmayhem | Wire the dead SetWeaponArena chain so Team Mayhem players spawn with the full arsenal (most_available) instead of only the Blaster, and apply the timelimit=20 gametype default on select (mitigated by the global cvar default already being 20). |
| `src/XonoticGodot.Common/Gameplay/GameTypes/Deathmatch.cs` | L | S4 | deathmatch | Add the remaining-frags announcer (1/2/3 frags left — assets exist, fragsleft never computed), check leadlimit / leadlimit_and_fraglimit in the DM win condition, and present the computed 10-9-8 respawn countdown (Player.RespawnCountdown has no reader). |

### weapon — 15 files

| file | imp | seams | shards | what to port (own-file work) |
|---|---|---|---|---|
| `src/XonoticGodot.Common/Gameplay/Weapons/Electro.cs` | H | S1, S3, +engine-entity-bo, +projectile-shoot | weapon-electro | Networked bolt/orb model+trail+electro_fly loop (invisible in flight); orb shoot-down-into-combo via ProjectileDamage; SND_ELECTRO_IMPACT/_COMBO on all detonations; combo_speed seed 2000 + thruwall LOS; orb bounce values; electrobitch announce |
| `src/XonoticGodot.Common/Gameplay/Weapons/Fireball.cs` | H | S3, S4, S8, +per-weapon-bot-w | weapon-fireball | Primary 5-frame charge-up windup (prefire sound + pre-muzzleflash); fix burn DOT model (frametime-scaled fire_damagepersec + Fire_AddDamage combine math, currently frame-rate-coupled); BFG sweep gates (!cnt, DIFF_TEAM, post-blast owner-survived); per-victim BFG/laser/sphere fx; firemine silent-impa… (+1 gaps total — see shards) |
| `src/XonoticGodot.Common/Gameplay/Weapons/Mortar.cs` | H | S1, S4, S8, +engine-entity-bo, +projectile-shoot | weapon-mortar | Networked grenade model/trail (invisible in flight, primary vs bouncing); grenade shoot-down via ProjectileDamage; airshot achievement; HAGAR_BOUNCE bounce particle; per-entity bounce values; seed RemoteDetonatePrimary/RemoteMinBounceCount from cvars; directHit to RadiusDamage |
| `src/XonoticGodot.Common/Gameplay/Weapons/Tuba.cs` | H | S5, S8, +int-deathtype-hi | weapon-tuba | HITTYPE_SOUND on blast (so tuba never hurts teammates) + instrument/secondary deathtype bits; wire reload->Tuba.Reload instrument cycle (Tuba/Accordion/Klein Bottle) + WrSetup reset; sustained pitched note sound (correct per-pitch .ogg assets, fade, networking); smoke ring; melody recognition; bot … (+1 gaps total — see shards) |
| `src/XonoticGodot.Common/Gameplay/Weapons/Crylink.cs` | M | S3, S8, +int-deathtype-hi | weapon-crylink | Fix primary impact sound (uses crylink_impact2/small for both modes); convergence join-explode think + CRYLINK_JOINEXPLODE at the right trigger; WouldHitFriendly gate on secondary linkexplode; link-explode trigger reorder; HITTYPE_SECONDARY+BOUNCE bits + bounce owner-null; wait-release ammo guard |
| `src/XonoticGodot.Common/Gameplay/Weapons/Devastator.cs` | M | S4, S8, +int-deathtype-hi, +per-weapon-bot-w, +projectile-shoot | weapon-devastator | Rocket shoot-down via ProjectileDamage (with HP subtract not insta-explode); airshot achievement; W_WeaponSpeedFactor on flight accel; ROCKET_GUIDE first-tick effect; rocket-jump remote variant; W_Devastator_Unregister multi-slot lastrocket clear + wr_resetplayer; HITTYPE splash/direct kill-msg; bo… (+1 gaps total — see shards) |
| `src/XonoticGodot.Common/Gameplay/Weapons/Hagar.cs` | M | S3, S4, S5, S8, +projectile-shoot | weapon-hagar | Loaded-secondary hold-to-charge state machine (currently fires full 4-rocket salvo instantly; needs per-tick held-button input) + load/beep/warning sounds; feed dead HUD load ring; HAGAR_BOUNCE particle; rocket shoot-down HP subtract; burst/spray + suicide kill messages |
| `src/XonoticGodot.Common/Gameplay/Weapons/Hlac.cs` | M | +int-deathtype-hi, +per-weapon-bot-w | weapon-hlac | Reset misc_bulletcounter on a fresh primary trigger pull (signature feather-the-trigger spread recovery is broken); g_norecoil gate on punchangle; HITTYPE_SECONDARY bit on secondary bolts; clip-OR ammo term; bot wr_aim; green->blaster fx fallback |
| `src/XonoticGodot.Common/Gameplay/Weapons/Machinegun.cs` | M | S3, S8, +int-deathtype-hi, +per-weapon-bot-w | weapon-machinegun | HITTYPE_SECONDARY on burst+mode0 snipe (so SNIPE obituary fires not SPRAY); bullet ricochet sound; casing ejection; g_norecoil gate on Recoil; bot wr_aim distance switch; fix C# Configure fallback literals (crouchmod/burst_spread/spread_decay) |
| `src/XonoticGodot.Common/Gameplay/Weapons/Minelayer.cs` | M | S1, S3, S4, +per-weapon-bot-w, +projectile-shoot | weapon-minelayer | Assign mine.Team (fix dead proximity team-filter arming on teammates); placed-mine model (invisible); mine shoot-down via ProjectileDamage; WEAPON_MINELAYER_LIMIT notif + UNAVAILABLE sound on over-limit; frozen-owner detonate; remote-detonate sound semantics; bot wr_aim |
| `src/XonoticGodot.Common/Gameplay/Weapons/Rifle.cs` | M | S3, S8, +int-deathtype-hi, +per-weapon-bot-w | weapon-rifle | Wire reload through clip-aware DecreaseAmmo + forced/secondary reload branches + clip CheckAmmo term; secondary fire sound; emit tr_rifle tracer + casing + ricochet; HITTYPE_SECONDARY hail kill-msg; accuracy FIRED credit; wr_resetplayer accumulator reset |
| `src/XonoticGodot.Common/Gameplay/Weapons/Seeker.cs` | M | S3, S4, S8, +projectile-shoot | weapon-seeker | Missile smart world-avoidance (missile_smart trace blend); seekerexp random explosion sound; tag dedupe (W_Seeker_Tagged_Info) + te_knightspike + tag-impact; volley controller ammo/switch self-destruct gates + rate factor; FLAC hagar muzzleflash; missile shoot-down |
| `src/XonoticGodot.Common/Gameplay/Weapons/Vortex.cs` | M | S3, S4, +weapon-charge-st | weapon-vortex | Network vortex_charge to client so the dead crosshair charge ring fills; charge-scale (sqrt) + team-tint the beam; player-model charge glow (wr_glow); yoda/impressive achievements; nexwhoosh fly-by + overcharge volume ramp |
| `src/XonoticGodot.Common/Gameplay/Weapons/Blaster.cs` | L | S1, S5, +per-weapon-bot-w | weapon-blaster | Bot wr_aim lead + bot_dodge of incoming bolts; networked PROJECTILE_BLASTER flying-bolt trail; g_projectiles_interact projectile-vs-projectile knockback; offhand-blaster refire W_WeaponRateFactor scaling |
| `src/XonoticGodot.Common/Gameplay/Weapons/OkMachinegun.cs` | L | S1, S3, S4, S8, +projectile-shoot | overkill-weapons | Muzzle flash / casings / punchangle recoil + client impact particles & ricochet/impact sounds for all 5 OK weapons; okhmg_nadesupport (nade self-damage 10%); okrpc CSQC chainsaw missile render + bot_dodge + damagedbycontents lava-detonate; oknex yoda/impressive announces |

### mutator — 40 files

| file | imp | seams | shards | what to port (own-file work) |
|---|---|---|---|---|
| `src/XonoticGodot.Common/Gameplay/Mutators/BuffsMutator.cs` | H | S3, S4, S5, S6, S8, +FilterItem power | mutator-buffs | Add the item_buff spawnfunc + compat classnames + lifecycle/respawn/relocate (buffs are unobtainable on stock maps) and the FilterItem powerup-replace path; make Jump-buff velocity readable by PlayerPhysics; implement Inferno + Swapper buffs; complete Disability (fire-rate/projectile/monster slows)… (+1 gaps total — see shards) |
| `src/XonoticGodot.Common/Gameplay/Mutators/HookMutator.cs` | H | S5, S6, +expr_evaluate fu, +gameplay-tips bu, +vehicle/weaponUs | mutator-hook | Wire the +hook input bit + writes to Entity.OffhandFirePressed (never assigned) so the offhand grapple becomes castable (currently entirely dead), add expr_evaluate grammar, the vehicle/forbidden suppression, warmup_start_ammo_fuel, the :grappling_hook string, and the gameplay tip |
| `src/XonoticGodot.Common/Gameplay/Mutators/InstagibMutator.cs` | H | S4, S6, +ItemTouch mutato, +MatchEnd/MakePla, +MonsterDropItem/, +entity FilterIte | mutator-instagib | Subscribe PlayerDamage_SplitHealthArmor (so armor-as-lives isn't 70%-absorbed), wire FilterItem item conversion/replacement + the VaporizerCells/ExtraLife item defs/spawnfuncs + ItemTouch full-heal/extra-life, add friendlyfire/contents/friendlypush/yoda branches, complete the countdown announcer la… (+1 gaps total — see shards) |
| `src/XonoticGodot.Common/Gameplay/Mutators/OverkillMutator.cs` | H | S6, +Item_RespawnCoun, +MonsterDropItem , +PlayerPreThink s, +entity-level Fil, +loot drop via St | mutator-overkill | Implement MUTATOR_ONADD item-block + g_overkill_items, the FilterItem health/armor filter + Strength/Shield->HMG/RPC superweapon replacement, fix player loot drop to run the item pipeline (currently non-collectible), add monster loot, wire countdown-blaster (PlayerPreThink) + the dead SetWeaponAren… (+1 gaps total — see shards) |
| `src/XonoticGodot.Common/Gameplay/Mutators/PowerupsMutator.cs` | H | S3, S4, S7, S8, +CustomizeWaypoin, +per-effect m_tic | mutator-powerups | Apply Strength/Shield EF_BLUE/EF_RED glow, fire pickup/powerdown notifications + strength-fire sound + countdown beep, implement drop-on-death (default on) with countdown waypoint, complete invisibility stealth (radar/monster/bot) and Speed attack-rate on all per-weapon refire paths |
| `src/XonoticGodot.Common/Gameplay/Mutators/WeaponArenaRandomMutator.cs` | H | +g_weaponarena st | mutator-weaponarena_random | Implement the base weapon-arena loadout expansion (g_weaponarena all/most/list -> start_weapons) so the random pool is non-degenerate, and add the warmup-vs-normal pool branch in GiveFragsForKill |
| `src/XonoticGodot.Common/Gameplay/Nades/NadesMutator.cs` | H | S1, S3, S4, S5, S8, +FreezeTag unfree | mutator-nades | Wire the dead throw input (offhand +hook / weapon_drop -> OffhandFirePressed) so the entire nade subsystem (prime/charge/toss/11 booms/bonus/pickup) becomes reachable, add the lifecycle-cleanup + MonsterDies-bonus + FreezeTag-revive seams, the melee/Nade_Damage damage branches, napalm damageforcesc… (+1 gaps total — see shards) |
| `src/XonoticGodot.Common/Gameplay/Waypoints/Waypoints.cs` | H | S3, S5, S9, +wire the dead Wa | mutator-waypoints | Wire the entirely-dead WaypointSprites manager API (deploy personal/here/danger + team HELP-ME impulses), add per-gametype CollectWaypoints (Dom/Ons/KH/Assault/Keepaway/Nexball/Race/FreezeTag/itemstime/monster/turret), honor SPRITERULE_DEFAULT/SPECTATOR visibility + the inert client tuning cvars/fa… (+1 gaps total — see shards) |
| `src/XonoticGodot.Common/Gameplay/Weapons/Hook.cs` | H | S5, S8, +beam-rope-csqc-r, +game_starttime/w, +grapple hook Set | mutator-vampirehook, weapon-hook | Draw the hook rope line (ENT_CLIENT_HOOK stream + cylindric line); tarzan=2 elastic rubber-band reel + player-pull (unblocks vampirehook); keep latched hook visible (MoveType.None drops from snapshot); offhand +hook input button; grapple cvars from g_balance_grapplehook_*; HOOK_IMPACT particle; Mak… (+2 gaps total — see shards) |
| `src/XonoticGodot.Common/Gameplay/Weapons/Vaporizer.cs` | H | S1, S3, S4, S8, +beam-rope-csqc-r, +hagar_load/jump_ | mutator-rocketminsta, weapon-vaporizer | Draw the rail beam (CSQC cylindric beam) + muzzle/impact fx + neximpact sound; fix RM cvar fallbacks to Base + WEP_DEVASTATOR/WEP_ELECTRO deathtypes + RM rapid ladder; non-instagib ammo path; yoda/impressive achievements; fire-sound 0.8 volume; Re-tag the RM explosion (devastator) and laser bolts (… (+2 gaps total — see shards) |
| `src/XonoticGodot.Common/Gameplay/Mutators/BugrigsMutator.cs` | M | S6, +ClientConnect mu, +disableclientpre | mutator-bugrigs | Force the 3rd-person chase camera on join (Base stuffcmds chase_active 1; bugrigs is unplayable in 1st person), set disableclientprediction, and add the :bugrigs active-mutator strings; the RaceCarPhysics drive model is otherwise faithful |
| `src/XonoticGodot.Common/Gameplay/Mutators/CampcheckMutator.cs` | M | S6, +CAMP deathtype r, +PlayerDies Kill_, +bot exemption (P, +round-start/pre- | mutator-campcheck | Register the 'camp' deathtype so camp kills get DEATH_SELF_CAMP obituary (currently generic), exempt bots (Base IS_REAL_CLIENT) + typing players, add the round-start/pre-match grace reset, and clear the 'Don't camp!' centerprint on death |
| `src/XonoticGodot.Common/Gameplay/Mutators/CloakedMutator.cs` | M | S6, S7, +SetDefaultAlpha , +per-entity Alpha | mutator-cloaked | Build the alpha infrastructure: a SetDefaultAlpha() world-init driver that fires the dead hook + seeds default_player_alpha (0.25 under cloaked), spawn/death/weapon/vehicle-exit reads of it, the Invisibility-powerup restore composition, plus a per-entity Alpha net channel + transparent player rende… (+1 gaps total — see shards) |
| `src/XonoticGodot.Common/Gameplay/Mutators/DamagetextMutator.cs` | M | +CSQC damagetext , +ClientDisconnect, +colorcoded-strin | mutator-damagetext | Network damage text to remote clients (currently host-only) with sv_damagetext 1/3 visibility tiers, implement the 2D placement heuristics (close-range/out-of-view/spectating/overlap/2D fade+shrink), render color codes via the HUD font, fix world-drift to the view basis, and add the ClientDisconnec… (+1 gaps total — see shards) |
| `src/XonoticGodot.Common/Gameplay/Mutators/DodgingMutator.cs` | M | S5, +MakePlayerObserv, +animdecide JUMP , +frozen dodging +, +input bridge wri | mutator-dodging | Bridge live movement input onto Entity.MovementForward/MovementRight (never written) so the double-tap detector can fire (currently entirely dead, also affects multijump/bugrigs), add frozen dodging + clientselect opt-in + the MakePlayerObserver reset + the jump animation action |
| `src/XonoticGodot.Common/Gameplay/Mutators/DynamicHandicapMutator.cs` | M | S6, +ClientDisconnect, +HANDICAP_DISABLE, +handicap_level n, +post-write Added | mutator-dynamic_handicap | Add the post-write AddedPlayerScore recompute hook + ClientDisconnect/MakePlayerObserver triggers (handicap currently goes stale on cap/objective scores and roster changes), gate off CTS/RACE, and network handicap_level for the scoreboard icon + the :handicap string |
| `src/XonoticGodot.Common/Gameplay/Mutators/KickTeamkillerMutator.cs` | M | S4, +Common->Server b, +per-client start | mutator-kick_teamkiller | Enforce the punishment severity switch (the Server Bans.cs substrate exists but the Common mutator never calls it: kick/ban/play-ban+observe), add the warmup_stage gate + per-client startplaytime denominator, and send the correct severity-dependent notifications (QUIT_PLAYBAN_TEAMKILL on the defaul… (+1 gaps total — see shards) |
| `src/XonoticGodot.Common/Gameplay/Mutators/MeleeOnlyMutator.cs` | M | S6, +!instagib/!ok/!a, +wire dead Forbid, +wire dead SetWea | mutator-melee_only | Wire the dead SetWeaponArena hook (so melee_only+g_weaponarena doesn't wrongly suppress all medium/large/ammo pickups) and ForbidRandomStartWeapons, add the !instagib/!ok/!weaponarena enable exclusion, and the :MeleeOnly mutator strings |
| `src/XonoticGodot.Common/Gameplay/Mutators/MidairMutator.cs` | M | S6, +bot_moveskill fi | mutator-midair | Add the bot bunnyhop-disable on spawn (bot_moveskill=0 via a PlayerSpawn hook; bots currently hug ground and stay shielded), honor zero damagemultiplier/shieldtime cvars + live re-read, fix ShieldTime default 0.5->0.3, and the :midair active-mutator string |
| `src/XonoticGodot.Common/Gameplay/Mutators/MultijumpMutator.cs` | M | S6, +per-client cl_mu | mutator-multijump | Implement the per-client cl_multijump opt-in/out/cap (and g_multijump_client default), fix the maxspeed gate to 3D speed, network multijump_count for prediction, and add the :multijump active-mutator string |
| `src/XonoticGodot.Common/Gameplay/Mutators/NewToysMutator.cs` | M | S3, +SetWeaponreplace, +warmup loadout f | mutator-new_toys | Implement the primary map weapon-entity replacement (SetWeaponreplace hook + per-entity new_toys key + auto-mapping) which is entirely unported, fix RANDOM start-items to OR both bits + cover the warmup loadout, and wire the New-Toys pickup roflsound |
| `src/XonoticGodot.Common/Gameplay/Mutators/OffhandBlasterMutator.cs` | M | S5, S6, +gameplay-tips bu | mutator-offhand_blaster | Add the offhand/+hook input bit + wire writes to Entity.OffhandFirePressed (currently never assigned) so the offhand fire becomes reachable, add W_WeaponRateFactor scaling + vehicle/weaponUseForbidden suppression, the :offhand_blaster mutator string, and the gameplay-tips line |
| `src/XonoticGodot.Common/Gameplay/Mutators/RandomGravityMutator.cs` | M | S2, S6, +cvar_settemp pus, +game_starttime p | mutator-random_gravity | Add the time<game_starttime pre-match gate (using StartItem.GameStartTimeProvider) and the round-handler pre-round gate, model cvar_settemp restore of sv_gravity, and the :RandomGravity active-mutator string |
| `src/XonoticGodot.Common/Gameplay/Mutators/RandomItemsMutator.cs` | M | +ItemTouch mutato, +RandomItems_GetR, +entity-level Fil | mutator-random_items | Implement item-registry enumeration for health/armor/resource/powerup picks, fix the weapon-prob cvar name (g_..._weapon_<name>_probability), wire FilterItem map replacement + ItemTouch re-randomize, fix loot to use StartItem.SpawnLoot (pickup-able), and add the mod-injection hook + recursion-guard… (+1 gaps total — see shards) |
| `src/XonoticGodot.Common/Gameplay/Mutators/RocketFlyingMutator.cs` | M | S6, +Devastator remot | mutator-rocketflying | Port the Devastator remote-jump system (remote_jump_* damage/force/radius/velocity blast) + the AllowRocketJumping force-on sub-hook, and the :RocketFlying active-mutator string |
| `src/XonoticGodot.Common/Gameplay/Mutators/RunningGunsMutator.cs` | M | S7, +SetDefaultAlpha  | mutator-running_guns | Add a SetDefaultAlpha() world-init driver that fires the dead MutatorHooks.SetDefaultAlpha chain and seeds default_player_alpha/default_weapon_alpha, and make player-spawn/corpse/exterior-weapon read those defaults + network/render the alpha (player invisible, gun visible) |
| `src/XonoticGodot.Common/Gameplay/Mutators/TouchExplodeMutator.cs` | M | S6, S8, +TOUCHEXPLODE dea | mutator-touchexplode | Fix the edgedamage zero-cvar guard (cfg 0 vs hardcoded 20), emit EFFECT_EXPLOSION_SMALL, move the impact sound to CH_TRIGGER, and wire DEATH_TOUCHEXPLODE (pass deathTag + register the deathtype) |
| `src/XonoticGodot.Common/Gameplay/Mutators/VampireMutator.cs` | M | S6, +!instagib enable | mutator-vampire | Add the missing spawn-shield guard to the heal-on-damage hook, honor g_vampire_factor 0 and live per-event cvar reads, and add the !instagib enable exclusion |
| `src/XonoticGodot.Common/Gameplay/Mutators/WalljumpMutator.cs` | M | S8, +client-side Muta, +per-model Player | mutator-walljump | Emit EFFECT_SMOKE_RING at the wall contact, route jump voice through PlayerPlayerSound (not the dead player/jump.wav path), add animdecide JUMP + oldvelocity stash, fix zero-cvar override guards, and predict the impulse client-side |
| `src/XonoticGodot.Common/Gameplay/Mutators/BloodlossMutator.cs` | L | S6 | mutator-bloodloss | Add the active-mutator signalling (:bloodloss / , Blood loss + MUT_BLOODLOSS HUD icon) and optionally live-reread the threshold; the rot/crouch/jump-block core is faithful and live |
| `src/XonoticGodot.Common/Gameplay/Mutators/BreakablehookMutator.cs` | L | +shared DiffTeam( | mutator-breakablehook | Fix the FFA self-break bug (Teams.SameTeam lacks the non-teamplay attacker!=owner case, so shooting your own hook in FFA self-punishes), and live-reread g_breakablehook_owner mid-match |
| `src/XonoticGodot.Common/Gameplay/Mutators/DoublejumpMutator.cs` | L | +per-player DOUBL | mutator-doublejump | Add the per-player DOUBLEJUMP stat via Physics_ClientOption (only matters under the default-off g_physics_clientselect); the jump grant/clip core is faithful and live |
| `src/XonoticGodot.Common/Gameplay/Mutators/GlobalForcesMutator.cs` | L | S6 | mutator-globalforces | Include dead players (corpses) in the knockback spread to match Base classname-only IS_PLAYER, and add the :GlobalForces active-mutator string |
| `src/XonoticGodot.Common/Gameplay/Mutators/InvincibleProjectilesMutator.cs` | L | S1, S6, +dispatch per-pro | mutator-invincibleproj | Make the EditProjectile health-zeroing observable by dispatching per-projectile ProjectileDamage shoot-down from the damage pipeline (currently never invoked for non-player targets), and add the :InvincibleProjectiles active-mutator string + client MUT_ detection |
| `src/XonoticGodot.Common/Gameplay/Mutators/PhysicalItemsMutator.cs` | L | +Item_Spawn mutat, +ODE-equivalent r | mutator-physical_items | Implement the physics-ghost item entity (MOVETYPE_PHYSICS/SOLID_CORPSE + hide real item) and the think/touch/damage reset+hazard-snap-home callbacks; requires both an Item_Spawn hook and a rigid-body integrator the port lacks |
| `src/XonoticGodot.Common/Gameplay/Mutators/PinataMutator.cs` | L | S6, +Prandom.Vec() un | mutator-pinata | Fix the throw spread distribution (Prandom.Vec cube vs Base unit-ball) and add the :Pinata active-mutator string; use ExprEvaluate for g_pinata to match the sibling mutators |
| `src/XonoticGodot.Common/Gameplay/Mutators/SandboxMutator.cs` | L | S3, S8, +generic drag/gra, +per-map text obj, +server-side sand | mutator-sandbox | Build the entire server-side sandbox backend the Tools dialog drives: g_sandbox mutator + object spawn/edit/scale/attach/remove/duplicate/claim/info commands, per-object think/touch material FX, drag/grab carry, readonly/Sandbox_* gating hooks, and per-map auto-save/load storage |
| `src/XonoticGodot.Common/Gameplay/Mutators/SpawnUniqueMutator.cs` | L | +expr_evaluate fu | mutator-spawn_unique | Replace the simplified bool ExprEvaluate with Base's full expr_evaluate grammar (empty-string -> true, +/- prefixes, cvar comparison tokens); the spawn-demote core is faithful and live |
| `src/XonoticGodot.Common/Gameplay/Mutators/StaleMoveNegationMutator.cs` | L | S6 | mutator-stale_move_negation | Honor explicit-zero tunable cvars (bonus_asymptote/cooldown_factor) and contribute the :StaleMoveNegation active-mutator string |
| `src/XonoticGodot.Common/Gameplay/Mutators/SuperSpecMutator.cs` | L | S6, +MakePlayerObserv, +mutator ItemTouc, +mutator SV_Parse, +per-client crypt | mutator-superspec | Build the entire Super Spectate mutator: g_superspectate registration, per-client option bitfields + persistence, auto-spectate on pickup/killer (ItemTouch/PlayerDies), spectator console commands, and the missing-UID notification |

### item — 5 files

| file | imp | seams | shards | what to port (own-file work) |
|---|---|---|---|---|
| `src/XonoticGodot.Common/Gameplay/Items/ItemPickupRules.cs` | H | S3, S4, +item-respawn-way, +itemtouch-mutato | items-pickups | Port the cl_autoswitch / CTS-autoswitch weapon re-selection block inline in Item_GiveTo (capture _switchweapon, force-switch to w_getbestweapon after a better-weapon give); send CENTER_ITEM_FUELREGEN_GOT / _JETPACK_GOT center-prints on first grant; Add the ItemTouch/ItemTouched mutator hook chain (… (+4 gaps total — see shards) |
| `game/client/ClientWorld.cs` | M | - | items-pickups | Client item-FX pass: add the distance alpha-fade (g_items_maxdist 4500 / cl_items_fadedist 500) so far items fade not pop; add simple-item sprite swap (sv_simple_items/cl_simple_items) and stay-weapon (cl_weapon_stay_alpha/color) + vehicle-hud tints |
| `src/XonoticGodot.Common/Gameplay/Items/StartItem.cs` | M | - | items-pickups | Wire the team-item random selection branch (Item_FindTeam) so teamed map items spawn one-at-a-time; apply q3compat item origin offset (z += -15 - mins.z) and item_armor1->ArmorSmall on Quake3-format maps |
| `src/XonoticGodot.Common/Gameplay/Mutators/ItemstimeMutator.cs` | M | S9, +CSQC itemstime n, +STAT(ITEMSTIME) , +per-player send , +spectator/warmup | items-pickups, mutator-itemstime | Network item respawn times to remote clients (currently host-only), register STAT(ITEMSTIME), fix the inverted panel enable-gate (spectator/warmup/mode-2 vs always-on), add per-player send tiers + sync hooks, and the spectator respawn waypoint sprites; Model hidebig (hud_panel_itemstime_hidebig) so… (+2 gaps total — see shards) |
| `src/XonoticGodot.Common/Gameplay/Items/Resources.cs` | L | - | resources | Add the TakeResource mutator hook (MUTATOR_CALLHOOK(TakeResource)) and port TakeResourceWithLimit (SVQC clamp-to -limit form); latent — no shipped mutator subscribes; Fire the GiveResourceWithLimit mutator hook (port delegates to GiveResource, skipping its own WithLimit chain); latent; Let the SetR… (+3 gaps total — see shards) |

### scoring — 2 files

| file | imp | seams | shards | what to port (own-file work) |
|---|---|---|---|---|
| `src/XonoticGodot.Common/Gameplay/GameTypes/ClanArena.cs` | H | S2, S4, +SetStartItems ga, +SetWeaponArena . | clanarena, scoring | Restore CA's defining traits: 200/200 + full-ammo start loadout (wire SetStartItems / ApplyStartItems) and the 'most' weapon arena (dead SetWeaponArena chain), wire the live round handler to CA's CheckWinner/PreventStalemate predicates + 10s grace + weapon-fire lockout + ROUNDS_PL award, fire round… (+2 gaps total — see shards) |
| `src/XonoticGodot.Common/Gameplay/Scoring/GameScores.cs` | M | - | scoring | Make the central game_stopped score clamp live: assign GameScores.GameStopped from the server (GameWorld) so AddToPlayer/AddToTeam uniformly drop post-match/warmup-end deltas (currently the static is never set; only per-call-site MatchEnded guards exist); Wire g_score_resetonjoin (currently a no-op… (+2 gaps total — see shards) |

### damage — 2 files

| file | imp | seams | shards | what to port (own-file work) |
|---|---|---|---|---|
| `src/XonoticGodot.Common/Gameplay/StatusEffects.cs` | H | S3, S4, +per-effect m_tic, +status-effect ma | damage-pipeline, mutator-status_effects | Add per-effect m_tick dispatch (burning frame-rate-correct dps + DEATH_FIRE deathtype + water/frozen self-extinguish; EF_FLAME/SHOCK/ADDITIVE flags; PERSISTENT recompute via SetPersistent; frozen ice block + grapple-detach) and the removeall/clearall lifecycle hooks + removal/countdown sounds; Fire… (+2 gaps total — see shards) |
| `src/XonoticGodot.Common/Gameplay/Weapons/WeaponSplash.cs` | H | - | damage-pipeline | RadiusDamage never OR's HITTYPE_SPLASH onto indirect victims' deathtype (DeathTypes.AddHitType(Splash) call missing) so splash-vs-direct kill classification is lost; also MIN_DAMAGEEXTRARADIUS per-target dist shrink not applied |

### server — 24 files

| file | imp | seams | shards | what to port (own-file work) |
|---|---|---|---|---|
| `game/hud/MapVotePanel.cs` | H | S5, +mapvote-net-enti | sv-intermission, sv-mapvoting | Feed the dead MapVotePanel (SetVote/SetVotes/Active have no callers) from a mapvote decode path + wire client input to cast.; Feed the dead MapVotePanel from the mapvote decode (Set* have zero callers), add MV_FADETIME easing, fix hud_panel_mapvote_highlight_border default (2->1).; Add the client v… (+3 gaps total — see shards) |
| `src/XonoticGodot.Common/Gameplay/Weapons/Arc.cs` | H | S3, S8, +beam-rope-csqc-r, +projectile-shoot, +setupshot-antila | sv-antilag, weapon-arc | Real cylindric bezier beam render (currently a wrong spark-particle trail; arc_beam mis-classified isTrail) + muzzle/hit lights + per-beam_type thickness; fix beam CheckAmmo (cells>0 not >=6, auto-switches away); Arc_Smoke/overheat fx + loop sound; heat-% HUD stat; burst-beam latch; bolt shoot-down… (+2 gaps total — see shards) |
| `src/XonoticGodot.Server/ClientManager.cs` | H | S2, S4, S5, +forced-team-dete, +put-observer-tra, +server-welcome-c | sv-client-lifecycle | Build the server->client welcome/cvar-push channel: FixClientCvars, MOTD, clientversion request, _teams_available, ClientInit_misc constants (entirely missing; replicated-cvar table is a physics-only stand-in).; Expand joinAllowed gates: version-mismatch, forced-spectator, playban, g_maxping, g_max… (+8 gaps total — see shards) |
| `src/XonoticGodot.Server/Commands.cs` | H | S2, S4, S5, +cheat-impulse-ro, +playerusekey-hoo, +put-observer-tra | sv-cheats, sv-commands-votes, sv-impulse, sv-ipban | Enforce playban on the live join path: CmdJoin/ClientManager join must reject a play-banned client with CENTER_JOIN_PLAYBAN (currently deferred — a banned player just re-joins).; Make CmdPlayBan run the real observer transition (not just the FragsStatus sentinel), add connect-time re-apply + miniga… (+15 gaps total — see shards) |
| `src/XonoticGodot.Server/MapVoting.cs` | H | S5, +mapvote-net-enti | sv-intermission, sv-mapvoting | Make the map vote castable: it runs server-side but no net opcode/impulse path lets players vote (g_maplist_votable=6 makes it the DEFAULT end path, so every vote closes with zero real votes).; Add the 0.5s think throttle + 1s winner-reveal-before-changelevel delay to the map-vote loop.; Seed playe… (+11 gaps total — see shards) |
| `game/net/ClientNet.cs` | M | - | net-entity-state, sv-intermission | Add missing REPLICATE client cvars (cl_autoscreenshot, cl_clippedspectating, cl_autoswitch_cts, cl_handicap*, g_xonoticversion) to the replicated set.; Add SVC_INTERMISSION client view freeze: lock to an intermission camera, hide viewmodel (EF_NODRAW), set -2342 health sentinel (MatchIntermission a… (+2 gaps total — see shards) |
| `src/XonoticGodot.Common/Gameplay/MapObjects/TargetUtilities.cs` | M | +campaign-host-se, +mapobject-center, +mapobject-host-s, +mapobject-key-bi, +mapobject-sounds, +target-spawn-tem | mapobject-func, mapobject-target, sv-campaign | door_secret `sounds`-key medieval/metal soundpack selection + .message centerprint; Wire dead target_changelevel: fix CHANGELEVEL_MULTIPLAYER bit (1→2, one-line bug), bind chmap/gametype keys, assign the host seams so end-of-level actually ends/switches the match; Assign CampaignLevelWarpHandler/Is… (+7 gaps total — see shards) |
| `src/XonoticGodot.Common/Gameplay/Mutators/SpawnNearTeammateMutator.cs` | M | +FixAngle/FixAngl, +death_origin fie, +expr_evaluate fu, +per-player REPLI | mutator-spawn_near_teammate, sv-spawnpoints | Route spawn-facing through FixAngle (currently writes only Angles, overwritten), implement expr_evaluate so the overkill-ruleset string gates correctly, add the sky/lava/water/hurt-trigger/nade relocate rejects, use death_origin for closetodeath, and add the checkpvs/weaponLocked/chat gates; Add th… (+2 gaps total — see shards) |
| `src/XonoticGodot.Common/Gameplay/Weapons/Shotgun.cs` | M | S3, S8, +int-deathtype-hi, +per-weapon-bot-w, +setupshot-antila | sv-antilag, weapon-shotgun | Spread style 4 (gauss-2D) for pellet scatter; melee_delay wind-up think loop + no_doubleslap; HITTYPE_SECONDARY slap kill-msg; ricochet sound + woosh + casing; out-of-ammo auto-melee + alt triple-shot timing; g_ballistics_mindistance 2; bot wr_aim; Lag-compensate the Shotgun melee secondary swing t… (+2 gaps total — see shards) |
| `src/XonoticGodot.Common/Gameplay/Weapons/WeaponImpulses.cs` | M | - | sv-impulse | Make priority impulses 200-229 cycle the per-group cl_weaponpriority0..9 lists (replicated but unread); PriorityHandle ignores the group number and only reads the main list.; Add the weaponLocked (frozen-player) gate to ReloadHandle so a frozen player can't reload (predicate exists nearby but uncal… (+2 gaps total — see shards) |
| `src/XonoticGodot.Server/Intermission.cs` | M | S5, +server-welcome-c | sv-intermission | Add the +10s input-grace window before the vote/exit and the per-player 'press fire to skip' early-exit.; Wire the dead Intermission.RequestExit to player fire/jump/atck2/hook/use input so players can skip the scoreboard (and an empty server can advance).; Add server-forced end-of-match autoscreens… (+4 gaps total — see shards) |
| `src/XonoticGodot.Server/OverTimeManager.cs` | M | +overtimes-stat-n | sv-world-rules | Network STAT(OVERTIMES) so the persistent HUD OVERTIME / overtime-count banner shows (decision logic already faithful). |
| `src/XonoticGodot.Server/PlayerFrameLogic.cs` | M | S3, S4, +playerregen-hook | resources, sv-client-lifecycle | Add the 10 missing PlayerRegen hook tuning slots (max_mod/regen_mod/rot_mod/limit_mod inert) and gate fuel regen on ITEM_FuelRegen ownership + skip the whole fuel block under IT_UNLIMITED_AMMO (currently runs unconditionally); Gate fuel REGEN on the IT_FUEL_REGEN item flag + skip the whole fuel blo… (+4 gaps total — see shards) |
| `src/XonoticGodot.Server/Teamplay.cs` | M | S4, +forced-team-dete, +team-change-hook, +team-nag-net (ne, +trueskill-rating | sv-teamplay | Add forced teams: .team_forced field, connect-time g_forced_team_red/blue/yellow/pink/otherwise parsing + force-spectate-unlisted (entirely unported).; Fire Player_ChangeTeam (can block) + Player_ChangedTeam mutator hooks around team changes (port sets Entity.Team directly with no dispatch).; Compu… (+11 gaps total — see shards) |
| `src/XonoticGodot.Server/TimeoutController.cs` | M | S4 | sv-commands-votes | Make timeout pause slowmo=0.0001 (inching) not a hard freeze + add ANNCE_TIMEOUT/PREPARE + per-second CENTER_TIMEOUT countdown. |
| `src/XonoticGodot.Server/VoteController.cs` | M | - | sv-commands-votes | Add the timeout_status guard (only 'timein' callable during a timeout) + sv_vote_gamestart pre-match gate to vote-call.; Fix vote parse: register timelimit_min/max (5/60) bound, run ValidateMap/MapInfo_FixName/recent-map block, per-command arg restriction, movetoX/allready-warmup parse, QC kick/kic… (+4 gaps total — see shards) |
| `src/XonoticGodot.Server/WarmupController.cs` | M | S4 | sv-teamplay, sv-world-rules | Fix g_warmup_limit default (-1 -> 180 per xonotic-server.cfg) and add g_warmup>1 min-N-players + map_minplayers warmup modes.; Fix ready-up majority to count humans only (rint, min factor 0.5), add minplayers/badteams/timeout gates + COUNTDOWN_STOP notifications (warmup may never end on a bot-fille… (+5 gaps total — see shards) |
| `game/client/SpawnPointParticles.cs` | L | - | sv-spawnpoints | Team-color the idle spawn-point glow (Team_ColorRGB) + read cl_spawn_point_dist_max instead of hardcoded 2000qu cull. |
| `src/XonoticGodot.Common/Gameplay/Inventory.cs` | L | - | sv-impulse | Make next/prev_bygroup (18/19) cycle weaponorder_byimpulse not the priority-list fallback. |
| `src/XonoticGodot.Server/Bans.cs` | L | - | sv-ipban | Add the one-shot .ban_checked flag + first-command ban re-check (Base Ban_MaybeEnforceBanOnce) on top of the connect-time enforce.; Add a shutdown Ban_SaveBans to refresh persisted seconds-remaining at session end (currently saved only on Insert/Delete). |
| `src/XonoticGodot.Server/Bot/BotPopulation.cs` | L | +forced-team-dete | bot-ai, sv-teamplay | Add autoskill/skill_auto auto-tune-to-best-human (the per-bot 12-column model is an intended single-knob divergence).; Parse the skin/shirt/pants + team-forcing columns of bots.txt (currently name+model only).; Honor bot_forced_team (5th arg of addbot) so a config can pin a bot to a team (currently… (+3 gaps total — see shards) |
| `src/XonoticGodot.Server/Campaign.cs` | L | S2, +server-welcome-c | sv-campaign | Add Campaign_Invalid: revalidate resolved map/gametype match the loaded level, else CampaignBailout.; Wire campaign_bots_may_start into the round-handler / monster move+think / campcheck / LMS start gates (currently only bot movement honors it).; Network the campaign level number (Campaign_GetLevel… (+3 gaps total — see shards) |
| `src/XonoticGodot.Server/Cheats.cs` | L | S3, S8, +cheat-frame-hook, +cheat-impulse-ro, +copybody-clone ( | sv-cheats | Add the cheat-refusal bprintf logging (Base logs the refused cheat attempt) + the sv_clones clone-allowance branch.; Add the CLONE_MOVING/STANDING cheat impulses (CopyBody corpse clones) + read sv_clones (no CopyBody primitive in the port).; Add SPEEDRUN_INIT/SPEEDRUN personal-waypoint snapshot+res… (+9 gaps total — see shards) |
| `src/XonoticGodot.Server/Cvars.cs` | L | +forced-team-dete, +telefrag-cvars | sv-campaign, teleporters | Register g_telefrags / g_telefrags_avoid so server overrides take effect (currently inert hardcoded fallbacks); Read g_campaign_forceteam to force the player onto a given team in a campaign team-mode level (registered but never read). |

### bot — 6 files

| file | imp | seams | shards | what to port (own-file work) |
|---|---|---|---|---|
| `src/XonoticGodot.Server/Bot/BotBrain.cs` | H | +bot-cmd-queue (b, +bot-wr_aim-drive | bot-ai | Add sticky-enemy tracking window, see-through-transparent + misc_breakablemodel target passes, and SUPERBOT health/distance target rating.; Port the QC far/mid/close ordered weapon-priority lists + bot_ai_weapon_combo + bot_rangepreference instead of the coarse hitscan/splash highest-impulse bucket… (+6 gaps total — see shards) |
| `src/XonoticGodot.Server/Bot/BotRoles.cs` | H | - | bot-ai | Rate goals by waypoint-cost (Dijkstra path length) not straight-line distance, restore per-call rangebias + skill-scaled time-item lead + moving-target interval. |
| `src/XonoticGodot.Server/Bot/BotAim.cs` | M | - | bot-ai | Replace closed-form ballistic-drop approximation with the QC 10-trace tracetoss search + per-weaponentity shot speed (W_WeaponSpeedFactor) for lobbed-weapon aim.; Add the bot_aggresskill modifier to the long-range fire-hesitation chance (port uses skill only, loses per-bot aggression variance). |
| `src/XonoticGodot.Server/Bot/BotNavigation.cs` | M | - | bot-ai, bot-waypoints | Add jetpack navigation (take off / fly / brake-land) + skill>6 trigger_hurt escape (jetpack up / Devastator rocketjump).; Derive step/jump heights from sv_stepheight/sv_jumpvelocity/sv_gravity (not hardcoded 34/48) + fuller teleport/ladder/water waypoint traversal.; Add keyboard-movement emulation … (+6 gaps total — see shards) |
| `src/XonoticGodot.Server/Bot/Waypoint.cs` | M | +waypoint-danger- | bot-waypoints | Fix link travel-cost: skill-dependent bunnyhop speedup, the unused JUMP-source fall-cost variant, and the half-path water/crouch averaging.; Add per-waypoint danger bias (.dmg) to the A* route cost + expanding multi-seed nearest search so routes detour around lava/rocket zones.; Add PVS cull before… (+9 gaps total — see shards) |
| `src/XonoticGodot.Server/Bot/BotDanger.cs` | L | - | bot-ai | Replace brake-only danger response with the QC lateral evadedanger sidestep vector + per-waypoint danger-cost accumulation. |

### net — 1 files

| file | imp | seams | shards | what to port (own-file work) |
|---|---|---|---|---|
| `src/XonoticGodot.Engine/Simulation/CsqcModelAppearance.cs` | L | - | net-entity-state | Check friend-color collision against the full team list (network team list) not just the local team. |

### physics — 4 files

| file | imp | seams | shards | what to port (own-file work) |
|---|---|---|---|---|
| `src/XonoticGodot.Engine/Simulation/MoveTypePhysics.cs` | M | +entity-bouncefac, +matchticrate-sub | physics-movetypes | Consume per-entity bouncefactor/bouncestop (currently dropped → wrong restitution+settle for grenades/casings/balls/spider/vehicle projectiles); use signed Dot for bounce-down-slopes; Route MOVETYPE_STEP through the full Walk integrator (slide+stair) like Base's step->walk alias, not freefall-only … (+5 gaps total — see shards) |
| `src/XonoticGodot.Common/Physics/PMAccelerate.cs` | L | +physics-preset-g | physics-player | Verify the Warsow-bunny air-accel path under a real non-default physics preset (code ported & faithful but unproven — DEAD at stock) |
| `src/XonoticGodot.Common/Physics/PlayerPhysics.cs` | L | - | physics-player | Gate the jump grunt on autocvar_g_jump_grunt (stock 0 = silent; port always plays player/jump.wav) + drive ANIMACTION_JUMP; Port the viewloc (1D-rail) water sub-cases (wishvel.z=-160 + separate plain-addspeed accel path); confirm swim liveness on a real water map; Compute func_water waterlevel-from… (+7 gaps total — see shards) |
| `src/XonoticGodot.Engine/Simulation/FlyMove.cs` | L | - | physics-movetypes | Add the MOVETYPE_PHYSICS rider fast-path (translate-only, skip PushEntity/blocked) to PushMove |

### mapobject — 24 files

| file | imp | seams | shards | what to port (own-file work) |
|---|---|---|---|---|
| `src/XonoticGodot.Common/Gameplay/MapObjects/MapObjectsRegistry.cs` | H | - | mapobject-misc | Port item_key* pickup keys (spawnfunc, touch, MF_ROTATE model, DropToFloor, grant itemkeys bits) — without keys, key-gated doors/keylocks are unopenable |
| `src/XonoticGodot.Common/Gameplay/MapObjects/Breakable.cs` | M | +mapobject-event- | mapobject-func | Honor NOSPLASH + team friendly-fire guard on breakable; fix DebrisTimeJitter default; carry killing-hit force; (waypoint/bot-attack/colormod out of scope) |
| `src/XonoticGodot.Common/Gameplay/MapObjects/Buttons.cs` | M | +mapobject-event- | mapobject-func | Fix INVERTED DONTACCUMULATEDMG (fire iff single hit >= health, no accumulation), honor NOSPLASH, and health==-1 (fire on any/damageless attack) |
| `src/XonoticGodot.Common/Gameplay/MapObjects/Doors.cs` | M | +mapobject-center, +mapobject-sounds | mapobject-func | Door NOSPLASH spawnflag (splash-immune shootable door), `sounds`-key sound-pack selection, and .message centerprint; Port DOOR_ROTATING_BIDIR / BIDIR_IN_DOWN trigger-side reverse-open (door_use trigger_reverse path: lip=666 + pos2 flip) so bidirectional rotating doors open toward the trigger; Show … (+3 gaps total — see shards) |
| `src/XonoticGodot.Common/Gameplay/MapObjects/DynamicLight.cs` | M | +realtime-dynamic | mapobject-misc | Path-following dynlight should walk the path_corner chain (install a train_next-style think) instead of parking on the first corner; Add a realtime dynamic-light render consumer so authored/moving/pulsing map lights actually emit visible light |
| `src/XonoticGodot.Common/Gameplay/MapObjects/LogicGates.cs` | M | +chat-say-pipelin, +gamestart-time-w | mapobject-triggers | Wire GameStartTime from the host countdown so trigger_gamestart wait>0 fires at the real start, not t=0; Wire trigger_magicear into a chat/say pipeline (string-match core is faithful but DEAD — no caller) |
| `src/XonoticGodot.Common/Gameplay/MapObjects/MapObjectsCommon.cs` | M | +set-platmovetype | mapobject-misc, mapobject-movers-platforms | Fix default easing: PlatMoveStart/End default must be 0/0 (smoothstep -2t^3+3t^2) not 1/1 (forced linear) — every long door/plat/train move is currently uneased; Port set_platmovetype (parse the `platmovetype` spawn key into PlatMoveStart/End + cubic_speedfunc_is_sane reverse-curve reject) — same s… (+2 gaps total — see shards) |
| `src/XonoticGodot.Common/Gameplay/MapObjects/MovingBrushes.cs` | M | +set-platmovetype | mapobject-func, mapobject-misc | Port TRAIN_NEEDACTIVATION intermediate-corner re-arm (train_wait re-installs train_use + parks the think to wait for re-triggering); func_rotating_init_for_player late-joiner ambient resend + start/stop ambient on RotatingSetActive toggle; PathCornerSetup must call set_platmovetype so a corner's pe… (+3 gaps total — see shards) |
| `src/XonoticGodot.Common/Gameplay/MapObjects/TargetMusic.cs` | M | +csqc-target-musi | mapobject-target, mapobject-triggers | Refine music fade curve to QC's per-source frametime/fade_time(in)/fade_rate(out) easing instead of the A/B crossfade approximation; target_music is listen-server-only + GLOBAL (not per-activator); port the net path + per-round reset/kill/restore plumbing; trigger_music is listen-server-only; fix f… (+3 gaps total — see shards) |
| `src/XonoticGodot.Common/Gameplay/MapObjects/TargetSpeaker.cs` | M | +per-client-sound | mapobject-target | target_speaker ACTIVATOR plays to ALL not just the triggering real client; *-voice sample randomization unimplemented; channel mapped to Body not CH_TRIGGER_SINGLE |
| `src/XonoticGodot.Common/Gameplay/MapObjects/Teleporters.cs` | M | S2, +ef-teleport-bit-, +remove-grappling, +telefrag-cvars, +telefrag-cvars ( | teleporters | Toggle EF_TELEPORT_BIT (player-model dissolve/flash) on teleport + set player.istypefrag for the kill-credit window; Add the missing telefrag suppression (!g_race && !g_cts && !round-pre-start) so telefrags don't fire in Race/CTS or during the round countdown; model TELEPORT_FLAG_FORCE_TDEATH; Regi… (+9 gaps total — see shards) |
| `src/XonoticGodot.Common/Gameplay/MapObjects/Triggers.cs` | M | +damagedbytrigger, +mapobject-center, +mapobject-event-, +swamp-slowdown-c | mapobject-triggers | Add the MISSING team-gate (this.team/INVERT_TEAMS) + pressedkeys gate to MultiTouch; per-client CTS/Race .wait buffers; NOSPLASH + team-attacker gate on the shootable path; Gate non-creature trigger_hurt on toucher.damagedbytriggers (port damages every non-creature toucher every touch); Honor HEAL_… (+6 gaps total — see shards) |
| `src/XonoticGodot.Common/Gameplay/MapObjects/ViewLocation.cs` | M | +viewloc-2.5d-cam | mapobject-triggers | Add the client 2.5D side-scroller camera that consumes player.ViewLoc (server stamps it each frame but it goes nowhere visible) |
| `src/XonoticGodot.Common/Gameplay/MapObjects/Warpzone.cs` | M | S1, S4, +portal-entity-ed, +warpzone-subview, +warpzone-telepor | portals, warpzones | Fix warpzone crossing gate to plane-side test (with view_ofs) instead of velocity-direction; add same-frame re-teleport guard (finishtime) + WarpZone_Projectile_Touch impact filter + frame-back re-trace; Fire SUB_UseTargets on BOTH this and partner zone with the QC skip-bitmasks, not just the trigg… (+10 gaps total — see shards) |
| `src/XonoticGodot.Common/Gameplay/Weapons/Porto.cs` | M | S3, +beam-rope-csqc-r, +surface-flags-on | portals, weapon-porto | Fix Strength lookup name (buff_strength->strength) + force 3; trajectory-preview polyline; complete touch decision tree (slick/noimpact/creature/not-owner/wireframe gates) + per-branch sounds; death/respawn cleanup (W_Porto_Remove); porto_forbidden setter/ticker; lifetime fail cleanup; Porto OnTouc… (+2 gaps total — see shards) |
| `src/XonoticGodot.Engine/Simulation/TriggerTouch.cs` | M | +warpzone-crossin | warpzones | Client-predict warpzone crossings so they are seamless instead of arriving as a rubber-band correction |
| `game/client/LaserRenderer.cs` | L | - | mapobject-misc | Animate laser beam sliding texcoord (time*3) + emit end-point particle bursts every frame instead of throttled ~10Hz |
| `src/XonoticGodot.Common/Gameplay/MapObjects/Follow.cs` | L | - | mapobject-misc | Bake attach_sameorigin tag-relative origin/angle (gettaginfo/gettagindex + AnglesTransform basis change) so a tag-attached entity sits at the exact tag pose |
| `src/XonoticGodot.Common/Gameplay/MapObjects/Jumppads.cs` | L | +csqcmodel-anim-a, +mapobject-center | mapobject-triggers | Jumppad: centerprint(message), ANIMACTION_JUMP pose, jumppadcount/jumppadsused kill-credit bookkeeping |
| `src/XonoticGodot.Common/Gameplay/MapObjects/MapModels.cs` | L | +bgmscript-adsr (, +colormap-render  | mapobject-misc | Port bgmscript ADSR envelope so func_clientwall/func_pointparticles animate to the map music (no stock map depends — low priority); Consume Entity.ColorMapOverride to actually team-tint a colormapped prop on screen |
| `src/XonoticGodot.Common/Gameplay/MapObjects/MapObjectFieldsExtra.cs` | L | +clientwall-fade- | mapobject-misc | Apply func_clientwall distance-fade alpha + PVS cull (stored but unrendered) and the antiwall g_clientmodel_use solid/visible toggle relay |
| `src/XonoticGodot.Common/Gameplay/MapObjects/MapVolumes.cs` | L | - | mapobject-func | Use exact-trigger plane overlap (not AABB) for conveyor/ladder volume tagging; func_ladder bot waypoint tracetest |
| `src/XonoticGodot.Common/Gameplay/MapObjects/Platforms.cs` | L | +mapobject-sounds, +q3compat-flag, +q3compat-flag (a | mapobject-func, mapobject-movers-platforms | Read the `sounds` map key (1=plat1/2, 2=medplat) + legacy sound1/sound2 for func_plat sound-pack selection; Apply the message2 ('was squished by') default in PlatSetup + read the `sounds`/sound1/sound2 keys for plat sound-pack selection; Port Q3/Q3DF func_plat compat defaults (speed 200/lip 8/dmg 2… (+5 gaps total — see shards) |
| `src/XonoticGodot.Common/Gameplay/Warpzone/WarpzoneRadiusQuery.cs` | L | +warpzone-trail-p | warpzones | Align portal splash falloff to transformed blast-to-zone distance + per-victim nearest-point-on-AABB + is_pure blacklist; Port WarpZone_TraceToss (ballistic toss prediction through portals, for bot grenade aim) + WarpZone_TrailParticles |

### monster — 6 files

| file | imp | seams | shards | what to port (own-file work) |
|---|---|---|---|---|
| `src/XonoticGodot.Common/Gameplay/Monsters/Mage.cs` | H | S8, +world-bounds-ran | monster-mage | Fix the shield damage-reduction bug (port cuts ~80% on top of the faithful ~0.8% armor block — ~100x too tanky; remove the double-count), fix self-heal eligibility (all skins, toward max 400 not skin-0/100), port the 40% teleport random-relocation branch, fix spike inline-default constants, and rem… (+1 gaps total — see shards) |
| `src/XonoticGodot.Common/Gameplay/Monsters/MonsterAI.cs` | H | S10, S7, +entity-reset-swe, +monster-healthba, +monster-sound-sy | monster-framework | Port the monster sound system (.sounds table, idle/melee/spawn cues, msound_delay/g_monsters_sounds), the WP_Monster healthbar sprite, monster_setupcolors skill/team colormap+glowmod, miniboss EF_RED red glow, the dead round-restart Entity.Reset sweep, and enemy-retention LOS/frozen/alpha drops + c… (+1 gaps total — see shards) |
| `src/XonoticGodot.Common/Gameplay/Monsters/Spider.cs` | H | S1, S8, +monster-anim-fra, +player-status-ef | monster-spider | Port the Webbed player move-speed slow (PlayerPhysics hook, signature CC), fix the web projectile to render as electro orb (PROJECTILE_ELECTRO catalog entry), emit EFFECT_ELECTRO_IMPACT on web explode, and the spider mr_pain/mr_death/mr_anim frame groups. |
| `src/XonoticGodot.Common/Gameplay/Monsters/Golem.cs` | M | S8, +monster-anim-fra, +monster-real-ite | monster-golem | Remove fabricated electro_fire2 lightning cue, emit EFFECT_EXPLOSION_MEDIUM (smash) + EFFECT_ELECTRO_IMPACT (lightning explode), fix per-swing melee animtime 0.5→0.8, golem mr_pain window 0.34→0.5, and real-item loot drop. |
| `src/XonoticGodot.Common/Gameplay/Monsters/Wyvern.cs` | M | S8, +monster-anim-fra | monster-wyvern | Override mr_death to fling the corpse (400r-200 / 100r+100 scatter) so a killed wyvern tumbles, fix the pain window 0.34→0.5, emit EFFECT_FIREBALL_EXPLODE on detonation, and add the firemine in-flight visual + mr_deadthink die2 swap. |
| `src/XonoticGodot.Common/Gameplay/Monsters/Zombie.cs` | M | S10, +monster-anim-fra, +monster-sound-sy | monster-zombie | Stop playing zombie pain + block(melee) sounds Base leaves silent (route via .sounds table), fix block armor-restore fallback 0.6→0.5, and drive the 14 mr_anim frame groups (currently Frame pinned 0). |

### turret — 11 files

| file | imp | seams | shards | what to port (own-file work) |
|---|---|---|---|---|
| `src/XonoticGodot.Common/Gameplay/Turrets/TurretAI.cs` | H | +turret-cvar-tabl, +turret-networkin, +turret-vehicle-e, +waypoint-path-en | turret-framework | Wire the dead TurretAI.Damage onto the live damage path (GtEventDamage), fix targetscan_maxdelay 0.6→1 + add g_turrets_nofire gate, remove the unintended death-blast (Base commented out), add the cvar-backed per-unit table, the death two-step + turret_heal + HEADSHAKE, and the whole turret networki… (+1 gaps total — see shards) |
| `src/XonoticGodot.Common/Gameplay/Turrets/WalkerTurret.cs` | H | +turret-cvar-tabl, +turret-networkin, +turret-vehicle-e, +waypoint-path-en | turret-walker | Fix the framerate-scaled body-yaw turn (port turns ~6x slower than Base per-frame), port idle roam/wander + last-seen pursuit + waypoint path-following + the missing 6 gaits (swim/jump/land/pain/strafe/turn), fix track_type STEPMOTOR vs FluidInertia, lead shot-speed 34920→18000, and the rocket laun… (+1 gaps total — see shards) |
| `src/XonoticGodot.Common/Gameplay/Turrets/EWheelTurret.cs` | M | +turret-cvar-tabl, +turret-networkin, +waypoint-path-en | turret-ewheel | Fix all inflated drive speeds (700/500/320/100 → 500/150/50/25) + body turnrate 90→200 + add aim-error speed gating, fix target_range_optimal 1500→900, head clamps (maxrot 360→20, maxpitch 20→45) + track_type STEPMOTOR, respawntime 60→30, shot_spread 0.0125→0.025, and waypoint path following. |
| `src/XonoticGodot.Common/Gameplay/Turrets/FlacTurret.cs` | M | +turret-cvar-tabl | turret-flac | Fix aim_maxpitch 90→35 (port acquires missiles outside the real flak cone), pass per-unit biases (rangebias 0.25/anglebias 0.5) + track accel/blend, remove the unintended death blast, fix respawntime 60→90, shot_spread 0.0125→0.02, target_range_optimal 2000→1250, the fuse-time derivation, and drop … (+1 gaps total — see shards) |
| `src/XonoticGodot.Common/Gameplay/Turrets/HellionTurret.cs` | M | +turret-cvar-tabl, +turret-fire-time, +turret-vehicle-e | turret-hellion | Fix the missile blast radius 500→80 (and the knock-on proximity 100→16 / stray 2500→400 thresholds), pass the per-unit biases (0.7/0.01/0.01/0) + range_optimal 3000→4500 + track accel/blend, fix guidance accel/decel constants, respawntime 60→90, launch spread 0.08, the AFF firecheck, and remove the… (+1 gaps total — see shards) |
| `src/XonoticGodot.Common/Gameplay/Turrets/HkTurret.cs` | M | +turret-cvar-tabl, +turret-target-br | turret-hk | Pass the per-unit biases (0.5/0.01/0.1 — samebias 100x too sticky at default 1.0), fix shot_speed_turnrate 0.1→0.25 + accel/decel constants (1.05/1.1/0.85 → 1.025/1.05/0.9), fix fuel-out to go inert (dud) not detonate, add enemy-lost re-seek scan, target_range_optimal 3000→5000, aim_maxpitch 30→20,… (+1 gaps total — see shards) |
| `src/XonoticGodot.Common/Gameplay/Turrets/MachinegunTurret.cs` | M | S8, +turret-fire-time, +turret-vehicle-e | turret-machinegun | Remove the unconditional uzi_fire.wav cue (Base turret path is silent), remove the death RadiusDamage blast (Base commented out), drop the extra SelectAngleLimits/zPredict flags + add checkpvs, and add the fire-time LOS trace (port currently fires through walls). |
| `src/XonoticGodot.Common/Gameplay/Turrets/MlrsTurret.cs` | M | +turret-cvar-tabl, +turret-vehicle-e | turret-mlrs | Implement VOLLYALWAYS burst-completion (port aborts the 6-rocket burst the instant the target is lost), pass the per-unit target-select biases (all defaulting to 1.0), fix target_range_optimal 1500→500, aim_maxpitch 30→20, shot_spread 0.0125→0.05, the fuse-floor 500→125 radius, and HEADSHAKE + ammo… (+1 gaps total — see shards) |
| `src/XonoticGodot.Common/Gameplay/Turrets/PlasmaDualTurret.cs` | M | +turret-cvar-tabl, +turret-vehicle-e | turret-plasma_dual | Implement TFL_DMG_HEADSHAKE (head not jolted on hit), remove the unintended death blast, pass per-unit biases (0.2/0.4/0.4) + override target_range_optimal (500→1000) + track accel/blendrate, fix shot_spread 0.0125→0.015, and the head-frame wheel anim. |
| `src/XonoticGodot.Common/Gameplay/Turrets/PlasmaTurret.cs` | M | +turret-cvar-tabl, +turret-vehicle-e | turret-plasma | Fix instagib values (port 800dmg/0force → Base 1e10dmg/800force), pass the per-unit select biases (0.5/0.01/0.25) + track accel/blendrate to MakeParams, fix shot_spread 0.0125→0.015, drop the extra zPredict, and remove the unintended death blast. |
| `src/XonoticGodot.Common/Gameplay/Turrets/TeslaTurret.cs` | M | +turret-cvar-tabl, +turret-networkin | turret-tesla | Fix respawntime 60→120, remove the discharge electro_fire.wav cue (Base turret is silent), set chain damage force to zero (Base toast force '0 0 0', port shoves at 400), add the realowner-skip branch, and the head spin-up + idle crackle arc. |

### vehicle — 5 files

| file | imp | seams | shards | what to port (own-file work) |
|---|---|---|---|---|
| `src/XonoticGodot.Common/Gameplay/Vehicles/Bumblebee.cs` | H | +turret-vehicle-e, +vehicle-boarding, +vehicle-bumble-r, +vehicle-hud-csqc | vehicle-bumblebee | The entire bumblebee (pilot, both gunners, raygun, death, respawn) is faithful but DEAD — wire the +use→UseKey boarding dispatch and the DamageVehicle damage routing, then add the networked BRG_* heal/damage beam, the multi-seat HUD/crosshairs, gib toss + blowup FX, dead-touch, and the +0 0 25 spaw… (+1 gaps total — see shards) |
| `src/XonoticGodot.Common/Gameplay/Vehicles/Spiderbot.cs` | H | S1, +vehicle-controll, +vehicle-flag-car | vehicle-spiderbot | Fix the per-frame controller timing magnitude error (head/body slew + gravity ~6x slower than Base), reattach a carried CTF flag to the head on enter, add the landing sound, average both hardpoint tags for head aim, and the death gib entities / burn FX. |
| `src/XonoticGodot.Common/Gameplay/Vehicles/VehicleCommon.cs` | H | S7, +turret-vehicle-e, +vehicle-hud-csqc, +vehicle-touch-di | vehicle-framework | Wire vehicles_damage/vehicles_heal onto the live damage path (install event_damage/event_heal — currently a shot vehicle takes player ARMOR/HEALTH-split damage and never ejects/dies via damage), port vehicles_touch crush + vehicles_impact fall-damage + vehicles_painframe smoke/jitter + vehicles_set… (+1 gaps total — see shards) |
| `src/XonoticGodot.Common/Gameplay/Vehicles/Racer.cs` | M | S1, +vehicle-flag-car, +vehicle-touch-di | vehicle-racer | Port vr_impact ram/collision fall-damage + bouncefactor/bouncestop, the water air-meter (racer_air_finished) + post-water downforce ramp (racer_watertime), fix the homing-rocket obstacle-avoid climb step + timeout-detonation deathtag (VhWakiRocket), the flag-carrier reposition, and the engine move/… (+1 gaps total — see shards) |
| `src/XonoticGodot.Common/Gameplay/Vehicles/Raptor.cs` | M | S1, +vehicle-touch-di | vehicle-raptor | Port vr_impact crash-pain/shake + bouncefactor/bouncestop/mass + liquid-sink mask, fix bomblet touch detonation to stagger (random*0.4s) + the bomblet_alt clear-fall early-burst, clear buttons during takeoff, and the rotor/anim/CSQC-projectile FX + engine fly sound. |

**Exit:** builds + tests green; `python tools/parity-assemble.py` then `Workflow{name:"parity-diff", args:{scope:"all", mode:"update"}}` shows the targeted server-side rows flipped to faithful/live. Remaining `presentation_split` rows are expected (Wave 3).

## Wave 3 — Presentation (renders Wave-2 state)

Client/HUD/FX/audio files (13) that consume the server state produced in Wave 2. Per-gametype client rendering (e.g. mod-icon cases, overlays) is added by the single agent that owns each shared client file. Parallelize per file.

| file | imp | seams | shards | client-side work |
|---|---|---|---|---|
| `game/hud/CenterPrintPanel.cs` | H | S4, +engine-centerpri, +server-private-c | cl-centerprint | Route the dead raw engine centerprint() family (door/trigger/target .message, locked-door, item hints, target_print) to the panel; wire SetTitle/SetDuelTitle drivers; carry durcnt per-step duration + correct ROUNDSTART ^COUNT arg; fix fade_in default 0->0.15; implement MSG_CENTER_KILL + Kill/ClearA… (+1 gaps total — see shards) |
| `src/XonoticGodot.Common/Framework/Entity.cs` | H | S10, S7, S8, +csqcmodel-net-ex | cl-csqcmodel | Network the CSQCMODEL extra-properties contract (colormod for ALL entities, scale, alpha, modelflags, traileffect, v_angle, anim_state/upper/lower_action+time) and port animdecide (8-direction + duck locomotion + upper-body actions); wire MF_* trails + jetpack loop; force-model (cl_forceplayermodel… (+1 gaps total — see shards) |
| `src/XonoticGodot.Common/Gameplay/Sounds/GameSound.cs` | H | S3 | fx-sounds | Fix the broken player/voice body-sound path: parse the .sounds manifest (key->file, e.g. pain50 -> sound/player/espeak/player/pain50) instead of treating sound/player/default.sounds as a directory — currently ALL pain/death/drown/taunt/team-radio are silent; fix hitsound sample misc/hitconfirm->mis… (+1 gaps total — see shards) |
| `game/client/FirstPersonView.cs` | M | +punchvector-net-, +spectator-camera | cl-view | Network + apply view_punchvector (origin recoil kick) — ServerNet/ClientNet/NetGame; wire the dead classic chase_active third-person cam (set CameraMode, read chase_back/up/front/overhead/pitchangle) + spectator third-person (View_SpectatorCamera, CAMERA_SPECTATOR); render HUD_Damage as gfx/blood t… (+1 gaps total — see shards) |
| `game/client/ShellCasings.cs` | M | S10, S8, +wframe-anim-net- | cl-viewmodel | Emit dead shell casings (ShellCasings sim complete but EffectSystem.SpawnCasing has zero callers + no server emit) incl. bounce sounds; apply view-model team colormap + per-weapon glowmod + EF_NODEPTHTEST + per-child skin; add muzzle-flash MODEL (Devastator/Machinegun spinning md3); network wframe … (+1 gaps total — see shards) |
| `game/hud/CrosshairPanel.cs` | M | +client-stat-read, +weapon-stat-clie | cl-crosshair | Feed the five dead weapon-stat rings (Vortex charge/pool, reload clip, Arc heat, Hagar load, MineCount) + pickup pulse from networked stats; make hit-indication + nade/capture/revive objective rings work on a pure remote client (read networked stats client-side, not host-only); fix teammate-shrink … (+1 gaps total — see shards) |
| `game/hud/ScoreboardPanel.cs` | M | +scoreboard-aux-n, +scoreboard-inter | cl-scoreboard | Set ScoreboardPanel.Active so the cross-fade runs (not Visible-only pop); feed dead SetSpectators/RespawnRemaining/SetRankings; network ping/pl/fps/skill columns + correct ping bands (20/80/200); add name-cell icons (color/ready/handicap/wants-join); add item-stats block + interactive UI (nav/team-… (+1 gaps total — see shards) |
| `src/XonoticGodot.Common/Gameplay/Notifications/DeathMessages.cs` | M | - | fx-deathtypes | Add the missing SelectSpecial branches: NADE family (nade/nade_ice/heal/darkness/napalm -> DEATH_*_NADE, LIVE under g_nades), KILL->DEATH_SELF_SUICIDE, MIRRORDAMAGE->DEATH_SELF_BETRAYAL (both LIVE), TEAMCHANGE/AUTOTEAMCHANGE self lines + death_team f1 arg (LIVE), HURTTRIGGER msg_from_ent + DEATH_CU… (+1 gaps total — see shards) |
| `src/XonoticGodot.Common/Gameplay/Notifications/Notification.cs` | M | S4, +notification-dur | fx-notifications | Carry per-notification centerprint durcnt duration on the wire (currently fixed 3s; item_centerprinttime=1.5 ignored); wire GentleMode setter (cl_gentle/sv_gentle) so gentle text is selected; add spectator-follow recipients in NotificationReaches (IS_SPEC/to.enemy clauses); implement Kill_Notificat… (+1 gaps total — see shards) |
| `src/XonoticGodot.Server/AnnouncerController.cs` | M | S2, S4 | cl-announcer | Wire dead centerprint title driver (gametype-name title + duel 'A vs B' title) and broadcast COUNTDOWN_ROUNDSTOP; read cl_announcer/_antispam cvars + AnnouncerOption mutator hook. |
| `game/client/EffectInfoParticle.cs` | L | - | fx-effectinfo | Fix stain baseline defaults: stainsize {1,1}->{2,2} (22 of 30 staintex blocks rely on the baseline -> half-size splats) and staincolor default+semantics (0xFFFFFF absolute -> 0xFFFFFFFF modding factor); implement `stainless` keyword as stain-disable (staintex[0]=-2) not a no-op. |
| `game/client/ShowNamesLayer.cs` | L | S7, +entcs-private-sl | cl-shownames | Apply entcs_GetAlpha model-alpha factor so tags fade with cloaked/fading models; truncate name to box width (textShortenToWidth); add self/spectatee own-tag + 1s spectatee-switch grace; gate sameteam + health/armor on the server private slice rather than client-side colormap. |
| `game/hud/RadarPanel.cs` | L | S11, +radar-ping-net-b, +radarlink-net-en | cl-teamradar | Add maximized fullscreen tactical radar + clickable layer (ONS spawn-select/teleport, input, hover-glow); ping pulse rings (preserve RadarIcon bit-7 in ServerNet, port teamradar_times pulse loop); Onslaught radar links (ENT_CLIENT_RADARLINK); fix luma-vs-code cvar defaults (4096/0.8) + honor hud_pa… (+1 gaps total — see shards) |

**Exit:** the `NEEDS-INGAME-CHECK.md` queue is walked in-game; presentation rows move off `unknown`/`missing`; `parity-diff` shows no remaining `presentation:missing` in scope.

## Wave 4 — Integration & verification

Sequential gate proving the waves actually closed gaps (not just compiled):

1. **Build + full test suite** green.
2. **Behavioral pass** — walk [NEEDS-INGAME-CHECK.md](NEEDS-INGAME-CHECK.md): launch the game per scenario and confirm presentation/liveness rows code-reading couldn't (flag wave/rotation, frozen overlay, mod-icons, ball physics, tracers/casings, announcer cues).
3. **Re-baseline** — `python tools/parity-assemble.py`, then `Workflow{name:"parity-diff", args:{scope:"all", mode:"update"}}` to rewrite the registry to the new truth and emit `DRIFT-<date>.md` documenting every fixed row.
4. **No regressions** — a second `parity-diff` (`mode:"diff"`) must report **0 regressions**.

## How to execute a wave

Dispatch a wave as a fan-out workflow — one agent per row, pointed at the file it owns and the shard(s) named in its row (`planning/parity/registry/<shard>.yaml` + `specs/<shard>.md`) for the exact gaps, Base symbols, and constants. The shard is the work order: implement until its gaps close and its status dimensions read faithful/live. After the wave, run the exit-criteria checks before the next. Re-run `python tools/parity-plan-gen.py` after a `parity-diff --update` to regenerate this plan against the shrunken gap set.
