# Parity drift — 2026-06-27

**0 regressions · 14 fixes · 2 new gaps · 1 unmapped Base feature · 6 reclassified** (3 units re-audited: net-entity-state, sv-world-rules, sv-client-lifecycle)

## Regressions

No regressions detected.

## New gaps

| unit | feature | dimension | was -> now | reason |
| --- | --- | --- | --- | --- |
| sv-client-lifecycle | frame.idle_kick | values | faithful -> partial | Port cvar defaults diverge from Base: `sv_maxidle_playertospectator` ships 0 (Base 60, server/client.qh:39) so move-to-spec never fires out of the box; `sv_maxidle_minplayers` ships 0 (Base 2, qh:38). Both read live (GameWorld.cs:1853-1856; Cvars.cs:349-350). Prior audit recorded base:'0'/port:'0'/match:true for both — incorrect. Logic/timing/liveness stay faithful/live; only numeric defaults wrong. |
| sv-client-lifecycle | frame.spectator_kick | values | faithful -> partial | `g_maxplayers_spectator_blocktime` ships 5 in the port (Cvars.cs:355) but Base ships 10 (xonotic-server.cfg:33), halving the `sv_spectate=0` kick grace. Read live at GameWorld.cs:1856. Prior audit recorded base:'5 s grace'/match:true — Base value is 10. |

## Registry coverage gaps

Base features with no registry row (newly discovered during this audit):

- **sv-world-rules.boot.default_alpha** (server/world.qc:SetDefaultAlpha) — default player/weapon alpha resolution (`g_player_alpha` with 0->1 fallback, weapon=player alpha, SetDefaultAlpha mutator hook for Cloaked/RunningGuns). Fully ported via `MutatorHooks.FireSetDefaultAlpha`, fired live at GameWorld.Boot cs:647. Added as a new faithful+live row.

## Fixes

| unit | feature | dimension | was -> now | reason |
| --- | --- | --- | --- | --- |
| net-entity-state | wepent.switch_alpha_misc | logic | missing -> partial | Wave-14a networked the wepent switch block (EntityField.Wepent BIT19): ServerNet.cs:1970-1983 produces m_switchweapon/m_switchingweapon + a derived WS_RAISE/WS_DROP phase from the live slot-0 weapon state machine; ClientEntityView.cs:148-153 decodes; ViewEntityRenderer renders the remote weapon raise/lower. The remote weapon-swap state machine is now ported (residual missing fields: gunalign/skin/porto/tuba). |
| net-entity-state | wepent.switch_alpha_misc | values | missing -> partial | m_switchweapon/m_switchingweapon now carried as registry-id shorts (match), exterior-weapon m_alpha carried as a byte (x255+sentinel vs Base x254 — intended divergence). gunalign/skin fields exist but unproduced (default 0); porto_v_angle_held/tuba_instrument still absent. |
| net-entity-state | wepent.switch_alpha_misc | timing | missing -> partial | Remote weapon raise/lower dip is now animated (ViewEntityRenderer SwitchRaiseTime/SwitchDropTime tween driven by the networked WepPhase), an approximation of Base's m_switchingweapon-driven viewmodel_draw timing. |
| net-entity-state | wepent.switch_alpha_misc | presentation | missing -> partial | ViewEntityRenderer (game/net) now renders the remote third-person weapon switch raise/lower dip + per-instance gun transparency on the live Player snapshot path (ClientEntityView.cs:213 + Process() at 110). |
| net-entity-state | wepent.switch_alpha_misc | liveness | na -> partial | Producer (ServerNet 1970-1983), codec, decode (ClientEntityView 148-153) and render (ViewEntityRenderer) are all wired on the live Player snapshot path. Partial (not full) because gunalign/skin have no producer and porto/tuba are not in the struct. |
| sv-world-rules | entity.randomseed | logic | missing -> faithful | RandomSeed now ported: server producer RandomSeed.cs (Tick every frame GameWorld.cs:1771), think/reroll/network/decode all present matching world.qc:601-614. |
| sv-world-rules | entity.randomseed | values | missing -> partial | 5s reroll period + 65536 seed range match Base, but the seed VALUE is a process System.Random not Base's engine random() stream (not bit-exact; intended divergence since no client effect consumes it). |
| sv-world-rules | entity.randomseed | timing | missing -> faithful | 5s nextthink re-roll cadence reproduced (RandomSeed.RerollPeriod=5) and ticked once per server frame. |
| sv-world-rules | entity.randomseed | presentation | missing -> partial | Network seam decodes the seed into a per-instance client PrandomContext (ClientNet.cs:617), but no client EFFECT reads RandomRng yet, so it is presentationally inert. |
| sv-world-rules | entity.randomseed | liveness | na -> live | Server tick (GameWorld.cs:1771) + network append (ServerNet.cs:987) + client decode (ClientNet.cs:602) are all on the live match path. |
| sv-world-rules | misc.redirection | logic | missing -> partial | quit_and_redirect now read at end-of-match map flow (GameWorld.cs:3975-3982) and reconnects clients via RedirectHandler -> MenuCommand.Connect; the 'self' restart variant, sv_public -2, retry loop and defer timer remain unported. |
| sv-world-rules | misc.redirection | values | missing -> partial | quit_and_redirect cvar default '' matches Base; quit_and_redirect_timer defer delay not ported (reconnect is immediate). |
| sv-world-rules | misc.redirection | timing | missing -> partial | Redirect fires once at match end rather than Base's per-second two-pass RedirectionThink retry loop with redirection_timeout/nextthink. |
| sv-world-rules | misc.redirection | liveness | na -> live | RedirectHandler wired live on both threaded and non-threaded host branches (NetGame.cs:777/810), invoked from DriveEndOfMatchMapFlow. |

## Reclassified

| unit | feature | dimension | was -> now | reason |
| --- | --- | --- | --- | --- |
| net-entity-state | csqcmodel.anim_state | liveness | partial -> partial | The networked anim-action WIRE now exists (EntityField.AnimAction BIT18, UpperAction/AnimActionTime fields, codec, ClientEntityView.cs:142-143 decode) but is UNFED — ServerNet has no animdecide producer (explicitly deferred to LI1) and no LI3 torso-overlay consumer. Behaviorally unchanged: the live path is still client-side LocomotionBlend.Split reconstruction. Port_refs/constants/notes updated to record the reserved-but-dead-producer wire; status dimensions unchanged. |
| net-entity-state | csqcmodel.change_mask | values | partial -> partial | Notes corrected: the stale claim that anim-action/wepent fields are NOT in NetEntityState is wrong — Alpha (BIT17), AnimAction (BIT18) and Wepent (BIT19) are now in the struct + codec; mask now uses bits 0..19 (still 32-bit). Status dimensions unchanged (codec still only-changed-fields faithful, NetPrecision.Low coords). |
| sv-world-rules | boot.initialize_entity_queue | logic | unknown -> partial | Port implements deferred post-spawn ordering via MapObjectsRegistry.RunPostSpawn (per-family init queues drained in INITPRIO order: links then findtarget) rather than Base's generic order-sorted linked list; covers families that need it, not a unified queue (intended_divergence). |
| sv-world-rules | boot.initialize_entity_queue | timing | unknown -> faithful | RunPostSpawn drained once at Boot after the whole BSP entity lump spawns, matching InitializeEntitiesRun's first-frame drain semantics. |
| sv-world-rules | boot.initialize_entity_queue | liveness | unknown -> live | MapObjectsRegistry.RunPostSpawn is called live at GameWorld.Boot (cs:739). |
| sv-client-lifecycle | observer.put_observer_in_server | values | faithful -> faithful | Corrected the placeholder g_maxplayers_spectator_blocktime constant (was base:'used for SPECTATE_WARNING') to the concrete Base value 10 vs port 5 mismatch; the live enforcement (and the value divergence) is owned by frame.spectator_kick, so the observer row's dimension statuses are unchanged. |
