# Parity drift — 2026-06-27

**0 regressions · 0 new gaps · 1 unmapped Base feature · 10 fixes · 1 reclassified** (2 units re-audited: cl-csqcmodel, net-entity-state)

## Regressions

No regressions detected.

## New gaps

No new gaps detected.

## Registry coverage gaps

Base features with no registry row (newly identified during this audit):

- **cl-csqcmodel — `common/wepent.qh` exterior-weapon networked block** (`CL_ExteriorWeaponentity_Think`: `w_ent.weaponname` / switch state / skin / align / alpha). Now networked (`EntityField.Wepent`) and consumed by `ViewEntityRenderer` for the remote third-person held weapon, but it has NO dedicated registry row — it is currently folded into `cl-csqcmodel.tagindex.weapon_attach`. Consider a standalone `cl-csqcmodel.wepent.exterior_weapon` row, since it is a distinct Base feature area with its own producer (`ServerNet` slot-0 weapon state machine) and its own consumer (`ViewEntityRenderer`).

## Fixes

| unit | feature | dimension | was -> now | reason |
| --- | --- | --- | --- | --- |
| cl-csqcmodel | animdecide.state_machine | logic | partial -> faithful | Upper-body ACTION state machine (shoot/melee/pain/draw/taunt/die) now fully ported via AnimDecide.GetUpperAnim (animdecide.qc:109-153 priority cascade DEAD>ACTIVE>IDLE) + SetAction (animdecide.qc:338-356 latch/restart). The 8-direction locomotion was already faithful from the prior drift fix. |
| cl-csqcmodel | animdecide.state_machine | values | partial -> faithful | Per-anim framerates (die 0.5 / shoot 5 / draw 3 / taunt 0.33 / pain 2) are now USED via AnimDecide.SpecFor in PlayerModel.BuildClipTable.ActionClip — baseline recorded them as unused (clips picked @ 20fps default). |
| cl-csqcmodel | animdecide.state_machine | timing | partial -> faithful | Action-window expiry resolved server-side (ServerNet.GetUpperAnim) and the play phase clamps at the last frame exactly when the server window elapses; producer/consumer agree on durations. Only residual is the raw-float AnimActionTime jitter (now intended_divergence). |
| cl-csqcmodel | animdecide.state_machine | presentation | partial -> faithful | PlayerModel.Pose:224-234 now plays the torso ACTION overlay clip via LocomotionBlend.SelectTorsoAction when e.UpperAction != 0; baseline recorded a static idle/aim torso (torsoTime=0). |
| cl-csqcmodel | animdecide.state_machine | liveness | partial -> live | Full producer chain live: WeaponFireGate (shoot/melee), WeaponFireDriver (draw), DamageSystem (pain1/pain2 + die1/die2), SoundSystem (taunt) latch the action; networked via EntityField.AnimAction (1<<18); client decodes + renders. Baseline grep claimed zero anim_* bits in NetEntity/ClientEntityView — now present. |
| cl-csqcmodel | skeleton.upper_lower_aim | presentation | partial -> faithful | The bone-split now carries the live networked upper-body ACTION overlay (PlayerModel.Pose:224-234), closing the baseline's 'static torso, torsoTime=0' presentation gap. fixbone / aim-pitch / split math were already faithful. |
| cl-csqcmodel | networking.csqcmodel_contract | liveness | partial -> partial | Two more contract properties now wired end-to-end since baseline: the upper-body anim ACTION block (UpperAction/AnimActionTime via EntityField.AnimAction 1<<18) and the exterior-weapon (wepent) block (SwitchWeapon/SwitchingWeapon/WepPhase/ViewmodelSkin/GunAlign/WepAlpha). Liveness stays partial overall (spine still mostly unmapped) but materially improved; constants reclassified anim_upper_action/time and wepent from match:false to match:true. |
| cl-csqcmodel | tagindex.weapon_attach | timing | na -> faithful | The exterior-weapon (wepent) block now networks the remote held-weapon switch raise/lower phase (WepPhase WS_RAISE=1 / WS_DROP=2) and ViewEntityRenderer drives the raise/lower dip tween, giving the held-weapon transition real timing (was na in baseline). |
| cl-csqcmodel | tagindex.weapon_attach | liveness | partial -> partial | Exterior-weapon (wepent) block (switch/phase/skin/align/alpha) is now networked from the slot-0 weapon state machine and consumed by ViewEntityRenderer for the remote third-person held weapon — a partial fix to the attach feature (full QC tag_networkentity graph still simplified). |
| net-entity-state | csqcmodel.anim_state | liveness | partial -> live | The upper-body anim ACTION channel is now fully wired on the live path. Server producers latch the action at real gameplay sites (WeaponFireGate.cs:163-173 SHOOT/MELEE, DamageSystem.cs:514-519 PAIN + 733-736 DIE, WeaponFireDriver.cs:303-305 DRAW, SoundSystem.cs:557-559 TAUNT); ServerNet.cs:1918-1924/1977-1982 networks the expiry-resolved id; ClientEntityView.cs:142-143 decodes it; PlayerModel.cs:225-234 renders the torso overlay via LocomotionBlend.SelectTorsoAction, driven by the live ServerTimeProvider (NetGame.cs:1224). Baseline recorded this as RESERVED/UNFED with no producer and no LI3 consumer. |
| net-entity-state | csqcmodel.anim_state | logic | partial -> faithful | The animdecide getupperanim decision flow (action latch + expiry-window resolution via AnimDecideUnit.GetUpperAnim with each action's numframes/framerate) is now reproduced server-side and the client plays the resolved action as a torso overlay over the velocity legs — the actual QC upper-body action state machine, not an inferred approximation. |
| net-entity-state | csqcmodel.anim_state | timing | missing -> faithful | The action play window is now timed by the resolved animdecide spec (start + numframes/framerate, e.g. SHOOT=0.2s) and the client derives the play phase as serverNow - AnimActionTime. Previously no timing was networked at all (the wire was unfed). |

## Reclassified

| unit | feature | dimension | was -> now | reason |
| --- | --- | --- | --- | --- |
| cl-csqcmodel | fallbackframe.remap | presentation | faithful -> faithful | Behavior unchanged but the substitution discipline (melee->shoot, duckwalk-variant->duckwalk) is now also baked into PlayerModel.BuildClipTable's Pick keyword chains for the skeletal action/locomotion clips, subsuming the runtime numeric-frame remap for skeletal players. Added BuildClipTable to port_refs; note updated. |
| net-entity-state | csqcmodel.anim_state | presentation | partial -> partial | Dimension label unchanged (still partial) but the underlying reality improved: the torso now plays the real server-decided SHOOT/PAIN/DRAW/TAUNT/DIE action clip instead of LocomotionBlend's static single-frame aim pose. Kept partial because anim_state byte (BIT7) + lower-body anim_lower_action (BIT8) are still un-networked (legs reconstructed client-side). |
