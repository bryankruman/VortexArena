# Parity drift — Wave-12 re-verify sweep (2026-06-27)

**1 regression (FIXED) · ~6 new gaps · ~15 registry-coverage gaps · ~218 fixes · ~30 reclassified** across the full
154-unit registry, run as 9 throttle-sized `parity-diff` batches (mode=update) against branch
`claude/parity-port-waves` HEAD after the Wave-12 implementation (commits 8563014/6da9e80/effd64e/0b3c2a8).
Net result: parity improved everywhere; the single regression was found, fixed, and committed during the sweep.

This file consolidates per-batch results (each batch overwrote the shared dated report; the durable record is the
per-unit registry yaml). Gap total fell 615 → **544** (1363 at Wave-1 start). Schema: 0 warnings.

## Regressions

| unit | feature | dimension | was → now | resolution |
| --- | --- | --- | --- | --- |
| fx-deathtypes | message.teamchange | logic/presentation/liveness | faithful/live → partial | **FIXED — commit d3139d3.** Wave-12b routed the bot auto-balance kill through the same `KillPlayerForTeamChange` that emits `DEATH_TEAMCHANGE` for a manual switch, so auto-balanced players rendered the manual self-line AND lost a frag. Fix: `KillPlayerForTeamChange(p, bool autoBalance)` → manual emits `TeamChange`, auto-balance emits `AutoTeamChange`; the suicide −1 is skipped for `AUTOTEAMCHANGE` in the generic team-scoring path (Tdm.OnDeath + Scores.Obituary), mirroring `damage.qc:304`. Build clean, 2812/2812 tests pass. Re-recorded faithful/live. |

## New gaps (newly-discovered defects — Wave-13 candidates, all recorded as registry rows)

| unit | feature | dimension | reason |
| --- | --- | --- | --- |
| overkill-weapons | shared.bot_ai | logic missing | OK weapons lack `wr_aim` bot fire-button overrides → bots fall back to generic always-primary/hitscan-straight (wrong for okshotgun ranged poke, okmg/okhmg range gate, okrpc projectile lead). |
| overkill-weapons | mode.weapons_activation | logic missing | `g_overkill_weapons` standalone `ok_weapons` activation path (un-blocks the 5 OK weapons without full `g_overkill`) not ported. |
| overkill-weapons | oknex.zoom | logic missing | oknex `wr_zoom`/`wr_zoomdir` sniper scope not ported as an OK-Nex override (dormant at stock secondary==2). |
| mutator-waypoints | weapon.whereis | values/timing partial | `whereis` marker uses a 2.5s fade vs Base `-2` one-frame-then-respawn cadence; `wp_extra` set but not networked (generic icon). |
| physics-movetypes | toss.ballistic | logic (row stays faithful) | `_suspendedInAir` set for any non-null GroundEntity vs Base's SOLID_BSP-only gate; diverges only for a freed non-BSP resting mover. Low impact. |

## Registry coverage gaps (Base features that gained first-class rows this sweep)

Menu/CSQC-presentation documentation features, mostly: weapon-guide `describe()` prose (shotgun/vortex/monsters),
oknex zoom, `g_overkill_weapons`, `crosshair_hittest_showimpact` debug path, `HUD_Mod_NexBall` client modicon,
monster `describe()` bestiary prose (mage/zombie). All low/no gameplay impact; tracked for completeness.

## Fixes (parity that improved vs the prior baseline) — per batch

| batch | scope | regressions | fixes | new gaps | coverage gaps | reclassified |
| --- | --- | --- | --- | --- | --- | --- |
| 1–3 | gametypes, monsters, turrets, vehicles, items, fx, early mutators | 1 (fixed) | ~38 | 1 | 6 | 1 |
| 4 | mutators campcheck…overkill | 0 | 31 | 0 | 1 | 2 |
| 5 | mutators physical_items…waypoints, resources, net-entity-state, overkill-weapons | 0 | 21 | 5 | 3 | 0 |
| 6 | physics, scoring, weapons arc…porto | 0 | 40 | 1 | 3 | 6 |
| 7 | weapons shotgun…vortex, bot, client CSQC | 0 | 35 | 0 | 5 | 10 |
| 8 | client view/viewmodel, fx-effectinfo, mapobjects, sv-antilag…clientkill | 0 | 24 | 0 | 1 | 4 |
| 9 | sv-commands…warpzones (incl. sv-teamplay) | 0 | 29 | 0 | 2 | 5 |

Notable fix clusters: vehicle/turret server-side audio+FX emission (raptor/spiderbot death blasts, takeoff/flight
sounds) went missing→faithful; bot objective roles (assault/keyhunt) graduated to timing-faithful; sv-teamplay bot
autobalance poll de-cvar-gated to match Base's commented-out gate + the stronger-team rejection notification pair;
weapon rows broadly re-confirmed faithful after the Wave-12 line-number shifts.

## Reclassified (neutral)

~30 rows moved between equivalent classifications after fresh adversarial re-derivation (mostly unknown→faithful
verifications and presentation/CSQC gaps re-homed to their correct rows). No behavioral change.

## Human-review correction

`sv-teamplay.change.kill_on_teamchange` (batch 9) carried forward stale baseline text claiming the port used "a
single AutoTeamChange deathtype for both" — superseded by commit d3139d3. Corrected in the opus review pass to
record the manual/auto deathtype split + the frag-negation-skip constant.
