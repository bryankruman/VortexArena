# Parity drift — 2026-06-27

**0 regressions · 2 new gaps · 9 fixes · 2 reclassified · 4 newly-unmapped Base features** across 10 units (deathmatch, domination, duel, invasion, mutator-campcheck, mutator-dodging, mutator-pinata, physics-movetypes, weapon-crylink).

## Regressions

No regressions detected.

## New gaps

| unit | feature | dimension | was -> now | reason |
| --- | --- | --- | --- | --- |
| physics-movetypes | physics-movetypes.walk.stepmove | logic | faithful (no residual gaps) -> faithful (1 new minor gap) | Base `_Movetype_Physics_Walk` calls `if(GAMEPLAYFIX_UNSTICKPLAYERS(this)) _Movetype_CheckStuck(this)` at its top (walk.qc:9-10; cvar default 1, xonotic-server.cfg:575). The port's `WalkMove` has no CheckStuck/unstick-probe call, so a monster/STEP body that begins a tick embedded in geometry is not extricated before the slide. Minor — FlyMove's startsolid-allsolid bail + PushEntity's own NudgeOutOfSolid still prevent runaway; logic remains faithful overall. Added gap + a match:false constant row. |
| weapon-crylink | crylink.menu.describe | presentation | (unmapped) -> missing | Base crylink.qc:609-626 has a MENUQC `describe()` weapon-guide prose method with no row in the baseline registry; added as a tracked missing/na row (menu-only documentation, no gameplay impact, consistent with weapon-shotgun.menu.describe). |

## Registry coverage gaps

Base features with no registry row (registry is missing coverage for these):

- **deathmatch** — `deathmatch.qc:METHOD(Deathmatch, describe)` (MENUQC): multi-paragraph gametype help/description prose shown in the menu gametype picker; no baseline row and no port implementation (GameType base class has no `Describe()` override). Added as new row `deathmatch.menu.describe` (presentation: missing).
- **duel** — `duel.qc:Duel.describe` (MENUQC): the duel gametype help-page descriptive text ("1-vs-1 arena battle ... essentially a 2-player version of Deathmatch", item-control / XPM paragraphs) shown in the menu gametype tooltip/help. No registry row; port has no equivalent gametype-description string for duel (presentation/menu-help, low impact).
- **invasion** — `invasion.qh:METHOD(Invasion, m_generate_mapinfo)`: a map advertises Invasion support when it contains an `invasion_spawnpoint` entity (`MapInfo_Map_supportedGametypes |= gametype_flags`). This mapinfo-generation/discovery tooling has no registry row (the port's map-support discovery path, if any, is unmapped). Marginal: build/editor-time tooling, not runtime gameplay.
- **weapon-crylink** — MENUQC `describe()` weapon-guide prose (crylink.qc:609-626): now added as `crylink.menu.describe` (see New gaps). Note: `SOUND(CRYLINK_LINKJOIN, crylink_linkjoin)` declared in crylink.qh:12 but NEVER called by Base QC — dead in Base too, so NOT a port gap; intentionally given no row.

## Fixes

| unit | feature | dimension | was -> now | reason |
| --- | --- | --- | --- | --- |
| domination | domination.capture.touch | liveness | live -> live | Baseline recorded the round-not-started capture gate as DEAD because `dom.RoundStartedSource` was never assigned by the host (only RoundEndTimeSource was wired). The host now assigns `dom.RoundStartedSource = () => domRounds.IsRoundStarted` at GameWorld.cs:2807 inside the `if(dom.RoundBased)` arm, so CapturePoint's `if (RoundStartedSource is {} s && !s()) return;` actually fires in the round-based variant. The dead-source gap is closed; the row's own liveness was already 'live' (the recapture guard path), but the previously-dead sub-feature is now live. Gap removed from gaps[]. |
| domination | domination.capture.touch | logic | faithful -> faithful | The `round_handler_IsActive && !round_handler_IsRoundStarted` capture-block branch is now reachable on the live path (RoundStartedSource wired @GameWorld.cs:2807), so the capture logic now fully mirrors QC dompointtouch in the round-based variant instead of having a dead gate. Status stays faithful but the worst-gap entry was removed. |
| duel | duel.mapsupport.gating | logic | missing -> partial | `m_isForcedSupported` (force duel on DM maps) now implemented in `MapInfoBackend.ApplyForcedGametypes` (MapInfoBackend.cs:115), called live from `Get` (:104). Only `m_isAlwaysSupported` diameter<3250 remains unported. |
| duel | duel.mapsupport.gating | values | missing -> partial | `g_duel_not_dm_maps` now read+honored (ForceDuelOnDmMaps default true = cvar 0) at CreateGameScreen.cs:590 / DialogCreateGameMapInfo.cs:50 — that constant now matches. max_diameter=3250 still unmodeled (match:false). |
| duel | duel.mapsupport.gating | liveness | na -> live | `ApplyForcedGametypes` runs on every `MapInfoBackend.Get` and is wired from the live menu cvar path; unit-tested MenuDataSourceTests.cs:348-373. |
| invasion | invasion.win.hunt | logic | partial -> faithful | The port's earlier dual-path (live world list OR Wave.Spawned/Killed counters) plus the `_huntSawMonster` 'must have seen >=1 monster' guard were both removed. Invasion.cs:556-561 now wins HUNT purely on `LiveNonRespawnedMonsterCount()<=0`, exactly mirroring Base `WinningCondition_Invasion`'s INV_TYPE_HUNT branch (IL_EACH g_monsters !MONSTERFLAG_RESPAWNED with RES_HEALTH>0), including Base's documented empty-map insta-win. A new test `HuntType_EmptyMap_InstaWins` covers that semantic. |
| mutator-campcheck | campcheck.realclient.gate | liveness | partial -> live | The chat-typing exemption carrier is now wired END-TO-END. The baseline recorded liveness partial because InputButtons had no Chat bit and ServerNet.ToMovementInput never set Typing, so player.ButtonChat was permanently false for net clients. Current code: InputCommand.cs:22 defines `Chat = 1 << 7`; NetGame.cs:4965 sets InputButtons.Chat when the console/chat prompt is open; ServerNet.cs:1626 decodes `Typing = (b & InputButtons.Chat) != 0`; PlayerPhysics.cs:252 writes `player.ButtonChat = input.Typing`. A typing real client is now genuinely exempted (TypeCheck || !ButtonChat). All four QC guard terms (interval, real-client/bot-exempt, typecheck-chat, weaponLocked) are present and live. |
| mutator-campcheck | campcheck.realclient.gate | values | partial (typecheck constant) -> faithful (typecheck constant match true) | The `typecheck / !PHYS_INPUT_BUTTON_CHAT` constant was marked match:partial in the baseline because the carrier never reached the server. With the wire now complete end-to-end, the constant matches Base faithfully (match:true). The row-level values status was already faithful; this updates the per-constant match flag. |
| mutator-dodging | mutator-dodging.clientselect.opt_in | gaps | RESIDUAL: cl_dodging_timeout registered as replicate cvar but has NO provider; read once server-side at Hook() and shared by all clients; the comment names a 'TimeoutProvider' seam that does not exist; double-tap window not per-client. | Resolved: DodgingMutator.TimeoutProvider now exists (DodgingMutator.cs:93) and is installed at Commands.cs:577-578 reading per-client cl_dodging_timeout from the replicated store; TimeoutFor consults it; double-tap window is now per-client. gaps now []. | The per-client double-tap-window seam the baseline recorded as missing is now fully wired in the current code: the TimeoutProvider field and its Commands.cs installation both exist and TimeoutFor (DodgingMutator.cs:258-262) consults it before the server-side fallback. Reality-better-than-baseline. |
| mutator-pinata | mutator-pinata.report.mutators_pretty_string | liveness | dead -> live | The MutatorActivation.BuildMutatorsPrettyString chain runner now has live callers: GameWorld.SendMutatorsWelcome (GameWorld.cs:1433) is invoked per-join from ClientConnect (GameWorld.cs:1318, !p.IsBot path) and surfaces the 'Active modifications: <list>' welcome line to the joining client (QC SendWelcomeMessage server/client.qc:1107); GameWorld.cs:1152 writes the stripped 'modifications' serverinfo key at match start; ServerNet.cs:608 builds it on the net path. The ', Piñata' token now reaches a UI. Baseline recorded liveness=dead (no external consumer). |
| mutator-pinata | mutator-pinata.enable.predicate | logic | faithful -> faithful | ExprEvaluate was a simplified local helper in the baseline (only ''/'0'/'false', no operators/cvar-refs, and expr_evaluate('') diverged returning false). It is now a full faithful port of QC expr_evaluate (lib/cvar.qh:48): +/- prefix, whitespace-token AND chain via ExprToken with comparison operators (>= <= == != === !== > <) in QC's strstrofs source order, bare literal-number vs named-cvar truthiness, '!' invert, and expr_evaluate('')=true matching Base. The two documented gaps in this row are now resolved (gaps cleared); status was already faithful but the defect described in the baseline is gone. |
| physics-movetypes | physics-movetypes.toss.ballistic | logic | faithful (with recorded gaps: bmodelstartsolid unstick-retry-giveup absent; move_suspendedinair SOLID_BSP gate absent) -> faithful (no gaps) | Prior-pass re-audit error. The port DOES replicate both: the bmodelstartsolid unstick-retry-give-up sequence (MoveTypePhysics.cs:170-182 == toss.qc:60-82) and the move_suspendedinair SOLID_BSP gate (MoveTypePhysics.cs:233-234 `trace.Ent==null || trace.Ent.Solid==Solid.Bsp` == toss.qc:130-131). MoveTypePhysics.cs is unchanged since Wave 13, so these were always present; the baseline incorrectly recorded them as missing. Removed both gaps; row now clean. |

## Reclassified

| unit | feature | dimension | was -> now | reason |
| --- | --- | --- | --- | --- |
| domination | domination.roundbased.player_blocked | liveness | partial -> partial | Liveness value unchanged (partial), but the rationale is corrected: the baseline tied this to the same DEAD RoundStartedSource wiring as capture.touch. That source is now wired (GameWorld.cs:2807), so the generic round-not-started gates (WeaponFireDriver.RoundFireForbidden @1621, capture gate, respawn suppression @3476) are live and cover most of player_blocked's practical effect during round warmup. The residual gap is now narrowed to the literal QC movement-freeze flag not being reproduced, not a fully-dead path. |
| invasion | invasion.win.hunt | liveness | live -> live | Confidence raised medium->high: the win source is now verified Base-faithful and exercised by three tests (HuntType_WinsWhenAllSpawnedMonstersCleared / HuntType_DoesNotAdvanceRounds / HuntType_EmptyMap_InstaWins); the prior 'unverified on a real HUNT map' caveat and the partial verification record are resolved. |
