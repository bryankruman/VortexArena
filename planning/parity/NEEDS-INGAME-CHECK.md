# Needs in-game check

_Generated 2026-06-22. 52 features whose presentation/liveness/timing code-reading could not confirm. Verify by running the app and observing._

| unit | feature | uncertain | what to look for |
|---|---|---|---|
| `cl-hud` | `cl-hud.engine.configure_mode` | timing | Drag/resize/keyboard/grid present; per-panel-settings-dialog + export parity not runtime-verified. |
| `cl-hud` | `cl-hud.panel.weapons` | timing | Owned-weapon grid + complainbubble + label modes + timeout fade present and fed live. |
| `cl-hud` | `cl-hud.panel.powerups` | timing | Active-powerup grid with timers; fed live on listen server. |
| `cl-hud` | `cl-hud.panel.racetimer` | timing | Full render code exists and works when given data; purely a wire-up gap. |
| `cl-hud` | `cl-hud.panel.modicons` | timing | Driven live from gametype; icon fidelity per mode varies. |
| `cl-hud` | `cl-hud.panel.pressedkeys` | low-conf | Key grid present; data feed for spectated players not confirmed. |
| `cl-hud` | `cl-hud.panel.engineinfo` | timing | FpsPanel (port extra) covers FPS; engineinfo parity is low-priority. |
| `cl-hud` | `cl-hud.panel.infomessages` | timing | Observing/press-to-join/warmup/countdown lines present and fed live. |
| `cl-hud` | `cl-hud.panel.physics` | timing | Speedometer + topspeed + jumpspeed + accel-in-g + unit present; cvar-gated show-mode wired. |
| `cl-hud` | `cl-hud.panel.itemstime` | timing | Per-item respawn timers + icons + countdown present and fed live. |
| `cl-hud` | `cl-hud.panel.quickmenu` | low-conf | Open/close + key/click nav present; entry-source fidelity unverified. |
| `cl-hud` | `cl-hud.panel.scoreboard` | timing | Columns + team totals + self/eliminated highlight + accuracy + header render live. |
| `cl-hud` | `cl-hud.panel.strafehud` | timing | Strafe bar present and fed; faithful-to-stock-strafehud detail unverified. |
| `cl-hud` | `cl-hud.panel.pickup` | timing | Recent-pickup line with icon + name + xcount fade present and fed. |
| `cl-hud` | `cl-hud.panel.checkpoints` | timing | Full render code exists; purely a wire-up gap (same family as racetimer). |
| `cl-hud` | `cl-hud.panel.mapvote` | timing | Server has MapVoting.cs; the client panel renders when given data but is never fed at intermission. |
| `cl-hud` | `cl-hud.panel.minigame` | timing | Draft OMITTED all 4 Base minigame HUD panels; they ARE ported and live via the MinigameClient coordinator (board renderer + menu + help). Board/status combined into MinigameRenderer. |
| `domination` | `domination.roundbased.win` | timing | Wave-2 FIX: the cumulative 5-cap match win is now enforced — CheckRoundWinner latches MatchEnded/LeaderTeam off ST_DOM_CAPS once a team banks roundbased_point_limit caps, and GameWorld reads them. Single-round resolution + time-out no-winner already wired. Roundbased is non-default (g_domination_roundbased 0). |
| `fx-effectinfo` | `fx-effectinfo.emit.te_builtins` | low-conf | Added by adversarial pass (draft omitted a row; only the spec port-mapping table mentioned te_*). Caller coverage of these specific wrappers not exhaustively traced; weapon/combo emission largely uses Emit(Effect,...) directly. |
| `lms` | `lms.lives.eliminate_at_zero` | timing | Unchanged by Wave-1/2. Liveness still partial: elimination bookkeeping is live, but the behavior that should follow (respawn denial) is not wired; the round gate substitutes. |
| `lms` | `lms.win.last_standing` | timing | Unchanged by Wave-1/2. Values still partial (forfeit time + tie-cancel constants absent). The single-survivor core works on the live path. |
| `lms` | `lms.respawn.continuous` | timing | Unchanged headline logic inversion. Flagged as a gap (expedient handler reuse), not an intended divergence. |
| `lms` | `lms.item.extra_life` | presentation | Unchanged. logic partial: the spawn pathway diverges from Base's in-place HealthMega replacement. Grant logic + CENTER_EXTRALIVES coded but dead; default g_lms_extra_lives=0 anyway. |
| `mapobject-target` | `mapobject-target.spawnpoint.use` | liveness | Re-verified 2026-06-22 unchanged. Sets actor.SpawnPointTarg = this, matching QC. Whether SpawnSystem honors it belongs to the spawnsystem unit. |
| `mapobject-target` | `mapobject-target.location.spawn` | presentation,liveness | Re-verified 2026-06-22 unchanged. Passive SOLID_NOT point pushed to MapObjectsState.Locations; matches target_push_init + IL_PUSH(g_locations). info_location copies netname->message then re-sets classname. |
| `mapobject-teleporters-portals` | `teleporters.dest.random_selection` | timing | Unchanged. RandomSelection RNG parity vs Base's per-tick RandomSelection not behaviorally verified (timing unknown). |
| `mapobject-teleporters-portals` | `teleporters.trigger.team_claim` | liveness | WAVE-2 FIX: the team claim is now `if(teamplay)`-guarded (matching trigger_teleport_use) AND — crucially — is now actually enforced, because TeleportActive gained the team gate (teleporters.trigger.active_gate). Liveness still unknown (whether a use-link calls TeleportUse on a teamplay map not traced) but the logic gap is closed. |
| `mapobject-teleporters-portals` | `portals.teleport.transform_and_amazing` | timing | Two-way LinkPair stays FAITHFUL on directionality. WAVE-2 added the FORCE_TDEATH bitmask to Teleporters, but the Porto-portal warpzone path does not use TeleportPlayer (it warps via Warpzone.Teleport), so FORCE_TDEATH is NOT actually applied to portal crossings — the bitmask is wired only for map teleporters. Amazing, activatetime self-skip, independent-player gating, owner kill-credit, and the unverified transform all remain open. |
| `monster-mage` | `monster-mage.anim.frames` | timing | Unchanged by Wave-2. mr_anim is GAMEQC (shared client+server) for frame-group setup; the port collapses distinct shoot/melee/duckjump frames into one Attack phase. Whether the renderer plays the correct frame ranges is unverified. |
| `monster-spider` | `monster-spider.respawn.cycle` | presentation | Respawn-at-death-point (MONSTER_RESPAWN_DEATHPOINT) modeled; corpse-fade visual is the client's concern. Wave-2 added EF_RED (=128) re-apply on a respawned miniboss (MonsterAI.cs:1733, Respawn) — faithful to QC Monster_Miniboss_Setup:523; not spider-specific. No regression. |
| `mutator-buffs` | `buffs.networking` | liveness,timing | Unchanged. Defer the live-send confirmation to the status_effects parity unit. |
| `mutator-bugrigs` | `mutator-bugrigs.racecar.drive_model` | presentation | Algorithm and all 6 numeric constants match Base line-for-line (MathF vs QC float math). presentation downgraded faithful->unknown: the drive model's whole purpose is the rendered rig orientation (surface-snap + body lean + smoothing writes to self.Angles), but there is NO in-game/visual verification of the integrated rig feel or rendered body fidelity, and the forced chase camera that makes it observable is itself missing (see chase_camera row). Per SCHEMA rule 5, unverified visible fidelity is 'unknown', not 'faithful'. |
| `mutator-campcheck` | `campcheck.check.vehicle` | liveness | Logic/value faithful; liveness depends on vehicles being driveable in the port (out of scope here). |
| `mutator-dodging` | `mutator-dodging.input.double_tap` | timing | FIXES vs baseline: (1) the frozen_no_doubletap single-tap branch is now present in all four direction tests (matches Base X-macro `\|\| frozen_no_doubletap`); (2) the spurious crouch-exclusion on the +dodge button was REMOVED — dodgeButton is now checked unconditionally, matching Base PHYS_INPUT_BUTTON_DODGE. So the detection SHAPE is now fully faithful. STILL DEAD: its input source fields are never populated. Multijump and bugrigs read the same dead MovementForward/Right fields — one input-bridge fix would revive all three. |
| `mutator-dodging` | `mutator-dodging.input.delay_state_gate` | timing | FIX vs baseline: the hardcoded C# fallback default for sv_dodging_delay is now 0.6 (was 0.5), matching Base — values upgraded partial→faithful. Gate ordering, the three OR'd state gates, and the trace logic still mirror Base. Only residual hazard is the if-nonzero fallback guard on the *_threshold/delay cvars (harmless with the shipped cfg). |
| `mutator-dodging` | `mutator-dodging.move.ramp_impulse` | timing | FIX vs baseline: the swimming/dead abort branch now includes BOTH the clientselect term AND the frozen_dodging carve-out, matching Base (sv_dodging.qc:223-224) — logic upgraded partial→faithful. Ramp math, the min cap, the one-shot up flag, the air-dodge v_angle choice, and the completion reset remain faithful. Correct but still unreachable because the trigger is dead. |
| `mutator-instagib` | `mutator-instagib.spawn.fullbright` | presentation | Flag set on the live spawn path; render effect not behaviorally verified. OnPlayerSpawn also reapplies the Vaporizer-only loadout and resets countdown bookkeeping. |
| `mutator-multijump` | `mutator-multijump.counter.networking` | presentation,liveness,timing | Unchanged by Wave-2. Base relies on REGISTER_STAT to ship multijump_count to CSQC so client prediction of the re-jump matches the server. The port's prediction architecture (single shared physics path) may make this moot or may need an explicit sync -- unresolved. Marked unknown/low per the no-inflated-confidence rule; a runtime/prediction check is needed to resolve it. |
| `mutator-overkill` | `overkill.weapons.unblock` | liveness | OK weapon balance/behavior is a separate weapon unit; only the unblock-gate is in scope here. |
| `physics-player` | `physics-player.water.swim` | liveness | Non-viewloc water physics ported faithfully incl. the QW water-jump-out launch and frozen-resurface. logic downgraded to partial for the unported viewloc sub-cases. liveness:unknown — code reachable from the branch chain but no in-game water-map check was run. |
| `physics-player` | `physics-player.ladder` | liveness | Gravity-free climb via the half-step that cancels the WalkMove gravity. Reachable from the branch chain (player.LadderEntity != null). func_water + viewloc ladder sub-cases unported (rare map features); logic:partial. Not behaviorally tested on a ladder map this audit. |
| `physics-player` | `physics-player.jetpack` | liveness | Closed-form thrust math ported verbatim incl. the reverse-thrust crouch branch. Liveness unknown: reached only when the Jetpack item is held + activated; not behaviorally tested. values:partial because the g_jetpack_* tunables are baked as constants rather than cvar-read. |
| `survival` | `survival.round.assign_roles` | timing | Assignment algorithm unchanged + still live. The added per-player role notify is dead (Notifications sink unwired). |
| `survival` | `survival.round.handler` | timing | Wave-2 FIXED the Init constant fallbacks (values partial→faithful). The dual-drive architecture is unchanged and the surv.Handler↔live-handler timing is still unmirrored — logic stays partial / liveness partial pending a runtime trace. |
| `sv-handicap` | `sv-handicap.init.defaults_to_one` | timing | Matters only if an entity is reused across matches without re-construction, or once handicap_level exists. |
| `tmayhem` | `tmayhem.teams.count` | presentation | TeamCount drives the team seed/leader; the map-entity team-color registration (Team_MapEnts_FindOrSpawn) is an open presentation question. Untouched by Wave-2. |
| `turret-plasma_dual` | `turret-plasma_dual.damage.gating_die_respawn` | timing | FIX (Wave-2 liveness): the pre-damage gate (TurretAI.Damage) was actually DEAD pre-wave — TurretSpawn.cs only had a comment claiming the damage router called it; grep confirms ZERO callers on main. Wave-2 added the GtEventDamage shim + DamageSystem routing, so the friendly-fire/inactive-immunity/retaliate gate + turret-specific health subtract now run on the real Apply path. STILL OPEN: the port-added death blast (Base commented out) and the unmodeled -0.2 respawn split. |
| `weapon-porto` | `weapon-porto.touch.portal_spawn` | presentation,timing | PortalSpawner is wired LIVE in GameWorld.Boot (line 412), so placement reaches the warpzone manager on the real match path. PortalClearWithId is declared (Porto.cs:331) but NOT wired in GameWorld (no host assignment found) — the cnt<0 fail in-portal cleanup is a no-op. Whether the realised warpzone teleports correctly + expires at 15s is unverified here (warpzone unit owns it). |
| `weapon-porto` | `weapon-porto.presentation.projectile_render` | liveness | Catalog entries present (scale 4, TR_WIZSPIKE, red/blue glow). In-match render not runtime-verified (porto rarely placed); the runtime red->blue reclassification remains suspect. Unchanged by Wave-2. |
| `weapon-vaporizer` | `weapon-vaporizer.rm.primary_explosion` | liveness | FIXES vs baseline: (1) cvar fallbacks raised from half-strength 35/15/90/200 to the correct Base 70/38/140/400; (2) blast now tagged as a Devastator death + HITTYPE_SPLASH (devTag) with accuracyWeapon=devastator, so the rocketminsta self-damage-nullify/gib hooks (keyed on devastator) and accuracy/obituary attribution match Base; (3) the sky/NOIMPACT endpoint guard from Base is now applied before exploding. Residual: explosion presentation effect + g_rm live-path reachability. |
| `weapon-vaporizer` | `weapon-vaporizer.rm.laser_barrage` | liveness | FIXES vs baseline: cvar fallbacks raised to correct Base values (was count 1/dmg 25/radius 80/force 300/speed 5000/lifetime 0.3/spread 0); bolt deathtype now WEP_ELECTRO (was Vaporizer — fixes kill feed, accuracy weapon, electro-keyed mutator); muzzle now ELECTRO_MUZZLEFLASH; mode-0 fan sound now crylink_fire (was electro_fire2). Bolt physics (BounceMissile, per-bolt EditProjectile hook, touch+think splash, lateral spread, /count damage&force split) remain structurally faithful. STILL OPEN: the rapid ladder, shooter-velocity inheritance, the CSQC bolt visual, and the ELECTROBITCH announce. |
| `weapon-vaporizer` | `weapon-vaporizer.messages.kill_suicide` | liveness | Unchanged by the waves. The Vaporizer wr_killmessage/wr_suicidemessage ARE ported, centralized in DeathMessages.cs (a string lookup keyed by weapon NetName). Liveness still unknown: the SelectKillMessage/SelectSuicideMessage call site in the kill-feed path was not traced end-to-end in this audit. |