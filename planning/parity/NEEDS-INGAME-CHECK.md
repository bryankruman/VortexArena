# Needs in-game check

_Generated 2026-06-22. 60 features whose presentation/liveness/timing code-reading could not confirm. Verify by running the app and observing._

| unit | feature | uncertain | what to look for |
|---|---|---|---|
| `cl-hud` | `cl-hud.engine.configure_mode` | timing | Drag/resize/keyboard/grid present; per-panel-settings-dialog + export parity not runtime-verified. |
| `cl-hud` | `cl-hud.panel.weapons` | timing | Owned-weapon grid + complainbubble + label modes + timeout fade present and fed live. |
| `cl-hud` | `cl-hud.panel.powerups` | timing | Active-powerup grid with timers; fed live on listen server. |
| `cl-hud` | `cl-hud.panel.racetimer` | timing | Full render code exists and works when given data; purely a wire-up gap. |
| `cl-hud` | `cl-hud.panel.modicons` | timing | Driven live from gametype; icon fidelity per mode varies. |
| `cl-hud` | `cl-hud.panel.pressedkeys` | low-conf | Key grid present; data feed for spectated players not confirmed. |
| `cl-hud` | `cl-hud.panel.engineinfo` | timing | FpsPanel (port extra) covers FPS; engineinfo parity is low-priority. |
| `cl-hud` | `cl-hud.panel.infomessages` | timing | Observing/press-to-join/warmup/countdown lines present and fed live. |
| `cl-hud` | `cl-hud.panel.physics` | timing | Speedometer + topspeed + jumpspeed + accel-in-g + unit present; cvar-gated show-mode wired. |
| `cl-hud` | `cl-hud.panel.itemstime` | timing | Per-item respawn timers + icons + countdown present and fed live. |
| `cl-hud` | `cl-hud.panel.quickmenu` | low-conf | Open/close + key/click nav present; entry-source fidelity unverified. |
| `cl-hud` | `cl-hud.panel.scoreboard` | timing | Columns + team totals + self/eliminated highlight + accuracy + header render live. |
| `cl-hud` | `cl-hud.panel.strafehud` | timing | Strafe bar present and fed; faithful-to-stock-strafehud detail unverified. |
| `cl-hud` | `cl-hud.panel.pickup` | timing | Recent-pickup line with icon + name + xcount fade present and fed. |
| `cl-hud` | `cl-hud.panel.checkpoints` | timing | Full render code exists; purely a wire-up gap (same family as racetimer). |
| `cl-hud` | `cl-hud.panel.mapvote` | timing | Server has MapVoting.cs; the client panel renders when given data but is never fed at intermission. |
| `cl-hud` | `cl-hud.panel.minigame` | timing | Draft OMITTED all 4 Base minigame HUD panels; they ARE ported and live via the MinigameClient coordinator (board renderer + menu + help). Board/status combined into MinigameRenderer. |
| `clanarena` | `clanarena.spawn.forbid_throw_weapon` | liveness | Low impact if the port has no weapon-throw feature at all. New row; needs a port-side weapon-drop check to finalize. |
| `domination` | `domination.roundbased.win` | timing | Roundbased is non-default (g_domination_roundbased 0). Single-round resolution is wired; the cumulative 5-cap match win is the logic gap. round_timelimit/warmup pass through the host round handler (numeric parity unverified). |
| `fx-effectinfo` | `fx-effectinfo.emit.te_builtins` | low-conf | Added by adversarial pass (draft omitted a row; only the spec port-mapping table mentioned te_*). Caller coverage of these specific wrappers not exhaustively traced; weapon/combo emission largely uses Emit(Effect,...) directly. |
| `keepaway` | `keepaway.ball.spawn` | timing | Entity layer is fully coded but unreachable. Needs a host-side procedural spawner to replace ka_Handler_CheckBall. |
| `lms` | `lms.lives.eliminate_at_zero` | timing | Liveness downgraded live→partial: the elimination *bookkeeping* is live, but the behavior that should follow from it (respawn denial) is not wired; the round gate substitutes. |
| `lms` | `lms.win.last_standing` | timing | Values downgraded (forfeit time + tie-cancel constants absent). The single-survivor core works on the live path. |
| `lms` | `lms.respawn.continuous` | timing | Confidence raised medium→high: the EnableRounds call and the round gates are confirmed in source. This is the headline logic inversion. Flagged as a gap (expedient handler reuse), not an intended divergence. |
| `lms` | `lms.item.extra_life` | presentation | logic downgraded faithful->partial: the spawn pathway diverges from Base's in-place HealthMega replacement. Grant logic + CENTER_EXTRALIVES coded but dead; default g_lms_extra_lives=0 anyway. |
| `mapobject-target` | `mapobject-target.spawnpoint.use` | liveness | Sets actor.SpawnPointTarg = this, matching QC. Whether SpawnSystem honors it belongs to the spawnsystem unit. |
| `mapobject-target` | `mapobject-target.location.spawn` | presentation,liveness | Passive SOLID_NOT point pushed to MapObjectsState.Locations; matches target_push_init + IL_PUSH(g_locations). info_location copies netname->message then re-sets classname (verified). |
| `mapobject-teleporters-portals` | `teleporters.dest.random_selection` | timing | RandomSelection RNG parity vs Base's per-tick RandomSelection not behaviorally verified (timing unknown). |
| `mapobject-teleporters-portals` | `teleporters.trigger.team_claim` | liveness | Liveness unknown — whether a use-link actually calls TeleportUse on a teamplay map was not traced. Even if claimed, the absence of the team gate in TeleportActive makes the claim inert. |
| `mapobject-teleporters-portals` | `portals.teleport.transform_and_amazing` | liveness,timing | CORRECTION vs draft: the draft claimed Base portals are ONE-WAY (in->out) and flagged the port's two-way linking as a gap/intended_divergence. That is WRONG — Portal_Connect sets enemy on BOTH portals and both run Portal_Think, so Base Porto portals ARE two-way (a player walks through either side). The red/blue skins are cosmetic. The port's two-way LinkPair is therefore FAITHFUL on directionality; the remaining gaps are Amazing, activatetime self-skip, independent-player gating, owner kill-credit, FORCE_TDEATH, and the unverified transform. |
| `monster-mage` | `monster-mage.anim.frames` | timing | mr_anim is GAMEQC (shared client+server) for frame-group setup; the port collapses distinct shoot/melee/duckjump frames into one Attack phase. Whether the renderer plays the correct frame ranges is unverified. |
| `monster-spider` | `monster-spider.respawn.cycle` | presentation | Respawn-at-death-point (MONSTER_RESPAWN_DEATHPOINT) modeled; the corpse-fade visual (SUB_SetFade) is the client's concern — the port just removes after time+3 (presentation unverified). |
| `monster-spider` | `monster-spider.menu.describe` | presentation,liveness | MENUQC-only flavor text; low gameplay impact. Listed for completeness. |
| `monster-wyvern` | `wyvern.menu.describe` | presentation,liveness | MENUQC-only flavor text; low gameplay impact. Listed for completeness (mirrors monster-spider.menu.describe). |
| `mutator-buffs` | `buffs.networking` | liveness,timing | Defer the live-send confirmation to the status_effects parity unit. |
| `mutator-bugrigs` | `mutator-bugrigs.racecar.drive_model` | presentation | Algorithm and all 6 numeric constants match Base line-for-line (MathF vs QC float math). presentation downgraded faithful->unknown: the drive model's whole purpose is the rendered rig orientation (surface-snap + body lean + smoothing writes to self.Angles), but there is NO in-game/visual verification of the integrated rig feel or rendered body fidelity, and the forced chase camera that makes it observable is itself missing (see chase_camera row). Per SCHEMA rule 5, unverified visible fidelity is 'unknown', not 'faithful'. |
| `mutator-campcheck` | `campcheck.check.vehicle` | liveness | Logic/value faithful; liveness depends on vehicles being driveable in the port (out of scope here). |
| `mutator-dodging` | `mutator-dodging.input.double_tap` | timing | Detection SHAPE for the doubletap is faithful (per-direction state-change test, timeout-vs-delay split, normalized tap dir). Dead because its input source fields are never populated. Multijump and bugrigs read the same dead MovementForward/Right fields — one input-bridge fix would revive all three. The CSQC pressedkeys-rebuild (PM_dodging_GetPressedKeys) is folded into the tail of CheckPressedKeys. |
| `mutator-dodging` | `mutator-dodging.input.delay_state_gate` | timing | Gate ordering, the three OR'd state gates, and the trace logic all mirror Base. MaxSpeed/AirMaxSpeed use the unguarded cvar read so 0=no-cap is faithful; only the *_threshold/delay/force/speed cvars carry the if-nonzero fallback hazard. |
| `mutator-dodging` | `mutator-dodging.move.ramp_impulse` | timing | Ramp math, the min cap, the one-shot up flag, the air-dodge v_angle vector choice, and the completion reset are faithful. ViewAnglesOf falls back to Angles when ViewAngles unset (port-specific safety, immaterial here). Correct but unreachable because the trigger is dead. |
| `mutator-hook` | `mutator-hook.precedence.blaster_override` | liveness,timing | Niche (both mutators rarely co-enabled) and currently moot (offhand input dead). Marked unknown pending a Mutators.All ordering trace. Draft listed this only as an open question; promoted to a tracked row. |
| `mutator-instagib` | `mutator-instagib.spawn.fullbright` | presentation | Flag set on the live spawn path; render effect not behaviorally verified. OnPlayerSpawn also reapplies the Vaporizer-only loadout and resets countdown bookkeeping. |
| `mutator-multijump` | `mutator-multijump.counter.networking` | presentation,liveness,timing | Base relies on REGISTER_STAT to ship multijump_count to CSQC so client prediction of the re-jump matches the server. The port's prediction architecture (single shared physics path) may make this moot or may need an explicit sync -- unresolved. Marked unknown/low per the no-inflated-confidence rule; a runtime/prediction check is needed to resolve it. |
| `mutator-overkill` | `overkill.weapons.unblock` | liveness | OK weapon balance/behavior is a separate weapon unit; only the unblock-gate is in scope here. |
| `physics-player` | `physics-player.water.swim` | liveness | Non-viewloc water physics ported faithfully incl. the QW water-jump-out launch and frozen-resurface. logic downgraded to partial for the unported viewloc sub-cases. liveness:unknown — code reachable from the branch chain but no in-game water-map check was run. |
| `physics-player` | `physics-player.ladder` | liveness | Gravity-free climb via the half-step that cancels the WalkMove gravity. Reachable from the branch chain (player.LadderEntity != null). func_water + viewloc ladder sub-cases unported (rare map features); logic:partial. Not behaviorally tested on a ladder map this audit. |
| `physics-player` | `physics-player.jetpack` | liveness | Closed-form thrust math ported verbatim incl. the reverse-thrust crouch branch. Liveness unknown: reached only when the Jetpack item is held + activated; not behaviorally tested. values:partial because the g_jetpack_* tunables are baked as constants rather than cvar-read. |
| `survival` | `survival.round.assign_roles` | timing | Algorithm pinned by tests and confirmed live. Re-classified liveness partial->live (assignment IS reached); the dual-handler desync is a timing/logic risk, captured in survival.round.handler. |
| `survival` | `survival.round.handler` | timing | Architectural double-drive. Needs a runtime trace to confirm which handler actually governs the live round and whether the timers desync. |
| `sv-handicap` | `sv-handicap.init.defaults_to_one` | timing | Matters only if an entity is reused across matches without re-construction, or once handicap_level exists. |
| `tka` | `tka.ball.spawn` | timing | Entity layer is coded but unreachable; needs a host-side procedural spawner to replace tka_Handler_CheckBall. |
| `tka` | `tka.score.nofrags` | liveness | Whether stray frags actually appear depends on the host's default scoring; flagged unknown for the live frag count. |
| `tmayhem` | `tmayhem.teams.count` | presentation | TeamCount drives the team seed/leader; the map-entity team-color registration (Team_MapEnts_FindOrSpawn) is an open presentation question. |
| `turret-plasma_dual` | `turret-plasma_dual.damage.gating_die_respawn` | timing | Friendly-fire gate + retaliate + inactive-immunity faithful. Death-blast is a port addition Base disabled. Headshake jitter (TFL_DMG_HEADSHAKE) moved to its own row (turret-plasma_dual.damage.headshake) — it is MISSING, contradicting the draft. |
| `weapon-porto` | `weapon-porto.touch.portal_spawn` | presentation,timing | PortalSpawner is wired LIVE in GameWorld.Boot (line 388), so placement reaches the warpzone manager on the real match path. Whether the realised warpzone teleports correctly + expires at 15s is unverified here (warpzone unit owns it). |
| `weapon-porto` | `weapon-porto.presentation.projectile_render` | liveness | Catalog entries present (scale 4, TR_WIZSPIKE, red/blue glow). In-match render not runtime-verified (porto rarely placed); the runtime red->blue reclassification is suspect. |
| `weapon-shotgun` | `weapon-shotgun.ammo.checkammo_reload` | timing | Ammo gate logic faithful for the default no-clip case; reload mechanics + the clip-load OR-term are shared-system concerns. Downgraded confidence note: the clip OR-term omission is a latent logic divergence if reload_ammo is ever set. |
| `weapon-vaporizer` | `weapon-vaporizer.rm.primary_explosion` | liveness | Also skips the trace_dphitq3surfaceflags sky/noimpact check; explodes unconditionally when g_rm is set. Liveness of g_rm in a shipped mode unverified. |
| `weapon-vaporizer` | `weapon-vaporizer.rm.laser_barrage` | liveness | Bolt physics (BounceMissile, per-bolt EditProjectile hook, touch+think splash, lateral spread, /count damage&force split) are structurally faithful; the values, deathtype, rapid ladder, muzzle/CSQC visual and the correct fire sounds are the gaps. VERIFIER: confirmed deathtype + sound + muzzle/visual mismatches against Base. |
| `weapon-vaporizer` | `weapon-vaporizer.messages.kill_suicide` | liveness | VERIFIER-ADDED feature the draft omitted (and mis-listed as a gap under the ammo feature). The Vaporizer's wr_killmessage/wr_suicidemessage ARE ported — centralized in DeathMessages.cs (a string lookup table keyed by weapon NetName) rather than as methods on the Vaporizer class. Liveness marked unknown: the SelectKillMessage/SelectSuicideMessage call site in the kill-feed path was not traced end-to-end in this audit. |