# Needs in-game check

_Generated 2026-06-28. 41 features whose presentation/liveness/timing code-reading could not confirm. Verify by running the app and observing._

| unit | feature | uncertain | what to look for |
|---|---|---|---|
| `cl-hud` | `cl-hud.engine.configure_mode` | timing | Drag/resize/keyboard/grid present; per-panel-settings-dialog + export parity not runtime-verified. |
| `cl-hud` | `cl-hud.panel.weapons` | timing | Owned-weapon grid + complainbubble + label modes + timeout fade present and fed live. Improved since baseline (Wave 8c): the per-weapon ammo clip bar now reveals the weapon_ammo art left-to-right (UV-clip) rather than distorting it into a stretched rect. |
| `cl-hud` | `cl-hud.panel.powerups` | timing | Active-powerup grid with timers; fed live on listen server. Improved since baseline (Wave 8c): the bar uses the shared faithful progressbar primitive and the expanding-flash now matches the QC expandingbox sizefactor + alpha fade. |
| `cl-hud` | `cl-hud.panel.modicons` | timing | Improved since baseline (Wave 17 2f0bfdb): the NexBall modicon (HUD_Mod_NexBall) is now ported AND wired — a real Nexball gametype exists (src/.../GameTypes/Nexball.cs), its ball-carrier net id rides GametypeStatusBlock.Kind.NexBall, and NetGame.UpdateModIcons feeds NexBallCarrying; the panel draws the blinking nexball_carrying carry-indicator with the QC expand transition. Now 11 of Base's 11 HUD_Mod handlers (closing the prior 'no NexBall gametype' gap). Earlier (Wave 12d 0b3c2a8): the CTF flag status-change crossfade is a line-for-line port and LMS/Race/TeamKeepaway were added. Remaining: approximate dom/nexball icon art fidelity (unverified). |
| `cl-hud` | `cl-hud.panel.engineinfo` | timing | FpsPanel (port extra) covers FPS; engineinfo parity is low-priority. |
| `cl-hud` | `cl-hud.panel.infomessages` | timing | Observing/press-to-join/warmup/countdown lines present and fed live. The cl_lms 'no more lives left' eliminated-player line is wired (LmsNoLives). |
| `cl-hud` | `cl-hud.panel.physics` | timing | Speedometer + topspeed + jumpspeed + accel-in-g + unit present; cvar-gated show-mode wired. The accel/speed/topspeed bars use the shared faithful progressbar primitive (accelbar art + signed baralign-3 center bar), now with the Wave 8c 3-slice rounded cap render. |
| `cl-hud` | `cl-hud.panel.itemstime` | timing | Per-item respawn timers + icons + countdown present and fed live. |
| `cl-hud` | `cl-hud.panel.quickmenu` | low-conf | Open/close + key/click nav present; entry-source fidelity unverified. |
| `cl-hud` | `cl-hud.panel.scoreboard` | timing | Improved since baseline (Wave 17 2f0bfdb): the per-row ping is now genuinely networked end-to-end — the server writes each human's ENet RTT into the wire row (bots/unmapped = -1), the wire carries it as a ushort, and the panel colorizes both score rows and the spectator block by the QC ping bands, closing the prior 'spectator-row ping not networked per-row' gap. Also added: playerid '#N' prefix, '... and N more' overflow line, sv_entnum sort tiebreak (stable order), and the warmup accuracy-block gate. Wave 12d: PingColor bands (20/80/200) + team_size_position 'N/M'. Wave 8c: per-round averaging, leadlimit/fraglimit header, SP_SKILL, spectator/observer split, 3-state respawn line. Remaining: the layout cvar-pos divergence (intended) + the accuracy show-DELAY timer (only the binary warmup gate is wired). |
| `cl-hud` | `cl-hud.panel.strafehud` | timing | Strafe bar present and fed; faithful-to-stock-strafehud detail unverified. |
| `cl-hud` | `cl-hud.panel.minigame` | timing | All 4 Base minigame HUD panels are ported and live via the MinigameClient coordinator (board renderer + menu + help). Board/status combined into MinigameRenderer. |
| `fx-effectinfo` | `fx-effectinfo.emit.te_builtins` | low-conf | Added by adversarial pass (draft omitted a row; only the spec port-mapping table mentioned te_*). Caller coverage of these specific wrappers not exhaustively traced; weapon/combo emission largely uses Emit(Effect,...) directly. |
| `mapobject-teleporters-portals` | `teleporters.dest.random_selection` | timing | RE-AUDIT 2026-06-28: the 'forces telefrag-avoid always-on' gap is REMOVED — PickDestination now reads g_telefrags_avoid live via CvarOr (Teleporters.cs:336), so the avoid priority is cvar-driven (see teleporters.core.telefrag_cvars). RandomSelection RNG parity vs Base's per-tick RandomSelection still not behaviorally verified (timing unknown), so confidence stays medium. |
| `mapobject-teleporters-portals` | `teleporters.trigger.team_claim` | liveness | Unchanged. The team claim is teamplay-guarded AND enforced by the TeleportActive team gate. Liveness still unknown (whether a use-link calls TeleportUse on a teamplay map not traced). |
| `mapobject-teleporters-portals` | `portals.teleport.transform_and_amazing` | timing | FIX (2026-06-27): three prior gaps CLOSED on the warpzone Teleport path (landed after the 2026-06-26 baseline). (1) ANNCE_ACHIEVEMENT_AMAZING is now awarded on a portal telefrag within 1s of portal creation (Warpzone.cs:330-333, portals.qc:192-194) — audio missing->faithful. (2) FORCE_TDEATH is now APPLIED: the warpzone crossing routes through Teleporters.PortalForceTelefrag with TELEPORT_FLAGS_PORTAL (Warpzone.cs:327) — the exit is force-telefragged, then EITHER the Amazing announcer OR the portal-owner kill-credit fires (mutually exclusive, mirroring QC). (3) Independent-player gating is in place (Warpzone.cs:286-292, a faithful no-op while IsIndependentPlayer==false). logic partial->faithful. STILL OPEN: the exact +/-48 plane-shift / safe-origin transform (values stays partial; correctness unverified, timing unknown). |
| `monster-mage` | `monster-mage.anim.frames` | timing | FIX since the 2026-06-25 baseline: AnimFrame now takes a variant arg (Mage.cs:69) and the Attack phase maps variant 0->anim_shoot '2', variant 1->anim_duckjump '4', variant 2->anim_melee '5' — matching M_Mage_Attack_Push (setanim anim_duckjump) and M_Mage_Defend_Heal (setanim anim_melee). The melee/duckjump-collapse gap is RESOLVED: values partial->faithful (all 7 groups now correct), presentation partial->faithful (distinct frame groups play per attack). die1/die2 random pick handled in monster-mage.death.pain. Only timing (animfixfps fps normalization) remains unknown. |
| `monster-spider` | `monster-spider.attack.bite` | presentation | logic/values/timing/audio faithful (dmg 35 / delay 1.5 / DEATH_MONSTER_SPIDER; silent empty-SoundCues melee cue bit-matching Base's no-.sounds spider). DRIFT vs baseline: presentation partial->unknown — the random melee/shoot bite anim coin-flip is now wired (Spider.cs:134) AND its frame is consumed client-side by DpmFrameDriver (Wave 17), closing the baseline's 'client DPM doesn't render it' note. Held at unknown (not faithful) per the not-runtime-verified rule. Real melee path unit-tested (obituary). |
| `monster-spider` | `monster-spider.anim.mr_anim` | presentation | RE-AUDITED 2026-06-28 (this wave): end-to-end DPM frame-driver chain re-verified line-by-line and STILL code-complete — ClientEntityView.cs:226 routes the monster (NetEntityKind.Generic) frameDriven:true -> ClientWorld.OnEntityUpdate adds entity.Index to _frameDriven (488) -> the EntityModelFactory DPM branch (ClientWorld.cs:1829) calls DpmFrameDriver.TryAttach(built, entity) when _frameDriven.Contains (1839-1840); DpmFrameDriver._Process reads Entity.Frame, clamps to the framegroup count and plays _clipsByGroup[group]; DpmBuilder bakes per-framegroup clips + FrameGroupClipsMeta. This wave's uncommitted client edits (raptor pitch/roll in ClientWorld, mage-spike order in ProjectileCatalog) do NOT touch this path. presentation HELD at unknown (confidence medium) per the 'visible fidelity not runtime-verified = unknown' rule — code-complete, not observed rendering in a live match. Shared infra: same chain serves all DPM monsters (golem/mage/zombie/wyvern). [History: landed Wave 17 / 2f0bfdb; baseline's 'client never consumes Entity.Frame' note is FALSE.] |
| `monster-spider` | `monster-spider.pain.mr_pain` | presentation | Mostly unchanged since baseline + one drift fix. logic/values/timing/audio stay faithful (PainWindow 0.44s, random pain1/pain2 via AnimVariant, silent empty-SoundCues cue all verified). DRIFT: presentation partial->unknown — the baseline held presentation:partial because 'the client DPM does not render the networked Entity.Frame'; that consumer (DpmFrameDriver, Wave 17) now exists and is wired, so the pain frame IS consumed client-side. Held at unknown (not faithful) per the not-runtime-verified rule. |
| `monster-spider` | `monster-spider.death.mr_death` | presentation | Mostly unchanged + one drift fix. logic/values/timing/audio stay faithful (RollDeathVariant random die1/die2, corpse/loot/scoring/NadeBonus, silent empty-SoundCues cue all verified). DRIFT: presentation partial->unknown — the baseline held presentation:partial because 'the client DPM doesn't render the networked frame'; that consumer (DpmFrameDriver, Wave 17) now exists and is wired, so the die1/die2 frame IS consumed client-side. Held at unknown (not faithful) per the not-runtime-verified rule. |
| `monster-spider` | `monster-spider.respawn.cycle` | presentation | Respawn-at-death-point (MONSTER_RESPAWN_DEATHPOINT) modeled; corpse-fade visual is the client's concern. EF_RED re-apply on a respawned miniboss; not spider-specific. No regression. |
| `monster-wyvern` | `wyvern.fireball.explode_effect` | presentation | Logic/values/liveness faithful (the blast effect is emitted on the live server explode path and registered for networking). DOWNGRADED presentation faithful->unknown, confidence high->medium: visible particle render not runtime-observed. Same emission/registration pattern as the player Fireball weapon. Fixed stale line cites (Wyvern :187, EffectsList :154). Unchanged this wave. |
| `monster-wyvern` | `wyvern.fireball.firemine_visual` | presentation | Code chain confirmed (server NetName 'wyvern' -> Projectile kind -> client wyvern->Firemine -> FireSprite+FIREMINE Desc) and UNCHANGED this wave. DOWNGRADED presentation faithful->unknown, confidence high->medium: the PROJECTILE_FIREMINE visual rendering is wired but not runtime-observed (the spec itself lists it among the not-yet-runtime-verified items). Fixed the stale :266-269 line citation -> :292. Same spawner shared by mage/golem (match via netname 'mage'/'golem'). |
| `monster-wyvern` | `wyvern.pain` | presentation | Logic/values/timing/audio faithful (0.5s window honored on the live damage path; voice cues are silent per the empty SoundCues set Wave-16 confirmed — so 'audio:faithful' = correct SILENCE, no wyvern_pain.wav, the earlier note's 'plays wyvern_pain.wav' was wrong). DOWNGRADED presentation faithful->unknown, confidence high->medium: the pain-frame client playback is wired but not runtime-observed. |
| `monster-wyvern` | `wyvern.death` | presentation | DOWNGRADED presentation faithful->unknown, confidence high->medium, audio faithful->na (Base wyvern mr_death plays NO death sound; the empty SoundCues set Wave-16 confirmed means MarkDead's MonsterSound('death') is silent — correct, but it is not an emitted cue so audio:na is more honest than audio:faithful). Logic/values faithful: the QC scatter velocities + Gravity restore are exact and verified in code. NUANCE: a GIBBED kill replaces .Think with a fade-only lambda so the scatter is skipped — faithful (a gibbed monster is in pieces). Per-edict scatter state in a ConditionalWeakTable, reset on respawn (Spawn removes the entry). The die1 frame PLAYBACK is wired but not runtime-observed. |
| `monster-wyvern` | `wyvern.deadthink` | presentation | Logic+timing+liveness faithful (the on-ground die2 hook is driven on the live corpse-fall path; the frame value swaps to 8 once grounded). DOWNGRADED presentation faithful->unknown, confidence high->medium: the landed-pose client morph is wired but not runtime-observed. |
| `monster-wyvern` | `wyvern.anim.frame_table` | presentation | Code seam (AnimFrame/DriveAnimFrame -> networked Entity.Frame -> ClientEntityView Generic frameDriven -> ModelAnimator.FollowEntityFrame) is fully wired and unchanged this wave. DOWNGRADED presentation faithful->unknown, confidence high->medium: the prior 'played client-side' claim was never runtime-verified and hinges on wyvern.dpm carrying >1 frame. Logic/values/timing/liveness remain faithful (the frame value IS computed + networked). Needs in-game spawn-and-observe to upgrade presentation. |
| `mutator-buffs` | `buffs.networking` | liveness,timing | Unchanged. Defer the live-send confirmation to the status_effects parity unit. |
| `mutator-bugrigs` | `mutator-bugrigs.racecar.drive_model` | presentation | Algorithm and all 6 numeric constants match Base line-for-line (MathF vs QC float math). presentation STAYS unknown after the 2026-06-28 re-audit: the drive model's whole purpose is the rendered rig orientation (surface-snap + body lean + smoothing writes to self.Angles), but there is STILL NO in-game/visual verification of the integrated rig feel or rendered body fidelity. The forced chase camera that makes it observable is now ported+live (see chase_camera row, NetGame.cs:3044 reads chase_active each frame), so the rig is now observable on a g_bugrigs 1 listen server — but no one has actually eyeballed it, so per SCHEMA rule 5 this remains 'unknown', not 'faithful'. This wave did NOT close it. |
| `mutator-campcheck` | `campcheck.check.vehicle` | liveness | Logic/value faithful; liveness depends on vehicles being driveable in the port (out of scope here). |
| `mutator-instagib` | `mutator-instagib.spawn.fullbright` | presentation | Flag set on the live spawn path; render effect not behaviorally verified. OnPlayerSpawn also reapplies the Vaporizer-only loadout and resets countdown bookkeeping. |
| `mutator-multijump` | `mutator-multijump.counter.networking` | presentation,liveness,timing | Unchanged this audit. Base relies on REGISTER_STAT to ship multijump_count to CSQC so client prediction of the re-jump matches the server. The port's prediction architecture (single shared physics path) may make this moot or may need an explicit sync -- unresolved. Marked unknown/low per the no-inflated-confidence rule; a runtime/prediction check is needed to resolve it. |
| `mutator-overkill` | `overkill.weapons.unblock` | liveness | OK weapon balance/behavior is a separate weapon unit; only the unblock-gate is in scope here. |
| `physics-player` | `physics-player.water.swim` | liveness | 2026-06-27 DRIFT FIX (Wave-13): logic UPGRADED partial->faithful — the viewloc (1D-rail) water sub-cases that the baseline flagged as unported are now present (PlayerPhysics.cs:760-761 wishvel.z=-160 drift; :793-801 the separate viewloc plain-addspeed accel with no QW pass), and the `viewloc drift speed -160` constant now matches (was match:false). Non-viewloc water physics already faithful incl. the QW water-jump-out launch and frozen-resurface. liveness:unknown — code reachable from the branch chain but no in-game water-map check was run; the viewloc legs are additionally inert on stock 3D maps (ViewLoc always null). |
| `physics-player` | `physics-player.ladder` | liveness | 2026-06-27 DRIFT FIX (Wave-13): logic UPGRADED partial->faithful — both baseline gaps closed. The func_water waterlevel-from-volume override (PlayerPhysics.cs:833-852) and the viewloc ladder sub-case (:828, wishvel.z = movement_old.x via player.MovementForwardOld) are now ported, so a func_water ladder fills waterlevel/watertype from the volume bounds (no longer WaterLevelNone) and a 1D-rail ladder drives the vertical wish from the previous tick's forward wishmove. Gravity-free climb via the half-step that cancels the WalkMove gravity, reachable from the branch chain (player.LadderEntity != null). liveness:unknown — code-verified, not behaviorally tested on a ladder map. |
| `physics-player` | `physics-player.jetpack` | liveness | Closed-form thrust math ported verbatim incl. the reverse-thrust crouch branch. 2026-06-25 RE-AUDIT: values UPGRADED partial->faithful — the g_jetpack_* tunables are now read live as cvars (JetpackCvar in PMJetpack + the CheckPlayerJump fuel gate), so a retuning server takes effect; the baked constants are only fallbacks and now carry the CORRECT stock values. Two baseline values defects fixed: (1) JetpackMaxSpeedSide was 1500 (the nexuiz25 balance) and is now 1200 to match balance-xonotic.cfg:248; (2) the maxspd_mod factor (player.qc:742) is now threaded through as maxspeedMod rather than hardcoded max(1,1). Liveness still unknown: reached only when the Jetpack item is held + activated; not behaviorally tested. |
| `tmayhem` | `tmayhem.teams.count` | presentation | TeamCount drives the team seed/leader; the map-entity team-color registration (Team_MapEnts_FindOrSpawn) is an open presentation question. Untouched by Wave-2. |
| `warpzones` | `warpzones.client.render` | presentation | Wave-18 portal render landed (listen-host, cl_portal_render default on). logic/timing faithful (SubViewport camera = warp(main camera), updated each frame); presentation:unknown pending the in-game eyeball (SCHEMA rule 5: unverified visible fidelity is unknown, not faithful); liveness:partial (live on a listen host, dead on a pure remote client). The crossing PREDICTION (EntityMovementStep) is separate + already live. RE-AUDIT 2026-06-28 (OPUS re-verify): code path re-read + line refs re-confirmed against current src; build+tests re-run green (32/32 warpzone); cl_portal_render is read via Api.Cvars default-on (not a registered Cvars.cs default), functional. Statuses held — no inflation: presentation stays unknown until an in-game eyeball, liveness stays partial until the zone transforms are networked to remote clients. |
| `weapon-hook` | `weapon-hook.presentation.rope_line` | presentation | 2026-06-28 re-audit: this wave CLOSED the rope-line presentation on the listen-host/demo path. HookRopeRenderer.cs + CylindricLine.cs landed and are hosted LIVE by ClientWorld (injected with the shared cross-ribbon pool, AddChild'd). The muzzle anchor mirrors the server reel origin exactly (hook_shotorigin '8 8 -12' rotated by the owner's view angles), width 8 and cl_grapplehook_alpha match Base, and the line persists after the latch (read off the live facade, not the dropped snapshot). NOT fully closed: presentation:unknown (team colour is a tint stand-in, not the particles/hook_<team> texture; single straight segment, no warpzone tracing; not runtime-verified) and liveness:partial (idles on a pure --connect client — no entity facade — exactly like LaserRenderer/PortoPreview). |
| `weapon-porto` | `weapon-porto.touch.portal_spawn` | presentation,timing | Logic faithful (since 2026-06-27): the port hands the carried+reflected right_vector to PlacePortoPortal (Porto.cs:468 passes self.AVelocity as the roll axis) and the warpzone derives angles via FixedVecToAngles2(surfaceNormal, rightVector) (Warpzone.cs:567-574) — matching Base's fixedvectoangles2(plane_normal, right_vector). PortalSpawner + PortalInId + PortalClearWithId + PortalClearAll all wired LIVE in GameWorld.Boot (437-449). 2026-06-28 (warpzone exit-angle wave): the warpzone teleport TRANSFORM that porto portals route through got real angle fixes (server now stamps the authoritative fixangle on exit + transformed angles are normalized — commits 1c62b01/11baede), but those fixes target the warpzone subsystem GENERALLY and were NOT runtime-verified against a porto-PLACED portal; porto-portal teleport correctness + the 15s lifetime remain delegated to (and audited by) the warpzone unit. values/timing/presentation stay UNKNOWN here — NOT upgraded to faithful (no in-game porto teleport observed). Re-audited 2026-06-28. |