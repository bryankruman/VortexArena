# Needs in-game check

_Generated 2026-06-26. 35 features whose presentation/liveness/timing code-reading could not confirm. Verify by running the app and observing._

| unit | feature | uncertain | what to look for |
|---|---|---|---|
| `cl-hud` | `cl-hud.engine.configure_mode` | timing | Drag/resize/keyboard/grid present; per-panel-settings-dialog + export parity not runtime-verified. |
| `cl-hud` | `cl-hud.panel.weapons` | timing | Owned-weapon grid + complainbubble + label modes + timeout fade present and fed live. Improved since baseline (Wave 8c): the per-weapon ammo clip bar now reveals the weapon_ammo art left-to-right (UV-clip) rather than distorting it into a stretched rect. |
| `cl-hud` | `cl-hud.panel.powerups` | timing | Active-powerup grid with timers; fed live on listen server. Improved since baseline (Wave 8c): the bar uses the shared faithful progressbar primitive and the expanding-flash now matches the QC expandingbox sizefactor + alpha fade. |
| `cl-hud` | `cl-hud.panel.modicons` | timing | Keepaway mode added (HUD_Mod_Keepaway: carry icon blink + status-change expand, fed live from the Keepaway status block). Driven live from gametype; icon-art fidelity per mode still varies. |
| `cl-hud` | `cl-hud.panel.pressedkeys` | low-conf | Key grid present; data feed for spectated players not confirmed. |
| `cl-hud` | `cl-hud.panel.engineinfo` | timing | FpsPanel (port extra) covers FPS; engineinfo parity is low-priority. |
| `cl-hud` | `cl-hud.panel.infomessages` | timing | Observing/press-to-join/warmup/countdown lines present and fed live. The cl_lms 'no more lives left' eliminated-player line is wired (LmsNoLives). |
| `cl-hud` | `cl-hud.panel.physics` | timing | Speedometer + topspeed + jumpspeed + accel-in-g + unit present; cvar-gated show-mode wired. The accel/speed/topspeed bars use the shared faithful progressbar primitive (accelbar art + signed baralign-3 center bar), now with the Wave 8c 3-slice rounded cap render. |
| `cl-hud` | `cl-hud.panel.itemstime` | timing | Per-item respawn timers + icons + countdown present and fed live. |
| `cl-hud` | `cl-hud.panel.quickmenu` | low-conf | Open/close + key/click nav present; entry-source fidelity unverified. |
| `cl-hud` | `cl-hud.panel.scoreboard` | timing | Improved since baseline (Wave 8c 0d649bb): the scoreboard now does the QC per-round column averaging (scores_per_round → frags/kdr/sum/dmg/score divided by rounds_played, with the right %.1f/%.2f precision), the leadlimit-and-fraglimit header math, and the SP_SKILL formatting. The spectator/observer block split + faithful 3-state respawn line were earlier fixes. Remaining: the layout cvar-pos divergence (intended) + per-row spectator ping not networked. |
| `cl-hud` | `cl-hud.panel.strafehud` | timing | Strafe bar present and fed; faithful-to-stock-strafehud detail unverified. |
| `cl-hud` | `cl-hud.panel.pickup` | timing | Recent-pickup line with icon + name + xcount fade present and fed. |
| `cl-hud` | `cl-hud.panel.minigame` | timing | All 4 Base minigame HUD panels are ported and live via the MinigameClient coordinator (board renderer + menu + help). Board/status combined into MinigameRenderer. |
| `fx-effectinfo` | `fx-effectinfo.emit.te_builtins` | low-conf | Added by adversarial pass (draft omitted a row; only the spec port-mapping table mentioned te_*). Caller coverage of these specific wrappers not exhaustively traced; weapon/combo emission largely uses Emit(Effect,...) directly. |
| `invasion` | `invasion.client.point_limit_menu` | low-conf | Newly mapped (registry coverage gap). Menu-only; the cvar g_invasion_point_limit is still honored at runtime (invasion.win.point_limit) regardless of the missing slider. Confidence low — the port menu wiring was not exhaustively traced. |
| `mapobject-teleporters-portals` | `teleporters.dest.random_selection` | timing | Unchanged. RandomSelection RNG parity vs Base's per-tick RandomSelection not behaviorally verified (timing unknown). |
| `mapobject-teleporters-portals` | `teleporters.trigger.team_claim` | liveness | Unchanged. The team claim is teamplay-guarded AND enforced by the TeleportActive team gate. Liveness still unknown (whether a use-link calls TeleportUse on a teamplay map not traced). |
| `mapobject-teleporters-portals` | `portals.teleport.transform_and_amazing` | timing | FIX (2026-06-25): two prior gaps CLOSED on the warpzone Teleport path — portal_activatetime (0.1s) owner self-skip is now modeled (Warpzone.cs:231-235, seeded at PlacePortoPortal:418) and the portal-owner kill-credit (player.pusher = portal_owner, pushltime = g_maxpushtime, istypefrag) is now set when a non-owner crosses (Warpzone.cs:263-272). Two-way LinkPair stays faithful. values upgraded missing->partial (activatetime matches). STILL OPEN: Amazing announcer (audio:missing), independent-player gating, FORCE_TDEATH not applied (warpzone runs no tdeath), and the unverified plane-shift transform. |
| `monster-mage` | `monster-mage.anim.frames` | timing | RECLASSIFIED since baseline: the port now has a concrete AnimFrame(phase, die2) override (Mage.cs:66) that returns the actual nanomage.dpm frame start indices and stamps them onto the networked Entity.Frame for client playback — so values went missing->partial (most groups correct; only melee '5'/duckjump '4' fold into shoot '2'), presentation missing->partial (real frame indices drive the renderer; the shoot-anim covers push/duckjump), liveness partial->live (AnimFrame is wired into the anim driver). die1/die2 random pick handled in monster-mage.death.pain. timing (animfixfps fps normalization) still unknown. |
| `monster-spider` | `monster-spider.respawn.cycle` | presentation | Respawn-at-death-point (MONSTER_RESPAWN_DEATHPOINT) modeled; corpse-fade visual is the client's concern. EF_RED re-apply on a respawned miniboss; not spider-specific. No regression. |
| `mutator-buffs` | `buffs.networking` | liveness,timing | Unchanged. Defer the live-send confirmation to the status_effects parity unit. |
| `mutator-bugrigs` | `mutator-bugrigs.racecar.drive_model` | presentation | Algorithm and all 6 numeric constants match Base line-for-line (MathF vs QC float math). presentation downgraded faithful->unknown: the drive model's whole purpose is the rendered rig orientation (surface-snap + body lean + smoothing writes to self.Angles), but there is NO in-game/visual verification of the integrated rig feel or rendered body fidelity, and the forced chase camera that makes it observable is itself missing (see chase_camera row). Per SCHEMA rule 5, unverified visible fidelity is 'unknown', not 'faithful'. |
| `mutator-campcheck` | `campcheck.check.vehicle` | liveness | Logic/value faithful; liveness depends on vehicles being driveable in the port (out of scope here). |
| `mutator-instagib` | `mutator-instagib.spawn.fullbright` | presentation | Flag set on the live spawn path; render effect not behaviorally verified. OnPlayerSpawn also reapplies the Vaporizer-only loadout and resets countdown bookkeeping. |
| `mutator-multijump` | `mutator-multijump.counter.networking` | presentation,liveness,timing | Unchanged this audit. Base relies on REGISTER_STAT to ship multijump_count to CSQC so client prediction of the re-jump matches the server. The port's prediction architecture (single shared physics path) may make this moot or may need an explicit sync -- unresolved. Marked unknown/low per the no-inflated-confidence rule; a runtime/prediction check is needed to resolve it. |
| `mutator-overkill` | `overkill.weapons.unblock` | liveness | OK weapon balance/behavior is a separate weapon unit; only the unblock-gate is in scope here. |
| `physics-player` | `physics-player.water.swim` | liveness | Non-viewloc water physics ported faithfully incl. the QW water-jump-out launch and frozen-resurface. logic downgraded to partial for the unported viewloc sub-cases. liveness:unknown — code reachable from the branch chain but no in-game water-map check was run. |
| `physics-player` | `physics-player.ladder` | liveness | Gravity-free climb via the half-step that cancels the WalkMove gravity. Reachable from the branch chain (player.LadderEntity != null). func_water + viewloc ladder sub-cases unported (rare map features); logic:partial. Not behaviorally tested on a ladder map this audit. |
| `physics-player` | `physics-player.jetpack` | liveness | Closed-form thrust math ported verbatim incl. the reverse-thrust crouch branch. 2026-06-25 RE-AUDIT: values UPGRADED partial->faithful — the g_jetpack_* tunables are now read live as cvars (JetpackCvar in PMJetpack + the CheckPlayerJump fuel gate), so a retuning server takes effect; the baked constants are only fallbacks and now carry the CORRECT stock values. Two baseline values defects fixed: (1) JetpackMaxSpeedSide was 1500 (the nexuiz25 balance) and is now 1200 to match balance-xonotic.cfg:248; (2) the maxspd_mod factor (player.qc:742) is now threaded through as maxspeedMod rather than hardcoded max(1,1). Liveness still unknown: reached only when the Jetpack item is held + activated; not behaviorally tested. |
| `tmayhem` | `tmayhem.teams.count` | presentation | TeamCount drives the team seed/leader; the map-entity team-color registration (Team_MapEnts_FindOrSpawn) is an open presentation question. Untouched by Wave-2. |
| `weapon-porto` | `weapon-porto.touch.portal_spawn` | presentation,timing | PortalSpawner is wired LIVE in GameWorld.Boot (line 421), so placement reaches the warpzone manager on the real match path. PortalClearWithId is ALSO wired (GameWorld.cs:425-426 -> Warpzones.ClearPortoPortal). Whether the realised warpzone teleports correctly + expires at 15s is unverified here (warpzone unit owns it). Re-audited 2026-06-25: wiring intact at GameWorld.cs:421/425/428; Warpzone.PlacePortoPortal pairs in/out by (owner,id) and LinkPair-s them; angle derived from FixedVecToAngles(surfaceNormal) only (no right_vector roll handed over). |
| `weapon-vaporizer` | `weapon-vaporizer.rm.primary_explosion` | liveness | FIX vs baseline (presentation/audio missing -> faithful): RocketMinstaExplosion now emits the Devastator explosion presentation at the rail endpoint (ROCKET_EXPLODE particles + rocket_impact sound), matching how the QC blast — tagged WEP_DEVASTATOR.m_id — routes through the Devastator's CSQC wr_impacteffect. logic/values unchanged (cvars 70/38/140/400, devastator death-tag + HITTYPE_SPLASH, sky/NOIMPACT guard all correct). STILL OPEN: g_rm live-path reachability untraced. |
| `weapon-vaporizer` | `weapon-vaporizer.rm.laser_barrage` | liveness | FIXES vs baseline: (1) rapid ladder NOW ported — WrThink fires the mode-0 fan on first press (arming hagar_load + jump_interval2=rapid_delay) then mode-1 rapid bolts on rapid_refire (logic+timing partial -> faithful). (2) ELECTROBITCH airshot announce NOW ported in RocketMinstaLaserExplode (mirrors Base: the timeout path passes NULL directhit so the guard never fires there, exactly like Base; touch path damages with no guard). (3) shooter-velocity: Base forces forceAbsolute=true which bypasses Newtonian owner-velocity inheritance, leaving only g_weaponspeedfactor — which the port DOES apply via WeaponSpeedFactor(), so this is now a faithful match, not a gap. STILL OPEN: the distinct CSQC PROJECTILE_ROCKETMINSTA_LASER bolt visual (no client descriptor found despite the netname tag) and rapid_animtime. |
| `weapon-vaporizer` | `weapon-vaporizer.messages.kill_suicide` | liveness | Unchanged. The Vaporizer wr_killmessage/wr_suicidemessage ARE ported, centralized in DeathMessages.cs (string lookup keyed by weapon NetName). Liveness still unknown: the kill-feed call site was not traced end-to-end in this audit. |