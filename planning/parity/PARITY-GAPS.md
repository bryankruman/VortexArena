# Parity Gaps — ranked by gameplay impact

_Generated 2026-06-26. 629 features with gaps, highest-impact first._

Score weights logic & liveness highest, then values/timing, then presentation/audio, scaled by category (core gameplay > niche entities). Intended divergences excluded.

| # | score | unit | feature | worst | gaps |
|---|---|---|---|---|---|
| 1 | 21.0 | `nexball` | `nexball.weapon.ballstealer` | logic:missing | WEP_NEXBALL / BallStealer weapon does not exist; there is no way to SHOOT or pass the carried ball.; No primary launch, no secondary tackle projectile (W_Nexball_Touch ball-steal), no homing safe-pass (W_Nexball_Think).; No weapon-arena handler, so Nexball is not enforced as a weapon-arena mode (carriers keep their normal weapons). |
| 2 | 21.0 | `onslaught` | `onslaught.captureshield` | logic:missing | No physical capture-shield entity around shielded generators/control points: an enemy player walks into a shielded node with no push-back, no GENERATOR/CONTROLPOINT_SHIELDED center notification on contact, and no spinning additive shield model. (The damage-ignore IS enforced via node.Shielded, and the weapon-damage blocked-by-shield SOUND hint IS played in IconDamage/GeneratorDamage; only the touch-push shield ENTITY + its center-print are missing.) |
| 3 | 21.0 | `onslaught` | `onslaught.spawn.teleport_choose` | logic:missing | No control-point-to-control-point teleport (use key / click-radar), no respawn-at-chosen-CP, no spawn_at_controlpoints / spawn_at_generator placement near owned nodes. Players spawn only via the generic team spawn system. The ons_spawn client command + clickradar HUD are absent. (spawn_choose defaults ON in Base.) ONS_TELEPORT/ONS_TELEPORT_ANTISPAM notifications registered as data but never emitted. |
| 4 | 21.0 | `onslaught` | `onslaught.presentation.csqc` | logic:missing | No generator/control-point/icon world models on the client; no progressive damage-model swaps; no icon bob/spin/punch-on-hit animation; no generator damage `frame`; no radar link lines; no objective death-cam (ENT_ONSCAMERA/WantEventchase/ons_camSetup) on round loss; no generator explosion sequence (rays/shockwave/gibs/electricity); no particle effects (EFFECT_SPARKS/CAP/RAGE/ROCKET_EXPLODE) or waypoint-sprite build/health bars. |
| 5 | 19.5 | `clanarena` | `clanarena.join.late_join_observer` | logic:missing | While !allowed_to_spawn, QC forces a joining player to Observer (TRANSMUTE), sets INGAME_STATUS_JOINING, and sends INFO_CA_JOIN_LATE so they play from the next round. The port still has no INGAME_JOINING state and no allowed_to_spawn spawn gate, so mid-round joiners are not handled the QC way. (The CA_JOIN_LATE notification IS registered, NotificationsList.cs:699, but never sent — no live caller.); QC Bot_FixCount counts in-game-joined clients (so bots fill mid-round) -- not ported. |
| 6 | 19.5 | `cts` | `cts.checkpoints.intermediate` | logic:missing | target_checkpoint and trigger_race_checkpoint are consumed as no-ops for CTS (GameWorld:2093-2094 documents them as Race-only); defrag-style maps lose ordered checkpoint enforcement.; No 'went backwards' detection, no spawnflag-4 backward-damage, no per-checkpoint split times/records, no .defragcp order file. Wave-5 added per-checkpoint split records to the Race port (Race.cs RecordCheckpointSplit / _checkpointRecords), but Cts.cs got no equivalent — CTS remains a single start->stop course with no intermediate checkpoints. |
| 7 | 19.5 | `nexball` | `nexball.hud.modicon` | logic:missing | ModIconsPanel has no nexball drawer: no 'carrying' icon and no charge-meter progress bar.; No event-chase (third-person) cam when not carrying; accuracy/item-stats scoreboard panels not hidden for nexball. |
| 8 | 19.5 | `physics-movetypes` | `physics-movetypes.matchticrate.interp` | logic:missing | STILL OPEN: No Movetype_Physics_MatchTicrate equivalent and no tic_* snapshot fields on Entity (grep: zero hits in src/ for MatchTicrate/tic_origin/tic_velocity). Entities advance only on the fixed server tick, with no between-tick interpolation in this layer. In Base this smooths CSQC gibs/casings/projectiles/trains at render rate; whether visible in the port depends on its separate render-side interpolation (PredictionBuffer/ClientNet — out of this unit's scope). |
| 9 | 19.5 | `survival` | `survival.join.late_join_observe` | logic:missing | Survival's ForbidSpawn / PutClientInServer mid-round force-observe is NOT wired. Base forces a player who joins while a round is live (!allowed_to_spawn) into Observer state (TRANSMUTE Observer), marks them INGAME_STATUS_JOINING, and sends INFO_CA_JOIN_LATE so they sit out until the next round. The port has a generic ClientManager.GametypeJoinGate + GametypeOnJoin mechanism, but it is installed only for the LMS arm (GameWorld.cs:1147) — Survival's ActivateGameType arm (GameWorld.cs:2393-2437) never sets it. So a player who joins mid-round in the port is not forced to spectate by the gametype; round integrity for mid-round joiners is unverified. (Related: the registration row already notes a late joiner is not marked eliminated until the next AssignRoles.) |
| 10 | 19.5 | `weapon-hook` | `weapon-hook.presentation.rope_line` | logic:missing | No hook ROPE line is drawn. There is no GrapplingHookSend/ENT_CLIENT_HOOK stream (with hook_start/hook_end), no Draw_CylindricLine, and no team-coloured rope texture/WarpZone-traced segment in game/. The chain HEAD is networked + rendered as a Hookbomb spike while in flight (and disappears once latched), but the connecting rope between the firer's gun and the latch point is entirely absent. |
| 11 | 19.5 | `weapon-porto` | `weapon-porto.presentation.trajectory_preview` | logic:missing | While holding porto (non-secondary mode), Base draws a reflecting red/blue cylindric-line polyline (with the same slick-continue / noimpact-stop / CheckWireframeBox size checks the placement uses) predicting where the in/out portals will land. The port STILL renders nothing — the signature porto view feature is absent. |
| 12 | 17.25 | `freezetag` | `freezetag.presentation.ice_model` | logic:missing | No distinct ice block entity (random 1-of-20 frame, team-colored, following the player at origin-16) — frozen players only get an icy-blue colormod on their normal model. |
| 13 | 17.25 | `nexball` | `nexball.weapon.power_meter` | logic:missing | No NB_METERSTART charge state; launch power can't be modulated (no minpower..maxpower triangle wave) because there is no launch weapon.; No HUD power-meter progress bar. |
| 14 | 16.8 | `sv-client-lifecycle` | `sv-client-lifecycle.frame.idle_kick` | logic:missing | Idle players are never kicked or demoted to spectator; the idle countdown bangs (SND TALK2 at countdown_time), CENTER_DISCONNECT_IDLING / CENTER_MOVETOSPEC_IDLING centerprints, and INFO_QUIT_KICK_IDLING / INFO_MOVETOSPEC_IDLING notifications never appear.; The alivetime parm_idlesince afk-gate (pause alivetime accrual after 30s idle) is not modeled, so xonstat skill-per-second would be skewed by afk time. |
| 15 | 16.5 | `freezetag` | `freezetag.bots` | values:missing | Bots have no FT-specific freeing/offense roles — they won't deliberately route to and revive frozen teammates (havocbot_role_ft_freeing/offense + havocbot_goalrating_ft_freeplayers absent).; The frozen-target ATTACK gate (Bot_ForbidAttack) IS effectively covered: BotBrain.ShouldAttack (BotBrain.cs:793) skips a target carrying the shared Frozen status effect, so bots don't waste fire on frozen enemies. The dedicated FT Bot_ForbidAttack hook is not registered, but the generic frozen-skip yields the same outcome. |
| 16 | 16.5 | `mutator-nades` | `mutator-nades.presentation.client` | logic:missing | No nade projectile model/trail (nade_customize / nade_spawn CSQCProjectile + Nade_TrailEffect), no orb 3D model (orb_draw), no orb 2D full-screen color flash (orb_draw2d / hud_colorflash_alpha), no darkness blind overlay (HUD_DarkBlinking) + SND_BLIND, no spawn-loc EF_STARDUST render.; No DrawAmmoNades (cl_nades.qc:86): the ammo-panel bonus-nade count + progress bar + (rainbow) type icon is not drawn.; The held-nade charge ring (CrosshairPanel.NadeTimer) is wired + networked AND now functional: a nade can finally be primed (input wired in Wave-5), so NadeTimer climbs 0->1 and the ring appears during a charge. |
| 17 | 16.5 | `nexball` | `nexball.weapon_arena.player_setup` | logic:missing | PlayerSpawn does not zero per-slot weapons + grant WEPSET(NEXBALL) for basketball mode (STAT(NB_METERSTART)=0; STAT(WEAPONS) handling).; PlayerPreThink's non-carrier branch (re-grant the held nexball weapon / restore normal weapons) is not ported.; WantWeapon (force weapon 0) and SpectateCopy (copy NB_METERSTART to the spectator) hooks absent. |
| 18 | 16.5 | `race` | `race.movement.quantization` | logic:missing | Analog (joystick) move input is not snapped to keyboard octants -> an analog precision advantage Base removes for fairness.; race_movetime per-frame fractional accumulator absent (lap timing uses wall clock instead).; cl_movecliptokeyboard 2 is not stuffed to clients. |
| 19 | 16.5 | `weapon-arc` | `weapon-arc.bot_aim` | logic:missing | Arc's weapon-specific wr_aim (bot_aim with beam_botaimspeed/beam_botaimlifetime, or the 1000000/0.001 fallback for the continuous beam) is not ported in Arc.cs. Bot aiming is handled by the generic port bot-aim system, not this per-weapon hook — likely a coverage gap rather than a true behavioral defect, but the Arc-specific continuous-beam aim tuning (long lifetime, instant) is not modeled per-weapon. |
| 20 | 16.5 | `weapon-porto` | `weapon-porto.bot.aim` | logic:missing | Base wr_aim clears ATCK/ATCK2 then, in non-secondary mode, calls bot_aim(speed, lifetime) and presses ATCK if a target is found — this is how a bot decides to fire the porto. The port has NO WrAim override on Porto.cs and no bot porto-aim path, so bots never fire the porto via the QC aim heuristic. Newly mapped 2026-06-25 (was absent from the registry). |
| 21 | 16.2 | `sv-mapvoting` | `sv-mapvoting.vote_scoreboard_hide` | logic:missing | The scoreboard-hide EFFECT is now achieved a DIFFERENT way: UpdateScoreboard (NetGame.cs:3768-3772) models Base's intermission==2 by hiding the scoreboard whenever the MapVote panel is Visible (mapVoteShowing → intermissionShow = MatchIntermission && !mapVoteShowing), so the player does not see the scoreboard during the vote (the visible outcome of Base's SVC_FINALE). Presentation goal met.; But Base's actual MECHANISM is absent: it forces RES_HEALTH=2342 on every client (incl. bots) and sends SVC_FINALE "" once. The port models neither (the only 2342 in src/ is the unrelated 'observing' %h reader in Chat.cs:615). Logic/values/timing therefore stay missing.; Base also zeroes CS(it).impulse each tick after reading it; the port reads the impulse synchronously in DispatchImpulse (no stored per-tick impulse field), so this specific clear is moot. |
| 22 | 15.6 | `sv-cheats` | `sv-cheats.impulse.clone` | logic:missing | No CopyBody / corpse-clone mechanic; the clone impulses do nothing and sv_clones is unread. Unchanged by Wave-5. |
| 23 | 15.6 | `sv-cheats` | `sv-cheats.frame.drag` | logic:missing | No CheatFrame per-frame hook and no Drag subsystem: cannot pick up/carry/throw entities (cheat or normal-range), no te_lightning1 drag beam. Unchanged by Wave-5.; No dragbox_spawn/dragpoint_spawn/drag_remove/drag_setcnt/drag_save/drag_saveraceent/drag_clear race-checkpoint map-editor commands.; g_grab_range cvar exists (Cvars.cs:372) but is never read (no consumer). |
| 24 | 15.6 | `sv-client-lifecycle` | `sv-client-lifecycle.frame.spectator_kick` | logic:missing | When sv_spectate is disabled, QC warns then drops a client that stays a spectator/observer past g_maxplayers_spectator_blocktime (SPECTATE_WARNING MSG_MULTI then INFO_QUIT_KICK_SPECTATING + dropclient_schedule). The port reads sv_spectate only to gate the autojoin hold; it never warns or kicks a stuck spectator. |
| 25 | 15.6 | `sv-client-lifecycle` | `sv-client-lifecycle.frame.name_and_version` | logic:missing | Outdated/old-client/beta version-nag INFO notifications (INFO_VERSION_OUTDATED/OLD/BETA via vercmp + version_nagtime) never sent.; Names are not truncated to sv_name_maxlength, invisible names not replaced with Player#<id>, the :name: event-log line and 'name was truncated/invisible' sprints are absent.; GOD MODE info (INFO_GODMODE_OFF when FL_GODMODE drops) not modeled. |
| 26 | 15.6 | `sv-clientkill` | `sv-clientkill.indicator.entity` | logic:missing | No floating numeric countdown model is attached above the player; nothing is networked for it (KillCountdown drives only the announcer + center-print, explicitly NOT the digit entity — KillCountdown.cs:12-14).; Clones on g_clones ghosts (the IL_EACH(g_clones) killindicator copy on each non-respawnghost clone) are also absent.; The digit-entity colormod tint (black / grey / Team_ColorRGB) is unmodeled. |
| 27 | 15.6 | `sv-world-rules` | `sv-world-rules.entity.randomseed` | logic:missing | No shared server->client RNG seed: client-side random effects cannot be kept deterministic with the server. |
| 28 | 15.6 | `sv-world-rules` | `sv-world-rules.entity.pingplreport` | logic:missing | No server->client ping / packet-loss / movement-loss report; scoreboard ping column and playerstats latency have no feed. |
| 29 | 15.0 | `tka` | `tka.score.preferplayerscore` | logic:missing | Base TKA subscribes PreferPlayerScore_Clear and returns true: when g_score_resetonjoin == -1, a player who specs out and rejoins KEEPS their score (the hook vetoes the conditional wipe). The port has a generic seam — GameScores.ClearPlayerOnJoin takes a preferClear delegate that maps to MUTATOR_CALLHOOK(PreferPlayerScore_Clear) — but TKA never supplies it, so the veto is absent.; Moot on stock servers: the default g_score_resetonjoin is 0, which short-circuits ClearPlayerOnJoin before the hook is consulted (and ClearPlayerOnJoin itself currently has NO live caller anywhere — the rejoin-clear path is dead). So there is no stock-server divergence; the gap only manifests under the admin -1 config. |
| 30 | 15.0 | `weapon-machinegun` | `machinegun.reload.checkammo` | logic:missing | wr_reload (the forced-reload branch at the top of wr_think, plus the manual W_Reload that respects misc_bulletcounter<0 to let a burst finish) is NOT modeled. Comment in WrThink (Machinegun.cs:141) explicitly notes 'reload_ammo isn't modeled, so ammo == the resource'. reload_ammo defaults 0 in bal-wep-xonotic.cfg so the path is dead in stock play; a server setting reload_ammo>0 would diverge (no clip, no reload cycle).; wr_checkammo2 is not ported as a separate gate: it returns false (suppresses the burst as a fire mode) when ATTACK2 is a zoom, and computes burst_ammo_per_shot. The port instead relies on ZoomOnSecondary in WrThink to suppress the burst, which is behaviorally equivalent for the zoom case at default balance, but the per-shot burst_ammo_per_shot ammo gate is absent. |
| 31 | 14.4 | `sv-client-lifecycle` | `sv-client-lifecycle.connect.fix_client_cvars_welcome` | values:missing | Only fragments of the welcome channel exist: the campaign welcome level-num (SendCampaignWelcome) and the grappling-hook gameplay tip (BuildGameplayTipsString) are surfaced to the connecting client. Still unmodeled: FixClientCvars (cl_jumpspeedcap_min/max, cl_shootfromfixedorigin, prediction/gentle), clientstuff, cmd clientversion, set _teams_available, the sv_notice_join/SendWelcomeMessage MOTD/hostname/map/mutators body, Send_NextMap_To_Player, and the ClientInit_misc per-client init entity (armor-blockpercent, damagepush speedfactor, fog, serverflags, g_trueaim_minrange, hook/arc shot origins).; The port pushes a few physics-relevant cvars via per-client replicated-cvar tables; there is no MOTD, no version request, and no per-client init-entity carrying the server's damage/fog/trueaim constants. |
| 32 | 14.3 | `net-entity-state` | `net-entity-state.wepent.switch_alpha_misc` | logic:missing | Remote weapon switch animation coarse: only the resolved active weapon is networked, not wepent m_switchweapon/m_switchingweapon — a remote player's weapon raise/lower transition is not reproduced.; wepent m_alpha (weapon fade), m_gunalign, porto_v_angle_held (Porto held-angle viewmodel), tuba_instrument, viewmodel skin are not networked. |
| 33 | 14.0 | `scoring` | `scoring.display.scorelog` | logic:missing | The server score-log / event-log standings dump (DumpStats(final), server/world.qc:1264) — which emits the `:labels:player:`, `:player:see-labels:<scores>:<time>:<team>`, and (teamplay) `:teamscores:` lines built by GetPlayerScoreString/GetTeamScoreString/GetScoreLogLabel to the console / sv_eventlog / sv_logscores_filename — is NOT ported (grep DumpStats/logscores/GetPlayerScoreString over src -> no match). Base calls it at intermission (world.qc:1429, final=true) and via a server command (sv_cmd.qc:1249). Distinct from the on-demand `teamstatus` console dump (scoring.display.niceprint, now ported), the XonStat report (scoring.report.playerstats), and the networked scoreboard (scoring.net.replication). Low impact: a server-admin logging/eventlog feature, no client-visible effect. |
| 34 | 13.2 | `sv-cheats` | `sv-cheats.impulse.speedrun` | logic:missing | No personal_wp snapshot/restore (origin/velocity/resources/weapons/items/statuseffects/pause-timers), no AbortSpeedrun hook, no speedrunning flag handling. Unchanged by Wave-5. |
| 35 | 13.2 | `sv-cheats` | `sv-cheats.impulse.teleport` | logic:missing | No emergency player-teleport cheat: neither the info_autoscreenshot path nor a player-side MoveToRandomMapLocation exists. (Note: a BallEntity.Relocate / MoveToRandomMapLocation exists for the Keepaway/Nexball/Invasion BALL, but that is the gametype ball-respawn mechanic, NOT the player-teleport cheat — different consumer, not reusable here.) Unchanged by Wave-5. |
| 36 | 13.2 | `sv-world-rules` | `sv-world-rules.misc.redirection` | logic:missing | No server-redirect / mass-reconnect command (sv_redirect_target / quit_and_redirect). |
| 37 | 13.2 | `sv-world-rules` | `sv-world-rules.eventlog.dumpstats` | logic:missing | DumpStats(final) emits a :scores:<gametype>_<map>:<time> (final, at NextLevel) or :status:... (periodic, via the printstats/status console cmd) block — a per-player score/ping/team/handicap line dump — to console (sv_logscores_console), the eventlog (sv_eventlog), and a log file (sv_logscores_file). The port has no analogue: no final scores block is written to the eventlog at match end, and there is no printstats/status server command. |
| 38 | 13.0 | `mutator-buffs` | `buffs.carrier_model` | logic:missing | A player holding a buff still has no colored MDL_BUFF glow model attached: no buffs_BuffModel_Spawn/Update/Think/Customize, no removal on observer/disconnect. EF_NOSHADOW IS set on the player while held (ApplyBuff:461), but the glow model itself is absent. |
| 39 | 13.0 | `mutator-buffs` | `buffs.buff_effect` | logic:missing | No per-carrier Send_Effect particle emission throttled by buff_effect_delay; g_buffs_effects (default ON) is never read. EntityMutatorState declares a buff_effect_delay field but nothing emits the effect. |
| 40 | 13.0 | `mutator-buffs` | `buffs.waypoint` | logic:missing | No WP_Buff radar/HUD waypoint on buff items (no buff_Waypoint_Spawn, no RADARICON_Buff, no build-finished timer); the colored buff icon never appears on radar/screen edge, and there is no per-viewer hide-if-already-held logic. |
| 41 | 13.0 | `mutator-itemstime` | `mutator-itemstime.waypoints.spectator_sprites` | logic:missing | No waypointsprite_itemstime / SPRITERULE_SPECTATOR support in the port: timed (SpectatorOnly) items (mega health/armor) get no spectator/warmup respawn-countdown waypoint sprite (Item_RespawnCountdown sets SPRITERULE_SPECTATOR + WaypointSprite_UpdateBuildFinished at time+ITEM_RESPAWN_TICKS), and the g_waypointsprite_itemstime cvar (default 2) is unregistered. STILL OPEN (grep waypointsprite_itemstime / SPRITERULE_SPECTATOR / g_waypointsprite_itemstime across src + game -> no matches, 2026-06-25 drift check). |
| 42 | 13.0 | `mutator-overkill` | `overkill.loot.drop_monster` | logic:missing | Monsters drop no Overkill loot and their normal drop is not suppressed. No MonsterDropItem hook chain exists in the port (grep MonsterDropItem.Call → zero hits), so there is nothing to subscribe. UNCHANGED by Wave-2. |
| 43 | 13.0 | `mutator-overkill` | `overkill.itemwaypoints.respawn` | logic:missing | No respawn-timer waypoints are shown for HealthMega/ArmorMedium/ArmorBig/ArmorMega under Overkill. No Item_RespawnCountdown / Item_ScheduleRespawn mutator hook chains exist in the port. UNCHANGED by Wave-2. |
| 44 | 13.0 | `mutator-sandbox` | `mutator-sandbox.drag.grab` | logic:missing | STILL MISSING: no Drag/Drag_Begin/Drag_Finish system. ObjectThink computes .Grab (e.Grab, written at SandboxMutator.cs:343-347) but nothing consumes it; +button8 does nothing; objects cannot be physically carried. (grep: no Drag_Begin/Drag_Finish/button8 grab consumer anywhere; e.Grab is written but never read.) |
| 45 | 12.75 | `weapon-tuba` | `weapon-tuba.audio.note_sound` | timing:missing | No pitch-step crossfade / sound7 speed shift: Base rounds note to the nearest multiple-of-step sample AND crossfades to its neighbour while pitch-shifting the remainder m via sound7; the port now correctly snaps to the nearest recorded sample (PitchStepNote) so a real file always plays, but it plays that one sample flat — the residual pitch is not shifted.; No sustained note: the port fires one static one-shot per blast tick (~0.05s) rather than a held loop that fades on release (cl_tuba_fadetime).; No client volume/attenuation cvars, no per-listener distance attenuation, no ENT_CLIENT_TUBANOTE networking (remote players' tuba notes are not driven by a dedicated note entity). |
| 46 | 12.6 | `mapobject-teleporters-portals` | `portals.entity.lifecycle` | logic:missing | Porto portals are realised as a seamless two-way warpzone, so there is no portal health (g_balance_portal_health=200) — Base portals can be shot down (Portal_Damage).; No 15s portal lifetime (g_balance_portal_lifetime) / fade-out (SUB_SetFade time,0.5) / recharge-to-200+reset-fade on each use; port warpzone portals persist for the whole match (death/reset teardown is wired, but no time-based expiry).; No portal model (MDL_PORTAL) / skins (0/1/2) or EF_RED / EF_BLUE\|EF_STARDUST / EF_ADDITIVE glow; a warpzone is a transparent window, not a Quake portal disc.; SND_PORTO_EXPLODE / SND_PORTO_EXPIRE (CH_SHOTS) and EFFECT_ROCKET_EXPLODE on destruction/expiry never play (Portal_Remove).; Portal_Customize visibility gating (hiding the portal model from players who can't use it, IS_INDEPENDENT_PLAYER) is absent.; Portal_FindSafeOrigin (move_out_of_solid 16u in front, portal_safe_origin) and CheckWireframeBox 96^3 clearance are not modeled. |
| 47 | 12.0 | `cts` | `cts.lifecycle.prepare_player` | logic:missing | Cts registers a PlayerSpawn hook that now forces the Shotgun loadout AND resets RunStartTime=0 (Cts.cs:506-519, the race_PreparePlayer run-clock reset) — but it still does NOT do the rest of the race_PreparePlayer bookkeeping (checkpoint reset to -1, race_SendAll, respawn-checkpoint/spotref recording, the spawn==qualifying re-prepare-vs-retract branch). No ClientConnect/PutClientInServer/MakePlayerObserver/AbortSpeedrun hooks.; A spectating player who holds a ranked time is not flagged FRAGS_PLAYER_OUT_OF_GAME (vs FRAGS_SPECTATOR), and g_allow_checkpoints practice-mode re-prepare on AbortSpeedrun is absent. |
| 48 | 12.0 | `cts` | `cts.bot.role` | logic:missing | No CTS bot role: bots are not routed to their next-expected race-checkpoint waypoint (with the raw-touch redirect for unreachable checkpoints), so bots do not actually run the CTS course. |
| 49 | 12.0 | `domination` | `domination.capture.eventlog` | logic:missing | dom_EventLog (the `:dom:taken:<team_before>:<playerid>` GameLogEcho on capture, gated by sv_eventlog) is not ported — no GameLogEcho call in CapturePoint/dompoint_captured. Telemetry-only (server gamelog parsers / stats), no in-game effect. |
| 50 | 12.0 | `duel` | `duel.mapsupport.gating` | logic:missing | STILL OPEN. No port logic gates duel auto-support by map size (diameter<3250) or force-lists duel on DM maps. grep 3250 / m_isAlwaysSupported / m_isForcedSupported / g_duel_not_dm -> only doc-comments (Duel.cs:37); no implementation. Map-pool/menu concern: a too-large map could be offered for duel, or a DM-only map might not list duel. |
| 51 | 12.0 | `overkill-weapons` | `overkill-weapons.okhmg.nadesupport` | logic:missing | STILL OPEN: throwing a nade while holding the OK HMG deals full nade self-damage instead of 10%. NadeProjectile.cs:186 explicitly defers the Nade_Damage mutator hook; no okhmg branch was added by Wave-8b (it only touched FX). |
| 52 | 12.0 | `race` | `race.qualifying.mode` | logic:partial | Retract uses Clients.Spawn (a normal spawn), not the Base checkpoint/grid-aware respawn (race_RetractPlayer + spawn_evalfunc by race_place).; independent_players / MAKE_INDEPENDENT_PLAYER (solo scoring isolation) not modeled.; Qualifying checkpoint records + RACE_NET_CHECKPOINT_*_QUALIFYING packets absent. |
| 53 | 12.0 | `race` | `race.bot.role` | logic:missing | Bots have no race role; they do not navigate toward the next checkpoint, so a bot-filled race has idle/wandering bots. |
| 54 | 12.0 | `tdm` | `tdm.rules.map_support` | logic:missing | The mapinfo/map-pool support gating that decides which maps auto-offer TDM is not ported: m_isAlwaysSupported (auto-offer on maps with >=8 spawnpoints AND diameter>3250) and m_isForcedSupported (g_tdm_on_dm_maps makes DM maps support TDM too) have no port equivalent. The g_tdm_on_dm_maps cvar ships (default 0) but is read by nothing. Map-pool / menu concern; the port plays whatever map it is told to. Consistently deferred across gametypes (see Duel/Mayhem notes). NOT addressed by Wave-2. |
| 55 | 12.0 | `tdm` | `tdm.rules.map_team_config` | logic:missing | Custom per-map team declarations are not ported: spawnfunc_tdm_team (map-defined team netname + scoreboard color cnt, team=cnt+1) is ignored, and the mapinfo `teams=` override path (m_parse_mapinfo / m_setTeams writing g_tdm_teams) is not implemented. The port reads team COUNT only from g_tdm_teams / g_tdm_teams_override cvars and always uses the default team set. Maps that rely on tdm_team for non-default team names/colors render with the stock teams. NOT addressed by Wave-2. |
| 56 | 12.0 | `weapon-arc` | `weapon-arc.beam.visual` | values:missing | The beam does NOT route through BeamRenderer.Beam. arc_beam is registered isTrail:true (EffectsList.cs:51), classified as Trail first, so the EffectClass.Beam route is never reached; the arc_beam effectinfo block is `type spark` trailspacing 10 with no `orientation beam` line. Renders as a SPARK PARTICLE TRAIL swept origin->endpos, not a cylindric beam. UNCHANGED.; No dedicated networked beam entity (ENT_CLIENT_ARC_BEAM): the beam is re-emitted as an arc_beam trail each SERVER tick and drawn STRAIGHT, not the curving bezier sweep. UNCHANGED.; Missing spinning MDL_ARC_MUZZLEFLASH flash entity, per-beam_type thickness (8/14), beam color, hit/muzzle dynamic lights (ARC_BT_HIT hitlight 20 / muzzlelight 50), and team-color tint. The heal-beam variant IS now selected (ARC_BEAM_HEAL emitted on the heal branch, Arc.cs:456). UNCHANGED otherwise. |
| 57 | 12.0 | `weapon-crylink` | `crylink.bot.wr_aim` | logic:missing | No Crylink WrAim override — the QC METHOD(Crylink, wr_aim) (10% chance primary via bot_aim(speed,0,middle_lifetime), else secondary) is not ported. Bots cannot select/lead a crylink shot through the per-weapon bot-aim path. Other weapons (Vaporizer/Shotgun/Minelayer/Rifle/Fireball/Devastator/Machinegun) DO have WrAim, so a generic fallback does not cover crylink. Same un-ported-bot-aim shape as several other weapons. |
| 58 | 12.0 | `weapon-electro` | `electro.bot_aim` | logic:missing | No Electro-specific bot aim is ported. Base wr_aim leads with bot_aim at primary_speed (or hitscan-ish when speed==0) and, with a small random chance (bot_secondary_electromooth), switches to a secondary mortar-style lob using secondary_speed/speed_up; switches back at >1000u range. The port BotBrain.cs has no electro branch. Bots can still fire generically but without the primary/secondary mix or the lead-time tuning. |
| 59 | 12.0 | `weapon-electro` | `electro.secondary.orb_damagedbycontents` | logic:missing | g_balance_electro_secondary_damagedbycontents defaults 1 (ON), so in Base a live orb resting in lava/slime or fully submerged takes content damage and detonates. The port never pushes the orb onto g_damagedbycontents (no DamagedByContents wiring for projectiles at all — only monsters/turrets/instagib reference the field), so an orb is immune to environmental hazards. The only on-by-default electro secondary feature that is unported. |
| 60 | 12.0 | `weapon-porto` | `weapon-porto.bot.dodge_projectile` | logic:missing | Base flags the launched porto projectile as bot-dodgeable (bot_dodge=true, bot_dodgerating=200) and pushes it onto g_bot_dodge so bots steer away from it. The port's Attack (Porto.cs:162-198) sets no bot_dodge/bot_dodgerating and there is no g_bot_dodge list registration for the porto gren, so bots do not avoid it. Newly mapped 2026-06-25. |
| 61 | 11.7 | `mapobject-misc` | `mapobject-misc.bgmscript.adsr` | logic:missing | No bgmscript ADSR envelope: a func_clientwall / func_pointparticles carrying a .bgmscript key does not animate its alpha / origin / angles / emission to the map's .bgs music script — the prop is static instead of pulsing to the soundtrack. doBGMScript / BGMScript_InitEntity / the .bgs loader are absent. |
| 62 | 11.7 | `warpzones` | `warpzones.client.render` | logic:missing | No Godot SubViewport+Camera3D renders the linked OUT side onto the portal surface; HeroMaterials.BuildPortal (HeroMaterials.cs:168) is a static dark near-black StandardMaterial3D mirror. The warpzone reads as an opaque/wavy wall — you cannot see through it. PVS-fade and the r_water enable HACK are also absent. |
| 63 | 11.7 | `warpzones` | `warpzones.client.fixview` | logic:missing | No client view rotate when the camera straddles a zone, no near-clip push-out (geometry clips at the seam), no roll-kill on a roll-changing crossing, no exterior-model hide. Crossing a warpzone is visually jarring. |
| 64 | 11.7 | `warpzones` | `warpzones.camera` | logic:missing | No func_camera spawnfunc; security-camera dpcamera surfaces render as a dark mirror placeholder (HeroMaterials) rather than a fixed camera view of the target position. |
| 65 | 11.25 | `weapon-porto` | `weapon-porto.fire.aim_hold` | liveness:dead | The hold is set on a Secondary tick and cleared when the held-angle shot is consumed (Porto.cs:117-132), but the QC release condition `if (!(fire & 2)) porto_v_angle_held = 0` cannot be reproduced from a Primary-only tick — the port can't observe an 'ATCK2 released' edge — so the hold is never released until a shot consumes it, which diverges from Base when the player taps then releases secondary without firing primary.; Only reachable when g_balance_porto_secondary=0 (non-default); dormant in stock play. |
| 66 | 10.5 | `clanarena` | `clanarena.spectate.force_spectate_cmd` | logic:missing | Base intercepts the `spectate` client command for an INGAME player: it force-spectates (MUT_SPECCMD_FORCE) and, when sv_spectate is on and they're already spec/observer, sends INFO_CA_LEAVE ('left the game'). The CA_LEAVE notification IS registered (NotificationsList.cs:700) but has no caller; the port has no CA ClientCommand_Spectate hook, so leaving CA mid-game isn't handled the QC way. Tied to the missing INGAME state. |
| 67 | 10.5 | `invasion` | `invasion.client.point_limit_menu` | logic:missing | Base exposes a 'Point limit:' slider (50..500, step 10) in the create-game gametype menu via m_configuremenu. The port's create-game menu has no Invasion-specific point-limit slider row. |
| 68 | 10.5 | `weapon-electro` | `electro.describe_guide` | logic:missing | The MENUQC weapon-guide describe paragraphs (electro.qc:753-767 — primary/secondary/combo description + DPS guide line) are not ported; the port has no in-menu weapon guide for electro. |
| 69 | 10.5 | `weapon-vortex` | `weapon-vortex.fx.charge_glow` | logic:missing | QC's wr_glow brightens the wielder's player model proportionally to vortex_charge (via vortex_glowcolor, team-tinted, f scaled by charge_animlimit). The port has no wr_glow override and emits no charge-driven player-model glow — the carrier looks identical regardless of charge. Also blocked by charge not being networked to clients. |
| 70 | 10.4 | `cl-scoreboard` | `cl-scoreboard.block.itemstats` | logic:missing | No item-stats block at all: Base shows a per-item icon grid of pickup counts (g_inventory.inv_items), filtered by itemstats_filter_mask=12. The port has no SetItemStats / DrawItemStats and inventory is not networked for it. |
| 71 | 10.4 | `cl-scoreboard` | `cl-scoreboard.ui.interactive` | logic:missing | No interactive UI: the port scoreboard is a passive overlay. Missing TAB panel cycling (SCOREBOARD↔RANKINGS), arrow-key player/team selection, ENTER/SPACE team-join + spectate, Ctrl+C column-layout cycle, Ctrl+R scores_per_round toggle, Ctrl+T tell, Ctrl+K vote-kick, ESC exit, and the team-selection screen (scoreboard_ui_enabled==2) with its join prompts and world dimming (0.7). |
| 72 | 10.4 | `cl-teamradar` | `cl-teamradar.links.onslaught` | logic:missing | Onslaught radar links (ENT_CLIENT_RADARLINK / draw_teamradar_link / g_radarlinks) not networked or drawn — ONS radar shows control points but no connection lines. |
| 73 | 10.4 | `cl-teamradar` | `cl-teamradar.maximized.map` | logic:missing | Maximized fullscreen radar entirely absent: no m-bind (+hud_panel_radar_maximized → cl_cmd hud radar 1), no fullscreen tactical overview, no separate maximized rotation/zoom/scale runtime, no default-border swap. |
| 74 | 10.4 | `cl-teamradar` | `cl-teamradar.maximized.click` | logic:missing | No clickable ONS spawn-point selection (cmd ons_spawn), no teleport-click, no mouse/key input handling (MOUSE1/MOUSE2/ESC/console-key passthrough), no dead+showscores temp-hide, no 'Click to select…' prompt, no icon hover-glow. |
| 75 | 10.4 | `cl-view` | `cl-view.demo.free_camera_lockview_ortho` | logic:missing | No demo free/chase camera (CSQC_Demo_Camera), no view lock (cl_lockview), no orthographic radar-render view (cl_orthoview / View_Ortho), no viewloc 2.5D side-scroller camera (GetViewLocationFOV), and no FPS report to the server (fpscounter_update STAT(SHOWFPS)). |
| 76 | 10.0 | `mutator-invincibleproj` | `mutator-invincibleproj.client.active_mut_flag` | logic:missing | The Base GetActiveMutators-style scan (util.qc:280-330) walks the active_mutators cvar (or, when empty, the live cvars) and calls mut_set_active(MUT_INVINCIBLE_PROJECTILES) + appends the translated name when g_invincible_projectiles is set, so the menu / server-browser 'active mutators' list and any MUT_ bitmask consumer can light up this mutator. The port still has NO active_mutators / mut_set_active / MUT_ flag surface (grep across src+game = 0 hits). |
| 77 | 10.0 | `mutator-offhand_blaster` | `offhand_blaster.menu.describe` | logic:missing | STILL MISSING: dedicated menu mutator-info describe text not ported. The multi-paragraph MutatorOffhandBlaster.describe() (gives all players a Blaster offhand used with hook, can fire any time but ordinary secondary can't be used, laser-jumping, overrides the hook mutator) has no port equivalent. The name 'Offhand Blaster' appears in the port only as a *reference* inside the Grappling Hook checkbox tooltip (DialogMutators.cs:197 'overridden by the Offhand Blaster mutator'), not as its own describe entry/checkbox. |
| 78 | 9.9 | `mapobject-teleporters-portals` | `portals.weapon.hook_and_predict` | logic:missing | Base Portal_Think predicts a player/hook crossing the portal plane THIS frame (Portal_WillHitPlane, PHYS_GRAVITY-aware tracebox over velocity*2*frametime) and teleports pre-emptively so fast movers don't tunnel; the warpzone realisation relies on a fixed trigger overlap, with no predict-ahead. (Warpzone.Teleport now has a same-frame finishtime guard and a plane-side test, but still no velocity*2*frametime predict-ahead.); A player's active grappling hook is still not teleported through the portal (Portal_Think iterates weaponentity.hook); the warpzone path does not relocate the hook entity.; item_flag_team is explicitly never portalled (Portal_Touch:275-276); the warpzone path has no such exclusion (a CTF flag could enter a warpzone). |
| 79 | 9.75 | `clanarena` | `clanarena.spawn.ingame_status_decimal_reset` | logic:partial | QC ca PlayerSpawn: INGAME_STATUS_SET(player, JOINED) — not ported (no INGAME state, tied to clanarena.join.late_join_observer).; QC ca PlayerSpawn: `if (time <= game_starttime) player.float2int_decimal_fld = 0` resets the damage2score decimal carry per-player ON GAME RESTART (not round start). The port clears the whole _scoreDecimal dictionary only on Activate/Deactivate (match boundary), not on the per-player spawn at game_starttime, so a mid-match game restart wouldn't zero a player's carried sub-point remainder the QC way. Sub-point cosmetic edge case.; QC ca PlayerSpawn: `if (!warmup_stage) eliminatedPlayers.SendFlags = 0xFFFFFF` (force eliminated-list resend on spawn) — the port's elimination-list capture runs per frame anyway, so the explicit dirty-flag is N/A. |
| 80 | 9.75 | `race` | `race.spawn.grid_respawn` | logic:partial | race_place START-GRID ordering still missing: racers do not get ordered start positions in a multi-racer race (no race_place read / info_player_race grid).; trigger_race_checkpoint_verify map-load validation (missing-spawn error) not performed. |
| 81 | 9.75 | `weapon-blaster` | `weapon-blaster.bot.aim_dodge` | logic:partial | bot_dodge danger-list CONSUMER still NOT implemented: the bolt now SETS bot_dodge=true/bot_dodgerating (Blaster.cs:156-157, producer fixed) and Entity carries BotDodge/BotDodgeRating, but no havocbot_dodge consumer (findchainfloat(bot_dodge,true)) exists in src/.../Bot, so the flags are inert and bots still do not specifically avoid incoming blaster bolts. The generic CombatMovement strafe is evasive combat movement, not the QC bolt-specific avoidance.; lead is a GENERIC engine-level projectile lead (BotBrain.ShotLead using the weapon's speed cvar), not the per-weapon wr_aim hook; faithful in effect for the blaster (leads at speed 6000), but the wr_aim/bot_aim weapon-dispatch seam itself is not modeled. |
| 82 | 9.6 | `cl-viewmodel` | `cl-viewmodel.anim.networked_frames` | values:missing | No networked weapon fire/idle/reload animation (the QC wframe temp entity is not networked); the gun never visibly cycles or reloads. |
| 83 | 9.6 | `sv-antilag` | `sv-antilag.solidmask.corpse` | logic:missing | Base temporarily switches the shooter's hit-contents mask to also include CORPSE so a hitscan can hit gibbed/corpse bodies during the antilag trace (tracebox_antilag_force_wz, and W_SetupShot's pre-trace mask); the port's FireBullet/FireRailgunBullet/SetupShot do not adjust any contents mask around the trace. The port HAS corpse entities (Solid.Corpse / Entity.IsCorpse), so corpse-hitting behavior may differ unless the port's default trace already includes corpse-solids. |
| 84 | 9.6 | `sv-antilag` | `sv-antilag.mode.client_verified_and_cursor` | logic:missing | g_antilag modes 1 (verified client-side) and 3 (prydon cursor hitscan) are unimplemented: a server set to either gets NO lag compensation at all (port treats anything != 2 as no-rewind). |
| 85 | 9.6 | `sv-clientkill` | `sv-clientkill.clientkill_now.vehicle_exit` | logic:missing | KillCountdown.PerformKill (GameWorld.cs:879-881) flat Combat.Damage(p,p,p,100000,DEATH_KILL) makes no attempt to vehicles_exit the player first, nor to destroy the vehicle (vehicle_health=-1 + 1-dmg DEATH_KILL on the non-team-change path). A player who `kill`s while mounted in Base is ejected and the vehicle is killed; the port just damages the player entity. Edge-case only — relevant when sv-vehicles are live (vehicles are an out-of-scope/partial port unit). |
| 86 | 9.6 | `sv-commands-votes` | `sv-commands-votes.debug_tools.missing` | logic:missing | radarmap is not registered: no in-game radar-image (XPM) generation for the minimap.; gettaginfo / animbench / bbox / trace / stuffto / adminmsg / delrec / database / effectindexdump / make_mapinfo / printstats console verbs are unimplemented (no Register() calls). |
| 87 | 9.6 | `sv-impulse` | `sv-impulse.cheat.command_impulse` | logic:missing | The clone/speedrun/fixed-clone/teleport/drag cheat impulses (140-147) are unported. They need engine services (entity cloning, speedrun checkpoint state, drag-mouse-grab) the port does not yet expose. (Some of the underlying CHEAT commands — e.g. teleporttotarget — exist in Cheats.cs as console commands, but the IMPULSE routing 140-147 has no DispatchImpulse branch.) |
| 88 | 9.6 | `sv-teamplay` | `sv-teamplay.queue.join_queue` | logic:missing | No join-queue: players join immediately regardless of balance; the deficit-filling tag-in (TeamBalance_QueuedPlayersTagIn: specific-team-first then any-team) and QueueNeeded / QueuedPlayersReady are absent.; wants_join (WantsJoin) exists on Player.cs:146 and is reset to 0 on connect/observer transitions, but is never set >0 or consulted for queueing — the field is plumbed but inert. |
| 89 | 9.1 | `bot-waypoints` | `bot-waypoints.editor` | logic:missing | No in-game waypoint editor: cannot place/remove/hardwire/mirror waypoints (g_waypointeditor*); no editor visualization (waypoint model, link lightning beams, colormod by flag).; No runtime auto-waypointing toward players (botframe_autowaypoints) or useless-waypoint pruning (botframe_deleteuselesswaypoints): the loaded graph is static. |
| 90 | 9.0 | `mapobject-misc` | `mapobject-misc.models.clientwall_fade` | logic:missing | func_clientwall distance-fade is not rendered: fade_start/fade_end/alpha_max/alpha_min/fade_vertical_offset are parsed onto the Entity (MapObjectFieldsExtra.cs) but NO renderer applies Ent_Wall_PreDraw's distance-fade alpha formula or its PVS alpha-cull — a clientwall meant to fade out with distance stays fully opaque. (The ClientWorld.cs:1234 ItemDistanceAlpha fade is the unrelated item-despawn fade; FadeEndDist is otherwise only read by WeatherSystem for rain/snow draw distance.); The antiwall relay (g_clientmodel_use: trigger sets antiwall_flag to toggle a func_clientwall/func_clientillusionary between its default_solid and SOLID_NOT, with the .inactive flag hiding it) is not ported — a map that toggles a clientwall solid/visible via a trigger has no effect. |
| 91 | 9.0 | `nexball` | `nexball.carry.perframe` | logic:partial | No safe-pass crosshair-lock onto teammates (the secondary-fire homing pass has no target — blocked on the missing BallStealer weapon).; STAT(OBJECTIVE_STATUS) NB_CARRYING bit is not networked (NB_CARRYING appears only in comments); the held-ball forteam-return notification (INFO_NEXBALL_RETURN_HELD) is not sent (the drop+reset effect IS applied).; No per-frame viewmodel scale (0.25) / EF_FLAME safe-pass lock indicator (client). |
| 92 | 9.0 | `sv-client-lifecycle` | `sv-client-lifecycle.frame.prethink_misc` | logic:partial | UpdateChatBubble (the chatbubble.spr / minigame_busy bubble over a typing or minigame-busy player, ChatBubbleThink + ShowChatBubble hook) is not modeled — no visible 'is typing' bubble.; The PlayerPreThink +use edge dispatch (PlayerUseKey) is not a single ported function: individual +use consumers (buttons/vehicles/ctf) read the button directly, but the usekeypressed edge tracker + PlayerUseKey routing (and its spectate/minigame branches) are scattered/absent.; SetZoomState (server-side weapon-zoom state for the zoom HUD/FOV net stat) + the taunt_soundtime taunt voice + target_voicescript_next are not driven from a lifecycle PreThink. (The deferred teamkill_soundtime friendly-fire complaint voice IS now fired — PlayerFrameLogic.Regen:130-149.) |
| 93 | 9.0 | `warpzones` | `warpzones.client.teleported_view` | logic:missing | Server never sends a warpzone_teleported entity and never re-rotates v_angle (WarpZone_PlayerPhysics_FixVAngle); the client never rotates VF_CL_VIEWANGLES / CL_RotateMoves on a crossing. The view does not carry through the seam. |
| 94 | 8.8 | `cl-teamradar` | `cl-teamradar.cvar.dynamichud` | liveness:dead | hud_panel_radar_dynamichud is registered with the correct default (1) but nothing consumes it: the port has no HUD_Scale_Enable/Disable (the dynamic-HUD shrink applied when the player is alive) so the radar never participates in dynamic-HUD scaling.; HUD_Radar_Export (writing the radar's 8 aesthetic cvars into a saved hud skin file) has no port analogue; the port's skin/HUD-config export path does not round-trip these radar cvars. |
| 95 | 8.4 | `turret-hk` | `turret-hk.weapon.player` | logic:missing | The hidden player weapon WEP_HK (HunterKillerAttack) — the player branch of wr_think (weapon_prepareattack / turret_initparams / W_SetupShot_Dir / weapon_thinkf WFRAME_FIRE1) that lets a given-the-weapon player fire the same guided rocket — is not ported at all (no WEP_HK / HunterKillerAttack symbol exists in the port). |
| 96 | 8.0 | `cl-crosshair` | `cl-crosshair.core.origin_chase` | logic:missing | Crosshair is always drawn at the geometric panel centre (DrawPanel: center = Size2*0.5f). The chase-camera branch (trace from player org along view_forward MOVE_WORLDONLY, project the impact, fade the player body alpha toward crosshair_chase_playeralpha while it obstructs the view, restore on chase-off via the crosshair_chase_state latch) is not implemented.; crosshair_chase and crosshair_chase_playeralpha are not even registered in RegisterDefaults (CrosshairPanel.cs:297-370). |
| 97 | 8.0 | `mutator-buffs` | `buffs.replace_powerups` | logic:missing | Still no FilterItem hook in BuffsMutator: with g_buffs>=0 and g_buffs_replace_powerups, QC replaces every powerup item with a random buff (buff_SpawnReplacement). The g_buffs>0 'replace' mode is entirely non-functional, removing the second obtain path. (A FilterItemDefinition hook chain exists in MutatorHooks.cs but BuffsMutator does not subscribe it.) |
| 98 | 8.0 | `mutator-overkill` | `overkill.countdown.blaster` | logic:missing | During an active-but-not-started round, pressing ATCK2 does not fire the secondary Blaster: Overkill does not subscribe the PlayerPreThink chain (which IS live — PlayerPreThink.Call at GameWorld.cs:1294). Players cannot blaster-jump during the countdown, and the button is not consumed. UNCHANGED; re-verified 2026-06-22 post-impl. |
| 99 | 7.8 | `sv-impulse` | `sv-impulse.cheat.impulses` | logic:partial | STILL OPEN: cheat impulses 140-147 (clone/speedrun/fixed-clone/teleport/drag) are not dispatched — they need engine services and remain unported. Only GIVE_ALL (99) and R00T (148) are routed. |
| 100 | 7.8 | `sv-intermission` | `sv-intermission.nextmap.override` | logic:partial | quit_when_empty is not implemented: a server that would start an empty next map does not quit.; quit_and_redirect is not implemented: players are not redirected/reconnected to another server at match end.; lastlevel (single-player 'show menu after match') is not implemented (the cvar is registered but never read in DriveEndOfMatchMapFlow). |
| 101 | 7.8 | `sv-spawnpoints` | `sv-spawnpoints.select.spawn_evalfunc` | logic:partial | The race_spawns gate (a race spot MUST have a target, else -1) and the SPAWN_PRIO_RACE_PREVIOUS_SPAWN=50 in-scoring checkpoint biasing are NOT ported inside ScoreSpot (inline NOTE deferring this at SpawnSystem.cs:477). Race's respawn-at-last-checkpoint is instead covered via the spawnpoint_targ one-shot redirect (see select.spawnpoint_targ), NOT the in-Spawn_Score evalfunc/race_spawns scoring path. The race score-biasing/requirement remains missing. STILL OPEN.; The port models the evalfunc as a single boolean REJECT predicate (SpotEvalReject -> bool), covering the assault '-1 0 0' veto case, but does NOT support an evalfunc that REWRITES the (prio,weight) score (Base spawn_evalfunc returns a full vector). Faithful for assault (which only ever vetoes), but a hypothetical score-rewriting evalfunc isn't representable. Minor. |
| 102 | 7.8 | `turret-framework` | `turret-framework.net.sync` | logic:missing | No turret networking: none of the TNSF_* send/parse / CSQC turret edict exists. |
| 103 | 7.8 | `vehicle-raptor` | `raptor.hud_crosshair` | logic:missing | Entire CSQC HUD path absent: no Vehicles_drawHUD ammo bars, no per-mode reticle (vCROSS_BURST bomb / vCROSS_RAIN flare), no raptor_dropmark tracetoss bomb-impact predictor (gravity-bounce traced to a 2D drawpic marker, green while live / red after release), no aux lock crosshair (vCROSS_LOCK) color feedback. Client-only; the server logic these decorate (mode switch, lock strength) is all live elsewhere. |
| 104 | 7.8 | `vehicle-raptor` | `raptor.cb_shellfrag_gib` | logic:missing | The bomb-casing shell-fragment gibs tossed client-side when the cluster bomb bursts (RaptorCBShellfragToss spawns MOVETYPE_BOUNCE drawables with avelocity jitter that fade over ~2-3s) are not ported. Purely cosmetic debris; the bomb burst gameplay (raptor.bombs) is fully live. |
| 105 | 7.7 | `bot-ai` | `bot-ai.special.jetpack_rocketjump` | logic:missing | Jetpack navigation (take off / fly to point / brake-land) is missing: bots can't cross jetpack-only gaps even with bot_ai_navigation_jetpack on. (UNCHANGED.); Skill>6 trigger_hurt escape (jetpack up, or Devastator rocketjump if HP allows) is missing: bots die in hurt volumes they could have escaped. (UNCHANGED.) |
| 106 | 7.7 | `bot-ai` | `bot-ai.move.keyboard` | logic:missing | Keyboard-movement emulation missing: port bots emit a fully analog wish-move at all skills, so they strafe/turn more precisely than stock bots below skill 10. (UNCHANGED.) |
| 107 | 7.7 | `bot-ai` | `bot-ai.scripting.commands` | logic:missing | Bot scripting entirely missing: the bot_cmd command queue does nothing, so map/console bot scripts, cutscene-driven bots, and g_waypointeditor bot driving are inert. (UNCHANGED.) |
| 108 | 7.7 | `bot-waypoints` | `bot-waypoints.file.save` | logic:missing | Port is a read-only consumer: no .waypoints/.cache/.hardwired file writers, so authored graphs cannot be saved or round-tripped. |
| 109 | 7.5 | `clanarena` | `clanarena.matchend.restore_status` | logic:missing | At match end Base restores each player's pre-CA spectator/team status from the .prev_team field (snapshotted in CA_RoundStart) so the post-match scoreboard reflects who was actually playing. The MatchEnd_BeforeScores hook infrastructure now EXISTS in the port (MutatorHooks.MatchEndBeforeScores, fired at GameWorld.cs:3193), but CA does not subscribe to it and snapshots no prev_team, so the final-scores spectator/team restoration is not modeled. |
| 110 | 7.5 | `clanarena` | `clanarena.cmd.shuffleteams_reset` | logic:missing | Base defers a `shuffleteams` command issued mid-round to the next ResetMap (shuffleteams_on_reset_map = !allowed_to_spawn) so a live CA round isn't disrupted. The port has a `shuffleteams` admin command (Commands.cs:627/CmdShuffleTeams) but no CA SV_ParseServerCommand hook to round-defer it, so it shuffles immediately even mid-round. |
| 111 | 7.5 | `domination` | `domination.roundbased.player_blocked` | logic:partial | QC reset_map_players sets it.player_blocked = 1 on map reset (roundbased) and PlayerSpawn sets player.player_blocked = !round_handler_IsRoundStarted(); Domination_RoundStart clears player_blocked = false for all players. The port's RoundStart only clears LastRoundWinner — no per-player block flag is set on reset/spawn or cleared at round start, so players are not frozen during round warmup in the round-based variant. |
| 112 | 7.5 | `freezetag` | `freezetag.damage.softkill_void` | logic:partial | STILL OPEN — no soft-kill relocate path (g_frozen_damage_trigger 0): the NEEDKILL soft-kill (restore HP=1+armor, teleport to a spawn point, no weapon reset) in PlayerDies / the Damage_Calculate relocate branch is absent — FT.OnDamageCalculate leaves NEEDKILL deathtypes alone (frozen player dies in void/lava), and the relocate needs the server spawn selector (intentionally NOT done in the gametype layer). Base default cvar is 1 = die, so this branch is dormant at stock cvars regardless.; Fall-damage revive (g_frozen_revive_falldamage) is implemented and live (FreezeTag.cs:507-515): a hard enough DEATH_FALL hit thaws the player to falldamage_health (40) and sends FREEZETAG_REVIVED_FALL/_REVIVE_SELF. Default off, so dormant at stock cvars but correct when enabled.; Nade self-revive is now FULLY implemented (FIX since baseline): FreezeTag.OnDamageCalculate (FreezeTag.cs:485-499) Unfreezes the player, restores g_freezetag_revive_nade_health (40), suppresses damage/force, and sends INFO_FREEZETAG_REVIVED_NADE + CENTER_FREEZETAG_REVIVE_SELF. g_freezetag_revive_nade ships 1 so it is live. (NadesMutator still also zeroes the same hit, but the actual unfreeze now lives in FT, hook-order-independent.) |
| 113 | 7.5 | `invasion` | `invasion.rules.accuracy_target_valid` | logic:missing | Base's AccuracyTargetValid hook returns MUT_ACCADD_INVALID for monster targets so hits on monsters don't count toward weapon accuracy stats. The port has no AccuracyTargetValid hook chain (WeaponAccuracyEvents), so monster hits still feed the accuracy tally on the server side. The CLIENT-side scoreboard hides the accuracy panel entirely in Invasion (invasion.client.hide_item_stats), so the server-side mis-tally is not visible, but the underlying accuracy data is still wrong. |
| 114 | 7.5 | `invasion` | `invasion.rules.mob_command_guards` | logic:missing | Base blocks the spawnmob console command (AllowMobSpawning: 'You cannot spawn monsters during an invasion!') and the butcher command (AllowMobButcher: 'This command does not work during an invasion!') while Invasion is active. The port has neither hook — Commands.cs:2363 explicitly notes 'no port analog (no AllowMobSpawning hook); skipped', so a player/admin could spawnmob or butcher mid-invasion. |
| 115 | 7.5 | `invasion` | `invasion.spawn.monster_hitcontents` | logic:missing | Base's MonsterSpawn hook stamps every invasion monster with dphitcontentsmask = SOLID\|BODY\|BOTCLIP\|MONSTERCLIP so the monster collides with bot-clip + monster-clip brushes. The port never sets this in the invasion spawn path (MonsterAI.cs only sets a special mask for the golem/spider PROJECTILE_MAKETRIGGER case at :1470); invasion monsters use the default monster collision mask, so they may ignore botclip/monsterclip volumes a Base server would honor. Minor — only affects maps that fence monster movement with those brush types. |
| 116 | 7.5 | `keyhunt` | `keyhunt.capture.instant_path_dead` | liveness:dead | AssignKey (KeyHunt.cs:415) routes to CheckCaptureGeometry() — the maxdist-gated path — so it would score CORRECTLY if a future caller picked it. It remains dead (zero source callers; the live collect path is KeyTouchEntity->CollectKey->CheckCaptureGeometry). Harmless dead code, no mis-score hazard. |
| 117 | 7.5 | `lms` | `lms.rules.no_throw_and_spec_lockout` | logic:partial | No ClientCommand_Spectate lockout: a ranked/out-of-game player can still `spectate` (Base returns MUT_SPECCMD_RETURN to forbid it, keyed on frags==FRAGS_SPECTATOR\|\|FRAGS_PLAYER_OUT_OF_GAME, sv_lms.qc:704-711). The port never sets a Player OUT_OF_GAME frags sentinel (verified: no OutOfGame/OUT_OF_GAME/ClientCommand_Spectate gate in ClientManager.cs), so even a spectate gate would have nothing to key on. UNCHANGED. |
| 118 | 7.5 | `mutator-buffs` | `buffs.item.auto_spawn` | logic:partial | Default count is 0 so this never runs unless explicitly configured (matches Base default) — this stays liveness:partial. (The map item_buff_* spawnfuncs are now the primary obtain path; see buffs.item.spawnfunc.); QC defers via InitializeEntity(buffs_DelayedInit, INITPRIO_FINDTARGET) and sets spawnflags\|=64 (always randomize/relocate) on each seed; port sets BuffAlwaysRelocate=true on each auto-seed (SpawnInitialBuffs), then routes through ConfigureBuffEntity (which now sets the relic model + box + cooldown + think + touch, same as the map spawn). Still spawns immediately at Hook() (not via the INITPRIO_FINDTARGET defer). Residual presentation gap: per-buff skin/color/glowmod still not applied, and no waypoint. |
| 119 | 7.5 | `mutator-buffs` | `buffs.item.random_location` | logic:partial | MoveToRandomMapLocation relocation IS done (BallEntity.RandomMapLocation, which folds in the SelectSpawnPoint fallback), with the '0 0 200' launch + a mins*1.5/maxs*1.5 tracebox unstick + g_buffs_random_lifetime arming + SND_KA_RESPAWN on relocation. The g_buffs_randomize teamplay gate (& (teamplay?2:1)) and g_buffs_random_location/spawnflag-64 relocate gate are honored.; Still missing: g_buffs_random_location_attempts is not read (RandomMapLocation uses its own fixed sampling rather than the attempts/256/1024 box-content rejection of MoveToRandomMapLocation), so the relocation's content-validity check differs from QC. No EFFECT_ELECTRO_COMBO x2 visual at the relocation, no WaypointSprite_Ping. |
| 120 | 7.5 | `nexball` | `nexball.item.filter_block` | logic:missing | FilterItem (remove dropped WEP_NEXBALL loot) is not ported.; ItemTouch (a ball-carrier can't pick up weapon items) is not ported.; ForbidThrowCurrentWeapon (can't throw WEP_NEXBALL) + ForbidDropCurrentWeapon (WEP_MORTAR) hooks absent. |
| 121 | 7.5 | `weapon-crylink` | `crylink.reload` | liveness:dead | Clip-load reload semantics: WrReload (the base WeaponFireGate machine) reloads the generic clip; QC's wr_reload also passes SND_RELOAD. The reload SOUND cue (SND_RELOAD) is whatever the base reload machine plays — not crylink-specific, faithful by inheritance. No crylink-specific reload gap remains. |
| 122 | 7.5 | `weapon-electro` | `electro.primary.midaircombo` | liveness:dead | DEAD on the live path: midaircombo_radius defaults 0 (bal-wep-xonotic.cfg), so the BoltThink midair branch (Electro.cs:258-269 -> MidairTriggerCombo) never runs under stock xonotic balance. The branch IS reachable (and now faithful) under samual/xdf balance where the cvar is 100.; IS_INDEPENDENT_PLAYER orb-owner skip is ported (Electro.cs:291) but Entity.IsIndependentPlayer is hardwired false, so the guard is dead-OFF-equivalent (no independent-player mode is ported). Harmless: default modes have no independent players. |
| 123 | 7.5 | `weapon-machinegun` | `machinegun.fire.single_mode0` | liveness:dead | mode 0 is not the bundled default (mode 1), so AttackSingle/AttackFrame/the snipe are dead on the live path — matches Base, where the xonotic balance also defaults mode 1 (only bal-wep-nexuiz25.cfg sets mode 0). |
| 124 | 7.5 | `weapon-shotgun` | `weapon-shotgun.secondary.melee_routing` | logic:partial | Alt triple-shot (secondary==2) still fires a single Attack (W_Shotgun_Attack) instead of the staged Frame1->Frame2 3-blast sequence + its dry-fire W_SwitchWeapon_Force. Non-default mode (the single-blast path now uses the correct alt_refire/alt_animtime timing). |
| 125 | 7.5 | `weapon-vortex` | `weapon-vortex.bot.wr_aim` | logic:missing | QC wr_aim calls bot_aim(actor, weaponentity, 1000000, 0, 1, false, true) and presses ATTACK when the bot has a lined-up target — the per-weapon bot aiming hook. The port's Weapon base has no WrAim/bot-aim override path at all (grep for WrAim/bot_aim over the weapon system returns nothing), so vortex bots have no weapon-specific aim solver. Likely belongs to a future bot unit; recorded here as a Base-side coverage gap for this weapon file. |
| 126 | 7.2 | `cl-csqcmodel` | `cl-csqcmodel.animdecide.state_machine` | logic:partial | animdecide is NOT ported: the networked anim_state / anim_upper_action / anim_lower_action / anim_time inputs are not sent or read (grep confirms zero anim_* bits in NetEntity/ClientEntityView), so upper-body ACTIONS (shoot/melee/pain/draw/taunt overlays on the torso) never play on the third-person model.; The 8-direction + duck-variant locomotion (~24 movement clips incl. runbackwards, strafeleft/right, forwardleft/right, backleft/right, duckwalk* directional set) is replaced by a coarse 6-state heuristic keyed only on 2D speed (20/220 u/s), on-ground, ducked, dead: remote players show a single forward-run cycle regardless of direction, never visibly shoot/flinch/reload/taunt with the torso, and ducking shows a generic crouch.; Per-anim framerates (die 0.5fps, shoot 5, jump 10, draw 3, taunt 0.33) and the per-clip framegroup names are not used; clips are picked by a fuzzy name Contains match with a 20fps fallback.; The CSQC-side implicit jump detection (ground->air transition sets ANIMACTION_JUMP) is approximated by the on-ground->Jump clip pick; no end-of-jump-anim freeze. |
| 127 | 7.2 | `cl-scoreboard` | `cl-scoreboard.field.name_icons` | logic:partial | Handicap icon is PORTED + LIVE (DrawRow:1075-1083 draws a square at the EXACT QC tint '1 0 0' + '0 1 1' * ((16-lvl)/15), white@1 → red@16, fed by the networked handicap_level).; Still missing: player-color shirt/pants swatch (gfx/scoreboard/playercolor_*), pulsing ready check, ignored icon, the pulsing (Q) wants-join team marker, and the playerid '#N' prefix (Scoreboard_AddPlayerId).; Handicap icon is drawn as a flat colored square (no gfx/scoreboard/player_handicap texture); presentation faithful in tint but not in the actual icon art. |
| 128 | 7.2 | `mutator-multijump` | `mutator-multijump.counter.networking` | logic:unknown | Whether the predicted client sees a correctly server-synced MultijumpCount (vs a server-only tally) was not traced; a desync could cause a predicted re-jump to be granted/denied differently from the server near the count limit. |
| 129 | 7.2 | `sv-client-lifecycle` | `sv-client-lifecycle.join.join_allowed` | logic:partial | MIN_SPEC_TIME + (teamplay && lockteams) + Player_GetForcedTeamIndex==TEAM_FORCE_SPECTATOR + g_playban (ForcedSpectate host hook→Bans.IsPlayBanned) + a gametype ForbidSpawn/late-join gate (LMS lockout, GametypeJoinGate) ARE enforced. Still missing: version_mismatch, g_maxping (ping<=0 too-soon + ping>maxping), and the whole QueueNeeded/queuePlayer/ShowTeamSelection + nJoinAllowed/GetPlayerLimit/g_maxplayers/bots_would_leave capacity gate.; No CENTER_JOIN_PREVENT / CENTER_JOIN_PREVENT_VERSIONMISMATCH / CENTER_JOIN_PLAYBAN / CENTER_JOIN_PREVENT_PING / CENTER_TEAMCHANGE_LOCKED centerprints for blocked joins. |
| 130 | 7.2 | `warpzones` | `warpzones.reconnect` | logic:missing | Link()/InitMapZones() run once at boot and never re-derive. Maps with moving warpzones (spawnflag 1 -> WarpZone_Think) or runtime trigger_warpzone_reconnect/target_warpzone_reconnect will have stale transforms. |
| 131 | 7.2 | `warpzones` | `warpzones.observer_warp` | logic:missing | Observers / SOLID_NOT clients are warped through zones each frame by WarpZone_StartFrame (WarpZone_Find + WarpZone_Teleport(-1,0), no targets) and by the teleportable-simple-teleporter pass; unported, so spectators cannot pass through warpzones. WarpZone_StoreProjectileData per-frame snapshot also absent. |
| 132 | 7.0 | `items-pickups` | `items-pickups.loot.item_damage` | logic:missing | StartItem sets loot TakeDamage=DamageMode.Yes (SetupLoot l.202, with a 'event_damage = Item_Damage' comment) but NO Item_Damage event handler is wired — so a loot item pushed into a lava/sky-kill volume by damage is never removed. The only NODROP kill is the at-spawn origin trace (SetupLoot l.214-223); loot that drifts into NODROP after spawn, or is struck by a NEEDKILL deathtype, survives. |
| 133 | 7.0 | `items-pickups` | `items-pickups.target.use_setactive` | logic:missing | A targetname'd item starts hidden (Item_Reset handles that — ported), but there is no use/trigger path to make it appear: QC sets item.use = item_use so a trigger firing the item's targetname spawns it (spawnflags&16 -> touch immediately, else respawn). The port wires no .use handler on items, so trigger_-activated map items never appear. item_setactive (the active/toggle relay used by relays/triggers) is likewise unported. |
| 134 | 7.0 | `items-pickups` | `items-pickups.team.finditem` | logic:missing | Maps that group several items under one .team key (so only ONE of the group is present at a time, randomly chosen, re-rolled on each respawn) are not supported: StartItem calls Item_Reset for every item unconditionally (the `if(team) Item_FindTeam else Item_Reset` branch collapses to the else). All grouped items spawn and respawn independently. |
| 135 | 7.0 | `mutator-bloodloss` | `mutator-bloodloss.presentation.active_mutator_signalling` | logic:partial | The util.qc:309 X("Blood loss", MUT_BLOODLOSS, g_bloodloss>0) GameType active-mutator row and the CSQC mut_set_active(MUT_BLOODLOSS) HUD active-mutator icon are STILL entirely unported — the port has no MUT_BLOODLOSS / mut_set_active / active_mutators flag surface at all, so the menu/server-browser HUD active-mutator ICON can never light up (the textual 'modifications' string IS now surfaced, but not the bitmask/icon).; BuildMutatorsString (':bloodloss') is LIVE but only via the gamelog/eventlog feed (sv_eventlog-gated) and the 'modifications' serverinfo; it is not consumed by an in-game scoreboard. |
| 136 | 7.0 | `mutator-buffs` | `buffs.observer_cleanup` | logic:missing | Still no MakePlayerObserver / ClientDisconnect handler to run buffs_RemovePlayer (delete the carrier buff_model). Moot only because the buff_model glow itself is unimplemented (buffs.carrier_model). |
| 137 | 7.0 | `mutator-nades` | `mutator-nades.bonus.economy` | logic:partial | GameRules_scoring_is_vip is treated as false: the VIP-kill medium bonus + flagcarrier time-score multiplier (g_nades_bonus_score_time_flagcarrier=2, scaled by KH key count) are not applied — the port always uses bonus_score_time.; The bonus notification + SND(NADE_BONUS) on banking a nade are not emitted (presentation/audio). |
| 138 | 7.0 | `mutator-new_toys` | `mutator-new_toys.menu.describe_page` | logic:missing | STILL OPEN — the MENUQC mutator describe() page (a help/info blurb in the mutator picker naming the current new-toy weapons and noting mutual exclusivity with InstaGib/Overkill) is not ported. This is a general port-wide menu-describe gap shared across all mutators, not specific to New Toys. Cosmetic. |
| 139 | 7.0 | `mutator-touchexplode` | `touchexplode.list.activemutators` | logic:missing | Base appends 'Touch explode' to the active_mutators() X-table string (util.qc:315, MUT_TOUCHEXPLODE bitfield) used for the server-browser mutator field and the scoreboard/info mutator list. The port has no active_mutators()/MUT_-bitfield string builder. NOTE: the port's newer BuildMutatorsString/BuildMutatorsPrettyString hook infra (MutatorActivation.cs:108/121) is a DIFFERENT mechanism (the per-mutator server-info/scoreboard token chain), and Base touchexplode registers NO BuildMutatorsString hook, so that infra does not advertise touchexplode and does not close this gap. Systemic gap shared by all mutators; only the create-game menu checkbox (DialogMutators.cs:129) exists. |
| 140 | 7.0 | `mutator-waypoints` | `waypoints.visibility.three_image_rule` | logic:missing | Port still networks a single SpriteName; the own-team/enemy-team/spectator image triple (model1/2/3) and the client-side SPRITERULE_TEAMPLAY image selection are not modeled. |
| 141 | 6.75 | `keepaway` | `keepaway.ball.respawn` | logic:partial | The relocate MOVES the ball — BallEntity.Relocate calls RandomMapLocation (a random spawnpoint, falling back to a jitter around home) + the '0 0 200' kick + re-arms NextThink; the loose-ball think (RespawnThink) is live via SpawnForGametype.; SND_KA_RESPAWN plays at relocate (Keepaway.RespawnBall → SoundSystem.PlayOn KA_RESPAWN, ATTEN_NONE).; FIXED (was STILL OPEN): the NEEDKILL ka_DamageEvent respawn path is now LIVE — AdoptBall installs OnBallDamage on the ball's Entity.GtEventDamage, the live DamageSystem.EventDamage (DamageSystem.cs:331-333) routes a non-player edict's damage to it, and a NEEDKILL hit (DeathTypes.ItemDamageNeedKill) force-respawns the loose ball (registry was 'no NEEDKILL respawn path').; STILL OPEN (logic): RandomMapLocation uses the QC FALLBACK path (random info_player_* spawnpoint) only — it does NOT do the full MoveToRandomMapLocation world-bounds sampling with the SOLID/SLIME/LAVA/SKY content rejection. Functionally close (ball relocates to a reachable spawn) but not bit-identical placement.; STILL OPEN (logic): no NOIMPACT fall-off respawn branch (ka_TouchEvent trace_dphitq3surfaceflags & Q3SURFACEFLAG_NOIMPACT — a ball that touches a NOIMPACT surface, i.e. off the map, is not force-respawned; the NEEDKILL damage branch IS now covered).; STILL OPEN (presentation): no EFFECT_KA_BALL_RESPAWN particle at the old + new origin (WP_KaBall waypoint sprite IS now spawned via CollectWaypoints — see keepaway.waypoints.tracking). |
| 142 | 6.75 | `keepaway` | `keepaway.ball.drop` | logic:partial | The drop velocity includes the crandom xy scatter (DropOffsetVelocity == Base '0 0 200' + '0 100 0'*crandom + '100 0 0'*crandom; Prandom.Signed == crandom); the 0.5s previous_owner/wait self-recapture lockout is armed (GtNextTakeTime + Owner=carrier).; SND_KA_DROPPED plays globally (PlayGlobal KA_DROPPED, ATTEN_NONE); INFO_KEEPAWAY_DROPPED (all) + CENTER_KEEPAWAY_DROPPED (all) notifications sent and registered.; FIXED (was STILL OPEN, liveness partial→live): ALL FOUR drop triggers are now wired. DEATH (OnDeath → Drop, Combat.Death bus); USE-KEY (OnUseKey on MutatorHooks.PlayerUseKey, fired live via VehicleBoarding.cs:106 FirePlayerUseKey on the +use impulse path); DISCONNECT + OBSERVE (OnPlayerRemoved, fired live at ClientManager.cs:473 PutObserver + :682 ClientDisconnect). QC's ClientDisconnect/MakePlayerObserver both ka_DropEvent.; STILL OPEN (logic): no nudgeoutofsolid_OrFallback after placing the dropped ball; no multi-ball chain advance / ka_PlayerReset chain semantics (single ball only).; STILL OPEN (presentation): EFFECT particle on drop is not emitted (the WP_KaBall waypoint sprite IS now re-spawned via CollectWaypoints when the ball is loose — see keepaway.waypoints.tracking).; NOTE: QC also drops via DropSpecialItems (the generic special-item drop hook); the port has no DropSpecialItems subscription, but death/disconnect/observe/use-key already cover every drop trigger DropSpecialItems would, so this is redundant rather than a gap. |
| 143 | 6.75 | `nexball` | `nexball.ball.giveball` | logic:partial | No weapon-arena swap to WEPSET(NEXBALL) / W_SwitchWeapon(WEP_NEXBALL) on pickup (the BallStealer weapon does not exist — see nexball.weapon.ballstealer).; No carrier waypoint sprite; the carrier glow effect bit is cleared on the ball but not rendered on the carrier (client-deferred). |
| 144 | 6.75 | `overkill-weapons` | `overkill-weapons.shared.secondary_blaster_jump` | logic:partial | refire_type==0 secondary path (blaster on the shared ATTACK_FINISHED timer) is not handled; only refire_type==1 is. Default is 1 for all five OK weapons, so no observable gap at stock balance.; FIRE2 weapon animation / animdecide_setaction not driven (no third-person fire-2 pose). |
| 145 | 6.75 | `physics-player` | `physics-player.water.swim` | logic:partial | Viewloc (1D-rail) water sub-cases not ported: physics.qc:269-270 sets wishvel.z = -160 when this.viewloc, and physics.qc:408-416 runs a SEPARATE accel path (plain addspeed accel, no PM_Accelerate QW pass) when in a viewloc. The port always runs the non-viewloc path. Inert on stock Xonotic maps (no 1D viewloc rails), so logic:partial not missing.; Liveness of the swim path on a live water map not behaviorally confirmed this audit (depends on CheckWater correctly setting WaterLevel from map liquid contents — the SUPERCONTENTS bitmasks are mirrored as literals and untested against a real BSP water volume here). |
| 146 | 6.75 | `physics-player` | `physics-player.ladder` | logic:partial | func_water waterlevel-from-volume override (physics.qc:280-299: clamp wishvel to ladder_entity.speed, set watertype=skin, compute waterlevel from origin.z+view_ofs.z vs ladder maxs_z) is NOT computed in LadderMove — the port only EARLY-RETURNS from CheckWater for a func_water ladder (PlayerPhysics.cs:949) but never fills the waterlevel/watertype from the volume bounds, so a func_water ladder gives WaterLevelNone.; Viewloc ladder sub-case (physics.qc:277-278: wishvel.z = movement_old.x) not ported. |
| 147 | 6.6 | `monster-framework` | `monster-framework.move.straight_2d` | logic:missing | Monster_Move_2D (walk straight in facing direction, reverse on wall/ledge unless allow_jumpoff, with the game_stopped/round/campaign/spawn_time movement gate + walk/idle anim) is NOT ported. It is a public Base API with NO caller in stock Xonotic (declared sv_monsters.qh:111, defined :1161, never invoked) — used only by mapper/custom monsters — so the omission is latent: no stock monster behaves differently. Flagged for registry completeness. |
| 148 | 6.6 | `sv-teamplay` | `sv-teamplay.balance.autobalance_bots` | logic:partial | Steady-state poll runs on a 3s cadence (BalanceTeamsTick, GameWorld.cs:2747-2749) where Base autobalances per-event; the cvar gate is now GONE (the poll is unconditional, stopped only by GameStopped/intermission, matching QC's commented-out gate). A synchronous compensation also fires on every human team-change (KillCountdown.PerformTeamChange), so the only timing divergence left is the 3s polling granularity.; Uses frag score (ScoreFrags), not SP_SCORE, to pick the bot to move; ordering can differ when frags != composite score. |
| 149 | 6.5 | `mutator-buffs` | `buffs.disability` | logic:partial | Movement-speed slow (Stunned x0.7 via SpeedMultiplier, OnPlayerPhysics:678-680) is LIVE.; Weapon fire-time slow (WeaponRateFactor x1.5) is LIVE: OnWeaponRateFactor (691-697) multiplies args.Factor by 1.5 for a stunned player; WeaponRateFactor.Call has a live site (WeaponFireGate).; STILL MISSING: weapon projectile-speed slow (WeaponSpeedFactor x0.7) and monster run/walk slow (MonsterMove x0.7) — BuffsMutator subscribes neither hook (no WeaponSpeedFactor mutator-hook chain exists; MonsterMove is unsubscribed). |
| 150 | 6.5 | `mutator-midair` | `mutator-midair.meta.mutator_strings` | logic:partial | Machine string is LIVE+faithful: MidairMutator.BuildMutatorsString returns `:midair`, MutatorActivation.BuildMutatorsString aggregates all Added mutators, and GameWorld.cs:871-878 feeds the tokens into GameLog.Init's `:gameinfo:mutators:LIST` line. This mirrors Base server/gamelog.qc:50 exactly, including the sv_eventlog gate (default 0 in both — so the line is only emitted when event logging is enabled, faithful).; STILL OPEN — pretty/display string: BuildMutatorsPrettyString override (`, Midair`) and its MutatorActivation aggregator (MutatorActivation.cs:121-125) exist, but the aggregator has NO live caller anywhere in the port (grep confirms only the per-mutator overrides + the aggregator's own loop body reference BuildMutatorsPrettyString). Base consumes it at server/client.qc:1107 to build the per-client active-mutators display string (scoreboard / serverinfo). The port has no equivalent consumer, so the `, Midair` label is never produced — an active midair is still invisible in the human-readable mutator display. |
| 151 | 6.5 | `mutator-nades` | `mutator-nades.boom.napalm` | logic:partial | g_nades_napalm_ball_damageforcescale (4) is not applied: napalm is modeled as the Burning status effect with no knockback, so the fireball/fountain damage-force push (QC proj.damageforcescale) is missing.; Water-contents velocity damping (pointcontents CONTENT_WATER -> velocity*0.5 / z=200) and the round-not-started fountain/ball delete guard are omitted; EFFECT_FIREBALL_LASER trail + EF_FLAME ball effect omitted (render). |
| 152 | 6.3 | `warpzones` | `warpzones.combat.traceextras` | logic:missing | Grenade/toss trajectory prediction (WarpZone_TraceToss) and projectile particle trails (WarpZone_TrailParticles[_WithMultiplier]) do not bend through portals — bot grenade aim and trail visuals are wrong across a warpzone. Minor. |
| 153 | 6.0 | `cl-crosshair` | `cl-crosshair.per_weapon_2d_vehicle` | logic:partial | Per-weapon crosshair: PerWeaponNumber dict is never populated by any caller (grep finds zero assignments outside the property def CrosshairPanel.cs:97), so every weapon shows the same `crosshair` number (Base maps each weapon to its own w_crosshair pic + w_crosshair_size multiplier).; crosshair_2d (side-scroller, default 54) is NOT registered and has no port. HUD_Crosshair_Vehicle (info.vr_crosshair) has no port. |
| 154 | 6.0 | `cl-csqcmodel` | `cl-csqcmodel.effects.ef_mf_trail_lights` | logic:partial | FIX (Wave 5): the per-player forced-effects seam is now LIVE for MF_ROCKET. ClientWorld.DriveCsqcModelHooks composes ForcedAppearance(jetpackActive:true) from the networked e.UsingJetpack and passes it to CsqcModelEffects.Apply(...,forced); forced.ModelFlags ORs MF_ROCKET into modelFlags, driving both the rocket trail return and the jetpack loop. So MF_ROCKET trail + loop now fire for remote players.; STILL OPEN: the OTHER MF trails (grenade/gib/tracer/zomgib/tracer2/tracer3) are mapped in CsqcModelEffectFlags but never supplied — base modelFlags is still 0 and only the jetpack composes MF_ROCKET, so non-jetpack MF-driven trails never fire on this path.; RECLASSIFIED (role-glow is actually LIVE): the baseline claimed Strength/Shield role-glow was DEAD because the ComposeForcedAppearance(strength,shield) seam is never fed true. But the role-glow VISUAL is achieved via a different, faithful path — PowerupsMutator.cs:391-392 ORs EF_BLUE(StrengthGlow)/EF_RED(ShieldGlow) into player.Effects server-side, which networks normally and renders the EF_BLUE/EF_RED dynamic lights in CsqcModelEffects.Apply. So Strength glows blue and Shield glows red for all players, live. The DEAD code is the unused CsqcModelEffectFlags.RoleGlowFlags helper (which also has the colors inverted) + the ComposeForcedAppearance strength/shield args — but they are dead-and-redundant, not a missing feature.; STILL OPEN (forced glowmod DEAD): the live composition passes forcedGlowmod (-1,-1,-1) = unset; ApplyForcedGlowmod has zero callers.; EF_BRIGHTFIELD on a player model is dropped (the trail return is unused in the entity-node pass).; Render-flags (additive/fullbright/nodepthtest) only apply to per-instance BaseMaterial3D overrides, not shared/cached shader materials.; MF_ROTATE spin is not applied (no caller supplies the bit).; boxparticles volume emission (absmin..absmax) is reduced to a point-burst at the entity origin for EF_FLAME/EF_SHOCK/EF_STARDUST. |
| 155 | 6.0 | `cl-csqcmodel` | `cl-csqcmodel.networking.csqcmodel_contract` | logic:partial | FIX (Wave 5): ALPHA is now FULLY wired end-to-end (closed the baseline 'half-wired/dead-on-consume' gap). The producer (ServerNet.QuantizeAlpha for players + entities), codec (WriteByte/ReadByte at EntityField.Alpha 1<<17), the client consume (ClientEntityView: `e.Alpha = s.Alpha == 0 ? 1f : s.Alpha/255f`), and the render (PlayerModel.ApplyAlpha sets per-instance GeometryInstance3D.Transparency on every mesh) are all live. A Cloaked (0.25) / Invisibility / fading player now renders translucent on the client.; FIX (Wave 5): UsingJetpack (IT_USING_JETPACK) is now networked (NetEntityFlags widened byte->ushort, bit 8) and consumed, letting the client re-derive MF_ROCKET (trail + jetpack loop) without networking the full modelflags field.; STILL NOT networked (unchanged-missing): colormap proper (only a team id is sent), the rest of modelflags, traileffect, non-player glowmod, the per-entity colormod (sent for EVERY entity in Base), scale, v_angle, and the entire anim_state/anim_upper/lower_action+time block.; Consequence chain (each also tracked in its feature row): no colormod => no server-driven per-model tint; no scale => no resized models; no traileffect/non-jetpack modelflags => no non-rocket MF trails; no anim_* => no upper-body actions / server-decided locomotion; no v_angle => aim pitch uses body angles; no networked colormap => force-color path leans on a reconstructed team id. |
| 156 | 6.0 | `deathmatch` | `deathmatch.scoring.teamkill_punishing` | logic:partial | DM is FFA so this never fires in practice; the escalating teamkill penalty is unmodeled, but unreachable in deathmatch (relevant only to team modes). |
| 157 | 6.0 | `keyhunt` | `keyhunt.bot.role` | logic:partial | STILL PARTIAL (logic): the four QC roles (carrier/defense/offense/freelancer with role timeouts + kh_Key_AllOwnedByWhichTeam-driven ratingscale switching + HavocBot_ChooseRole random role pick) are collapsed into a single freelancer-style rating. The collapsed role fires correctly (finds keys + rates ally-support vs grab/kill), but the per-role behavior tree + timeouts are absent. |
| 158 | 6.0 | `lms` | `lms.lives.starting_count` | logic:partial | Base derives lives from the per-map mapinfo string `lives=N` (default 5) with `m_legacydefaults "9 20 0"` selecting legacy=9 (lms.qh:11/22 re-confirmed); the port hard-codes DefaultLives=5 and reads a NON-EXISTENT cvar `g_lms_lives` as its 'primary' source (Base has only g_lms_lives_override — grep confirmed no g_lms_lives). Net: the default still lands on 5, but the port ignores both the per-map `lives=` override and the legacy-defaults switch. UNCHANGED. |
| 159 | 6.0 | `mutator-physical_items` | `mutator-physical_items.item_spawn.ghost_entity` | liveness:dead | setSendEntity(item, func_null) is not ported — the real item's net-send is not explicitly suppressed; the port relies on EF_NODRAW for client invisibility. Minor net-presentation divergence (item stays in the entity feed but draws nothing).; DropToFloor_QC_DelayedInit is not called for map-item ghosts; the port assumes the Physics/TOSS integrator settles the ghost on the next step. With no ODE integrator wired this never matters today (mutator is dead), but it is a settle-behavior divergence if the gate were ever opened.; DEAD: the whole hook is unreachable because IsEnabled is hard-false, so OnItemSpawn is never subscribed and never fires in a normal match. |
| 160 | 6.0 | `mutator-physical_items` | `mutator-physical_items.item_callbacks.think_touch_damage` | liveness:dead | Ghost colormap/colormod/glowmod are NOT copied from the owner each think (QC sv_physical_items.qc:43-46) — a cosmetic per-frame ghost-tint divergence; alpha IS followed.; DEAD: the think/touch/damage callbacks are only wired onto a ghost created by OnItemSpawn, which never runs because the mutator never adds (gate hard-false). No ghost exists, so nothing thinks/touches/damages in a normal match. |
| 161 | 6.0 | `race` | `race.mode.setup_limits` | logic:partial | Unset g_race_laps_limit defaults to 7 in the port; Base (-1) consults the mapinfo laplimit.; The remaining legacy gametype_init defaults (teamlaplimit=15, the 20-min timelimit / 5-min qualifying defaults) are not applied as defaults — only laplimit=7 has a port fallback. |
| 162 | 6.0 | `sv-chat` | `sv-chat.helpers.printtochat` | liveness:dead | All six helpers (PrintToChat / DebugPrintToChat / PrintToChatAll / DebugPrintToChatAll / PrintToChatTeam / DebugPrintToChatTeam) are now ported faithfully (Chat.cs:770-823): the \{1}^7 (\x01^7) chat marker + trailing \n, the autocvar_developer>0 gate on the Debug* variants (Cvars.Float("developer") > 0), and the bprint server-console echo (modeled by the sv_dedicated-gated DedicatedPrint tail). BUT they are DEAD — nothing in the port calls them. NOTE this matches Base: every PrintToChatAll/PrintToChatTeam use in teamplay.qc and the random_items mutator is COMMENTED-OUT debug code, so there is no live stock caller to wire to in the first place. |
| 163 | 6.0 | `sv-cheats` | `sv-cheats.mapent.info_autoscreenshot` | logic:missing | No info_autoscreenshot spawnfunc: the entity is dropped on map load and the TELEPORT cheat's preferred destination never exists. |
| 164 | 6.0 | `sv-clientkill` | `sv-clientkill.teamchange.excess_removal` | logic:missing | After a deferred team change resolves, Base re-runs excess-player removal (TeamBalance_RemoveExcessPlayers, gated on g_balance_teams_remove and only when no queued players are tagging in). The port's KillCountdown.PerformTeamChange (GameWorld.cs:898-923) does SetTeam + KillPlayerForTeamChange + a bot autobalance, but never the excess-removal pass. The same machinery IS wired on the leave path (Teamplay.cs:862, ClientManager.cs:790) — only the team-change-resolve invocation is missing. Off by default (g_balance_teams_remove 0), so dormant in stock play. |
| 165 | 6.0 | `sv-teamplay` | `sv-teamplay.global.team_entities` | logic:partial | No unified Team_* global entity API: per-team score lives in Scores, alive/owned-item counts and winner queries are reimplemented ad hoc per gametype (CA/Dom/Onslaught) rather than via Team_GetWinnerAliveTeam / Team_GetWinnerTeam_WithOwnedItems. |
| 166 | 6.0 | `weapon-arc` | `weapon-arc.bolt.secondary_burst` | logic:partial | Whole bolt_count burst fires in ONE WrThink call (one PrepareAttack, a for-loop over count at Arc.cs:493); Base fires one bolt per bolt_refire via misc_bulletcounter counting up from -to_shoot, enforcing bolt_refire2 only after the last bolt. Dormant at stock bolt_count 1. UNCHANGED.; Ammo-fraction scaling is ported (Arc.cs:473-481: to_shoot = floor(count*min(1,ammo/bolt_ammo)), TakeResource(min(bolt_ammo,ammo))) matching arc.qc:610-622 — but applied once to the whole one-shot burst, not per-bolt, so the per-bolt refire2 tail is still absent. UNCHANGED. |
| 167 | 6.0 | `weapon-vaporizer` | `weapon-vaporizer.ammo.consumption` | liveness:partial | Instagib flag is hardcoded true on the class default; in non-instagib play the bare default loadout/CheckAmmoPrimary treat it as instagib (1 cell). Attack/CheckAmmoPrimary DO re-read g_instagib via InstagibActive for the actual decrement/check, so under the mutator it is correct — but the bare class default is wrong. |
| 168 | 5.85 | `mapobject-triggers` | `mapobject-triggers.swamp.trigger_swamp` | logic:partial | slowdown is STILL DEAD: SwampThink stamps it.SwampSlug + the 0.5 swamp_slowdown, but NO code in PlayerPhysics/MovementParameters reads SwampSlug.SwampSlowdown as maxspd_mod (Base player.qc:50: maxspd_mod *= (swampslug.active==ACTIVE_ACTIVE) ? swampslug.swamp_slowdown : 1). RE-CONFIRMED 2026-06-25: grep of src finds writers in Triggers.cs:696/720/744 + the field def (EntityMapObjectStateExtra.cs:86) only — no reader. Players are damaged but move at full speed. |
| 169 | 5.7 | `mutator-overkill` | `overkill.weapons.unblock` | logic:partial | No ok_weapons sub-mutator gating the OK weapons behind (g_overkill_weapons \|\| ok). Whether the OK weapons are WEP_FLAG_MUTATORBLOCKED outside Overkill — and unblocked under it — is not enforced via this hook; the OK weapons exist as direct registrations. UNCHANGED by Wave-2. |
| 170 | 5.7 | `turret-framework` | `turret-framework.client.presentation` | logic:partial | No tur_head sub-entity: the steady-state head MODEL rotation (turret_draw tur_head.angles += avelocity*dt) is never rendered, and the per-shot head-frame anim rides the turret body's own Entity.Frame instead of a separate head bone.; Framework-wide low-HP feedback only partial: just EWheelTurret emits the <127 te_spark; the all-turret turret_draw sparks and the <85 SMOKE_LARGE / <32 SMOKE_SMALL smoke tiers are unported.; No turret_draw2d radar layer: no waypoint sprite and no over-turret healthbar.; No turret_changeteam team recolor (colormap/glowmod).; No client gib toss (turret_gibtoss/turret_gibboom) — needs a CSQC turret edict. |
| 171 | 5.6 | `bot-waypoints` | `bot-waypoints.graph.autolink` | logic:partial | No PVS cull: Base culls candidate links by checkpvs before tracewalking; the port does distance + tracewalk only (slower, may keep links Base culls).; No crouch<->normal short-link special case (Base caps those at 100qu, not 1050qu).; Box / JUMP / SUPPORT directional link forbidding not replicated: Base forbids specific outgoing/incoming directions for boxes, JUMP and SUPPORT (waypoints.qc:1212-1237); the port skips teleport/jump/ladder/custom/support sources wholesale instead. |
| 172 | 5.6 | `bot-waypoints` | `bot-waypoints.auto.spawnforitem` | logic:partial | Only runs in the no-.waypoints-file auto-generate path; on maps that ship a .waypoints file the port does NOT inject runtime item/teleporter waypoints (Base always does waypoint_spawnforitem at item spawn, snapping to the item).; bot_waypoints_for_items (= g_waypoints_for_items) gate not honored; floor-snapping uses a fixed +24 z offset rather than Base's waypoint_fixorigin tracedown. |
| 173 | 5.6 | `bot-waypoints` | `bot-waypoints.steer.steerlib` | logic:missing | No steerlib library: flocking/swarm/traceavoid/beamsteer primitives absent (group flocking, the trident/funnel traceavoid obstacle steering, and beamsteer). Bots do not flock and use the simpler single-probe avoidance in Steer + the BotDanger fall probe. |
| 174 | 5.6 | `cl-scoreboard` | `cl-scoreboard.draw.export` | logic:missing | No equivalent of Scoreboard_Draw_Export (writes the scoreboard table to a file handle for the dump-scoreboard console command); the port has no scoreboard-export command. |
| 175 | 5.6 | `cl-teamradar` | `cl-teamradar.mutator.hook` | logic:missing | No TeamRadar_Draw client mutator hook: mutators cannot add custom blips/icons to the radar, and the hook's return value (which Base ORs into the per-icon team-match gate for hover-glow visibility) is unmodeled. |
| 176 | 5.6 | `fx-sounds` | `fx-sounds.emit.play2_family` | logic:partial | play2 (per-client 2D send) and play2team (per-team 2D send) still have no port equivalent. |
| 177 | 5.58 | `mapobject-teleporters-portals` | `portals.teleport.transform_and_amazing` | logic:partial | ANNCE_ACHIEVEMENT_AMAZING on a portal telefrag within 1s of portal creation (portals.qc:193-194) is still never triggered — no teleport_time/tdeath_hit tracking on the warpzone path, and the warpzone crossing runs no tdeath at all.; Independent-player gating (IS_INDEPENDENT_PLAYER: you can't walk through someone else's portal, portals.qc:307-314 / 486-487) is still absent.; TELEPORT_FLAGS_PORTAL (FORCE_TDEATH) — the bitmask EXISTS (Teleporters.cs FlagsPortal) and TeleportPlayer honors it, but the warpzone teleport path (Warpzone.Teleport) does NOT route through TeleportPlayer, so a portal crossing is NOT force-telefragged: the warpzone uses its own transform with no tdeath at all.; The exact +/-48 plane-shift, tracebox safe-origin validation, and Portal_ApplyTransformToPlayerAngle angle transform are replaced by the warpzone transform; correctness (angle/velocity/seam) not behaviorally verified. |
| 178 | 5.5 | `mutator-buffs` | `buffs.medic.regen` | logic:partial | QC PlayerRegen scales the existing regen/limit(max)/rot factors (rot_mod=0.2, limit_mod=max_mod=1.5, regen_mod=1.7); port no-ops PlayerRegen (returns false) and runs a separate ad-hoc per-frame heal toward a recomputed medic max in MedicTick.; Health-rot multiplier (0.2 = slower rot above stable) is still not applied — above-max health rots at the normal rate.; Regen pacing/ceiling differs from QC's folded multipliers. |
| 179 | 5.5 | `mutator-nades` | `mutator-nades.boom.entrap_veil_darkness` | logic:partial | Entrap MonsterMove run/walk slow is omitted (no MonsterMove hook chain in the port).; Veil tint of enemies (render colormod) omitted; only the teammate alpha-hide is ported. Veil monster-alpha lapse omitted.; Darkness blind overlay (HUD_DarkBlinking full-screen fill) + SND_BLIND are CSQC-only and missing; only the NadeDarknessTime deadline is set server-side. |
| 180 | 5.4 | `turret-walker` | `turret-walker.client.draw` | logic:partial | No walk/run/melee animation frames (turrets_setframe(animflag) + TNSF_MOVE net): the walker model never plays its gait animations.; No CSQC walker_draw 4-point ground-align over terrain and no head avelocity spin (pure client render, still absent). |
| 181 | 5.4 | `vehicle-bumblebee` | `vehicle-bumblebee.client.presentation` | logic:partial | The networked BRG_* healray/damage beam (the signature green/red ray; ENT_CLIENT_BUMBLE_RAYGUN, bumble_raygun_send/NET_HANDLE) + bumble_raygun_draw multi-segment Draw_CylindricLine cylinder NOT IMPLEMENTED (placeholder EffectEmitter.TeHealBeam/TeBeam stub only; colour bit-faithful).; Wave-7 LIVE NOW (in addition to the Wave-5 pilot HUD): the GUNNER cockpit HUD (VehicleHudKind.BumblebeeGun selected for a seated gunner, NetGame.cs:3001) + the two GUNNER aux crosshairs (magenta '1 0 1' lead + reload-color READY, NetGame.cs:3025-3034) + the PILOT mirror of both gunners' READY markers (FeedPilotGunnerAux, NetGame.cs:3050-3051) all render each frame. STILL missing: the 'No right/left gunner!' blinking prompts (vr_hud text drawn when AuxiliaryXhair[1]/[2] are absent), and the vr_crosshair vCROSS_HEAL pilot pointer (the aux LOCK crosshair is drawn but not the dedicated vCROSS_HEAL center pointer).; Cosmetic gun models (CANNON_RIGHT/LEFT/CENTER) + gun cockpit + cockpit/viewport offsets + scale 1.5 NOT IMPLEMENTED.; MENUQC describe text (the three-passenger role explanation) NOT IMPLEMENTED. |
| 182 | 5.25 | `keepaway` | `keepaway.waypoints.tracking` | logic:partial | FIXED (was missing/na→partial/live): CollectWaypoints (overriding GameType.CollectWaypoints, pulled live each tick by ServerNet.cs:927 SendWaypoints) now EMITS the sprites — a KaBallCarrier sprite on the carrier (offset '0 0 64', RADARICON_FLAGCARRIER) when held, else a KaBall sprite at the loose ball's origin. The sprite TYPES were already registered in the catalog (Waypoints.cs:137-138 KaBall/KaBallCarrier with radar icons).; FIXED (was 'cvar never read'): g_keepawayball_tracking is now read (BallTracking) and honored: 0 = no waypoint at all, 1 = always, 2 = only when the ball is dropped/loose (the carrier sprite is suppressed when tracking != 1).; STILL OPEN (logic, why partial): the spectator/invisibility visibility rules (ka_ballcarrier_waypointsprite_visible_for_player: hide the carrier sprite from spectators-of-the-carrier and when the owner IS_INVISIBLE) are not modeled; QC also forces the sprite ON during warmup, but the host pull path has no warmup flag here so the cvar governs unconditionally.; STILL OPEN (presentation): waypoint sprite RENDER fidelity is the client's; this row asserts the server-side spawn/attach + cvar gating, not the CSQC draw. |
| 183 | 5.25 | `tka` | `tka.ball.pickup` | presentation:missing | GiveBall is LIVE: BallTouchEntity is the ball's touch handler (registered in the live SpawnBall), so a player touching the loose ball becomes the carrier, with SND_KA_PICKEDUP, INFO/CENTER_KEEPAWAY_PICKUP[_SELF] notifications, the 0.5s self-recapture lockout, AttachToCarrier, and TKA_PICKUPS+1.; The carried-ball orbit anim (Tick -> BallEntity.CarryOrbit, BALL_XYSPEED 100 / XYDIST 24) runs — GameWorld.cs:2583 calls tka.Tick(Simulation.FrameTime) every frame. The ball orbits the carrier.; No scale 0.75 while carried, no GameRules_scoring_vip equivalent, no alpha-sync invisibility, no multi-ball chaining (this.ballcarried insert + cnt offset).; World-touch non-player branch plays SND_KA_TOUCH but omits the EFFECT_BALL_SPARKS particle (presentation). |
| 184 | 5.25 | `weapon-devastator` | `weapon-devastator.presentation.flight_fx` | liveness:partial | Muzzle-flash MODEL (models/flash.md3) is not attached at the muzzle — only the ROCKET_MUZZLEFLASH particle effect is emitted (Attack: EffectEmitter.Emit ROCKET_MUZZLEFLASH, no flash-model attach).; Crosshair image/size (gfx/crosshairrocketlauncher, 0.7) not audited in this pass. |
| 185 | 5.25 | `weapon-hook` | `weapon-hook.primary.fire_grapplinghook` | logic:partial | No dedicated hook entity stream (GrapplingHookSend / ENT_CLIENT_HOOK with hook_start/hook_end). The chain HEAD IS networked via the generic projectile feed (ServerNet.Classify -> Projectile: FL_PROJECTILE/Item + Owner + MoveType.Fly) and renders as a red Hookbomb spike — but with no rope-endpoint stream there is no rope line (see weapon-hook.presentation.rope_line).; No WarpZone refsys tracking (WarpZone_RefSys_SpawnSameRefSys). |
| 186 | 5.25 | `weapon-minelayer` | `minelayer.mine.render` | liveness:partial | No CSQCProjectile(PROJECTILE_MINE) in-flight networking analogue: Base networks the in-flight mine as a CSQC projectile and only the STUCK mine is a plain server entity. The port renders the mine as a plain server model for its whole life, so in-flight prediction/trail networking differs (functional but not the CSQC path). |
| 187 | 5.2 | `cl-scoreboard` | `cl-scoreboard.block.accuracy` | logic:partial | Drawn as plain text '%' cells, not the per-weapon ICON grid (drawpic weapon model2) with column/row highlighting.; No accuracy_showdelay gating, no warmup suppression, no doublerows, no Accuracy_GetColor weapon ramp or nocolors option; the port's average color ramp differs from QC's per-weapon ramp. |
| 188 | 5.0 | `mutator-buffs` | `buffs.item.respawn_cooldown` | logic:partial | Port respawn is still a single Think after cooldown_respawn (clears Owner and runs MaybeRandomize/MaybeRelocate); no buff_activetime countdown, no activate-cooldown arming on round start (Base buff_Reset uses g_buffs_cooldown_activate; port only models cooldown_respawn), no buff_active/buff_activetime state machine, no buff_Customize light pulse.; SND_STRENGTH_RESPAWN + EFFECT_ITEM_RESPAWN ARE now emitted on (re)activation (ActivateBuff:336-337, ScheduleRespawn:462-463). Still missing: the waypoint build-finished timer and g_buffs_pickup_anyway instant-respawn.; Owner-gone / dead / frozen / wrong-buff respawn conditions (QC buff_Think) are still collapsed: the port unconditionally clears the owner and re-arms after the respawn cooldown. |
| 189 | 5.0 | `mutator-dodging` | `mutator-dodging.anim.jump_action` | logic:missing | The explicit server-side animdecide_setaction(ANIMACTION_JUMP) write is not ported (logic: missing) — the port has NO animdecide/anim-action server->client one-shot channel. HOWEVER this seam has no distinct rendered effect even in Base: animdecide.qc:283 states verbatim 'currently, in CSQC this is the only jump detection, as the explicit jump action is never called!' — Base's client renderer drives the jump clip off the IMPLICIT ground->air (ANIMIMPLICITSTATE_INAIR) transition, identical to the port's path. So the missing logic produces no observable presentation gap. |
| 190 | 5.0 | `mutator-instagib` | `mutator-instagib.misc.strings_monsters` | logic:partial | MonsterDropItem (drop 'vaporizer_cells', sv_instagib.qc:109-112) not ported — no instagib MonsterDropItem hook.; MonsterSpawn (MON_MAGE skin=1, sv_instagib.qc:114-121) not ported — no instagib MonsterSpawn hook.; RandomItems_GetRandomItemClassName -> RandomItems_GetRandomInstagibItemClassName (weighted instagib-item pick over g_instagib_items, sv_instagib.qc:24-39,102-107) not ported. |
| 191 | 5.0 | `mutator-nades` | `mutator-nades.lifecycle.cleanup` | logic:partial | FIXED Wave-6b (commit f86d947): MakePlayerObserver + ClientDisconnect are now subscribed and wired to RemovePlayer (port of nades_RemovePlayer = nades_Clear + RemoveBonus + delete nade_spawnloc, sv_nades.qc:914), and BOTH hook chains have live callers (ClientManager.cs:480/707). A held nade / banked bonus / spawn marker no longer leaks across observer or disconnect.; STILL MISSING: reset_map_global (round-reset FOREACH_CLIENT nades_RemovePlayer, sv_nades.qc:932) + PutClientInServer (RemoveBonus, sv_nades.qc:470) have no port HookChain — a banked bonus / held nade still leaks across a round reset and across the respawn-into-server path.; STILL MISSING: DropSpecialItems (the dedicated death-drop toss hook, sv_nades.qc:906) has no port HookChain. NOTE: the death-toss itself IS handled by the PlayerDies hook (OnPlayerDies tosses the held nade) — but the QC DropSpecialItems hook (which also tosses on non-death special-item drops) is unported.; STILL MISSING: SpectateCopy (copy NADE_TIMER/NADE_BONUS_TYPE/NADE_BONUS/NADE_BONUS_SCORE/pokenade_type to the spectator, sv_nades.qc:937) has no port hook — a spectator's nade HUD ring / bonus state is not mirrored from the spectatee. |
| 192 | 5.0 | `mutator-nix` | `mutator-nix.modname.strings` | logic:partial | BuildMutatorsString (':NIX', NixMutator.cs:382) is overridden AND live (consumed at GameWorld.cs:876, sv_eventlog-gated, via MutatorActivation.BuildMutatorsString). BuildMutatorsPrettyString (', NIX', NixMutator.cs:385) is overridden but has NO live consumer — MutatorActivation.BuildMutatorsPrettyString (MutatorActivation.cs:121) has zero server/client caller. SetModname ('NIX', sv_nix.qc:285) is wholly absent: no MutatorBase virtual/hook and no override. Cosmetic/reporting; no gameplay effect. |
| 193 | 5.0 | `mutator-overkill` | `overkill.randomitems.inject_ok_items` | logic:partial | Minor: the OK item pool is a hard-coded NetName list (OverkillItemNetNames = health_mega/armor_small/armor_medium/armor_big/armor_mega + weapon_okhmg/okrpc), NOT the Base g_overkill_items IntrusiveList built in MUTATOR_ONADD (which the port never builds — see overkill.onadd row). The set/weights match Base, but it is not driven by the real ITEM_FLAG_MUTATORBLOCKED-filtered list; Item_IsDefinitionAllowed is approximated by def.ItemDef.IsAllowed.; liveness=partial: only fires when the random_items mutator is also active (the chain is reached via RandomItemsMutator.GetRandomItemClassName). |
| 194 | 5.0 | `mutator-pinata` | `mutator-pinata.report.mutators_pretty_string` | liveness:dead | The pretty-string contribution is ported (override + MutatorActivation.BuildMutatorsPrettyString chain runner) but the chain runner has NO external live caller (re-verified 2026-06-25: grep finds no consumer outside MutatorActivation.cs). QC calls MUTATOR_CALLHOOK(BuildMutatorsPrettyString, "") from server/client.qc:1107 for the map-info 'modifications' line shown to joining clients / scoreboard; the port assembles this nowhere, so the ', Piñata' token never reaches a UI. Logic/values faithful, liveness DEAD. |
| 195 | 5.0 | `mutator-powerups` | `powerups.jetpack.start_item_hookreplace` | logic:missing | QC Jetpack/FuelRegen m_spawnfunc_hookreplace returns ITEM_Fuel when (start_items & m_itemid) — i.e. when the player already gets the jetpack/fuel-regen as a START item (e.g. the Hook mutator forces ITEM_FuelRegen), a map's item_jetpack / item_fuel_regen spawn is replaced with a plain item_fuel pickup so the map still drops useful ammo instead of a redundant held-bit pickup. The port's PowerupItem (JetpackItem/FuelRegen) has no spawnfunc-hookreplace path: the map item spawns as-is even when the bit is already a start item. |
| 196 | 5.0 | `mutator-waypoints` | `waypoints.gametype.collect` | logic:partial | Seven live CollectWaypoints producers now: CTF (flag base/dropped/carrier), Domination (control-point markers), FreezeTag (Frozen/Reviving + thaw-progress health bar), Keepaway (KaBall/KaBallCarrier, tracking-gated), TeamKeepaway (team-colored TkaBallCarrier<team>/KaBall, tracking-gated), Key Hunt (KeyDropped + KeyCarrier<team>/KeyCarrierFinish, tracking-gated per-viewer predicates), LMS (LmsLeader on each leader, show-window gated). Plus Assault via the manager API, and itemstime/Seeker/powerup-drop via the manager. All reached via ServerNet.SendWaypoints:1022 per tick. FIX vs baseline (was CTF+Dom+FT+Ka+Tka).; Still missing as CollectWaypoints producers: Onslaught control points + generators, Nexball balls + goals, Race start/finish/checkpoint, monster/turret/vehicle/buff markers. |
| 197 | 4.8 | `cl-csqcmodel` | `cl-csqcmodel.tagindex.weapon_attach` | logic:partial | The QC h_/v_ model tag resolution cascade (weapon bone -> gettagindex weapon/tag_weapon -> shot/tag_shot, with recursive parent predraw and drawmask=0 when the tag can't resolve) is simplified to a tag_weapon marker attach; weapon-on-weapon and the shot-muzzle tag chain are not faithfully reproduced.; No recursive predraw of the parent entity to fix forcemodel/LOD bone-index mismatch (forcemodel is live, so a forced-model bone-index mismatch on the held weapon attach is a possible edge case; LOD swap is still absent so no LOD-driven mismatch). |
| 198 | 4.8 | `monster-spider` | `monster-spider.attack.player_wielded` | logic:missing | STILL OPEN. The SpiderAttack weapon's IS_PLAYER(actor) path (a player wielding the hidden spider attack via impulse 9 / monster-riding or dev tooling) is not ported: no SpiderAttack weapon in the port (the port's Spider re-derives melee/ranged inside Monster.Attack and never goes through a Weapon). The player path uses weapon_prepareattack timing, attack_range=60, and Monster_FindTarget self-targeting — all absent. |
| 199 | 4.8 | `turret-hk` | `turret-hk.target.recieve` | logic:missing | turret_hk_addtarget not implemented: the HK cannot receive a designated target from relays / other turrets (TUR_FLAG_RECIEVETARGETS); it can only self-acquire. |
| 200 | 4.8 | `turret-phaser` | `turret-phaser.fire.bot_dodge` | logic:missing | Base wr_think sets beam.bot_dodge=true, beam.bot_dodgerating=shot_dmg and IL_PUSHes the beam onto g_bot_dodge so havocbots actively strafe out of the phaser beam. The port's PhaserTurret.Attack/BeamThink spawn the beam without setting Entity.BotDodge/BotDodgeRating (those fields now EXIST on Entity and are set by e.g. Blaster.cs, but the phaser beam omits them). The havocbot_dodge danger-list CONSUMER is also still unported, so even producer-side wiring would be inert today. |
| 201 | 4.72 | `cl-hud` | `cl-hud.panel.modicons` | logic:partial | Mode coverage is now CA/FreezeTag/Keyhunt/Survival/Assault/Domination/CTF/Keepaway; the full per-gametype icon-art fidelity (dom point ownership icons, some flag transitions) is partial. |
| 202 | 4.72 | `cl-hud` | `cl-hud.panel.minigame` | logic:partial | Board + status are merged into one MinigameRenderer (board square + team pieces + win-line + status line) rather than the QC BOARD/STATUS split; only the implemented minigame rule-sets are renderable. Per-game _hud_board/_hud_status art coverage not all runtime-verified. |
| 203 | 4.55 | `bot-waypoints` | `bot-waypoints.auto.spawnforteleporter` | logic:partial | No warpzone-angle math (Base makevectors(warpzone_angles) src/dest projection) and no oblique-warpzone JUMP flag; jumppad push-time cost is approximated (Base uses trigger_push_get_push_time / the auto-link cost), the port uses the plain travel cost.; Generated-graph path only; shipped maps use authored teleport waypoints from the file. |
| 204 | 4.5 | `ctf` | `ctf.capture_shield` | presentation:missing | No physical ctf_captureshield push entity (no shield model, no force push, no shielded centerprints) — the status is computed and gates pickups + reaches the HUD, but the visible/pushing shield is absent. |
| 205 | 4.5 | `domination` | `domination.roundbased.win` | values:partial | Round time limit is WRONG: Base round_handler_Init uses autocvar_g_domination_round_timelimit (default 120s), but GameWorld.ActivateGameType's Domination arm (@2264) calls EnableRounds(dom.CanRoundStart, dom.CheckRoundWinner, dom.RoundStart) with NO endDelay/countdown/roundTimeLimit args — so RoundHandler.Init is never called and the round keeps the generic default RoundTimeLimit=180s (and EndDelay=5s). The port's round-based dom rounds run 180s instead of 120s. (Warmup 5s matches only because the RoundHandler default countdown is also 5.) |
| 206 | 4.5 | `mutator-nades` | `mutator-nades.boom.ice` | logic:partial | The recently-revived skip (revival_time, time-revival_time < 1.5s) is omitted, so a just-unfrozen player can be immediately re-frozen (confirmed ice.qc:59).; The round-not-started delete guard is omitted (relevant only in CA/round modes). The muzzleflash/icefield particle gate omitted (render). |
| 207 | 4.5 | `mutator-nades` | `mutator-nades.boom.translocate` | logic:partial | move_out_of_solid is a minimal upward-nudge approximation (no builtin), so embedded-in-solid recovery is less robust than Base's full search.; MUTATOR_CALLHOOK(PortalTeleport) is not fired (no subscribers in port). |
| 208 | 4.5 | `mutator-overkill` | `overkill.onadd.precache_and_itemblock` | logic:partial | ok_player models are never precached / forced as the default player model (precache_all_playermodels equivalent absent).; g_overkill_items list (the random-items injection source) is never built.; Mechanism differs: Base ONADD sets ITEM_FLAG_MUTATORBLOCKED on the four items at add-time; the port instead reads the four filter cvars in Hook() and enforces the block per-spawn in the FilterItem hook (overkill.filter row). End behavior (which items are blocked, with which defaults) now matches — but the ONADD spawnflag-blocking and ONREMOVE clear are not modeled. |
| 209 | 4.5 | `turret-phaser` | `turret-phaser.presentation.beam_visual_and_head_anim` | logic:partial | No PhaserTurret_beam model spawned/scaled to the impact distance (MDL_TUR_PHASER_BEAM + .scale = vlen/256 + setattachment tag_fire); acknowledged TODO comment in Attack (PhaserTurret.cs:161-165). The server-side beam logic/damage IS done; only the visible beam model is absent.; phaser.md3 head model is not attached as a separate tracking head bone — the head-frame animation drives the turret edict's own networked Entity.Frame, so the frame VALUE animates faithfully but there is no visible phaser.md3 head to show it (presentation gap shared with identity.model_hitbox). |
| 210 | 4.5 | `weapon-tuba` | `weapon-tuba.note.sustain_lifetime` | values:partial | W_WeaponRateFactor scaling on the keep-alive — the port DOES multiply refire*2 by WeaponRateFactor(actor) (Tuba.cs:163), so this matches Base; only relevant under haste/rate modifiers.; No W_Tuba_NoteThink server-side per-listener volume/angle re-origin + SendFlags (moot until tuba-note sound networking exists). The port's NoteThink only models the teleport_time self-expiry, not the re-origin broadcast. |
| 211 | 4.5 | `weapon-vaporizer` | `weapon-vaporizer.rm.laser_barrage` | liveness:unknown | No confirmed distinct CSQC bolt visual: Vaporizer.cs:359 tags netname 'rocketminsta' to pick a RocketMinstaLaser/elaser descriptor, but no such client projectile descriptor exists in the port — the bolt likely renders as a generic plasma/laser. No weapon_thinkf rapid_animtime.; Liveness of the RM-laser path (requires g_rm + g_rm_laser, or g_rm_laser==2) in a shipped/normal match not traced end-to-end. |
| 212 | 4.2 | `sv-client-lifecycle` | `sv-client-lifecycle.join.join_player` | logic:partial | Join queue + team-selection ordering not modeled: the queued_join TeamBalance_AreEqual two-pass (queued-team dibs first pass + strongest-autoselect-by-skill second pass, player_with_dibs conflict) is absent; the port autoselects via a single connect-time AssignBestTeam.; Campaign Join branch (campaign_bots_may_start / ReadyRestart) only partially modeled (CampaignHostAutojoin opts the host in); the bot-start ReadyRestart trigger is not in ClientManager.Join.; Queue-conflict + queued-player branches still deferred: CENTER_JOIN_PLAY_TEAM_QUEUECONFLICT (depends on the unmodeled wants_join/player_with_dibs queue) and ANNCE_BEGIN for a queued player are not fired; Kill_Notification(CPID_PREVENT_JOIN) clear not modeled. |
| 213 | 4.2 | `turret-hk` | `turret-hk.menu.describe` | logic:missing | The MENUQC HunterKiller.describe page text (turret info / description panel) is not ported; the port sets only HkTurret.DisplayName ('Hunter-Killer Turret'), not the full PAGE_TEXT body that references WEP_DEVASTATOR. |
| 214 | 4.05 | `mapobject-triggers` | `mapobject-triggers.viewloc.trigger_viewlocation` | liveness:partial | no client 2.5D side-scroller camera consumer: the server stamps player.ViewLoc each frame but the view is never locked to the start->end line, so the camera region has no visible effect (unchanged this wave) |
| 215 | 4.0 | `cl-teamradar` | `cl-teamradar.cvar.zoomscale` | logic:partial | Zoom/scale uses a RangeUnits=2500 + scale/ScaleReferenceDivisor heuristic for the bigsize/normalsize endpoints, not the Base mi_scale/scale2d-derived sizes; the actual arena-fit big size is approximated by a fixed 2500u range.; Big-zoom recentering only happens when map bounds are loaded (HasMapBounds); the standalone path always has bounds, so this is live there, but the heuristic scale endpoints still differ from Base's exact fit. |
| 216 | 4.0 | `mutator-bugrigs` | `mutator-bugrigs.player_physics.prevangles` | logic:partial | player.disableclientprediction = 2 is not set (Base force-disables prediction every tick for a bugrigs player). The port treats bugrigs as server-authoritative so this is LIKELY inert, but that is unverified under the port's net smoothing / cl_movement_errorcompensation layer — could surface as camera/position drift for a bugrigs client. |
| 217 | 3.92 | `cl-hud` | `cl-hud.panel.infomessages` | logic:partial | Cycling group hints (spectate controls), team-unbalanced nag, spectator list and the full message set are partially covered (the eliminated-LMS line is now present). |
| 218 | 3.85 | `bot-ai` | `bot-ai.combat.dodge` | logic:partial | Combat movement is invented (periodic strafe-direction flip + health-advantage close/flee bias), not QC havocbot_dodge which sidesteps specific incoming projectiles by their flight path. (UNCHANGED.); The SUPERBOT random-direction combat jitter (havocbot_movetogoal:1280-1307) is not reproduced; only the generic strafe stands in. (UNCHANGED.) |
| 219 | 3.76 | `cl-hud` | `cl-hud.panel.pressedkeys` | liveness:partial | Spectating-only enable gate + the key_*/key_*_inv art coverage not runtime-verified; only shows for the local/spectated pressed-keys stat. |
| 220 | 3.76 | `cl-hud` | `cl-hud.panel.quickmenu` | logic:partial | Nested quick-chat tree from a quickmenu file + translatecommands/server_is_default parity not verified. |
| 221 | 3.76 | `fx-effectinfo` | `fx-effectinfo.emit.te_builtins` | liveness:partial | The te_* helper -> registry-name mapping (e.g. TeSmallflash -> BLASTER_MUZZLEFLASH, TeGunshot -> MACHINEGUN_IMPACT) is a port convention, not a 1:1 DP te_* semantic; not verified that every live caller hits these wrappers vs emitting the registry effect directly. Beam/arc temp-effects route to BeamRenderer, not the particle catalog. |
| 222 | 3.75 | `clanarena` | `clanarena.hud.eliminated_greyout` | logic:partial | IsEliminatedPlayer == IsDead only; QC ca_isEliminated also greys INGAME_JOINING late joiners and FRAGS_PLAYER_OUT_OF_GAME, which the port still lacks, so a late joiner waiting out the round isn't greyed until they die. |
| 223 | 3.75 | `clanarena` | `clanarena.lifecycle.reset_map` | logic:partial | QC reset_map_players: for each INGAME-or-bot client, clear killcount, TRANSMUTE(Player), INGAME_STATUS_SET(JOINED), PutClientInServer, reset prev_team. QC reset_map_global: allowed_to_spawn = true. The port re-spawns everyone at round reset via its round-gated DeadPlayerThink/ResetMap machinery (effective one-life-per-round result holds), but has no CA-specific reset_map hook: no INGAME->Player transmute (no INGAME state at all), no killcount reset, no prev_team reset, and no explicit allowed_to_spawn flag (the port gates spawn on round phase instead). Practically equivalent for the common case; the late-joiner/INGAME details are tied to clanarena.join.late_join_observer. |
| 224 | 3.75 | `ctf` | `ctf.flag.remove_player` | liveness:partial | Portal/vehicle carry leave handling (g_ctf_portalteleport drop, vehicle board/leave drop) is still NOT wired into ctf_RemovePlayer — only the disconnect (ClientManager:770) + MakePlayerObserver (ClientManager:517) leave-play hooks are wired. The carried-flag vehicle COCKPIT offset IS honored in Tick (FlagVehicleCarryOffset, carrier.Vehicle), but boarding/leaving a vehicle or portal does not drop via this path. |
| 225 | 3.75 | `ctf` | `ctf.hud.waypoints` | values:partial | Base/dropped/carrier-own (health bar) + carrier-enemy (stalemate/self-carry reveal) sprites + the carrier helpme FLASH are emitted; no dedicated flag-return sprite, no on-pickup enemy-FC spotting reveal (g_ctf_flagcarrier_waypointforenemy_spotting). |
| 226 | 3.75 | `ctf` | `ctf.oneflag` | liveness:partial | Liveness depends on a map shipping item_flag_neutral; the neutral-pickup + team-base-capture + reverse logic is present but exercised only on a one-flag map. g_ctf_reverse (two-flag reverse) is NOT modeled (only OneFlagReverse). |
| 227 | 3.75 | `cts` | `cts.map_reset` | logic:partial | ResetMapGlobal now resets every runner's in-progress run (RunStartTime=0, QC race_PreparePlayer) and cancels pending finish-retracts on map reset — this is live (GameWorld:3138). The persistent top-99 CTS records are intentionally kept (QC keeps them too; QC's race_ClearRecords clears the per-round score table, not the persistent DB).; Still not modeled: the cts_EventLog place echo per client (Score_NicePrint + the ftos(race_place) gamelog line) and the qualifying==2 -> race collapse (moot: CTS pins g_race_qualifying=1). The PlayerScore_Sort/race_place bookkeeping (CTS has no intermediate-checkpoint race_place tracking) is also absent. |
| 228 | 3.75 | `cts` | `cts.records.getrecords` | logic:partial | The port models the single-map ranking listing (QC getrankings: ranks 1..RANKINGS_CNT of the current map) and wires it live to the rankings/records console commands. The QC GetRecords hook's multi-map paged form (200 maps/page across MapInfo, the cross-map 'records [<page>]' listing) is NOT modeled — only the current map's records are listed. |
| 229 | 3.75 | `invasion` | `invasion.win.hunt` | logic:partial | Base derives the HUNT win solely from the live g_monsters list (no non-respawned monster remaining). The port now reads BOTH the live world monster list (LiveNonRespawnedMonsterCount: FindByClass(monster), alive, !Respawned) AND its own Wave.Spawned/Killed counters, requiring _huntSawMonster before treating 'none left' as a win. Closer to Base than before, but the dual-path + the 'must have seen >=1 monster' guard is still unverified on a real HUNT map. |
| 230 | 3.75 | `keepaway` | `keepaway.ball.pickup` | values:partial | Pickup is reachable — the live engine trigger-touch pass dispatches Solid.Trigger entities' Touch=BallTouchEntity, which calls PickUp; AttachToCarrier makes the ball SOLID_NOT + MOVETYPE_NONE + sets GtCarrier.; SND_KA_PICKEDUP plays (PlayOn KA_PICKEDUP, ATTEN_NONE); INFO_KEEPAWAY_PICKUP (all) + CENTER_KEEPAWAY_PICKUP (all-except) + CENTER_KEEPAWAY_PICKUP_SELF (one) notifications all sent and registered.; KEEPAWAY_PICKUPS column increment runs (AddCol on PickUp); the 0.5s self-recapture lockout is honored (BallTouchEntity → IsRecaptureLocked).; The non-player world-touch branch plays SND_KA_TOUCH (ATTEN_NORM).; STILL OPEN (values/presentation): carried scale 0.75 is not applied (no entity scale field). EFFECT_BALL_SPARKS particle on the non-player touch is not emitted (only the sound is).; STILL OPEN (logic): GameRules_scoring_vip(toucher,true) has no direct port equivalent (the carrier role is tracked via Ball.Carrier instead); functionally the carrier is identified for the mod-icon, but the QC VIP flag/scoreboard marker is not modeled.; STILL OPEN (logic): multi-ball chaining (g_keepaway_ballcarrier_maxballs, cnt chain) is collapsed to a single ball (cnt=1). |
| 231 | 3.75 | `keepaway` | `keepaway.bot.role` | logic:partial | RoleKeepaway is live (set on the brain at bot creation via BotRoles.ChooseRole; 'ka'/'keepaway'/'tka' → RoleKeepaway) and engages the ball: FindBall() queries classname 'keepawayball' (BotObjectiveRoles.cs:387) == the live spawned ball; carrier read off ball.GtCarrier (the field AttachToCarrier sets). A loose ball is chased (collector behaviour: items 10000, enemies 500, ball 8000) and a carried ball's carrier is contested; carrying switches to carrier behaviour (items 10000, enemies 10000).; FIXED (was STILL OPEN): the Bot_ForbidAttack equivalent is now wired LIVE. Keepaway.ForbidBotAttack(bot,targ) = `!targCarries && !botCarries && haveHeldBall` (== QC: neither carries + a held ball exists), installed at GameWorld.cs:1896 onto BotBrain.ForbidAttackHook and consulted in bot_shouldattack (BotBrain.cs:797) on the live attack-eligibility path — so bots cluster the carrier instead of fragging bystanders.; STILL OPEN (logic, why status stays partial): the collector/carrier split is collapsed into one adaptive RoleKeepaway (Base has two distinct havocbot_role_ka_carrier / havocbot_role_ka_collector functions with a per-bot role-timeout state machine). The port branches on carry-state inline with the same ratingscales rather than swapping the dedicated role function — a structural simplification, behaviourally equivalent at the rating level. |
| 232 | 3.75 | `lms` | `lms.forfeit.remove_player` | liveness:partial | RemovePlayer is wired only on the DISCONNECT path (InfraClientDisconnect, voluntary:true). Base also calls lms_RemovePlayer from MakePlayerObserver (a forced-observer / mid-match spectate with the is_forced/voluntary distinction, sv_lms.qc:413-436). The MutatorHooks.MakePlayerObserver DISPATCH now exists (ClientManager.cs:523-524 fires MakePlayerObserver.Call on demote-to-observer), but LMS does NOT subscribe to it (no MakePlayerObserver handler in LastManStanding.cs — grep confirmed), so a forced-to-observer (non-disconnect) player still isn't ranked/reshuffled. The voluntary-vs-forced branch exists in RemovePlayer but only the voluntary (disconnect) caller is wired — liveness partial.; RemovePlayer records last_forfeiter_* but the spawn-side consumer (PlayerSpawn least-healthy clone) is still missing (see lms.loadout.start_items). |
| 233 | 3.75 | `nexball` | `nexball.sound.cues` | liveness:partial | SHOOT1/SHOOT2/STEAL are registered (SoundsList) but still dead — they belong to the missing BallStealer weapon (see nexball.weapon.ballstealer). |
| 234 | 3.75 | `overkill-weapons` | `overkill-weapons.mode.enablement` | liveness:partial | Mode reachable only by exec'ing ruleset-overkill.cfg / setting g_overkill via console — no in-menu arena radio. VERIFIED this MATCHES Base (Base dialog_multiplayer_create_mutators.qc also exposes only g_instagib + g_nix radios). So this is faithful to Base, NOT a port-side regression. |
| 235 | 3.75 | `physics-movetypes` | `physics-movetypes.push.pushmove` | logic:partial | Base special-cases MOVETYPE_PHYSICS riders (push.qc:124-129): they just translate + relink and skip the PushEntity/blocked logic. The port has no such branch — a MOVETYPE_PHYSICS rider goes through the normal PushEntity sweep + stuck-check. Low impact (the port rarely uses MOVETYPE_PHYSICS riders).; Base gathers riders via findradius over the whole live entity list (FOREACH_ENTITY_RADIUS); the port iterates Entities() with a swept-box prefilter — functionally equivalent result set, but a flat scan rather than the area-grid broadphase.; Base zero-thickness/corpse rider squash mutates the rider's mins/maxs to zero (push.qc:171-172) then continues; the port just continues without zeroing the box. Cosmetic-state difference; both avoid blocking. |
| 236 | 3.75 | `physics-player` | `physics-player.crouch` | logic:partial | Viewloc force-crouch (player.qc:192-193: viewloc && !(VIEWLOC_FREEMOVE) && movement.x < 0 => do_crouch=true) is NOT ported.; have_hook force-uncrouch (player.qc:175-195: a grappling hook held in any weapon slot forces do_crouch=false) is NOT ported — the port models the frozen/dead/PlayerCanCrouch-hook force-false but not the hook or in-vehicle (PHYS_INVEHICLE) force-false sub-cases.; Q1BSP crouch-disable (PL_CROUCH_MAX.z == PL_MAX.z => do_crouch=false, player.qc:207-208) not modelled (Xonotic maps are Q3BSP, so inert). |
| 237 | 3.75 | `race` | `race.penalty.zones` | logic:partial | Freeze release restores MoveType.Walk unconditionally (Base just clears the freeze; a swimming/flying racer would be forced to Walk).; disableclientprediction = 2 (Base) is not set during the freeze; the client may mispredict through the freeze.; RACE_NET_PENALTY_* networking (the on-screen penalty line) is not sent — penalty is invisible to the HUD. |
| 238 | 3.75 | `race` | `race.hud.mod_icon_rankings` | liveness:partial | The mod-icon + split panels are fed host-side (LocalServerPlayer / _serverWorld.GameType), so on a PURE remote client the floating PB/server-best icon + medal flash do not render — there is no TE_CSQC_RACE / RACE_NET_SERVER_RECORD net stream feeding ModIconsPanel; only the RANKINGS scoreboard column is genuinely networked. On a listen server the mod-icon renders live.; ReadName/ReadTime are read straight from the persistent RaceRecords DB (no separate networked record-name stream); the local PB is resolved by PersistentId rather than the QC ClientProgsDB cache. |
| 239 | 3.75 | `tka` | `tka.ball.respawn` | presentation:missing | BallEntity.RandomMapLocation uses the QC SelectSpawnPoint FALLBACK path (a random spawnpoint), not the engine MoveToRandomMapLocation world-bounds sampler (rejecting solid/lava/sky). Faithful to the fallback QC itself drops to, but the primary random-world-point sampler is approximated.; Respawn sound (SND_KA_RESPAWN, ATTEN_NONE) IS played in RespawnBall, but the EFFECT_KA_BALL_RESPAWN particles at the old+new origin remain deferred (the WP_KaBall waypoint IS now produced — see tka.waypoints.tracking).; No NOIMPACT/fall-off respawn branch (tka_TouchEvent off-map Q3SURFACEFLAG_NOIMPACT handling not ported — the world-touch goes to the spark/sfx branch instead). |
| 240 | 3.75 | `weapon-arc` | `weapon-arc.beam.heat_overheat` | logic:partial | Structural divergence (UNCHANGED): Base stores beam_heat on the beam ENTITY (arc_beam) and DELETES that entity when the beam ends — heat is gone, and arc_overheat/arc_cooldown are pure timestamps. The port keeps heat as a persistent slot-state value (st.BeamHeat) drained over time by BeamCooldown. Observably close at stock values; not bit-structurally identical.; cooldown_release still ignored at stock (Arc.cs:230 CooldownSpeed branch only sets ArcOverheat when CooldownRelease != 0); Base sets arc_overheat when (cooldown_release \|\| overheat). Dormant at stock cooldown_release 0.; Minor: the NetGame heat ring while-firing branch keys on st.BeamHeat > 0 rather than st.ArcBeam != null (arc.qc:62 keys on player.arc_beam); functionally equivalent at stock (the beam is the only thing that raises heat). |
| 241 | 3.75 | `weapon-arc` | `weapon-arc.heat_persistence` | logic:partial | DROP/PICKUP migration (wr_drop/wr_pickup) still unported: arc_overheat/arc_cooldown are not migrated to a dropped Arc and restored on pickup. The port has no weapon-throw/heat-carry path yet (WeaponThrowing.cs:51 notes no weapon implements wr_drop). Mostly dormant since primary beam_heat is 0. UNCHANGED.; Reset is per-slot in WrSetup, not a respawn-wide hook: WrSetup runs on every weapon switch-in/raise, so a switch away+back also clears heat (benign — the beam is gone while holstered). Base wr_resetplayer/wr_playerdeath are dedicated respawn/death hooks. UNCHANGED. |
| 242 | 3.75 | `weapon-blaster` | `weapon-blaster.offhand.fire` | liveness:partial | Live only when g_offhand_blaster is set (default 0 in mutators.cfg:435), so liveness stays partial — but when enabled the behaviour is now faithful. |
| 243 | 3.75 | `weapon-electro` | `electro.orb.csqc_netlink_draw` | values:partial | The dedicated REGISTER_NET_LINKED(Electro_Orb) link/draw is not reproduced 1:1: Base streams the orb as its own CSQC entity (electro_orb_send writes origin+owner+lifetime; electro_orb_draw applies a (ltime-time)-based scale shrink as the orb nears expiry plus an avelocity spin '7 0 11', alpha 0.7). The port renders the orb via the generic ProjectileCatalog/ProjectileRenderer Electro body (ebomb model + TR_NEXUIZPLASMA trail + dynamic light + electro_fly loop). The in-flight identity matches; the time-based shrink pulse and explicit spin axis are cosmetic and unreproduced. |
| 244 | 3.75 | `weapon-hook` | `weapon-hook.primary.remove_reset` | logic:partial | No central RemoveGrapplingHooks reset on freezetag-freeze / teleport / vehicle-enter / wr_resetplayer; the hook is cleared only via the weapon's own state machine.; Shoot-down frag attribution sets pusher/pushltime but hardcodes istypefrag=false (chat-input PHYS_INPUT_BUTTON_CHAT not wired), so the istypefrag obituary classification differs from Base in the chat-while-shot-down edge case. |
| 245 | 3.75 | `weapon-machinegun` | `machinegun.notification.kill_suicide` | liveness:partial | The message-SELECTION code is faithful and wired live (Scores.cs obituary path calls SelectKillMessage/SelectSuicideMessage; DeathMessages keys SNIPE/SPRAY on HasHitType(deathType, Secondary)). The SNIPE branch is reachable ONLY from the mode-0 secondary (now correctly carrying HITTYPE_SECONDARY in the port) — but mode 0 is non-default and dead, so in default mode-1 play BOTH Base and the port emit MURDER_SPRAY (faithful). |
| 246 | 3.75 | `weapon-minelayer` | `minelayer.fire.lay_mine` | liveness:partial | WEP_FLAG_MUTATORBLOCKED (Minelayer.cs:63): the weapon is excluded from the default loadout/allweapons (GiveItems.cs:340 = SpawnFlags & (MutatorBlocked\|Hidden) != 0), so this fire path is NOT reached in a normal match. Live only when granted via weaponarena/NewToys/impulse-give. WrThink dispatch (WeaponFireDriver) is correct on that conditional path. Liveness stays partial. |
| 247 | 3.75 | `weapon-minelayer` | `minelayer.fire.mine_limit` | liveness:partial | Liveness partial: MUTATORBLOCKED weapon (see minelayer.fire.lay_mine). |
| 248 | 3.75 | `weapon-minelayer` | `minelayer.mine.stick` | liveness:partial | Liveness partial: MUTATORBLOCKED weapon (see minelayer.fire.lay_mine). |
| 249 | 3.75 | `weapon-minelayer` | `minelayer.mine.proximity` | liveness:partial | Base proximity loop also skips IS_INDEPENDENT_PLAYER(head); the port does not model independent-player mode, so that exclusion is absent (no functional effect outside that unported mode).; Liveness partial: MUTATORBLOCKED weapon (see minelayer.fire.lay_mine). |
| 250 | 3.75 | `weapon-minelayer` | `minelayer.mine.lifetime_countdown` | liveness:partial | Port lifetime-arm branch adds an extra `self.Frame == 0f` term to the QC `!mine_time && cnt > 0` guard. Redundant (Frame is only set to 1 after MaxHealth is set, so MaxHealth==0f already excludes re-entry; deathTime is always >0 since lifetime-countdown=29.5) — no behavioral difference. Model note only.; Liveness partial: MUTATORBLOCKED weapon (see minelayer.fire.lay_mine). |
| 251 | 3.75 | `weapon-minelayer` | `minelayer.mine.owner_death` | liveness:partial | HITTYPE_BOUNCE deathtype tag on the auto-detonate is carried only as a bool param (the port's int weapon-id deathtype has no bounce flag); the obituary picks the same splash murder string regardless, so this is documentation-only (minor).; Liveness partial: MUTATORBLOCKED weapon (see minelayer.fire.lay_mine). |
| 252 | 3.75 | `weapon-minelayer` | `minelayer.fire.remote_detonate` | liveness:partial | Port detonates inline in RemoteDetonate rather than flagging minelayer_detonate=true and letting each mine's own W_MineLayer_Think call W_MineLayer_RemoteExplode the next tick (one-tick timing difference; behaviorally equivalent in practice).; deathtype HITTYPE_BOUNCE tag on remote blasts not set (obituary picks the splash murder string regardless; inert).; Liveness partial: MUTATORBLOCKED weapon (see minelayer.fire.lay_mine). |
| 253 | 3.75 | `weapon-minelayer` | `minelayer.mine.explode` | liveness:partial | Airshot achievement (ANNCE_ACHIEVEMENT_AIRSHOT on a direct hit to an enemy flying player) not implemented.; Out-of-ammo forced weapon switch after the last mine fires (Base W_MineLayer_Explode tail: ATTACK_FINISHED + m_switchweapon = w_getbestweapon) is not done here.; Liveness partial: MUTATORBLOCKED weapon (see minelayer.fire.lay_mine). |
| 254 | 3.75 | `weapon-minelayer` | `minelayer.ammo.checks` | liveness:partial | Port WrThink omits the forced-reload branch, but reload_ammo defaults to 0 so Base never enters it either — faithful at default values (would diverge if reload_ammo were set).; Base wr_checkammo1 also counts clip_load (weapon_load) as a second ammo source; port checks only the rocket resource. Inert at reload_ammo=0 (clip is never loaded), so no behavioral difference at defaults.; Liveness partial: MUTATORBLOCKED weapon (see minelayer.fire.lay_mine). |
| 255 | 3.75 | `weapon-porto` | `weapon-porto.touch.placement_tree` | logic:partial | Surface-flag gating logic differs from Base in WHERE the flags come from: Base reads trace_dphit* off the ORIGINAL bouncemissile collision trace; the port re-probes with a short worldonly traceline (ProbeImpact, Porto.cs:319-337) because the engine touch carries no per-contact flags. The TraceService genuinely fills DpHitQ3SurfaceFlags/DpHitContents from the BSP surface, so the slick/clip + noimpact branches are now reachable and play the right cues — but the re-trace can disagree with the true contact face (e.g. on a glancing bounce the probe ray may miss or hit an adjacent face), so the branch selection is approximate rather than exact.; No CheckWireframeBox surface-size gate: a hit on a wall too small for a 96^3 portal is accepted (Base rejects via the preview-side wireframe check).; Base's final 'else' branch verifies realowner.portal_in.portal_id == this.portal_id before placing the out-portal of a combined shot; the port's blue-shot branch (Porto.cs:289-296) places unconditionally and has no portal_in id check or the mismatch->ClearAll fail path. |
| 256 | 3.75 | `weapon-porto` | `weapon-porto.lifecycle.lifetime_self_destruct` | logic:partial | No throwable-drop: when a combined porto fails softly Base can drop it as a pickup (W_ThrowNewWeapon) with CENTER_PORTO_FAILED; the port just cleans up.; No center-print notifications (CENTER_PORTO_CREATED_IN/OUT/FAILED) anywhere. |
| 257 | 3.75 | `weapon-porto` | `weapon-porto.lifecycle.death_reset_cleanup` | liveness:partial | Dispatch is current-weapon-scoped: WrPlayerDeath is invoked from DamageSystem.cs:634-638 only for Inventory.CurrentWeapon(victim) at slot 0, and WrResetPlayer from SpawnSystem.cs:825-836 only for the current weapon at slot 0. In Base, Portal_ClearAll on death runs regardless of which weapon the player holds, and wr_resetplayer runs for EVERY owned weapon. So in the port, if the player is NOT holding porto at the moment of death/respawn, the porto death/reset cleanup does not run — a placed portal or in-flight porto can survive a death where porto wasn't the active weapon. (Single-slot port mitigates the resetplayer half; the death half is the real exposure.) |
| 258 | 3.75 | `weapon-vaporizer` | `weapon-vaporizer.primary.beam_particle` | values:partial | Beam is a generic networked trail effect (TE_TEI_G3 / _HIT), not the Base cylindric gauntletbeam/lgbeam beam; no team-color (cl_vaporizerbeam_teamcolor) or colorboost tint, no 0.8 beam lifetime cvar. |
| 259 | 3.75 | `weapon-vaporizer` | `weapon-vaporizer.secondary.blaster_laser` | liveness:partial | Optional Blaster-ammo decrement (W_DecreaseAmmo if WEP_CVAR_PRI(BLASTER,ammo) set) is omitted — harmless at the default Blaster ammo 0. |
| 260 | 3.75 | `weapon-vortex` | `weapon-vortex.fx.beam` | values:partial | Beam is emitted at a constant intensity: alpha/fade/trail-spacing are NOT scaled by sqrt(charge), so a barely-charged shot and a full shot look identical. EffectRequest carries colorMin/colorMax but no per-emission alpha, so the sqrt(charge) brightness modulation has no port-side hook.; Team tint is computed server-side from the FIRER's charge+team and broadcast as a single colour (EmitBeam), so the per-recipient cl_tracers_teamcolor / cl_particles_oldvortexbeam client-cvar nuance is collapsed to one broadcast emit (the broadcast reads the host's cvars). |
| 261 | 3.75 | `weapon-vortex` | `weapon-vortex.hud.charge_ring` | liveness:partial | Host-only: the charge stat is fed from LocalServerPlayer's slot state (NetGame.UpdateCrosshairWeaponRings), so the ring fills on a listen-server / single-player host but a PURE remote client has no charge data and still sees no ring. QC networks vortex_charge as a STAT to every client; the port still has no cross-client charge stat (the wepent-props follow-up). |
| 262 | 3.6 | `mapobject-triggers` | `mapobject-triggers.multi.trigger_multiple` | logic:partial | MultiTouch is MISSING the pressedkeys check (CS(toucher).pressedkeys & this.pressedkeys, Base multi.qc:135-137) — Entity.PressedKeys now EXISTS (written by the dodging mutator) but MultiTouch (Triggers.cs:156-158) never reads it AND the trigger's own this.pressedkeys parse-field is unported, so the gate is skipped and a key-gated trigger fires without the player holding the keys (the only remaining MultiTouch logic gap; the comment at the gate site documents the omission); CTS client-specific .wait buffers (multi.qc triggertimes buf_create / bufstr_get keyed by client number) not ported: the port uses the single shared nextthink, so per-client re-trigger timing in CTS/Race diverges; q3compat wait defaults (0.5 / wait-forever) + q3compat red/blue spawnflag-1/2 team filter not applied (no q3 path) |
| 263 | 3.6 | `mapobject-triggers` | `mapobject-triggers.music.target_music` | values:partial | fade curve approximated: the client uses a two-AudioStreamPlayer A/B crossfade rather than QC's per-source state easing by frametime/fade_time (in) and frametime/fade_rate (out) |
| 264 | 3.6 | `sv-world-rules` | `sv-world-rules.checkrules.cascade` | logic:partial | WinningCondition_RanOutOfSpawns (team spawn-exhaustion draw/win, g_spawn_useallspawns) is still not ported (confirmed absent, GameWorld.cs:2884 comment).; leadlimit / leadlimit_and_fraglimit WIN semantics are still not evaluated in the world layer (the win comes from the gametype MatchEnded latch + tie report). The leadlimit math exists in GameScores.CountFragsRemaining but only feeds the frags-remaining announcer, not the end-of-match decision.; The CheckRules_World mutator hook (M_ARGV status override) is still not invoked. |
| 265 | 3.6 | `turret-flac` | `turret-flac.weapon.player_form` | liveness:dead | FlacWeapon is implemented but its [Weapon] registration is intentionally OFF (FlacTurret.cs:178): impulse 5 collides with the Raptor bomb-mode impulse and registering shifts WeaponById/MenuWeaponOrder, which would break those test contracts. So the hidden player FLAC weapon has NO live caller (liveness dead).; presentation partial: BLASTER_MUZZLEFLASH is now emitted (FlacTurret.cs:273) — but with except: actor, whereas Base flac_weapon.qc:30 uses Send_Effect (networks to ALL incl. the firing player, not Send_Effect_Except). Minor: a player firing their own hidden weapon won't see their own muzzle flash. Also the hagar trail RENDER is client-side (Godot project, outside qcsrc parity). |
| 266 | 3.5 | `bot-ai` | `bot-ai.weapon.wr_aim` | logic:partial | Only the primary attack is ever driven (ButtonAttack2 is hardcoded false at Emit, BotBrain.cs:374): the per-weapon wr_aim — SECONDARY fire, charge-up/release, projectile detonation and weapon combos — is never invoked, so bots never use alt-fire or detonate/combo. (UNCHANGED by Waves 1-2.); Because wr_aim is bypassed, weapon combos and the lastfiredweapon bookkeeping never happen; bots fire only the primary. (UNCHANGED.) |
| 267 | 3.5 | `mutator-buffs` | `buffs.swapper` | logic:partial | Core swap is implemented and LIVE: nearest enemy within range (DIFF_TEAM/dead/frozen/vehicle filtered), origin/velocity/angles + oldorigin exchanged, SND_KA_RESPAWN at both ends, buff_RemoveAll consumes the buff. The ForbidThrowCurrentWeapon hook has live Call sites (WeaponThrowing.cs:149/185).; Divergences from QC: uses center-distance + Api.Entities.FindInRadius instead of WarpZone_FindRadius (no warpzone traversal); omits Drop_Special_Items(closest) (flag/key carriers keep their objective on swap); omits MUTATOR_CALLHOOK(PortalTeleport, player); omits the EFFECT_ELECTRO_COMBO x2 visual at both endpoints. |
| 268 | 3.5 | `mutator-powerups` | `powerups.obituary_mutator_strings` | logic:partial | Death log does not append 'S' (Strength) / 'I' (Shield) item codes for a victim who held the powerup (LogDeath_AppendItemCodes unported — the port's kill log carries no :items= field). |
| 269 | 3.5 | `resources` | `resources.api.setresource` | logic:partial | Base's SetResource hook reads back BOTH res_type (M_ARGV(8) entity) and amount (M_ARGV(9)) after the forbid check, so a mutator may rewrite the RESOURCE TYPE as well as the amount. The port's CallSetResource (ResourceHooks.cs:57-64) only rewrites the amount (ref float); res_type is fixed. No shipped mutator rewrites res_type, so latent. |
| 270 | 3.5 | `resources` | `resources.api.take` | logic:partial | TakeResourceWithLimit STILL does NOT fire its own QC mutator hook (sv_resources.qc:211 MUTATOR_CALLHOOK(TakeResourceWithLimit)) — Resources.cs:172-182 clamps the drain and delegates to TakeResource but never calls a ResourceHooks chain (comment only; ResourceHooks has no TakeResourceWithLimit chain). It does delegate to TakeResource (which now DOES fire the TakeResource hook), so the inner hook still runs; only the WithLimit-specific hook is absent. No shipped mutator subscribes, so latent. |
| 271 | 3.5 | `scoring` | `scoring.winningcondition.remaining_frags_announce` | liveness:partial | PARTIAL MODE COVERAGE: Deathmatch (RecomputeLeader -> CountFragsRemaining), TDM (UpdateLeaderAndCheckLimit -> CountFragsRemaining) AND Keepaway (RecomputeLeader -> AnnounceFragsRemaining -> CountFragsRemaining, gated on g_keepaway_score_timepoints==0) now call it live. Base enables the Scores_CountFragsRemaining hook for 11 modes (ca/dm/duel/ft/ka/mayhem/rc/surv/tdm/tka/tmayhem — verified by grep of MUTATOR_HOOKFUNCTION over the gametype tree); the other 8 (ca/duel/ft/mayhem/rc/surv/tka/tmayhem) do NOT call CountFragsRemaining (Mayhem.cs:295 + TeamMayhem.cs:37 explicitly defer the suppression/announcer). So those modes play no 'N frags left' cue.; suddenDeathEnding is always passed false (all three callers note checkrules_suddendeathend is framework-owned and not surfaced), so the QC 'force fragsleft=1 during sudden-death overtime end' branch never fires. |
| 272 | 3.5 | `scoring` | `scoring.lifecycle.clear` | liveness:partial | STILL DEAD ARM: ClearPlayerOnJoin faithfully models the QC mode logic (0 keep / 1 wipe / -1 wipe-unless-PreferPlayerScore_Clear) but has ZERO call sites — grep over src+tests finds only the definition; no join/spawn/Teamplay path invokes it. So a player who specs out and rejoins still keeps their score regardless of g_score_resetonjoin (0/-1/1). No-op at the default 0; only diverges when an admin sets 1 or -1. |
| 273 | 3.3 | `turret-framework` | `turret-framework.mapents.path_trigger` | logic:partial | turret_targettrigger (TUR_FLAG_RECIEVETARGETS externally-fed targets, 0.5s debounce, turret_addtarget) not ported.; turrets_manager_think (g_turrets_reloadcvars: re-load every turret's per-unit cvars on a 1s think) not ported — consistent with the per-unit cvar table being absent.; turret_findtarget defendpoint wiring (resolve .target -> tur_defend, derive idle_aim) not wired from the spawnfunc (DefendPoint exists as a TurretParams field but no map entity sets it). |
| 274 | 3.2 | `mutator-buffs` | `buffs.networking` | liveness:unknown | Write/Read round-trip mirrors the QC major/minor bitmap + float time + byte flags, but whether the host net loop actually Flush()es buff state to clients each tick was not traced here (status_effects unit owns that). |
| 275 | 3.0 | `assault` | `assault.objective.waypoint_sprite` | presentation:missing | Presentation only: the objective waypoint sprite is now spawned, health-bar-updated, disowned-on-destroy, and ticked SERVER-SIDE (SpawnObjectiveSprite + WaypointSprites.Think, GameWorld.cs:1557). But there is NO client render/networking path — no code reads WaypointSprites.Active to serialize/render it (net+engine grep empty). So the attacker has no actual on-screen objective marker, health bar, or radar icon yet. The state machine is faithful; the visible pixels are absent. |
| 276 | 3.0 | `monster-mage` | `monster-mage.menu.describe` | values:missing | The mage's MENUQC describe() bestiary prose (mage.qc:515-527, 5 paragraphs on the homing sphere / shield+push / heal variants / teleport) is NOT surfaced in the port. No mage describe text exists anywhere in src/ or assets/; DialogMonsterTools is a spawn/edit tool, not a describe-tooltip surface (same as the spider/golem/wyvern describe rows). |
| 277 | 3.0 | `monster-spider` | `monster-spider.menu.describe` | values:missing | STILL OPEN. The spider's MENUQC describe() prose (spider.qc:256) is NOT surfaced in the port. DialogMonsterTools.cs is the spawn/edit tool (a radio group of monster netnames + Spawn/Remove/movetarget/recolor buttons) — it does NOT display any per-monster describe() tooltip text. No other port path surfaces the describe prose. |
| 278 | 3.0 | `nexball` | `nexball.goal.touch_scoring` | presentation:missing | No bprint / Send_Notification goal announcement (Goaaaaal! / own-goal / fault / out text).; GOAL_TOUCHPLAYER spawnflag is approximated: GoalTouchEntity always treats a touching ball-carrier as a hit (the port has no per-goal spawnflag bit), and the early BallTeam/BallPusher clear differs from QC (which keeps them through the glide) but is behaviorally equivalent under the ball.cnt!=0 guard. |
| 279 | 3.0 | `nexball` | `nexball.ball.dropball` | presentation:missing | No ground waypoint sprite on drop (client-deferred). |
| 280 | 3.0 | `onslaught` | `onslaught.cp.touch_build` | presentation:missing | Presentation still missing: no EFFECT_CAP/EFFECT_RAGE particles, no pad-model swap (MDL_ONS_CP_PAD1/2), no build-bar waypoint sprite.; Audio emitted: SpawnIcon→ONS_CONTROLPOINT_BUILD on the CP; build completion→ONS_CONTROLPOINT_BUILT. |
| 281 | 3.0 | `onslaught` | `onslaught.cp.icon_damage` | presentation:missing | Audio emitted: shielded→ONS_DAMAGEBLOCKEDBYSHIELD to attacker (debounced); per-hit ONS_HIT1/2; destroy→ONS_GENERATOR_EXPLODE + ONSLAUGHT_CPDESTROYED_NONAME info. Presentation still missing: no EFFECT_SPARKS/ROCKET_EXPLODE particles, no progressive icon model swap.; Proximity self-destruct nearby-enemy reward MODELED (IconDamage attacker==self branch: ONS_TAKES+1/SCORE+5 to every nearby enemy within proximitydecap_distance), closed alongside onslaught.cp.proximity_decap. |
| 282 | 3.0 | `onslaught` | `onslaught.gen.damage_destroy` | presentation:missing | Audio emitted: blocked-by-shield hint; GENERATOR_UNDERATTACK center + ONS_GENERATOR_UNDERATTACK (10s debounce); per-hit ONS_GENERATOR_EXPLODE at chance damage/220 else ONS_HIT1/2; destroy→ONSLAUGHT_GENDESTROYED(_OVERTIME) info + ONS_GENERATOR_EXPLODE. Presentation still missing: progressive damage `frame=10*(1-hp/max)`, flaming-gib/EFFECT_SPARKS particles, ons_camSetup death-cam.; NOTE: the per-hit 'gib impact' branch maps QC SND_ROCKET_IMPACT to ONS_GENERATOR_EXPLODE; under-attack alarm uses PlayGlobal not play2team. |
| 283 | 3.0 | `onslaught` | `onslaught.cp.proximity_decap` | presentation:missing | Proximity-decap IMPLEMENTED in IconThink: (friendly - enemy) live-player count within g_onslaught_cp_proximitydecap_distance changes icon HP by ±dps*THINKRATE each tick; at <=0 the icon is destroyed via IconDamage (lone enemy, or `self` to spread the multi-attacker +5 reward to all nearby enemies); a pure-friendly crowd repairs it. Master toggle defaults OFF in Base. Remaining: no waypoint/HUD presentation of the decap state. |
| 284 | 3.0 | `physics-player` | `physics-player.jetpack` | liveness:unknown | PM_jetpack maxspd_mod factor (player.qc:742 maxairspd *= max(1, maxspd_mod)) is now threaded through PMJetpack as maxspeedMod (PlayerPhysics.cs:886 `mp.MaxAirSpeed * MathF.Max(1f, maxspeedMod)`), so it is NO LONGER hardcoded to 1 — a spectator/high-speed-mod jetpack scales correctly. (Was a gap in the baseline; closed.) Residual: still unreachable in normal play (a real player has maxspeed_mod==1).; cl_jetpack_jump is pinned ON / mode-1 semantics (JetpackJumpEnabled const); QC mode 2 (latched thrust between frames) is not modelled. |
| 285 | 3.0 | `sv-chat` | `sv-chat.routing.recipients` | logic:partial | INGAME(it) round-mode semantics not modeled: the port has no per-player .ingame round flag, so IsIngame(p) == !IsObserver(p). In round/elimination modes (Clan Arena / LMS / Survival) an eliminated-but-still-INGAME player is treated as a spectator, so the spectator-downgrade (teamsay->-1) and the (IS_PLAYER\|\|INGAME) team-routing filter diverge from Base for those players. No effect on FFA/standard CTF/DM. |
| 286 | 3.0 | `sv-chat` | `sv-chat.ignore.crud` | logic:partial | Permanent (db-persisted) ignore tier is dropped (the QC return-2 path / ServerProgsDB by UID); ignores are keyed by PersistentId in-memory only (acceptable within a match, lost across server restarts). This is the sole remaining logic-partial reason. |
| 287 | 3.0 | `sv-cheats` | `sv-cheats.cmd.particles_make` | logic:partial | make: the bad-surface guard is weaker than Base. Base rejects when (trace_dphitq3surfaceflags & Q3SURFACEFLAG_NOIMPACT) OR trace_fraction==1; the port (Cheats.cs:348-353) only rejects on hit-nothing (fraction==1), so a NOIMPACT/sky surface would still place the breakable.; make: mode-1 surface-align is an APPROXIMATION — Base does fixedvectoangles2(normal, v_forward) then AnglesTransform_ApplyToAngles(angles,'-90 0 0'); the port (Cheats.cs:365-369) does FixedVecToAngles2 + a flat pitch -=90 (comment-acknowledged). The e.mdl='rocket_explode' wreck/explosion model is presentation-only and intentionally skipped.; penalty: only acts when the live gametype is Race (Cheats.cs:211); in non-race modes Base's race_ImposePenaltyTime is also effectively inert, so faithful, but the <reason> string's CSQC HUD notification is dropped (presentation-only). |
| 288 | 3.0 | `sv-client-lifecycle` | `sv-client-lifecycle.connect.client_connect` | logic:partial | Connect-side server->client channel deferred (see connect.fix_client_cvars_welcome row): no clientstuff/FixClientCvars/clientversion/_teams_available stuffcmds, no sv_notice_join/MOTD, no Handicap_Initialize/playban/chatban, no model_randomizer, no Send_NextMap.; Player_DetermineForcedTeam / forced-team-otherwise spectator routing not modeled (team is pre-assigned via AssignBestTeam only). |
| 289 | 3.0 | `sv-client-lifecycle` | `sv-client-lifecycle.spawn.put_player_in_server` | logic:partial | In-PutPlayerInServer team assignment (bot_forced_team / wants_join>0 SetPlayerTeam / TeamBalance_JoinBestTeam) is not run here — the port pre-assigns the team once at connect, so a mid-match team change or wants_join preference isn't honored on respawn.; wr_resetplayer + reload-all-reloadable-weapons-on-spawn (weapon_load = reloading_ammo) and CL_SpawnWeaponentity per slot are flagged as client/networking concerns and not done server-side.; FixPlayermodel resolves every player+bot to sv_defaultplayermodel (client playermodel cvar not plumbed); faithful stand-in but not the real per-client model. GiveRandomWeapons IS wired (randomCount>0 branch); the ForbidRandomStartWeapons hook is not dispatched. |
| 290 | 3.0 | `sv-client-lifecycle` | `sv-client-lifecycle.observer.put_observer_in_server` | logic:partial | Still omits: SelectObservePoint / observe-spawn placement (the port keeps the current origin instead), VoteCount/ReadyCount recount, the SPECTATE_WARNING / sv_maxplayers_spectator_blocktime block, CHAT_NOSPECTATORS notice, RemoveGrapplingHooks/Portal_ClearAll, alivetime PlayerStats report, and the is_forced/use_spawnpoint signature (the port has no is_forced/use_spawnpoint params). |
| 291 | 3.0 | `sv-client-lifecycle` | `sv-client-lifecycle.observer.observer_think` | logic:partial | SpectatePrev (impulse 12/16/19/220-229), the IMP_weapon_drop CAMERA_SPECTATOR cycle, MinigameImpulse handling, the +use clippedspectating MOVETYPE_FLY toggle, and the sv_spectate 2 'spectating not allowed' block are not modeled. |
| 292 | 3.0 | `sv-client-lifecycle` | `sv-client-lifecycle.observer.autojoin_delayed` | logic:partial | g_maxping / forced-team (TEAM_FORCE_SPECTATOR) / AutoJoinOnConnection mutator hook / (!teamplay \|\| g_balance_teams) gates deferred. The QC `ping > 0` precondition (delay until centreprints work) is not modeled. sv_spectate and g_campaign holds ARE honored. |
| 293 | 3.0 | `sv-client-lifecycle` | `sv-client-lifecycle.disconnect.client_disconnect` | logic:partial | Still not modeled: ignore-list cleanup, Portal_ClearAll/RemoveGrapplingHooks, chatbubble/killindicator/personal/g_counters delete, minigame part, WaypointSprite_PlayerGone, ReadyCount/VoteCount-on-leave, sv_hook_lastleave, and TeamBalance_RemoveExcessPlayers / QueuedPlayersTagIn. (Vehicle exit, player_powerups_remove_all + poweroff sound, the status-effect clear, the gametype objective-drop, and the generic MUTATOR_CALLHOOK(ClientDisconnect) ARE now modeled.) |
| 294 | 3.0 | `sv-impulse` | `sv-impulse.weapon.getcycle` | logic:partial | The impulse-group complain path is simplified: QC computes have_other and a custom-group bitmask for imp<0 (selection.qc:148-171); the port omits both since it drives the imp=-1 cycle/best path. The weaponsInMap (map-spawned-but-unowned weapon) branch (selection.qc:166,187) is unmodeled, so unowned-but-on-map weapons are skipped (cannot be cycle-selected before pickup). |
| 295 | 3.0 | `sv-impulse` | `sv-impulse.waypoints.sprites` | logic:partial | The _crosshair (31/35/38) and _death (32/36/39) variants reuse the player's ORIGIN — there is no server-side WarpZone crosshair trace or death_origin on this path, so all three personal/here/danger variants deploy at the player's feet rather than at the aimed/death location.; The .personal CHECKPOINT entity is not modeled, so the CTS/race clear self-kill (g_allow_checkpoints) resets the runner to the gametype's normal start/respawn spawn, not a stored personal-checkpoint origin. The HelpMePing mutator hook (waypoint_here_follow's `if (!MUTATOR_CALLHOOK(HelpMePing, this))` override) is not consulted — the port unconditionally attaches WP_Helpme. |
| 296 | 3.0 | `sv-intermission` | `sv-intermission.mapvote.server_core` | liveness:partial | There is still no NetControl.MapVote opcode, so the ballot/tally is not serialized over the wire and remote (--connect) clients can neither see nor cast. On a LISTEN server the cast path is live: a player's number-key 1..9 emits a C2S impulse, which Commands.DispatchImpulse intercepts while MapVoting.Running and routes to MapVoting.CastVote(caller, imp-1) before the weapon-impulse path. For a dedicated/remote-client server the vote still closes on timeout/PlayerCount with zero real votes (deterministic RNG tiebreak). |
| 297 | 3.0 | `sv-intermission` | `sv-intermission.mapvote.client_ui` | liveness:partial | The panel is fed only from the in-process GameWorld.MapVote (NetGame.UpdateMapVotePanel reads _serverWorld?.MapVote), so it lights up ONLY on a listen server. A pure --connect client has no in-process MapVoting and no NetControl.MapVote decode, so its ballot stays blank and it cannot vote — full remote parity still needs the networked opcode (see sv-intermission.mapvote.server_core).; The panel has no mouse/cursor click handler (Base MapVote_InputEvent lets you click a map tile); voting is by number-key 1..9 only. Faithful enough to vote, but the click-to-vote affordance is absent. |
| 298 | 3.0 | `sv-ipban` | `sv-ipban.enforce.on_connect` | logic:partial | Base uses Ban_MaybeEnforceBanOnce (a one-shot .ban_checked flag) AND re-checks on the client's first command (cmd.qc:1181); the port enforces only once on connect (GameWorld.cs:1144 InfraClientConnect) with no first-command re-check. Low impact (connect-time drop is dominant). |
| 299 | 3.0 | `sv-ipban` | `sv-ipban.insert` | logic:partial | The dosync leg (OnlineBanList_SendBan, with the reason!="" and not-~-prefixed guard) is dropped — no online ban propagation on insert (see sv-ipban.sync.online). |
| 300 | 3.0 | `sv-mapvoting` | `sv-mapvoting.gametype_vote` | logic:partial | The hooks DON'T fire: GameWorld.ApplyGametypeSwitch calls Commands.GameTypeVoteHookHandler?.Invoke("all")/(gt) (GameWorld.cs:3397-3398) but GameTypeVoteHookHandler is NEVER assigned (grep: no `GameTypeVoteHookHandler =`), so the sv_vote_gametype_hook_all/_<name> localcmd aliases are no-ops. Stock hooks are empty aliases (gametypes-server.cfg) so the visible effect is nil, but a server.cfg that defines a hook (e.g. g_maxplayers tweak) would be silently dropped.; GameTypeVote_AvailabilityStatus' next-map availability filter is absent: StartGametype marks every resolvable type GTV_AVAILABLE (MapVoting.cs:298-300) without the MapInfo_Get_ByName(get_nextmap()) + supported-gametypes check, so an option that the queued nextmap can't support is shown available instead of dimmed (documented in the StartGametype comment).; Maplist-reset enumerate fallback absent: ApplyGametypeSwitch sets g_maplist only from a per-gametype sv_vote_gametype_<name>_maplist cvar; with maplist_reset=1 but no per-gametype list Base regenerates g_maplist via MapInfo_ListAllowedMaps, the port keeps the current list (GameWorld.cs:3407-3408).; The match_gametype temp-switch / restart-reapply dance (MapVote_Init's `if (match_gametype) GameTypeVote_SetGametype(...)`) is not modeled; the port applies the resolved gametype to the `gametype` cvar for the next boot (GameWorld.cs:3393) rather than live-switching MapInfo mid-process. |
| 301 | 3.0 | `sv-mapvoting` | `sv-mapvoting.client.draw` | liveness:partial | Panel is FED on a listen server: NetGame.UpdateMapVotePanel calls SetVote/SetVotes/SetAvailability/OwnVote/SetWinner and owns Visible (NetGame.cs:3393-3484), so the player sees the ballot grid, level-shots, live counts, own-vote highlight, the selection pulse, the top-2 reduce-fade, and the winner reveal during a real listen-server match.; The GametypeVote DRAW path is now ALSO driven: UpdateMapVotePanel passes gametypeVote: isGametypeVote (NetGame.cs:3441) and feeds gametype icons (gfx/menu/<skin>/gametype_<name>), pretty names + descriptions + sv_vote_gametype_detail (NetGame.cs:3421-3444), so GameTypeVote_DrawGameTypeItem's counterpart renders during a real gametype vote (was 'never driven' in the baseline).; The mv_top2_alpha reduce-fade is now driven: SetAvailability stamps _top2Time on first reduce (NetGame.cs:3460 -> MapVotePanel.cs:281-290).; STILL DEAD for remote/dedicated-server clients: no net entity feeds the panel when _serverWorld is null (see sv-mapvoting.net.sync), so a non-listen client never sees the vote screen (liveness partial). |
| 302 | 3.0 | `sv-spawnpoints` | `sv-spawnpoints.select.select_spawn_point` | logic:partial | Emergency fallback differs: Base, when Spawn_FilterOutBadSpots returns empty, RE-FILTERS the spot list once with targetcheck=false (server/spawnpoints.qc:421-435) before giving up. The port does run a targetcheck=false re-filter (SpawnSystem.cs:386-387), but then seeds every spot at (prio 0, weight 0) if even that comes back empty (SpawnSystem.cs:392-396), so the assault/active gate is relaxed once but a wrong-team / all-embedded list still falls through to a prio-0 fallback. UNCHANGED.; anypoint / g_spawn_useallspawns path: Base skips Spawn_FilterOutBadSpots entirely and does Spawn_WeightedPoint(1,1,1) over ALL spots (spawnpoints.qc:413-416). The port mirrors this (SpawnSystem.cs:366-372: seeds all spots at prio0/weight0, NO startsolid/team/active/restriction filtering), so this matches Base — the earlier note that the port still ran ScoreSpot on the anyPoint path is now obsolete. UNCHANGED (faithful).; testplayerstart debug edict (spawnpoints.qc:363-370) — Base returns a 'testplayerstart' classname spot before any other logic; not ported (debug-only). UNCHANGED.; info_player_survivor spawn class (Base spawnpoints.qc:182, aliases info_player_deathmatch with no team) is MISSING from SpawnSystem.SpawnClassNames (SpawnSystem.cs:63-71) and handled nowhere in the port — a Survival-mode map using info_player_survivor spots would have them ignored by GatherSpawnPoints. Low severity (Survival mode is not ported). NEW (2026-06-25 re-run). |
| 303 | 3.0 | `sv-spawnpoints` | `sv-spawnpoints.mutator.spawn_near_teammate` | logic:partial | Residual relocation-branch defers: tracebox_hits_trigger_hurt (hurt-volume reject), checkpvs (PVS visibility), and the g_nades nade-in-range reject are still skipped (a faithful superset that may keep a spot QC would reject); weaponLocked and PHYS_INPUT_BUTTON_CHAT gates are always false (no plumbed input/weapon-lock); closetodeath uses the player's current origin not .death_origin. The sky-surface (Q3SURFACEFLAG_SKY) and lava/slime/water pointcontents rejects ARE applied. STILL OPEN (the residual defers), but no change since baseline. |
| 304 | 3.0 | `sv-teamplay` | `sv-teamplay.admin.lock_shuffle_move` | logic:partial | moveplayer/team don't enforce lockteams backup/restore semantics of MoveToTeam (which temporarily disables the lock for the move then restores it); behavior overlaps but isn't the exact MoveToTeam path. |
| 305 | 3.0 | `sv-world-rules` | `sv-world-rules.warmup.reset_map` | logic:partial | No per-client accuracy_reset / Inventory_clear pass on reset_map.; The waypoint/assault .reset2 second pass (FOREACH_ENTITY_FLOAT_ORDERED two-stage ordering) is not separately driven — only the single .reset callback per entity fires. |
| 306 | 3.0 | `sv-world-rules` | `sv-world-rules.misc.shutdown` | logic:partial | ServerProgsDB persistence on shutdown (db_save/db_dump of server.db<sessionid>) has no port analogue.; bot_endgame, CheatShutdown, WeaponStats_Shutdown and MapInfo_Shutdown are not performed on world teardown. |
| 307 | 3.0 | `tka` | `tka.ball.drop` | presentation:missing | DropBall is invoked from OnDeath (LIVE death hook) when the victim carried. It detaches, drops at the feet with the crandom xy scatter, arms the 0.5s self-recapture lockout, re-arms the relocate think, plays SND_KA_DROPPED, sends INFO/CENTER_KEEPAWAY_DROPPED, and (via CollectWaypoints next tick) shows the loose-ball waypoint.; FIXED (liveness partial -> live): the use-key, observer, and disconnect drop triggers are now wired. OnUseKey subscribes MutatorHooks.PlayerUseKey (Activate, TeamKeepaway.cs:387) and is fired live via VehicleBoarding.cs:106 FirePlayerUseKey on the +use impulse path (QC tka PlayerUseKey -> tka_DropEvent, consumes the press). OnPlayerRemoved (TeamKeepaway.cs:406) drops the ball when the carrier is demoted to observer or disconnects, dispatched from ClientManager.cs:513 (PutObserverInServer/MakePlayerObserver) + :761 (ClientDisconnect) — QC MakePlayerObserver/ClientDisconnect both tka_DropEvent.; RESIDUAL: DropSpecialItems (the special-item-strip drop edge) is still not wired — 3 of Base's 4 drop hooks are now live. Also: no nudgeoutofsolid_OrFallback, no multi-ball chain advance / tka_PlayerReset VIP clear. |
| 308 | 3.0 | `turret-framework` | `turret-framework.lifecycle.round_reset` | logic:missing | No round-reset for turrets: Base wires .reset = turret_reset (turret_link:1342) so every turret re-runs turret_respawn (full health/ammo/volley/head, re-active) at round restart in round-based gametypes (Arena/CA/LMS/Freezetag/etc.). The port has TurretAI.Respawn but never calls it on a round reset — a damaged/dead turret carries its state across rounds. |
| 309 | 3.0 | `turret-fusionreactor` | `turret-fusionreactor.fx.attack` | liveness:partial | No client turret render (ENT_CLIENT_TURRET): the reactor renders no model and no head, so the ammo-scaled head spin (Base '0 250 0'*ammo/ammo_max, up to 250 deg/s) is computed onto TurretState.HeadAVelocity but never integrated/displayed. No waypoint sprite/healthbar/death FX either.; te_smallflash now IS emitted at each recipient's bbox centre (EffectEmitter.TeSmallflash), but it resolves to a generic BLASTER_MUZZLEFLASH-style flash, not the networked DarkPlaces te_smallflash temp-entity — presentation approximation. |
| 310 | 3.0 | `turret-fusionreactor` | `turret-fusionreactor.mutator.validtarget_hook` | logic:missing | The FusionReactor_ValidTarget mutator hook (lets a mutator force-accept/reject a recharge target) is not implemented; no stock Xonotic mutator registers it, so it is presently inert in Base too. |
| 311 | 3.0 | `weapon-arc` | `weapon-arc.beam.burst_variant` | presentation:missing | ARC_BT_BURST visual thickness (14) and burst beam color/visuals absent (depends on the beam.visual feature). UNCHANGED. |
| 312 | 3.0 | `weapon-blaster` | `weapon-blaster.bolt.think_lifetime` | presentation:missing | CSQCProjectile(PROJECTILE_BLASTER) client trail/sprite STILL not networked: the flying bolt has no DP-style render trail (Wave-1/2 added no client-render seam for the bolt). |
| 313 | 3.0 | `weapon-devastator` | `weapon-devastator.menu.describe` | presentation:missing | The MENUQC weapon-guide paragraphs (remote-controlled rocket, hold-to-guide, ammo drain, rocket-flying with the rocketflying mutator, keybinds, DPS) are not ported — the port has no in-game weapon-guide page for the Devastator. |
| 314 | 3.0 | `weapon-tuba` | `weapon-tuba.describe.guide` | presentation:missing | The MENUQC weapon-guide describe paragraphs (tuba.qc:600-612 — close-range ear-bleeding music, secondary higher pitch, breath-only ammo, movement-key pitch, keybinds, DPS line) are not ported; the port has no in-menu weapon guide for the tuba. |
| 315 | 3.0 | `weapon-vaporizer` | `weapon-vaporizer.rm.primary_explosion` | liveness:unknown | Liveness of g_rm in a shipped/normal match (a map or mutator that sets g_rm) not traced end-to-end. |
| 316 | 2.8 | `bot-ai` | `bot-ai.aim.lead` | logic:partial | Ballistic aim is a closed-form gravity-drop approximation, not QC's 10-trace tracetoss search; lobbed-weapon aim (mortar/nade) is less accurate, especially with obstacles in the arc, and never traces the world to confirm the toss lands. (UNCHANGED.); Shot speed is read from g_balance_<wep>_primary_speed/gravity for the single ChosenWeapon, not per-weaponentity with W_WeaponSpeedFactor applied; weapons without those cvars are treated as hitscan (no lead). (UNCHANGED.) |
| 317 | 2.8 | `bot-ai` | `bot-ai.weapon.choose` | logic:partial | Weapon picking is a hitscan-at-range / splash-up-close bucket choosing the highest-impulse owned weapon, not the QC ordered far/mid/close priority lists. (UNCHANGED.); No weapon-combo behaviour (bot_ai_weapon_combo): bots don't hold a weapon for a follow-up combo shot. (UNCHANGED.); No bot_rangepreference shift, so per-bot 'sniper vs spammer' weapon-range personalities are absent. (UNCHANGED.) |
| 318 | 2.8 | `bot-ai` | `bot-ai.roles.goalrating` | logic:partial | Goal rating still uses straight-line inverse distance (f*rangebias/(rangebias+vlen(dist))) instead of the QC waypoint-cost (Dijkstra) field; bots can prefer a goal near in a straight line but far by path. (UNCHANGED — GoalRater.Rate at BotRoles.cs:58 is still vlen.); rangebias is still hardcoded 2000 for every rate call (items/enemies/roam) instead of the per-rating-call bias QC uses. (UNCHANGED.); No separate _movingtarget (5.5s) strategy interval for goals that can move. (UNCHANGED.); enemyplayers aggression refinement: QC skill>3 nudges the t factor by Strength/Shield powerup timers (StatusEffects_gettime); the port has no status-effect-expiry accessor yet, so that refinement is deferred. |
| 319 | 2.8 | `bot-ai` | `bot-ai.move.steer` | logic:partial | The brain's danger-check JumpStepHeight (BotNavigation const = 48) is still a hardcoded constant, and is even inconsistent with Base's jumpstepheightvec (stepheight + jumpheight_vec*0.85 ≈ 70 @ stock) and with the live Steer reach (StepHeightLive + JumpHeightApex, missing the ×0.85). Non-default physics adjusts the Steer obstacle-jump but not this const.; QC's full movetogoal handling of teleport/ladder/box-volume waypoints, water swim-up, and the per-goal stop/turn logic is partially collapsed; complex waypoint traversal is less robust than stock. (UNCHANGED.) |
| 320 | 2.8 | `cl-crosshair` | `cl-crosshair.color.special_modes` | logic:partial | crosshair_color_special 3 (rainbow: randomvec()*brightness re-rolled every rainbow_delay, crosshair.qc:189-196) is not ported — ColorModeCvar (CrosshairPanel.cs:537-542) maps only 0/1/2, so mode 3 silently falls back to fixed color. The rainbow_brightness/delay cvars are registered but dead. |
| 321 | 2.8 | `cl-crosshair` | `cl-crosshair.objring.nade_capture_revive` | liveness:partial | PARTIAL FIX: REVIVE_PROGRESS is now fed (NetGame:2328, _fullHud.Crosshair.ReviveProgress = LocalServerPlayer?.ReviveProgress) alongside NadeTimer (2327), both host-only inside `if (_server is not null)`. Player.ReviveProgress is genuinely mirrored each tick by FreezeTag (FreezeTag.cs), so the host player's thaw ring now renders. STILL OPEN: CAPTURE_PROGRESS is never assigned on any production path (grep finds only test-side .CaptureProgress= plus the networked NetEntity.CaptureProgress field which ServerNet.cs:1741 explicitly leaves 0 with a comment that no gametype sets it) so the capture ring is DEAD; and although NetEntityState.ReviveProgress IS networked to remote clients (ServerNet.cs), NOTHING reads it back into the panel client-side — a pure remote client gets no nade/thaw ring.; Rings drawn centred on the crosshair panel (radius 64px) rather than at Base's `0.5*conwidth, 0.6*conheight` screen anchor with `0.1*conheight` radius; nade-timer text label (cl_nade_timer 2) not drawn. |
| 322 | 2.8 | `cl-csqcmodel` | `cl-csqcmodel.predraw.dispatch_order` | logic:partial | Order is appearance->LOD->effects (skeletal anim runs in a separate PlayerModel.Pose pass, not interleaved here); the appearance-before-LOD ordering is moot because LOD is a no-op swap.; AutoTagIndex recursion + drawmask=0 on null-model are not part of this pass (held weapons attach via ViewEntityRenderer). |
| 323 | 2.8 | `cl-hud` | `cl-hud.panel.radar` | liveness:partial | Maximized clickable mode (ons spawn / teleport select) is still unported; the discovered radar panel is force-hidden (StartHiddenIds) to avoid a double radar and the live radar is the standalone _radar. |
| 324 | 2.8 | `cl-scoreboard` | `cl-scoreboard.field.formatting` | logic:partial | SP_FPS and SP_SKILL columns never render (FieldByLabel returns null for fps/skill — client-only display fields with no port plumbing); the GetField FPS/SKILL branches exist (ScoreboardPanel.cs:684-697) but are unreachable because the columns are never fed.; scores_per_round averaging is now plumbed (ScoresPerRound + per-field roundsPlayed division), but no interactive scores_per_round toggle (Ctrl+R) — only a console cvar drives it. |
| 325 | 2.8 | `cl-shownames` | `cl-shownames.gate.self_spectatee` | liveness:partial | Chase own-tag for the truly-local player still can't draw: the predicted local player is never in ClientNet.RemoteIds (HandleSnapshot skips LocalNetId), so the self branch only fires for the spectatee. In stock play this matters only for hud_shownames_self + chase_active on your OWN model in third person — a residual deferral; the spectatee path (the common observed case) is live. |
| 326 | 2.8 | `fx-effectinfo` | `fx-effectinfo.emit.by_name_fallback` | liveness:partial | The Effect=null engine-fallback request (unregistered effectinfo name) records the name but there is no DP-engine __pointparticles equivalent client-side; only names that also resolve in the effectinfo.txt catalog render. Used by te_csqc_lightningarc/te_beam (arc_lightning/heal_beam) which are handled by the beam renderer rather than the registry. |
| 327 | 2.72 | `cl-hud` | `cl-hud.panel.itemstime` | values:unknown | Progressbar / dynamicsize / ratio-columns fidelity not runtime-verified. |
| 328 | 2.72 | `cl-hud` | `cl-hud.panel.strafehud` | values:unknown | Bar zones / best-angle marker / wturn-switch indicator + mode/style/range fidelity not runtime-verified. |
| 329 | 2.7 | `mapobject-target` | `mapobject-target.speaker.ambient_triggered` | logic:partial | ACTIVATOR (BIT3) still plays to ALL players, not just the triggering client — SpeakerUseActivator gates on IsRealClient (a bot/world activator produces nothing, matching QC's early-out) and emits on the stacking CH_TRIGGER channel, but there is no MSG_ONE / per-client sound facade, so every client hears it (documented TODO at TargetSpeaker.cs:211).; The *-prefixed per-player voice-message sample now RESOLVES (ResolveNoise routes a *-name through Sounds.PlayerSoundSample + the wired ModelSoundResolver, which handles argv(1)-style variant-count randomization) — but it requires a real-client activator with a model; an ambient/null/no-model invocation still falls back to silence (matching QC's SND(Null) not-found). Plays to all clients (no MSG_ONE), not just the triggerer.; QC objerrors a LOOPED_OFF untargeted speaker; the port silently leaves it inert. |
| 330 | 2.7 | `monster-spider` | `monster-spider.anim.mr_anim` | liveness:partial | CLIENT DPM PLAYBACK STILL MISSING. The server now maps the logical MonsterAnim phase to the spider's DPM frame-group start index (Spider.AnimFrame) and stamps it onto the networked Entity.Frame each think (MonsterAI.DriveAnimFrame, called in RunThink/MarkPain/MarkDead) — this IS live and the frame values match mr_anim exactly. BUT the client renders the spider's spider.dpm via ClientWorld.EntityModelFactory -> NetGame.BuildEntityModel -> AssetLoader.LoadModel -> DpmBuilder (an AnimationPlayer-backed node that is NOT bound to the Entity and NEVER consumes the networked Entity.Frame). Only the MD3 morph path (ClientWorld ModelAnimator with FollowEntityFrame) honours Entity.Frame, and ResolveEntityModel returns null for a DPM, so the spider never takes that path. Net: the server anim PHASE is correct + networked, but the visible DPM model does not play walk/bite/web/pain/die. Shared monster-presentation gap (all DPM/IQM monsters). |
| 331 | 2.7 | `turret-walker` | `turret-walker.drive.gaits_extra` | logic:partial | No standalone STRAFE_L/R or PAIN gaits (Base never drives these from the SVQC brain either — they are leftover model anim states — so this is parity-by-omission, not a behavioral gap).; JUMP/LAND/TURN gait cases exist in ApplyGait but are never assigned (same as Base — the brain never sets them). |
| 332 | 2.5 | `mutator-bugrigs` | `mutator-bugrigs.serverinfo.mutator_strings` | liveness:partial | BuildMutatorsPrettyString chain (MutatorActivation.BuildMutatorsPrettyString) has NO live consumer in the port — no scoreboard/votescreen/endmatch reader calls it — so the ', Bug rigs' pretty token, though implemented and correct, is never displayed. The machine ':bugrigs' token IS live (consumed at gamelog init, GameWorld.cs:876). |
| 333 | 2.5 | `mutator-campcheck` | `campcheck.realclient.gate` | liveness:partial | Chat-typing exemption is inert on the live net path: the gate term `(TypeCheck \|\| !ButtonChat)` exists and g_campcheck_typecheck is now consulted, and ButtonChat is bridged from input.Typing — but the wire never carries the typing flag (no InputButtons.Chat bit; ServerNet.ToMovementInput omits Typing), so ButtonChat is always false for real network clients and a typing player is still not actually exempted. Gate logic is faithful; the carrier signal is partially-live (only the interface default reaches the server).; weaponLocked gate is the freeze subset only: FrozenStat \|\| STATUSEFFECT_Frozen is checked (matching WeaponFireDriver.WeaponLocked), but the broader QC weaponLocked() pieces (player_blocked, the LockWeapon mutator hook) are not modeled. |
| 334 | 2.5 | `mutator-globalforces` | `mutator-globalforces.advertise.mutatorstring` | liveness:partial | BuildMutatorsPrettyString chain (MutatorActivation.cs:121) has NO live caller in src/XonoticGodot.Server (grep 'BuildMutatorsPrettyString(' returns no Server callsite), so the ', Global forces' pretty token is never emitted — the QC consumer is server/client.qc:1107 (scoreboard/votescreen mutator line). The machine token ':GlobalForces' IS live (GameWorld.cs:876 GameLogInit). Cosmetic only; no gameplay effect. |
| 335 | 2.5 | `mutator-multijump` | `mutator-multijump.presentation.mutator_list_string` | liveness:partial | BuildMutatorsPrettyString (', Multi jump') override and MutatorActivation.BuildMutatorsPrettyString aggregator both exist, but the aggregator has NO live consumer in the Server project -- nothing calls BuildMutatorsPrettyString() to feed a scoreboard/votescreen line (grep over src/XonoticGodot.Server: zero callers). So the human-readable label never reaches a UI. The machine ':multijump' tag IS live (consumed by gamelog init, GameWorld.cs:876, gated by sv_eventlog). |
| 336 | 2.5 | `mutator-nades` | `mutator-nades.boom.dispatch` | presentation:missing | The colored explosion particle (Send_Effect_Except with per-type EFFECT + player-color min/max via findbetterlocation) is omitted (render-only).; Base plays SND_Null on CH_SHOTS_SINGLE + SND_ROCKET_IMPACT on CH_SHOTS; the port plays only weapons/rocket_impact.wav. |
| 337 | 2.5 | `mutator-nades` | `mutator-nades.damage.freezetag_revive` | logic:partial | The port suppresses the self-nade damage+force but does NOT call freezetag_Unfreeze / set freezetag_revive_nade_health / send EFFECT_ICEORGLASS + INFO_FREEZETAG_REVIVED_NADE/CENTER_FREEZETAG_REVIVE_SELF notifications (documented cross-task seam — FreezeTag owns the unfreeze). So the revive-nade does not actually revive. |
| 338 | 2.5 | `mutator-nix` | `mutator-nix.add.wrinit` | logic:partial | Port resets the rotation globals (_nixWeapon/_nixNextWeapon/_nixNextChange = 0) and reads cvars (Hook lines 81-94), but does NOT run the `FOREACH(Weapons, choosable, it.wr_init(it))` warm-init pass. QC itself comments the per-rotation re-init as 'too slow' and skips it; the ONADD warm is cosmetic/perf and has no observable gameplay effect. |
| 339 | 2.5 | `mutator-nix` | `mutator-nix.rotation.engine` | logic:partial | wpn.wr_resetplayer (weapon think-state reset) is still not called on the per-round resync (QC sv_nix.qc:166). The full-clip prefill for reloadable weapons (QC weapon_load[nix_weapon] = wpn.reloading_ammo across slots) IS now ported (NixMutator.cs:275-289 seeds slot-0 weapon_load + tops the live clip), closing that half of the prior gap. Observable outcome largely preserved but not bit-identical (wr_resetplayer omitted). |
| 340 | 2.5 | `mutator-overkill` | `overkill.respawn.remember_restore_weapon` | logic:partial | Port records/restores only slot 0: OnPlayerDies stores Inventory.CurrentWeapon(target) into OkLastWeapon[0] and nulls slots 1..MAX_WEAPONSLOTS; Base captures every slot's m_switchweapon (ok_lastwep[MAX_WEAPONSLOTS]). Low practical impact — Overkill loadouts occupy slot 0 only — but the per-slot capture is not faithfully reproduced. UNCHANGED by Wave-2. |
| 341 | 2.5 | `mutator-random_gravity` | `random_gravity.roll.round_gate` | liveness:partial | Liveness is partial-by-design: RoundNotStartedProvider is wired only inside GameWorld.EnableRounds (GameWorld.cs:1134), i.e. only for round-based gametypes (CA/Freezetag/round Domination/…). In non-round modes the provider is null and RoundGateBlocks() returns false — which exactly matches QC where round_handler_IsActive() is false outside round modes. So the gate is correctly inert where Base is inert, and active where Base is active. Not a defect, just mode-scoped reachability. |
| 342 | 2.5 | `mutator-random_gravity` | `random_gravity.advertise.mutator_string` | liveness:partial | BuildMutatorsPrettyString chain has NO live caller: MutatorActivation.BuildMutatorsPrettyString (MutatorActivation.cs:121) and the per-mutator override (RandomGravityMutator.cs:113) both exist, but grep finds no invocation of the chain anywhere in the port. So the human-readable active-mutator list (scoreboard / votescreen / server-browser pretty field) never includes ", Random gravity". The machine-readable ":RandomGravity" token IS live (BuildMutatorsString chain called at GameWorld.cs:876 during gamelog init, gated on sv_eventlog). |
| 343 | 2.5 | `mutator-random_items` | `random_items.engine.mod_injection_hook` | liveness:partial | The Instagib override (sv_instagib.qc:102, substituting vaporizer_cells/extralife) is NOT wired: InstagibMutator has no RandomItemsGetClassName subscriber. Deferred until those Instagib items are ported (their classnames cannot resolve to a Pickup yet). Under instagib the random pool falls through to vanilla. |
| 344 | 2.5 | `mutator-sandbox` | `mutator-sandbox.object.duplicate` | liveness:partial | WIRED: copy/paste reachable via CmdSandbox → HandleCommand; PASTE is fully live (flood/cap/ObjectPortLoad).; COPY clipboard STILL inert: SandboxObjectStore.SetClipboard is an empty body (NetGame.cs:985) — the port has no svc_stufftext channel to set a remote client's cvar. Paste from a manually-set cl_sandbox_clipboard works.; Paste shares the flood + maxobjects gates with object_spawn (faithful). |
| 345 | 2.5 | `mutator-spawn_near_teammate` | `mutator-spawn_near_teammate.relocate.eligibility` | logic:partial | weaponLocked(teammate) gate is always false in the port (WeaponLocked returns false, line 462; no per-player weapon-lock reachable): may relocate beside a mate Base would skip. (Unchanged from baseline — still open, minor.); PHYS_INPUT_BUTTON_CHAT (teammate typing) gate is always false in the port (IsChatting returns false, line 466; no chat-button input plumbed): may relocate beside a typing mate Base would skip. (Unchanged from baseline — still open, minor.) |
| 346 | 2.5 | `mutator-stale_move_negation` | `smneg.mutator_string` | liveness:partial | BuildMutatorsPrettyString chain is DEAD: MutatorActivation.BuildMutatorsPrettyString exists and the smneg override returns ', Stale-move negation', but no server code invokes the pretty-string chain (QC server/client.qc:1107 votescreen/scoreboard consumer not ported). So the pretty label never reaches any UI. |
| 347 | 2.5 | `mutator-waypoints` | `waypoints.registry.defs` | liveness:partial | Residual: the wp_extra-DRIVEN spritelookupblinkvalue overrides (superweapon Weapon=2, Item=item.m_waypointblink) are not modeled — Weapon's def blink is a flat 1 and there is no per-instance wp_extra blink lookup. Harmless while those producers (superweapon pickups) are absent. The Seeker/Item/Weapon DEFS are now exercised by live producers; only the wp_extra per-instance blink override is unmodeled.; liveness: partial — defs are reachable only once their gametype emits waypoints; CTF + Domination + FreezeTag + Keepaway + TeamKeepaway + Assault + Key Hunt + LMS + itemstime (Item/Weapon) + Seeker + player-deployed pings now exercise their defs. Still NOT on the live path: Ons*/Nb*/Race*/Monster/Vehicle*/Buff. |
| 348 | 2.5 | `mutator-waypoints` | `waypoints.server.manager_api` | liveness:partial | DEPLOY + CLEAR + HELPME slice is live (DeployFixed/DeployPersonal/Attach/ClearPersonal/ClearOwned/HelpMePing from Commands.cs; Reset() + Think() in GameWorld; SendWaypoints merges WaypointSprites.Active per peer).; OBJECTIVE/UPDATE slice is BROADLY live now: SpawnFixed/Spawn/UpdateMaxHealth/UpdateHealth/Disown/Kill via Assault (SpawnObjectiveSprite); Spawn/UpdateBuildFinished/Ping/Kill via itemstime (ItemPickupRules.RespawnCountdown, ItemPickupRules.cs:757/768/783); Spawn/UpdateRule/Kill via Seeker (Seeker.cs:496/514/519); Spawn/UpdateMaxHealth/UpdateHealth/Kill via powerup drops (PowerupsMutator.cs:541-575). FIX vs baseline which marked Ping/UpdateBuildFinished/UpdateRule zero-caller dead.; Still NO live caller: AttachCarrier, DetachCarrier, UpdateTeamRadar — the CTF/Dom/FreezeTag/Keepaway/KeyHunt/LMS producers build WaypointSprite records directly in CollectWaypoints (not via the manager carrier helper). |
| 349 | 2.4 | `monster-framework` | `monster-framework.move.brain` | logic:partial | movelib_move_simple / movelib_brake_simple are approximated with a fixed 0.4 lerp / (1 - stopspeed*0.01) decay 'so behavior is stable regardless of frametime' rather than the QC frametime-scaled accel — timing/feel diverges slightly. (UNCHANGED.); steerlib_attract2 (the QC steering blend toward moveto with leadahead 500/0.95) is replaced by a direct (moveto-origin) yaw turn; the steer-prediction smoothing is not modeled. (UNCHANGED.); The MatchHalted brake-and-idle halt is LIVE (RunThink->Move on every monster Think); MatchHalted reads the live VehicleCommon.GameStopped (per-tick server write), so monsters brake-and-idle for prematch AND intermission/timeout. DRIFT FIX (2026-06-25): the campaign bots-may-start movement hold (QC sv_monsters.qc:846 autocvar_g_campaign && !campaign_bots_may_start) is now LIVE too — CampaignMovementHeld() reads MonsterAI.CampaignBotHold, wired at GameWorld.cs:686 to (g_campaign && !Campaign.BotsMayStart). MonsterMove mutator hook + draggedby remain deferred host concerns; the movelib/steerlib approximations remain (the residual partial). |
| 350 | 2.4 | `sv-commands-votes` | `sv-commands-votes.vote.call` | presentation:partial | ANNCE_VOTE_CALL announce + the networked Nagger vote HUD are not emitted by this core (broadcast text only). |
| 351 | 2.4 | `sv-commands-votes` | `sv-commands-votes.vote.resolve` | presentation:partial | ANNCE_VOTE_ACCEPT / ANNCE_VOTE_FAIL announcer cues are not played (presentation/audio handled in host, not invoked here).; VoteSpam drops the QC 'didn't have to vote' wording variant (always prints 'didn't vote'); cosmetic broadcast-text divergence. |
| 352 | 2.4 | `sv-teamplay` | `sv-teamplay.nagger.team_nagger` | presentation:missing | No Nagger network entity / center-print: warmup is now HELD while teams are unbalanced, but players are never SENT the unbalanced-teams warning (no SendFlags bits 5/6, no center-print). The visible nag is absent.; Warmup-hold uses SizeDifference over the full roster; Base TeamBalance_SizeDifference(NULL) is the same NULL-ignore form, so the hold trigger is faithful. |
| 353 | 2.4 | `sv-world-rules` | `sv-world-rules.warmup.serverflags` | presentation:missing | Client-side consumer not ported: nothing reads the published serverflags bits to actually gate fullbright player rendering (Base client/view.qc:1115) or hide the HUD item-pickup timer (Base client/hud/panel/pickup.qc:91). The server publishes ServerFlags.Value + the `serverflags` cvar mirror, but no port render/HUD code consumes it — so the two bits have no player-observable effect yet (presentation: missing). |
| 354 | 2.4 | `sv-world-rules` | `sv-world-rules.boot.lightstyles` | presentation:missing | Client render consumer not ported: nothing samples LightStyles.Sample to modulate map-light/dynlight brightness per frame, so a map light driven by a style index still does not visibly animate. The server-side table install is present, but the presentation (the actual light animation) is unconsumed (DynamicLightRenderer renders a steady radius). |
| 355 | 2.25 | `assault` | `assault.mapobject.assault_wall` | timing:partial | Timing: QC re-evaluates each func_assault_wall on a 0.2s nextthink; the port re-evaluates every tick (DriveWalls from the per-frame DriveFrame) but only re-stamps the edict on a state transition. Functionally equivalent (toggle happens at most ~0.2s sooner); transition-gated and cheap. Not a behavioral divergence. |
| 356 | 2.25 | `assault` | `assault.bot.objective_role` | timing:partial | RoleAssault now rates EVERY destructible whose linked objective is active (QC active-objective filter), with the Base radii (650 offense / 3000 defense) and ratingscale (20000), plus items 30000/10000. Still omits the 120s havocbot_role_timeout, the PVS-best-waypoint pick around each target, and the havocbot_attack_time 2s commitment window — so the role refreshes every nav timeout rather than committing for a fixed period. |
| 357 | 2.25 | `cts` | `cts.mode.register_latch` | values:partial | Activate() now forces g_race_qualifying=1 (Cts.cs:180, QC MUTATOR_ONADD) before declaring score rules, so the latch is no longer just a cvar read. But independent_players=1 is still not modeled, and gametype_init defaults (timelimit=20, cloaked, m_generate_mapinfo target_startTimer) are not modeled. |
| 358 | 2.25 | `cts` | `cts.timing.start_finish` | timing:partial | Run time is still wall-clock (StartTimer Cts.cs:254 / FinishStage:268 use Api.Clock.Time) not the QC frame-accumulated .race_movetime; at variable frame rate / with prediction the recorded time can drift from QC's deterministic value. The CTS PlayerPhysics hook is now ported (OnPlayerPhysics) but it only does the keyboard quantization — it does NOT maintain a .race_movetime accumulator, so the run clock is unchanged. |
| 359 | 2.25 | `duel` | `duel.identity.registration` | values:partial | timelimit=10 / leadlimit=0 gametype-init defaults are not seeded per-gametype (timelimit handling is generic); pointlimit=0 is matched. UNCHANGED. |
| 360 | 2.25 | `freezetag` | `freezetag.mode.registration` | values:partial | Match limit semantics differ: Base reads mapinfo pointlimit (10) / leadlimit (6) via g_freezetag_point_limit=-1 passthrough; port keys off fraglimit/leadlimit. Live default round target (10) coincidentally matches, leadlimit 6 likely not honored. |
| 361 | 2.25 | `freezetag` | `freezetag.revive.score` | values:partial | No nade revival bonus (g_nades_bonus_score_low) on a completed revive — minor, requires the nades mutator. |
| 362 | 2.25 | `freezetag` | `freezetag.round.flow` | values:partial | C# fallback timings (warmup 5/timelimit 180/enddelay 5) still differ from Base (10/360/0); masked live because gametypes-server.cfg ships. The round-timelimit-expiry tie path (CheckWinner ROUND_OVER) IS implemented (FreezeTag.cs:797-811: thaw all + ROUND_OVER notif + return -1) but depends on Handler.RoundEndTime being mirrored from the live RoundHandler. |
| 363 | 2.25 | `freezetag` | `freezetag.win.roundlimit` | values:partial | Lead-limit default (Base 6 via mapinfo) likely not honored (port reads leadlimit cvar, default 0 = off). |
| 364 | 2.25 | `keyhunt` | `keyhunt.unreachable.damage_destroy` | values:partial | Minor: g_balance_keyhunt_damageforcescale (default 1) is not read — the port's KeyDamage gates the re-team branch on force != 0 (raw force) rather than scaling the key's knockback by damageforcescale. At stock 1 this is a no-op; a tuned damageforcescale wouldn't affect the key's physical kick. The destroy/return behavior (NEEDKILL -> delay_damage_return) and the force-reteam logic are faithful. |
| 365 | 2.25 | `lms` | `lms.win.last_standing` | timing:partial | TimeLimitCancelled (TopTwoLivesEqual ⇒ QC WINNING_NEVER) is computed but has NO host consumer — the timelimit-cancel seam isn't wired, so an equal-top-two tie does not actually suppress a timelimit expiry. The latch is correct; the effect is unhooked (additive, non-weakening).; The campaign human-loss WINNING_YES case (Base sv_lms.qc:85-93) IS now modeled (CheckWinningCondition line 604, gated on g_campaign + CampaignBotsMayStart), but the CampaignBotsMayStart hook (static Func<bool>) must be set by the server for it to fire — unverified whether the server actually wires it; in a non-campaign/headless match it is null ⇒ false (correct early-out).; Ranks/totalPlayed are counted over LmsState, not the QC FRAGS_PLAYER / LMS_RANK set; equivalent result on the live path but a different source of truth. Win latched from a per-frame poll (DriveGametypeFrame) vs Base's CheckRules_World tick — tick ordering vs Base unverified (timing partial). |
| 366 | 2.25 | `mapobject-func` | `mapobject-func.door.rotating` | logic:partial | DOOR_ROTATING_BIDIR / BIDIR_IN_DOWN trigger-side reverse-open logic (door_use's trigger.trigger_reverse path: set lip=666 + pos2=-pos2, and the BIDIR_IN_DOWN re-open guard) is STILL NOT ported; DoorUse still takes (self, actor) with no trigger arg (Doors.cs:383), so lip is never set to 666. Bidirectional rotating doors always open the default direction. Explicitly documented out-of-scope at Doors.cs:15.; shares the (now-LIVE) reset + CSQC gaps of mapobject-func.door.slide; inherits its NOSPLASH fix + sounds-key fix + the live .message centerprint (door_rotating runs the shared DoorTouch -> MapMover.Centerprint) AND the now-live key-door cpid notifications. Only the CSQC ENT_CLIENT draw remains (latent in Base). (CORRECTED 2026-06-25: the 'dead-reset' wording was stale — DoorReset is wired for door_rotating too, Doors.cs:130.) |
| 367 | 2.25 | `mapobject-movers-platforms` | `mapobject-movers-platforms.plat.outside_target_use` | liveness:partial | PlatOutsideTouch is implemented and faithful but DEAD — and that is itself faithful to Base: Base platforms.qc only DEFINES plat_outside_touch (line 111) and never installs/spawns an outside trigger anywhere, so stock func_plat never calls it. The port matches (no outside trigger generated). Reachable only via custom-mapper wiring neither Base nor the port produces. |
| 368 | 2.25 | `mapobject-target` | `mapobject-target.items.use` | logic:partial | The spawnfunc's normalized re-serialization of item tokens into a max/min/minus-prefixed give string (driven by spawnflags 0/1/2/4, then re-tokenized) is NOT reproduced; the port keeps the raw netname and parses it directly via GiveItems.Apply — equivalent for the common (give-string) case, but spawnflag-prefixed (max/min/minus) target_items semantics and the GetResource-driven resource serialization are not honored.; The loot-toucher delete branch (ITEM_IS_LOOT actor -> EXACTTRIGGER_TOUCH + delete, and IL_EACH delete of loot enemies) is not modelled (target_items is .use-only here; the port early-returns on an Item-flagged actor instead of deleting it). |
| 369 | 2.25 | `mapobject-teleporters-portals` | `teleporters.core.telefrag` | logic:partial | The !g_race && !g_cts && !(round_handler_IsActive() && !round_handler_IsRoundStarted()) suppression is STILL a STUB: TeleportRoundGateSuppressed() always returns false (Teleporters.cs:485), so telefrags still fire in Race/CTS and during the round countdown. The gate is structurally faithful and the seam exists, but the cross-file gametype/round-state accessor is not yet wired. |
| 370 | 2.25 | `mapobject-teleporters-portals` | `teleporters.trigger.touch_and_targets` | logic:partial | RemoveGrapplingHooks(player) on teleport is still not called (teleport.qc:59-61) — a hooked player keeps the grappling hook through a teleporter.; No EXACTTRIGGER_TOUCH brush-face precision test (box overlap only). |
| 371 | 2.25 | `mapobject-teleporters-portals` | `teleporters.spawn.entities` | logic:partial | target_teleporter's spawn-time disambiguation (target_teleporter_checktarget: dest / self-target / normal teleporter) is still simplified to 'teleport activator to target on use'; the self-target (.enemy = this) case is not reproduced.; teleport_findtarget bot-waypoint spawning (waypoint_spawnforteleporter) is omitted (bot navigation).; noise precache (FOREACH_WORD precache_sound) is a no-op; harmless headless. |
| 372 | 2.25 | `mapobject-triggers` | `mapobject-triggers.magicear.trigger_magicear` | logic:partial | TUBA melody match branch (MAGICEAR_TUBA / W_Tuba_HasPlayed) out of scope — a TUBA ear returns msgin unchanged; strdecolorize is a no-op (chat color codes minimal) so MAGICEAR_NODECOLORIZE is largely inert |
| 373 | 2.25 | `onslaught` | `onslaught.bot.roles` | timing:partial | Residual (logic simplifications, not partial-correctness): no waypoint-near-objective routing (Base navigation_routerating onto g_waypoints within 400 qu of the node + ++bestwp.cnt spread), no havocbot_attack_time (5s) throttle, no havocbot_goalrating_enemyplayers(20000,650) pass, and no havocbot_ons_target teammate-interest cost balancing. The decision FLOW (generator-attack first, skip CPs if a gen is rated, shield+neighbor-filter CPs, free-point ×2) is faithful.; timing partial: Base throttles re-rating via navigation_goalrating_timeout + havocbot_attack_time; the port re-rates every brain tick with no objective-attack cooldown. |
| 374 | 2.25 | `race` | `race.lap.timing_scoring` | timing:partial | Lap time uses wall Api.Clock.Time, not the QC race_movetime per-frame fractional accumulator; lap times drift from Base under frame-time jitter or pause. |
| 375 | 2.25 | `weapon-arc` | `weapon-arc.beam.dps_core` | timing:partial | Damage applied along a single straight trace; Base traces a multi-segment bezier (control point pulled toward wantdir by tightness) so the beam body can clip geometry differently near a swept corner. Endpoint hit-reg matches. UNCHANGED.; LagComp.Begin/End bracket around the damage trace (Arc.cs:389-395) mirrors Base WarpZone_traceline_antilag. Provider IS installed live (ServerNet ctor assigns LagComp.Provider = new LagCompProvider). Remaining true gap: the warpzone TRANSFORM half of WarpZone_traceline_antilag is still not modeled (plain trace, no warpzone re-projection); identical to Base only when no warpzones are on the beam path. |
| 376 | 2.25 | `weapon-arc` | `weapon-arc.beam.curve_direction` | values:partial | tightness / degreespersegment / distancepersegment / ARC_MAX_SEGMENTS loaded but unused: no bezier control point, no segmentation. The beam_dir blend math itself is bit-faithful. UNCHANGED. |
| 377 | 2.25 | `weapon-fireball` | `weapon-fireball.primary.shootable` | timing:partial | Depletion explode is SYNCHRONOUS (fired inside the damage call) rather than Base's deferred next-frame W_PrepareExplosionByDamage (nextthink=time; setthink), and the port does NOT call the MUTATOR_CALLHOOK(PrepareExplosionByDamage) hook. Shared Projectiles.MakeShootable simplification (Devastator/Electro/Hagar/Arc/Minelayer/Mortar/Seeker/OkRpc all share it). Only observable when a server raises g_balance_fireball_primary_health > 0 — at the stock health 0 the whole path is a faithful no-op. NOTE: the owner-reassign-to-attacker IS now ported (ShootDown gates it on g_projectiles_keep_owner==0, faithful to W_PrepareExplosionByDamage:94-98).; find/LaserPlay are still plain radius (warpzone-unaware) — unrelated to this row but the projectile world is non-warpzone like all port finds. |
| 378 | 2.25 | `weapon-fireball` | `weapon-fireball.bot.obituary` | values:partial | Bot wr_aim secondary lead-speed: when the bot is in firemine mode the brain still leads by the PRIMARY speed (1200) rather than QC's bot_aim(speed 900, speed_up 100). The mode-switch toggle (0.02/0.01 random flip) and the primary lead ARE ported and live; only the secondary lead speed/up is the shared port wr_aim simplification (BotAimShotSpeed returns a single speed). |
| 379 | 2.25 | `weapon-hook` | `weapon-hook.offhand.mutator` | timing:partial | Residual 1-tick read/publish latency: OffhandFirePressed is published in PlayerPostThink (WeaponFireDriver.Frame, line 66) but read in PlayerPreThink (HookMutator.OnPlayerPreThink), which runs EARLIER in the same OnClientMove tick — so the offhand-think observes the +hook button one server tick (~1/72 s) stale. QC dispatches offhand_think inside W_WeaponFrame same-frame. Functionally castable; minor latency (timing: partial).; No useammo ammo_factor toggle on the Hook weapon (handled via fuel top-up instead) — behaviourally equivalent; only matters with g_grappling_hook_useammo on.; No in-mutator vehicle / weaponUseForbidden gate on the +hook key: WeaponUseForbidden (WeaponFireDriver.cs) is a stub returning false, and the seated case is excluded only by WeaponThink's early return, which can leave OffhandFirePressed un-zeroed/stale while seated (edge case). |
| 380 | 2.25 | `weapon-shotgun` | `weapon-shotgun.primary.firebullet_falloff` | values:partial | g_ballistics_penetrate_clips (default 1) / weapclip handling still not modeled (no weapclip distinction in the penetration loop). |
| 381 | 2.25 | `weapon-shotgun` | `weapon-shotgun.ammo.checkammo_reload` | values:partial | wr_checkammo1/2 in Base also OR the clip load (weapon_load[m_id] >= ammo) (shotgun.qc:357,370); the port's CheckAmmoPrimary/Secondary DO include the GetWeaponLoad OR-term (Shotgun.cs:366-367,374-375), so the clip-load term is now present. reload_ammo defaults 0 so no clip is used by default.; pickup_ammo (15) still not read in Shotgun.cs (item-pickup concern, not the weapon's checkammo). |
| 382 | 2.22 | `monster-mage` | `monster-mage.anim.frames` | values:partial | Push (anim_melee '5') and duckjump (anim_duckjump '4') frame groups collapse into the single Attack start-frame 2 (anim_shoot); the distinct melee/duckjump frame ranges from mr_anim are not played separately. |
| 383 | 2.1 | `monster-framework` | `monster-framework.presentation.team_colors` | liveness:partial | monster_setupcolors is PORTED + LIVE — SetupColors (MonsterAI.cs:361) picks .colormap by team (1024+(team-1)*17) / owner / skill tier (1126/1075/1228/1092/1160) bit-faithful to QC, and is called from SpawnFromMap:493 for every natural (!RESPAWNED) monster (gated exactly as QC at sv_monsters.qc:1525). It writes Entity.ColorMapOverride with RENDER_COLORMAPPED set; the csqcmodel derives the glowmod from the colormap nibble client-side, so the QC .glowmod write is covered by the established colormap render seam (presentation: the actual client tint depends on that csqcmodel path being wired — verified for the same field g_model_setcolormaptoactivator uses).; monster_changeteam is PORTED (ChangeTeam, MonsterAI.cs:393): re-teams + re-tints (SetupColors) in teamplay. But it has NO live external caller today (no dispatch site outside the monster module), so the teamplay monster re-team is dead-wired (liveness: partial). The team radar-icon update inside it is a documented no-op pending the WP_Monster sprite (healthbar gap). |
| 384 | 2.0 | `cl-announcer` | `cl-announcer.voice.cvars` | liveness:partial | Base AnnouncerFilename resolves the voice pack via AnnouncerOption() = autocvar_cl_announcer + the MUTATOR_CALLHOOK(AnnouncerOption) override (used to swap the announcer voice, e.g. overkill). The port reads the user cvar but has no AnnouncerOption mutator hook, so a mutator cannot override the announcer voice. (Also: AnnouncerResolver in NetGame captures the voice once at HUD setup, so a mid-match cl_announcer change isn't picked up — minor.) |
| 385 | 2.0 | `cl-crosshair` | `cl-crosshair.core.master_gating` | logic:partial | Port gates only on Visible + crosshair_enabled/size/alpha/number; Base's full suppress set (scoreboard/camera/intermission2/game-stopped/lockview/spectatee==-1/HEALTH<=0/DrawCrosshair mutator hook/viewloc/minigame menu/CAMERA_SPECTATOR==2/hud!=HUD_NORMAL) is reduced to the NetGame Visible toggle (havePlayer at NetGame:3366). |
| 386 | 2.0 | `cl-crosshair` | `cl-crosshair.anim.pickup_pulse` | liveness:partial | FIXED (host path): UpdatePickupFeed now calls CrosshairPanel.PulsePickup() (NetGame:2864) when a new weapon or a resource jump is detected on the LocalServerPlayer (re-baselined on respawn so the spawn loadout isn't counted). The crosshair bumps on item pickup on the host/listen-server. Still host-only: the detection reads LocalServerPlayer, so a pure remote client (LocalServerPlayer==null) gets no pulse — the networked STAT(LAST_PICKUP) equivalent is not read back into the panel client-side.; Pickup trigger is derived (new-weapon-owned or per-frame resource jump > threshold) rather than the Base STAT(LAST_PICKUP) tick; approximate but matches the visible cue for the host player. |
| 387 | 2.0 | `cl-crosshair` | `cl-crosshair.anim.hit_indication` | liveness:partial | Host-only: HitFlash=1 is set only inside `if (_server is not null)` (NetGame:2301, driven by the host-side damagetext mutator drain). A pure remote client gets no hit-indication bump/flash even though HitDamageDealtTotal is networked (ServerNet.cs) and the client diffs it for the hit SOUND — but not for the crosshair flash.; Coupled to the damagetext mutator events rather than the Base path (UpdateDamage diffs STAT(HITSOUND_DAMAGE_DEALT_TOTAL) on STAT(HIT_TIME) advance); fires on local-player-dealt damage only (ev.Attacker==localPlayer && ev.Target!=localPlayer). |
| 388 | 2.0 | `cl-crosshair` | `cl-crosshair.anim.switch_crossfade` | liveness:partial | Keyed on the resolved Texture2D changing rather than the wcross_name/resolution string pair; with the per-weapon dict unpopulated the trigger only fires on a `crosshair` cvar change, NOT on every weapon switch — so in normal play (one crosshair number for all weapons) the cross-fade effectively never runs. |
| 389 | 2.0 | `cl-crosshair` | `cl-crosshair.trueaim.teammate_blur_signal` | values:partial | Teammate shrink divisor diverges at DEFAULT cvars: Base `wcross_scale /= crosshair_hittest` divides by 1 (crosshair_hittest default 1 => NO shrink), but the port divides by a separate HitTestTeammateShrink property (default 1.25 => visibly shrinks). The port shrinks where Base does not.; Blur is a 5x5 spread of 0.04-alpha texture copies (matches Base's CROSSHAIR_DO_BLUR for textured crosshairs, crosshair.qc:585-596); only applies to the textured path (DrawCrosshairTexture), not the vector fallback (DrawVectorCrosshair). |
| 390 | 2.0 | `cl-crosshair` | `cl-crosshair.ring.vortex_charge` | liveness:partial | FIXED (host path): UpdateCrosshairWeaponRings now feeds CrosshairPanel.ChargeFraction = st.VortexCharge and .ChargePool = st.VortexChargePoolAmmo (NetGame:2905-06) while a vortex/vaporizer/*nex* weapon is active off the host-side slot-0 WeaponSlotState (the C# home of QC wepent.*). The Vortex charge ring + inner chargepool/moving-average ring now render for the host player. VortexCharge is genuinely produced server-side (Vortex.cs/OkNex.cs). Still host-only: read off LocalServerPlayer inside `if (_server is not null)`; a pure remote client (LocalServerPlayer==null) gets the -1 sentinel and no ring.; Minor fidelity: ChargePool is fed unconditionally for vortex-like weapons (default VortexChargePoolAmmo=1f), so the inner chargepool ring shows even when secondary chargepool isn't enabled; Base gates the inner-pool branch on use_vortex_chargepool. Overkill-Nex weapon itself is otherwise absent from this port. |
| 391 | 2.0 | `cl-crosshair` | `cl-crosshair.ring.reload_ammo` | liveness:partial | FIXED (host path): UpdateCrosshairWeaponRings now feeds CrosshairPanel.ClipLoad = st.ClipLoad and .ClipSize = st.ClipSize when the active slot has a clip (NetGame:2917-18). The ammo ring renders for every reloadable weapon on the host. ClipLoad/ClipSize are genuinely maintained on the slot by WeaponFireGate/WeaponFireDriver. Still host-only: read off LocalServerPlayer inside `if (_server is not null)`; remote clients get the -1/0 sentinel and no ring. |
| 392 | 2.0 | `cl-crosshair` | `cl-crosshair.ring.minelayer_hagar_arc` | liveness:partial | FIXED (host path): UpdateCrosshairWeaponRings now feeds CrosshairPanel.HagarLoad (st.HagarLoad, NetGame:2929), .MineCount (live g_mines scan of mines owned by the player, 2941-44), and .ArcHeat (st.BeamHeat/OverheatMax with the overheat-jam latch, 2960-63) when the matching weapon is active. Mine-count, Hagar burst-load, and Arc overheat rings now render for the host player. HagarLoad/BeamHeat/ArcOverheat are genuinely produced server-side (Hagar.cs/Arc.cs). Still host-only: read off LocalServerPlayer inside `if (_server is not null)`; remote clients get the -1 sentinel and no ring. |
| 393 | 2.0 | `cl-crosshair` | `cl-crosshair.ring.switch_fade` | liveness:partial | Now reachable on the host path: with the weapon-stat rings fed (UpdateCrosshairWeaponRings), DrawStatRing produces rings and calls SwitchRingFade (DrawStatRing:943), so the switch fade runs when the host player swaps between ring-bearing weapons. Still dead for a pure remote client (no ring ever draws there). |
| 394 | 2.0 | `cl-csqcmodel` | `cl-csqcmodel.fallbackframe.remap` | liveness:partial | Live only on the MD3 ModelAnimator player path; skeletal-IQM players drive frames via LocomotionBlend, which never produces the high-numbered melee/duckwalk frame ids that the remap targets, so the remap rarely fires in practice. |
| 395 | 2.0 | `cl-scoreboard` | `cl-scoreboard.teams.grouping_totals` | values:partial | Only the primary team slot total is shown (SetWireRows stores wire.Teams primary slot); the secondary teamscore (e.g. keyhunt) and the team-size N/M side display (team_size_position) are not rendered.; team_size_position (score-on-left vs right) layout option absent; totals drawn as colored banner boxes, not the QC bold hud_fontsize*1.5 side numbers. |
| 396 | 2.0 | `cl-scoreboard` | `cl-scoreboard.block.mapstats` | liveness:partial | Map-stats are read from server-side static state (MonsterAI / MapObjectsState) directly on the client feed path — live for a listen-server host, but neither monsters nor secrets are networked as a STAT, so a remote (non-host) client would not see the rows. |
| 397 | 2.0 | `cl-teamradar` | `cl-teamradar.cvar.defaults` | liveness:partial | Discovered HUD-registry radar is force-hidden (HudManager StartHiddenIds 'radar'), so only the standalone NetGame instance (256x256 at 24,24) is live — the enable/team gating runs against the standalone draw, which is correct, but the discovered panel never participates.; No hud_panel_radar_size cvar analogue (port keys off live control size + the 2500u range fallback instead of the 128px default). |
| 398 | 2.0 | `cl-view` | `cl-view.chase.classic_thirdperson` | logic:partial | The server-side spectator-camera mode toggle is not ported: View_SpectatorCamera's STAT(CAMERA_SPECTATOR) 1/2 state + IMP_weapon_drop cycling (the chase_active<=0 auto-switch that flips chase_active to -2 and Impulse_Sends weapon-drop to cycle the server spectator camera) has no port analogue. The port drives the spectator third-person purely from the user chase_active cvar. |
| 399 | 2.0 | `cl-viewmodel` | `cl-viewmodel.model.skin_colormap_glow_effects` | values:partial | Team colormap is an approximate whole-model albedo tint (0.6 + 0.4*teamColor), not Base's true colormap palette region swap (256 + c); reads as your team color but is not pixel-faithful.; Per-child m_skin (wepskin) not applied; the gun renders with its base material. |
| 400 | 2.0 | `cl-viewmodel` | `cl-viewmodel.muzzle.flash_particle` | values:partial | Local muzzle position is a heuristic at the tag_shot socket rather than Base's view-projected movedir_aligned (not verified to read identically on-screen).; Secondary fire does not flash (only the primary _attackHeld path calls Fire; UpdateLocalFireFeedback comments 'secondary FX stay networked for now').; ClientWorld.OnMuzzleFlash wrapper has no caller (NetGame calls _viewModel.Fire() directly) — dead wrapper, but the feature is live by another path. |
| 401 | 2.0 | `fx-deathtypes` | `fx-deathtypes.message.special_nade_misc` | liveness:partial | DEATH_SHOOTING_STAR has a selector (SelectSpecial maps DeathTypes.ShootingStar -> SHOOTING_STAR) but NO live emitter in the port: no jumppad/launch volume tags a kill with it, so the path is dead. Latent-only (Base registers it but it is a niche death). All other rows in this family are now live. |
| 402 | 2.0 | `fx-deathtypes` | `fx-deathtypes.message.teamchange` | liveness:partial | DEATH_TEAMCHANGE (manual team change) has no live emitter in the port: every force-kill team change routes through DeathTypes.AutoTeamChange (Teamplay.cs:698). The AUTOTEAMCHANGE branch IS live; the manual TEAMCHANGE self-line is selectable but dead. Minor — the rendered text differs only by the auto/manual wording. |
| 403 | 2.0 | `fx-deathtypes` | `fx-deathtypes.message.mapper_custom` | liveness:partial | DEATH_CUSTOM emitter is DEAD: the selector + emit path are now fully wired (DeathTypes.Custom const, Scores.EmitObituary CUSTOM branch with the 'You were %s' formatting matching damage.qc:457), but nothing in the port tags a death with DeathTypes.Custom (grep finds only the consumer at Scores.cs:610) — no mapper trigger_hurt/deathmessage emitter, so the path never fires. |
| 404 | 2.0 | `fx-notifications` | `fx-notifications.net.shouldsend` | logic:partial | STILL OPEN (unchanged this wave): NotificationReaches (:2399-2417) omits the QC spectator-follow clause: NOTIF_ONE should also reach `IS_SPEC(to) && to.enemy == other`; NOTIF_TEAM the spec whose `enemy.team == other.team`; NOTIF_ALL_EXCEPT must also exclude `IS_SPEC(to) && to.enemy == other`; NOTIF_TEAM_EXCEPT has a parallel spec nuance. The port covers only the direct player match for each, so a spectator following a player will not receive that player's personal NOTIF_ONE/TEAM notifications.; TEAM/TEAM_EXCEPT require `d.Target is Player`; a null-team or non-Player target silently reaches nobody (QC NOTIF_TEAM tolerates a missing/zero team). |
| 405 | 2.0 | `fx-sounds` | `fx-sounds.emit.sound_allowed` | liveness:partial | SoundAllowedGate.WalkOwner (owner-walk re-home) is implemented but DEAD: no caller uses it to re-home corpse/projectile sounds to the originating player before the gate check.; IsAllowed (bot_sound_monopoly gate) is live only on the vehicle hit/shield emit paths (VehicleCommon.cs:502/511/522); it is NOT applied on the general one-shot emit path (SoundService.Play / PlayOn), so most SVQC emits bypass the gate.; QC MSG_ONE 'sounds to self always pass' branch omitted (no per-recipient msg_entity context at the gate; documented cosmetic edge-case). |
| 406 | 2.0 | `fx-sounds` | `fx-sounds.voice.globalsound_dispatch` | logic:partial | AUTOTAUNT per-recipient probability roll (tauntrand < CS_CVAR(it).cl_autotaunt) still omitted: per-client cl_autotaunt is not networked, so autotaunts that clear sv_autotaunt always play for all recipients (documented deferral; a single global cl_autotaunt default 0 would wrongly silence ALL autotaunts).; TAUNT does not trigger animdecide_setaction(ANIMACTION_TAUNT) on the living speaker (no anim-action seam yet). |
| 407 | 2.0 | `mutator-campcheck` | `campcheck.check.vehicle` | liveness:unknown | Vehicle branch liveness unverified: whether a player is ever actually in a driveable vehicle in the port is unconfirmed, so this branch may never execute in a real match. |
| 408 | 2.0 | `mutator-nades` | `mutator-nades.boom.normal` | presentation:missing | DamageInfo (Damage_DamageInfo) network record omitted (render-only). |
| 409 | 2.0 | `mutator-nades` | `mutator-nades.boom.spawn` | presentation:missing | EF_STARDUST marker render + drawonlytoclient omitted (render). |
| 410 | 2.0 | `mutator-nades` | `mutator-nades.boom.heal` | presentation:missing | EFFECT_HEALING particle omitted (render). |
| 411 | 2.0 | `mutator-nades` | `mutator-nades.boom.ammo` | presentation:missing | Per-weapon weapon_load[] persistent store collapsed onto each slot's live ClipLoad (port note). EFFECT_AMMO_REGEN particle omitted (render). |
| 412 | 2.0 | `mutator-sandbox` | `mutator-sandbox.object.spawn` | presentation:missing | WIRED + LIVE: `g_sandbox` client-callable (ClientCommandRegistry:105) → CmdSandbox → HandleCommand; NetGame.WireSandbox assigns Trace (SandboxTraceProvider — real crosshair forward TraceService), so object_spawn lands at the trace endpoint with a real edict (Api.Entities.Spawn + SetModel + SetOrigin).; ObjectSpawn now DOES register the engine touch callback (e.Edict.Touch = ObjectTouch, SandboxMutator.cs:204) — the port equivalent of Base settouch (sv_sandbox.qc:180); see touch_material_fx row (now live). Think (setthink, sv_sandbox.qc:181) is driven externally by OnStartFrame over all objects rather than the per-edict engine think, which is faithful enough.; ModelExists() is permissive (returns true; no IModelService.fexists), so the Base non-existent-model rejection is not enforced.; presentation: CSQCMODEL_AUTOINIT (object→client networking) has no port seam (TODO SandboxMutator.cs:232) — client-side render of a spawned object is unverified (the object carries a real edict+model, so the generic entity feed MIGHT show it, but this was not confirmed in-game). |
| 413 | 2.0 | `mutator-sandbox` | `mutator-sandbox.object.think_grab` | presentation:missing | WIRED + LIVE: OnStartFrame is subscribed to MutatorHooks.SvStartFrame (fired in the server tick) and ticks ObjectThink for every object over the live RealClientsProvider roster (NetGame.WireSandbox — non-bot players); CryptoIdfpProvider(=PersistentId) resolves realowner from UID and clears it on disconnect.; Sandbox_DragAllowed veto hook ported and fired (MutatorHooks.FireSandboxDragAllowed, SandboxMutator.cs:346) — the grab-class branch is bit-faithful to Base (readonly OR DragAllowed→0). No subscriber exists, identical to Base's empty MUTATOR_CALLHOOK.; presentation: CSQCMODEL_AUTOUPDATE has no port seam (TODO SandboxMutator.cs:364); .Grab is computed but never consumed (no drag system). |
| 414 | 2.0 | `mutator-sandbox` | `mutator-sandbox.object.touch_material_fx` | presentation:missing | FIXED this audit (was dead): ObjectSpawn now assigns e.Edict.Touch = (self,other)=>ObjectTouch(e, other.Velocity) (SandboxMutator.cs:204), the port equivalent of Base settouch(e, sandbox_ObjectFunction_Touch) (sv_sandbox.qc:180). The object carries a real MOVETYPE_TOSS edict registered in _entities.All; the engine physics loop (SimulationLoop.cs:243 → MoveTypePhysics.RunEntity → FlyMove.Impact:107) dual-dispatches e1.Touch on collision, so the velocity-scaled impact SOUND fires on the live match path. Touch averaging (object+toucher vel), >=min gate, offset, *factor clamp(0,1), 0.1s rate-limit all bit-faithful.; presentation: STILL MISSING (re-confirmed 2026-06-25) — ObjectTouch calls EffectEmitter.EmitByEffectInfoName("impact_<mat>", …) (SandboxMutator.cs:398), BUT no `impact_metal/stone/wood/flesh` effect is registered in EffectsList.cs, so EmitByEffectInfoName resolves to the null-Effect engine-fallback branch (EffectEmitter.cs:233) and ServerNet.WriteEffect DROPS that request (`if (body is null \|\| r.Effect is null) return false;` ServerNet.cs:2583) — it never networks, never renders. The OnMaterialEffect observer delegate also has no `=` assignment (only def :405 + invoke :399). So the sound plays but no particle burst is emitted.; audio: sound playback uses the right cue (object/impact_<mat>_N.wav, channel TriggerAuto, vol VOL_BASE*intensity, atten ATTEN_NORM) and is now reachable; precache of the 5 variants is deferred to lazy Play (intentional). |
| 415 | 2.0 | `mutator-sandbox` | `mutator-sandbox.object.edit_properties` | presentation:missing | WIRED + LIVE: object_edit reachable via CmdSandbox → HandleCommand on the live path.; physics-2 maps to MoveType.Physics (EntityEnums.cs:21, value 32) at SandboxMutator.cs:811, matching Base MOVETYPE_PHYSICS=32 (dpextensions.qc:1707) — the serialized movetype byte is Base-identical. (Behaviorally still think-only since the port has no ODE integrator, same as Base without ODE.); The faithful Base solidity→physics fall-through (missing break;) IS preserved (re-verified Base sv_sandbox.qc:685-697 has no break on solidity; port reproduces via goto case "physics" at SandboxMutator.cs:797). |
| 416 | 2.0 | `mutator-sandbox` | `mutator-sandbox.object.scale` | presentation:missing | WIRED + LIVE: reached from object_edit (live via CmdSandbox) and ObjectPortLoad (live via Store load/paste).; Model re-set + mins/maxs resize depend on the entity service (Edict); now that objects carry a real edict the SetModel/SetSize path executes. |
| 417 | 2.0 | `mutator-sandbox` | `mutator-sandbox.object.attach` | presentation:missing | WIRED + LIVE: object_attach get/set/remove reachable via CmdSandbox → HandleCommand.; presentation: setattachment (engine tag-follow) has no port seam (TODO SandboxMutator.cs:291) — attached children store the tag and old_solid/old_movetype faithfully but are not actually tag-followed. |
| 418 | 2.0 | `mutator-sandbox` | `mutator-sandbox.object.claim_info` | presentation:missing | WIRED + LIVE: claim/info reachable via CmdSandbox → HandleCommand; CryptoIdfpProvider(=PersistentId)/NetNameProvider/PrintTo(=SendChatToPlayer) assigned in NetGame.WireSandbox, so claim/owner-name/info-print all work.; object_info mesh: tag enumeration (FOR_EACH_TAG) needs the model-skeleton service — not implemented (TODO SandboxMutator.cs:876); mesh subcommand prints the header but no tag list. |
| 419 | 2.0 | `mutator-walljump` | `mutator-walljump.fx.anim_and_oldvel` | presentation:missing | animdecide_setaction(player, ANIMACTION_JUMP, true) not triggered on a wall jump (no anim-action seam in the port; WalljumpMutator.cs:128 is a comment only). UNCHANGED — same missing server->client one-shot action channel noted on Dodging. Purely cosmetic (jump pose). |
| 420 | 2.0 | `mutator-walljump` | `mutator-walljump.menu.describe` | presentation:missing | Base METHOD(MutatorWallJump, describe) supplies the localized blurb shown for the walljump mutator in the MENUQC Mutators dialog ('...enables jumping off walls for added mobility. To do a wall jump, tap jump when against a wall.'). No equivalent per-mutator describe text is wired in the port's mutator-selection menu. Cosmetic menu-only text; newly mapped this audit (not in the prior baseline). |
| 421 | 1.8 | `mapobject-teleporters-portals` | `teleporters.trigger.team_claim` | liveness:unknown | SendFlags networking (SF_TRIGGER_UPDATE) still not replicated (client-side only; harmless headless). |
| 422 | 1.8 | `sv-chat` | `sv-chat.flood.per_type_throttle` | values:partial | Line-wrap width is an integer 82 visible chars vs Base's 82.4289758859709 px-equiv (perl averagewidth on gfx/vera-sans.width); proportional per-char widths are not modeled, so long-message line-count / flood==2 'too long' boundary can differ by one char on edge-length messages. |
| 423 | 1.8 | `sv-commands-votes` | `sv-commands-votes.timeout.state_machine` | presentation:partial | Presentation/audio carried as chat-broadcast lines, not the QC MSG_CENTER per-client overwriting center-print nor the real ANNCE_PREPARE sound cue: the Godot-free core has no networked center-print/announcer channel here. The BEGINNING countdown is surfaced in full but the ENDING countdown is throttled to only the final-stretch lines (<= sv_timeout_resumetime+1) to avoid flooding chat. The state/timing is faithful; only the delivery medium differs. |
| 424 | 1.8 | `sv-teamplay` | `sv-teamplay.join.skill_weighting` | values:partial | No TrueSkill ratings for humans: SkillProvider returns the bot's real BotSkill (0..10) for bots but a flat 5 for every human (IsRated treats only bots as rated; humans fall through the unranked server-average path). So skill weighting biases bot-involved ties but human-vs-human equal-size ties still reduce to a plain count. This is a VALUES/data gap, not a logic gap — the algorithm now matches Base.; No warmup skill-aware join branch (Base TeamBalance_FindBestTeams warmup+sv_teamnagger+skill block picking the team whose weighted-mean skill is furthest from the joiner). The port always uses the size->strength ladder even in warmup.; The weighted path is only reached from AssignBestTeam/IsTeamAllowedForSwitch (connect/moveplayer/team/selectteam); the per-gametype TeamBalance.JoinSmallestTeam callers (CTF/CA/FreezeTag/Dom/KeyHunt/TDM/TeamMayhem) ignore skill entirely. |
| 425 | 1.8 | `sv-world-rules` | `sv-world-rules.checkrules.overtime` | presentation:partial | STAT(OVERTIMES) is not networked to clients (the HUD OVERTIME banner has no server feed) — deferred per T42.; race/qualifying race_StartCompleting branch deliberately omitted (those modes latch their own winner). |
| 426 | 1.8 | `turret-machinegun` | `turret-machinegun.lifecycle.die_respawn` | presentation:missing | No client death FX: the non-NORESPAWN branch in Base turret_die does NO sound/effect (only the NORESPAWN branch plays SND_ROCKET_IMPACT + EFFECT_ROCKET_EXPLODE before delete — the port does port that NORESPAWN branch at Die:927-932); base/head gib toss lives in cl_turrets.qc (client). Port emits no client FX/gibs on the normal respawn path. Still open.; Respawn restores state but does not re-stamp a head model / send a full CSQC update (TNSF_FULL_UPDATE) — no networking layer. Still open. |
| 427 | 1.8 | `turret-phaser` | `turret-phaser.aim.head_track_tuning` | values:partial | PhaserTurret.Think passes only trackType:3 to TurretParams; track_accel_rot (0.65) and track_blendrate (0.2) are NOT passed, so they fall back to the TurretParams defaults (0.5 / 0.35). The TrackFluidInertia motor (TurretAI.cs:556-561) is faithful and live, but the phaser's head turns/settles at a slightly different rate than Base — track_accel_pitch (0.5) happens to match the default so it is correct. |
| 428 | 1.8 | `turret-plasma_dual` | `turret-plasma_dual.damage.gating_die_respawn` | presentation:missing | Respawn EF_NODRAW hide-frame split not modeled: Base hides (EF_NODRAW) at time+0.2 then turret_respawn at respawntime-0.2; port schedules Respawn directly at now+RespawnTime. NET respawn timing is identical (0.2 + (60-0.2) = 60 == now+60) — only the intermediate EF_NODRAW hide-frame (a presentation detail) is unmodeled.; Death FX/sound (SND_ROCKET_IMPACT + EFFECT_ROCKET_EXPLODE) only fire in Base on TFL_DMG_DEATH_NORESPAWN; plasma_dual always respawns so this branch never applies (port has it coded in Die but unreachable for this unit). tr_death is a no-op for plasma_dual. |
| 429 | 1.8 | `vehicle-framework` | `vehicle-framework.init.initialize` | values:partial | delayspawn + random jitter scheduling not ported (vehicles spawn at game_starttime only). STILL OPEN (sv_vehicles.qc:1278-1281). Keeps values/timing partial.; targetname controller / ACTIVE_NOT gating / vehicle_use not ported (map-scripted activation absent, sv_vehicles.qc:1178-1201/1276-1277). STILL OPEN. The targetname branch that ASSIGNS .team is unported, but the team-CLEAR branch (below) is independent of it. |
| 430 | 1.8 | `vehicle-raptor` | `raptor.takeoff` | presentation:missing | Animation frame ramp tracked (VehAnimFrame) but drives no visible model; rotor avelocity ramp (90 + frame/25*25000) absent (no rotor entities).; Engine fly sound (SND_VEH_RAPTOR_SPEED every 7.955812s) not played during takeoff.; Wave-2 added a button-clear (ATCK/ATCK2/CROUCH) at the end of Takeoff, but it writes vehicle.VehInput which GameWorld.cs:1104-1105 overwrites with fresh player input every tick BEFORE the next Think — so the clear is effectively a no-op on the live path. QC's per-tick button zero is faithful but the port's input-refresh architecture makes it inert (minor — input only reaches Frame, not Takeoff, so no fire/dive actually leaks). |
| 431 | 1.8 | `vehicle-raptor` | `raptor.flight_controller` | presentation:missing | Engine fly sound (every 7.955812s) not played.; No visible model pitch/roll/yaw pose beyond entity angles; player glue origin+'0 0 32' done but no cockpit/exterior model. |
| 432 | 1.8 | `vehicle-raptor` | `raptor.death` | presentation:missing | No EFFECT_EXPLOSION_MEDIUM on death (findbetterlocation), no per-think 5% EFFECT_EXPLOSION_SMALL + SND_ROCKET_IMPACT during the tumble (TODO marked in Death think). |
| 433 | 1.8 | `warpzones` | `warpzones.porto` | presentation:missing | Porto portals share the missing client portal render — they teleport correctly server-side but show as dark placeholders, same as map warpzones.; The Amazing-on-portal-telefrag announcer (portals.qc:192-194, ANNCE_ACHIEVEMENT_AMAZING within 1s of portal creation) needs the tdeath path the warpzone crossing does not run — see the portals.teleport unresolved gap noted inline (Warpzone.cs:262).; The +/-48 PortoMins/Maxs trigger box is a port choice; Base Porto portal bounds not re-verified. |
| 434 | 1.8 | `warpzones` | `warpzones.client.prediction` | presentation:missing | Render/view side of the crossing still missing: a predicted warp relocates the carrier in lockstep with authority (no rubber-band) and stamps .fixangle, but the seamless view-rotate/portal render (warpzones.client.render / .fixview / .teleported_view) is unported — the camera still snaps visually across the seam even though the origin no longer corrects. |
| 435 | 1.75 | `bot-ai` | `bot-ai.population.namemodel` | logic:partial | skin/shirt/pants columns and the 12 per-bot skill-modifier columns of bots.txt are still not parsed (name+model+forced-team only); those drive the skill-modifier and player-color gaps elsewhere. |
| 436 | 1.75 | `bot-ai` | `bot-ai.target.chooseenemy` | logic:partial | No second see-through-transparent (DPCONTENTS_OPAQUE) scan pass and no misc_breakablemodel secondary targeting; bots won't shoot breakables or acquire targets through transparent surfaces. (UNCHANGED — Wave-2 did not add the second-scan loop.) |
| 437 | 1.75 | `bot-ai` | `bot-ai.move.danger` | logic:partial | Danger evade is still brake-only: the QC lateral evadedanger vector (steer along the safe edge, distance scaled by skill) is collapsed into a reverse-velocity brake, so bots back straight off a ledge instead of sidestepping along it. (UNCHANGED.); Per-waypoint danger-cost ROUTING is inert: Wave-2 added Waypoint.Danger + made A* add nb.Danger, but no botframe_updatedangerousobjects producer ever SETS Danger (it is always 0), so routing still does not avoid chronically dangerous waypoints — the consumer is dead. |
| 438 | 1.75 | `bot-waypoints` | `bot-waypoints.file.load_hardwired` | logic:partial | Hardwired links are marked with the CustomJp flag as a stand-in (no dedicated wphwXX mirror / hardwired concept), so links cannot be identified for save/round-trip; the special-link '*'-prefix is loaded the same as a normal link rather than getting Base's special-link source-type filter. |
| 439 | 1.75 | `bot-waypoints` | `bot-waypoints.path.findnearest` | logic:partial | No jumppad-fallback (Base returns a reachable jumppad's nearest waypoint when no plain waypoint is reachable); no walkfromwp (walk-TO vs walk-FROM) direction distinction in the reachability tracewalk. |
| 440 | 1.75 | `bot-waypoints` | `bot-waypoints.path.routetogoal` | logic:partial | Teleport-goal forcing (push wp00 destination, set goalentity), dynamic/moving-target shortcut (bot_ai_strategyinterval_movingtarget 5.5) and the item incoming-link walkability cache are simplified/absent; the port pushes the A* node chain + goal directly. |
| 441 | 1.75 | `bot-waypoints` | `bot-waypoints.steer.movetogoal` | logic:partial | Obstacle avoidance is a single forward step/jump-height tracebox probe rather than Base's steerlib trident/funnel sweep (see bot-waypoints.steer.steerlib); lateral evade is folded into a straight -velocity brake (BotBrain do_break), a documented simplification of Base's AI_STATUS_DANGER_AHEAD sidestep. |
| 442 | 1.75 | `bot-waypoints` | `bot-waypoints.steer.bunnyhop` | logic:partial | Base forbids bunnyhop when evadedanger \|\| do_break is set this frame (havocbot.qc:1315); the port computes Bunnyhop inside Steer (BotNavigation.cs:318) before the BotBrain danger brake runs, so a braking bot can still have WantJump set from bunnyhop. The Steer-internal ledge brake (BotNavigation.cs:289-301) is separate from the BotBrain do_break/evadedanger gate and is not consulted by Bunnyhop. |
| 443 | 1.65 | `net-entity-state` | `net-entity-state.replicate.client_cvars` | values:partial | Two replicated cvars are stored server-side but have no server-side consumer yet: cl_autoscreenshot + g_xonoticversion are in SentCvarAllowlist (Commands.cs:233/236) so they are accepted+stored per-client, but nothing on the server reads the REPLICATED value (autoscreenshot is driven CLIENT-locally off the networked intermission flag reading the client's own Api.Cvars cl_autoscreenshot — NetGame.cs:3289 — not the per-client replicated store; g_xonoticversion is unenforced). FIX 2026-06-25: cl_clippedspectating left this list — it now has a live server consumer (ClientManager.ClippedSpectatingProvider, Commands.cs:453 → ClientManager.cs:456 free-fly MOVETYPE). |
| 444 | 1.5 | `assault` | `assault.objective.activate` | presentation:partial | ActivateObjective now also spawns the objective's waypoint sprite (SpawnObjectiveSprite → WP_AssaultDestroy + health bar) on the server, matching QC's WaypointSprite_SpawnFixed. But there is no client render/networking path, so the sprite is not actually visible on-screen (see assault.objective.waypoint_sprite). Logic/state is faithful; the visible marker is still absent. |
| 445 | 1.5 | `assault` | `assault.objective.destructible_heal` | presentation:partial | Presentation: the heal updates the wall's waypoint-sprite health bar server-side (WaypointSprites.UpdateHealth) but, like all the Assault sprites, that bar is not surfaced to the client (no waypoint render/network path). QC's func_breakable_colormod (the visible color tint on a healed wall) is not modeled. The heal LOGIC + health restoration is faithful + live. |
| 446 | 1.5 | `ctf` | `ctf.flag.spawn_setup` | presentation:partial | Base flag is static at the spawn origin (no DropToFloor settle on non-fixed spawns); dropped flags now DO settle (MoveType.Toss integrator, see ctf.flag.drop_on_death).; No glow trail (g_ctf_flag_glowtrails), no dynamic team light (g_ctf_dynamiclights); EF_LOWPRECISION is set, EF_FULLBRIGHT only when g_ctf_fullbrightflags (matches Base default off). |
| 447 | 1.5 | `ctf` | `ctf.flag.capture` | presentation:partial | Only the PERSISTENT capture-time record (ctf_CaptureRecord ServerProgsDB write + cross-session g_ctf_leaderboard) remains unported (needs a persistent DB). The in-match fastest-cap broadcast (CTF_CAPTURE_TIME/_BROKEN/_UNBROKEN, picked via MSG_CHOICE) IS now fired by CaptureRecord.; Per-flag score_capture override + cnt-group guard not modeled (own flag home requirement IS faithful). |
| 448 | 1.5 | `ctf` | `ctf.flag.fckill_score` | presentation:partial | Auto-helpme is modeled as a carrier-sprite helpme FLASH (GtHelpMeUntil networked via ServerNet, consumed in CollectWaypoints), not the full Base WaypointSprite_HelpMePing sprite; effective-HP test approximates healtharmor_maxdamage with health+armor. |
| 449 | 1.5 | `cts` | `cts.hud.race_timer` | presentation:partial | CTS has no intermediate checkpoints, so the panel's per-checkpoint split / anticipation delta / next-best lines are intentionally inert for CTS (RaceCheckpoint=254, no split feed) — this matches a single start->stop course but means the panel shows only the running clock + medal flash, not the full Race split UI. |
| 450 | 1.5 | `cts` | `cts.hud.cl_panel_gating` | presentation:partial | HUD_Score_show (hide the score panel while observing in CTS) and DrawScoreboardItemStats / DrawDeathScoreboard hiding are not specifically modeled per-CTS; the 'Rankings' scoreboard header is a hard-coded literal, not a CTS-conditional relabel.; Scoreboard accuracy is still not networked, so the CTS-gated accuracy block draws nothing (low impact: CTS has no accuracy display by intent). |
| 451 | 1.5 | `freezetag` | `freezetag.unfreeze` | presentation:partial | Does not delete an ice entity (not spawned in the first place); the waypoint sprite IS cleared (it stops being collected once IsFrozen flips false). |
| 452 | 1.5 | `freezetag` | `freezetag.presentation.overlay_eventchase` | presentation:partial | Overlay + crosshair ring + eventchase cam are all driven on the host/listen-server path (LocalServerPlayer / its Frozen status effect); a pure remote client with no LocalServerPlayer shows none of them (the cross-client wepent/stat follow-up). |
| 453 | 1.5 | `keepaway` | `keepaway.ball.spawn` | presentation:partial | SpawnBall is CALLED on the live match path — GameWorld.Boot (the NetGame match-start path) procedurally spawns the single ball at a RandomMapLocation right after the entity lump, replacing the missing QC ka_Handler_CheckBall driver.; PRESENTATION residual: the EF_DIMLIGHT bit + glow_color/glow_trail are set as data on the edict, but the actual ball MODEL render + glow trail are client concerns not yet realized (model path models/orbs/orbblue.md3 is configured but the CSQC render of it is deferred). |
| 454 | 1.5 | `monster-framework` | `monster-framework.target.validtarget` | logic:partial | PVS line-of-sight uses a traceline only; QC also does checkpvs(eye, targ) before the trace. Traceline is stricter so behavior is close, but a different-PVS-leaf but traceline-visible target would be acquired where QC would not. (UNCHANGED.); Alpha-fade gate (QC alpha<0.5 && alpha!=0 rejects) is a no-op: the headless Entity has no alpha field, so a faded/cloaked target stays targetable. (UNCHANGED.); Vehicle-vs-melee rejection (QC: IS_VEHICLE(targ) && !MON_FLAG_RANGED) not in ValidTarget; the port's ValidTarget has no vehicle gate at all. (UNCHANGED.); DRIFT FIX (2026-06-25): the typefrag/chat gate is now PORTED + LIVE — ValidTarget (MonsterAI.cs:644) rejects a chatting target when g_monsters_typefrag==0 via targ.ButtonChat, the live input mirror written by PlayerPhysics.cs:243 (player.ButtonChat = input.Typing) every physics frame. Matches QC default-1 (gate inactive) and diverges correctly only when the cvar is 0. |
| 455 | 1.5 | `monster-golem` | `monster-golem.anim_frames` | timing:partial | AnimFrame stamps only the group START index onto Entity.Frame (server-side, per think via DriveAnimFrame). The animfixfps framecount/fps per-frame playback advance WITHIN a group is not driven — there is no true .dpm frame timeline; the client renders a single stamped frame per phase/group (timing/presentation partial).; Smash maps to the Attack group (melee2 '4'), not the dedicated anim_melee1 '6' the QC ranged-smash branch uses (setanim(anim_melee1)) — a representative-group simplification (presentation). |
| 456 | 1.5 | `mutator-nades` | `mutator-nades.toss.projectile` | values:partial | Newton-style 0 is implemented as pure absolute _velocity rather than W_CalculateProjectileVelocity(e, e.velocity, _velocity, true); for non-default g_projectiles_newton_style this diverges from Base's velocity blending (style 0 is the shipped default, so identical at defaults). |
| 457 | 1.5 | `mutator-powerups` | `powerups.item.defs_spawn_gate` | values:partial | m_botvalue not set on the port pickups (Base 11000 for strength/shield/speed/invisibility, 3000 for jetpack/fuelregen) — bot item desirability may be miscalibrated. |
| 458 | 1.5 | `mutator-status_effects` | `status_effects.framework.registry` | values:partial | StatusEffectDef still drops m_color/m_icon/m_name (HUD reconstructs label/icon/color from a separate PowerupMeta table in PowerupsPanel). The behavioral methods (RemovalSound/PersistentCheck/OnTick/OnRemove) are all carried, and burning now also carries OnRemove (BurningRemove clears EF_FLAME) — only the cosmetic m_color/m_icon/m_name remain dropped. |
| 459 | 1.5 | `nexball` | `nexball.ball.spawn` | presentation:partial | Scale 1.3 + glow_trail rendering are still client-deferred (model path + trail color are configured; SpawnForGametype never sets e.Scale; bbox/effects/playerclip/relocate are exact). |
| 460 | 1.5 | `onslaught` | `onslaught.cp.icon_think_regen` | audio:missing | Damaged-FX sparks + ONS_SPARK1/2 ambient sounds not emitted (presentation/audio FX, not the steady regen logic). |
| 461 | 1.5 | `onslaught` | `onslaught.win.check_winner` | presentation:partial | BankRoundWin sends ROUND_TEAM_WIN_<team> center+info to all + play2all CTF_CAPTURE_<team> win jingle (winner>0), ROUND_TIED (winner==-1). Remaining: scoreboard column rendering + winner-banner HUD is a client concern (tracked under onslaught.presentation.csqc). |
| 462 | 1.5 | `onslaught` | `onslaught.audio.cues` | presentation:partial | Core cues EMITTED live: SpawnIcon→ONS_CONTROLPOINT_BUILD; build completion→ONS_CONTROLPOINT_BUILT + ONSLAUGHT_CAPTURE_NONAME + ONS_CAPTURE(_TEAM)_NONAME; IconDamage→ONS_DAMAGEBLOCKEDBYSHIELD / ONS_HIT1/2 / ONS_GENERATOR_EXPLODE + ONSLAUGHT_CPDESTROYED_NONAME; GeneratorDamage→blocked-by-shield / GENERATOR_UNDERATTACK + ONS_GENERATOR_UNDERATTACK / ONS_HIT1-2 (or explode at chance damage/220) / ONSLAUGHT_GENDESTROYED(_OVERTIME) + explode; GeneratorThink→ONS_NOTSHIELDED_TEAM/_<team> + ONS_GENERATOR_ALARM (every 5s while un-shielded); OvertimeDecayTick first tick→OVERTIME_CONTROLPOINT + ONS_GENERATOR_DECAY; BankRoundWin→ROUND_TEAM_WIN / ROUND_TIED + play2all CTF_CAPTURE jingle. Sounds (7) + notifications (40) registered AND now all emitting.; Remaining (presentation/cross-boundary): particle effects, waypoint-sprite build/health bars, the CP map .message name (port emits NONAME variants), and play2team→global emission (NotifBroadcast.All + SoundSystem.PlayGlobal) as the headless approximation (no per-team routing). |
| 463 | 1.5 | `onslaught` | `onslaught.gen.unshielded_alarm` | presentation:partial | Presentation: the alarm is server-authoritative (center prints via NotificationSystem.Send + siren via SoundSystem); no client-side HUD/visual of the un-shielded state beyond the center-print. The siren is emitted once globally (SoundSystem.PlayGlobal) as the headless approximation of QC's per-player ATTEN_NONE soundto — no per-listener spatial routing. |
| 464 | 1.5 | `overkill-weapons` | `overkill-weapons.oknex.primary_rail` | presentation:partial | FIXED (Wave-8b): wr_impacteffect now wired — Attack:150-158 traces the surface, emits EFFECT_VORTEX_IMPACT puff + SND_OK_NEX_IMPACT (neximpact) at the hit point (silent if sky/miss), plus a VORTEX_MUZZLEFLASH approximation. Closes the baseline's 'no client rail-impact particle / impact sound' gap.; STILL OPEN: no CSQC beam particle (SendCSQCVortexBeamParticle) — the actual rail beam between muzzle and impact is not networked (the port emits a muzzleflash + impact puff but not the beam itself). Yoda / Impressive achievement announcements never fire. wr_glow charge glow not present (covered by oknex.charge, default-off). |
| 465 | 1.5 | `overkill-weapons` | `overkill-weapons.okrpc.primary_chainsaw_missile` | presentation:partial | FIXED (Wave-8b): W_MuzzleFlash (Attack:171 EFFECT_ROCKET_MUZZLEFLASH — matches okrpc.qh m_muzzleeffect) and wr_impacteffect (Explode:230-231 EFFECT_ROCKET_EXPLODE puff + SND_ROCKET_IMPACT at the blast point) are now wired LIVE — closing the baseline 'no wr_impacteffect / no W_MuzzleFlash' gap.; Shoot-down LIVE+faithful (unchanged): OkRpc.Attack:166 Projectiles.MakeShootable(missile, exception:-1) installs the GtEventDamage shim DamageSystem dispatches; ShootDown runs W_CheckProjectileDamage(-1) + RES_HEALTH subtract → Explode at hp<=0. Correctly INERT under stock g_projectiles_damage=-2, exactly like Base.; STILL OPEN: missile not networked as a CSQC projectile (no visible flying chainsaw on clients) — CSQCProjectile(PROJECTILE_RPC) omitted.; STILL OPEN: missile not registered for bot dodge (QC bot_dodge=true, bot_dodgerating=damage*2, IL_PUSH g_bot_dodge).; STILL OPEN: missile does NOT set damagedbycontents / IL_PUSH g_damagedbycontents, so lava/slime won't detonate the in-flight chainsaw even at g_projectiles_damage>=0 (QC does).; Accuracy bookkeeping (first-hit fired/hit add, explode 'add fired back') omitted (stat-only).; Pass-through victim filter is `(Flags & Client) && TakeDamage!=No` vs QC IS_PLAYER(trace_ent): could catch a spectator client edict carrying the Client flag. Minor; corpses filtered by TakeDamage. |
| 466 | 1.5 | `tka` | `tka.ball.spawn` | presentation:partial | Single ball only: SpawnBall makes one ball; g_tkaball_count multi-ball chaining (toucher.ballcarried.cnt vs g_tka_ballcarrier_maxballs) not modeled. Default count is 1, so values match the stock server.; Model/EF_DIMLIGHT/trail_color/damageforcescale are set on the sim edict, but the actual glow_trail particle trail + EF_DIMLIGHT light are client/render concerns — not yet visible.; Spawn is procedural at map-load (GameWorld.cs:718 switch on GameType; TKA case), replacing QC tka_Handler_CheckBall's per-frame INITPRIO_GAMETYPE think. There is no game_starttime gate / remove-and-respawn-during-warmup loop, but the net effect (one ball once the match is live) matches. |
| 467 | 1.5 | `tka` | `tka.waypoints.tracking` | presentation:partial | FIXED (missing -> faithful logic/values + live): CollectWaypoints (TeamKeepaway.cs:713) produces a TEAM-COLORED carrier sprite (TkaBallCarrier<team> + per-team radar tint, offset '0 0 64', RADARICON_FLAGCARRIER, Rule=SPRITERULE_TEAMPLAY) while held, and the neutral KaBall sprite on the loose ball. Honors g_tkaball_tracking (0 = no waypoint). Pulled LIVE by ServerNet.SendWaypoints (ServerNet.cs:927 _world.GameType.CollectWaypoints), filtered per peer, networked, rendered by WaypointSpriteLayer + RadarPanel.; FIXED (values partial -> faithful): g_tkaball_tracking's THREE Base modes (0 none / 1 always / 2 only-when-dropped) are now all distinguished — CarrierWaypointVisibleFor returns BallTracking==1 for the enemy case, so ==2 hides the carrier sprite from enemies (matching the QC `return autocvar_g_tkaball_tracking == 1`).; FIXED (presentation): the carrier sprite is now networked with SPRITERULE_TEAMPLAY + a per-viewer VisibleForPlayer predicate (CarrierWaypointVisibleFor), honored per-peer at ServerNet.cs:996. It models same-team-always, spectator-always, warmup-always, and enemy-tracking==1 faithfully.; RESIDUAL (presentation:partial): only the two narrowest QC nuances of tka_ballcarrier_waypointsprite_visible_for_player remain approximated — hide-from-spectators-of-the-carrier (`view.ballcarried && IS_SPEC(player)`) and hide-on-IS_INVISIBLE(owner). The predicate has no spectatee/invisibility field, so these resolve as visible. |
| 468 | 1.5 | `turret-framework` | `turret-framework.registry.descriptor` | logic:partial | Per-unit cvar table unported (load_unit_settings / TR_PROPS_COMMON / turret_scale_* / turret_initparams clamping / dumpturrets) — per-unit balance is hardcoded C# const, mappers can't retune. (Framework-level g_turrets_* delays/nofire ARE cvar-backed; per-unit fields are not.) |
| 469 | 1.5 | `turret-phaser` | `turret-phaser.firecheck.fireflag_guard` | logic:partial | Port holds AttackFinished = endTime to block re-fire during the beam (functional analogue), but the firecheck gate does not CONSULT the fireflag==2 DISCHARGE window during which Base also forbids fire (~5 tr_think frames after the beam ends, until the head animation returns to idle). NOTE: the fireflag STATE is now tracked (FireFlag set to 1 in Attack, 2 in BeamThink end, reset to 0 by TrThinkHeadAnim) for the head animation, but RunCombat's firecheck never reads it — the fire-block is still purely the shot_refire (4s) AttackFinished hold. |
| 470 | 1.5 | `turret-walker` | `turret-walker.drive.idle_roam` | logic:partial | Walker brain does not consume the turret_checkpoint waypoint chain (walker_move_path / walker_findtarget / pathcurrent unported on the walker side): a walker with a `target` set does not patrol its path graph. The turret_checkpoint/walker_checkpoint node spawnfunc itself is now registered (TurretSpawnFuncs.Checkpoint), but the walker never reads it.; TSL_ROAM (TSF_NO_PATHBREAK) roam-walk branch not plumbed: the port has no roam spawnflag, so it always takes the stop-and-idle branch (Base also defaults to stop when the flag is unset, so behavior matches a plain walker). |
| 471 | 1.5 | `weapon-arc` | `weapon-arc.identity.attributes` | presentation:partial | EFFECT_ARC_MUZZLEFLASH is now emitted on the bolt secondary (Arc.cs:489 ARC_MUZZLEFLASH -> electro_muzzleflash), but it is not carried as the weapon's m_muzzleeffect/m_muzzlemodel attribute and there is no spinning flash.md3 muzzle entity on the beam (that lives in Draw_ArcBeam — tracked under weapon-arc.beam.visual). crosshair gfx/crosshairhlac size 0.7 is a CSQC attribute not carried headless. Presentation stays partial. |
| 472 | 1.5 | `weapon-blaster` | `weapon-blaster.identity.registration` | presentation:partial | model/anim/crosshair fidelity (view/world/item models, gfx/crosshairlaser, weaponlaser image) not audited here — names declared only. |
| 473 | 1.5 | `weapon-fireball` | `weapon-fireball.primary.launch` | presentation:partial | No projectile flight sound (fireball_fly) / FIREBALL trail attached to the live plasma_prim entity (deferred CSQC render concern, same as other weapons). |
| 474 | 1.5 | `weapon-fireball` | `weapon-fireball.primary.explode` | presentation:partial | modeleffect_spawn(sphere.md3) BFG visual ball not produced (CSQC render layer); FIREBALL_EXPLODE + impact sound at ATTEN_NORM*0.25 are emitted. |
| 475 | 1.5 | `weapon-fireball` | `weapon-fireball.primary.bfg` | presentation:partial | IS_INDEPENDENT_PLAYER(e) is checked but Entity.IsIndependentPlayer is a hardcoded stub (always false) — faithful for all non-independent (default) modes; the filter is a no-op until that mode is wired.; EFFECT_FIREBALL_BFGDAMAGE per-victim Emit is now wired, but its render fidelity (and the sphere.md3 ball) is the deferred CSQC layer. |
| 476 | 1.5 | `weapon-fireball` | `weapon-fireball.primary.laserscorch` | presentation:partial | find is a plain radius (FindInRadius), not WarpZone_FindRadius (no warpzone-spanning scorch); same omission as all radius finds in the port.; EFFECT_FIREBALL_LASER scorch beam is Emit-wired (render fidelity deferred). |
| 477 | 1.5 | `weapon-fireball` | `weapon-fireball.secondary.firemine` | presentation:partial | Port omits the bulletcounter%4 muzzle-corner f_diff offset + traceline nudge (cosmetic spawn-point spread of consecutive mines); all mines spawn from the same muzzle point. Unchanged from baseline.; No firemine flight sound / FIREMINE trail attached (deferred CSQC render). |
| 478 | 1.5 | `weapon-hlac` | `weapon-hlac.fx.muzzle_impact` | presentation:partial | Runtime green->blaster effect fallback (v0.8.6 compat, hlac.qc:225-228) not ported; cosmetic, only matters on a build missing the green HLAC effects (both are registered in the port). Unchanged by Wave-2. |
| 479 | 1.5 | `weapon-hook` | `weapon-hook.primary.latch` | presentation:partial | Latched hook becomes INVISIBLE: GrapplingHook_Stop sets MoveType.None, which is NOT a projectile movetype, so ServerNet.Classify (IsProjectileMoveType, ServerNet.cs:2095/2115) no longer classifies it as Projectile; with no model it falls to Generic and is dropped from the render. The rendered spike disappears on latch. Base keeps the latched hook drawn via CSQC. |
| 480 | 1.5 | `weapon-hook` | `weapon-hook.secondary.blast_curve` | presentation:partial | HITTYPE_SPAM flag not OR'd into the deathtype after the first blast tick (cosmetic deathtype/obituary detail).; EF_NODRAW is not set on the bomb at blast start (Base hides the model while the pull ticks); minor visual. |
| 481 | 1.5 | `weapon-rifle` | `rifle.fx.impact` | presentation:partial | Emit PATH still diverges from Base: the port emits RIFLE_IMPACT server-side per bullet inside FireBullets off a world-only re-trace; Base emits it CSQC in wr_impacteffect off the Damage_DamageInfo feed. The effect NAME + the org2/backoff offsets now match QC, but the per-bullet server emit is a different fan-out than the CSQC damageinfo path — remote-client render not visually verified. |
| 482 | 1.5 | `weapon-tuba` | `weapon-tuba.reload.instrument_cycle` | presentation:partial | weaponname (akordeon/kleinbottle) is not set — in Base it feeds only the CSQC wr_viewmodel (per-instrument view model h_akordeon/h_kleinbottle). The instrument index (TubaInstrument) IS tracked, so the obituary/sound branch correctly; only the per-instrument VIEW MODEL swap is unwired (render-only gap). |
| 483 | 1.5 | `weapon-tuba` | `weapon-tuba.fx.smoke_ring` | presentation:partial | Ring origin uses the player eye (Origin + ViewOfs) rather than the weapon muzzle tag (gettaginfo), so the puff spawns slightly off from Base — but Base itself FIXMEs that gettaginfo doesn't return the real tag origin (tuba.qc:309), so this is a near-equivalent approximation, not a faithful tag read. Per-instrument vertical offsets and the 0.25s throttle are bit-exact. |
| 484 | 1.5 | `weapon-tuba` | `weapon-tuba.melody.recognition` | presentation:partial | The cosmetic 'NAME played on the @!#%'n Tuba/Accordeon/Klein Bottle: ...' bprint chat line on a recognised melody is NOT emitted — the magic-ear TRIGGER (UseTargets) fires, but the broadcast-print of the matched message text needs a bprint seam absent in Common (Tuba.NoteOff:213-214 notes this). |
| 485 | 1.5 | `weapon-tuba` | `weapon-tuba.identity.metadata` | presentation:partial | Per-instrument view models (h_akordeon/h_kleinbottle, driven by CSQC wr_viewmodel/weaponname) still unwired; render-only (see weapon-tuba.reload.instrument_cycle).; Crosshair gfx/crosshairtuba and weapon-image weapontuba assets exist in data but live-render wiring not verified (headless audit). |
| 486 | 1.4 | `items-pickups` | `items-pickups.show.visibility` | presentation:partial | ITS_ALLOWSI (sv_simple_items, Base default 1) not honored — no simple-item model swap (no client resolver for the *_simple variants, no bob suppression). The bit is left unset rather than faked. |
| 487 | 1.35 | `mapobject-func` | `mapobject-func.train` | presentation:partial | no func_train reset (QC explicitly has `// TODO make a reset function for this one` — so this is faithful to Base's own omission, unlike door/plat/button).; train moving-sound stop (stopsoundto MSG_BROADCAST on arrival) is not done; the moving noise plays but is not explicitly stopped at each dwell (cosmetic). Port uses SoundChannel.Voice vs QC ATTEN_IDLE (cosmetic).; no CSQC ENT_CLIENT_TRAIN draw/networking. |
| 488 | 1.35 | `mapobject-teleporters-portals` | `portals.weapon.cleanup` | timing:partial | Re-shooting after a COMPLETED pair does not tear down the prior pair: PlacePortoPortal keys _pendingPorto by (owner,portalId) and a fresh shot spawns new warpzones without disconnecting the previous fully-placed pair (Base Portal_SetInPortal disconnects + Portal_Remove the old). A player can still hold more than one live pair if they re-fire after one completes.; No 15s time-based expiry (g_balance_portal_lifetime) on the warpzone path — a living player's portals persist until death/reset (see portals.entity.lifecycle); only death/reset/checkpoint teardown is modeled, not the timer.; Death/reset teardown (PortalClearAll) is dispatched only for the player's CURRENTLY-held weapon slot (DamageSystem/SpawnSystem dispatch WrPlayerDeath/WrResetPlayer on the active weapon), whereas Base's Portal_ClearAll fires irrespective of held weapon — so portals can outlive the owner if they died holding a different weapon. |
| 489 | 1.35 | `warpzones` | `warpzones.spawn.trigger` | values:partial | scale/modelscale and the warpzone_isboxy box-vs-exacttrigger distinction from WarpZoneLib_ExactTrigger_Init are not modeled; the EF_NODEPTHTEST render flag and WarpZone_Send CSQC serialisation are unported (no client entity).; RE-AUDIT 2026-06-25: the port registers ONLY the spawnfunc names `trigger_warpzone` and `trigger_warpzone_position` (MapObjectsRegistry.cs:110-111). Base's CANONICAL position spawnfunc is `misc_warpzone_position` (server.qc:642); `trigger_warpzone_position` merely delegates to it (server.qc:650). A map authored with the bare `misc_warpzone_position` name is NOT registered by the port → its explicit warpzone orientation is silently dropped. Behaviourally identical to the mapped alias, so a one-line spawnfunc registration. |
| 490 | 1.2 | `cl-viewmodel` | `cl-viewmodel.align.gunalign_gunoffset` | values:partial | cl_gunalign side magnitude is a fixed representative AuthoredShotSideY (-3.5u) rather than each v_ model's own shot-tag vecs.y; left/center now move the gun but by an approximate amount, not the per-model Base value. |
| 491 | 1.2 | `cl-viewmodel` | `cl-viewmodel.muzzle.flash_model` | values:partial | MuzzleModelFor only maps Devastator/MineLayer (flash.md3) and Machinegun/Shotgun (uziflash.md3); Base also sets m_muzzlemodel for Vortex/Vaporizer (nexflash.md3) and the Overkill HMG/MachineGun/RPC — those weapons get no model flash in the port.; Frame is animated in Base via frame+=2 (the md3 flash frame cycle); the port spawns a fresh static node and fades/shrinks it without advancing the md3 frame. |
| 492 | 1.2 | `cl-viewmodel` | `cl-viewmodel.casings.brass_eject` | timing:partial | Casing physics is a self-integrated gravity + single ground-plane bounce (FloorZ), not full MOVETYPE_BOUNCE world collision at cl_casings_ticrate (0.03125); on most maps no floorZ is supplied so the casing falls through and only the lifetime fade culls it. |
| 493 | 1.2 | `fx-sounds` | `fx-sounds.presentation.hitsound_sample` | values:partial | Pitch curve simplified: the port uses a linear damage->pitch ramp (mode 2/3) instead of QC's asymptotic gradient function (a=cl_hitsound_max_pitch, b=cl_hitsound_min_pitch, c=cl_hitsound_nom_damage); audible only as a slightly different pitch shape, and only for cl_hitsound 2/3 (default 1 = fixed pitch, identical). |
| 494 | 1.2 | `monster-golem` | `monster-golem.attack.lightning` | presentation:missing | CSQC projectile model (models/ebomb.mdl, scale 2.5, PROJECTILE_GOLEM_LIGHTNING) not rendered — SpawnProjectile sets no proj.Model (headless projectile). |
| 495 | 1.2 | `monster-golem` | `monster-golem.menu.describe` | presentation:missing | MENUQC describe() returns the golem's 3-paragraph monsterpedia entry; the port has no monster info-page system, so this text is not surfaced. Pure menu/UI flavor, no gameplay effect. |
| 496 | 1.2 | `sv-campaign` | `sv-campaign.welcome.levelnum` | presentation:partial | Presentation surface differs from Base: Base's CSQC net_handle_ServerWelcome (client/main.qc:1379) builds a full Welcome MENU DIALOG; the port surfaces the same text as a single per-client CHAT line via Commands.ChatToPlayer (Campaign.Log fallback on a bare host). The level number, the per-level description (shortdesc + longdesc, sourced from the all-levels catalogue keyed by absolute level index), and the +jump keybind (resolved exactly like Base getcommandkey) are ALL reproduced — the only residual divergence is dialog-vs-chat. The 'no per-level description string' gap from the 2026-06-22 baseline is CLOSED (Wave 8e, commit 412ca19): CampaignLevelDescription (GameWorld.cs) now reconstructs the description the SVQC slice drops. |
| 497 | 1.2 | `sv-client-lifecycle` | `sv-client-lifecycle.frame.pressed_keys` | presentation:partial | Net delivery to the spectated HUD is still missing: Player.PressedKeys is computed server-side but is NOT in the owner-state net protocol, and PressedKeysPanel.PressedKeysOverride is never assigned — so a SPECTATED player's held keys don't reach the client HUD. The local-player panel works because PressedKeysPanel computes the bitmask client-side from bind state.; GetPressedKeys mutator hook is not fired as a discrete hook here (the race/cts speed-award + Vortex/OkNex velocity-charge consumers are driven from their own per-frame ticks instead). |
| 498 | 1.2 | `sv-impulse` | `sv-impulse.action.drop` | presentation:partial | Dual-wield sideways offset (dv = v_right * -movedir.y) collapses to zero — the port drives a single weapon slot (documented). The FL_TOSSED flag + colormap/glowmod render nits on the thrown loot are not modeled (presentation partial). |
| 499 | 1.2 | `sv-intermission` | `sv-intermission.overtime.cascade` | presentation:partial | STAT(OVERTIMES) is not networked, so the persistent HUD 'OVERTIME' / overtime-count banner does not show. The one-shot CENTER_OVERTIME_TIME and CENTER_OVERTIME_FRAG center notifications ARE wired live (→ NotificationSystem.Send), so the begin-of-overtime / 'Overtime has begun!' messages do fire. |
| 500 | 1.2 | `sv-ipban` | `sv-ipban.enforce.roster` | presentation:partial | The bprint summary wording differs (port "^1banned: <names> (<reason>)" vs Base per-client "^1NOTE:^7 banned client <name>^7 has to go\n" + the ": affects <name>, ..." reason-append). Functionally equivalent; cosmetic. |
| 501 | 1.2 | `sv-ipban` | `sv-ipban.view_delete` | presentation:partial | Ban_View footer wording differs: Base "^2Done listing all active (N) bans." (+ a "^2Listing all existing active bans:" header), port "{n} ban(s) active" (no header). Per-line "#i: <ip> is still banned for <secs> seconds" matches.; Ban_Delete's OnlineBanList_SendUnban side-effect is dropped (no online sync); slot deletion itself is faithful. |
| 502 | 1.2 | `sv-spawnpoints` | `sv-spawnpoints.put.put_player_in_server` | presentation:partial | EF_TELEPORT_BIT \| EF_RESTARTANIM_BIT are now SET on the (re)spawned player server-side (SpawnSystem.cs:752, OR'd into p.Effects, networked via EntityField.Effects), but NO client csqcmodel consumer reads them to play the teleport flash / restart the model animation (grep for EffectFlags.Teleport/RestartAnim in game/ + src/XonoticGodot.Client = none). The authority half matches Base; the visible spawn flash relies on the (now team-colored) EFFECT_SPAWN particle burst (fx.spawn_event_particle). Residual presentation gap. STILL OPEN (presentation only). |
| 503 | 1.2 | `sv-world-rules` | `sv-world-rules.nextlevel.intermission` | presentation:partial | DumpStats(true) final scores dump at NextLevel is not called (see sv-world-rules.eventlog.dumpstats); target_music_kill and the MatchEnd_BeforeScores/MatchEnd mutator hooks are also not invoked here.; MatchEnd_RestoreSpectatorStatus (move match-joiners back to spectator at match end) is not called here (see new_unmapped).; The bprint team/player 'wins the match' broadcast lines are handled by the notification layer, not this path (verify coverage in sv-notification). |
| 504 | 1.2 | `tmayhem` | `tmayhem.teams.count` | presentation:unknown | QC tmayhem_DelayedInit spawns/labels tmayhem_team map entities via Team_MapEnts_FindOrSpawn; the port derives only the team COUNT and relies on generic team registration. Whether tmayhem_team colours/entities are honoured was not verified (unchanged from baseline). |
| 505 | 1.2 | `turret-flac` | `turret-flac.identity.spawn` | presentation:missing | head model models/turrets/flac.md3 not set (no head-bone attachment in port). NOTE: the head-FRAME cycle (++frame wrap 4) DID land in Wave 8e — it runs on the turret edict's own networked Entity.Frame (FlacTurret.cs:154) — but there is still no separate head model/bone to drive. |
| 506 | 1.2 | `turret-fusionreactor` | `turret-fusionreactor.setup.tr_setup` | presentation:missing | Initial head spin (50 deg/s) and head scale 0.75 are not applied — no head entity / no client render. |
| 507 | 1.2 | `turret-hellion` | `turret-hellion.anim.headspin` | presentation:missing | Server-authoritative frame STATE is now cycled (idle 0 -> spinning 1..6 kicked per shot, wrap at >=7), but there is no separate head MODEL entity (hellion.md3) attached to a bone, so the cycling frame drives no VISIBLE spinning launcher and is not used to select the firing muzzle tag (tag_fire vs tag_fire2 / two-cannon alternation) — both remain client-render gaps tracked here + on turret-hellion.weapon.launch. |
| 508 | 1.2 | `turret-plasma` | `turret-plasma.track.fluid_inertia` | presentation:missing | Head angles live only in server TurretState and are never networked/rendered — the head never visibly aims (presentation). |
| 509 | 1.2 | `turret-plasma` | `turret-plasma.headspin.frame_anim` | presentation:missing | PRESENTATION only: the frame-cycle logic runs server-side on the turret edict's e.Frame, but the body is a static base.md3 with no head-spin frames and there is no rendered tur_head sub-entity / CSQC turret net layer — so the spin is computed but never visibly shown. |
| 510 | 1.2 | `turret-plasma_dual` | `turret-plasma_dual.damage.headshake` | presentation:missing | Server-side head-angle jolt is now applied, but the CLIENT-rendered head model (plasmad.md3) is not attached, so the visible flinch is not drawn (the head-angle state nudges the next shot's aim, which IS the gameplay effect). Base also nets TNSF_ANG to CSQC; the port has no turret CSQC net layer. |
| 511 | 1.2 | `turret-plasma_dual` | `turret-plasma_dual.presentation.head_frame_anim` | presentation:missing | Server-authoritative head-frame state (Entity.Frame) is now ported and live, but the CLIENT render of the spinning plasmad.md3 head bone is absent — the static base.md3 body has no spin frames and there is no tur_head sub-entity / CSQC turret net layer, so the wheel is not visibly drawn.; Two-barrel tag_fire alternation (tag_fire on plasmad.md3) not modeled — single computed ShotOrg muzzle. |
| 512 | 1.2 | `turret-walker` | `turret-walker.weapon.minigun` | presentation:missing | No EFFECT_BLASTER_MUZZLEFLASH at the head tag and no EFFECT_BULLET tracer on fire (client-render). |
| 513 | 1.2 | `vehicle-bumblebee` | `vehicle-bumblebee.spawn.vr_spawn` | presentation:missing | Shield hit-flash entity (vehicle_shieldent, MDL_VEH_BUMBLEBEE_SHIELD, scale=512/vlen(maxs-mins)), cosmetic gun models (CANNON_RIGHT/LEFT/CENTER), cockpit/viewport model offsets (body hudmodel '50 0 -5'/viewport '5 0 2'; gun1 '90 -27 -23'/'-85 0 50'; gun2 '90 27 -23'/'-85 0 50'), scale 1.5, and the networked BRG_* raygun entity (Net_LinkEntity + bumble_raygun_send) are NOT created. |
| 514 | 1.2 | `vehicle-framework` | `vehicle-framework.enter.steal` | presentation:missing | The CSQC presentation half is deferred: the WP_VehicleIntruder radar waypoint (g_vehicles_steal_show_waypoint) and the CENTER_VEHICLE_STEAL / _SELF centerprint notifications. The authoritative steal STATE (shield zero + flag backup/strip, restored on exit) is fully applied. |
| 515 | 1.2 | `vehicle-framework` | `vehicle-framework.respawn.return_waypoint` | presentation:missing | No WP_Vehicle return waypoint / radar icon is shown while a vehicle returns (still deferred, CSQC). Base spawns a separate vehicle_return entity with vehicles_showwp as think; the port re-arms the vehicle's own think directly (functionally equivalent for the return SCHEDULE, but no radar icon). |
| 516 | 1.2 | `vehicle-framework` | `vehicle-framework.presentation.hud_camera_xhair` | presentation:partial | Cockpit/chase camera STILL dead: no client reads p.Vehicle to apply SVC_SETVIEWPORT (vehicle_viewport) — the pilot keeps the normal player view.; Framework low-health/low-shield HUD alarm (SND_VEH_ALARM / SND_VEH_ALARM_SHIELD, cl_vehicles.qc:230-264, gated cl_vehicles_alarm default 0) never plays (no audio cue). (The Raptor's SEPARATE incoming-missile alarm at Raptor.cs:408 IS live.); Host-only feeder: UpdateVehicleHud reads LocalServerPlayer (host path). The cross-client VEHICLESTAT_* networking for a pure remote pilot is still unwired. |
| 517 | 1.2 | `vehicle-raptor` | `raptor.spawn.setup` | presentation:missing | Cosmetic sub-entities not created: no raptor_spinner rotor entities (engine_left/right), no raptor_rotor_anglefix, no bomb1/bomb2/tur_head cosmetic models (gun1/gun2 ARE created for aim).; mass=1 is set on the entity but has NO behavioral effect — the port's MoveTypePhysics solver never reads .Mass, so the QC bounce-missile mass tuning is inert (value set for fidelity only).; FIXED (Wave-2 fixup): Spawn() no longer nulls vehicle.Touch, so the VehicleCommon.Touch dispatcher SpawnVehicle installed stands (crush + vr_impact path now live, see raptor.impact). Matches QC vr_spawn (does NOT clear .touch) and Racer (Racer.cs:189-190). |
| 518 | 1.2 | `vehicle-raptor` | `raptor.enter` | presentation:missing | tur_head.exteriormodeltoclient owner-binding and CSQCVehicleSetup (client viewport/HUD) not done here (cross-boundary client/net).; Carried-flag reparent ('-20 0 96') not modeled (CTF-on-raptor edge): a flag carried while boarding the raptor will not be repositioned to the cockpit mount. |
| 519 | 1.2 | `vehicle-raptor` | `raptor.cannon_aim_lock` | presentation:missing | Aux lock crosshair color feedback (red==1 / green>0.5 / blue<0.5 by lock strength, UpdateAuxiliaryXhair) is client-only and not drawn.; locktarget==2 alternate direct-trace lock path unimplemented (default is 1, so off the live path). |
| 520 | 1.2 | `vehicle-raptor` | `raptor.cannon_fire` | presentation:missing | No EFFECT_RAPTOR_MUZZLEFLASH and no CSQCProjectile (PROJECTILE_RAPTORCANNON) visual for the cannon bolt (server hit logic only).; Spread sampling differs from Base: QC does normalize(dir + randomvec()*spread) (a cube offset added to forward, perturbing forward too); the port uses Prandom.Spread (uniform-in-disc projected onto right/up). Distribution shape differs — negligible at spread 0.01 but not bit-faithful. |
| 521 | 1.2 | `vehicle-raptor` | `raptor.bombs` | presentation:missing | No CSQCProjectile visual (PROJECTILE_RAPTORBOMB / RAPTORBOMBLET) for bomb/bomblet, no Damage_DamageInfo (DEATH_VH_RAPT_FRAGMENT) fragment effect on burst (presentation only). |
| 522 | 1.2 | `vehicle-raptor` | `raptor.flares` | presentation:missing | No EF_FLAME flare glow / MDL_VEH_RAPTOR_FLARE model / scale 0.5 (decoy is invisible but functional).; Flare damage uses Health<=0 -> remove inline rather than the dedicated raptor_flare_damage event_damage hook; behaviorally equivalent (a shot flare with health 20 dies).; Seduction scan filters proj.VehGuideMode>=0; QC's raptor_flare_think scans g_projectiles by it.enemy==owner WITHOUT a guide-mode filter (the MIF_GUIDED_TRACKING gate is only on the incoming-alarm path, not the flare retarget). The port's extra filter could under-match guided rockets the flare should seduce. |
| 523 | 1.2 | `vehicle-raptor` | `raptor.mode_switch` | presentation:missing | CSQCVehicleSetup (client HUD reticle update on mode change) not done (client concern). |
| 524 | 1.2 | `vehicle-raptor` | `raptor.exit_land` | presentation:missing | Land animation frame from altitude (hgt<128: (hgt/128)*25) + rotor spin tracked-but-invisible (no model).; antilag_clear on exit + CSQCVehicleSetup viewport restore are cross-boundary (net/client). |
| 525 | 1.2 | `vehicle-raptor` | `raptor.painframe` | presentation:missing | EFFECT_SMOKE_SMALL low-health smoke puff (origin + randomvec()*80) is not emitted — the headless Common layer has no particle/temp-entity emitter (noted VehicleCommon.cs PainFrame; presentation only). |
| 526 | 1.1 | `net-entity-state` | `net-entity-state.csqcmodel.interpolation` | presentation:partial | Animation frame-group cross-fade (cl_lerpanim_maxdelta_framegroups/cl_nolerp) not networked; replaced by client-side LocomotionBlend — remote animation FRAME blending diverges from Base's networked 2-frame lerp. (The separate networked anim ACTION state is tracked in net-entity-state.csqcmodel.anim_state.) |
| 527 | 1.08 | `mapobject-teleporters-portals` | `teleporters.dest.random_selection` | timing:unknown | Port still forces telefrag-avoid always-on for clients rather than reading the now-registered g_telefrags_avoid / STAT(TELEPORT_TELEFRAG_AVOID) — equivalent at the Base default of 1 but not cvar-driven (see teleporters.core.telefrag_cvars). |
| 528 | 1.05 | `bot-ai` | `bot-ai.think.throttle` | values:partial | Simulated bot ping/aim-latency (CS(this).ping, bot_aimlatency) is not modeled; bots have zero network latency, so their lead/reaction is slightly sharper than stock at low skill. |
| 529 | 1.05 | `bot-ai` | `bot-ai.aim.fire` | values:partial | Long-range fire-hesitation chance still omits the bot_aggresskill modifier (port uses skill only), so per-bot aggression variance is lost. (UNCHANGED.) |
| 530 | 1.0 | `mutator-bloodloss` | `mutator-bloodloss.menu.dialog` | presentation:partial | The full MENUQC describe paragraph (bloodloss.qc:72-79, two sentences on stun/impaired movement/rapid health drain) is reduced to a one-line tooltip on the checkbox/slider. |
| 531 | 1.0 | `mutator-buffs` | `buffs.item.spawnfunc` | presentation:partial | Per-buff visual (skin/color/glowmod) is NOT applied to the spawned item: ConfigureBuffEntity sets the relic model + size but never assigns buff.m_skin / buff.m_color / glowmod, so the pickup renders as the uncolored relic regardless of which buff it carries (QC buff_Think colors it). Presentation partial.; Minor lifecycle omissions vs buff_Init: PFLAGS_FULLDYNAMIC dynamic light + buff_Customize light pulse, bot_pickupbasevalue 1000 (bots don't weight buff items), and the buff_Waypoint_Spawn build-finished waypoint (see buffs.waypoint) are not ported. |
| 532 | 1.0 | `mutator-buffs` | `buffs.item.touch_pickup` | presentation:partial | EFFECT_ITEM_PICKUP particle is still not spawned (no effect-spawn service); only the pickup sound + notifications play.; cl_buffs_autoreplace is read as the server-side value (no per-client REPLICATE store), so it can't differ per client; functionally equivalent on a server-wide setting. |
| 533 | 1.0 | `mutator-buffs` | `buffs.inferno` | presentation:partial | Carrier-as-target fire-immunity (DEATH_FIRE -> 0) + half-lava (DEATH_LAVA x0.5) present and match Base. The 0.5 lava factor is QC's hardcoded `0.5` (QC has a `TODO cvarize` so this is faithful).; Burn-on-hit via MonsterFramework.AddFireDamage applies the shared Burning status effect rather than QC's .fire_* accumulator; the integrated total over the burn window approximates QC. Minor numeric divergence in the per-tick distribution, not the total or the curve.; Presentation: no EF_FLAME flame visual is networked specifically for inferno's burn. |
| 534 | 1.0 | `mutator-damagetext` | `damagetext.cl.draw_fade_shrink_move` | presentation:partial | drawfontscale crisp-cell scaling: QC renders the string into a size_max-sized font cell scaled by drawfontscale = size/size_max, then offsets y by drawfontscale.x*size/2 (cl_damagetext.qc:91-93). The port draws at a direct integer pixel size with no drawfontscale cell, so glyph metrics and the small vertical re-centering differ slightly. Minor presentation residual; the visible number, color codes, font, drift and horizontal centering are otherwise faithful. |
| 535 | 1.0 | `mutator-dynamic_handicap` | `dynamic_handicap.present.handicap_level` | presentation:partial | Presentation is faithful in color/level/value but uses a flat colored square as the scoreboard marker instead of the real gfx/scoreboard/player_handicap 32x32 texture (the port panel has no gfx texture path; ScoreboardPanel.cs:1073-1081 draws an exact-tint square). The level int, the network round-trip, and the tint formula are all bit-faithful to Base. |
| 536 | 1.0 | `mutator-powerups` | `powerups.glow.effects` | presentation:partial | Presentation fidelity: Base's EF_ADDITIVE\|EF_FULLBRIGHT recolors the WHOLE model additively; the port renders the glow primarily as a blue/red OmniLight3D (EF_BLUE->blue light, EF_RED->red light, CsqcModelEffects.cs:119-120), and the EF_ADDITIVE/EF_FULLBRIGHT bits only affect per-instance-material meshes (shared/shader-material player models skip them — a documented CsqcModelEffects render gap). |
| 537 | 1.0 | `mutator-touchexplode` | `touchexplode.menu.describe` | presentation:partial | The MENUQC MutatorTouchExplode.describe() 2-paragraph info-page text ('silly fun to a server' / 'new tactics in some gametypes') is not ported faithfully; the port surfaces only a one-line checkbox tooltip paraphrase. The menu label (message field, 'Touch explode') IS faithful. Cosmetic menu help only — no gameplay effect. Several sibling mutators (cloaked/vampire/hook/new_toys/pinata) DO port their describe() text onto the checkbox tooltip; touchexplode does not. |
| 538 | 1.0 | `mutator-waypoints` | `waypoints.ctf.flag_markers` | presentation:partial | No flag-carrier HEALTH BAR (QC WaypointSprite_AttachCarrier sets one from 2*healtharmor_maxdamage; the port's CollectWaypoints sets Health=-1 and AttachCarrier has no live caller). UNCHANGED.; No auto-helpme on carrier hit (g_ctf_flagcarrier_auto_helpme_damage 100) — depends on the uncalled AttachCarrier/HelpMePing-on-carrier path. UNCHANGED.; Enemy-flagcarrier waypoint reveal on stalemate not wired: the QC ctf_stalemate enemy-FC waypoint reveal is not produced from the port's stalemate state. UNCHANGED. |
| 539 | 1.0 | `mutator-waypoints` | `waypoints.client.draw` | presentation:partial | g_waypointsprite_text / _uppercase / _iconsize / _fontsize / _scale / _alpha / _edgeoffset_* are read live and take effect; cl_hidewaypoints 0/1/2 + hideflag&1 (Hideable) gating honored. UNCHANGED.; g_waypointsprite_iconcolor still hardcoded white (QC iconcolor 0 default → white, so faithful at default; the non-zero tint option is not modeled). Minor. UNCHANGED.; The off-screen arrow is a simpler filled triangle than QC's bordered glyph (cosmetic). UNCHANGED. |
| 540 | 1.0 | `mutator-waypoints` | `waypoints.client.fades` | presentation:partial | Lifetime fade is server-computed (FadeAlpha) and networked rather than client-computed from fadetime/lifetime**timealphaexponent; timealphaexponent (1) not applied at the client (value 1 → no shape difference, but the computation site differs). Minor. UNCHANGED.; Off-screen arrow glyph still a simplified triangle (tracked under waypoints.client.draw). |
| 541 | 0.96 | `cl-hud` | `cl-hud.panel.weapons` | timing:unknown | Selection-slide animation + accuracy-tint overlay fidelity not runtime-verified. |
| 542 | 0.96 | `cl-hud` | `cl-hud.panel.powerups` | timing:unknown | Countdown expanding-flash (<=5s) timing not runtime-verified. |
| 543 | 0.96 | `cl-hud` | `cl-hud.panel.timer` | values:unknown | Secondary round-timer + overtime/sudden-death/warmup subtext coverage not all runtime-verified. |
| 544 | 0.96 | `cl-hud` | `cl-hud.panel.physics` | timing:unknown | topspeed peak fidelity not runtime-verified. |
| 545 | 0.96 | `cl-hud` | `cl-hud.panel.pickup` | timing:unknown | Item-respawn-timer (_showtimer) display fidelity not runtime-verified. |
| 546 | 0.9 | `mapobject-func` | `mapobject-func.button` | presentation:partial | the pressed-state alternate texture (Frame=1) is set on the entity but whether the brush model renders the alt frame in-engine is unverified (presentation).; CORRECTED 2026-06-25: button_reset round-restart re-arm is LIVE — ButtonReset is assigned at Buttons.cs:42 and dispatched by GameWorld.ResetMapObjects (GameWorld.cs:3887). The prior gap note claiming it was 'STILL dead' / the comment is wrong was stale; the Buttons.cs:15 comment is correct. (See mapobject-func.reset.) |
| 547 | 0.9 | `mapobject-func` | `mapobject-func.plat` | presentation:partial | plat_reset initial placement is live + faithful (targeted->raised, untargeted->bottom); the round-restart .reset is ALSO live now — PlatReset is assigned at Platforms.cs:82 and dispatched by GameWorld.ResetMapObjects. (CORRECTED 2026-06-25; the prior 'STILL NOT wired' note was stale — see mapobject-func.reset.); plat_outside_touch (the CSQC-side outside trigger that sends a top plat down when a player leaves) is not ported — it is gated to CSQC in Base; minor.; no CSQC ENT_CLIENT_PLAT draw/networking. |
| 548 | 0.9 | `mapobject-misc` | `mapobject-misc.keys.item_key` | presentation:partial | item_key pickup centerprint ('You've picked up the X key!') is silently dropped: MapMover.Centerprint(toucher, this.Message) IS called (MapObjectsRegistry.cs:509) but MapObjectsCommon.CenterprintHandler (declared MapObjectsCommon.cs:816) is never ASSIGNED anywhere in game/ or src/ (grep 'CenterprintHandler =' = 0 hits), so Centerprint no-ops on the live path.; item_keys_names[lowestbit] save (for keylist display strings) and the Q3COMPAT_COMMON QL +8z origin nudge are omitted (no Q3-compat global; the latter documented). |
| 549 | 0.9 | `mapobject-teleporters-portals` | `portals.weapon.projectile` | presentation:partial | OnTouch re-probes the impact face with a worldonly traceline (ProbeImpact) to recover Q3 surface flags + the true plane normal, so the slick/clip + noimpact branches are reachable; but on a miss (no nearby solid) it falls back to the negated-velocity normal rather than the real wall normal.; porto_v_angle held-angle capture for the non-secondary combined shot is dormant in stock play (g_balance_porto_secondary defaults to 1, so that branch is never reached); the hold state machine is modeled but only exercised with secondary disabled. |
| 550 | 0.9 | `mapobject-triggers` | `mapobject-triggers.push.trigger_push` | presentation:partial | no animdecide_setaction(ANIMACTION_JUMP) jump pose on launch (jumppads.qc:402) — needs the cross-file anim seam; no jumppadcount / jumppadsused multi-pad bookkeeping (jumppads.qc:379-405) — affects multi-jumppad kill-credit + the jump anim gate; q3compat gravity correction (grav /= 750/800) and the q3 target_push 1.5s debounce not applied |
| 551 | 0.9 | `monster-spider` | `monster-spider.identity.registration` | values:partial | Spider.qh spawnflags (MON_FLAG_MELEE\|RANGED\|RIDE) are not represented on the port descriptor; RIDE is moot (no monster-riding mode), MELEE/RANGED are advisory (the Attack picks by distance regardless). |
| 552 | 0.9 | `monster-spider` | `monster-spider.think.brain` | timing:partial | Spider.mr_think in QC is a trivial return-true (no per-type logic); the port Think -> RunThink matches. The shared move steering uses a FIXED 0.4 velocity blend (movelib_move_simple approximation) rather than QC's frametime-scaled blend — a documented determinism choice, shared by all monsters; flagged timing:partial. |
| 553 | 0.9 | `monster-zombie` | `monster-zombie.setup.mr_setup` | timing:partial | Spawn-anim/no-think window hardcoded to 1/3s instead of being derived from anim_spawn '30 1 3' via animfixfps/animstate_endtime (close approximation, not model-exact). The spawn POSE (frame 30) IS played; only the per-frame fps/duration of the group is approximate (timing). |
| 554 | 0.9 | `monster-zombie` | `monster-zombie.presentation.animation` | timing:partial | Per-frame fps/duration (the second/third animfixfps components, e.g. die1 '9 1 0.5' = 2s) is not modeled by the phase enum: frame VALUES are byte-exact but the per-group playback RATE is approximate (timing). No frame-group/value losses remain — all 9 distinct groups (spawn 30, idle 19, walk/run 27, melee 4, pain1/2 20/22, die1/2 9/12, blockstart 8, blockend 7, shoot 0) are mapped, and both random variants (pain1/pain2, die1/die2) now alternate. |
| 555 | 0.9 | `turret-framework` | `turret-framework.spawn.init` | values:partial | Per-unit cvar table unported (load_unit_settings / TR_PROPS_COMMON / turret_scale_* / turret_initparams clamping / dumpturrets) — per-unit balance is hardcoded C# const.; No tur_head child entity / tag_head attachment (head bone is a TurretState.HeadAngles vector, not an edict).; turret_initparams clamping/derivation absent: per-turret consts are used raw with no bound/default fill. |
| 556 | 0.9 | `turret-framework` | `turret-framework.projectile.generic` | presentation:partial | The generic TurretSpawn.Projectile helper itself leaves NetName empty (Generic trail); the proper proj_type trail only appears because every turret CALLER overwrites NetName after the spawn — a turret that forgot to stamp it would render trailless. |
| 557 | 0.9 | `turret-framework` | `turret-framework.lifecycle.death_respawn` | timing:partial | Port respawns directly after RespawnTime (no turret_hide 0.2s + respawntime-0.2 two-step). |
| 558 | 0.9 | `turret-hk` | `turret-hk.setup.tr_setup` | values:partial | Select flags differ: Base has VEHICLES + TRIGGERTARGET (no discrete ANGLELIMITS); port has Players + AngleLimits and NO SelectVehicles/SelectTriggerTarget flag (those flags do not exist in TurretAI) — vehicle acquirability unverified, trigger-target acquisition absent.; AFF (avoid-friendly-fire trace) firecheck flag not modeled as a discrete gate. |
| 559 | 0.9 | `turret-phaser` | `turret-phaser.fire.target_select_bias` | values:partial | PhaserTurret.Think builds TurretParams without the phaser-specific scoring biases (turrets.cfg:302-306: rangebias 0.85, samebias 0, anglebias 0.25, missilebias 0, playerbias 1), so RangeBias/AngleBias/SameBias/MissileBias all default to 1.0. The scoring machinery (TurretAI.cs:381) is faithful and live, but the phaser's target-priority weighting differs from Base — it weights distance and angle more heavily and does not zero out same-target/missile bias. |
| 560 | 0.9 | `vehicle-spiderbot` | `vehicle-spiderbot.spawn.setup` | values:partial | mass=5000 is not modeled (no mass field on the entity); affects push/knockback interactions QC scales by mass.; Port adds a MoveGround vehicle flag not present in QC vr_setup; harmless shared-helper flag, not a Base value. |
| 561 | 0.8 | `cl-crosshair` | `cl-crosshair.anim.smooth_ease` | presentation:partial | Color is NOT eased: Base eases scale, alpha AND color toward goal over effect_time (crosshair.qc:444 `wcross_color = f*wcross_color + (1-f)*wcross_color_prev`); the port's SmoothGoal eases only scale+alpha and sets color directly each frame. Visually equivalent only for slow color changes within the effect_time window. |
| 562 | 0.8 | `cl-csqcmodel` | `cl-csqcmodel.skeleton.upper_lower_aim` | presentation:partial | Presentation gap is on the ACTION side, not the split mechanism: the upper-body torso always shows a stable idle/aim frame (LocomotionBlend.Split torsoTime=0), so the bone split + fixbone + aim-pitch are structurally faithful but the torso never animates beyond the aim-bone pitch bend (the upper-body ACTION overlay is the animdecide feature's gap).; Aim pitch reads Entity.Angles.X instead of a separately-networked v_angle_x (BIT(10)); for remote players these can differ slightly (the visible head/torso bend may lag or use the body yaw-pitch rather than the true view pitch). |
| 563 | 0.8 | `cl-hud` | `cl-hud.engine.registry_and_order` | presentation:partial | Draw order follows Godot child order, not the QC _hud_panelorder reverse-walk; relative panel layering may differ but panels don't overlap in luma. |
| 564 | 0.8 | `cl-hud` | `cl-hud.engine.skin_border` | presentation:partial | Directional border variants (_south/_north/_east/_corner_*) render as the full 9-slice frame rather than the engine's edge-masked crops; bg-enabled panels show a full frame where Base shows a partial edge. |
| 565 | 0.8 | `cl-scoreboard` | `cl-scoreboard.header.gameinfo` | presentation:partial | Lead-limit header term IS now built (BuildLimitsHeader handles ll > 0 && (ll < fl \|\| fl <= 0) with the '^2+N' term and the '& '/'/'  delimiter, LeadLimit/LeadAndFragLimit fed from cvars NetGame.cs:4175-4176).; m_hidelimits and campaign player-count suppression not handled. |
| 566 | 0.8 | `cl-scoreboard` | `cl-scoreboard.block.spectators` | presentation:partial | Spectator-row ping is still -1 (per-row ping not networked) so showping shows a dim '-' rather than a real value.; spectators_position (0/1/2/3 placement around the blocks) and spectators_aligned column alignment not implemented (drawn after the table only). |
| 567 | 0.8 | `cl-scoreboard` | `cl-scoreboard.block.rankings` | presentation:partial | Speed-award / all-time-fastest line is now PORTED + LIVE (DrawSpeedAward, ScoreboardPanel.cs:1257, fed by SetSpeedAward NetGame.cs:3837 from networked SpeedAward/Best + holders; QC unit conversion + alltimebest gate).; No multi-column wrap / horizontal scroll (single-column rankings list). |
| 568 | 0.8 | `fx-notifications` | `fx-notifications.args.tokens` | presentation:partial | Tokens are resolved SERVER-side reading the SERVER's notification_show_location / _show_sprees* cvars; in Base these are CSQC-side per-client, so the port loses per-client control of location/spree display (direct consequence of server-side formatting — see fx-notifications.net.protocol intended divergence). This is now the ONLY residual on the cvar-driven tokens (the default polarities are fixed).; race tokens (f1race_time/f2race_time/f3race_time, race_col, race_diff) referenced by the RACE_* notifs in NotificationsList.cs are NOT implemented in ResolveOne (drop to '' at :99) — race split/record lines lose their time/diff/color text.; key-bind tokens (pass_key/nade_key/join_key — getcommandkey lookups) and the s3#s2/#s2 HASH_REPLACE tokens are not implemented in ResolveOne (drop to ''); used by CTF flag-pass / nade / spectator-join hints and a few hash-replace info lines. |
| 569 | 0.8 | `fx-notifications` | `fx-notifications.info.killfeed` | presentation:partial | MULTIICON_INFO (icon derived from args via iconargs) has no port path; the only Base user is MINIGAME_INVITE (minigames not a port priority).; notification_allow_chatboxprint (MSG_INFO -> chatbox routing) not modelled; benign — Base disables it in code (default 0). |
| 570 | 0.8 | `mutator-instagib` | `mutator-instagib.spawn.fullbright` | presentation:unknown | Whether EffectFlags.FullBright actually renders the player model fullbright in the Godot renderer is still unverified (behavioral check needed). |
| 571 | 0.75 | `assault` | `assault.objective.decrease_and_score` | audio:partial | QC MUTATOR_HOOKFUNCTION(as, PlayHitsound) returns true for a func_assault_destructible victim (forces the standard hitsound when shelling a wall); the port doesn't special-case it. Parity-neutral but not explicitly modeled (now tracked as new_unmapped assault.notify.play_hitsound). |
| 572 | 0.75 | `weapon-rifle` | `rifle.audio.ric` | audio:partial | MINOR channel mismatch: Base plays SND_RIC_RANDOM on CH_SHOTS; the port's PlayRic->PlayOn uses the RIC sound's default channel hint (Weapon), not CH_SHOTS. Inaudible difference in practice (affects only same-channel sound cutoff), but not byte-faithful. |
| 573 | 0.6 | `monster-mage` | `monster-mage.attack.spike` | presentation:partial | CSQC spike projectile model (PROJECTILE_MAGE_SPIKE, mage.qc:238 CSQCProjectile) still not wired — SpawnProjectile sets no projectile visual/ProjectileType, so the spike has no client-visible model/trail in flight (cf. Spider.cs which DOES set PROJECTILE_ELECTRO). |
| 574 | 0.6 | `monster-mage` | `monster-mage.defend.heal` | presentation:partial | Healed-ally waypoint-sprite health update (WaypointSprite_UpdateHealth, mage.qc:280-281) not invoked in the port — no WaypointSprite_UpdateHealth/UpdateHealth call anywhere in Gameplay/Monsters. |
| 575 | 0.6 | `monster-spider` | `monster-spider.attack.bite` | presentation:partial | QC bite picks a random melee anim (anim_melee vs anim_shoot via setanim). The port now sets the Attack anim PHASE (frame 0 mapped server-side via AnimFrame) but does NOT randomise melee/shoot, and (more importantly) the client DPM model does not render the networked Entity.Frame at all (see monster-spider.anim.mr_anim) — presentation only.; QC NPC bite uses actor.attack_range (= g_monsters_attack_range 120); the player-wielded path forces attack_range 60. Port uses st.AttackRange for the NPC path (faithful); the 60 player path is part of the unported player-wielded SpiderAttack (see monster-spider.attack.player_wielded). |
| 576 | 0.6 | `monster-spider` | `monster-spider.pain.mr_pain` | presentation:partial | Pain anim variant is now picked (MarkPain sets st.AnimVariant random 0/1 -> AnimFrame Pain 7/8) and the pain-window now matches the QC anim length (0.44s), but the CLIENT DPM model does not render the networked Entity.Frame (see monster-spider.anim.mr_anim), so the pain twitch is computed server-side yet not visible. Presentation partial. |
| 577 | 0.6 | `monster-spider` | `monster-spider.death.mr_death` | presentation:partial | QC mr_death picks anim_die1/anim_die2 at random (random()>0.5). The port's AnimFrame maps Death->die1('1')/die2('2') keyed off the die2 flag (st.DeathLanded = RollDeathVariant()), but Spider does NOT override RollDeathVariant() (defaults false), so the death phase ALWAYS resolves die1 — the random pick is absent (Zombie overrides RollDeathVariant; Spider does not).; CLIENT DPM playback missing anyway: even die1 is not rendered because the DPM model does not consume the networked Entity.Frame (see monster-spider.anim.mr_anim). |
| 578 | 0.6 | `turret-ewheel` | `turret-ewheel.identity.def` | presentation:partial | Head model (ewheel-gun1.md3) not instantiated as a separate head entity; no tag_head attachment.; ROAM spawnflag not modeled as an explicit flag (the path-roaming behavior it gates is now implemented and live — see turret-ewheel.drive.path). |
| 579 | 0.6 | `turret-ewheel` | `turret-ewheel.weapon.fire` | presentation:partial | No two-cannon head-frame alternation (Base ewheel_weapon.qc:32-35: tur_head.frame += 2, wrap > 3 -> 0) — needs the unported head sub-entity (the turret's own Entity.Frame is consumed by the locomotion drive animation); tracked under turret-ewheel.identity.def.; EFFECT_BLASTER_MUZZLEFLASH is emitted with except: turret, but Base Send_Effect -> Send_Effect_Except(..., NULL) does NOT exclude the firer — minor over-exclusion (cosmetic for a turret, which is not a first-person view source). |
| 580 | 0.6 | `turret-ewheel` | `turret-ewheel.client.draw` | presentation:partial | CSQC ewheel_draw rolling integration not ported: the model does not client-side roll (origin += velocity*dt, head angles += avelocity*dt, MOVETYPE_BOUNCE+gravity). The server already networks origin/velocity, so the visible motion follows the netted transform rather than the client integration. |
| 581 | 0.6 | `turret-flac` | `turret-flac.fire.flak_shell` | presentation:partial | Presentation partial (Wave 8e landed the server-emit side): BLASTER_MUZZLEFLASH is now emitted via EffectEmitter (FlacTurret.cs:149), the shell is stamped NetName='hagar' so the client ProjectileCatalog resolves the HAGAR trail (FlacTurret.cs:114), and the 4-frame head cycle runs on turret.Frame (FlacTurret.cs:154). REMAINING: the actual HAGAR trail RENDER + muzzleflash draw live in the Godot client project (not qcsrc) and are not yet verified end-to-end; the turret has no head MODEL/bone so the frame index drives nothing visible yet. |
| 582 | 0.6 | `turret-fusionreactor` | `turret-fusionreactor.identity.def` | presentation:partial | head_model (reactor.md3) is not carried — no head entity exists in the port turret model.; TUR_FLAG_SUPPORT/AMMOSOURCE are not stored as flags; support behavior is hardcoded in the C# class instead of derived from spawnflags. (Verified TUR_FLAG_AMMOSOURCE is declared but NEVER read anywhere in Base qcsrc — purely declarative — so its omission is functionally harmless; TUR_FLAG_SUPPORT only selects turret_targetscore_support, which the reactor never invokes since it has no targetscore-driven path.) |
| 583 | 0.6 | `turret-hellion` | `turret-hellion.identity.def` | presentation:partial | Head model (hellion.md3) not instantiated as a separate spinning-launcher head entity; no tag attachment. |
| 584 | 0.6 | `turret-hellion` | `turret-hellion.weapon.launch` | presentation:partial | No two-cannon tag_fire/tag_fire2 muzzle alternation (single computed ShotOrg origin). The head-spin frame state that Base uses to SELECT the tag is now cycled (turret-hellion.anim.headspin) but there is no head-model bone, so the muzzle-tag alternation stays a client-render gap.; No PROJECTILE_ROCKET CSQC trail / MIF_SPLASH\|MIF_PROXY\|MIF_GUIDED_HEAT flags (no visible smoke trail). |
| 585 | 0.6 | `turret-hk` | `turret-hk.identity.def` | presentation:partial | Head model (hk.md3) not instantiated as a separate head entity; no tag_head attachment.; TUR_FLAG_RECIEVETARGETS spawnflag not modeled (see turret-hk.target.recieve). |
| 586 | 0.6 | `turret-hk` | `turret-hk.anim.head` | presentation:partial | Frame cycler runs on the turret edict's own .Frame (the port has no separate tur_head bone entity), and there is no hk.md3 head model instantiated, so the 0..5 load/cycle animation is computed but never visibly displayed. |
| 587 | 0.6 | `turret-machinegun` | `turret-machinegun.spawn.identity` | presentation:partial | No separate tur_head sub-entity / head model attached at tag_head (Base turret_initialize:1344 spawns a turret_head entity and _setmodel(head_model)); the port's head is angle-state only (TurretState.HeadAngles), so the head model never renders or animates. |
| 588 | 0.6 | `turret-machinegun` | `turret-machinegun.lifecycle.damage` | presentation:partial | Headshake jitter is applied to the angle-state head (TurretState.HeadAngles) but there is no separate tur_head edict and no CSQC TNSF_ANG send, so the recoil flinch is server-state only — it will not be visible until a head model is attached and networked. Behaviour is faithful; visual is presentation-deferred. |
| 589 | 0.6 | `turret-mlrs` | `turret-mlrs.identity.def` | presentation:partial | Head model (mlrs.md3) not instantiated as a separate tur_head entity; no tag_head attachment. The ammo-gauge frame now rides the turret's own .Frame (see turret-mlrs.anim.ammogauge) but the head model is not separately rendered/animated. |
| 590 | 0.6 | `turret-mlrs` | `turret-mlrs.anim.ammogauge` | presentation:partial | Frame formula now ported every think onto the turret edict's OWN .Frame (Bound(0, 6 - floor(0.1 + Ammo/ShotDamage), 6)) — logic/values/timing faithful. But Base writes it to the separate tur_head entity's .frame and nets that head; the port has no distinct head bone entity, so the gauge is not rendered on a separately-attached mlrs.md3 head. Presentation partial. |
| 591 | 0.6 | `turret-mlrs` | `turret-mlrs.damage.lifecycle` | presentation:partial | Death gib toss still missing: Base cl_turrets.qc turret_die tosses base-gibN.md3 + head_model gibs (turret_gibtoss). The port has no CSQC turret edict to spawn client gib entities, so no gibs fly on death.; No client turret_draw (cl_turrets.qc): the head does not visibly spin (angles += avelocity*dt) and a damaged MLRS emits no low-health sparks (<127 hp -> te_spark) or smoke (<85 EFFECT_SMOKE_LARGE / <32 EFFECT_SMOKE_SMALL). |
| 592 | 0.6 | `turret-phaser` | `turret-phaser.identity.model_hitbox` | presentation:partial | Head model (phaser.md3) is not attached as a separate tracking head bone; head frame animation is also absent (see tr_think feature). |
| 593 | 0.6 | `turret-plasma` | `turret-plasma.identity.model_hitbox` | presentation:partial | head_model (plasma.md3/plasmad.md3) is never loaded or rendered — there is no tur_head sub-entity, only the base body. |
| 594 | 0.6 | `turret-plasma` | `turret-plasma.death.die_respawn` | presentation:partial | Death client gibs (base-gib1..4 + head gib toss/turret_gibboom) are still not spawned — needs a CSQC turret edict. The explosion + impact sound ARE now emitted. |
| 595 | 0.6 | `turret-tesla` | `turret-tesla.identity.def` | presentation:partial | Head model (tesla_head.md3) not instantiated as a separate head/coil entity; no tag_head attachment. TrThink computes head spin as state (HeadAngles) but grep confirms no head-bone node consumes HeadAngles, so the coil never visibly spins (see anim feature). |
| 596 | 0.6 | `turret-tesla` | `turret-tesla.anim.tr_think` | presentation:partial | Head spin not rendered: TrThink computes HeadAVelocity ('0 45 0' low-ammo / '0 180 0' charged, scaled by ammo/shot_dmg, zeroed when inactive) and integrates HeadAngles, but grep confirms no head-bone/coil entity (tesla_head.md3) consumes HeadAngles — the coil never visibly spins. Server-side state only.; Idle crackle arc reach distribution: Prandom.Vec() returns each component in [-1,1] (a cube) whereas QC randomvec() (DP builtin #91) rejection-samples a uniform direction inside the unit sphere; the *350 idle crackle endpoint is thus a slightly different shape. Cosmetic-only (idle muzzle crackle). |
| 597 | 0.6 | `turret-walker` | `turret-walker.identity.def` | presentation:partial | Head model (walker_head_minigun.md3) not instantiated as a separate tur_head entity; no tag attachment, so the minigun head never visibly tracks. |
| 598 | 0.6 | `turret-walker` | `turret-walker.weapon.rocket` | presentation:partial | No tag_rocket01/tag_rocket02 alternation (port has no head tags; all rockets fire from a single muzzle org).; No EFFECT_BLASTER_MUZZLEFLASH presentation; only the te_explosion launch flash is emitted (Base's launch te_explosion IS now reproduced). |
| 599 | 0.6 | `vehicle-bumblebee` | `vehicle-bumblebee.pilot.raygun` | presentation:partial | The visible BRG_* beam is a placeholder EffectEmitter.TeHealBeam/TeBeam stub (Bumblebee.cs:698-700), not the networked CSQC bumble_raygun beam; the per-tick BRG_START/BRG_END send + `wait = time + 1` beam-hold + the bumble_raygun_draw multi-segment cylinder are not modeled. Beam COLOUR is bit-faithful (pure green heal / pure red damage). |
| 600 | 0.6 | `vehicle-bumblebee` | `vehicle-bumblebee.gunner.frame` | presentation:partial | Gunner aux crosshairs + cockpit HUD are now LIVE (Wave-7): GunnerFrame writes VehGunnerLeadPoint/Valid + VehGunnerHitPoint/Valid (Bumblebee.cs:605/648) and mirrors gunner.VehicleHealth/Shield/Ammo1/Energy (632-636); NetGame.UpdateVehicleHud selects the BumblebeeGun art for a seated gunner and projects the magenta '1 0 1' LEAD (aux slot 1) + reload-color READY (aux slot 0) crosshairs, AND mirrors the gunner READY markers onto the PILOT (slots 1/2 via FeedPilotGunnerAux). RESIDUAL presentation: cosmetic gun cockpit models + viewport offsets are still not built (HUD bars/crosshairs render, but the 3D gun cockpit art does not). |
| 601 | 0.6 | `vehicle-bumblebee` | `vehicle-bumblebee.gunner.cannon` | presentation:partial | Wave-8e FIX: EFFECT_BIGPLASMA_MUZZLEFLASH is now emitted at the muzzle (FireCannon: EffectEmitter.Emit('BIGPLASMA_MUZZLEFLASH', org, vel, 1); BIGPLASMA_MUZZLEFLASH registered EffectsList.cs:163). RESIDUAL: the networked CSQCProjectile (PROJECTILE_BUMBLE_GUN) bolt trail/model visual is still NOT IMPLEMENTED. |
| 602 | 0.6 | `vehicle-bumblebee` | `vehicle-bumblebee.death` | presentation:partial | Gib toss (gun1/gun2/gun3 from their tags + body via vehicle_tossgib, scale 1.5) NOT IMPLEMENTED — the vehicle_gib physics-entity subsystem is unported. |
| 603 | 0.6 | `vehicle-framework` | `vehicle-framework.spawn.respawn` | presentation:partial | EFFECT_TELEPORT spawn flash, CSQCMODEL_AUTOINIT, hook-removal sweep, and vehicles_reset_colors are deferred (client/presentation). |
| 604 | 0.6 | `vehicle-framework` | `vehicle-framework.think.tick` | presentation:partial | The painframe smoke particle (EFFECT_SMOKE_SMALL) is deferred — note: EffectEmitter now EXISTS in the Common layer (used by the descriptor death/muzzle FX) so the old 'no headless emitter' rationale is stale, but PainFrame still does not emit the smoke. The authoritative jitter IS produced (see combat.painframe). |
| 605 | 0.6 | `vehicle-framework` | `vehicle-framework.enter.handshake` | presentation:partial | MOVETYPE_NOCLIP + disableclientprediction=1 modeled as MoveType.None (player origin rewritten by the vehicle); behaviorally close but not the QC movetype.; SVC_SETVIEWPORT/SETVIEWANGLES + CSQCVehicleSetup, the temp_wepent weaponslot swap, CENTER_VEHICLE_ENTER notification, and RemoveGrapplingHooks are deferred (cross-boundary). The VehicleHud panel (the client view of CSQCVehicleSetup) IS fed live — see hud_camera_xhair. antilag_clear is now wired (pl.AntilagNeedsClear set on enter). |
| 606 | 0.6 | `vehicle-framework` | `vehicle-framework.exit.handshake` | presentation:partial | SVC_SETVIEWPORT/SETVIEWANGLES + CSQCVehicleSetup, temp_wepent switchweapon restore, Kill_Notification(CPID_VEHICLES), and last_vehiclecheck are deferred. (The HUD exits cleanly: UpdateVehicleHud calls hud.Exit() once p.Vehicle is null.) antilag_clear is now wired (pl.AntilagNeedsClear set on exit).; The WP_Vehicle return waypoint (vehicles_showwp) itself is still CSQC-deferred (the return SCHEDULE is now set — see respawn.return_waypoint). |
| 607 | 0.6 | `vehicle-framework` | `vehicle-framework.combat.damage` | presentation:partial | vehicle_shieldent hit flash (colormod/alpha angled to hitloc) is a CSQC visual — not drawn.; antilag_clear on death is deferred (cross-boundary). |
| 608 | 0.6 | `vehicle-framework` | `vehicle-framework.combat.painframe` | presentation:partial | Smoke particle (EFFECT_SMOKE_SMALL at origin + randomvec()*80) is deferred — the only unported piece. NOTE: EffectEmitter now EXISTS in the Common layer (Wave 8 wired it for the descriptor death/muzzle FX), so the old 'no headless emitter' rationale is stale; PainFrame simply does not call it yet. The authoritative velocity/angle jitter IS produced. |
| 609 | 0.6 | `vehicle-framework` | `vehicle-framework.aim.locktarget` | presentation:partial | The cockpit aux-crosshair lock-color update is LIVE for the building/locked target (UpdateVehicleHud -> SetAuxiliaryXhairLock projects VehLockTarget tinted by VehLockStrength); the per-target-class lock SEMANTICS are now also complete. |
| 610 | 0.6 | `vehicle-framework` | `vehicle-framework.projectile.shared` | presentation:partial | CSQCProjectile networking + the in-flight projectile trail/model visual are deferred (client/net). The muzzle EFFECT_* IS now emitted by the descriptors (Send_Effect via EffectEmitter).; g_bot_dodge list (bot AI) deferred. |
| 611 | 0.6 | `vehicle-racer` | `vehicle-racer.identity.def` | presentation:partial | hud cockpit model + tag_viewport attachment and the scale-0.5 model hack are cosmetic-only and not instantiated (physics uses the real hitbox).; view_ofs '0 0 50' + height 200 (camera view offset/dist) not set; affects the cockpit camera, not the sim. |
| 612 | 0.6 | `vehicle-racer` | `vehicle-racer.spawn.lifecycle` | presentation:partial | scale 0.5 (cosmetic model-shrink hack) still not applied; physics uses the real hitbox so this is presentation-only. |
| 613 | 0.6 | `vehicle-racer` | `vehicle-racer.weapon.cannon` | presentation:partial | The EFFECT_RACER_MUZZLEFLASH muzzle effect is now emitted; the CSQCProjectile (PROJECTILE_WAKICANNON) bolt trail remains a client-render concern and is unported. |
| 614 | 0.6 | `vehicle-racer` | `vehicle-racer.weapon.rocket` | presentation:partial | The EFFECT_RACER_ROCKETLAUNCH launch effect is now emitted; the CSQCProjectile (PROJECTILE_WAKIROCKET) rocket trail remains a client-render concern and is unported. |
| 615 | 0.6 | `vehicle-racer` | `vehicle-racer.weapon.rockethud` | presentation:partial | Authority-side ammo2/reload2 % stats are written each tick onto the pilot; the client HUD progressbar that READS them (Vehicles_drawHUD vehicle_icon_ammo2) is presentation/CSQC and not yet wired — a client-render concern, not an authority gap. |
| 616 | 0.6 | `vehicle-raptor` | `raptor.impact` | audio:missing | CONFIRMED LIVE (fixup commit 4be8357, re-verified 2026-06-22): Raptor.Spawn no longer nulls vehicle.Touch, so the VehicleCommon.Touch dispatcher SpawnVehicle installs stands and calls vehic.VehicleDef.Impact on a hard touch (matches Racer.cs:217-219 / QC vr_spawn which does NOT reset .touch). Raptor.Think:231 snapshots vehicle.OldVelocity BEFORE the tick's physics (matches Racer.Think:296), and Spawn:141 seeds PlayTime = Time to clear the impact debounce on respawn — so vehicles_impact computes the true Velocity-OldVelocity delta. The raptor takes DEATH_FALL crash damage on hard impacts.; Dispatch chain VERIFIED: a BounceMissile-movetype raptor is integrated by MoveTypePhysics.cs:160 which calls PushEntity(doTouch:true) -> FlyMove.cs:107-110 invokes e.Touch for non-Solid.Not solids -> VehicleCommon.Touch:766 -> VehicleDef.Impact when PlayTime<Time. (Earlier baseline cited TriggerTouch.cs for this; the actual moving-solid touch dispatch is FlyMove/MoveTypePhysics — corrected, behavior unchanged.); VehicleCommon.Impact:783-796 is line-for-line faithful to vehicles_impact (delta=Velocity-OldVelocity, min(speedfac*len,maxpain), DEATH_FALL, play_time=time+0.25). Only the trace_dphitq3surfaceflags & Q3SURFACEFLAG_NOIMPACT skip is deferred (no per-touch surface-flag in the headless collision result — noted at VehicleCommon.cs:785). |
| 617 | 0.6 | `vehicle-spiderbot` | `vehicle-spiderbot.identity.registration` | presentation:partial | view_ofs '0 0 70' / height 170 (the seated camera offset + chase distance) are client-view concerns not modeled on the headless server descriptor. |
| 618 | 0.6 | `vehicle-spiderbot` | `vehicle-spiderbot.weapon.minigun` | presentation:partial | Cosmetic barrel spin (QC gun1.angles.z += 45 / gun2.angles.z -= 45 with 360 wrap) is done client-side (Firing flag in VehicleVisuals); the server-state field is not advanced — functionally fine, non-1:1 split.; FIXED-SINCE-BASELINE: EFFECT_SPIDERBOT_MINIGUN_MUZZLEFLASH is now emitted server-side via EffectEmitter.Emit (Spiderbot.cs:494; effect registered EffectsList.cs:155), matching QC spiderbot.qc:276 Send_Effect(EFFECT_SPIDERBOT_MINIGUN_MUZZLEFLASH, v, v_forward*2500, 1). (The client VehicleVisuals Firing flag also pops a flash; the server emit is the QC-faithful path.) Residual presentation gap = barrel-spin server-state only. |
| 619 | 0.6 | `vehicle-spiderbot` | `vehicle-spiderbot.weapon.rockets` | presentation:partial | ARTILLERY clearance-height traces use a headless WorldOnly trace; the h1/h2 (0.75 of the up/diagonal world clearance) are reproduced but the resulting arc was not in-game verified.; FIXED-SINCE-BASELINE: the launch effect EFFECT_SPIDERBOT_ROCKETLAUNCH is now emitted server-side via EffectEmitter.Emit (Spiderbot.cs:630; effect registered EffectsList.cs:151), matching QC vehicles_projectile Send_Effect. Residual: the rocket-in-flight CSQCProjectile visual (PROJECTILE_SPIDERROCKET) stays client/net deferred to the shared projectile catalog. |
| 620 | 0.6 | `vehicle-spiderbot` | `vehicle-spiderbot.death.blowup` | presentation:partial | Presentation still simplified: no 4 physics gib entities (head MF_ROCKET+EF_FLAME bounce+fade via spiderbot_headfade, 2 tossed guns body-vel v_forward*700, body frame 11). FIXED-SINCE-BASELINE: the per-burn EFFECT_EXPLOSION_SMALL (Spiderbot.cs:459) and the spiderbot_headfade EFFECT_EXPLOSION_BIG (Spiderbot.cs:440) are now emitted server-side via EffectEmitter.Emit (the Send_Effect equivalent; effect names registered EffectsList.cs:29/31). The head EF_FLAME flag + the physics gib bodies stay a TODO (client VehicleVisuals does an approximate scripted gib arc). |
| 621 | 0.5 | `mutator-waypoints` | `waypoints.itemstime.respawn_countdown` | audio:partial | On the first countdown tick spawns WP_Weapon (weapon pickups) / WP_Item (else) attached to the item, UpdateBuildFinished drives the build-progress bar to the scheduled respawn, and every tick Pings the waypoint (radar ring) + plays ITEMRESPAWNCOUNTDOWN. SpectatorOnly items get SPRITERULE_SPECTATOR (the sv_itemstime 1/2 gate).; audio: partial — QC soundto(MSG_ONE) plays ITEMRESPAWNCOUNTDOWN per visible client; the port's Common layer has no per-client soundto seam, so it emits a single positional cue on CH_TRIGGER (ATTEN_NORM). SpectatorOnly items over-broadcast slightly to nearby live players — the only divergence.; wp_extra-driven per-item icon/color (m_waypointblink etc.) not networked — WP_Item renders generic magenta, WP_Weapon generic black/client-resolved. |
| 622 | 0.48 | `sv-impulse` | `sv-impulse.action.reload` | audio:unknown | weaponLocked is modeled only for the FREEZE half (FrozenStat + STATUSEFFECT_Frozen). QC weaponLocked also covers player_blocked and the LockWeapon mutator hook — both still unmodeled (low impact; not reachable in stock modes the same way). The game-start/game-stopped/timeout halves are gated upstream by the dispatcher. |
| 623 | 0.45 | `mapobject-func` | `mapobject-func.bobbing` | audio:partial | the spawn-time soundto(MSG_INIT) looping ambient is not resent per-player; played through the facade where present. |
| 624 | 0.45 | `mapobject-func` | `mapobject-func.pendulum` | audio:partial | ambient .noise not resent per-player (same reduction as bobbing). |
| 625 | 0.45 | `mapobject-func` | `mapobject-func.fourier` | audio:partial | looping ambient .noise resent per-player (soundto MSG_INIT) reduced to a single facade Play. |
| 626 | 0.45 | `mapobject-func` | `mapobject-func.vectormamamam` | audio:partial | looping ambient .noise per-player resend reduced to facade Play in setactive. |
| 627 | 0.45 | `mapobject-movers-platforms` | `mapobject-movers-platforms.plat.q3compat_defaults` | audio:partial | CPMA sound_start/sound_end + the pt1_strt/pt1_end default-file probe are not ported (require GetField_fullspawndata / FindFileInMapPack, unavailable headless). The medplat default still applies, so a Q3 plat is audible and correct — only the CPMA per-mapper sound OVERRIDE keys are skipped. Sole residual gap on this row; audio marked partial for it. |
| 628 | 0.3 | `monster-golem` | `monster-golem.audio.voice_cues` | audio:partial | Full data-driven .sounds-file parse + skin-keyed Monster_Sounds_Update is still hardcoded per-descriptor (SoundCues set + SoundCueCount switch mirror golem.dpm_0.sounds by hand) rather than parsed from the model's .sounds at load; behaviorally faithful for golem but the general GlobalSound/Monster_Sounds_Update plumbing is a named no-op.; QC spamsound(SND_BODYIMPACT1) pain placeholder (when a monster has no pain sample) not played — n/a for golem (golem has pain samples). |
| 629 | 0.24 | `turret-ewheel` | `turret-ewheel.damage.lifecycle` | audio:unknown | Death blast is applied unconditionally in Die (Base turret_die has the RadiusDamage variant commented out) — minor over-fidelity, scaled by leftover ammo; not ewheel-specific (framework-level). |