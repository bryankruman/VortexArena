# Parity Gaps — ranked by gameplay impact

_Generated 2026-06-27. 526 features with gaps, highest-impact first._

Score weights logic & liveness highest, then values/timing, then presentation/audio, scaled by category (core gameplay > niche entities). Intended divergences excluded.

| # | score | unit | feature | worst | gaps |
|---|---|---|---|---|---|
| 1 | 21.0 | `onslaught` | `onslaught.presentation.csqc` | logic:missing | No generator/control-point/icon world models on the client; no progressive damage-model swaps; no icon bob/spin/punch-on-hit animation; no generator damage `frame`; no radar link lines; no objective death-cam (ENT_ONSCAMERA/WantEventchase/ons_camSetup) on round loss; no generator explosion sequence (rays/shockwave/gibs/electricity); no particle effects (EFFECT_SPARKS/CAP/RAGE/ROCKET_EXPLODE) or waypoint-sprite build/health bars. |
| 2 | 19.5 | `clanarena` | `clanarena.join.late_join_observer` | logic:missing | While !allowed_to_spawn, QC forces a joining player to Observer (TRANSMUTE), sets INGAME_STATUS_JOINING, and sends INFO_CA_JOIN_LATE so they play from the next round. The port still has no INGAME_JOINING state and no allowed_to_spawn spawn gate, so mid-round joiners are not handled the QC way. (The CA_JOIN_LATE notification IS registered, NotificationsList.cs:702, but never sent — no live caller.); QC Bot_FixCount counts in-game-joined clients (so bots fill mid-round) -- not ported. |
| 3 | 19.5 | `cts` | `cts.checkpoints.intermediate` | logic:missing | target_checkpoint and trigger_race_checkpoint are consumed as no-ops for CTS (GameWorld:3207-3208 documents them as Race-only); defrag-style maps lose ordered checkpoint enforcement.; No 'went backwards' detection, no spawnflag-4 backward-damage, no per-checkpoint split times/records, no .defragcp order file. Wave-5 added per-checkpoint split records to the Race port (Race.cs RecordCheckpointSplit / _checkpointRecords), but Cts.cs got no equivalent — CTS remains a single start->stop course with no intermediate checkpoints. |
| 4 | 19.5 | `nexball` | `nexball.hud.modicon` | logic:missing | ModIconsPanel has no nexball drawer: no 'carrying' icon and no charge-meter progress bar (grep finds no nexball case in ModIcons / any GametypeStatusBlock).; No event-chase (third-person) cam when not carrying; accuracy/item-stats scoreboard panels not hidden for nexball.; STAT(OBJECTIVE_STATUS) NB_CARRYING + STAT(NB_METERSTART) are not networked (NB_CARRYING does not exist anywhere in the port), so even a drawer would have no data. |
| 5 | 19.5 | `physics-movetypes` | `physics-movetypes.matchticrate.interp` | logic:missing | STILL OPEN: No Movetype_Physics_MatchTicrate equivalent and no tic_* snapshot fields on Entity (grep: zero hits in src/ for MatchTicrate/tic_origin/tic_velocity/tic_flags/sloppy). Entities advance only on the fixed server tick, with no between-tick interpolation in this layer. In Base this smooths CSQC gibs/casings/projectiles/trains at render rate; whether visible in the port depends on its separate render-side interpolation (PredictionBuffer/ClientNet — out of this unit's scope). |
| 6 | 19.5 | `weapon-hook` | `weapon-hook.presentation.rope_line` | logic:missing | No hook ROPE line is drawn. There is no GrapplingHookSend/ENT_CLIENT_HOOK stream (with hook_start/hook_end), no Draw_CylindricLine/Draw_GrapplingHook, and no team-coloured rope texture/WarpZone-traced segment in game/ (all Draw_CylindricLine hits in game/ are unrelated beam/laser/porto renderers). The chain HEAD is networked + rendered as a Hookbomb spike while in flight (and disappears once latched), but the connecting rope between the firer's gun and the latch point is entirely absent. |
| 7 | 19.5 | `weapon-porto` | `weapon-porto.presentation.trajectory_preview` | logic:missing | While holding porto (non-secondary mode), Base draws a reflecting red/blue cylindric-line polyline (with the same slick-continue / noimpact-stop / CheckWireframeBox size checks the placement uses) predicting where the in/out portals will land. The port STILL renders nothing — the signature porto view feature is absent. |
| 8 | 18.0 | `freezetag` | `freezetag.frags.center_message` | logic:missing | Base ft FragCenterMessage replaces the generic kill centerprint with CHOICE_FRAG_FREEZE (attacker: 'You froze X', with kill count + ping) and CHOICE_FRAGGED_FREEZE (target: 'You were frozen by Y'), suppressing it entirely when the target was already frozen (pushed off a cliff). The choice notifications are DECLARED in NotificationsList.cs (1270-1271) but FreezeTag has no FragCenterMessage hook to emit them — they have no live producer. The kill-feed INFO lines (INFO_FREEZETAG_FREEZE/SELF) DO fire (covered by freezetag.presentation.notifications); only the per-kill CHOICE centerprint to the two involved players is missing. |
| 9 | 18.0 | `freezetag` | `freezetag.notif.alone` | logic:missing | Base freezetag_LastPlayerForTeam_Notify (called from PlayerDies/PlayerSpawn/teamchange when a freeze leaves exactly one unfrozen teammate alive) sends CENTER_ALONE to that survivor — the 'you are now alone' warning. The port's FreezeTag never calls a NotifyLastPlayerForTeam (the method exists only on ClanArena.cs, not shared, and FreezeTag.Freeze/OnDeath/OnPlayerSpawn don't invoke it). The ALONE center notification IS declared (NotificationsList.cs:850) but has no live FT producer. |
| 10 | 17.25 | `freezetag` | `freezetag.presentation.ice_model` | logic:missing | No distinct ice block entity (random 1-of-20 frame, team-colored, following the player at origin-16) — frozen players only get an icy-blue colormod on their normal model. |
| 11 | 16.5 | `freezetag` | `freezetag.bots` | values:missing | Bots have no FT-specific freeing/offense roles — they won't deliberately route to and revive frozen teammates (havocbot_role_ft_freeing/offense + havocbot_goalrating_ft_freeplayers absent).; The frozen-target ATTACK gate (Bot_ForbidAttack) IS effectively covered: BotBrain.ShouldAttack (BotBrain.cs:793) skips a target carrying the shared Frozen status effect, so bots don't waste fire on frozen enemies. The dedicated FT Bot_ForbidAttack hook is not registered, but the generic frozen-skip yields the same outcome. |
| 12 | 16.5 | `mutator-nades` | `mutator-nades.presentation.client` | logic:missing | No nade projectile model/trail (nade_customize / nade_spawn CSQCProjectile + Nade_TrailEffect), no orb 3D model (orb_draw), no orb 2D full-screen color flash (orb_draw2d / hud_colorflash_alpha), no darkness blind overlay (HUD_DarkBlinking) + SND_BLIND, no spawn-loc EF_STARDUST render.; No DrawAmmoNades (cl_nades.qc:86): the ammo-panel bonus-nade count + progress bar + (rainbow) type icon is not drawn.; The held-nade charge ring (CrosshairPanel.NadeTimer) is wired + networked AND now functional: a nade can finally be primed (input wired in Wave-5), so NadeTimer climbs 0->1 and the ring appears during a charge. |
| 13 | 16.5 | `weapon-vortex` | `weapon-vortex.charge.rot_decay` | logic:missing | The per-frame charge-decay loop (server/client.qc:2470-2487) is UNPORTED. QC drops vortex_charge back toward charge_limit (or the unequipped limit when the vortex is not the held weapon) at charge_rot_rate / charge_rot_unequipped_rate per frame, but only after vortex_charge_rottime (set in wr_think to time + charge_rot_pause). The port stores VortexChargeRotTime (EntityWeaponState.cs:312) and sets it during charging (Vortex.cs:176), but there is NO decay consumer and the rot_rate / unequipped cvars are never even read. So overcharge (e.g. from velocity charging above charge_limit) would never decay back in the port. |
| 14 | 16.2 | `sv-mapvoting` | `sv-mapvoting.vote_scoreboard_hide` | logic:missing | The scoreboard-hide EFFECT is achieved a DIFFERENT way: UpdateScoreboard models Base's intermission==2 by hiding the scoreboard whenever the MapVote panel is Visible (mapVoteShowing → intermissionShow = MatchIntermission && !mapVoteShowing), so the player does not see the scoreboard during the vote (the visible outcome of Base's SVC_FINALE). Presentation goal met.; But Base's actual MECHANISM is absent: it forces RES_HEALTH=2342 on every client (incl. bots) and sends SVC_FINALE "" once. The port models neither (logic/values/timing missing). The -2342 refs in GameWorld/NetGame are the separate INTERMISSION sentinel (FixIntermissionClient), explicitly NOT reproduced.; Base also zeroes CS(it).impulse each tick after reading it; the port reads the impulse synchronously in DispatchImpulse (no stored per-tick impulse field), so this specific clear is moot. |
| 15 | 15.6 | `sv-cheats` | `sv-cheats.impulse.clone` | logic:missing | No CopyBody / corpse-clone mechanic; the clone impulses do nothing and sv_clones is unread. Unchanged by Wave-5. |
| 16 | 15.6 | `sv-cheats` | `sv-cheats.frame.drag` | logic:missing | No CheatFrame per-frame hook and no Drag subsystem: cannot pick up/carry/throw entities (cheat or normal-range), no te_lightning1 drag beam. Unchanged by Wave-5.; No dragbox_spawn/dragpoint_spawn/drag_remove/drag_setcnt/drag_save/drag_saveraceent/drag_clear race-checkpoint map-editor commands.; g_grab_range cvar exists (Cvars.cs:395) but is never read (no consumer). |
| 17 | 15.6 | `sv-clientkill` | `sv-clientkill.indicator.entity` | logic:missing | No floating numeric countdown model is attached above the player; nothing is networked for it (KillCountdown drives only the announcer + center-print, explicitly NOT the digit entity — KillCountdown.cs:12-14).; Clones on g_clones ghosts (the IL_EACH(g_clones) killindicator copy on each non-respawnghost clone) are also absent.; The digit-entity colormod tint (black / grey / Team_ColorRGB) is unmodeled. |
| 18 | 14.4 | `sv-client-lifecycle` | `sv-client-lifecycle.connect.fix_client_cvars_welcome` | values:missing | More of the welcome channel exists now: the campaign welcome level-num (SendCampaignWelcome), the non-campaign 'Active modifications:' line (SendMutatorsWelcome → BuildMutatorsPrettyString), the g_mutatormsg server mutator message, the sv_motd MOTD (\n-escapes expanded), and the grappling-hook + offhand-blaster gameplay tips are all surfaced to the connecting client. Still unmodeled: FixClientCvars (cl_jumpspeedcap_min/max, cl_shootfromfixedorigin, prediction/gentle), clientstuff, set _teams_available, Send_NextMap_To_Player, and the ClientInit_misc per-client init entity (armor-blockpercent, damagepush speedfactor, fog, serverflags, g_trueaim_minrange, hook/arc shot origins).; The port pushes a few physics-relevant cvars via per-client replicated-cvar tables; there is no FixClientCvars cvar-push stuffcmd and no per-client ClientInit_misc init-entity carrying the server's damage/fog/trueaim constants. (The `cmd clientversion` request IS now handled — Commands.cs:3248 — and its mismatch nag fires via frame.name_and_version.) |
| 19 | 13.0 | `mutator-buffs` | `buffs.carrier_model` | logic:missing | A player holding a buff still has no colored MDL_BUFF glow model attached: no buffs_BuffModel_Spawn/Update/Think/Customize, no removal on observer/disconnect. EF_NOSHADOW IS set on the player while held (ApplyBuff:461), but the glow model itself is absent. |
| 20 | 13.0 | `mutator-buffs` | `buffs.buff_effect` | logic:missing | No per-carrier Send_Effect particle emission throttled by buff_effect_delay; g_buffs_effects (default ON) is never read. EntityMutatorState declares a buff_effect_delay field but nothing emits the effect. |
| 21 | 13.0 | `mutator-buffs` | `buffs.waypoint` | logic:missing | No WP_Buff radar/HUD waypoint on buff items (no buff_Waypoint_Spawn, no RADARICON_Buff, no build-finished timer); the colored buff icon never appears on radar/screen edge, and there is no per-viewer hide-if-already-held logic. |
| 22 | 13.0 | `mutator-sandbox` | `mutator-sandbox.drag.grab` | logic:missing | STILL MISSING: no Drag/Drag_Begin/Drag_Finish system. ObjectThink computes .Grab (e.Grab, written at SandboxMutator.cs:343-347) but nothing consumes it; +button8 does nothing; objects cannot be physically carried. (grep: no Drag_Begin/Drag_Finish/button8 grab consumer anywhere; e.Grab is written but never read.) |
| 23 | 12.0 | `clanarena` | `clanarena.mapinfo.always_supported` | logic:missing | Base CLASS(ClanArena) overrides m_isAlwaysSupported to auto-enable CA on any map with >=8 spawnpoints and a world diameter >3250 (so big arena maps offer CA even without an explicit mapinfo gametype tag). The port has no map-support / auto-gametype heuristic for CA (the same gap noted for Duel.cs:37 and Mayhem.cs:296 — a map-pool concern needing BSP world bounds), so CA availability per map is not derived this way. No in-match gameplay impact. |
| 24 | 12.0 | `cts` | `cts.bot.role` | logic:missing | No CTS bot role: bots are not routed to their next-expected race-checkpoint waypoint (with the raw-touch redirect for unreachable checkpoints), so bots do not actually run the CTS course. (CTS does, however, force _independent_players=1, which makes bots hold fire via the BotBrain fire-gate — see cts.mode.register_latch.) |
| 25 | 12.0 | `overkill-weapons` | `overkill-weapons.mode.weapons_activation` | logic:missing | Base has a SECOND activation path: the ok_weapons mutator un-blocks (clears WEP_FLAG_MUTATORBLOCKED on) the five OK weapons whenever g_overkill_weapons evaluates true, EVEN WITHOUT full Overkill mode (g_overkill). This lets a server add the OK weapons to an otherwise-normal game. The port couples OK-weapon availability to OverkillMutator (g_overkill) only (OverkillMutator.IsEnabled = g_overkill!=0); there is no g_overkill_weapons reader anywhere in src/. |
| 26 | 12.0 | `race` | `race.qualifying.mode` | logic:partial | Retract uses Clients.Spawn (a normal spawn), not the Base checkpoint/grid-aware respawn (race_RetractPlayer + spawn_evalfunc by race_place).; independent_players / MAKE_INDEPENDENT_PLAYER (solo scoring isolation) not modeled.; Qualifying checkpoint records + RACE_NET_CHECKPOINT_*_QUALIFYING packets absent. |
| 27 | 12.0 | `tdm` | `tdm.rules.map_support` | logic:missing | The mapinfo/map-pool support gating that decides which maps auto-offer TDM is not ported: m_isAlwaysSupported (auto-offer on maps with >=8 spawnpoints AND diameter>3250) and m_isForcedSupported (g_tdm_on_dm_maps makes DM maps support TDM too) have no port equivalent. The g_tdm_on_dm_maps cvar ships (default 0) but is read by nothing. Map-pool / menu concern; the port plays whatever map it is told to. Consistently deferred across gametypes (see Duel/Mayhem notes). NOT addressed by Wave-2. |
| 28 | 12.0 | `tdm` | `tdm.rules.map_team_config` | logic:missing | Custom per-map team declarations are not ported: spawnfunc_tdm_team (map-defined team netname + scoreboard color cnt, team=cnt+1) is ignored, and the mapinfo `teams=` override path (m_parse_mapinfo / m_setTeams writing g_tdm_teams) is not implemented. The port reads team COUNT only from g_tdm_teams / g_tdm_teams_override cvars and always uses the default team set. Maps that rely on tdm_team for non-default team names/colors render with the stock teams. NOT addressed by Wave-2. |
| 29 | 12.0 | `weapon-arc` | `weapon-arc.beam.visual` | values:missing | The beam does NOT route through BeamRenderer.Beam. arc_beam is registered isTrail:true (EffectsList.cs:51), classified as Trail first, so the EffectClass.Beam route is never reached; the arc_beam effectinfo block is `type spark` trailspacing 10 with no `orientation beam` line. Renders as a SPARK PARTICLE TRAIL swept origin->endpos, not a cylindric beam. UNCHANGED.; No dedicated networked beam entity (ENT_CLIENT_ARC_BEAM): the beam is re-emitted as an arc_beam trail each SERVER tick and drawn STRAIGHT, not the curving bezier sweep. UNCHANGED.; Missing spinning MDL_ARC_MUZZLEFLASH flash entity, per-beam_type thickness (8/14), beam color, hit/muzzle dynamic lights (ARC_BT_HIT hitlight 20 / muzzlelight 50), and team-color tint. The heal-beam variant IS now selected (ARC_BEAM_HEAL emitted on the heal branch, Arc.cs:456). UNCHANGED otherwise. |
| 30 | 11.7 | `mapobject-misc` | `mapobject-misc.bgmscript.adsr` | logic:missing | No bgmscript ADSR envelope: a func_clientwall / func_pointparticles carrying a .bgmscript key does not animate its alpha / origin / angles / emission to the map's .bgs music script — the prop is static instead of pulsing to the soundtrack. doBGMScript / BGMScript_InitEntity / the .bgs loader are absent. |
| 31 | 11.7 | `warpzones` | `warpzones.client.render` | logic:missing | No Godot SubViewport+Camera3D renders the linked OUT side onto the portal surface; HeroMaterials.BuildPortal (HeroMaterials.cs:168) is a static dark near-black StandardMaterial3D mirror. The warpzone reads as an opaque/wavy wall — you cannot see through it. PVS-fade and the r_water enable HACK are also absent. |
| 32 | 11.7 | `warpzones` | `warpzones.client.fixview` | logic:missing | No client view rotate when the camera straddles a zone, no near-clip push-out (geometry clips at the seam), no roll-kill on a roll-changing crossing, no exterior-model hide. Crossing a warpzone is visually jarring. |
| 33 | 11.7 | `warpzones` | `warpzones.camera` | logic:missing | No func_camera spawnfunc; security-camera dpcamera surfaces render as a dark mirror placeholder (HeroMaterials) rather than a fixed camera view of the target position. |
| 34 | 11.0 | `mutator-overkill` | `overkill.oknex.charge_velocity` | logic:missing | The velocity-based OK-Nex charge accumulation (oknex_charge GetPressedKeys: while holding OK-Nex above charge_minspeed, add up to charge_velocity_rate*f*frametime to oknex_charge, scaled by xy-speed between min/maxspeed) is NOT ported. OkNex.cs ports only the time-based charge regen (ChargeRate*FrameTime) and explicitly defers the velocity-charge path + chargepool (OkNex.cs:20-23 'don't port the charge path unless the cvar is set'). INERT at the shipped balance: g_balance_oknex_charge defaults 0, so neither path runs by default. |
| 35 | 10.8 | `sv-world-rules` | `sv-world-rules.entity.pingplreport` | logic:partial | Only the packet-loss (PL) half is fed: the port has NO ping feed — the scoreboard ping column is hard -1 (ScoreboardPanel.cs:228/249 'Ping isn't networked per row yet'), and there is no playerstats latency record (the LATENCY_THINKRATE=10s avg-latency accumulation for the xonstat report).; Mechanism diverges: Base uses the TE_CSQC_PINGPLREPORT round-robin entity (one client per think, 3/maxclients s); the port quantizes ENet PacketLoss to a 0..255 byte and folds it into the per-tick scoreboard wire (ScoreRowWire.PacketLossByte), de-quantized client-side for the PL column. |
| 36 | 10.5 | `clanarena` | `clanarena.spectate.force_spectate_cmd` | logic:missing | Base intercepts the `spectate` client command for an INGAME player: it force-spectates (MUT_SPECCMD_FORCE) and, when sv_spectate is on and they're already spec/observer, sends INFO_CA_LEAVE ('left the game'). The CA_LEAVE notification IS registered (NotificationsList.cs:703) but has no caller; the port has no CA ClientCommand_Spectate hook, so leaving CA mid-game isn't handled the QC way. Tied to the missing INGAME state. |
| 37 | 10.5 | `clanarena` | `clanarena.hud.spectator_info_messages` | logic:missing | Base cl_ca DrawInfoMessages returns true when ISGAMETYPE(CA) && the local player's entcs spec-state is ENTCS_SPEC_IN_SCOREBOARD, which keeps the HUD info-message panel drawing for a CA spectator who is looking at the scoreboard. The port's InfoMessagesPanel (game/hud/InfoMessagesPanel.cs) ports the cl_lms DrawInfoMessages variant (locally-eliminated LMS player) but has NO CA branch and no ENTCS_SPEC_IN_SCOREBOARD spec-state gate, so this CA-specific info-message visibility is not modeled. Minor client-presentation only. |
| 38 | 10.5 | `ctf` | `ctf.leaderboard` | logic:missing | QC the g_ctf_leaderboard subsystem (ClientConnect race_SendAll, GetPressedKeys race_checkAndWriteName, GetRecords) sends + records the persistent cross-session fastest-capture leaderboard via the race record store + ServerProgsDB. The port ports neither the race record store nor any of these CTF hooks, so the persistent leaderboard does not exist (the in-match fastest-cap broadcast in ctf.flag.capture is unaffected). |
| 39 | 10.5 | `freezetag` | `freezetag.presentation.frozen_anim` | logic:missing | Base ft PlayerAnim ORs ANIMSTATE_FROZEN into the player's animation state so a frozen player holds the static frozen pose. The port conveys frozen-ness only via the FrozenColormod tint (freezetag.presentation.ice_model); the frozen animation-state freeze is not applied, so a frozen player's normal animation may keep playing under the tint. |
| 40 | 10.5 | `overkill-weapons` | `overkill-weapons.oknex.zoom` | logic:missing | oknex wr_zoom/wr_zoomdir not ported as an OK-Nex-specific override. At stock balance (secondary==2 = blaster), the QC gate `!WEP_CVAR(secondary)` is false, so ATCK2 does NOT trigger zoom — the OK Nex only zooms via the dedicated zoom button (button_zoom/zoomscript), i.e. the generic per-weapon zoom path. So at stock balance the observable behavior is the generic zoom button only; no OK-Nex-specific divergence is visible.; If a server sets g_balance_oknex_secondary 0, QC would zoom on ATCK2 (and forgo the blaster secondary) — that non-default branch is unmodeled. |
| 41 | 10.4 | `cl-scoreboard` | `cl-scoreboard.block.itemstats` | logic:missing | No item-stats block at all: Base shows a per-item icon grid of pickup counts (g_inventory.inv_items), filtered by itemstats_filter_mask=12. The port has no SetItemStats / DrawItemStats and inventory is not networked for it. |
| 42 | 10.4 | `cl-scoreboard` | `cl-scoreboard.ui.interactive` | logic:missing | No interactive UI: the port scoreboard is a passive overlay. Missing TAB panel cycling (SCOREBOARD↔RANKINGS), arrow-key player/team selection, ENTER/SPACE team-join + spectate, Ctrl+C column-layout cycle, Ctrl+R scores_per_round toggle, Ctrl+T tell, Ctrl+K vote-kick, ESC exit, and the team-selection screen (scoreboard_ui_enabled==2) with its join prompts and world dimming (0.7). |
| 43 | 10.4 | `cl-teamradar` | `cl-teamradar.links.onslaught` | logic:missing | Onslaught radar links (ENT_CLIENT_RADARLINK / draw_teamradar_link / g_radarlinks) not networked or drawn — ONS radar shows control points but no connection lines. |
| 44 | 10.4 | `cl-teamradar` | `cl-teamradar.maximized.map` | logic:missing | Maximized fullscreen radar entirely absent: no m-bind (+hud_panel_radar_maximized → cl_cmd hud radar 1), no fullscreen tactical overview, no separate maximized rotation/zoom/scale runtime, no default-border swap. |
| 45 | 10.4 | `cl-teamradar` | `cl-teamradar.maximized.click` | logic:missing | No clickable ONS spawn-point selection (cmd ons_spawn), no teleport-click, no mouse/key input handling (MOUSE1/MOUSE2/ESC/console-key passthrough), no dead+showscores temp-hide, no 'Click to select…' prompt, no icon hover-glow. |
| 46 | 10.4 | `cl-view` | `cl-view.demo.free_camera_lockview_ortho` | logic:missing | No demo free/chase camera (CSQC_Demo_Camera), no view lock (cl_lockview), no orthographic radar-render view (cl_orthoview / View_Ortho), no viewloc 2.5D side-scroller camera (GetViewLocationFOV), and no FPS report to the server (fpscounter_update STAT(SHOWFPS)). |
| 47 | 10.0 | `mutator-invincibleproj` | `mutator-invincibleproj.client.active_mut_flag` | logic:missing | The Base GetActiveMutators-style scan (util.qc:280-330) walks the active_mutators cvar (or, when empty, the live cvars) and calls mut_set_active(MUT_INVINCIBLE_PROJECTILES) + appends the translated name when g_invincible_projectiles is set, so the menu / server-browser 'active mutators' list and any MUT_ bitmask consumer can light up this mutator. The port still has NO active_mutators / mut_set_active / MUT_ flag implementation (only TouchExplodeMutator.cs comments reference the gap; zero impl hits across src+game). |
| 48 | 9.9 | `mapobject-teleporters-portals` | `portals.entity.lifecycle` | logic:partial | Porto portals are realised as a seamless two-way warpzone, so there is no portal health (g_balance_portal_health=200) — Base portals can be shot down (Portal_Damage); the cvar is unused.; Portal expiry is now modeled (15s g_balance_portal_lifetime, recharged on each crossing, ExpirePortals sweep at GameWorld.cs:1787) BUT the SUB_SetFade(time,0.5) 0.5s visual fade-out is absent — a timed-out pair is hard-removed, not faded; and the recharge-to-200-health half is moot (no portal health).; No portal model (MDL_PORTAL) / skins (0/1/2) or EF_RED / EF_BLUE\|EF_STARDUST / EF_ADDITIVE glow; a warpzone is a transparent window, not a Quake portal disc.; SND_PORTO_EXPLODE / SND_PORTO_EXPIRE (CH_SHOTS) and EFFECT_ROCKET_EXPLODE on destruction/expiry never play (Portal_Remove).; Portal_Customize visibility gating (hiding the portal model from players who can't use it, IS_INDEPENDENT_PLAYER) is absent.; Portal_FindSafeOrigin (move_out_of_solid 16u in front, portal_safe_origin) and CheckWireframeBox 96^3 clearance are not modeled. |
| 49 | 9.75 | `cts` | `cts.lifecycle.prepare_player` | logic:partial | OnPlayerSpawn now does the run-clock reset (AbandonRun, incl. the .race_movetime accumulator) AND race_checkpoint=-1 (Cts.cs:701-705) — the meaningful half of race_PreparePlayer for a single start->stop course. OnMakePlayerObserver is now ported+live (FRAGS_PLAYER_OUT_OF_GAME when the observer holds a ranked time, else FRAGS_SPECTATOR; AbandonRun + race_checkpoint=-1, Cts.cs:611-623), matching QC. Still MISSING: ClientConnect / PutClientInServer / AbortSpeedrun hooks (race_SendAll on connect, the PutClientInServer spawn==qualifying re-prepare-vs-race_RetractPlayer branch, the g_allow_checkpoints practice-mode re-prepare), and respawn-checkpoint/spotref recording. |
| 50 | 9.75 | `race` | `race.spawn.grid_respawn` | logic:partial | race_place START-GRID ordering still missing: racers do not get ordered start positions in a multi-racer race (no race_place read / info_player_race grid).; trigger_race_checkpoint_verify map-load validation (missing-spawn error) not performed. |
| 51 | 9.6 | `cl-viewmodel` | `cl-viewmodel.anim.networked_frames` | values:missing | No networked weapon fire/idle/reload animation (the QC wframe temp entity is not networked); the gun never visibly cycles or reloads. |
| 52 | 9.6 | `sv-antilag` | `sv-antilag.solidmask.corpse` | logic:missing | Base temporarily switches the shooter's hit-contents mask to also include CORPSE so a hitscan can hit gibbed/corpse bodies during the antilag trace (tracebox_antilag_force_wz, and W_SetupShot's pre-trace mask); the port's FireBullet/FireRailgunBullet/SetupShot do not adjust any contents mask around the trace. The port HAS corpse entities (Solid.Corpse / Entity.IsCorpse), so corpse-hitting behavior may differ unless the port's default trace already includes corpse-solids. |
| 53 | 9.6 | `sv-antilag` | `sv-antilag.mode.client_verified_and_cursor` | logic:missing | g_antilag modes 1 (verified client-side) and 3 (prydon cursor hitscan) are unimplemented: a server set to either gets NO lag compensation at all (port treats anything != 2 as no-rewind). |
| 54 | 9.6 | `sv-commands-votes` | `sv-commands-votes.debug_tools.missing` | logic:missing | radarmap is not registered: no in-game radar-image (XPM) generation for the minimap.; gettaginfo / animbench / bbox / trace / stuffto / database / effectindexdump / make_mapinfo console verbs are unimplemented (no Register() calls). |
| 55 | 9.6 | `sv-teamplay` | `sv-teamplay.queue.join_queue` | logic:missing | No join-queue: players join immediately regardless of balance; the deficit-filling tag-in (TeamBalance_QueuedPlayersTagIn: specific-team-first then any-team) and QueueNeeded / QueuedPlayersReady are absent.; wants_join (WantsJoin) exists on Player.cs:172 and is only ever assigned 0 (connect/observer transitions, plus KickTeamkillerMutator.cs:152 on a kicked attacker); it is never set >0 or consulted for queueing — the field is plumbed but inert. |
| 56 | 9.1 | `bot-waypoints` | `bot-waypoints.editor` | logic:missing | No in-game waypoint editor: cannot place/remove/hardwire/mirror waypoints (g_waypointeditor*); no editor visualization (waypoint model, link lightning beams, colormod by flag).; No runtime auto-waypointing toward players (botframe_autowaypoints) or useless-waypoint pruning (botframe_deleteuselesswaypoints): the loaded graph is static. |
| 57 | 9.0 | `sv-client-lifecycle` | `sv-client-lifecycle.frame.prethink_misc` | logic:partial | UpdateChatBubble (the chatbubble.spr / minigame_busy bubble over a typing or minigame-busy player, ChatBubbleThink + ShowChatBubble hook) is not modeled — no visible 'is typing' bubble.; The PlayerPreThink +use edge dispatch (PlayerUseKey) is not a single ported function: individual +use consumers (buttons/vehicles/ctf) read the button directly, but the usekeypressed edge tracker + PlayerUseKey routing (and its spectate/minigame branches) are scattered/absent.; SetZoomState (server-side weapon-zoom state for the zoom HUD/FOV net stat) + the taunt_soundtime taunt voice + target_voicescript_next are not driven from a lifecycle PreThink. (The deferred teamkill_soundtime friendly-fire complaint voice IS now fired — PlayerFrameLogic.Regen:130-149.) |
| 58 | 9.0 | `warpzones` | `warpzones.client.teleported_view` | logic:missing | Server never sends a warpzone_teleported entity and never re-rotates v_angle (WarpZone_PlayerPhysics_FixVAngle); the client never rotates VF_CL_VIEWANGLES / CL_RotateMoves on a crossing. The view does not carry through the seam. |
| 59 | 9.0 | `weapon-vortex` | `weapon-vortex.fx.charge_glow` | logic:partial | Glow is applied to the FIRST-PERSON VIEW model (the local player's own gun, via ViewModel.SetTeamGlow), NOT the exterior player/carrier model that QC's wr_glow→weaponentity_glowmod brightens for OTHER players. So the local player's gun now brightens with charge, but a remote observer still sees the carrier identical regardless of charge.; Host-only: VortexCharge is read off LocalServerPlayer's slot state (NetGame.UpdateViewModelTeamGlow), so on a pure remote client there is no charge value and the glow stays at the base team color. Same cross-client-charge-STAT root cause as the charge ring and beam tint. |
| 60 | 8.8 | `cl-teamradar` | `cl-teamradar.cvar.dynamichud` | liveness:dead | hud_panel_radar_dynamichud is registered with the correct default (1) but nothing consumes it: the port has no HUD_Scale_Enable/Disable (the dynamic-HUD shrink applied when the player is alive) so the radar never participates in dynamic-HUD scaling.; HUD_Radar_Export (writing the radar's 8 aesthetic cvars into a saved hud skin file) has no port analogue; the port's skin/HUD-config export path does not round-trip these radar cvars. |
| 61 | 8.7 | `assault` | `assault.notify.play_hitsound` | liveness:dead | The PlayHitsound handler is registered (Assault.cs:598-599) and its predicate matches QC exactly, but the hook chain is DEAD: MutatorHooks.FirePlayHitsound (MutatorHooks.cs:611) has ZERO callers. The port's hitsound is driven entirely by the STAT(HITSOUND_DAMAGE_DEALT_TOTAL) accumulator (DamageSystem.cs:337-343), which only accrues for IS_PLAYER victims and never consults the PlayHitsound mutator hook. So the assault forced-hitsound on a wall victim never actually plays. Behaviorally parity-neutral (a func_assault_destructible victim isn't a player, so the stat path wouldn't fire a hitsound either), but the code is unreachable. |
| 62 | 8.4 | `vehicle-framework` | `vehicle-framework.death.gibs` | logic:missing | vehicle_tossgib (the vehicle_gib physics-entity subsystem) is UNPORTED — only a NOTE comment at Bumblebee.cs:502. A destroyed vehicle's vr_death does not spawn the flying debris gibs (model gib at a tag, tossed with velocity+avelocity, optional burn/explode, alpha fade, explosion FX + rocket-impact sound on contact). The descriptors emit a single death EXPLOSION_* FX (Wave 8) but no per-piece gibs. |
| 63 | 8.0 | `cl-crosshair` | `cl-crosshair.core.origin_chase` | logic:missing | Crosshair is always drawn at the geometric panel centre (DrawPanel: center = Size2*0.5f). The chase-camera branch (trace from player org along view_forward MOVE_WORLDONLY, project the impact, fade the player body alpha toward crosshair_chase_playeralpha while it obstructs the view, restore on chase-off via the crosshair_chase_state latch) is not implemented.; crosshair_chase / crosshair_chase_playeralpha / crosshair_2d are now REGISTERED in RegisterDefaults (CrosshairPanel.cs:323-325) with the Base default values, but have NO reader — they are inert settable cvars (the chase trace + body-alpha fade + side-scroller selection are still unported). |
| 64 | 7.8 | `turret-framework` | `turret-framework.net.sync` | logic:missing | No turret networking: none of the TNSF_* send/parse / CSQC turret edict exists. |
| 65 | 7.8 | `vehicle-raptor` | `raptor.hud_crosshair` | logic:missing | Entire CSQC HUD path absent: no Vehicles_drawHUD ammo bars, no per-mode reticle (vCROSS_BURST bomb / vCROSS_RAIN flare), no raptor_dropmark tracetoss bomb-impact predictor (gravity-bounce traced to a 2D drawpic marker, green while live / red after release), no aux lock crosshair (vCROSS_LOCK) color feedback. Client-only; the server logic these decorate (mode switch, lock strength) is all live elsewhere. |
| 66 | 7.8 | `vehicle-raptor` | `raptor.cb_shellfrag_gib` | logic:missing | The bomb-casing shell-fragment gibs tossed client-side when the cluster bomb bursts (RaptorCBShellfragToss spawns MOVETYPE_BOUNCE drawables with avelocity jitter that fade over ~2-3s) are not ported. Purely cosmetic debris; the bomb burst gameplay (raptor.bombs) is fully live. |
| 67 | 7.7 | `bot-ai` | `bot-ai.special.jetpack_rocketjump` | logic:missing | Jetpack navigation (take off / fly to point / brake-land) is missing: bots can't cross jetpack-only gaps even with bot_ai_navigation_jetpack on. (UNCHANGED.); Skill>6 trigger_hurt escape (jetpack up, or Devastator rocketjump if HP allows) is missing: bots die in hurt volumes they could have escaped. (UNCHANGED.) |
| 68 | 7.7 | `bot-ai` | `bot-ai.scripting.commands` | logic:missing | Bot scripting entirely missing: the bot_cmd command queue does nothing, so map/console bot scripts, cutscene-driven bots, and g_waypointeditor bot driving are inert. (UNCHANGED.) |
| 69 | 7.65 | `mapobject-misc` | `mapobject-misc.models.clientwall_fade` | logic:partial | func_clientwall distance-fade is not rendered: fade_start/fade_end/alpha_max/alpha_min/fade_vertical_offset are parsed onto the Entity (MapObjectFieldsExtra.cs) but NO renderer applies Ent_Wall_PreDraw's distance-fade alpha formula or its PVS alpha-cull — a clientwall meant to fade out with distance stays fully opaque. (The ClientWorld.cs:1234 ItemDistanceAlpha fade is the unrelated item-despawn fade; FadeEndDist is otherwise only read by WeatherSystem for rain/snow draw distance.); The antiwall .inactive flag toggles SOLID server-side but does NOT also force the wall's render alpha to 0 (Ent_Wall_PreDraw sets alph=0 when inactive); a deactivated clientwall goes non-solid but stays VISIBLE until a wall renderer consumes .inactive. |
| 70 | 7.65 | `mapobject-teleporters-portals` | `portals.weapon.hook_and_predict` | values:missing | Base Portal_Think predicts a player/hook crossing the portal plane THIS frame (Portal_WillHitPlane, PHYS_GRAVITY-aware tracebox over velocity*2*frametime, portals.qc:237/317) and teleports pre-emptively so fast movers don't tunnel; the warpzone realisation relies on a fixed trigger overlap + a same-frame finishtime guard + plane-side test, with no velocity*2*frametime predict-ahead.; A player's active grappling hook is still not teleported through the portal (Portal_Think iterates weaponentity.hook); the warpzone path does not relocate the hook entity. |
| 71 | 7.5 | `clanarena` | `clanarena.matchend.restore_status` | logic:missing | At match end Base restores each player's pre-CA spectator/team status from the .prev_team field (snapshotted in CA_RoundStart) so the post-match scoreboard reflects who was actually playing. The MatchEnd_BeforeScores hook infrastructure now EXISTS in the port (MutatorHooks.FireMatchEndBeforeScores, fired at GameWorld.cs:3865), but CA does not subscribe to it and snapshots no prev_team, so the final-scores spectator/team restoration is not modeled. |
| 72 | 7.5 | `clanarena` | `clanarena.cmd.shuffleteams_reset` | logic:missing | Base defers a `shuffleteams` command issued mid-round to the next ResetMap (shuffleteams_on_reset_map = !allowed_to_spawn) so a live CA round isn't disrupted. The port has a `shuffleteams` admin command (Commands.cs:627/CmdShuffleTeams) but no CA SV_ParseServerCommand hook to round-defer it, so it shuffles immediately even mid-round. |
| 73 | 7.5 | `ctf` | `ctf.abort_speedrun` | logic:missing | QC the ctf AbortSpeedrun hook: when a player aborts a speedrun (CTS/race-style speedrunning) while carrying a flag, the flag is force-respawned (ctf_RespawnFlag) and INFO_CTF_FLAGRETURN_ABORTRUN is broadcast. The port has no AbortSpeedrun dispatch (the speedrun/CTS abort path is not wired into CTF), so a carried flag is not auto-returned on a speedrun abort. |
| 74 | 7.5 | `ctf` | `ctf.drop_special_items` | logic:partial | QC the ctf DropSpecialItems hook throws a carried flag (ctf_Handle_Throw DROP_THROW) when the generic DropSpecialItems event fires (e.g. nade-drop / forced item-drop). The port drops a carried flag on death (OnDeath -> DropFlag) but has no DropSpecialItems hook, so any non-death special-item-drop path that exists for other items does NOT also eject the flag. |
| 75 | 7.5 | `ctf` | `ctf.logdeath_itemcode` | logic:missing | QC appends the item code 'F' to a player's death-log line when they die carrying the flag (LogDeath_AppendItemCodes). The port's death logging does not append the CTF flag item code. |
| 76 | 7.5 | `ctf` | `ctf.ignore_frags` | logic:missing | QC the ctf GiveFragsForKill hook zeroes the per-kill frag score (M_ARGV(2)=0) and, when g_ctf_ignore_frags is set, also suppresses the frag COUNT entirely. The port has no GiveFragsForKill hook for CTF, so a CTF kill still books a normal frag and g_ctf_ignore_frags is a no-op. |
| 77 | 7.5 | `ctf` | `ctf.followfc_superspec` | logic:missing | QC the ctf SV_ParseClientCommand hook handles the spectator 'followfc [team]' command (gated by g_superspectate): a superspectator jumps to spectating the/an active flag carrier (optionally of a given team), or gets a 'No active flag carrier' message. The port has no followfc command and no superspectate subsystem. |
| 78 | 7.5 | `ctf` | `ctf.prefer_score_clear` | logic:missing | QC the ctf PreferPlayerScore_Clear hook returns true, signalling the scoreboard-clear path to prefer the per-player score representation in CTF. The port has no equivalent hook; the port's scoreboard-clear path does not branch on a gametype preference, so this nuance is unmodeled. |
| 79 | 7.5 | `domination` | `domination.roundbased.player_blocked` | logic:partial | QC reset_map_players sets it.player_blocked = 1 on map reset (roundbased) and PlayerSpawn sets player.player_blocked = !round_handler_IsRoundStarted(); Domination_RoundStart clears player_blocked = false for all players. The port's RoundStart only clears LastRoundWinner — no per-player block FLAG is set on reset/spawn or cleared at round start. The generic round-not-started gates (WeaponFireDriver.RoundFireForbidden @GameWorld.cs:1621, the dompoint capture gate via the now-live RoundStartedSource, and respawn suppression @GameWorld.cs:3476) DO hold players in check during the pre-round countdown, but the QC movement-freeze semantics of player_blocked are not reproduced 1:1. |
| 80 | 7.5 | `freezetag` | `freezetag.damage.softkill_void` | logic:partial | STILL OPEN — no soft-kill relocate path (g_frozen_damage_trigger 0): the NEEDKILL soft-kill (restore HP=1+armor, teleport to a spawn point, no weapon reset) in PlayerDies / the Damage_Calculate relocate branch is absent — FT.OnDamageCalculate leaves NEEDKILL deathtypes alone (frozen player dies in void/lava), and the relocate needs the server spawn selector (intentionally NOT done in the gametype layer). Base default cvar is 1 = die, so this branch is dormant at stock cvars regardless.; Fall-damage revive (g_frozen_revive_falldamage) is implemented and live (FreezeTag.cs:507-515): a hard enough DEATH_FALL hit thaws the player to falldamage_health (40) and sends FREEZETAG_REVIVED_FALL/_REVIVE_SELF. Default off, so dormant at stock cvars but correct when enabled.; Nade self-revive is now FULLY implemented (FIX since baseline): FreezeTag.OnDamageCalculate (FreezeTag.cs:485-499) Unfreezes the player, restores g_freezetag_revive_nade_health (40), suppresses damage/force, and sends INFO_FREEZETAG_REVIVED_NADE + CENTER_FREEZETAG_REVIVE_SELF. g_freezetag_revive_nade ships 1 so it is live. (NadesMutator still also zeroes the same hit, but the actual unfreeze now lives in FT, hook-order-independent.) |
| 81 | 7.5 | `keyhunt` | `keyhunt.capture.instant_path_dead` | liveness:dead | AssignKey (KeyHunt.cs:415) routes to CheckCaptureGeometry() — the maxdist-gated path — so it would score CORRECTLY if a future caller picked it. It remains dead (zero source callers; the live collect path is KeyTouchEntity->CollectKey->CheckCaptureGeometry). Harmless dead code, no mis-score hazard. |
| 82 | 7.5 | `lms` | `lms.bot.fixcount` | logic:missing | Base overrides Bot_FixCount (CBC_ORDER_EXCLUSIVE) so the bot-balancer counts only INGAME real clients as activerealplayers (an eliminated/out-of-game player no longer keeps a bot slot filled), while realplayers counts every real client. The port has no LMS Bot_FixCount equivalent (grep found no Bot_FixCount/activerealplayers anywhere in src/), so bot count balancing during an LMS match does not exclude eliminated players. NEWLY MAPPED coverage gap (was absent from the registry). |
| 83 | 7.5 | `lms` | `lms.scoreboard.rank_intermission_write` | logic:missing | Base AddPlayerScore (sv_lms.qc:725-730) permits writing the SP_LMS_RANK score field even while game_stopped (intermission), because a newly-joining player still needs a rank assigned. The port's GameScores.AddPlayerScoreHook is generic (used by Survival/dynamic_handicap) and LMS does not register an LMS-specific override to whitelist RANK writes in intermission. Likely benign (the port assigns Rank directly on the LmsState and SyncColumns mirrors it), but the explicit Base whitelist is not modeled. NEWLY MAPPED coverage gap. |
| 84 | 7.5 | `mutator-buffs` | `buffs.item.auto_spawn` | logic:partial | Default count is 0 so this never runs unless explicitly configured (matches Base default) — this stays liveness:partial. (The map item_buff_* spawnfuncs are now the primary obtain path; see buffs.item.spawnfunc.); QC defers via InitializeEntity(buffs_DelayedInit, INITPRIO_FINDTARGET) and sets spawnflags\|=64 (always randomize/relocate) on each seed; port sets BuffAlwaysRelocate=true on each auto-seed (SpawnInitialBuffs), then routes through ConfigureBuffEntity (which now sets the relic model + box + cooldown + think + touch, same as the map spawn). Still spawns immediately at Hook() (not via the INITPRIO_FINDTARGET defer). Residual presentation gap: per-buff skin/color/glowmod still not applied, and no waypoint. |
| 85 | 7.5 | `mutator-buffs` | `buffs.item.random_location` | logic:partial | MoveToRandomMapLocation relocation IS done (BallEntity.RandomMapLocation, which folds in the SelectSpawnPoint fallback), with the '0 0 200' launch + a mins*1.5/maxs*1.5 tracebox unstick + g_buffs_random_lifetime arming + SND_KA_RESPAWN on relocation. The g_buffs_randomize teamplay gate (& (teamplay?2:1)) and g_buffs_random_location/spawnflag-64 relocate gate are honored.; Still missing: g_buffs_random_location_attempts is not read (RandomMapLocation uses its own fixed sampling rather than the attempts/256/1024 box-content rejection of MoveToRandomMapLocation), so the relocation's content-validity check differs from QC. No EFFECT_ELECTRO_COMBO x2 visual at the relocation, no WaypointSprite_Ping. |
| 86 | 7.5 | `weapon-crylink` | `crylink.reload` | liveness:dead | Clip-load reload semantics: WrReload (the base WeaponFireGate machine) reloads the generic clip; QC's wr_reload also passes SND_RELOAD. The reload SOUND cue (SND_RELOAD) is whatever the base reload machine plays — not crylink-specific, faithful by inheritance. No crylink-specific reload gap remains. |
| 87 | 7.5 | `weapon-electro` | `electro.primary.midaircombo` | liveness:dead | DEAD on the live path: midaircombo_radius defaults 0 (bal-wep-xonotic.cfg:232), so the BoltThink midair branch (Electro.cs:323-334 -> MidairTriggerCombo) never runs under stock xonotic balance. The branch IS reachable (and now faithful) under samual/xdf balance where the cvar is 100.; IS_INDEPENDENT_PLAYER orb-owner skip is ported (Electro.cs:356) but Entity.IsIndependentPlayer is hardwired false, so the guard is dead-OFF-equivalent (no independent-player mode is ported). Harmless: default modes have no independent players. |
| 88 | 7.5 | `weapon-machinegun` | `machinegun.fire.single_mode0` | liveness:dead | mode 0 is not the bundled default (mode 1), so AttackSingle/AttackFrame/the snipe are dead on the live path — matches Base, where the xonotic balance also defaults mode 1 (only bal-wep-nexuiz25.cfg sets mode 0). |
| 89 | 7.5 | `weapon-machinegun` | `machinegun.reload.checkammo` | liveness:dead | Inert at stock balance only: reload_ammo defaults 0 in bal-wep-xonotic.cfg, so the forced-reload branch (WrThink:120 gated on ReloadingAmmo()!=0) and the clip cycle never run in stock play — identical to Base, where reload is likewise off. The code is now fully present and faithful, so liveness is 'dead' purely because no stock cvar reaches it; a server setting reload_ammo>0 exercises the same clip/reload behavior as Base. |
| 90 | 7.5 | `weapon-porto` | `weapon-porto.fire.aim_hold` | liveness:dead | Only reachable when g_balance_porto_secondary=0 (non-default); dormant in stock play (liveness:dead). |
| 91 | 7.2 | `cl-csqcmodel` | `cl-csqcmodel.animdecide.state_machine` | logic:partial | Upper-body ACTIONS still NOT ported: the networked anim_state / anim_upper_action / anim_lower_action / anim_time inputs are not sent or read (grep confirms zero anim_* bits in NetEntity/ClientEntityView), so shoot/melee/pain/draw/taunt torso overlays never play on the third-person model (the torso shows a single stable idle/aim clip, Split torsoTime=0).; Per-anim framerates (die 0.5fps, shoot 5, jump 10, draw 3, taunt 0.33) and the per-clip framegroup names are not used; clips are picked by a fuzzy name Contains match with a 20fps default (BuildClipTable.Pick + FrameGroup(...,20f,...)).; The CSQC-side implicit jump detection (ground->air transition sets ANIMACTION_JUMP) is approximated by the inAir->Jump/DuckJump clip pick; no explicit end-of-jump-anim freeze (the comment 'play the END of the jump anim' notes the intent but the non-looping clamp is the approximation). |
| 92 | 7.2 | `cl-scoreboard` | `cl-scoreboard.field.name_icons` | logic:partial | Handicap icon is PORTED + LIVE (DrawRow:1075-1083 draws a square at the EXACT QC tint '1 0 0' + '0 1 1' * ((16-lvl)/15), white@1 → red@16, fed by the networked handicap_level).; Still missing: player-color shirt/pants swatch (gfx/scoreboard/playercolor_*), pulsing ready check, ignored icon, the pulsing (Q) wants-join team marker, and the playerid '#N' prefix (Scoreboard_AddPlayerId).; Handicap icon is drawn as a flat colored square (no gfx/scoreboard/player_handicap texture); presentation faithful in tint but not in the actual icon art. |
| 93 | 7.2 | `mutator-multijump` | `mutator-multijump.counter.networking` | logic:unknown | Whether the predicted client sees a correctly server-synced MultijumpCount (vs a server-only tally) was not traced; a desync could cause a predicted re-jump to be granted/denied differently from the server near the count limit. |
| 94 | 7.2 | `sv-client-lifecycle` | `sv-client-lifecycle.join.join_allowed` | logic:partial | MIN_SPEC_TIME + (teamplay && lockteams) + Player_GetForcedTeamIndex==TEAM_FORCE_SPECTATOR + g_playban (ForcedSpectate host hook→Bans.IsPlayBanned) + a gametype ForbidSpawn/late-join gate (LMS lockout, GametypeJoinGate) ARE enforced. Still missing: version_mismatch, g_maxping (ping<=0 too-soon + ping>maxping), and the whole QueueNeeded/queuePlayer/ShowTeamSelection + nJoinAllowed/GetPlayerLimit/g_maxplayers/bots_would_leave capacity gate.; No CENTER_JOIN_PREVENT / CENTER_JOIN_PREVENT_VERSIONMISMATCH / CENTER_JOIN_PLAYBAN / CENTER_JOIN_PREVENT_PING / CENTER_TEAMCHANGE_LOCKED centerprints for blocked joins. |
| 95 | 7.0 | `items-pickups` | `items-pickups.itemgroup.networking` | logic:missing | Small-item clustering is not ported: Base groups small health/armor/ammo within 120qu into one item_group (networked via item_group/item_group_count) so the itemstime HUD can show a single 'xN' badge for a tightly-packed cluster instead of overlapping timers. The port has no item_group field, no setItemGroup/setItemGroupCount pass, and the itemstime producer (ItemstimeMutator) doesn't aggregate clustered smalls. Presentation-only effect on the timed-item HUD. |
| 96 | 7.0 | `mutator-bloodloss` | `mutator-bloodloss.presentation.active_mutator_signalling` | logic:partial | The util.qc:309 X("Blood loss", MUT_BLOODLOSS, g_bloodloss>0) GameType active-mutator row and the CSQC mut_set_active(MUT_BLOODLOSS) HUD active-mutator icon are STILL entirely unported — the port has no MUT_BLOODLOSS / mut_set_active / active_mutators flag surface at all, so the menu/server-browser HUD active-mutator ICON can never light up (the textual 'modifications' string IS now surfaced, but not the bitmask/icon).; BuildMutatorsString (':bloodloss') is LIVE but only via the gamelog/eventlog feed (sv_eventlog-gated) and the 'modifications' serverinfo; it is not consumed by an in-game scoreboard. |
| 97 | 7.0 | `mutator-buffs` | `buffs.observer_cleanup` | logic:missing | Still no MakePlayerObserver / ClientDisconnect handler to run buffs_RemovePlayer (delete the carrier buff_model). Moot only because the buff_model glow itself is unimplemented (buffs.carrier_model). |
| 98 | 7.0 | `mutator-touchexplode` | `touchexplode.list.activemutators` | logic:missing | Base appends 'Touch explode' to the active_mutators() X-table string (util.qc:315, MUT_TOUCHEXPLODE bitfield) used for the server-browser mutator field and the scoreboard/info mutator list. The port has no active_mutators()/MUT_-bitfield string builder. NOTE: the port's BuildMutatorsString/BuildMutatorsPrettyString hook infra (MutatorActivation.cs) is a DIFFERENT mechanism (the per-mutator server-info/scoreboard token chain), and Base touchexplode registers NO BuildMutatorsString hook, so that infra does not advertise touchexplode and does not close this gap. Systemic gap shared by all mutators; only the create-game menu checkbox (DialogMutators.cs:132) exists. |
| 99 | 7.0 | `mutator-waypoints` | `waypoints.visibility.three_image_rule` | logic:missing | Port still networks a single SpriteName; the own-team/enemy-team/spectator image triple (model1/2/3) and the client-side SPRITERULE_TEAMPLAY image selection are not modeled. |
| 100 | 6.75 | `keepaway` | `keepaway.ball.respawn` | logic:partial | The relocate MOVES the ball — BallEntity.Relocate calls RandomMapLocation (a random spawnpoint, falling back to a jitter around home) + the '0 0 200' kick + re-arms NextThink; the loose-ball think (RespawnThink) is live via SpawnForGametype.; SND_KA_RESPAWN plays at relocate (Keepaway.RespawnBall → SoundSystem.PlayOn KA_RESPAWN, ATTEN_NONE).; FIXED (was STILL OPEN): the NEEDKILL ka_DamageEvent respawn path is now LIVE — AdoptBall installs OnBallDamage on the ball's Entity.GtEventDamage, the live DamageSystem.EventDamage (DamageSystem.cs:331-333) routes a non-player edict's damage to it, and a NEEDKILL hit (DeathTypes.ItemDamageNeedKill) force-respawns the loose ball (registry was 'no NEEDKILL respawn path').; STILL OPEN (logic): RandomMapLocation uses the QC FALLBACK path (random info_player_* spawnpoint) only — it does NOT do the full MoveToRandomMapLocation world-bounds sampling with the SOLID/SLIME/LAVA/SKY content rejection. Functionally close (ball relocates to a reachable spawn) but not bit-identical placement.; STILL OPEN (logic): no NOIMPACT fall-off respawn branch (ka_TouchEvent trace_dphitq3surfaceflags & Q3SURFACEFLAG_NOIMPACT — a ball that touches a NOIMPACT surface, i.e. off the map, is not force-respawned; the NEEDKILL damage branch IS now covered).; STILL OPEN (presentation): no EFFECT_KA_BALL_RESPAWN particle at the old + new origin (WP_KaBall waypoint sprite IS now spawned via CollectWaypoints — see keepaway.waypoints.tracking). |
| 101 | 6.75 | `keepaway` | `keepaway.ball.drop` | logic:partial | The drop velocity includes the crandom xy scatter (DropOffsetVelocity == Base '0 0 200' + '0 100 0'*crandom + '100 0 0'*crandom; Prandom.Signed == crandom); the 0.5s previous_owner/wait self-recapture lockout is armed (GtNextTakeTime + Owner=carrier).; SND_KA_DROPPED plays globally (PlayGlobal KA_DROPPED, ATTEN_NONE); INFO_KEEPAWAY_DROPPED (all) + CENTER_KEEPAWAY_DROPPED (all) notifications sent and registered.; FIXED (was STILL OPEN, liveness partial→live): ALL FOUR drop triggers are now wired. DEATH (OnDeath → Drop, Combat.Death bus); USE-KEY (OnUseKey on MutatorHooks.PlayerUseKey, fired live via VehicleBoarding.cs:106 FirePlayerUseKey on the +use impulse path); DISCONNECT + OBSERVE (OnPlayerRemoved, fired live at ClientManager.cs:473 PutObserver + :682 ClientDisconnect). QC's ClientDisconnect/MakePlayerObserver both ka_DropEvent.; STILL OPEN (logic): no nudgeoutofsolid_OrFallback after placing the dropped ball; no multi-ball chain advance / ka_PlayerReset chain semantics (single ball only).; STILL OPEN (presentation): EFFECT particle on drop is not emitted (the WP_KaBall waypoint sprite IS now re-spawned via CollectWaypoints when the ball is loose — see keepaway.waypoints.tracking).; NOTE: QC also drops via DropSpecialItems (the generic special-item drop hook); the port has no DropSpecialItems subscription, but death/disconnect/observe/use-key already cover every drop trigger DropSpecialItems would, so this is redundant rather than a gap. |
| 102 | 6.75 | `overkill-weapons` | `overkill-weapons.shared.secondary_blaster_jump` | logic:partial | refire_type==0 secondary path (blaster on the shared ATTACK_FINISHED timer) is not handled; only refire_type==1 is. Default is 1 for all five OK weapons, so no observable gap at stock balance.; FIRE2 weapon animation / animdecide_setaction not driven (no third-person fire-2 pose). |
| 103 | 6.6 | `sv-teamplay` | `sv-teamplay.balance.autobalance_bots` | logic:partial | Steady-state poll runs on a 3s cadence (BalanceTeamsTick, GameWorld.cs:3319-3321) where Base autobalances per-event; the cvar gate is now GONE (the poll is unconditional, stopped only by GameStopped/intermission, matching QC's commented-out gate). A synchronous compensation also fires on every human team-change (KillCountdown.PerformTeamChange, GameWorld.cs:959-980), so the only timing divergence left is the 3s polling granularity.; Uses frag score (ScoreFrags), not SP_SCORE, to pick the bot to move; ordering can differ when frags != composite score. |
| 104 | 6.6 | `sv-world-rules` | `sv-world-rules.misc.redirection` | logic:partial | Only the core quit_and_redirect path is ported (read at match-end map flow, RedirectHandler -> MenuCommand.Connect reconnects the local/listen client). MISSING: the 'self' restart variant (redirection_target == "self" -> disconnect; defer reconnect), the cvar_set sv_public -2 advertise-suppression, the per-second retry loop with redirection_timeout/redirection_nextthink (Base tries twice to catch slow clients), the quit_and_redirect_timer defer delay, and the localcmd quit once all clients are gone. |
| 105 | 6.6 | `turret-hk` | `turret-hk.menu.describe` | logic:missing | The MENUQC HunterKiller.describe PAGE_TEXT body is stored as a plain HkTurret.Description string (with the %s name slots resolved literally to 'Hunter-Killer Turret' / 'Devastator'), but the port has no live turret-info/describe page system that renders it, so it is never displayed. |
| 106 | 6.5 | `mutator-buffs` | `buffs.disability` | logic:partial | Movement-speed slow (Stunned x0.7 via SpeedMultiplier, OnPlayerPhysics:678-680) is LIVE.; Weapon fire-time slow (WeaponRateFactor x1.5) is LIVE: OnWeaponRateFactor (691-697) multiplies args.Factor by 1.5 for a stunned player; WeaponRateFactor.Call has a live site (WeaponFireGate).; STILL MISSING: weapon projectile-speed slow (WeaponSpeedFactor x0.7) and monster run/walk slow (MonsterMove x0.7) — BuffsMutator subscribes neither hook (no WeaponSpeedFactor mutator-hook chain exists; MonsterMove is unsubscribed). |
| 107 | 6.3 | `warpzones` | `warpzones.combat.traceextras` | logic:missing | Grenade/toss trajectory prediction (WarpZone_TraceToss) and projectile particle trails (WarpZone_TrailParticles[_WithMultiplier]) do not bend through portals — bot grenade aim and trail visuals are wrong across a warpzone. Minor. |
| 108 | 6.0 | `cl-crosshair` | `cl-crosshair.per_weapon_2d_vehicle` | logic:partial | Per-weapon crosshair: PerWeaponNumber dict is never populated by any caller (grep finds zero assignments outside the property def CrosshairPanel.cs:97), so every weapon shows the same `crosshair` number (Base maps each weapon to its own w_crosshair pic + w_crosshair_size multiplier).; crosshair_2d (side-scroller, default 54) is now REGISTERED (CrosshairPanel.cs:323) but has no reader — the FREEAIM/viewloc side-scroller crosshair selection is unported. HUD_Crosshair_Vehicle (info.vr_crosshair) has no port. |
| 109 | 6.0 | `cl-csqcmodel` | `cl-csqcmodel.effects.ef_mf_trail_lights` | logic:partial | FIX (Wave 5): the per-player forced-effects seam is now LIVE for MF_ROCKET. ClientWorld.DriveCsqcModelHooks composes ForcedAppearance(jetpackActive:true) from the networked e.UsingJetpack and passes it to CsqcModelEffects.Apply(...,forced); forced.ModelFlags ORs MF_ROCKET into modelFlags, driving both the rocket trail return and the jetpack loop. So MF_ROCKET trail + loop now fire for remote players.; STILL OPEN: the OTHER MF trails (grenade/gib/tracer/zomgib/tracer2/tracer3) are mapped in CsqcModelEffectFlags but never supplied — base modelFlags is still 0 and only the jetpack composes MF_ROCKET, so non-jetpack MF-driven trails never fire on this path.; RECLASSIFIED (role-glow is actually LIVE): the baseline claimed Strength/Shield role-glow was DEAD because the ComposeForcedAppearance(strength,shield) seam is never fed true. But the role-glow VISUAL is achieved via a different, faithful path — PowerupsMutator.cs:391-392 ORs EF_BLUE(StrengthGlow)/EF_RED(ShieldGlow) into player.Effects server-side, which networks normally and renders the EF_BLUE/EF_RED dynamic lights in CsqcModelEffects.Apply. So Strength glows blue and Shield glows red for all players, live. The DEAD code is the unused CsqcModelEffectFlags.RoleGlowFlags helper (which also has the colors inverted) + the ComposeForcedAppearance strength/shield args — but they are dead-and-redundant, not a missing feature.; STILL OPEN (forced glowmod DEAD): the live composition passes forcedGlowmod (-1,-1,-1) = unset; ApplyForcedGlowmod has zero callers.; EF_BRIGHTFIELD on a player model is dropped (the trail return is unused in the entity-node pass).; Render-flags (additive/fullbright/nodepthtest) only apply to per-instance BaseMaterial3D overrides, not shared/cached shader materials.; MF_ROTATE spin is not applied (no caller supplies the bit).; boxparticles volume emission (absmin..absmax) is reduced to a point-burst at the entity origin for EF_FLAME/EF_SHOCK/EF_STARDUST. |
| 110 | 6.0 | `cl-csqcmodel` | `cl-csqcmodel.networking.csqcmodel_contract` | logic:partial | FIX (Wave 5): ALPHA is now FULLY wired end-to-end (closed the baseline 'half-wired/dead-on-consume' gap). The producer (ServerNet.QuantizeAlpha for players + entities), codec (WriteByte/ReadByte at EntityField.Alpha 1<<17), the client consume (ClientEntityView: `e.Alpha = s.Alpha == 0 ? 1f : s.Alpha/255f`), and the render (PlayerModel.ApplyAlpha sets per-instance GeometryInstance3D.Transparency on every mesh) are all live. A Cloaked (0.25) / Invisibility / fading player now renders translucent on the client.; FIX (Wave 5): UsingJetpack (IT_USING_JETPACK) is now networked (NetEntityFlags widened byte->ushort, bit 8) and consumed, letting the client re-derive MF_ROCKET (trail + jetpack loop) without networking the full modelflags field.; STILL NOT networked (unchanged-missing): colormap proper (only a team id is sent), the rest of modelflags, traileffect, non-player glowmod, the per-entity colormod (sent for EVERY entity in Base), scale, v_angle, and the entire anim_state/anim_upper/lower_action+time block.; Consequence chain (each also tracked in its feature row): no colormod => no server-driven per-model tint; no scale => no resized models; no traileffect/non-jetpack modelflags => no non-rocket MF trails; no anim_* => no upper-body actions / server-decided locomotion; no v_angle => aim pitch uses body angles; no networked colormap => force-color path leans on a reconstructed team id. |
| 111 | 6.0 | `deathmatch` | `deathmatch.scoring.teamkill_punishing` | logic:partial | DM is FFA so this never fires in practice; the escalating teamkill penalty is unmodeled, but unreachable in deathmatch (relevant only to team modes). |
| 112 | 6.0 | `duel` | `duel.mapsupport.gating` | logic:partial | FIXED (m_isForcedSupported half): MapInfoBackend.ApplyForcedGametypes (MapInfoBackend.cs:115) now promotes any DM map to also support duel unless g_duel_not_dm_maps, called live from MapInfoBackend.Get (MapInfoBackend.cs:104) and wired from the menu cvar at CreateGameScreen.cs:590 + DialogCreateGameMapInfo.cs:50. Unit-tested in MenuDataSourceTests.cs:348-373 (both g_duel_not_dm_maps=0 forces-in and =1 excludes). Matches QC MapInfo_Get_ByName FOREACH(Gametypes, it.m_isForcedSupported...).; STILL OPEN (m_isAlwaysSupported half): no port logic gates duel auto-support by map size (diameter<3250). grep 3250 / m_isAlwaysSupported / diameter over src+game -> only doc-comments (Duel.cs:37). A too-large map could still be offered for duel. Requires BSP world bounds not available in the menu's file-only mapinfo parsing layer (per Duel.cs:36-38 note). |
| 113 | 6.0 | `lms` | `lms.lives.starting_count` | logic:partial | Base derives lives from the per-map mapinfo string `lives=N` (default 5) with `m_legacydefaults "9 20 0"` selecting legacy=9 (lms.qh:11/22 re-confirmed); the port hard-codes DefaultLives=5 and reads a NON-EXISTENT cvar `g_lms_lives` as its 'primary' source (Base has only g_lms_lives_override — grep confirmed no g_lms_lives). Net: the default still lands on 5, but the port ignores both the per-map `lives=` override and the legacy-defaults switch. UNCHANGED. |
| 114 | 6.0 | `lms` | `lms.respawn.deadflag_reset` | logic:unknown | Base PlayerPreThink (sv_lms.qc:444-454) does two things: (a) exports the recycled REDALIVE/BLUEALIVE leader stats (covered by lms.hud.mod_icon via GametypeStatusBlock instead), and (b) flips a dying player's deadflag DEAD_DYING → DEAD_RESPAWNING so LMS auto-respawns living players without a fire press. The port sets RespawnFlag.Force in ApplyRespawnTiming (covering the auto-respawn intent), but the explicit per-frame deadflag transition is not separately ported — its necessity vs the port's RespawnFlag.Force path is unverified. NEWLY MAPPED coverage gap. |
| 115 | 6.0 | `mutator-physical_items` | `mutator-physical_items.item_spawn.ghost_entity` | liveness:dead | setSendEntity(item, func_null) is not ported — the real item's net-send is not explicitly suppressed; the port relies on EF_NODRAW for client invisibility. Minor net-presentation divergence (item stays in the entity feed but draws nothing).; DropToFloor_QC_DelayedInit is not called for map-item ghosts; the port assumes the Physics/TOSS integrator settles the ghost on the next step. With no ODE integrator wired this never matters today (mutator is dead), but it is a settle-behavior divergence if the gate were ever opened.; Ghost colormap/glowmod are NOT copied from item.owner at spawn (QC sv_physical_items.qc:115-116 wep.colormap = item.owner.colormap; wep.glowmod = item.owner.glowmod) — cosmetic ghost-tint divergence, same class as the per-think colormap/colormod/glowmod omission in the think feature.; DEAD: the whole hook is unreachable because IsEnabled is hard-false, so OnItemSpawn is never subscribed and never fires in a normal match. |
| 116 | 6.0 | `mutator-physical_items` | `mutator-physical_items.item_callbacks.think_touch_damage` | liveness:dead | Ghost colormap/colormod/glowmod are NOT copied from the owner each think (QC sv_physical_items.qc:43-46) — a cosmetic per-frame ghost-tint divergence; alpha IS followed.; DEAD: the think/touch/damage callbacks are only wired onto a ghost created by OnItemSpawn, which never runs because the mutator never adds (gate hard-false). No ghost exists, so nothing thinks/touches/damages in a normal match. |
| 117 | 6.0 | `race` | `race.mode.setup_limits` | logic:partial | Unset g_race_laps_limit defaults to 7 in the port; Base (-1) consults the mapinfo laplimit.; The remaining legacy gametype_init defaults (teamlaplimit=15, the 20-min timelimit / 5-min qualifying defaults) are not applied as defaults — only laplimit=7 has a port fallback. |
| 118 | 6.0 | `sv-chat` | `sv-chat.helpers.printtochat` | liveness:dead | All six helpers (PrintToChat / DebugPrintToChat / PrintToChatAll / DebugPrintToChatAll / PrintToChatTeam / DebugPrintToChatTeam) are now ported faithfully (Chat.cs:770-823): the \{1}^7 (\x01^7) chat marker + trailing \n, the autocvar_developer>0 gate on the Debug* variants (Cvars.Float("developer") > 0), and the bprint server-console echo (modeled by the sv_dedicated-gated DedicatedPrint tail). BUT they are DEAD — nothing in the port calls them. NOTE this matches Base: every PrintToChatAll/PrintToChatTeam use in teamplay.qc and the random_items mutator is COMMENTED-OUT debug code, so there is no live stock caller to wire to in the first place. |
| 119 | 6.0 | `sv-impulse` | `sv-impulse.cheat.command_impulse` | logic:partial | CLONE_MOVING (140) and CLONE_STANDING (142) are still unported — they need a CopyBody engine service (clone the player entity into a static decoy, ++this.lip, with the v_forward*300 momentary nudge for the moving variant) the port does not expose. Neither is routed in DispatchImpulse.; SPEEDRUN (141) restore omits the QC start-solid tracebox guard (`tracebox(personal.origin, mins, maxs, ..., MOVE_WORLDONLY); if(trace_startsolid) sprint('Cannot move there, cheater ...')`) — the port teleports unconditionally — and does NOT fire the AbortSpeedrun mutator hook (race-timing reset). Otherwise the teleport-back + resource/weapon/item/statuseffect restore + pause-timer/statuseffect rebase are faithful.; SPEEDRUN_INIT (30) snapshot is faithful (origin/v_angle/velocity/RES_*/WEAPONS/items/statuseffects/pause timers/teleport_time into Player.PersonalCheckpoint, health floored at 1). The .personal is modeled as a value record on the Player, not a simulated edict (QC's personal_wp edict only ever stores fields, so this is behaviorally equivalent).; NOTE: there is no separate 'drag' or 'fixed-clone' CHIMPULSE — Drag is mouse-grab via CheatFrame/CheatCommand, not an impulse. The actual unported impulse set is exactly CLONE_MOVING(140) and CLONE_STANDING(142). |
| 120 | 6.0 | `sv-teamplay` | `sv-teamplay.global.team_entities` | logic:partial | No unified Team_* global entity API: per-team score lives in Scores, alive/owned-item counts and winner queries are reimplemented ad hoc per gametype (CA/Dom/Onslaught) rather than via Team_GetWinnerAliveTeam / Team_GetWinnerTeam_WithOwnedItems. |
| 121 | 6.0 | `weapon-arc` | `weapon-arc.bolt.secondary_burst` | logic:partial | Whole bolt_count burst fires in ONE WrThink call (one PrepareAttack, a for-loop over count at Arc.cs:493); Base fires one bolt per bolt_refire via misc_bulletcounter counting up from -to_shoot, enforcing bolt_refire2 only after the last bolt. Dormant at stock bolt_count 1. UNCHANGED.; Ammo-fraction scaling is ported (Arc.cs:473-481: to_shoot = floor(count*min(1,ammo/bolt_ammo)), TakeResource(min(bolt_ammo,ammo))) matching arc.qc:610-622 — but applied once to the whole one-shot burst, not per-bolt, so the per-bolt refire2 tail is still absent. UNCHANGED. |
| 122 | 6.0 | `weapon-tuba` | `weapon-tuba.audio.note_sound` | values:partial | No two-sample cos/sin crossfade: Base's in-range branch plays the floor sample at cos(pi/2 m/step) AND the upper neighbour at sin(...) for a smooth timbre blend; the port plays only the floor neighbour at full volume (residual PITCH is shifted via 2^(m/12), but the upper-neighbour timbre blend needs a second emitter and is unported).; No cl_tuba_fadetime release ramp: NoteOff Stops the loop cleanly rather than ramping volume down over 0.25s (needs a client-side per-frame volume think).; No ENT_CLIENT_TUBANOTE networking: remote players' tuba notes are not driven by a dedicated note entity (no per-listener re-origin / SendFlags broadcast). |
| 123 | 6.0 | `weapon-vaporizer` | `weapon-vaporizer.ammo.consumption` | liveness:partial | Instagib flag is hardcoded true on the class default; in non-instagib play the bare default loadout/CheckAmmoPrimary treat it as instagib (1 cell). Attack/CheckAmmoPrimary DO re-read g_instagib via InstagibActive for the actual decrement/check, so under the mutator it is correct — but the bare class default is wrong. |
| 124 | 5.7 | `mutator-overkill` | `overkill.weapons.unblock` | logic:partial | No ok_weapons sub-mutator gating the OK weapons behind (g_overkill_weapons \|\| ok). Whether the OK weapons are WEP_FLAG_MUTATORBLOCKED outside Overkill — and unblocked under it — is not enforced via this hook; the OK weapons exist as direct registrations. UNCHANGED by Wave-2. |
| 125 | 5.7 | `turret-framework` | `turret-framework.client.presentation` | logic:partial | No tur_head sub-entity: the steady-state head MODEL rotation (turret_draw tur_head.angles += avelocity*dt) is never rendered, and the per-shot head-frame anim rides the turret body's own Entity.Frame instead of a separate head bone.; No turret_draw2d radar layer: no waypoint sprite and no over-turret healthbar.; No turret_changeteam team recolor (colormap/glowmod).; No client gib toss (turret_gibtoss/turret_gibboom) — needs a CSQC turret edict. |
| 126 | 5.6 | `bot-waypoints` | `bot-waypoints.steer.steerlib` | logic:missing | No steerlib library: flocking/swarm/traceavoid/beamsteer primitives absent (group flocking, the trident/funnel traceavoid obstacle steering, and beamsteer). Bots do not flock and use the simpler single-probe avoidance in Steer + the BotDanger fall probe. |
| 127 | 5.6 | `cl-crosshair` | `cl-crosshair.trueaim.showimpact` | logic:missing | Base (crosshair.qc:306-321): the hit-test block reprojects the box-trace endpoint into screen space (wcross_origin) and, unless crosshair_hittest_showimpact is set, restores wcross_origin to the geometric centre. With showimpact=1 the crosshair is left drawn AT the projected impact point (a debug/aim-assist cue). The port always draws at the geometric panel centre (DrawPanel: center = Size2*0.5f) and never reprojects — the cvar is registered with the Base default (0) but has no reader.; Tied to the same missing screen-space reprojection as the trueaim HITOBSTRUCTION test; default 0 so the visible default behaviour matches Base. |
| 128 | 5.6 | `cl-scoreboard` | `cl-scoreboard.draw.export` | logic:missing | No equivalent of Scoreboard_Draw_Export (writes the scoreboard table to a file handle for the dump-scoreboard console command); the port has no scoreboard-export command. |
| 129 | 5.6 | `cl-teamradar` | `cl-teamradar.mutator.hook` | logic:missing | No TeamRadar_Draw client mutator hook: mutators cannot add custom blips/icons to the radar, and the hook's return value (which Base ORs into the per-icon team-match gate for hover-glow visibility) is unmodeled. |
| 130 | 5.4 | `vehicle-bumblebee` | `vehicle-bumblebee.client.presentation` | logic:partial | The networked BRG_* healray/damage beam (the signature green/red ray; ENT_CLIENT_BUMBLE_RAYGUN, bumble_raygun_send/NET_HANDLE) + bumble_raygun_draw multi-segment Draw_CylindricLine cylinder NOT IMPLEMENTED (placeholder EffectEmitter.TeHealBeam/TeBeam stub only; colour bit-faithful).; Wave-7 LIVE NOW (in addition to the Wave-5 pilot HUD): the GUNNER cockpit HUD (VehicleHudKind.BumblebeeGun selected for a seated gunner, NetGame.cs:3001) + the two GUNNER aux crosshairs (magenta '1 0 1' lead + reload-color READY, NetGame.cs:3025-3034) + the PILOT mirror of both gunners' READY markers (FeedPilotGunnerAux, NetGame.cs:3050-3051) all render each frame. STILL missing: the 'No right/left gunner!' blinking prompts (vr_hud text drawn when AuxiliaryXhair[1]/[2] are absent), and the vr_crosshair vCROSS_HEAL pilot pointer (the aux LOCK crosshair is drawn but not the dedicated vCROSS_HEAL center pointer).; Cosmetic gun models (CANNON_RIGHT/LEFT/CENTER) + gun cockpit + cockpit/viewport offsets + scale 1.5 NOT IMPLEMENTED.; MENUQC describe text (the three-passenger role explanation) NOT IMPLEMENTED. |
| 131 | 5.25 | `keepaway` | `keepaway.waypoints.tracking` | logic:partial | FIXED (was missing/na→partial/live): CollectWaypoints (overriding GameType.CollectWaypoints, pulled live each tick by ServerNet.cs:927 SendWaypoints) now EMITS the sprites — a KaBallCarrier sprite on the carrier (offset '0 0 64', RADARICON_FLAGCARRIER) when held, else a KaBall sprite at the loose ball's origin. The sprite TYPES were already registered in the catalog (Waypoints.cs:137-138 KaBall/KaBallCarrier with radar icons).; FIXED (was 'cvar never read'): g_keepawayball_tracking is now read (BallTracking) and honored: 0 = no waypoint at all, 1 = always, 2 = only when the ball is dropped/loose (the carrier sprite is suppressed when tracking != 1).; STILL OPEN (logic, why partial): the spectator/invisibility visibility rules (ka_ballcarrier_waypointsprite_visible_for_player: hide the carrier sprite from spectators-of-the-carrier and when the owner IS_INVISIBLE) are not modeled; QC also forces the sprite ON during warmup, but the host pull path has no warmup flag here so the cvar governs unconditionally.; STILL OPEN (presentation): waypoint sprite RENDER fidelity is the client's; this row asserts the server-side spawn/attach + cvar gating, not the CSQC draw. |
| 132 | 5.25 | `tka` | `tka.ball.pickup` | presentation:missing | GiveBall is LIVE: BallTouchEntity is the ball's touch handler (registered in the live SpawnBall), so a player touching the loose ball becomes the carrier, with SND_KA_PICKEDUP, INFO/CENTER_KEEPAWAY_PICKUP[_SELF] notifications, the 0.5s self-recapture lockout, AttachToCarrier, and TKA_PICKUPS+1.; The carried-ball orbit anim (Tick -> BallEntity.CarryOrbit, BALL_XYSPEED 100 / XYDIST 24) runs — GameWorld.cs:2583 calls tka.Tick(Simulation.FrameTime) every frame. The ball orbits the carrier.; No scale 0.75 while carried, no GameRules_scoring_vip equivalent, no alpha-sync invisibility, no multi-ball chaining (this.ballcarried insert + cnt offset).; World-touch non-player branch plays SND_KA_TOUCH but omits the EFFECT_BALL_SPARKS particle (presentation). |
| 133 | 5.25 | `weapon-devastator` | `weapon-devastator.presentation.flight_fx` | liveness:partial | Muzzle-flash MODEL (models/flash.md3) is not attached at the muzzle — only the ROCKET_MUZZLEFLASH particle effect is emitted (Attack: EffectEmitter.Emit ROCKET_MUZZLEFLASH, no flash-model attach).; Crosshair image/size (gfx/crosshairrocketlauncher, 0.7) not audited in this pass. |
| 134 | 5.25 | `weapon-electro` | `electro.describe_guide` | logic:partial | GuideDescription (Electro.cs:104-113) ports the four describe() prose paragraphs but NOT the trailing W_Guide_Keybinds line nor the computed W_Guide_DPS_secondaryMultishotWithCombo metric line (electro.qc:764-765 — those helpers aren't ported). |
| 135 | 5.25 | `weapon-hook` | `weapon-hook.primary.fire_grapplinghook` | logic:partial | No dedicated hook entity stream (GrapplingHookSend / ENT_CLIENT_HOOK with hook_start/hook_end). The chain HEAD IS networked via the generic projectile feed (ServerNet.Classify -> Projectile: FL_PROJECTILE/Item + Owner + MoveType.Fly, ServerNet.cs:2365) and renders as a red Hookbomb spike — but with no rope-endpoint stream there is no rope line (see weapon-hook.presentation.rope_line).; No WarpZone refsys tracking (WarpZone_RefSys_SpawnSameRefSys, server/hook.qc:323).; weaponLocked(actor)/actor.vehicle early-return guard at the top of FireGrapplingHook IS now ported (Hook.cs:278, IsWeaponLocked \|\| Vehicle is not null) — the prior 'weaponLocked guard absent' note is resolved; only the broader weaponLocked subset (game-start/player_blocked/LockWeapon) remains unmodeled, gated upstream. |
| 136 | 5.25 | `weapon-minelayer` | `minelayer.mine.render` | liveness:partial | No CSQCProjectile(PROJECTILE_MINE) in-flight networking analogue: Base networks the in-flight mine as a CSQC projectile and only the STUCK mine is a plain server entity. The port renders the mine as a plain server model for its whole life, so in-flight prediction/trail networking differs (functional but not the CSQC path). |
| 137 | 5.2 | `cl-scoreboard` | `cl-scoreboard.block.accuracy` | logic:partial | Drawn as plain text '%' cells, not the per-weapon ICON grid (drawpic weapon model2) with column/row highlighting.; No accuracy_showdelay gating, no warmup suppression, no doublerows, no Accuracy_GetColor weapon ramp or nocolors option; the port's average color ramp differs from QC's per-weapon ramp. |
| 138 | 5.0 | `mutator-buffs` | `buffs.item.respawn_cooldown` | logic:partial | Port respawn is still a single Think after cooldown_respawn (clears Owner and runs MaybeRandomize/MaybeRelocate); no buff_activetime countdown, no activate-cooldown arming on round start (Base buff_Reset uses g_buffs_cooldown_activate; port only models cooldown_respawn), no buff_active/buff_activetime state machine, no buff_Customize light pulse.; SND_STRENGTH_RESPAWN + EFFECT_ITEM_RESPAWN ARE now emitted on (re)activation (ActivateBuff:336-337, ScheduleRespawn:462-463). Still missing: the waypoint build-finished timer and g_buffs_pickup_anyway instant-respawn.; Owner-gone / dead / frozen / wrong-buff respawn conditions (QC buff_Think) are still collapsed: the port unconditionally clears the owner and re-arms after the respawn cooldown. |
| 139 | 5.0 | `mutator-dodging` | `mutator-dodging.anim.jump_action` | logic:missing | The explicit server-side animdecide_setaction(ANIMACTION_JUMP) write is not ported (logic: missing) — the port has NO animdecide/anim-action server->client one-shot channel. HOWEVER this seam has no distinct rendered effect even in Base: animdecide.qc:283 states verbatim 'currently, in CSQC this is the only jump detection, as the explicit jump action is never called!' — Base's client renderer drives the jump clip off the IMPLICIT ground->air (ANIMIMPLICITSTATE_INAIR) transition, identical to the port's path. So the missing logic produces no observable presentation gap. |
| 140 | 5.0 | `mutator-overkill` | `overkill.randomitems.inject_ok_items` | logic:partial | Minor: the OK item pool is a hard-coded NetName list (OverkillItemNetNames = health_mega/armor_small/armor_medium/armor_big/armor_mega + weapon_okhmg/okrpc), NOT the Base g_overkill_items IntrusiveList built in MUTATOR_ONADD (which the port never builds — see overkill.onadd row). The set/weights match Base, but it is not driven by the real ITEM_FLAG_MUTATORBLOCKED-filtered list; Item_IsDefinitionAllowed is approximated by def.ItemDef.IsAllowed.; liveness=partial: only fires when the random_items mutator is also active (the chain is reached via RandomItemsMutator.GetRandomItemClassName). |
| 141 | 5.0 | `mutator-waypoints` | `waypoints.gametype.collect` | logic:partial | Seven live CollectWaypoints producers now: CTF (flag base/dropped/carrier), Domination (control-point markers), FreezeTag (Frozen/Reviving + thaw-progress health bar), Keepaway (KaBall/KaBallCarrier, tracking-gated), TeamKeepaway (team-colored TkaBallCarrier<team>/KaBall, tracking-gated), Key Hunt (KeyDropped + KeyCarrier<team>/KeyCarrierFinish, tracking-gated per-viewer predicates), LMS (LmsLeader on each leader, show-window gated). Plus Assault via the manager API, and itemstime/Seeker/powerup-drop via the manager. All reached via ServerNet.SendWaypoints:1022 per tick. FIX vs baseline (was CTF+Dom+FT+Ka+Tka).; Still missing as CollectWaypoints producers: Onslaught control points + generators, Nexball balls + goals, Race start/finish/checkpoint, turret/vehicle/buff markers. (Weapon_whereis and MONSTER healthbars are now ported as on-demand/persistent manager producers — see waypoints.weapon.whereis and waypoints.monster.healthbar — not via CollectWaypoints.) |
| 142 | 4.8 | `cl-csqcmodel` | `cl-csqcmodel.tagindex.weapon_attach` | logic:partial | The QC h_/v_ model tag resolution cascade (weapon bone -> gettagindex weapon/tag_weapon -> shot/tag_shot, with recursive parent predraw and drawmask=0 when the tag can't resolve) is simplified to a tag_weapon marker attach; weapon-on-weapon and the shot-muzzle tag chain are not faithfully reproduced.; No recursive predraw of the parent entity to fix forcemodel/LOD bone-index mismatch (forcemodel is live, so a forced-model bone-index mismatch on the held weapon attach is a possible edge case; LOD swap is still absent so no LOD-driven mismatch). |
| 143 | 4.8 | `monster-spider` | `monster-spider.attack.player_wielded` | logic:missing | STILL OPEN. The SpiderAttack weapon's IS_PLAYER(actor) path (a player wielding the hidden spider attack via impulse 9 / monster-riding or dev tooling) is not ported: no SpiderAttack weapon in the port (the port's Spider re-derives melee/ranged inside Monster.Attack and never goes through a Weapon). The player path uses weapon_prepareattack timing, attack_range=60, and Monster_FindTarget self-targeting — all absent. |
| 144 | 4.8 | `sv-cheats` | `sv-cheats.impulse.teleport` | logic:partial | Random-location path uses BallEntity.RandomMapLocation (a spawnpoint-sampling fallback), NOT the true MoveToRandomMapLocation world-bounds sampler; the 100 / 100000 attempt-count constants (the sv_cheats<2 vs >=2 wider search) and the DPCONTENTS/Q3SURFACEFLAG_SKY content masks are not honored.; The noclip-preferred info_autoscreenshot path (Cheats.cs:300-318) is now REACHABLE: as of the info_autoscreenshot spawnfunc fix (sv-cheats.mapent.info_autoscreenshot) the entity spawns from the BSP lump, so on a map with autoscreenshot points a noclipping cheater is teleported to (and consumes) one — matching Base. On maps without them it still falls through to the random path, as in Base. |
| 145 | 4.72 | `cl-hud` | `cl-hud.panel.minigame` | logic:partial | Board + status are merged into one MinigameRenderer (board square + team pieces + win-line + status line) rather than the QC BOARD/STATUS split; only the implemented minigame rule-sets are renderable. Per-game _hud_board/_hud_status art coverage not all runtime-verified. |
| 146 | 4.55 | `bot-waypoints` | `bot-waypoints.auto.spawnforteleporter` | logic:partial | No warpzone-angle math (Base makevectors(warpzone_angles) src/dest projection) and no oblique-warpzone JUMP flag; jumppad push-time cost is approximated (Base uses trigger_push_get_push_time / the auto-link cost), the port uses the plain travel cost.; Generated-graph path only; shipped maps use authored teleport waypoints from the file. |
| 147 | 4.5 | `mutator-nades` | `mutator-nades.boom.translocate` | logic:partial | move_out_of_solid is a minimal upward-nudge approximation (no builtin), so embedded-in-solid recovery is less robust than Base's full search.; MUTATOR_CALLHOOK(PortalTeleport) is not fired (no subscribers in port). |
| 148 | 4.5 | `mutator-overkill` | `overkill.onadd.precache_and_itemblock` | logic:partial | ok_player models are never precached / forced as the default player model (precache_all_playermodels equivalent absent).; g_overkill_items list (the random-items injection source) is never built.; Mechanism differs: Base ONADD sets ITEM_FLAG_MUTATORBLOCKED on the four items at add-time; the port instead reads the four filter cvars in Hook() and enforces the block per-spawn in the FilterItem hook (overkill.filter row). End behavior (which items are blocked, with which defaults) now matches — but the ONADD spawnflag-blocking and ONREMOVE clear are not modeled. |
| 149 | 4.5 | `turret-phaser` | `turret-phaser.presentation.beam_visual_and_head_anim` | logic:partial | No PhaserTurret_beam model spawned/scaled to the impact distance (MDL_TUR_PHASER_BEAM + .scale = vlen/256 + setattachment tag_fire); acknowledged TODO comment in Attack (PhaserTurret.cs:161-165). The server-side beam logic/damage IS done; only the visible beam model is absent.; phaser.md3 head model is not attached as a separate tracking head bone — the head-frame animation drives the turret edict's own networked Entity.Frame, so the frame VALUE animates faithfully but there is no visible phaser.md3 head to show it (presentation gap shared with identity.model_hitbox). |
| 150 | 4.5 | `weapon-tuba` | `weapon-tuba.note.sustain_lifetime` | values:partial | W_WeaponRateFactor scaling on the keep-alive — the port DOES multiply refire*2 by WeaponRateFactor(actor) (Tuba.cs:163), so this matches Base; only relevant under haste/rate modifiers.; No W_Tuba_NoteThink server-side per-listener volume/angle re-origin + SendFlags (moot until tuba-note sound networking exists). The port's NoteThink only models the teleport_time self-expiry, not the re-origin broadcast. |
| 151 | 4.2 | `sv-client-lifecycle` | `sv-client-lifecycle.join.join_player` | logic:partial | Join queue + team-selection ordering not modeled: the queued_join TeamBalance_AreEqual two-pass (queued-team dibs first pass + strongest-autoselect-by-skill second pass, player_with_dibs conflict) is absent; the port autoselects via a single connect-time AssignBestTeam.; Campaign Join branch (campaign_bots_may_start / ReadyRestart) only partially modeled (CampaignHostAutojoin opts the host in); the bot-start ReadyRestart trigger is not in ClientManager.Join.; Queue-conflict + queued-player branches still deferred: CENTER_JOIN_PLAY_TEAM_QUEUECONFLICT (depends on the unmodeled wants_join/player_with_dibs queue) and ANNCE_BEGIN for a queued player are not fired; Kill_Notification(CPID_PREVENT_JOIN) clear not modeled. |
| 152 | 4.2 | `turret-fusionreactor` | `turret-fusionreactor.menu.describe` | logic:missing | The MENUQC describe() help string (shown in the menu turret codex) is not ported — the port has no menu turret-description surface for the Fusion Reactor. |
| 153 | 4.05 | `mapobject-triggers` | `mapobject-triggers.viewloc.trigger_viewlocation` | liveness:partial | no client 2.5D side-scroller camera consumer: the server stamps player.ViewLoc each frame but the view is never locked to the start->end line, so the camera region has no visible effect (unchanged this wave — no ViewLoc consumer found under game/client/) |
| 154 | 4.0 | `cl-teamradar` | `cl-teamradar.cvar.zoomscale` | logic:partial | Zoom/scale uses a RangeUnits=2500 + scale/ScaleReferenceDivisor heuristic for the bigsize/normalsize endpoints, not the Base mi_scale/scale2d-derived sizes; the actual arena-fit big size is approximated by a fixed 2500u range.; Big-zoom recentering only happens when map bounds are loaded (HasMapBounds); the standalone path always has bounds, so this is live there, but the heuristic scale endpoints still differ from Base's exact fit. |
| 155 | 4.0 | `mutator-nades` | `mutator-nades.bonus.economy` | logic:partial | GameRules_scoring_is_vip / flagcarrier multiplier: the VIP-kill medium bonus + flagcarrier time-score multiplier (g_nades_bonus_score_time_flagcarrier=2, scaled by KH key count) ARE now wired in OnPlayerDies / OnPlayerPreThink (GametypeEntities.ScoringIsVip + KeyHuntKeyCount). Whether those two helpers return Base-faithful values for every gametype (CTF/KA/TKA VIP, KH key carrier) is the residual values:partial risk — the per-second accrual path now reads CvarRaw(g_nades_bonus_score_time_flagcarrier) and the VIP/key branches, so the previously-MISSING flagcarrier cvar is now consumed. |
| 156 | 3.92 | `cl-hud` | `cl-hud.panel.infomessages` | logic:partial | Cycling group hints (spectate controls), team-unbalanced nag, spectator list and the full message set are partially covered (the eliminated-LMS line is now present). |
| 157 | 3.9 | `turret-walker` | `turret-walker.client.draw` | logic:partial | Gait frame is stamped server-side (DoMovement SetFrame(e, animflag) writes Entity.Frame, faithful to turrets_setframe) AND netted (ServerNet snapshot Frame=(int)e.Frame) AND consumed client-side: the walker classifies as NetEntityKind.Generic (ServerNet.Classify), which ClientEntityView routes with frameDriven:true, so ClientWorld attaches a ModelAnimator with FollowEntityFrame=true that plays Entity.Frame as the raw model frame (requires walker_body.md3 FrameCount>1). So the walk/run/melee/swim/idle gait animations now DO play. TNSF_MOVE origin/velocity netting still has no dedicated counterpart (origin rides the shared entity feed).; No CSQC walker_draw 4-point ground-align over terrain (movelib_groundalign4point 300,100,0.25,45) and no head avelocity spin (pure client render, still absent — the values:partial / timing:partial / groundalign4point + client-movetype + anim-frames constants reflect these unported pieces). |
| 158 | 3.85 | `bot-ai` | `bot-ai.combat.dodge` | logic:partial | 14 of Base's 20 bot_dodge producer files are ported (all weapon projectiles + the PhaserTurret beam); the 6 still missing are monsters (golem/spider), the napalm nade, the grappling hook, and the generic sv_turrets producer, so dodge does not trigger for those hazards. (NARROWED 2026-06-27 second pass from 3 producers.); Non-SUPERBOT bots still only do the generic CombatMovement strafe-flip + HP-bias (an invented behaviour), not a per-projectile dodge — Base also restricts havocbot_dodge to SUPERBOT, so this matches stock, but the generic strafe is not a Base behaviour. (UNCHANGED.); The SUPERBOT random-direction combat jitter (havocbot_movetogoal:1280-1307) is not reproduced. (UNCHANGED.) |
| 159 | 3.76 | `cl-hud` | `cl-hud.panel.quickmenu` | logic:partial | Nested quick-chat tree from a quickmenu file + translatecommands/server_is_default parity not verified. |
| 160 | 3.76 | `fx-effectinfo` | `fx-effectinfo.emit.te_builtins` | liveness:partial | The te_* helper -> registry-name mapping (e.g. TeSmallflash -> BLASTER_MUZZLEFLASH, TeGunshot -> MACHINEGUN_IMPACT) is a port convention, not a 1:1 DP te_* semantic; not verified that every live caller hits these wrappers vs emitting the registry effect directly. Beam/arc temp-effects route to BeamRenderer, not the particle catalog. |
| 161 | 3.75 | `clanarena` | `clanarena.hud.eliminated_greyout` | logic:partial | IsEliminatedPlayer == IsDead only; QC ca_isEliminated also greys INGAME_JOINING late joiners and FRAGS_PLAYER_OUT_OF_GAME, which the port still lacks, so a late joiner waiting out the round isn't greyed until they die. |
| 162 | 3.75 | `clanarena` | `clanarena.lifecycle.reset_map` | logic:partial | QC reset_map_players: for each INGAME-or-bot client, clear killcount, TRANSMUTE(Player), INGAME_STATUS_SET(JOINED), PutClientInServer, reset prev_team. QC reset_map_global: allowed_to_spawn = true. The port re-spawns everyone at round reset via its round-gated DeadPlayerThink/ResetMap machinery (effective one-life-per-round result holds), but has no CA-specific reset_map hook: no INGAME->Player transmute (no INGAME state at all), no killcount reset, no prev_team reset, and no explicit allowed_to_spawn flag (the port gates spawn on round phase instead). Practically equivalent for the common case; the late-joiner/INGAME details are tied to clanarena.join.late_join_observer. |
| 163 | 3.75 | `clanarena` | `clanarena.spawn.ingame_status_decimal_reset` | logic:partial | QC ca PlayerSpawn: INGAME_STATUS_SET(player, JOINED) — not ported (no INGAME state, tied to clanarena.join.late_join_observer). This is the only remaining gap on this row; logic stays partial because of it.; QC ca PlayerSpawn: `if (!warmup_stage) eliminatedPlayers.SendFlags = 0xFFFFFF` (force eliminated-list resend on spawn) — the port's elimination-list capture runs per frame anyway, so the explicit dirty-flag is N/A. |
| 164 | 3.75 | `ctf` | `ctf.flag.remove_player` | liveness:partial | PORTAL-teleport carry-leave drop (g_ctf_portalteleport) is still NOT wired — the ctf PortalTeleport hook has no port dispatch (the PortalTeleport event itself is never fired in the port; BuffsMutator/NadeTranslocateBoom note it omitted). Disconnect + MakePlayerObserver (OnPlayerRemoved) and vehicle board/leave (OnVehicleEnter/Exit) DO drop the flag now. |
| 165 | 3.75 | `ctf` | `ctf.hud.waypoints` | values:partial | Base/dropped/carrier-own (health bar) + carrier-enemy (stalemate/self-carry reveal) sprites + the carrier helpme FLASH are emitted; no dedicated flag-return sprite, no on-pickup enemy-FC spotting reveal (g_ctf_flagcarrier_waypointforenemy_spotting). |
| 166 | 3.75 | `ctf` | `ctf.oneflag` | liveness:partial | Liveness depends on a map shipping item_flag_neutral; the neutral-pickup + team-base-capture + reverse logic is present but exercised only on a one-flag map. g_ctf_reverse (two-flag reverse) is NOT modeled (only OneFlagReverse). |
| 167 | 3.75 | `cts` | `cts.map_reset` | logic:partial | ResetMapGlobal resets every runner's in-progress run (AbandonRun: RunStartTime + .race_movetime accumulator zeroed), zeroes the round-best speed award, and cancels pending finish-retracts on map reset — live (GameWorld:4442). The persistent top-99 CTS records are intentionally kept (QC keeps them too).; Still not modeled: the cts_EventLog place echo per client (Score_NicePrint + the ftos(race_place) gamelog line) and the qualifying==2 -> race collapse (moot: CTS pins g_race_qualifying=1). The PlayerScore_Sort/race_place bookkeeping is also absent (CTS has no intermediate-checkpoint race_place tracking). |
| 168 | 3.75 | `cts` | `cts.records.getrecords` | logic:partial | The port models the single-map ranking listing (QC getrankings: ranks 1..RANKINGS_CNT of the current map) and wires it live to the rankings/records console commands. The QC GetRecords hook's multi-map paged form (200 maps/page across MapInfo, the cross-map 'records [<page>]' listing) is NOT modeled — only the current map's records are listed. |
| 169 | 3.75 | `freezetag` | `freezetag.frozen.no_pickup` | liveness:partial | BuffTouch has no separate hook in the port — OnItemTouch (the world-item gate) is the only frozen-pickup gate; buff-pickups route through a different path that has no frozen gate. Minor: frozen players can't initiate movement, and buffs are a mutator. World-item pickups ARE correctly gated. |
| 170 | 3.75 | `keepaway` | `keepaway.ball.pickup` | values:partial | Pickup is reachable — the live engine trigger-touch pass dispatches Solid.Trigger entities' Touch=BallTouchEntity, which calls PickUp; AttachToCarrier makes the ball SOLID_NOT + MOVETYPE_NONE + sets GtCarrier.; SND_KA_PICKEDUP plays (PlayOn KA_PICKEDUP, ATTEN_NONE); INFO_KEEPAWAY_PICKUP (all) + CENTER_KEEPAWAY_PICKUP (all-except) + CENTER_KEEPAWAY_PICKUP_SELF (one) notifications all sent and registered.; KEEPAWAY_PICKUPS column increment runs (AddCol on PickUp); the 0.5s self-recapture lockout is honored (BallTouchEntity → IsRecaptureLocked).; The non-player world-touch branch plays SND_KA_TOUCH (ATTEN_NORM).; STILL OPEN (values/presentation): carried scale 0.75 is not applied (no entity scale field). EFFECT_BALL_SPARKS particle on the non-player touch is not emitted (only the sound is).; STILL OPEN (logic): GameRules_scoring_vip(toucher,true) has no direct port equivalent (the carrier role is tracked via Ball.Carrier instead); functionally the carrier is identified for the mod-icon, but the QC VIP flag/scoreboard marker is not modeled.; STILL OPEN (logic): multi-ball chaining (g_keepaway_ballcarrier_maxballs, cnt chain) is collapsed to a single ball (cnt=1). |
| 171 | 3.75 | `keepaway` | `keepaway.bot.role` | logic:partial | RoleKeepaway is live (set on the brain at bot creation via BotRoles.ChooseRole; 'ka'/'keepaway'/'tka' → RoleKeepaway) and engages the ball: FindBall() queries classname 'keepawayball' (BotObjectiveRoles.cs:387) == the live spawned ball; carrier read off ball.GtCarrier (the field AttachToCarrier sets). A loose ball is chased (collector behaviour: items 10000, enemies 500, ball 8000) and a carried ball's carrier is contested; carrying switches to carrier behaviour (items 10000, enemies 10000).; FIXED (was STILL OPEN): the Bot_ForbidAttack equivalent is now wired LIVE. Keepaway.ForbidBotAttack(bot,targ) = `!targCarries && !botCarries && haveHeldBall` (== QC: neither carries + a held ball exists), installed at GameWorld.cs:2920 onto BotBrain.ForbidAttackHook (in the case Keepaway ka block of ActivateGameType) and consulted in bot_shouldattack (BotBrain.cs:797) on the live attack-eligibility path — so bots cluster the carrier instead of fragging bystanders.; STILL OPEN (logic, why status stays partial): the collector/carrier split is collapsed into one adaptive RoleKeepaway (Base has two distinct havocbot_role_ka_carrier / havocbot_role_ka_collector functions with a per-bot role-timeout state machine). The port branches on carry-state inline with the same ratingscales rather than swapping the dedicated role function — a structural simplification, behaviourally equivalent at the rating level. |
| 172 | 3.75 | `keepaway` | `keepaway.score.resetmapglobal` | logic:partial | QC's reset_map_global hook clears the shared `.float2int_decimal_fld` (the fractional-score carry) on EVERY client at a global map reset. The port keeps a SINGLE gametype-level remainder (_timePointsRemainder) rather than a per-player field, and zeroes it in Activate (Keepaway.cs:157) at match (re)start — functionally equivalent for the single-ball case (only the lone carrier ever accrues fractional points), but it is not a per-client FOREACH reset and is not wired to a dedicated reset_map_global seam.; Low impact: the only observable effect would be a sub-1-point fractional-score carry surviving a map reset, which Activate already clears; the per-client breadth matters only if multiple players simultaneously held fractional remainders, which the single-ball model precludes. |
| 173 | 3.75 | `nexball` | `nexball.weapon_arena.player_setup` | logic:partial | SpectateCopy hook (copy STAT(NB_METERSTART) from the spectatee to the spectator) is not ported — a spectator watching a charging carrier won't see the meter (also blocked on the unnetworked NB_METERSTART).; WantWeapon (force the start weapon to 0) is not explicitly ported as a hook; the port instead grants BallStealer on spawn (basketball mode) and clears all weapons (football mode). Behaviorally close but the QC WantWeapon force-0 path is folded into OnPlayerSpawn.; PlayerSpawn basketball-mode grant uses \|= (keeps the player's normal start weapons + adds BallStealer), matching QC's STAT(WEAPONS, player) \|= WEPSET(NEXBALL). The active weapon swap to BallStealer happens on pickup (GiveBall), not on spawn — matches QC. |
| 174 | 3.75 | `nexball` | `nexball.item.filter_block` | logic:partial | ForbidDropCurrentWeapon (QC returns true for WEP_MORTAR.m_id — a long-standing odd special-case) is NOT ported as a distinct hook; the port only wires ForbidThrowCurrentWeapon (returns true for the BallStealer). The QC WEP_MORTAR drop-block is dropped (its purpose is unclear in Base, marked 'TODO: what is this for?'). |
| 175 | 3.75 | `overkill-weapons` | `overkill-weapons.mode.enablement` | liveness:partial | Mode reachable only by exec'ing ruleset-overkill.cfg / setting g_overkill via console — no in-menu arena radio. VERIFIED this MATCHES Base (Base dialog_multiplayer_create_mutators.qc also exposes only g_instagib + g_nix radios). So this is faithful to Base, NOT a port-side regression. |
| 176 | 3.75 | `race` | `race.hud.mod_icon_rankings` | liveness:partial | The mod-icon + split panels are fed host-side (LocalServerPlayer / _serverWorld.GameType), so on a PURE remote client the floating PB/server-best icon + medal flash do not render — there is no TE_CSQC_RACE / RACE_NET_SERVER_RECORD net stream feeding ModIconsPanel; only the RANKINGS scoreboard column is genuinely networked. On a listen server the mod-icon renders live.; ReadName/ReadTime are read straight from the persistent RaceRecords DB (no separate networked record-name stream); the local PB is resolved by PersistentId rather than the QC ClientProgsDB cache. |
| 177 | 3.75 | `tka` | `tka.ball.respawn` | presentation:missing | BallEntity.RandomMapLocation uses the QC SelectSpawnPoint FALLBACK path (a random spawnpoint), not the engine MoveToRandomMapLocation world-bounds sampler (rejecting solid/lava/sky). Faithful to the fallback QC itself drops to, but the primary random-world-point sampler is approximated.; Respawn sound (SND_KA_RESPAWN, ATTEN_NONE) IS played in RespawnBall, but the EFFECT_KA_BALL_RESPAWN particles at the old+new origin remain deferred (the WP_KaBall waypoint IS now produced — see tka.waypoints.tracking).; No NOIMPACT/fall-off respawn branch (tka_TouchEvent off-map Q3SURFACEFLAG_NOIMPACT handling not ported — the world-touch goes to the spark/sfx branch instead). |
| 178 | 3.75 | `weapon-arc` | `weapon-arc.beam.heat_overheat` | logic:partial | Structural divergence (UNCHANGED): Base stores beam_heat on the beam ENTITY (arc_beam) and DELETES that entity when the beam ends — heat is gone, and arc_overheat/arc_cooldown are pure timestamps. The port keeps heat as a persistent slot-state value (st.BeamHeat) drained over time by BeamCooldown. Observably close at stock values; not bit-structurally identical.; cooldown_release still ignored at stock (Arc.cs:230 CooldownSpeed branch only sets ArcOverheat when CooldownRelease != 0); Base sets arc_overheat when (cooldown_release \|\| overheat). Dormant at stock cooldown_release 0.; Minor: the NetGame heat ring while-firing branch keys on st.BeamHeat > 0 rather than st.ArcBeam != null (arc.qc:62 keys on player.arc_beam); functionally equivalent at stock (the beam is the only thing that raises heat). |
| 179 | 3.75 | `weapon-arc` | `weapon-arc.heat_persistence` | logic:partial | DROP/PICKUP migration (wr_drop/wr_pickup) still unported: arc_overheat/arc_cooldown are not migrated to a dropped Arc and restored on pickup. The port has no weapon-throw/heat-carry path yet (WeaponThrowing.cs:51 notes no weapon implements wr_drop). Mostly dormant since primary beam_heat is 0. UNCHANGED.; Reset is per-slot in WrSetup, not a respawn-wide hook: WrSetup runs on every weapon switch-in/raise, so a switch away+back also clears heat (benign — the beam is gone while holstered). Base wr_resetplayer/wr_playerdeath are dedicated respawn/death hooks. UNCHANGED. |
| 180 | 3.75 | `weapon-blaster` | `weapon-blaster.offhand.fire` | liveness:partial | Live only when g_offhand_blaster is set (default 0 in mutators.cfg:435), so liveness stays partial — but when enabled the behaviour is now faithful. |
| 181 | 3.75 | `weapon-electro` | `electro.orb.csqc_netlink_draw` | values:partial | The dedicated REGISTER_NET_LINKED(Electro_Orb) link/draw is not reproduced 1:1: Base streams the orb as its own CSQC entity (electro_orb_send writes origin+owner+lifetime; electro_orb_draw applies a (ltime-time)-based scale shrink as the orb nears expiry plus an avelocity spin '7 0 11', alpha 0.7). The port renders the orb via the generic ProjectileCatalog/ProjectileRenderer Electro body (ebomb model + TR_NEXUIZPLASMA trail + dynamic light + electro_fly loop). The in-flight identity matches; the time-based shrink pulse and explicit spin axis are cosmetic and unreproduced. |
| 182 | 3.75 | `weapon-machinegun` | `machinegun.notification.kill_suicide` | liveness:partial | The message-SELECTION code is faithful and wired live (Scores.cs obituary path calls SelectKillMessage/SelectSuicideMessage; DeathMessages keys SNIPE/SPRAY on HasHitType(deathType, Secondary)). The SNIPE branch is reachable ONLY from the mode-0 secondary (now correctly carrying HITTYPE_SECONDARY in the port) — but mode 0 is non-default and dead, so in default mode-1 play BOTH Base and the port emit MURDER_SPRAY (faithful). |
| 183 | 3.75 | `weapon-minelayer` | `minelayer.fire.lay_mine` | liveness:partial | WEP_FLAG_MUTATORBLOCKED (Minelayer.cs:63): the weapon is excluded from the default loadout/allweapons (GiveItems.cs:340 = SpawnFlags & (MutatorBlocked\|Hidden) != 0), so this fire path is NOT reached in a normal match. Live only when granted via weaponarena/NewToys/impulse-give. WrThink dispatch (WeaponFireDriver) is correct on that conditional path. Liveness stays partial. |
| 184 | 3.75 | `weapon-minelayer` | `minelayer.fire.mine_limit` | liveness:partial | Liveness partial: MUTATORBLOCKED weapon (see minelayer.fire.lay_mine). |
| 185 | 3.75 | `weapon-minelayer` | `minelayer.mine.stick` | liveness:partial | Liveness partial: MUTATORBLOCKED weapon (see minelayer.fire.lay_mine). |
| 186 | 3.75 | `weapon-minelayer` | `minelayer.mine.proximity` | liveness:partial | Base proximity loop also skips IS_INDEPENDENT_PLAYER(head); the port does not model independent-player mode, so that exclusion is absent (no functional effect outside that unported mode).; Liveness partial: MUTATORBLOCKED weapon (see minelayer.fire.lay_mine). |
| 187 | 3.75 | `weapon-minelayer` | `minelayer.mine.lifetime_countdown` | liveness:partial | Port lifetime-arm branch adds an extra `self.Frame == 0f` term to the QC `!mine_time && cnt > 0` guard. Redundant (Frame is only set to 1 after MaxHealth is set, so MaxHealth==0f already excludes re-entry; deathTime is always >0 since lifetime-countdown=29.5) — no behavioral difference. Model note only.; Liveness partial: MUTATORBLOCKED weapon (see minelayer.fire.lay_mine). |
| 188 | 3.75 | `weapon-minelayer` | `minelayer.mine.owner_death` | liveness:partial | HITTYPE_BOUNCE deathtype tag on the auto-detonate is carried only as a bool param (the port's int weapon-id deathtype has no bounce flag); the obituary picks the same splash murder string regardless, so this is documentation-only (minor).; Liveness partial: MUTATORBLOCKED weapon (see minelayer.fire.lay_mine). |
| 189 | 3.75 | `weapon-minelayer` | `minelayer.fire.remote_detonate` | liveness:partial | Port detonates inline in RemoteDetonate rather than flagging minelayer_detonate=true and letting each mine's own W_MineLayer_Think call W_MineLayer_RemoteExplode the next tick (one-tick timing difference; behaviorally equivalent in practice).; deathtype HITTYPE_BOUNCE tag on remote blasts not set (obituary picks the splash murder string regardless; inert).; Liveness partial: MUTATORBLOCKED weapon (see minelayer.fire.lay_mine). |
| 190 | 3.75 | `weapon-minelayer` | `minelayer.mine.explode` | liveness:partial | Airshot-achievement branch is ported (Explode:391-395 -> NotificationSystem.Announce('ACHIEVEMENT_AIRSHOT')) but UNREACHABLE for the mine in Base too: every W_MineLayer_Explode caller (Explode_think, ProximityExplode) passes directhitentity=NULL, so the QC `if (directhitentity.takedamage == DAMAGE_AIM ...)` guard never fires for a mine. Carried for structural fidelity. Not a gap (faithful to the unreachable Base branch).; Liveness partial: MUTATORBLOCKED weapon (see minelayer.fire.lay_mine). |
| 191 | 3.75 | `weapon-minelayer` | `minelayer.ammo.checks` | liveness:partial | Port WrThink omits the forced-reload branch, but reload_ammo defaults to 0 so Base never enters it either — faithful at default values (would diverge if reload_ammo were set).; Base wr_checkammo1 also counts clip_load (weapon_load) as a second ammo source; port checks only the rocket resource. Inert at reload_ammo=0 (clip is never loaded), so no behavioral difference at defaults.; Liveness partial: MUTATORBLOCKED weapon (see minelayer.fire.lay_mine). |
| 192 | 3.75 | `weapon-porto` | `weapon-porto.touch.placement_tree` | logic:partial | Surface-flag gating logic differs from Base in WHERE the flags come from: Base reads trace_dphit* off the ORIGINAL bouncemissile collision trace; the port re-probes with a short worldonly traceline (ProbeImpact, Porto.cs:384-402) because the engine touch carries no per-contact flags. The TraceService fills DpHitQ3SurfaceFlags/DpHitContents/PlaneNormal from the BSP surface, so the slick/clip + noimpact branches are reachable and play the right cues — but the re-trace can disagree with the true contact face on a glancing bounce, so branch selection is approximate rather than exact. |
| 193 | 3.75 | `weapon-vaporizer` | `weapon-vaporizer.primary.beam_particle` | values:partial | Beam is a generic networked trail effect (TE_TEI_G3 / _HIT), not the Base cylindric gauntletbeam/lgbeam beam; no team-color (cl_vaporizerbeam_teamcolor) or colorboost tint, no 0.8 beam lifetime cvar. |
| 194 | 3.75 | `weapon-vaporizer` | `weapon-vaporizer.secondary.blaster_laser` | liveness:partial | Optional Blaster-ammo decrement (W_DecreaseAmmo if WEP_CVAR_PRI(BLASTER,ammo) set) is omitted — harmless at the default Blaster ammo 0. |
| 195 | 3.75 | `weapon-vortex` | `weapon-vortex.fx.beam` | values:partial | Beam is emitted at a constant intensity: alpha/fade/trail-spacing are NOT scaled by sqrt(charge), so a barely-charged shot and a full shot look identical. EffectRequest carries colorMin/colorMax but no per-emission alpha, so the sqrt(charge) brightness modulation has no port-side hook.; Team tint is computed server-side from the FIRER's charge+team and broadcast as a single colour (EmitBeam), so the per-recipient cl_tracers_teamcolor / cl_particles_oldvortexbeam client-cvar nuance is collapsed to one broadcast emit (the broadcast reads the host's cvars). |
| 196 | 3.75 | `weapon-vortex` | `weapon-vortex.hud.charge_ring` | liveness:partial | Host-only: the charge stat is fed from LocalServerPlayer's slot state (NetGame.UpdateCrosshairWeaponRings), so the ring fills on a listen-server / single-player host but a PURE remote client has no charge data and still sees no ring. QC networks vortex_charge as a STAT to every client; the port still has no cross-client charge stat (the wepent-props follow-up). |
| 197 | 3.6 | `mapobject-triggers` | `mapobject-triggers.music.target_music` | values:partial | fade curve approximated: the client uses a two-AudioStreamPlayer A/B crossfade rather than QC's per-source state easing by frametime/fade_time (in) and frametime/fade_rate (out) |
| 198 | 3.6 | `monster-mage` | `monster-mage.menu.describe` | liveness:dead | Mage.Describe() (Mage.cs:86-93) is the verbatim bestiary prose but has NO caller: the only references to Describe() are the base virtual (EntityClasses.cs:106) and unrelated DPM/DeferredCommands.Describe overloads. No bestiary/monster-tooltip UI surface in the port renders descriptor Describe() text, so the prose never reaches the screen. |
| 199 | 3.6 | `sv-world-rules` | `sv-world-rules.checkrules.cascade` | logic:partial | leadlimit / leadlimit_and_fraglimit WIN semantics are still not evaluated in the world layer (the win comes from the gametype MatchEnded latch + tie report). The leadlimit math exists in GameScores.CountFragsRemaining but only feeds the frags-remaining announcer, not the end-of-match decision.; The CheckRules_World mutator hook (M_ARGV status override) is still not invoked. |
| 200 | 3.6 | `turret-flac` | `turret-flac.weapon.player_form` | liveness:dead | FlacWeapon is implemented but its [Weapon] registration is intentionally OFF (FlacTurret.cs:178): impulse 5 collides with the Raptor bomb-mode impulse and registering shifts WeaponById/MenuWeaponOrder, which would break those test contracts. So the hidden player FLAC weapon has NO live caller (liveness dead).; presentation partial: BLASTER_MUZZLEFLASH is now emitted (FlacTurret.cs:273) — but with except: actor, whereas Base flac_weapon.qc:30 uses Send_Effect (networks to ALL incl. the firing player, not Send_Effect_Except). Minor: a player firing their own hidden weapon won't see their own muzzle flash. Also the hagar trail RENDER is client-side (Godot project, outside qcsrc parity). |
| 201 | 3.5 | `bot-waypoints` | `bot-waypoints.file.save` | liveness:dead | DEAD scaffolding: the three writers (SaveToText/SaveLinksToText/SaveHardwiredLinksToText, Waypoint.cs:880-945) are a faithful inverse of the loaders — version/symmetry/time header, generated-node skip, vtos 1-decimal formatting, the JUMP/SUPPORT/CUSTOM_JP source filter for cache vs hardwired/special sections — BUT have ZERO live callers (grep 2026-06-27: only referenced inside Waypoint.cs). No editor, save command, or test invokes them, so authored graphs still cannot be re-saved on the live path.; Hardwired-vs-special partitioning uses the CustomJp source flag as the hardwired marker (no dedicated wphwXX mirror), so a CUSTOM_JP source's links appear in both the HARDWIRED and SPECIAL sections, matching the QC overlap but losing the precise per-link hardwired identity on round-trip. |
| 202 | 3.5 | `mutator-buffs` | `buffs.swapper` | logic:partial | Core swap is implemented and LIVE: nearest enemy within range (DIFF_TEAM/dead/frozen/vehicle filtered), origin/velocity/angles + oldorigin exchanged, SND_KA_RESPAWN at both ends, buff_RemoveAll consumes the buff. The ForbidThrowCurrentWeapon hook has live Call sites (WeaponThrowing.cs:149/185).; Divergences from QC: uses center-distance + Api.Entities.FindInRadius instead of WarpZone_FindRadius (no warpzone traversal); omits Drop_Special_Items(closest) (flag/key carriers keep their objective on swap); omits MUTATOR_CALLHOOK(PortalTeleport, player); omits the EFFECT_ELECTRO_COMBO x2 visual at both endpoints. |
| 203 | 3.5 | `mutator-powerups` | `powerups.obituary_mutator_strings` | logic:partial | Death log does not append 'S' (Strength) / 'I' (Shield) item codes for a victim who held the powerup (LogDeath_AppendItemCodes unported — the port's kill log carries no :items= field). |
| 204 | 3.5 | `scoring` | `scoring.winningcondition.remaining_frags_announce` | liveness:partial | PARTIAL MODE COVERAGE (greatly expanded since the prior audit): 9 of Base's 11 Scores_CountFragsRemaining modes now call CountFragsRemaining live — Deathmatch (cs:300), TDM (cs:389), Keepaway (cs:644, gated on g_keepaway_score_timepoints==0), ClanArena (cs:791), FreezeTag (cs:1013), Duel (cs:264), Race (cs:1147, gated !g_race_qualifying), TeamKeepaway (cs:731, gated !g_tka_score_timepoints) plus the GameWorld per-frame driver. Only Mayhem (Mayhem.cs:295) and TeamMayhem (TeamMayhem.cs:37) still defer the announcer (suppression noted in comments). So those 2 modes play no 'N frags left' cue.; suddenDeathEnding is always passed false (all callers note checkrules_suddendeathend is framework-owned and not surfaced), so the QC 'force fragsleft=1 during sudden-death overtime end' branch never fires. |
| 205 | 3.2 | `mutator-buffs` | `buffs.networking` | liveness:unknown | Write/Read round-trip mirrors the QC major/minor bitmap + float time + byte flags, but whether the host net loop actually Flush()es buff state to clients each tick was not traced here (status_effects unit owns that). |
| 206 | 3.0 | `assault` | `assault.objective.waypoint_sprite` | presentation:missing | Presentation only: the objective waypoint sprite is spawned, health-bar-updated, disowned-on-destroy, and ticked SERVER-SIDE (SpawnObjectiveSprite + WaypointSprites.Think; WaypointSprites.Active = Waypoints.cs:244). But there is NO client render/networking path — nothing in src/XonoticGodot.Net reads WaypointSprites to serialize/render it. So the attacker has no on-screen objective marker, health bar, or radar icon yet. The state machine is faithful; the visible pixels are absent. |
| 207 | 3.0 | `deathmatch` | `deathmatch.menu.describe` | presentation:missing | Base's MENUQC METHOD(Deathmatch, describe) supplies the multi-paragraph gametype help text shown in the menu's gametype picker; the port's GameType base class has no Describe() override (monsters/weapons/mutators have Describe() but gametypes do not), so DM has no ported description. Pure menu cosmetic; no gameplay impact. |
| 208 | 3.0 | `mutator-nades` | `mutator-nades.boom.entrap_veil_darkness` | presentation:missing | Veil tint of enemies (render colormod) omitted; only the teammate alpha-hide is ported.; Darkness blind overlay (HUD_DarkBlinking full-screen fill) + SND_BLIND are CSQC-only and missing; only the NadeDarknessTime deadline is set server-side. |
| 209 | 3.0 | `mutator-waypoints` | `waypoints.weapon.whereis` | values:partial | FIX vs baseline (was missing/dead): Weapon_whereis is now ported in Inventory.cs and live — on the complain path of client_hasweapon (reached from the weapon-switch/cycle impulse via GetCycleWeapon:503 + SwitchWeaponWithComplain:518), when a non-bot player presses a weapon key for a weapon they don't own and g_showweaponspawns != 0, it scans the live item set and spawns a personal WP_Weapon sprite over every matching shown weapon-spawn (wp_extra = weapon id, radarIcon 0 = in-world only). All three g_showweaponspawns modes (1/2/3, loot skip, impulse-group expansion) are modeled.; values/timing: the marker lifetime diverges — Base uses -2 ('instant, no fade', re-spawned every press), the port uses a flat WhereisLifetime=2.5s deployed-style fade. Functionally similar (a brief on-demand marker) but not the bit-exact one-frame-then-re-spawn cadence. Minor.; wp_extra-driven client weapon-icon resolution: WpExtra is now SET on the sprite, but it is still not networked (waypoints.net.serialize gap), so the client renders the generic Weapon def rather than the per-weapon icon. Cosmetic. |
| 210 | 3.0 | `nexball` | `nexball.goal.touch_scoring` | presentation:missing | No bprint / Send_Notification goal announcement (Goaaaaal! / own-goal / fault / out text).; GOAL_TOUCHPLAYER spawnflag is approximated: GoalTouchEntity always treats a touching ball-carrier as a hit (the port has no per-goal spawnflag bit), and the early BallTeam/BallPusher clear differs from QC (which keeps them through the glide) but is behaviorally equivalent under the ball.cnt!=0 guard. |
| 211 | 3.0 | `nexball` | `nexball.ball.giveball` | presentation:missing | No carrier waypoint sprite; the carrier glow effect bit is cleared on the ball but not rendered on the carrier (client-deferred). |
| 212 | 3.0 | `nexball` | `nexball.ball.dropball` | presentation:missing | No ground waypoint sprite on drop (client-deferred). |
| 213 | 3.0 | `nexball` | `nexball.carry.perframe` | presentation:missing | STAT(OBJECTIVE_STATUS) NB_CARRYING bit is not networked (no NB_CARRYING anywhere in the port; no nexball case in any GametypeStatusBlock); the held-ball forteam-return notification (INFO_NEXBALL_RETURN_HELD) is not sent (the drop+reset effect IS applied).; No per-frame viewmodel scale (0.25) / EF_FLAME safe-pass lock indicator (client; QC ball_customize is CSQC setcefc). |
| 214 | 3.0 | `nexball` | `nexball.weapon.ballstealer` | presentation:missing | Primary launch routes velocity through WeaponFiring.ProjectileVelocity (newton-style OFF, no owner-velocity inheritance) where QC uses W_CalculateProjectileVelocity(actor, actor.velocity, ...); intended port-wide convention, but means the carrier's velocity is not added to the launched ball.; TackleTouch shove uses the secondary_force cvar directly (QC multiplies by toucher.damageforcescale, untracked in the headless player model — equals Base when damageforcescale==1).; No viewmodel / muzzle-flash / projectile model presentation (CSQC); QC spoofs WEP_PORTO.m_id in W_SetupShot to keep duplicates off the scoreboard — the port uses BallStealer identity instead (cosmetic, no scoreboard column for it). |
| 215 | 3.0 | `nexball` | `nexball.weapon.power_meter` | presentation:missing | No HUD power-meter progress bar (the HUD_Mod_NexBall charge bar is client-deferred — see nexball.hud.modicon). |
| 216 | 3.0 | `onslaught` | `onslaught.cp.touch_build` | presentation:missing | Presentation still missing: no EFFECT_CAP/EFFECT_RAGE particles, no pad-model swap (MDL_ONS_CP_PAD1/2), no build-bar waypoint sprite.; Audio emitted: SpawnIcon→ONS_CONTROLPOINT_BUILD on the CP; build completion→ONS_CONTROLPOINT_BUILT. |
| 217 | 3.0 | `onslaught` | `onslaught.cp.icon_damage` | presentation:missing | Audio emitted: shielded→ONS_DAMAGEBLOCKEDBYSHIELD to attacker (debounced); per-hit ONS_HIT1/2; destroy→ONS_GENERATOR_EXPLODE + ONSLAUGHT_CPDESTROYED_NONAME info. Presentation still missing: no EFFECT_SPARKS/ROCKET_EXPLODE particles, no progressive icon model swap.; Proximity self-destruct nearby-enemy reward MODELED (IconDamage attacker==self branch: ONS_TAKES+1/SCORE+5 to every nearby enemy within proximitydecap_distance), closed alongside onslaught.cp.proximity_decap. |
| 218 | 3.0 | `onslaught` | `onslaught.cp.icon_think_regen` | presentation:missing | Damaged-FX EFFECT_ELECTRIC_SPARKS particle still not spawned (presentation/client). The ONS_SPARK1/2 ambient spark crackle IS now emitted: IconThink rolls random()<0.6-hp/max, then SPARK1 at random()>0.8 else SPARK2 at random()>0.5 (faithful branch structure). |
| 219 | 3.0 | `onslaught` | `onslaught.gen.damage_destroy` | presentation:missing | Audio emitted: blocked-by-shield hint; GENERATOR_UNDERATTACK center + ONS_GENERATOR_UNDERATTACK (10s debounce); per-hit ONS_GENERATOR_EXPLODE at chance damage/220 else ONS_HIT1/2; destroy→ONSLAUGHT_GENDESTROYED(_OVERTIME) info + ONS_GENERATOR_EXPLODE. Presentation still missing: progressive damage `frame=10*(1-hp/max)`, flaming-gib/EFFECT_SPARKS particles, ons_camSetup death-cam.; NOTE: the per-hit 'gib impact' branch maps QC SND_ROCKET_IMPACT to ONS_GENERATOR_EXPLODE; under-attack alarm uses PlayGlobal not play2team. |
| 220 | 3.0 | `onslaught` | `onslaught.cp.proximity_decap` | presentation:missing | Proximity-decap IMPLEMENTED in IconThink: (friendly - enemy) live-player count within g_onslaught_cp_proximitydecap_distance changes icon HP by ±dps*THINKRATE each tick; at <=0 the icon is destroyed via IconDamage (lone enemy, or `self` to spread the multi-attacker +5 reward to all nearby enemies); a pure-friendly crowd repairs it. Master toggle defaults OFF in Base. Remaining: no waypoint/HUD presentation of the decap state. |
| 221 | 3.0 | `physics-player` | `physics-player.water.swim` | liveness:unknown | Liveness of the swim path on a live water map not behaviorally confirmed this audit (depends on CheckWater correctly setting WaterLevel from map liquid contents — the SUPERCONTENTS bitmasks are mirrored as literals and untested against a real BSP water volume here). |
| 222 | 3.0 | `physics-player` | `physics-player.ladder` | liveness:unknown | Not behaviorally tested on a live ladder map this audit; code-verified only (liveness:unknown). The func_water + viewloc ladder sub-cases are additionally inert on stock 3D maps (ViewLoc null; func_water ladder is a rare map feature). |
| 223 | 3.0 | `physics-player` | `physics-player.jetpack` | liveness:unknown | PM_jetpack maxspd_mod factor (player.qc:742 maxairspd *= max(1, maxspd_mod)) is now threaded through PMJetpack as maxspeedMod (PlayerPhysics.cs:886 `mp.MaxAirSpeed * MathF.Max(1f, maxspeedMod)`), so it is NO LONGER hardcoded to 1 — a spectator/high-speed-mod jetpack scales correctly. (Was a gap in the baseline; closed.) Residual: still unreachable in normal play (a real player has maxspeed_mod==1).; cl_jetpack_jump is pinned ON / mode-1 semantics (JetpackJumpEnabled const); QC mode 2 (latched thrust between frames) is not modelled. |
| 224 | 3.0 | `sv-chat` | `sv-chat.routing.recipients` | logic:partial | INGAME(it) round-mode semantics not modeled: the port has no per-player .ingame round flag, so IsIngame(p) == !IsObserver(p). In round/elimination modes (Clan Arena / LMS / Survival) an eliminated-but-still-INGAME player is treated as a spectator, so the spectator-downgrade (teamsay->-1) and the (IS_PLAYER\|\|INGAME) team-routing filter diverge from Base for those players. No effect on FFA/standard CTF/DM. |
| 225 | 3.0 | `sv-chat` | `sv-chat.ignore.crud` | logic:partial | Permanent (db-persisted) ignore tier is dropped (the QC return-2 path / ServerProgsDB by UID); ignores are keyed by PersistentId in-memory only (acceptable within a match, lost across server restarts). This is the sole remaining logic-partial reason. |
| 226 | 3.0 | `sv-cheats` | `sv-cheats.impulse.speedrun` | logic:partial | RestorePersonal (Speedrun.cs:95) omits the QC start-solid tracebox guard: Base traceboxes the personal.origin (MOVE_WORLDONLY) and, if start-solid, sprints 'Cannot move there, cheater...' and does NOT teleport (but still restores resources). The port always teleports + restores.; The AbortSpeedrun mutator hook (MUTATOR_CALLHOOK(AbortSpeedrun) — race-timing abort) is not modeled (Speedrun.cs comment-acknowledged). |
| 227 | 3.0 | `sv-cheats` | `sv-cheats.cmd.particles_make` | logic:partial | make: the bad-surface guard is weaker than Base. Base rejects when (trace_dphitq3surfaceflags & Q3SURFACEFLAG_NOIMPACT) OR trace_fraction==1; the port (Cheats.cs:348-353) only rejects on hit-nothing (fraction==1), so a NOIMPACT/sky surface would still place the breakable.; make: mode-1 surface-align is an APPROXIMATION — Base does fixedvectoangles2(normal, v_forward) then AnglesTransform_ApplyToAngles(angles,'-90 0 0'); the port (Cheats.cs:365-369) does FixedVecToAngles2 + a flat pitch -=90 (comment-acknowledged). The e.mdl='rocket_explode' wreck/explosion model is presentation-only and intentionally skipped.; penalty: only acts when the live gametype is Race (Cheats.cs:211); in non-race modes Base's race_ImposePenaltyTime is also effectively inert, so faithful, but the <reason> string's CSQC HUD notification is dropped (presentation-only). |
| 228 | 3.0 | `sv-cheats` | `sv-cheats.mapent.info_autoscreenshot` | logic:partial | g_observepoints membership is unwired: Base does tracebox(MOVE_WORLDONLY) and IL_PUSH(g_observepoints) only when NOT start-solid (the observer camera cycles these); the port has no observer-camera g_observepoints consumer (client concern) and skips the start-solid test entirely, keeping every point findable. For the TELEPORT cheat path (the only port consumer) this is faithful — it only needs the entity to exist with the right origin/angles. |
| 229 | 3.0 | `sv-client-lifecycle` | `sv-client-lifecycle.connect.client_connect` | logic:partial | Connect-side server->client channel partly deferred (see connect.fix_client_cvars_welcome row): no clientstuff/FixClientCvars/_teams_available stuffcmds, no Handicap_Initialize-here/model_randomizer, no Send_NextMap. (The version_nagtime arming, the `clientversion` request handler, the MOTD/g_mutatormsg/mutators welcome lines, and playban/chatban ARE now wired.); Player_DetermineForcedTeam / forced-team-otherwise spectator routing IS now wired (ClientManager.cs:220 DetermineForcedTeam), with team pre-assigned via AssignBestTeam for team games. |
| 230 | 3.0 | `sv-client-lifecycle` | `sv-client-lifecycle.spawn.put_player_in_server` | logic:partial | In-PutPlayerInServer team assignment (bot_forced_team / wants_join>0 SetPlayerTeam / TeamBalance_JoinBestTeam) is not run here — the port pre-assigns the team once at connect, so a mid-match team change or wants_join preference isn't honored on respawn.; wr_resetplayer + reload-all-reloadable-weapons-on-spawn (weapon_load = reloading_ammo) and CL_SpawnWeaponentity per slot are flagged as client/networking concerns and not done server-side.; FixPlayermodel resolves every player+bot to sv_defaultplayermodel (client playermodel cvar not plumbed); faithful stand-in but not the real per-client model. GiveRandomWeapons IS wired (randomCount>0 branch); the ForbidRandomStartWeapons hook is not dispatched. |
| 231 | 3.0 | `sv-client-lifecycle` | `sv-client-lifecycle.observer.put_observer_in_server` | logic:partial | Still omits: SelectObservePoint / observe-spawn placement (the port keeps the current origin instead), VoteCount/ReadyCount recount, the SPECTATE_WARNING / sv_maxplayers_spectator_blocktime block, CHAT_NOSPECTATORS notice, RemoveGrapplingHooks/Portal_ClearAll, alivetime PlayerStats report, and the is_forced/use_spawnpoint signature (the port has no is_forced/use_spawnpoint params). |
| 232 | 3.0 | `sv-client-lifecycle` | `sv-client-lifecycle.observer.observer_think` | logic:partial | SpectatePrev (impulse 12/16/19/220-229), the IMP_weapon_drop CAMERA_SPECTATOR cycle, MinigameImpulse handling, the +use clippedspectating MOVETYPE_FLY toggle, and the sv_spectate 2 'spectating not allowed' block are not modeled. |
| 233 | 3.0 | `sv-client-lifecycle` | `sv-client-lifecycle.observer.autojoin_delayed` | logic:partial | g_maxping / forced-team (TEAM_FORCE_SPECTATOR) / AutoJoinOnConnection mutator hook / (!teamplay \|\| g_balance_teams) gates deferred. The QC `ping > 0` precondition (delay until centreprints work) is not modeled. sv_spectate and g_campaign holds ARE honored. |
| 234 | 3.0 | `sv-client-lifecycle` | `sv-client-lifecycle.disconnect.client_disconnect` | logic:partial | Still not modeled: ignore-list cleanup, Portal_ClearAll/RemoveGrapplingHooks, chatbubble/killindicator/personal/g_counters delete, minigame part, WaypointSprite_PlayerGone, ReadyCount/VoteCount-on-leave, sv_hook_lastleave, and TeamBalance_RemoveExcessPlayers / QueuedPlayersTagIn. (Vehicle exit, player_powerups_remove_all + poweroff sound, the status-effect clear, the gametype objective-drop, and the generic MUTATOR_CALLHOOK(ClientDisconnect) ARE now modeled.) |
| 235 | 3.0 | `sv-impulse` | `sv-impulse.weapon.getcycle` | logic:partial | The impulse-group complain path is simplified: QC computes have_other and a custom-group bitmask for imp<0 (selection.qc:148-171); the port omits both since it drives the imp=-1 cycle/best path. The weaponsInMap (map-spawned-but-unowned weapon) branch (selection.qc:166,187) is unmodeled, so unowned-but-on-map weapons are skipped (cannot be cycle-selected before pickup). |
| 236 | 3.0 | `sv-intermission` | `sv-intermission.mapvote.server_core` | liveness:partial | There is still no NetControl.MapVote opcode (NetProtocol.cs enum ends at MatchState=18 + waypoints), so the ballot/tally is not serialized over the wire and remote (--connect) clients can neither see nor cast. On a LISTEN server the cast path is live: a player's number-key 1..9 emits a C2S impulse, which Commands.DispatchImpulse intercepts while MapVoting.Running and routes to MapVoting.CastVote(caller, imp-1). For a dedicated/remote-client server the vote still closes on timeout/PlayerCount with zero real votes (deterministic RNG tiebreak). |
| 237 | 3.0 | `sv-intermission` | `sv-intermission.mapvote.client_ui` | liveness:partial | The panel is fed only from the in-process GameWorld.MapVote (NetGame.UpdateMapVotePanel reads _serverWorld?.MapVote), so it lights up ONLY on a listen server. A pure --connect client has no in-process MapVoting and no NetControl.MapVote decode, so its ballot stays blank and it cannot vote — full remote parity still needs the networked opcode (see sv-intermission.mapvote.server_core).; The panel has no mouse/cursor click handler (Base MapVote_InputEvent lets you click a map tile); voting is by number-key 1..9 only. Faithful enough to vote, but the click-to-vote affordance is absent. |
| 238 | 3.0 | `sv-ipban` | `sv-ipban.insert` | logic:partial | The dosync leg (OnlineBanList_SendBan, with the reason!="" and not-~-prefixed guard) is dropped — no online ban propagation on insert (see sv-ipban.sync.online). |
| 239 | 3.0 | `sv-mapvoting` | `sv-mapvoting.gametype_vote` | logic:partial | FIXED: the sv_vote_gametype_hook_all/_<name> localcmd hooks NOW FIRE. Commands.GameTypeVoteHookHandler is assigned at NetGame.cs:825 to `name => _serverWorld?.LoadedConfig?.ExecuteLine($"sv_vote_gametype_hook_{name}")`, and ApplyGametypeSwitch invokes it for 'all' + the winning gametype (GameWorld.cs:4075-4076). Stock hooks are empty aliases (no visible effect), but a server.cfg that redefines an alias now runs against the cvar store. (Was the primary baseline logic-partial reason — now resolved.); STILL PARTIAL: GameTypeVote_AvailabilityStatus' next-map availability filter is absent — StartGametype marks every resolvable type GTV_AVAILABLE (MapVoting.cs:307-309) without the MapInfo_Get_ByName(get_nextmap()) + supported-gametypes check, so an option the queued nextmap can't support is shown available instead of dimmed.; STILL PARTIAL: Maplist-reset enumerate fallback absent — ApplyGametypeSwitch sets g_maplist only from a per-gametype sv_vote_gametype_<name>_maplist cvar; with maplist_reset=1 but no per-gametype list Base regenerates g_maplist via MapInfo_ListAllowedMaps, the port keeps the current list (GameWorld.cs:4085-4086).; The match_gametype temp-switch / restart-reapply dance (MapVote_Init's `if (match_gametype) GameTypeVote_SetGametype(...)`) is not modeled; the port applies the resolved gametype to the `gametype` cvar for the next boot (GameWorld.cs:4071) rather than live-switching MapInfo mid-process. |
| 240 | 3.0 | `sv-mapvoting` | `sv-mapvoting.client.draw` | liveness:partial | Panel is FED on a listen server: NetGame.UpdateMapVotePanel calls SetVote/SetVotes/SetAvailability/OwnVote/SetWinner and owns Visible, so the player sees the ballot grid, level-shots, live counts, own-vote highlight, the selection pulse, the top-2 reduce-fade, and the winner reveal during a real listen-server match.; The GametypeVote DRAW path is ALSO driven: UpdateMapVotePanel passes gametypeVote: isGametypeVote and feeds gametype icons (gfx/menu/<skin>/gametype_<name>), pretty DisplayNames + descriptions + sv_vote_gametype_detail, so GameTypeVote_DrawGameTypeItem's counterpart renders during a real gametype vote.; The mv_top2_alpha reduce-fade is driven: SetAvailability stamps _top2Time on first reduce.; STILL DEAD for remote/dedicated-server clients: no net entity feeds the panel when _serverWorld is null (see sv-mapvoting.net.sync), so a non-listen client never sees the vote screen (liveness partial). |
| 241 | 3.0 | `sv-spawnpoints` | `sv-spawnpoints.select.select_spawn_point` | logic:partial | Emergency fallback differs: Base, when Spawn_FilterOutBadSpots returns empty, RE-FILTERS the spot list once with targetcheck=false (server/spawnpoints.qc:421-435) before giving up. The port runs the targetcheck=false re-filter (SpawnSystem.cs:435-436), then returns null if even that comes back empty (SpawnSystem.cs:445-446) so the ClientManager caller can retry/log — rather than seeding all spots at prio-0. Faithful in shape; the only divergence is the post-re-filter NULL handling vs Base's error()/GotoNextMap branch. Low severity. UNCHANGED.; anypoint / g_spawn_useallspawns path: Base skips Spawn_FilterOutBadSpots and does Spawn_WeightedPoint(1,1,1) over ALL spots (spawnpoints.qc:413-416). The port mirrors this (SpawnSystem.cs:415-421: seeds all spots at prio0/weight0, NO startsolid/team/active/restriction filtering) — matches Base. UNCHANGED (faithful).; testplayerstart debug edict (spawnpoints.qc:363-370) — Base returns a 'testplayerstart' classname spot before any other logic; not ported (debug-only). UNCHANGED. |
| 242 | 3.0 | `sv-spawnpoints` | `sv-spawnpoints.mutator.spawn_near_teammate` | logic:partial | Residual relocation-branch defers: tracebox_hits_trigger_hurt (hurt-volume reject), checkpvs (PVS visibility), and the g_nades nade-in-range reject are still skipped (a faithful superset that may keep a spot QC would reject); weaponLocked and PHYS_INPUT_BUTTON_CHAT gates are always false (no plumbed input/weapon-lock); closetodeath uses the player's current origin not .death_origin. The sky-surface (Q3SURFACEFLAG_SKY) and lava/slime/water pointcontents rejects ARE applied. STILL OPEN (the residual defers), but no change since baseline. |
| 243 | 3.0 | `sv-teamplay` | `sv-teamplay.admin.lock_shuffle_move` | logic:partial | moveplayer/team don't enforce lockteams backup/restore semantics of MoveToTeam (which temporarily disables the lock for the move then restores it); behavior overlaps but isn't the exact MoveToTeam path. |
| 244 | 3.0 | `sv-world-rules` | `sv-world-rules.warmup.reset_map` | logic:partial | No per-client accuracy_reset pass on reset_map (the per-player hit/fire accuracy counters are not zeroed). Inventory_clear is subsumed by the Clients.Spawn -> PutClientInServer start-loadout re-give. |
| 245 | 3.0 | `sv-world-rules` | `sv-world-rules.misc.shutdown` | logic:partial | ServerProgsDB persistence on shutdown (db_save/db_dump of server.db<sessionid>) has no port analogue.; bot_endgame, CheatShutdown, WeaponStats_Shutdown and MapInfo_Shutdown are not performed on world teardown. |
| 246 | 3.0 | `tka` | `tka.ball.drop` | presentation:missing | DropBall is invoked from OnDeath (LIVE death hook) when the victim carried. It detaches, drops at the feet with the crandom xy scatter, arms the 0.5s self-recapture lockout, re-arms the relocate think, plays SND_KA_DROPPED, sends INFO/CENTER_KEEPAWAY_DROPPED, and (via CollectWaypoints next tick) shows the loose-ball waypoint.; FIXED (liveness partial -> live): the use-key, observer, and disconnect drop triggers are now wired. OnUseKey subscribes MutatorHooks.PlayerUseKey (Activate, TeamKeepaway.cs:387) and is fired live via VehicleBoarding.cs:106 FirePlayerUseKey on the +use impulse path (QC tka PlayerUseKey -> tka_DropEvent, consumes the press). OnPlayerRemoved (TeamKeepaway.cs:406) drops the ball when the carrier is demoted to observer or disconnects, dispatched from ClientManager.cs:513 (PutObserverInServer/MakePlayerObserver) + :761 (ClientDisconnect) — QC MakePlayerObserver/ClientDisconnect both tka_DropEvent.; RESIDUAL: DropSpecialItems (the special-item-strip drop edge) is still not wired — 3 of Base's 4 drop hooks are now live. Also: no nudgeoutofsolid_OrFallback, no multi-ball chain advance / tka_PlayerReset VIP clear. |
| 247 | 3.0 | `turret-fusionreactor` | `turret-fusionreactor.fx.attack` | liveness:partial | No client turret render (ENT_CLIENT_TURRET): the reactor renders no model and no head, so the ammo-scaled head spin (Base '0 250 0'*ammo/ammo_max, up to 250 deg/s) is computed onto TurretState.HeadAVelocity but never integrated/displayed. No waypoint sprite/healthbar/death FX either. |
| 248 | 3.0 | `turret-hk` | `turret-hk.weapon.player` | liveness:dead | DEAD: HunterKillerWeapon carries NO [Weapon] attribute (registration intentionally deferred — WEP_FLAG_SPECIALATTACK would shift WeaponByIdTests.ByIdOrder_Covers... skipped count 0->1 and break the test). So the impulse-9 / `give weapon_turret_hk` fire path has no live caller through WeaponFireDriver; WrThink is unreachable in a normal match. Class compiles + is fully faithful, but never runs on the live path. |
| 249 | 3.0 | `vehicle-framework` | `vehicle-framework.lifecycle.reset` | logic:missing | The QC `.reset = vehicles_reset` round-restart hook is NOT registered on any port vehicle (grep: no GtReset/.Reset assignment in src/.../Vehicles). On a round/match restart Base ejects any pilot (vehicles_exit VHEF_RELEASE), removes the return-helper entities (vehicles_clearreturn), and respawns the vehicle when active. The port has no equivalent reset path — a vehicle's state across a round boundary depends on whatever the generic spawn/respawn does, not this hook. |
| 250 | 3.0 | `weapon-arc` | `weapon-arc.beam.burst_variant` | presentation:missing | ARC_BT_BURST visual thickness (14) and burst beam color/visuals absent (depends on the beam.visual feature). UNCHANGED. |
| 251 | 3.0 | `weapon-blaster` | `weapon-blaster.bolt.think_lifetime` | presentation:missing | CSQCProjectile(PROJECTILE_BLASTER) client trail/sprite STILL not networked: the flying bolt has no DP-style render trail (Wave-1/2 added no client-render seam for the bolt). |
| 252 | 3.0 | `weapon-crylink` | `crylink.menu.describe` | presentation:missing | The MENUQC describe() weapon-guide prose (burst of laser-like projectiles, converge-on-release, double-damage-on-bounce, secondary tight-group, cells ammo, 'crylink running' pull blurb + W_Guide_Keybinds/DPS_bothMultishot) has no port. Menu-only documentation; no gameplay impact. Consistent with the weapon-shotgun.menu.describe gap. |
| 253 | 3.0 | `weapon-devastator` | `weapon-devastator.menu.describe` | presentation:missing | The MENUQC weapon-guide paragraphs (remote-controlled rocket, hold-to-guide, ammo drain, rocket-flying with the rocketflying mutator, keybinds, DPS) are not ported — the port has no in-game weapon-guide page for the Devastator. |
| 254 | 3.0 | `weapon-minelayer` | `minelayer.describe` | presentation:missing | The MENUQC describe() weapon-guide prose (minelayer.qc:549-563 — the multi-paragraph in-menu weapon description shown in the Weapons guide page) has no port analogue. Several other weapons reference describe() only in comments (Electro.cs:98, Tuba.cs:63, Crylink.cs:160) but the port has no weapon-guide menu page, so this is a menu-only presentation gap (no gameplay effect). |
| 255 | 3.0 | `weapon-shotgun` | `weapon-shotgun.menu.describe` | presentation:missing | The MENUQC describe() weapon-guide prose (primary single round spreading into pellets, secondary slap timing, ammo/falloff blurb + W_Guide_Keybinds/DPS) has no port. Other weapons (Crylink/Electro/Tuba) reference a describe seam but the shotgun text is not ported. Menu-only documentation; no gameplay impact. |
| 256 | 3.0 | `weapon-vaporizer` | `weapon-vaporizer.menu.describe` | presentation:missing | The Base describe() weapon-guide prose (four PAR paragraphs: the energy-beam superweapon overview, the instagib one-shot caveat, the Blaster-laser secondary, and the cell-ammo note + W_Guide_Keybinds/W_Guide_DPS lines) is not ported — the Vaporizer has no GuideDescription override, so the live menu weapon guide (DialogMediaGuide) renders blank for the Vaporizer. |
| 257 | 2.8 | `bot-ai` | `bot-ai.aim.lead` | logic:partial | Ballistic aim is a closed-form gravity-drop approximation, not QC's 10-trace tracetoss search; lobbed-weapon aim (mortar/nade) is less accurate, especially with obstacles in the arc, and never traces the world to confirm the toss lands. (UNCHANGED.); Shot speed is read from g_balance_<wep>_primary_speed/gravity for the single ChosenWeapon (via the BotAimShotSpeed override hook, now used by Devastator/Crylink/Fireball), not per-weaponentity with W_WeaponSpeedFactor applied; weapons without those cvars are treated as hitscan (no lead). (PARTIALLY NARROWED.) |
| 258 | 2.8 | `bot-ai` | `bot-ai.weapon.choose` | logic:partial | Weapon picking is a hitscan-at-range / splash-up-close bucket choosing the highest-impulse owned weapon, not the QC ordered far/mid/close priority lists. (UNCHANGED.); No weapon-combo behaviour (bot_ai_weapon_combo): bots don't hold a weapon for a follow-up combo shot. (UNCHANGED.); No bot_rangepreference shift, so per-bot 'sniper vs spammer' weapon-range personalities are absent. (UNCHANGED.) |
| 259 | 2.8 | `bot-ai` | `bot-ai.roles.goalrating` | logic:partial | Goal rating still uses straight-line inverse distance (f*rangebias/(rangebias+vlen(dist))) instead of the QC waypoint-cost (Dijkstra) field; bots can prefer a goal near in a straight line but far by path. (UNCHANGED.); rangebias is still hardcoded 2000 for every rate call (items/enemies/roam) instead of the per-rating-call bias QC uses. (UNCHANGED.); No separate _movingtarget (5.5s) strategy interval for goals that can move. (UNCHANGED.); enemyplayers aggression refinement: QC skill>3 nudges the t factor by Strength/Shield powerup timers (StatusEffects_gettime); the port has no status-effect-expiry accessor yet, so that refinement is deferred. |
| 260 | 2.8 | `cl-crosshair` | `cl-crosshair.color.special_modes` | logic:partial | crosshair_color_special 3 (rainbow: randomvec()*brightness re-rolled every rainbow_delay, crosshair.qc:189-196) is not ported — ColorModeCvar (CrosshairPanel.cs:537-542) maps only 0/1/2, so mode 3 silently falls back to fixed color. The rainbow_brightness/delay cvars are registered but dead. |
| 261 | 2.8 | `cl-crosshair` | `cl-crosshair.objring.nade_capture_revive` | liveness:partial | PARTIAL FIX: REVIVE_PROGRESS is now fed (NetGame:2559, _fullHud.Crosshair.ReviveProgress = LocalServerPlayer?.ReviveProgress) alongside NadeTimer (2558), both host-only inside `if (_server is not null)`. Player.ReviveProgress is genuinely mirrored each tick by FreezeTag (FreezeTag.cs), so the host player's thaw ring now renders. STILL OPEN: CAPTURE_PROGRESS is never assigned on any production path (grep finds only test-side .CaptureProgress= plus the networked NetEntity.CaptureProgress field which ServerNet.cs explicitly leaves 0 with a comment that no gametype sets it) so the capture ring is DEAD; and although NetEntityState.ReviveProgress IS networked to remote clients (ServerNet.cs), NOTHING reads it back into the panel client-side — a pure remote client gets no nade/thaw ring.; Rings drawn centred on the crosshair panel (radius 64px) rather than at Base's `0.5*conwidth, 0.6*conheight` screen anchor with `0.1*conheight` radius; nade-timer text label (cl_nade_timer 2) not drawn. |
| 262 | 2.8 | `cl-csqcmodel` | `cl-csqcmodel.predraw.dispatch_order` | logic:partial | Order is appearance->LOD->effects (skeletal anim runs in a separate PlayerModel.Pose pass, not interleaved here); the appearance-before-LOD ordering is moot because LOD is a no-op swap.; AutoTagIndex recursion + drawmask=0 on null-model are not part of this pass (held weapons attach via ViewEntityRenderer). |
| 263 | 2.8 | `cl-hud` | `cl-hud.panel.radar` | liveness:partial | Maximized clickable mode (ons spawn / teleport select) is still unported; the discovered radar panel is force-hidden (StartHiddenIds) to avoid a double radar and the live radar is the standalone _radar. |
| 264 | 2.8 | `cl-scoreboard` | `cl-scoreboard.field.formatting` | logic:partial | SP_FPS and SP_SKILL columns never render (FieldByLabel returns null for fps/skill — client-only display fields with no port plumbing); the GetField FPS/SKILL branches exist (ScoreboardPanel.cs:684-697) but are unreachable because the columns are never fed.; scores_per_round averaging is now plumbed (ScoresPerRound + per-field roundsPlayed division), but no interactive scores_per_round toggle (Ctrl+R) — only a console cvar drives it. |
| 265 | 2.8 | `cl-shownames` | `cl-shownames.gate.self_spectatee` | liveness:partial | Chase own-tag for the truly-local player still can't draw: the predicted local player is never in ClientNet.RemoteIds (HandleSnapshot skips LocalNetId), so the self branch only fires for the spectatee. In stock play this matters only for hud_shownames_self + chase_active on your OWN model in third person — a residual deferral; the spectatee path (the common observed case) is live. |
| 266 | 2.8 | `fx-effectinfo` | `fx-effectinfo.emit.by_name_fallback` | liveness:partial | The Effect=null engine-fallback request (unregistered effectinfo name) records the name but there is no DP-engine __pointparticles equivalent client-side; only names that also resolve in the effectinfo.txt catalog render. Used by te_csqc_lightningarc/te_beam (arc_lightning/heal_beam) which are handled by the beam renderer rather than the registry. |
| 267 | 2.72 | `cl-hud` | `cl-hud.panel.itemstime` | values:unknown | Progressbar / dynamicsize / ratio-columns fidelity not runtime-verified. |
| 268 | 2.72 | `cl-hud` | `cl-hud.panel.strafehud` | values:unknown | Bar zones / best-angle marker / wturn-switch indicator + mode/style/range fidelity not runtime-verified. |
| 269 | 2.7 | `monster-spider` | `monster-spider.anim.mr_anim` | liveness:partial | CLIENT DPM PLAYBACK STILL MISSING. The server now maps the logical MonsterAnim phase to the spider's DPM frame-group start index (Spider.AnimFrame) and stamps it onto the networked Entity.Frame each think (MonsterAI.DriveAnimFrame, called in RunThink/MarkPain/MarkDead) — this IS live and the frame values match mr_anim exactly. BUT the client renders the spider's spider.dpm via ClientWorld.EntityModelFactory -> NetGame.BuildEntityModel -> AssetLoader.LoadModel -> DpmBuilder (an AnimationPlayer-backed node that is NOT bound to the Entity and NEVER consumes the networked Entity.Frame). Only the MD3 morph path (ClientWorld ModelAnimator with FollowEntityFrame) honours Entity.Frame, and ResolveEntityModel returns null for a DPM, so the spider never takes that path. Net: the server anim PHASE is correct + networked, but the visible DPM model does not play walk/bite/web/pain/die. Shared monster-presentation gap (all DPM/IQM monsters). |
| 270 | 2.7 | `turret-walker` | `turret-walker.drive.gaits_extra` | logic:partial | No standalone STRAFE_L/R or PAIN gaits (Base never drives these from the SVQC brain either — they are leftover model anim states — so this is parity-by-omission, not a behavioral gap).; JUMP/LAND/TURN gait cases exist in ApplyGait but are never assigned (same as Base — the brain never sets them). |
| 271 | 2.5 | `mutator-nades` | `mutator-nades.boom.dispatch` | presentation:missing | The colored explosion particle (Send_Effect_Except with per-type EFFECT + player-color min/max via findbetterlocation) is omitted (render-only).; Base plays SND_Null on CH_SHOTS_SINGLE + SND_ROCKET_IMPACT on CH_SHOTS; the port plays only weapons/rocket_impact.wav. |
| 272 | 2.5 | `mutator-nades` | `mutator-nades.boom.napalm` | presentation:missing | EFFECT_FIREBALL_LASER trail + EF_FLAME ball effect omitted (render). SND_NADE_NAPALM_FIRE spamsound on ball-eject is not emitted (audio partial). |
| 273 | 2.5 | `mutator-nades` | `mutator-nades.damage.freezetag_revive` | logic:partial | The port suppresses the self-nade damage+force but does NOT call freezetag_Unfreeze / set freezetag_revive_nade_health / send EFFECT_ICEORGLASS + INFO_FREEZETAG_REVIVED_NADE/CENTER_FREEZETAG_REVIVE_SELF notifications (documented cross-task seam — FreezeTag owns the unfreeze). So the revive-nade does not actually revive. |
| 274 | 2.5 | `mutator-nix` | `mutator-nix.add.wrinit` | logic:partial | Port resets the rotation globals (_nixWeapon/_nixNextWeapon/_nixNextChange = 0) and reads cvars (Hook lines 81-94), but does NOT run the `FOREACH(Weapons, choosable, it.wr_init(it))` warm-init pass. QC itself comments the per-rotation re-init as 'too slow' and skips it; the ONADD warm is cosmetic/perf and has no observable gameplay effect. |
| 275 | 2.5 | `mutator-overkill` | `overkill.respawn.remember_restore_weapon` | logic:partial | Port records/restores only slot 0: OnPlayerDies stores Inventory.CurrentWeapon(target) into OkLastWeapon[0] and nulls slots 1..MAX_WEAPONSLOTS; Base captures every slot's m_switchweapon (ok_lastwep[MAX_WEAPONSLOTS]). Low practical impact — Overkill loadouts occupy slot 0 only — but the per-slot capture is not faithfully reproduced. UNCHANGED by Wave-2. |
| 276 | 2.5 | `mutator-random_gravity` | `random_gravity.roll.round_gate` | liveness:partial | Liveness is partial-by-design: RoundNotStartedProvider is wired only inside GameWorld.EnableRounds (GameWorld.cs:1625), i.e. only for round-based gametypes (CA/Freezetag/round Domination/…). In non-round modes the provider is null and RoundGateBlocks() returns false — which exactly matches QC where round_handler_IsActive() is false outside round modes. So the gate is correctly inert where Base is inert, and active where Base is active. Not a defect, just mode-scoped reachability. |
| 277 | 2.5 | `mutator-sandbox` | `mutator-sandbox.object.duplicate` | liveness:partial | WIRED: copy/paste reachable via CmdSandbox → HandleCommand; PASTE is fully live (flood/cap/ObjectPortLoad).; COPY clipboard STILL inert: SandboxObjectStore.SetClipboard is an empty body (NetGame.cs:1046) — the port has no svc_stufftext channel to set a remote client's cvar. Paste from a manually-set cl_sandbox_clipboard works.; Paste shares the flood + maxobjects gates with object_spawn (faithful). |
| 278 | 2.5 | `mutator-waypoints` | `waypoints.registry.defs` | liveness:partial | Residual: the wp_extra-DRIVEN spritelookupblinkvalue overrides (superweapon Weapon=2, Item=item.m_waypointblink) are not modeled — Weapon's def blink is a flat 1 and there is no per-instance wp_extra blink lookup. Harmless while those producers (superweapon pickups) are absent. The Seeker/Item/Weapon DEFS are now exercised by live producers; only the wp_extra per-instance blink override is unmodeled.; liveness: partial — defs are reachable only once their gametype emits waypoints; CTF + Domination + FreezeTag + Keepaway + TeamKeepaway + Assault + Key Hunt + LMS + itemstime (Item/Weapon) + Seeker + Weapon_whereis (Weapon def) + Monster (g_monsters_healthbars) + player-deployed pings now exercise their defs. Still NOT on the live path: Ons*/Nb*/Race*/Vehicle*/Buff. |
| 279 | 2.5 | `mutator-waypoints` | `waypoints.server.manager_api` | liveness:partial | DEPLOY + CLEAR + HELPME slice is live (DeployFixed/DeployPersonal/Attach/ClearPersonal/ClearOwned/HelpMePing from Commands.cs; Reset() + Think() in GameWorld; SendWaypoints merges WaypointSprites.Active per peer).; OBJECTIVE/UPDATE slice is BROADLY live now: SpawnFixed/Spawn/UpdateMaxHealth/UpdateHealth/Disown/Kill via Assault (SpawnObjectiveSprite); Spawn/UpdateBuildFinished/Ping/Kill via itemstime (ItemPickupRules.RespawnCountdown, ItemPickupRules.cs:794/809/824); Spawn/UpdateRule/Kill via Seeker (Seeker.cs:496/514/519); Spawn/UpdateMaxHealth/UpdateHealth/Kill via powerup drops (PowerupsMutator.cs:541-575); Spawn (personal, radarIcon 0) via Weapon_whereis (Inventory.cs:403); Spawn/UpdateMaxHealth/UpdateHealth/UpdateTeamRadar/Kill via MONSTER healthbars (MonsterAI.cs:452/460/461/433/691).; FIX this audit: UpdateTeamRadar is now LIVE — MonsterAI.ChangeTeam (monster_changeteam) re-colors the monster healthbar radar icon to the new team (MonsterAI.cs:433). Still NO live caller: AttachCarrier, DetachCarrier — the CTF/Dom/FreezeTag/Keepaway/KeyHunt/LMS carrier producers build WaypointSprite records directly in CollectWaypoints (not via the manager carrier helper). |
| 280 | 2.43 | `mapobject-teleporters-portals` | `portals.teleport.transform_and_amazing` | values:partial | The exact +/-48 plane-shift (Portal_ApplyTransformToPlayerAngle s/t bound onto the exit plane), Portal_FindSafeOrigin tracebox safe-origin validation, and the precise angle transform are replaced by the generic warpzone transform; angle/velocity/seam correctness is not behaviorally verified (values: partial). |
| 281 | 2.4 | `monster-framework` | `monster-framework.move.brain` | logic:partial | movelib_move_simple / movelib_brake_simple are approximated with a fixed 0.4 lerp / (1 - stopspeed*0.01) decay 'so behavior is stable regardless of frametime' rather than the QC frametime-scaled accel — timing/feel diverges slightly. (UNCHANGED.); steerlib_attract2 (the QC steering blend toward moveto with leadahead 500/0.95) is replaced by a direct (moveto-origin) yaw turn; the steer-prediction smoothing is not modeled. (UNCHANGED.); The MatchHalted brake-and-idle halt is LIVE (RunThink->Move on every monster Think); MatchHalted reads the live VehicleCommon.GameStopped (per-tick server write), so monsters brake-and-idle for prematch AND intermission/timeout. DRIFT FIX (2026-06-25): the campaign bots-may-start movement hold (QC sv_monsters.qc:846 autocvar_g_campaign && !campaign_bots_may_start) is now LIVE too — CampaignMovementHeld() reads MonsterAI.CampaignBotHold, wired at GameWorld.cs:686 to (g_campaign && !Campaign.BotsMayStart). MonsterMove mutator hook + draggedby remain deferred host concerns; the movelib/steerlib approximations remain (the residual partial). |
| 282 | 2.4 | `sv-teamplay` | `sv-teamplay.nagger.team_nagger` | presentation:missing | No Nagger network entity / center-print: warmup is now HELD while teams are unbalanced, but players are never SENT the unbalanced-teams warning (no SendFlags bits 5/6, no center-print). The visible nag is absent.; Warmup-hold uses SizeDifference over the full roster; Base TeamBalance_SizeDifference(NULL) is the same NULL-ignore form, so the hold trigger is faithful. |
| 283 | 2.25 | `assault` | `assault.mapobject.assault_wall` | timing:partial | Timing: QC re-evaluates each func_assault_wall on a 0.2s nextthink; the port re-evaluates every tick (DriveWalls from the per-frame DriveFrame) but only re-stamps the edict on a state transition. Functionally equivalent (toggle happens at most ~0.2s sooner); transition-gated and cheap. Not a behavioral divergence. |
| 284 | 2.25 | `freezetag` | `freezetag.mode.registration` | values:partial | Match limit semantics differ: Base reads mapinfo pointlimit (10) / leadlimit (6) via g_freezetag_point_limit=-1 passthrough; port keys off fraglimit/leadlimit. Live default round target (10) coincidentally matches, leadlimit 6 likely not honored. |
| 285 | 2.25 | `freezetag` | `freezetag.revive.score` | values:partial | No nade revival bonus (g_nades_bonus_score_low) on a completed revive — minor, requires the nades mutator. |
| 286 | 2.25 | `freezetag` | `freezetag.round.flow` | values:partial | C# fallback timings (warmup 5/timelimit 180/enddelay 5) still differ from Base (10/360/0); masked live because gametypes-server.cfg ships. The round-timelimit-expiry tie path (CheckWinner ROUND_OVER) IS implemented (FreezeTag.cs:797-811: thaw all + ROUND_OVER notif + return -1) but depends on Handler.RoundEndTime being mirrored from the live RoundHandler. |
| 287 | 2.25 | `freezetag` | `freezetag.win.roundlimit` | values:partial | Lead-limit default (Base 6 via mapinfo) likely not honored (port reads leadlimit cvar, default 0 = off). |
| 288 | 2.25 | `keyhunt` | `keyhunt.unreachable.damage_destroy` | values:partial | Minor: g_balance_keyhunt_damageforcescale (default 1) is not read — the port's KeyDamage gates the re-team branch on force != 0 (raw force) rather than scaling the key's knockback by damageforcescale. At stock 1 this is a no-op; a tuned damageforcescale wouldn't affect the key's physical kick. The destroy/return behavior (NEEDKILL -> delay_damage_return) and the force-reteam logic are faithful. |
| 289 | 2.25 | `mapobject-movers-platforms` | `mapobject-movers-platforms.plat.outside_target_use` | liveness:partial | PlatOutsideTouch is implemented and faithful but DEAD — and that is itself faithful to Base: Base platforms.qc only DEFINES plat_outside_touch (line 111) and never installs/spawns an outside trigger anywhere, so stock func_plat never calls it. The port matches (no outside trigger generated). Reachable only via custom-mapper wiring neither Base nor the port produces. |
| 290 | 2.25 | `mapobject-target` | `mapobject-target.items.use` | logic:partial | The spawnfunc's normalized re-serialization of item tokens into a max/min/minus-prefixed give string (driven by spawnflags 0/1/2/4, then re-tokenized) is NOT reproduced; the port keeps the raw netname and parses it directly via GiveItems.Apply — equivalent for the common (give-string) case, but spawnflag-prefixed (max/min/minus) target_items semantics and the GetResource-driven resource serialization are not honored.; The loot-toucher delete branch (ITEM_IS_LOOT actor -> EXACTTRIGGER_TOUCH + delete, and IL_EACH delete of loot enemies) is not modelled (target_items is .use-only here; the port early-returns on an Item-flagged actor instead of deleting it). |
| 291 | 2.25 | `mapobject-target` | `mapobject-target.speaker.ambient_triggered` | logic:partial | QC objerrors a LOOPED_OFF untargeted speaker; the port silently leaves it inert (SpeakerSetup falls through with no error).; The *-prefixed per-player voice-message sample requires a real-client activator WITH a model; an ambient/null/no-model invocation falls back to silence (matching QC's SND(Null) not-found). For the ACTIVATOR path this is correct (it gates on IsRealClient first); for the looped/one-shot *-name paths a model is still required. |
| 292 | 2.25 | `mapobject-teleporters-portals` | `teleporters.spawn.entities` | logic:partial | teleport_findtarget bot-waypoint spawning (waypoint_spawnforteleporter) is omitted (bot navigation).; noise precache (FOREACH_WORD precache_sound) is a no-op; harmless headless. |
| 293 | 2.25 | `onslaught` | `onslaught.bot.roles` | timing:partial | Residual (logic simplifications, not partial-correctness): no waypoint-near-objective routing (Base navigation_routerating onto g_waypoints within 400 qu of the node + ++bestwp.cnt spread), no havocbot_attack_time (5s) throttle, no havocbot_goalrating_enemyplayers(20000,650) pass, and no havocbot_ons_target teammate-interest cost balancing. The decision FLOW (generator-attack first, skip CPs if a gen is rated, shield+neighbor-filter CPs, free-point ×2) is faithful.; timing partial: Base throttles re-rating via navigation_goalrating_timeout + havocbot_attack_time; the port re-rates every brain tick with no objective-attack cooldown. |
| 294 | 2.25 | `weapon-fireball` | `weapon-fireball.primary.shootable` | timing:partial | Depletion explode is SYNCHRONOUS (fired inside the damage call) rather than Base's deferred next-frame W_PrepareExplosionByDamage (nextthink=time; setthink), and the port does NOT call the MUTATOR_CALLHOOK(PrepareExplosionByDamage) hook. Shared Projectiles.MakeShootable simplification (Devastator/Electro/Hagar/Arc/Minelayer/Mortar/Seeker/OkRpc all share it). Only observable when a server raises g_balance_fireball_primary_health > 0 — at the stock health 0 the whole path is a faithful no-op. NOTE: the owner-reassign-to-attacker IS now ported (ShootDown gates it on g_projectiles_keep_owner==0, faithful to W_PrepareExplosionByDamage:94-98).; find/LaserPlay are still plain radius (warpzone-unaware) — unrelated to this row but the projectile world is non-warpzone like all port finds. |
| 295 | 2.25 | `weapon-shotgun` | `weapon-shotgun.primary.firebullet_falloff` | values:partial | g_ballistics_penetrate_clips (default 1) / weapclip handling still not modeled (no weapclip distinction in the penetration loop). |
| 296 | 2.25 | `weapon-shotgun` | `weapon-shotgun.ammo.checkammo_reload` | values:partial | wr_checkammo1/2 in Base also OR the clip load (weapon_load[m_id] >= ammo) (shotgun.qc:357,370); the port's CheckAmmoPrimary/Secondary DO include the GetWeaponLoad OR-term (Shotgun.cs:366-367,374-375), so the clip-load term is now present. reload_ammo defaults 0 so no clip is used by default.; pickup_ammo (15) still not read in Shotgun.cs (item-pickup concern, not the weapon's checkammo). |
| 297 | 2.0 | `cl-crosshair` | `cl-crosshair.core.master_gating` | logic:partial | Port gates only on Visible + crosshair_enabled/size/alpha/number; Base's full suppress set (scoreboard/camera/intermission2/game-stopped/lockview/spectatee==-1/HEALTH<=0/DrawCrosshair mutator hook/viewloc/minigame menu/CAMERA_SPECTATOR==2/hud!=HUD_NORMAL) is reduced to the NetGame Visible toggle (havePlayer at NetGame:3366). |
| 298 | 2.0 | `cl-crosshair` | `cl-crosshair.anim.pickup_pulse` | liveness:partial | FIXED (host path): UpdatePickupFeed now calls CrosshairPanel.PulsePickup() (NetGame:2864) when a new weapon or a resource jump is detected on the LocalServerPlayer (re-baselined on respawn so the spawn loadout isn't counted). The crosshair bumps on item pickup on the host/listen-server. Still host-only: the detection reads LocalServerPlayer, so a pure remote client (LocalServerPlayer==null) gets no pulse — the networked STAT(LAST_PICKUP) equivalent is not read back into the panel client-side.; Pickup trigger is derived (new-weapon-owned or per-frame resource jump > threshold) rather than the Base STAT(LAST_PICKUP) tick; approximate but matches the visible cue for the host player. |
| 299 | 2.0 | `cl-crosshair` | `cl-crosshair.anim.hit_indication` | liveness:partial | Host-only: HitFlash=1 is set only inside `if (_server is not null)` (NetGame:2532, driven by the host-side damagetext mutator drain). A pure remote client gets no hit-indication bump/flash even though HitDamageDealtTotal is networked (ServerNet.cs) and the client diffs it for the hit SOUND — but not for the crosshair flash.; Coupled to the damagetext mutator events rather than the Base path (UpdateDamage diffs STAT(HITSOUND_DAMAGE_DEALT_TOTAL) on STAT(HIT_TIME) advance); fires on local-player-dealt damage only (ev.Attacker==localPlayer && ev.Target!=localPlayer). |
| 300 | 2.0 | `cl-crosshair` | `cl-crosshair.anim.switch_crossfade` | liveness:partial | Keyed on the resolved Texture2D changing rather than the wcross_name/resolution string pair; with the per-weapon dict unpopulated the trigger only fires on a `crosshair` cvar change, NOT on every weapon switch — so in normal play (one crosshair number for all weapons) the cross-fade effectively never runs. |
| 301 | 2.0 | `cl-crosshair` | `cl-crosshair.ring.vortex_charge` | liveness:partial | FIXED (host path): UpdateCrosshairWeaponRings now feeds CrosshairPanel.ChargeFraction = st.VortexCharge and .ChargePool = st.VortexChargePoolAmmo (NetGame:3153-54) while a vortex/vaporizer/*nex* weapon is active off the host-side slot-0 WeaponSlotState (the C# home of QC wepent.*). The Vortex charge ring + inner chargepool/moving-average ring now render for the host player. VortexCharge is genuinely produced server-side (Vortex.cs/OkNex.cs). Still host-only: read off LocalServerPlayer inside `if (_server is not null)`; a pure remote client (LocalServerPlayer==null) gets the -1 sentinel and no ring.; Minor fidelity: ChargePool is fed unconditionally for vortex-like weapons (default VortexChargePoolAmmo=1f), so the inner chargepool ring shows even when secondary chargepool isn't enabled; Base gates the inner-pool branch on use_vortex_chargepool. Overkill-Nex weapon itself is otherwise absent from this port. |
| 302 | 2.0 | `cl-crosshair` | `cl-crosshair.ring.reload_ammo` | liveness:partial | FIXED (host path): UpdateCrosshairWeaponRings now feeds CrosshairPanel.ClipLoad = st.ClipLoad and .ClipSize = st.ClipSize when the active slot has a clip (NetGame:3165-66). The ammo ring renders for every reloadable weapon on the host. ClipLoad/ClipSize are genuinely maintained on the slot by WeaponFireGate/WeaponFireDriver. Still host-only: read off LocalServerPlayer inside `if (_server is not null)`; remote clients get the -1/0 sentinel and no ring. |
| 303 | 2.0 | `cl-crosshair` | `cl-crosshair.ring.minelayer_hagar_arc` | liveness:partial | FIXED (host path): UpdateCrosshairWeaponRings now feeds CrosshairPanel.HagarLoad (st.HagarLoad, NetGame:3177), .MineCount (live g_mines scan of mines owned by the player, 3189-92), and .ArcHeat (st.BeamHeat/OverheatMax with the overheat-jam latch, 3208-11) when the matching weapon is active. Mine-count, Hagar burst-load, and Arc overheat rings now render for the host player. HagarLoad/BeamHeat/ArcOverheat are genuinely produced server-side (Hagar.cs/Arc.cs). Still host-only: read off LocalServerPlayer inside `if (_server is not null)`; remote clients get the -1 sentinel and no ring. |
| 304 | 2.0 | `cl-crosshair` | `cl-crosshair.ring.switch_fade` | liveness:partial | Now reachable on the host path: with the weapon-stat rings fed (UpdateCrosshairWeaponRings), DrawStatRing produces rings and calls SwitchRingFade (DrawStatRing:943), so the switch fade runs when the host player swaps between ring-bearing weapons. Still dead for a pure remote client (no ring ever draws there). |
| 305 | 2.0 | `cl-csqcmodel` | `cl-csqcmodel.fallbackframe.remap` | liveness:partial | Live only on the MD3 ModelAnimator player path; skeletal-IQM players drive frames via LocomotionBlend, which never produces the high-numbered melee/duckwalk frame ids that the remap targets, so the remap rarely fires in practice. |
| 306 | 2.0 | `cl-scoreboard` | `cl-scoreboard.teams.grouping_totals` | values:partial | Only the primary team slot total is shown (SetWireRows stores wire.Teams primary slot); the secondary teamscore (e.g. keyhunt) is not networked/rendered (the wire's Teams list carries a single (team, score) pair).; team_size_position N/M side display is now PORTED (DrawTeamTotals:934-962, 0/1/2 placement), but totals are still drawn as colored banner boxes rather than the QC bold hud_fontsize*1.5 free-standing side numbers; size string uses 12px text on the box. |
| 307 | 2.0 | `cl-scoreboard` | `cl-scoreboard.block.mapstats` | liveness:partial | Map-stats are read from server-side static state (MonsterAI / MapObjectsState) directly on the client feed path — live for a listen-server host, but neither monsters nor secrets are networked as a STAT, so a remote (non-host) client would not see the rows. |
| 308 | 2.0 | `cl-teamradar` | `cl-teamradar.cvar.defaults` | liveness:partial | Discovered HUD-registry radar is force-hidden (HudManager StartHiddenIds 'radar'), so only the standalone NetGame instance (256x256 at 24,24) is live — the enable/team gating runs against the standalone draw, which is correct, but the discovered panel never participates.; No hud_panel_radar_size cvar analogue (port keys off live control size + the 2500u range fallback instead of the 128px default). |
| 309 | 2.0 | `cl-view` | `cl-view.chase.classic_thirdperson` | logic:partial | The server-side spectator-camera mode toggle is not ported: View_SpectatorCamera's STAT(CAMERA_SPECTATOR) 1/2 state + IMP_weapon_drop cycling (the chase_active<=0 auto-switch that flips chase_active to -2 and Impulse_Sends weapon-drop to cycle the server spectator camera) has no port analogue. The port drives the spectator third-person purely from the user chase_active cvar. |
| 310 | 2.0 | `cl-viewmodel` | `cl-viewmodel.model.skin_colormap_glow_effects` | values:partial | Team colormap is an approximate whole-model albedo tint (0.6 + 0.4*teamColor), not Base's true colormap palette region swap (256 + c); reads as your team color but is not pixel-faithful.; Per-child m_skin (wepskin) not applied; the gun renders with its base material. |
| 311 | 2.0 | `cl-viewmodel` | `cl-viewmodel.muzzle.flash_particle` | values:partial | Local muzzle position is a heuristic at the tag_shot socket rather than Base's view-projected movedir_aligned (not verified to read identically on-screen).; Secondary fire does not flash (only the primary _attackHeld path calls Fire; UpdateLocalFireFeedback comments 'secondary FX stay networked for now').; ClientWorld.OnMuzzleFlash wrapper has no caller (NetGame calls _viewModel.Fire() directly) — dead wrapper, but the feature is live by another path. |
| 312 | 2.0 | `fx-deathtypes` | `fx-deathtypes.message.mapper_custom` | liveness:partial | DEATH_CUSTOM emitter is DEAD: the selector + emit path are now fully wired (DeathTypes.Custom const, Scores.EmitObituary CUSTOM branch with the 'You were %s' formatting matching damage.qc:457), but nothing in the port tags a death with DeathTypes.Custom (grep finds only the consumer at Scores.cs:610) — no mapper trigger_hurt/deathmessage emitter, so the path never fires. |
| 313 | 2.0 | `fx-sounds` | `fx-sounds.emit.play2_family` | liveness:partial | play2 per-recipient targeting is a broadcast approximation: QC play2 sends MSG_ONE (msg_entity=e) so EXACTLY one client hears it; the port's SoundWire has no per-client send, so Play2/Play2Team broadcast to all connected clients — exact only on a single-human listen server.; Vehicle lock/locking play2 sites (sv_vehicles.qc:114-139) remain unwired; a couple of map-object/weapon cues use Api.Sound.Play directly rather than the SoundSystem.Play2 idiom (functionally audible, but not per-recipient and not gated). |
| 314 | 2.0 | `fx-sounds` | `fx-sounds.voice.globalsound_dispatch` | logic:partial | AUTOTAUNT per-recipient probability roll (tauntrand < CS_CVAR(it).cl_autotaunt) still omitted: per-client cl_autotaunt is not networked, so autotaunts that clear sv_autotaunt always play for all recipients (documented deferral; a single global cl_autotaunt default 0 would wrongly silence ALL autotaunts).; TAUNT does not trigger animdecide_setaction(ANIMACTION_TAUNT) on the living speaker (no anim-action seam yet). |
| 315 | 2.0 | `mutator-campcheck` | `campcheck.check.vehicle` | liveness:unknown | Vehicle branch liveness unverified: whether a player is ever actually in a driveable vehicle in the port is unconfirmed, so this branch may never execute in a real match. |
| 316 | 2.0 | `mutator-nades` | `mutator-nades.boom.normal` | presentation:missing | DamageInfo (Damage_DamageInfo) network record omitted (render-only). |
| 317 | 2.0 | `mutator-nades` | `mutator-nades.boom.ice` | presentation:missing | The muzzleflash/icefield particle gate is omitted (render). |
| 318 | 2.0 | `mutator-nades` | `mutator-nades.boom.spawn` | presentation:missing | EF_STARDUST marker render + drawonlytoclient omitted (render). |
| 319 | 2.0 | `mutator-nades` | `mutator-nades.boom.heal` | presentation:missing | EFFECT_HEALING particle omitted (render). |
| 320 | 2.0 | `mutator-nades` | `mutator-nades.boom.ammo` | presentation:missing | Per-weapon weapon_load[] persistent store collapsed onto each slot's live ClipLoad (port note). EFFECT_AMMO_REGEN particle omitted (render). |
| 321 | 2.0 | `mutator-sandbox` | `mutator-sandbox.object.spawn` | presentation:missing | WIRED + LIVE: `g_sandbox` client-callable (ClientCommandRegistry:105) → CmdSandbox → HandleCommand; NetGame.WireSandbox assigns Trace (SandboxTraceProvider — real crosshair forward TraceService), so object_spawn lands at the trace endpoint with a real edict (Api.Entities.Spawn + SetModel + SetOrigin).; ObjectSpawn now DOES register the engine touch callback (e.Edict.Touch = ObjectTouch, SandboxMutator.cs:204) — the port equivalent of Base settouch (sv_sandbox.qc:180); see touch_material_fx row (now live). Think (setthink, sv_sandbox.qc:181) is driven externally by OnStartFrame over all objects rather than the per-edict engine think, which is faithful enough.; ModelExists() is permissive (returns true; no IModelService.fexists), so the Base non-existent-model rejection is not enforced.; presentation: CSQCMODEL_AUTOINIT (object→client networking) has no port seam (TODO SandboxMutator.cs:232) — client-side render of a spawned object is unverified (the object carries a real edict+model, so the generic entity feed MIGHT show it, but this was not confirmed in-game). |
| 322 | 2.0 | `mutator-sandbox` | `mutator-sandbox.object.think_grab` | presentation:missing | WIRED + LIVE: OnStartFrame is subscribed to MutatorHooks.SvStartFrame (fired in the server tick) and ticks ObjectThink for every object over the live RealClientsProvider roster (NetGame.WireSandbox — non-bot players); CryptoIdfpProvider(=PersistentId) resolves realowner from UID and clears it on disconnect.; Sandbox_DragAllowed veto hook ported and fired (MutatorHooks.FireSandboxDragAllowed, SandboxMutator.cs:346) — the grab-class branch is bit-faithful to Base (readonly OR DragAllowed→0). No subscriber exists, identical to Base's empty MUTATOR_CALLHOOK.; presentation: CSQCMODEL_AUTOUPDATE has no port seam (TODO SandboxMutator.cs:364); .Grab is computed but never consumed (no drag system). |
| 323 | 2.0 | `mutator-sandbox` | `mutator-sandbox.object.edit_properties` | presentation:missing | WIRED + LIVE: object_edit reachable via CmdSandbox → HandleCommand on the live path.; physics-2 maps to MoveType.Physics (EntityEnums.cs:21, value 32) at SandboxMutator.cs:811, matching Base MOVETYPE_PHYSICS=32 (dpextensions.qc:1707) — the serialized movetype byte is Base-identical. (Behaviorally still think-only since the port has no ODE integrator, same as Base without ODE.); The faithful Base solidity→physics fall-through (missing break;) IS preserved (re-verified Base sv_sandbox.qc:685-697 has no break on solidity; port reproduces via goto case "physics" at SandboxMutator.cs:797). |
| 324 | 2.0 | `mutator-sandbox` | `mutator-sandbox.object.scale` | presentation:missing | WIRED + LIVE: reached from object_edit (live via CmdSandbox) and ObjectPortLoad (live via Store load/paste).; Model re-set + mins/maxs resize depend on the entity service (Edict); now that objects carry a real edict the SetModel/SetSize path executes. |
| 325 | 2.0 | `mutator-sandbox` | `mutator-sandbox.object.attach` | presentation:missing | WIRED + LIVE: object_attach get/set/remove reachable via CmdSandbox → HandleCommand.; presentation: setattachment (engine tag-follow) has no port seam (TODO SandboxMutator.cs:291) — attached children store the tag and old_solid/old_movetype faithfully but are not actually tag-followed. |
| 326 | 2.0 | `mutator-sandbox` | `mutator-sandbox.object.claim_info` | presentation:missing | WIRED + LIVE: claim/info reachable via CmdSandbox → HandleCommand; CryptoIdfpProvider(=PersistentId)/NetNameProvider/PrintTo(=SendChatToPlayer) assigned in NetGame.WireSandbox, so claim/owner-name/info-print all work.; object_info mesh: tag enumeration (FOR_EACH_TAG) needs the model-skeleton service — not implemented (TODO SandboxMutator.cs:876); mesh subcommand prints the header but no tag list. |
| 327 | 2.0 | `mutator-walljump` | `mutator-walljump.fx.anim_and_oldvel` | presentation:missing | animdecide_setaction(player, ANIMACTION_JUMP, true) not triggered on a wall jump (no anim-action seam in the port; WalljumpMutator.cs:128 is a comment only). UNCHANGED — same missing server->client one-shot action channel noted on Dodging. Purely cosmetic (jump pose). |
| 328 | 2.0 | `mutator-walljump` | `mutator-walljump.menu.describe` | presentation:missing | Base METHOD(MutatorWallJump, describe) supplies the localized blurb shown for the walljump mutator in the MENUQC Mutators dialog ('...enables jumping off walls for added mobility. To do a wall jump, tap jump when against a wall.'). No equivalent per-mutator describe text is wired in the port's mutator-selection menu. Cosmetic menu-only text; newly mapped this audit (not in the prior baseline). |
| 329 | 1.92 | `cl-hud` | `cl-hud.panel.modicons` | values:unknown | NexBall modicon (HUD_Mod_NexBall) is not ported (the port has no NexBall gametype yet).; Per-gametype icon-art fidelity (dom point-ownership icons) is approximate; not all runtime-verified. |
| 330 | 1.8 | `mapobject-teleporters-portals` | `teleporters.trigger.team_claim` | liveness:unknown | SendFlags networking (SF_TRIGGER_UPDATE) still not replicated (client-side only; harmless headless). |
| 331 | 1.8 | `sv-chat` | `sv-chat.flood.per_type_throttle` | values:partial | Line-wrap width is an integer 82 visible chars vs Base's 82.4289758859709 px-equiv (perl averagewidth on gfx/vera-sans.width); proportional per-char widths are not modeled, so long-message line-count / flood==2 'too long' boundary can differ by one char on edge-length messages. |
| 332 | 1.8 | `sv-client-lifecycle` | `sv-client-lifecycle.frame.idle_kick` | values:partial | DRIFT 2026-06-27 (values): the port's idle cvar DEFAULTS diverge from Base. Base sources sv_maxidle_playertospectator=60 (server/client.qh:39) and sv_maxidle_minplayers=2 (qh:38); the port ships both at 0 (Cvars.cs:349-350) and reads them live (GameWorld.cs:1853-1856). Out of the box the port NEVER does move-to-spec (Base does after 60s) and arms the idle kick with 0 minplayers (Base needs >=2 humans). The machinery is correct — only the numeric defaults are wrong.; The dedicated-server sv_maxidle_slots free-slot threshold (maxclients - totalClients > sv_maxidle_slots disables the kick) is NOT enforced — the headless core has no engine-level maxclients to read, so the slots gate is a no-op (the cvars are read but skipped). Off by default; only affects a dedicated operator who sets sv_maxidle_slots. |
| 333 | 1.8 | `sv-client-lifecycle` | `sv-client-lifecycle.frame.spectator_kick` | values:partial | DRIFT 2026-06-27 (values): g_maxplayers_spectator_blocktime ships 5 in the port (Cvars.cs:355) but Base ships 10 (xonotic-server.cfg:33), so the sv_spectate=0 spectator-kick grace is HALF the Base duration out of the box. Read live at GameWorld.cs:1856.; The port resets the grace clock via the spectator's JoinTime (QC spectatortime) rather than a dedicated spectatortime field; behaviorally equivalent (spectatortime is set to jointime on PutObserverInServer in QC). The dropclient_schedule deferred-drop is realised as a direct ClientDisconnect after the grace, which is functionally the same outcome. |
| 334 | 1.8 | `sv-teamplay` | `sv-teamplay.join.skill_weighting` | values:partial | No TrueSkill ratings for humans: SkillProvider returns the bot's real BotSkill (0..10) for bots but a flat 5 for every human (IsRated treats only bots as rated; humans fall through the unranked server-average path). So skill weighting biases bot-involved ties but human-vs-human equal-size ties still reduce to a plain count. This is a VALUES/data gap, not a logic gap — the algorithm now matches Base.; No warmup skill-aware join branch (Base TeamBalance_FindBestTeams warmup+sv_teamnagger+skill block picking the team whose weighted-mean skill is furthest from the joiner). The port always uses the size->strength ladder even in warmup.; The weighted path is only reached from AssignBestTeam/IsTeamAllowedForSwitch (connect/moveplayer/team/selectteam); the per-gametype TeamBalance.JoinSmallestTeam callers (CTF/CA/FreezeTag/Dom/KeyHunt/TDM/TeamMayhem) ignore skill entirely. |
| 335 | 1.8 | `turret-machinegun` | `turret-machinegun.lifecycle.die_respawn` | presentation:missing | No client death FX: the non-NORESPAWN branch in Base turret_die does NO sound/effect (only the NORESPAWN branch plays SND_ROCKET_IMPACT + EFFECT_ROCKET_EXPLODE before delete — the port does port that NORESPAWN branch at Die:927-932); base/head gib toss lives in cl_turrets.qc (client). Port emits no client FX/gibs on the normal respawn path. Still open.; Respawn restores state but does not re-stamp a head model / send a full CSQC update (TNSF_FULL_UPDATE) — no networking layer. Still open. |
| 336 | 1.8 | `turret-plasma_dual` | `turret-plasma_dual.damage.gating_die_respawn` | presentation:missing | Respawn EF_NODRAW hide-frame split not modeled: Base hides (EF_NODRAW) at time+0.2 then turret_respawn at respawntime-0.2; port schedules Respawn directly at now+RespawnTime. NET respawn timing is identical (0.2 + (60-0.2) = 60 == now+60) — only the intermediate EF_NODRAW hide-frame (a presentation detail) is unmodeled.; Death FX/sound (SND_ROCKET_IMPACT + EFFECT_ROCKET_EXPLODE) only fire in Base on TFL_DMG_DEATH_NORESPAWN; plasma_dual always respawns so this branch never applies (port has it coded in Die but unreachable for this unit). tr_death is a no-op for plasma_dual. |
| 337 | 1.8 | `vehicle-framework` | `vehicle-framework.init.initialize` | values:partial | delayspawn + random jitter scheduling not ported (vehicles spawn at game_starttime only). STILL OPEN (sv_vehicles.qc:1278-1281). Keeps values/timing partial.; targetname controller / ACTIVE_NOT gating / vehicle_use not ported (map-scripted activation absent, sv_vehicles.qc:1178-1201/1276-1277). STILL OPEN. The targetname branch that ASSIGNS .team is unported, but the team-CLEAR branch (below) is independent of it. |
| 338 | 1.8 | `warpzones` | `warpzones.porto` | presentation:missing | Porto portals share the missing client portal render — they teleport correctly server-side but show as dark placeholders, same as map warpzones.; The Amazing-on-portal-telefrag announcer (portals.qc:192-194, ANNCE_ACHIEVEMENT_AMAZING within 1s of portal creation) needs the tdeath path the warpzone crossing does not run — see the portals.teleport unresolved gap noted inline (Warpzone.cs:262).; The +/-48 PortoMins/Maxs trigger box is a port choice; Base Porto portal bounds not re-verified. |
| 339 | 1.8 | `warpzones` | `warpzones.client.prediction` | presentation:missing | Render/view side of the crossing still missing: a predicted warp relocates the carrier in lockstep with authority (no rubber-band) and stamps .fixangle, but the seamless view-rotate/portal render (warpzones.client.render / .fixview / .teleported_view) is unported — the camera still snaps visually across the seam even though the origin no longer corrects. |
| 340 | 1.75 | `bot-ai` | `bot-ai.target.chooseenemy` | logic:partial | No second see-through-transparent (DPCONTENTS_OPAQUE) scan pass and no misc_breakablemodel secondary targeting; bots won't shoot breakables or acquire targets through transparent surfaces. (UNCHANGED.) |
| 341 | 1.75 | `bot-ai` | `bot-ai.weapon.wr_aim` | logic:partial | Only the ChosenWeapon's wr_aim is consulted — there is no MAX_WEAPONSLOTS per-slot loop (Base runs wr_aim for every owned weapon slot each think). (UNCHANGED.); wr_aim is ported on 14 of Base's 18 wr_aim weapons; still missing: vortex (charge), seeker, hlac, blaster. (NARROWED 2026-06-27 second pass from 12/18; mortar grenademooth toggle + hagar 15%-secondary burst are now ported.); lastfiredweapon combo bookkeeping is not modeled; combos that depend on the previously-fired weapon don't trigger. |
| 342 | 1.75 | `bot-ai` | `bot-ai.move.steer` | logic:partial | QC's full movetogoal handling of teleport/ladder/box-volume waypoints, water swim-up, and the per-goal stop/turn logic is partially collapsed; complex waypoint traversal is less robust than stock. (UNCHANGED.); BotTracewalk (reachability test, separate from the brain danger check) still uses a hardcoded JumpStepHeight const = 48 rather than the live derivation; non-default physics doesn't adjust that one path. (Minor.) |
| 343 | 1.75 | `bot-ai` | `bot-ai.move.danger` | logic:partial | Danger evade is still brake-only: the QC lateral evadedanger vector (steer along the safe edge, distance scaled by skill) is collapsed into a reverse-velocity brake, so bots back straight off a ledge instead of sidestepping along it. (UNCHANGED.); Per-waypoint danger-cost ROUTING is inert: a Waypoint.Danger field exists and A* adds nb.Danger, but no botframe_updatedangerousobjects producer ever SETS Danger (always 0), so routing still does not avoid chronically dangerous waypoints — the consumer is dead. (UNCHANGED.) |
| 344 | 1.75 | `bot-waypoints` | `bot-waypoints.graph.autolink` | liveness:partial | The it.SUPPORT_WP incoming-direction forbid (waypoints.qc:1213/1227) is not ported — the port has no SUPPORT_WP back-pointer field, so a SUPPORT waypoint forbids only its OWN outgoing/incoming links, not links INTO an arbitrary node it supports. Documented at Waypoint.cs:335. Edge case (SUPPORT waypoints are rare); affects only those specific pairs. |
| 345 | 1.75 | `bot-waypoints` | `bot-waypoints.auto.spawnforitem` | liveness:partial | Only runs in the no-.waypoints-file auto-generate path; on maps that ship a .waypoints file the port does NOT inject runtime item/teleporter waypoints (Base always does waypoint_spawnforitem at item spawn, snapping to the item). This is the liveness-partial: the fileless auto-generate fallback is a port-only playability helper, not a Base feature, but it cannot replace Base's always-on runtime injection on shipped maps. |
| 346 | 1.75 | `bot-waypoints` | `bot-waypoints.path.findnearest` | logic:partial | No jumppad-fallback: Base's navigation_findnearestwaypoint tail (navigation.qc:1000, IL_EACH(g_jumppads)) returns a reachable jumppad's nearest waypoint when no plain waypoint is reachable; the port has no such fallback. |
| 347 | 1.75 | `bot-waypoints` | `bot-waypoints.path.routetogoal` | logic:partial | Teleport-goal forcing (push wp00 destination, set goalentity), dynamic/moving-target shortcut (bot_ai_strategyinterval_movingtarget 5.5) and the item incoming-link walkability cache are simplified/absent; the port pushes the A* node chain + goal directly. The path-optimization shortcut (drop the nearest node when the second is a shorter straight shot, BotNavigation.cs:241-250) IS ported. |
| 348 | 1.75 | `bot-waypoints` | `bot-waypoints.steer.movetogoal` | logic:partial | Obstacle avoidance is a single forward step/jump-height tracebox probe rather than Base's steerlib trident/funnel sweep (see bot-waypoints.steer.steerlib); lateral evade is folded into a straight -velocity brake (do_break), a documented simplification of Base's AI_STATUS_DANGER_AHEAD sidestep. |
| 349 | 1.75 | `bot-waypoints` | `bot-waypoints.steer.bunnyhop` | logic:partial | Base forbids bunnyhop when evadedanger \|\| do_break is set this frame (havocbot.qc:1315). The port reports bunnyhop intent via a SEPARATE WantBunnyhop flag (BotNavigation.cs:112/378) gated by the Steer-internal ledge brake, then the BotBrain ANDs it with 'no danger brake this frame' before pressing jump — so the brain owns the final danger-suppression. The two-stage gate is close to Base but not a single in-place check. |
| 350 | 1.65 | `net-entity-state` | `net-entity-state.replicate.client_cvars` | values:partial | ONE replicated cvar is stored server-side but not consumed off the replicated value: cl_autoscreenshot is in SentCvarAllowlist (Commands.cs:233) so it is accepted+stored per-client, but nothing on the server reads the REPLICATED value — autoscreenshot is driven CLIENT-locally off the networked intermission flag reading the client's own Api.Cvars cl_autoscreenshot (NetGame.cs), not the per-client replicated store. FIX 2026-06-27: g_xonoticversion left this list — it now has a live server consumer (Commands.CheckClientVersion reads the per-client replicated g_xonoticversion to send the version-mismatch notification, armed at ClientManager.cs:229, fired on the live PlayerPreThink path GameWorld.cs:2165). FIX 2026-06-25: cl_clippedspectating left this list — live free-fly MOVETYPE consumer (ClientManager.ClippedSpectatingProvider). |
| 351 | 1.5 | `assault` | `assault.objective.activate` | presentation:partial | ActivateObjective also spawns the objective's waypoint sprite (SpawnObjectiveSprite → WP_AssaultDestroy + health bar) on the server, matching QC's WaypointSprite_SpawnFixed. But there is no client render/networking path, so the sprite is not visible on-screen (see assault.objective.waypoint_sprite). Logic/state is faithful; the visible marker is still absent. |
| 352 | 1.5 | `assault` | `assault.objective.destructible_heal` | presentation:partial | Presentation: the heal updates the wall's waypoint-sprite health bar server-side (WaypointSprites.UpdateHealth) but, like all the Assault sprites, that bar is not surfaced to the client (no waypoint render/network path). QC's func_breakable_colormod (the visible color tint on a healed wall) is not modeled. The heal LOGIC + health restoration is faithful + live. |
| 353 | 1.5 | `ctf` | `ctf.flag.spawn_setup` | presentation:partial | Base flag is static at the spawn origin (no DropToFloor settle on non-fixed spawns); dropped flags now DO settle (MoveType.Toss integrator, see ctf.flag.drop_on_death).; No glow trail (g_ctf_flag_glowtrails), no dynamic team light (g_ctf_dynamiclights); EF_LOWPRECISION is set, EF_FULLBRIGHT only when g_ctf_fullbrightflags (matches Base default off). |
| 354 | 1.5 | `ctf` | `ctf.flag.capture` | presentation:partial | Only the PERSISTENT capture-time record (ctf_CaptureRecord ServerProgsDB write + cross-session g_ctf_leaderboard) remains unported (needs a persistent DB). The in-match fastest-cap broadcast (CTF_CAPTURE_TIME/_BROKEN/_UNBROKEN, picked via MSG_CHOICE) IS now fired by CaptureRecord.; Per-flag score_capture override + cnt-group guard not modeled (own flag home requirement IS faithful). |
| 355 | 1.5 | `ctf` | `ctf.flag.fckill_score` | presentation:partial | Auto-helpme is modeled as a carrier-sprite helpme FLASH (GtHelpMeUntil networked via ServerNet, consumed in CollectWaypoints), not the full Base WaypointSprite_HelpMePing sprite; effective-HP test approximates healtharmor_maxdamage with health+armor. |
| 356 | 1.5 | `cts` | `cts.hud.race_timer` | presentation:partial | CTS has no intermediate checkpoints, so the panel's per-checkpoint split / anticipation delta / next-best lines are intentionally inert for CTS (RaceCheckpoint=254, no split feed) — this matches a single start->stop course but means the panel shows only the running clock + medal flash, not the full Race split UI. |
| 357 | 1.5 | `cts` | `cts.hud.cl_panel_gating` | presentation:partial | HUD_Score_show (hide the score panel while observing in CTS) and DrawScoreboardItemStats / DrawDeathScoreboard hiding are not specifically modeled per-CTS; the 'Rankings' scoreboard header is a hard-coded literal, not a CTS-conditional relabel.; Scoreboard accuracy is still not networked, so the CTS-gated accuracy block draws nothing (low impact: CTS has no accuracy display by intent). |
| 358 | 1.5 | `freezetag` | `freezetag.unfreeze` | presentation:partial | Does not delete an ice entity (not spawned in the first place); the waypoint sprite IS cleared (it stops being collected once IsFrozen flips false). |
| 359 | 1.5 | `freezetag` | `freezetag.presentation.overlay_eventchase` | presentation:partial | Overlay + crosshair ring + eventchase cam are all driven on the host/listen-server path (LocalServerPlayer / its Frozen status effect); a pure remote client with no LocalServerPlayer shows none of them (the cross-client wepent/stat follow-up). |
| 360 | 1.5 | `keepaway` | `keepaway.ball.spawn` | presentation:partial | SpawnBall is CALLED on the live match path — GameWorld.Boot (the NetGame match-start path) procedurally spawns the single ball at a RandomMapLocation right after the entity lump, replacing the missing QC ka_Handler_CheckBall driver.; PRESENTATION residual: the EF_DIMLIGHT bit + glow_color/glow_trail are set as data on the edict, but the actual ball MODEL render + glow trail are client concerns not yet realized (model path models/orbs/orbblue.md3 is configured but the CSQC render of it is deferred). |
| 361 | 1.5 | `monster-framework` | `monster-framework.presentation.team_colors` | liveness:partial | monster_setupcolors is PORTED + LIVE — SetupColors (MonsterAI.cs:385) picks .colormap by team (1024+(team-1)*17) / owner / skill tier (1126/1075/1228/1092/1160) bit-faithful to QC, and is called from SpawnFromMap (MonsterAI.cs:563) for every natural (!RESPAWNED) monster (gated exactly as QC at sv_monsters.qc:1525). It writes Entity.ColorMapOverride with RENDER_COLORMAPPED set; the csqcmodel derives the glowmod from the colormap nibble client-side, so the QC .glowmod write is covered by the established colormap render seam (presentation: the actual client tint depends on that csqcmodel path being wired — verified for the same field g_model_setcolormaptoactivator uses).; DRIFT FIX (2026-06-27): monster_changeteam is PORTED (ChangeTeam, MonsterAI.cs:416): re-teams + re-tints (SetupColors) in teamplay, AND the team radar-icon update is now LIVE — ChangeTeam (:430) calls WaypointSprites.UpdateTeamRadar on st.Sprite + sets sprite.Team, exactly as QC monster_changeteam:211-216, because the WP_Monster sprite now exists (healthbar fix). RESIDUAL: ChangeTeam still has NO live external caller today (no dispatch site outside the monster module), so the teamplay monster re-team itself is dead-wired (liveness: partial). When healthbars are off the sprite is null and the radar update is a harmless no-op (matching QC's `if (this.sprite)` guard). |
| 362 | 1.5 | `monster-golem` | `monster-golem.anim_frames` | timing:partial | AnimFrame stamps only the group START index onto Entity.Frame (server-side, per think via DriveAnimFrame). The animfixfps framecount/fps per-frame playback advance WITHIN a group is not driven — there is no true .dpm frame timeline; the client renders a single stamped frame per phase/group (timing/presentation partial). |
| 363 | 1.5 | `mutator-nades` | `mutator-nades.toss.projectile` | values:partial | Newton-style 0 is implemented as pure absolute _velocity rather than W_CalculateProjectileVelocity(e, e.velocity, _velocity, true); for non-default g_projectiles_newton_style this diverges from Base's velocity blending (style 0 is the shipped default, so identical at defaults). |
| 364 | 1.5 | `nexball` | `nexball.ball.spawn` | presentation:partial | Scale 1.3 + glow_trail rendering are still client-deferred (model path + trail color are configured; SpawnForGametype never sets e.Scale; bbox/effects/playerclip/relocate are exact). |
| 365 | 1.5 | `onslaught` | `onslaught.win.check_winner` | presentation:partial | BankRoundWin sends ROUND_TEAM_WIN_<team> center+info to all + play2all CTF_CAPTURE_<team> win jingle (winner>0), ROUND_TIED (winner==-1). Remaining: scoreboard column rendering + winner-banner HUD is a client concern (tracked under onslaught.presentation.csqc). |
| 366 | 1.5 | `onslaught` | `onslaught.captureshield` | presentation:partial | Server-side faithful + live: SpawnGenerator/SpawnControlPoint spawn a co-located SOLID_TRIGGER/MOVETYPE_NOCLIP ons_captureshield with the back-link (GtShieldFlag), EF_ADDITIVE, avelocity '7 0 11', hitbox 1.20x. ShieldTouchEntity reproduces ons_CaptureShield_Touch: pass-through for same-team or attackable/un-shielded node, else Combat.Damage(0, force=normalize(dir)*ShieldForce) push + play2 ONS_DAMAGEBLOCKEDBYSHIELD + GENERATOR/CONTROLPOINT_SHIELDED center.; presentation partial (client-deferred): the shield MODEL + EF_ADDITIVE additive blend + avelocity SPIN are assigned on the entity, but actually RENDERING the spinning additive shield is a CSQC/client concern not done here. ons_CaptureShield_Customize (per-viewer visibility) + ons_CaptureShield_Reset (colormap on map reset) not ported. |
| 367 | 1.5 | `onslaught` | `onslaught.audio.cues` | presentation:partial | Core cues EMITTED live: SpawnIcon→ONS_CONTROLPOINT_BUILD; build completion→ONS_CONTROLPOINT_BUILT + ONSLAUGHT_CAPTURE_NONAME + ONS_CAPTURE(_TEAM)_NONAME; IconDamage→ONS_DAMAGEBLOCKEDBYSHIELD / ONS_HIT1/2 / ONS_GENERATOR_EXPLODE + ONSLAUGHT_CPDESTROYED_NONAME; GeneratorDamage→blocked-by-shield / GENERATOR_UNDERATTACK + ONS_GENERATOR_UNDERATTACK / ONS_HIT1-2 (or explode at chance damage/220) / ONSLAUGHT_GENDESTROYED(_OVERTIME) + explode; GeneratorThink→ONS_NOTSHIELDED_TEAM/_<team> + ONS_GENERATOR_ALARM (every 5s while un-shielded); OvertimeDecayTick first tick→OVERTIME_CONTROLPOINT + ONS_GENERATOR_DECAY; BankRoundWin→ROUND_TEAM_WIN / ROUND_TIED + play2all CTF_CAPTURE jingle. Sounds (7) + notifications (40) registered AND now all emitting.; Newly EMITTED this pass: ShieldTouchEntity→play2 ONS_DAMAGEBLOCKEDBYSHIELD + GENERATOR/CONTROLPOINT_SHIELDED center on a shield bump; IconThink damaged-icon spark crackle ONS_SPARK1/2.; Remaining (presentation/cross-boundary): particle effects, waypoint-sprite build/health bars, the CP map .message name (port emits NONAME variants), and play2team→global emission (NotifBroadcast.All + SoundSystem.PlayGlobal) as the headless approximation (no per-team routing). |
| 368 | 1.5 | `onslaught` | `onslaught.gen.unshielded_alarm` | presentation:partial | Presentation: the alarm is server-authoritative (center prints via NotificationSystem.Send + siren via SoundSystem); no client-side HUD/visual of the un-shielded state beyond the center-print. The siren is emitted once globally (SoundSystem.PlayGlobal) as the headless approximation of QC's per-player ATTEN_NONE soundto — no per-listener spatial routing. |
| 369 | 1.5 | `overkill-weapons` | `overkill-weapons.oknex.primary_rail` | presentation:partial | FIXED (Wave-8b): wr_impacteffect now wired — Attack:150-158 traces the surface, emits EFFECT_VORTEX_IMPACT puff + SND_OK_NEX_IMPACT (neximpact) at the hit point (silent if sky/miss), plus a VORTEX_MUZZLEFLASH approximation. Closes the baseline's 'no client rail-impact particle / impact sound' gap.; STILL OPEN: no CSQC beam particle (SendCSQCVortexBeamParticle) — the actual rail beam between muzzle and impact is not networked (the port emits a muzzleflash + impact puff but not the beam itself). Yoda / Impressive achievement announcements never fire. wr_glow charge glow not present (covered by oknex.charge, default-off). |
| 370 | 1.5 | `overkill-weapons` | `overkill-weapons.okrpc.primary_chainsaw_missile` | presentation:partial | FIXED (Wave-8b): W_MuzzleFlash (Attack:171 EFFECT_ROCKET_MUZZLEFLASH — matches okrpc.qh m_muzzleeffect) and wr_impacteffect (Explode:230-231 EFFECT_ROCKET_EXPLODE puff + SND_ROCKET_IMPACT at the blast point) are now wired LIVE — closing the baseline 'no wr_impacteffect / no W_MuzzleFlash' gap.; Shoot-down LIVE+faithful (unchanged): OkRpc.Attack:166 Projectiles.MakeShootable(missile, exception:-1) installs the GtEventDamage shim DamageSystem dispatches; ShootDown runs W_CheckProjectileDamage(-1) + RES_HEALTH subtract → Explode at hp<=0. Correctly INERT under stock g_projectiles_damage=-2, exactly like Base.; STILL OPEN: missile not networked as a CSQC projectile (no visible flying chainsaw on clients) — CSQCProjectile(PROJECTILE_RPC) omitted.; STILL OPEN: missile not registered for bot dodge (QC bot_dodge=true, bot_dodgerating=damage*2, IL_PUSH g_bot_dodge).; STILL OPEN: missile does NOT set damagedbycontents / IL_PUSH g_damagedbycontents, so lava/slime won't detonate the in-flight chainsaw even at g_projectiles_damage>=0 (QC does).; Accuracy bookkeeping (first-hit fired/hit add, explode 'add fired back') omitted (stat-only).; Pass-through victim filter is `(Flags & Client) && TakeDamage!=No` vs QC IS_PLAYER(trace_ent): could catch a spectator client edict carrying the Client flag. Minor; corpses filtered by TakeDamage. |
| 371 | 1.5 | `tka` | `tka.ball.spawn` | presentation:partial | Single ball only: SpawnBall makes one ball; g_tkaball_count multi-ball chaining (toucher.ballcarried.cnt vs g_tka_ballcarrier_maxballs) not modeled. Default count is 1, so values match the stock server.; Model/EF_DIMLIGHT/trail_color/damageforcescale are set on the sim edict, but the actual glow_trail particle trail + EF_DIMLIGHT light are client/render concerns — not yet visible.; Spawn is procedural at map-load (GameWorld.cs:718 switch on GameType; TKA case), replacing QC tka_Handler_CheckBall's per-frame INITPRIO_GAMETYPE think. There is no game_starttime gate / remove-and-respawn-during-warmup loop, but the net effect (one ball once the match is live) matches. |
| 372 | 1.5 | `tka` | `tka.waypoints.tracking` | presentation:partial | FIXED (missing -> faithful logic/values + live): CollectWaypoints (TeamKeepaway.cs:713) produces a TEAM-COLORED carrier sprite (TkaBallCarrier<team> + per-team radar tint, offset '0 0 64', RADARICON_FLAGCARRIER, Rule=SPRITERULE_TEAMPLAY) while held, and the neutral KaBall sprite on the loose ball. Honors g_tkaball_tracking (0 = no waypoint). Pulled LIVE by ServerNet.SendWaypoints (ServerNet.cs:927 _world.GameType.CollectWaypoints), filtered per peer, networked, rendered by WaypointSpriteLayer + RadarPanel.; FIXED (values partial -> faithful): g_tkaball_tracking's THREE Base modes (0 none / 1 always / 2 only-when-dropped) are now all distinguished — CarrierWaypointVisibleFor returns BallTracking==1 for the enemy case, so ==2 hides the carrier sprite from enemies (matching the QC `return autocvar_g_tkaball_tracking == 1`).; FIXED (presentation): the carrier sprite is now networked with SPRITERULE_TEAMPLAY + a per-viewer VisibleForPlayer predicate (CarrierWaypointVisibleFor), honored per-peer at ServerNet.cs:996. It models same-team-always, spectator-always, warmup-always, and enemy-tracking==1 faithfully.; RESIDUAL (presentation:partial): only the two narrowest QC nuances of tka_ballcarrier_waypointsprite_visible_for_player remain approximated — hide-from-spectators-of-the-carrier (`view.ballcarried && IS_SPEC(player)`) and hide-on-IS_INVISIBLE(owner). The predicate has no spectatee/invisibility field, so these resolve as visible. |
| 373 | 1.5 | `turret-framework` | `turret-framework.registry.descriptor` | logic:partial | Per-unit cvar table unported (load_unit_settings / TR_PROPS_COMMON / turret_scale_* / turret_initparams clamping / dumpturrets) — per-unit balance is hardcoded C# const, mappers can't retune. (Framework-level g_turrets_* delays/nofire ARE cvar-backed; per-unit fields are not.) |
| 374 | 1.5 | `weapon-arc` | `weapon-arc.identity.attributes` | presentation:partial | EFFECT_ARC_MUZZLEFLASH is now emitted on the bolt secondary (Arc.cs:489 ARC_MUZZLEFLASH -> electro_muzzleflash), but it is not carried as the weapon's m_muzzleeffect/m_muzzlemodel attribute and there is no spinning flash.md3 muzzle entity on the beam (that lives in Draw_ArcBeam — tracked under weapon-arc.beam.visual). crosshair gfx/crosshairhlac size 0.7 is a CSQC attribute not carried headless. Presentation stays partial. |
| 375 | 1.5 | `weapon-blaster` | `weapon-blaster.identity.registration` | presentation:partial | model/anim/crosshair fidelity (view/world/item models, gfx/crosshairlaser, weaponlaser image) not audited here — names declared only. |
| 376 | 1.5 | `weapon-fireball` | `weapon-fireball.primary.launch` | presentation:partial | No projectile flight sound (fireball_fly) / FIREBALL trail attached to the live plasma_prim entity (deferred CSQC render concern, same as other weapons). |
| 377 | 1.5 | `weapon-fireball` | `weapon-fireball.primary.explode` | presentation:partial | modeleffect_spawn(sphere.md3) BFG visual ball not produced (CSQC render layer); FIREBALL_EXPLODE + impact sound at ATTEN_NORM*0.25 are emitted. |
| 378 | 1.5 | `weapon-fireball` | `weapon-fireball.primary.bfg` | presentation:partial | IS_INDEPENDENT_PLAYER(e) is checked but Entity.IsIndependentPlayer is a hardcoded stub (always false) — faithful for all non-independent (default) modes; the filter is a no-op until that mode is wired.; EFFECT_FIREBALL_BFGDAMAGE per-victim Emit is now wired, but its render fidelity (and the sphere.md3 ball) is the deferred CSQC layer. |
| 379 | 1.5 | `weapon-fireball` | `weapon-fireball.primary.laserscorch` | presentation:partial | find is a plain radius (FindInRadius), not WarpZone_FindRadius (no warpzone-spanning scorch); same omission as all radius finds in the port.; EFFECT_FIREBALL_LASER scorch beam is Emit-wired (render fidelity deferred). |
| 380 | 1.5 | `weapon-fireball` | `weapon-fireball.secondary.firemine` | presentation:partial | Port omits the bulletcounter%4 muzzle-corner f_diff offset + traceline nudge (cosmetic spawn-point spread of consecutive mines); all mines spawn from the same muzzle point. Unchanged from baseline.; No firemine flight sound / FIREMINE trail attached (deferred CSQC render). |
| 381 | 1.5 | `weapon-hlac` | `weapon-hlac.fx.muzzle_impact` | presentation:partial | Runtime green->blaster effect fallback (v0.8.6 compat, hlac.qc:225-228) not ported; cosmetic, only matters on a build missing the green HLAC effects (both are registered in the port). Unchanged by Wave-2. |
| 382 | 1.5 | `weapon-hook` | `weapon-hook.primary.latch` | presentation:partial | Latched hook becomes INVISIBLE: GrapplingHook_Stop sets MoveType.None (Hook.cs:340), which is NOT a projectile movetype, so ServerNet.Classify (IsProjectileMoveType, ServerNet.cs:2345/2365) no longer classifies it as Projectile; with no model it falls to Generic and is dropped from the render. The rendered spike disappears on latch. Base keeps the latched hook drawn via CSQC. |
| 383 | 1.5 | `weapon-rifle` | `rifle.fx.impact` | presentation:partial | Emit PATH still diverges from Base: the port emits RIFLE_IMPACT server-side per bullet inside FireBullets off a world-only re-trace; Base emits it CSQC in wr_impacteffect off the Damage_DamageInfo feed. The effect NAME + the org2/backoff offsets now match QC, but the per-bullet server emit is a different fan-out than the CSQC damageinfo path — remote-client render not visually verified. |
| 384 | 1.5 | `weapon-tuba` | `weapon-tuba.reload.instrument_cycle` | presentation:partial | weaponname (akordeon/kleinbottle) is not set — in Base it feeds only the CSQC wr_viewmodel (per-instrument view model h_akordeon/h_kleinbottle). The instrument index (TubaInstrument) IS tracked, so the obituary/sound branch correctly; only the per-instrument VIEW MODEL swap is unwired (render-only gap). |
| 385 | 1.5 | `weapon-tuba` | `weapon-tuba.fx.smoke_ring` | presentation:partial | Ring origin uses the player eye (Origin + ViewOfs) rather than the weapon muzzle tag (gettaginfo), so the puff spawns slightly off from Base — but Base itself FIXMEs that gettaginfo doesn't return the real tag origin (tuba.qc:309), so this is a near-equivalent approximation, not a faithful tag read. Per-instrument vertical offsets and the 0.25s throttle are bit-exact. |
| 386 | 1.5 | `weapon-tuba` | `weapon-tuba.melody.recognition` | presentation:partial | The cosmetic 'NAME played on the @!#%'n Tuba/Accordeon/Klein Bottle: ...' bprint chat line on a recognised melody is NOT emitted — the magic-ear TRIGGER (UseTargets) fires, but the broadcast-print of the matched message text needs a bprint seam absent in Common (Tuba.NoteOff:213-214 notes this). |
| 387 | 1.5 | `weapon-tuba` | `weapon-tuba.identity.metadata` | presentation:partial | Per-instrument view models (h_akordeon/h_kleinbottle, driven by CSQC wr_viewmodel/weaponname) still unwired; render-only (see weapon-tuba.reload.instrument_cycle).; Crosshair gfx/crosshairtuba and weapon-image weapontuba assets exist in data but live-render wiring not verified (headless audit). |
| 388 | 1.4 | `items-pickups` | `items-pickups.show.visibility` | presentation:partial | ITS_ALLOWSI (sv_simple_items, Base default 1) not honored — no simple-item model swap (no client resolver for the *_simple variants, no bob suppression). The bit is left unset rather than faked. |
| 389 | 1.35 | `mapobject-func` | `mapobject-func.train` | presentation:partial | no func_train reset (QC explicitly has `// TODO make a reset function for this one` — so this is faithful to Base's own omission, unlike door/plat/button).; train moving-sound stop (stopsoundto MSG_BROADCAST on arrival) is not done; the moving noise plays but is not explicitly stopped at each dwell (cosmetic). Port uses SoundChannel.Voice vs QC ATTEN_IDLE (cosmetic).; no CSQC ENT_CLIENT_TRAIN draw/networking. |
| 390 | 1.2 | `cl-viewmodel` | `cl-viewmodel.align.gunalign_gunoffset` | values:partial | cl_gunalign side magnitude is a fixed representative AuthoredShotSideY (-3.5u) rather than each v_ model's own shot-tag vecs.y; left/center now move the gun but by an approximate amount, not the per-model Base value. |
| 391 | 1.2 | `cl-viewmodel` | `cl-viewmodel.casings.brass_eject` | timing:partial | Casing physics is a self-integrated gravity + single ground-plane bounce (FloorZ), not full MOVETYPE_BOUNCE world collision at cl_casings_ticrate (0.03125); on most maps no floorZ is supplied so the casing falls through and only the lifetime fade culls it. |
| 392 | 1.2 | `monster-golem` | `monster-golem.menu.describe` | presentation:missing | MENUQC describe() returns the golem's 3-paragraph monsterpedia entry; the port has no monster info-page system, so this text is not surfaced. Pure menu/UI flavor, no gameplay effect. |
| 393 | 1.2 | `sv-campaign` | `sv-campaign.welcome.levelnum` | presentation:partial | Presentation surface differs from Base: Base's CSQC net_handle_ServerWelcome (client/main.qc:1379) builds a full Welcome MENU DIALOG; the port surfaces the same text as a single per-client CHAT line via Commands.ChatToPlayer (Campaign.Log fallback on a bare host). The level number, the per-level description (shortdesc + longdesc, sourced from the all-levels catalogue keyed by absolute level index), and the +jump keybind (resolved exactly like Base getcommandkey) are ALL reproduced — the only residual divergence is dialog-vs-chat. The 'no per-level description string' gap from the 2026-06-22 baseline is CLOSED (Wave 8e, commit 412ca19): CampaignLevelDescription (GameWorld.cs) now reconstructs the description the SVQC slice drops. |
| 394 | 1.2 | `sv-client-lifecycle` | `sv-client-lifecycle.frame.pressed_keys` | presentation:partial | Net delivery to the spectated HUD is still missing: Player.PressedKeys is computed server-side but is NOT in the owner-state net protocol, and PressedKeysPanel.PressedKeysOverride is never assigned — so a SPECTATED player's held keys don't reach the client HUD. The local-player panel works because PressedKeysPanel computes the bitmask client-side from bind state.; GetPressedKeys mutator hook is not fired as a discrete hook here (the race/cts speed-award + Vortex/OkNex velocity-charge consumers are driven from their own per-frame ticks instead). |
| 395 | 1.2 | `sv-commands-votes` | `sv-commands-votes.vote.call` | presentation:partial | The networked Nagger vote HUD entity (the on-screen vote prompt) is not emitted by this core — a client-side networked-entity presentation gap. The ANNCE_VOTE_CALL announcer cue IS now played (audio fixed). |
| 396 | 1.2 | `sv-impulse` | `sv-impulse.action.drop` | presentation:partial | Dual-wield sideways offset (dv = v_right * -movedir.y) collapses to zero — the port drives a single weapon slot (documented). The FL_TOSSED flag + colormap/glowmod render nits on the thrown loot are not modeled (presentation partial). |
| 397 | 1.2 | `sv-ipban` | `sv-ipban.enforce.roster` | presentation:partial | The bprint summary wording differs (port "^1banned: <names> (<reason>)" vs Base per-client "^1NOTE:^7 banned client <name>^7 has to go\n" + the ": affects <name>, ..." reason-append). Functionally equivalent; cosmetic. |
| 398 | 1.2 | `sv-ipban` | `sv-ipban.view_delete` | presentation:partial | Ban_View footer wording differs: Base "^2Done listing all active (N) bans." (+ a "^2Listing all existing active bans:" header), port "{n} ban(s) active" (no header). Per-line "#i: <ip> is still banned for <secs> seconds" matches.; Ban_Delete's OnlineBanList_SendUnban side-effect is dropped (no online sync); slot deletion itself is faithful. |
| 399 | 1.2 | `sv-spawnpoints` | `sv-spawnpoints.put.put_player_in_server` | presentation:partial | EF_TELEPORT_BIT \| EF_RESTARTANIM_BIT are SET on the (re)spawned player server-side (SpawnSystem.cs:813, OR'd into p.Effects, networked via EntityField.Effects). The NO-INTERP half of EF_TELEPORT_BIT is now CONSUMED client-side (ServerNet.cs:1921 raises NetEntityFlags.Teleported on the spawn/teleport clear signal so the model snaps to the spawn spot instead of lerping). EF_RESTARTANIM_BIT is an intended no-op (the port's procedural LocomotionBlend re-derives the pose every frame; no frame-anim to restart). The remaining gap is the visible teleport SPARKLE: there is no dedicated csqcmodel flash from the EF bit — the spawn flash is delivered by the (team-colored) EFFECT_SPAWN particle burst (fx.spawn_event_particle) instead. Residual presentation gap, narrowed from baseline. STILL OPEN (presentation only). |
| 400 | 1.2 | `sv-world-rules` | `sv-world-rules.warmup.serverflags` | presentation:partial | Only the FORBID_PICKUPTIMER consumer is ported: the HUD pickup panel reads the bit to gate the leading match timer (PickupPanel.cs:169-173, QC client/hud/panel/pickup.qc:91). The ALLOW_FULLBRIGHT consumer is still unported — nothing reads SERVERFLAG_ALLOW_FULLBRIGHT to gate fullbright player rendering (Base client/view.qc:1115 cl_fullbright gate), so that bit has no on-screen effect. |
| 401 | 1.2 | `sv-world-rules` | `sv-world-rules.nextlevel.intermission` | presentation:partial | target_music_kill (stop any playing trigger-music entity at match end) is not invoked here — no MusicKill analogue in the port.; MatchEnd_RestoreSpectatorStatus (move match-joiners back to spectator at match end) is not called here (see new_unmapped).; The bprint team/player 'wins the match' broadcast lines are handled by the notification layer, not this path (verify coverage in sv-notification). |
| 402 | 1.2 | `sv-world-rules` | `sv-world-rules.boot.lightstyles` | presentation:partial | Styled lights animate by modulating only the dynlight's own RADIUS (DynamicLightRenderer samples LightStyles.Sample at 10fps and scales OmniRange). DarkPlaces additionally animates WORLD-BRUSH lightmaps for named styles; the port has no lightmap-modulation subsystem, so styled world geometry renders steady (only the dynlight node flickers). (Tracked also under mapobject-misc.yaml.) |
| 403 | 1.2 | `tmayhem` | `tmayhem.teams.count` | presentation:unknown | QC tmayhem_DelayedInit spawns/labels tmayhem_team map entities via Team_MapEnts_FindOrSpawn; the port derives only the team COUNT and relies on generic team registration. Whether tmayhem_team colours/entities are honoured was not verified (unchanged from baseline). |
| 404 | 1.2 | `turret-flac` | `turret-flac.identity.spawn` | presentation:missing | head model models/turrets/flac.md3 not set (no head-bone attachment in port). NOTE: the head-FRAME cycle (++frame wrap 4) DID land in Wave 8e — it runs on the turret edict's own networked Entity.Frame (FlacTurret.cs:154) — but there is still no separate head model/bone to drive. |
| 405 | 1.2 | `turret-fusionreactor` | `turret-fusionreactor.setup.tr_setup` | presentation:missing | Initial head spin (50 deg/s) and head scale 0.75 are not applied — no head entity / no client render. |
| 406 | 1.2 | `turret-hellion` | `turret-hellion.anim.headspin` | presentation:missing | Server-authoritative frame STATE is now cycled (idle 0 -> spinning 1..6 kicked per shot, wrap at >=7), but there is no separate head MODEL entity (hellion.md3) attached to a bone, so the cycling frame drives no VISIBLE spinning launcher and is not used to select the firing muzzle tag (tag_fire vs tag_fire2 / two-cannon alternation) — both remain client-render gaps tracked here + on turret-hellion.weapon.launch. |
| 407 | 1.2 | `turret-plasma` | `turret-plasma.track.fluid_inertia` | presentation:missing | Head angles live only in server TurretState and are never networked/rendered — the head never visibly aims (presentation). |
| 408 | 1.2 | `turret-plasma` | `turret-plasma.headspin.frame_anim` | presentation:missing | PRESENTATION only: the frame-cycle logic runs server-side on the turret edict's e.Frame, but the body is a static base.md3 with no head-spin frames and there is no rendered tur_head sub-entity / CSQC turret net layer — so the spin is computed but never visibly shown. |
| 409 | 1.2 | `turret-plasma_dual` | `turret-plasma_dual.damage.headshake` | presentation:missing | Server-side head-angle jolt is now applied, but the CLIENT-rendered head model (plasmad.md3) is not attached, so the visible flinch is not drawn (the head-angle state nudges the next shot's aim, which IS the gameplay effect). Base also nets TNSF_ANG to CSQC; the port has no turret CSQC net layer. |
| 410 | 1.2 | `turret-plasma_dual` | `turret-plasma_dual.presentation.head_frame_anim` | presentation:missing | Server-authoritative head-frame state (Entity.Frame) is now ported and live, but the CLIENT render of the spinning plasmad.md3 head bone is absent — the static base.md3 body has no spin frames and there is no tur_head sub-entity / CSQC turret net layer, so the wheel is not visibly drawn.; Two-barrel tag_fire alternation (tag_fire on plasmad.md3) not modeled — single computed ShotOrg muzzle. |
| 411 | 1.2 | `vehicle-bumblebee` | `vehicle-bumblebee.spawn.vr_spawn` | presentation:missing | Shield hit-flash entity (vehicle_shieldent, MDL_VEH_BUMBLEBEE_SHIELD, scale=512/vlen(maxs-mins)), cosmetic gun models (CANNON_RIGHT/LEFT/CENTER), cockpit/viewport model offsets (body hudmodel '50 0 -5'/viewport '5 0 2'; gun1 '90 -27 -23'/'-85 0 50'; gun2 '90 27 -23'/'-85 0 50'), scale 1.5, and the networked BRG_* raygun entity (Net_LinkEntity + bumble_raygun_send) are NOT created. |
| 412 | 1.2 | `vehicle-framework` | `vehicle-framework.enter.steal` | presentation:missing | The CSQC presentation half is deferred: the WP_VehicleIntruder radar waypoint (g_vehicles_steal_show_waypoint) and the CENTER_VEHICLE_STEAL / _SELF centerprint notifications. The authoritative steal STATE (shield zero + flag backup/strip, restored on exit) is fully applied. |
| 413 | 1.2 | `vehicle-framework` | `vehicle-framework.respawn.return_waypoint` | presentation:missing | No WP_Vehicle return waypoint / radar icon is shown while a vehicle returns (still deferred, CSQC). Base spawns a separate vehicle_return entity with vehicles_showwp as think; the port re-arms the vehicle's own think directly (functionally equivalent for the return SCHEDULE, but no radar icon). |
| 414 | 1.2 | `vehicle-raptor` | `raptor.spawn.setup` | presentation:missing | Cosmetic sub-entities not created: no raptor_spinner rotor entities (engine_left/right), no raptor_rotor_anglefix, no bomb1/bomb2/tur_head cosmetic models (gun1/gun2 ARE created for aim).; mass=1 is set on the entity but has NO behavioral effect — the port's MoveTypePhysics solver never reads .Mass, so the QC bounce-missile mass tuning is inert (value set for fidelity only).; FIXED (Wave-2 fixup): Spawn() no longer nulls vehicle.Touch, so the VehicleCommon.Touch dispatcher SpawnVehicle installed stands (crush + vr_impact path now live, see raptor.impact). Matches QC vr_spawn (does NOT clear .touch) and Racer (Racer.cs:189-190). |
| 415 | 1.2 | `vehicle-raptor` | `raptor.enter` | presentation:missing | tur_head.exteriormodeltoclient owner-binding and CSQCVehicleSetup (client viewport/HUD) not done here (cross-boundary client/net).; Carried-flag reparent ('-20 0 96') not modeled (CTF-on-raptor edge): a flag carried while boarding the raptor will not be repositioned to the cockpit mount. |
| 416 | 1.2 | `vehicle-raptor` | `raptor.takeoff` | presentation:missing | Animation frame ramp tracked (VehAnimFrame) but drives no visible model; rotor avelocity ramp (90 + frame/25*25000) absent (no rotor entities) — presentation only.; Wave-2 added a button-clear (ATCK/ATCK2/CROUCH) at the end of Takeoff, but it writes vehicle.VehInput which GameWorld.cs:1104-1105 overwrites with fresh player input every tick BEFORE the next Think — so the clear is effectively a no-op on the live path. QC's per-tick button zero is faithful but the port's input-refresh architecture makes it inert (minor — input only reaches Frame, not Takeoff, so no fire/dive actually leaks). |
| 417 | 1.2 | `vehicle-raptor` | `raptor.flight_controller` | presentation:missing | No visible model pitch/roll/yaw pose beyond entity angles; player glue origin+'0 0 32' done but no cockpit/exterior model (presentation only). |
| 418 | 1.2 | `vehicle-raptor` | `raptor.cannon_aim_lock` | presentation:missing | Aux lock crosshair color feedback (red==1 / green>0.5 / blue<0.5 by lock strength, UpdateAuxiliaryXhair) is client-only and not drawn.; locktarget==2 alternate direct-trace lock path unimplemented (default is 1, so off the live path). |
| 419 | 1.2 | `vehicle-raptor` | `raptor.cannon_fire` | presentation:missing | No EFFECT_RAPTOR_MUZZLEFLASH and no CSQCProjectile (PROJECTILE_RAPTORCANNON) visual for the cannon bolt (server hit logic only).; Spread sampling differs from Base: QC does normalize(dir + randomvec()*spread) (a cube offset added to forward, perturbing forward too); the port uses Prandom.Spread (uniform-in-disc projected onto right/up). Distribution shape differs — negligible at spread 0.01 but not bit-faithful. |
| 420 | 1.2 | `vehicle-raptor` | `raptor.flares` | presentation:missing | No EF_FLAME flare glow / MDL_VEH_RAPTOR_FLARE model / scale 0.5 (decoy is invisible but functional).; Flare damage uses Health<=0 -> remove inline rather than the dedicated raptor_flare_damage event_damage hook; behaviorally equivalent (a shot flare with health 20 dies).; Seduction scan filters proj.VehGuideMode>=0; QC's raptor_flare_think scans g_projectiles by it.enemy==owner WITHOUT a guide-mode filter (the MIF_GUIDED_TRACKING gate is only on the incoming-alarm path, not the flare retarget). The port's extra filter could under-match guided rockets the flare should seduce. |
| 421 | 1.2 | `vehicle-raptor` | `raptor.mode_switch` | presentation:missing | CSQCVehicleSetup (client HUD reticle update on mode change) not done (client concern). |
| 422 | 1.2 | `vehicle-raptor` | `raptor.exit_land` | presentation:missing | Land animation frame from altitude (hgt<128: (hgt/128)*25) + rotor spin tracked-but-invisible (no model).; antilag_clear on exit + CSQCVehicleSetup viewport restore are cross-boundary (net/client). |
| 423 | 1.2 | `vehicle-raptor` | `raptor.painframe` | presentation:missing | EFFECT_SMOKE_SMALL low-health smoke puff (origin + randomvec()*80) is not emitted — the headless Common layer has no particle/temp-entity emitter (noted VehicleCommon.cs PainFrame; presentation only). |
| 424 | 1.1 | `net-entity-state` | `net-entity-state.csqcmodel.interpolation` | presentation:partial | Animation frame-group cross-fade (cl_lerpanim_maxdelta_framegroups/cl_nolerp) not networked; replaced by client-side LocomotionBlend — remote animation FRAME blending diverges from Base's networked 2-frame lerp. (The separate networked anim ACTION state is tracked in net-entity-state.csqcmodel.anim_state.) |
| 425 | 1.08 | `mapobject-teleporters-portals` | `teleporters.dest.random_selection` | timing:unknown | Port still forces telefrag-avoid always-on for clients rather than reading the now-registered g_telefrags_avoid / STAT(TELEPORT_TELEFRAG_AVOID) — equivalent at the Base default of 1 but not cvar-driven (see teleporters.core.telefrag_cvars). |
| 426 | 1.05 | `bot-ai` | `bot-ai.think.throttle` | values:partial | Simulated bot ping/aim-latency (CS(this).ping, bot_aimlatency) is not modeled; bots have zero network latency, so their lead/reaction is slightly sharper than stock at low skill. |
| 427 | 1.0 | `mutator-bloodloss` | `mutator-bloodloss.menu.dialog` | presentation:partial | The full MENUQC describe paragraph (bloodloss.qc:72-79, two sentences on stun/impaired movement/rapid health drain) is reduced to a one-line tooltip on the checkbox/slider. |
| 428 | 1.0 | `mutator-buffs` | `buffs.item.spawnfunc` | presentation:partial | Per-buff visual (skin/color/glowmod) is NOT applied to the spawned item: ConfigureBuffEntity sets the relic model + size but never assigns buff.m_skin / buff.m_color / glowmod, so the pickup renders as the uncolored relic regardless of which buff it carries (QC buff_Think colors it). Presentation partial.; Minor lifecycle omissions vs buff_Init: PFLAGS_FULLDYNAMIC dynamic light + buff_Customize light pulse, bot_pickupbasevalue 1000 (bots don't weight buff items), and the buff_Waypoint_Spawn build-finished waypoint (see buffs.waypoint) are not ported. |
| 429 | 1.0 | `mutator-buffs` | `buffs.item.touch_pickup` | presentation:partial | EFFECT_ITEM_PICKUP particle is still not spawned (no effect-spawn service); only the pickup sound + notifications play.; cl_buffs_autoreplace is read as the server-side value (no per-client REPLICATE store), so it can't differ per client; functionally equivalent on a server-wide setting. |
| 430 | 1.0 | `mutator-buffs` | `buffs.inferno` | presentation:partial | Carrier-as-target fire-immunity (DEATH_FIRE -> 0) + half-lava (DEATH_LAVA x0.5) present and match Base. The 0.5 lava factor is QC's hardcoded `0.5` (QC has a `TODO cvarize` so this is faithful).; Burn-on-hit via MonsterFramework.AddFireDamage applies the shared Burning status effect rather than QC's .fire_* accumulator; the integrated total over the burn window approximates QC. Minor numeric divergence in the per-tick distribution, not the total or the curve.; Presentation: no EF_FLAME flame visual is networked specifically for inferno's burn. |
| 431 | 1.0 | `mutator-buffs` | `buffs.replace_powerups` | presentation:partial | Replacement buff item inherits the spawnfunc presentation gap (buffs.item.spawnfunc): per-buff skin/color/glowmod is not applied, so the replaced powerup renders as the uncolored relic. No carrier glow/waypoint either. |
| 432 | 1.0 | `mutator-damagetext` | `damagetext.cl.draw_fade_shrink_move` | presentation:partial | drawfontscale crisp-cell scaling: QC renders the string into a size_max-sized font cell scaled by drawfontscale = size/size_max, then offsets y by drawfontscale.x*size/2 (cl_damagetext.qc:91-93). The port draws at a direct integer pixel size with no drawfontscale cell, so glyph metrics and the small vertical re-centering differ slightly. Minor presentation residual; the visible number, color codes, font, drift and horizontal centering are otherwise faithful. |
| 433 | 1.0 | `mutator-dynamic_handicap` | `dynamic_handicap.present.handicap_level` | presentation:partial | Presentation is faithful in color/level/value but uses a flat colored square as the scoreboard marker instead of the real gfx/scoreboard/player_handicap 32x32 texture (the port panel has no gfx texture path; ScoreboardPanel.cs:1073-1081 draws an exact-tint square). The level int, the network round-trip, and the tint formula are all bit-faithful to Base. |
| 434 | 1.0 | `mutator-new_toys` | `mutator-new_toys.menu.describe_page` | presentation:partial | The describe() content (gimmicky-weapons intro, InstaGib/Overkill mutual-exclusivity note, and the current new-toy weapon list) IS ported, but as the New Toys checkbox tooltip rather than a dedicated PAGE_TEXT describe-page widget (the port has no describe-page surface — DialogMutators.cs:214 comment). Presentation surface differs; text is faithful. The weapon list is a hardcoded literal ('Seeker, Mine Layer, HLAC, Rifle, Arc') instead of Base's runtime FOREACH(Weapons, nt_IsNewToy) enumeration, so it would not auto-update if the new-toy set changed — inert at the current fixed set. |
| 435 | 1.0 | `mutator-powerups` | `powerups.glow.effects` | presentation:partial | Presentation fidelity: Base's EF_ADDITIVE\|EF_FULLBRIGHT recolors the WHOLE model additively; the port renders the glow primarily as a blue/red OmniLight3D (EF_BLUE->blue light, EF_RED->red light, CsqcModelEffects.cs:119-120), and the EF_ADDITIVE/EF_FULLBRIGHT bits only affect per-instance-material meshes (shared/shader-material player models skip them — a documented CsqcModelEffects render gap). |
| 436 | 1.0 | `mutator-waypoints` | `waypoints.ctf.flag_markers` | presentation:partial | No flag-carrier HEALTH BAR (QC WaypointSprite_AttachCarrier sets one from 2*healtharmor_maxdamage; the port's CollectWaypoints sets Health=-1 and AttachCarrier has no live caller). UNCHANGED.; No auto-helpme on carrier hit (g_ctf_flagcarrier_auto_helpme_damage 100) — depends on the uncalled AttachCarrier/HelpMePing-on-carrier path. UNCHANGED.; Enemy-flagcarrier waypoint reveal on stalemate not wired: the QC ctf_stalemate enemy-FC waypoint reveal is not produced from the port's stalemate state. UNCHANGED. |
| 437 | 1.0 | `mutator-waypoints` | `waypoints.client.draw` | presentation:partial | g_waypointsprite_text / _uppercase / _iconsize / _fontsize / _scale / _alpha / _edgeoffset_* are read live and take effect; cl_hidewaypoints 0/1/2 + hideflag&1 (Hideable) gating honored. UNCHANGED.; g_waypointsprite_iconcolor still hardcoded white (QC iconcolor 0 default → white, so faithful at default; the non-zero tint option is not modeled). Minor. UNCHANGED.; The off-screen arrow is a simpler filled triangle than QC's bordered glyph (cosmetic). UNCHANGED. |
| 438 | 1.0 | `mutator-waypoints` | `waypoints.client.fades` | presentation:partial | Lifetime fade is server-computed (FadeAlpha) and networked rather than client-computed from fadetime/lifetime**timealphaexponent; timealphaexponent (1) not applied at the client (value 1 → no shape difference, but the computation site differs). Minor. UNCHANGED.; Off-screen arrow glyph still a simplified triangle (tracked under waypoints.client.draw). |
| 439 | 1.0 | `mutator-waypoints` | `waypoints.monster.healthbar` | presentation:partial | Live producer of the floating WP_Monster healthbar: SpawnHealthbar (MonsterAI.cs:446, called from Monster spawn at :568 under the g_monsters_healthbars gate) spawns WP_Monster at maxs.z+15 following the monster (maxdist 1024, hideable, RADARICON_DANGER), primes the bar unless INVINCIBLE, refreshes on hit (UpdateHealthbar / UpdateHealth), re-teams via ChangeTeam→UpdateTeamRadar, and is Killed on death (MonsterAI.cs:691/1909). Merged per peer via WaypointSprites.Active → ServerNet.SendWaypoints.; presentation: wp_extra (= monsterid, used client-side to resolve the monster's name/icon via get_monsterinfo) is not set on the sprite (and not networked), so the client renders the generic WP_Monster def text ('Monster') rather than the per-monster name. Cosmetic — same class as the other un-networked wp_extra gaps (waypoints.net.serialize). |
| 440 | 0.96 | `cl-hud` | `cl-hud.panel.weapons` | timing:unknown | Selection-slide animation + accuracy-tint overlay fidelity not runtime-verified. |
| 441 | 0.96 | `cl-hud` | `cl-hud.panel.powerups` | timing:unknown | Countdown expanding-flash (<=5s) timing not runtime-verified. |
| 442 | 0.96 | `cl-hud` | `cl-hud.panel.timer` | values:unknown | Secondary round-timer + overtime/sudden-death/warmup subtext coverage not all runtime-verified. |
| 443 | 0.96 | `cl-hud` | `cl-hud.panel.physics` | timing:unknown | topspeed peak fidelity not runtime-verified. |
| 444 | 0.9 | `mapobject-func` | `mapobject-func.button` | presentation:partial | the pressed-state alternate texture (Frame=1) is set on the entity but whether the brush model renders the alt frame in-engine is unverified (presentation).; CORRECTED 2026-06-25: button_reset round-restart re-arm is LIVE — ButtonReset is assigned at Buttons.cs:42 and dispatched by GameWorld.ResetMapObjects (GameWorld.cs:3887). The prior gap note claiming it was 'STILL dead' / the comment is wrong was stale; the Buttons.cs:15 comment is correct. (See mapobject-func.reset.) |
| 445 | 0.9 | `mapobject-func` | `mapobject-func.plat` | presentation:partial | plat_reset initial placement is live + faithful (targeted->raised, untargeted->bottom); the round-restart .reset is ALSO live now — PlatReset is assigned at Platforms.cs:82 and dispatched by GameWorld.ResetMapObjects. (CORRECTED 2026-06-25; the prior 'STILL NOT wired' note was stale — see mapobject-func.reset.); plat_outside_touch (the CSQC-side outside trigger that sends a top plat down when a player leaves) is not ported — it is gated to CSQC in Base; minor.; no CSQC ENT_CLIENT_PLAT draw/networking. |
| 446 | 0.9 | `mapobject-teleporters-portals` | `portals.weapon.projectile` | presentation:partial | OnTouch re-probes the impact face with a worldonly traceline (ProbeImpact) to recover Q3 surface flags + the true plane normal, so the slick/clip + noimpact branches are reachable; but on a miss (no nearby solid) it falls back to the negated-velocity normal rather than the real wall normal.; porto_v_angle held-angle capture for the non-secondary combined shot is dormant in stock play (g_balance_porto_secondary defaults to 1, so that branch is never reached); the hold state machine is modeled but only exercised with secondary disabled. |
| 447 | 0.9 | `mapobject-triggers` | `mapobject-triggers.push.trigger_push` | presentation:partial | no animdecide_setaction(ANIMACTION_JUMP) third-person jump pose on launch (jumppads.qc) — needs the cross-file networked anim-action seam (same gap as PlayerPhysics jump / walljump grunt); the locomotion blend still shows the airborne pose |
| 448 | 0.9 | `monster-spider` | `monster-spider.identity.registration` | values:partial | Spider.qh spawnflags (MON_FLAG_MELEE\|RANGED\|RIDE) are not represented on the port descriptor; RIDE is moot (no monster-riding mode), MELEE/RANGED are advisory (the Attack picks by distance regardless). |
| 449 | 0.9 | `monster-spider` | `monster-spider.think.brain` | timing:partial | Spider.mr_think in QC is a trivial return-true (no per-type logic); the port Think -> RunThink matches. The shared move steering uses a FIXED 0.4 velocity blend (movelib_move_simple approximation) rather than QC's frametime-scaled blend — a documented determinism choice, shared by all monsters; flagged timing:partial. |
| 450 | 0.9 | `monster-zombie` | `monster-zombie.setup.mr_setup` | timing:partial | Spawn-anim/no-think window hardcoded to 1/3s instead of being derived from anim_spawn '30 1 3' via animfixfps/animstate_endtime (close approximation, not model-exact). The spawn POSE (frame 30) IS played; only the per-frame fps/duration of the group is approximate (timing). |
| 451 | 0.9 | `monster-zombie` | `monster-zombie.presentation.animation` | timing:partial | Per-frame fps/duration (the second/third animfixfps components, e.g. die1 '9 1 0.5' = 2s) is not modeled by the phase enum: frame VALUES are byte-exact but the per-group playback RATE is approximate (timing). No frame-group/value losses remain — all 9 distinct groups (spawn 30, idle 19, walk/run 27, melee 4, pain1/2 20/22, die1/2 9/12, blockstart 8, blockend 7, shoot 0) are mapped, and both random variants (pain1/pain2, die1/die2) now alternate. |
| 452 | 0.9 | `turret-framework` | `turret-framework.spawn.init` | values:partial | Per-unit cvar table unported (load_unit_settings / TR_PROPS_COMMON / turret_scale_* / turret_initparams clamping / dumpturrets) — per-unit balance is hardcoded C# const.; No tur_head child entity / tag_head attachment (head bone is a TurretState.HeadAngles vector, not an edict).; turret_initparams clamping/derivation absent: per-turret consts are used raw with no bound/default fill. |
| 453 | 0.8 | `cl-crosshair` | `cl-crosshair.anim.smooth_ease` | presentation:partial | Color is NOT eased: Base eases scale, alpha AND color toward goal over effect_time (crosshair.qc:444 `wcross_color = f*wcross_color + (1-f)*wcross_color_prev`); the port's SmoothGoal eases only scale+alpha and sets color directly each frame. Visually equivalent only for slow color changes within the effect_time window. |
| 454 | 0.8 | `cl-crosshair` | `cl-crosshair.trueaim.teammate_blur_signal` | presentation:partial | Blur is a 5x5 spread of 0.04-alpha texture copies (matches Base's CROSSHAIR_DO_BLUR for textured crosshairs, crosshair.qc:585-596); only applies to the textured path (DrawCrosshairTexture), not the vector fallback (DrawVectorCrosshair). |
| 455 | 0.8 | `cl-csqcmodel` | `cl-csqcmodel.skeleton.upper_lower_aim` | presentation:partial | Presentation gap is on the ACTION side, not the split mechanism: the upper-body torso always shows a stable idle/aim frame (LocomotionBlend.Split torsoTime=0), so the bone split + fixbone + aim-pitch are structurally faithful but the torso never animates beyond the aim-bone pitch bend (the upper-body ACTION overlay is the animdecide feature's gap).; Aim pitch reads Entity.Angles.X instead of a separately-networked v_angle_x (BIT(10)); for remote players these can differ slightly (the visible head/torso bend may lag or use the body yaw-pitch rather than the true view pitch). |
| 456 | 0.8 | `cl-hud` | `cl-hud.engine.registry_and_order` | presentation:partial | Draw order follows Godot child order, not the QC _hud_panelorder reverse-walk; relative panel layering may differ but panels don't overlap in luma. |
| 457 | 0.8 | `cl-hud` | `cl-hud.engine.skin_border` | presentation:partial | Directional border variants (_south/_north/_east/_corner_*) render as the full 9-slice frame rather than the engine's edge-masked crops; bg-enabled panels show a full frame where Base shows a partial edge. |
| 458 | 0.8 | `cl-hud` | `cl-hud.panel.pressedkeys` | presentation:partial | key_*/key_*_inv per-key art coverage not all runtime-verified (the inverted/pressed art set). |
| 459 | 0.8 | `cl-scoreboard` | `cl-scoreboard.header.gameinfo` | presentation:partial | Lead-limit header term IS now built (BuildLimitsHeader handles ll > 0 && (ll < fl \|\| fl <= 0) with the '^2+N' term and the '& '/'/'  delimiter, LeadLimit/LeadAndFragLimit fed from cvars NetGame.cs:4175-4176).; m_hidelimits and campaign player-count suppression not handled. |
| 460 | 0.8 | `cl-scoreboard` | `cl-scoreboard.block.spectators` | presentation:partial | Spectator-row ping is still -1 (per-row ping not networked) so showping shows a dim '-' rather than a real value.; spectators_position (0/1/2/3 placement around the blocks) and spectators_aligned column alignment not implemented (drawn after the table only). |
| 461 | 0.8 | `cl-scoreboard` | `cl-scoreboard.block.rankings` | presentation:partial | Speed-award / all-time-fastest line is now PORTED + LIVE (DrawSpeedAward, ScoreboardPanel.cs:1257, fed by SetSpeedAward NetGame.cs:3837 from networked SpeedAward/Best + holders; QC unit conversion + alltimebest gate).; No multi-column wrap / horizontal scroll (single-column rankings list). |
| 462 | 0.8 | `fx-notifications` | `fx-notifications.args.tokens` | presentation:partial | Tokens are resolved SERVER-side reading the SERVER's notification_show_location / _show_sprees* cvars; in Base these are CSQC-side per-client, so the port loses per-client control of location/spree display (direct consequence of server-side formatting — see fx-notifications.net.protocol intended divergence). This is now the ONLY residual on the cvar-driven tokens (the default polarities are fixed).; race tokens (f1race_time/f2race_time/f3race_time, race_col, race_diff) referenced by the RACE_* notifs in NotificationsList.cs are NOT implemented in ResolveOne (drop to '' at :99) — race split/record lines lose their time/diff/color text.; key-bind tokens (pass_key/nade_key/join_key — getcommandkey lookups) and the s3#s2/#s2 HASH_REPLACE tokens are not implemented in ResolveOne (drop to ''); used by CTF flag-pass / nade / spectator-join hints and a few hash-replace info lines. |
| 463 | 0.8 | `fx-notifications` | `fx-notifications.info.killfeed` | presentation:partial | MULTIICON_INFO (icon derived from args via iconargs) has no port path; the only Base user is MINIGAME_INVITE (minigames not a port priority).; notification_allow_chatboxprint (MSG_INFO -> chatbox routing) not modelled; benign — Base disables it in code (default 0). |
| 464 | 0.8 | `mutator-instagib` | `mutator-instagib.spawn.fullbright` | presentation:unknown | Whether EffectFlags.FullBright actually renders the player model fullbright in the Godot renderer is still unverified (behavioral check needed). |
| 465 | 0.72 | `monster-mage` | `monster-mage.anim.frames` | timing:unknown | timing (animfixfps fps normalization — the '<start> 1 <fps>' frame-rate component) is not modeled; only the start frame index is stamped onto Entity.Frame, so per-group playback rate is not normalized to Base. |
| 466 | 0.6 | `monster-golem` | `monster-golem.attack.lightning` | presentation:partial | Projectile renders the correct model (models/ebomb.mdl) but at 1x — QC gren.scale = 2.5 (golem.qc:145) is not networked (Entity has no render-scale field; the CSQCMODEL scale-bit contract is unported). Visible bolt is smaller than Base but no longer headless. |
| 467 | 0.6 | `monster-mage` | `monster-mage.attack.spike` | presentation:partial | CSQC spike projectile model (PROJECTILE_MAGE_SPIKE, mage.qc:238 CSQCProjectile) still not wired — SpawnProjectile sets no projectile visual/ProjectileType, so the spike has no client-visible model/trail in flight (cf. Spider.cs which DOES set PROJECTILE_ELECTRO). |
| 468 | 0.6 | `monster-mage` | `monster-mage.defend.heal` | presentation:partial | Healed-ally waypoint-sprite health update (WaypointSprite_UpdateHealth, mage.qc:280-281) not invoked in the port — no WaypointSprite_UpdateHealth/UpdateHealth call anywhere in Gameplay/Monsters. |
| 469 | 0.6 | `monster-spider` | `monster-spider.attack.bite` | presentation:partial | QC bite picks a random melee anim (anim_melee vs anim_shoot via setanim). The port now sets the Attack anim PHASE (frame 0 mapped server-side via AnimFrame) but does NOT randomise melee/shoot, and (more importantly) the client DPM model does not render the networked Entity.Frame at all (see monster-spider.anim.mr_anim) — presentation only.; QC NPC bite uses actor.attack_range (= g_monsters_attack_range 120); the player-wielded path forces attack_range 60. Port uses st.AttackRange for the NPC path (faithful); the 60 player path is part of the unported player-wielded SpiderAttack (see monster-spider.attack.player_wielded). |
| 470 | 0.6 | `monster-spider` | `monster-spider.pain.mr_pain` | presentation:partial | Pain anim variant is now picked (MarkPain sets st.AnimVariant random 0/1 -> AnimFrame Pain 7/8) and the pain-window now matches the QC anim length (0.44s), but the CLIENT DPM model does not render the networked Entity.Frame (see monster-spider.anim.mr_anim), so the pain twitch is computed server-side yet not visible. Presentation partial. |
| 471 | 0.6 | `monster-spider` | `monster-spider.death.mr_death` | presentation:partial | CLIENT DPM playback missing: the chosen die1/die2 frame is networked on Entity.Frame but the DPM model does not consume it (see monster-spider.anim.mr_anim), so neither death variant is visually rendered. Presentation partial; the random pick itself is now wired server-side. |
| 472 | 0.6 | `sv-world-rules` | `sv-world-rules.checkrules.overtime` | audio:partial | race/qualifying race_StartCompleting branch deliberately omitted (those modes latch their own winner). |
| 473 | 0.6 | `turret-ewheel` | `turret-ewheel.identity.def` | presentation:partial | Head model (ewheel-gun1.md3) not instantiated as a separate head entity; no tag_head attachment.; ROAM spawnflag not modeled as an explicit flag (the path-roaming behavior it gates is now implemented and live — see turret-ewheel.drive.path). |
| 474 | 0.6 | `turret-ewheel` | `turret-ewheel.weapon.fire` | presentation:partial | No two-cannon head-frame alternation (Base ewheel_weapon.qc:32-35: tur_head.frame += 2, wrap > 3 -> 0) — needs the unported head sub-entity (the turret's own Entity.Frame is consumed by the locomotion drive animation); tracked under turret-ewheel.identity.def.; EFFECT_BLASTER_MUZZLEFLASH is emitted with except: turret, but Base Send_Effect -> Send_Effect_Except(..., NULL) does NOT exclude the firer — minor over-exclusion (cosmetic for a turret, which is not a first-person view source). |
| 475 | 0.6 | `turret-ewheel` | `turret-ewheel.client.draw` | presentation:partial | CSQC ewheel_draw rolling integration not ported: the model does not client-side roll (origin += velocity*dt, head angles += avelocity*dt, MOVETYPE_BOUNCE+gravity). The server already networks origin/velocity, so the visible motion follows the netted transform rather than the client integration. |
| 476 | 0.6 | `turret-flac` | `turret-flac.fire.flak_shell` | presentation:partial | Presentation partial (Wave 8e landed the server-emit side): BLASTER_MUZZLEFLASH is now emitted via EffectEmitter (FlacTurret.cs:149), the shell is stamped NetName='hagar' so the client ProjectileCatalog resolves the HAGAR trail (FlacTurret.cs:114), and the 4-frame head cycle runs on turret.Frame (FlacTurret.cs:154). REMAINING: the actual HAGAR trail RENDER + muzzleflash draw live in the Godot client project (not qcsrc) and are not yet verified end-to-end; the turret has no head MODEL/bone so the frame index drives nothing visible yet. |
| 477 | 0.6 | `turret-fusionreactor` | `turret-fusionreactor.identity.def` | presentation:partial | head_model (reactor.md3) is not carried — no head entity exists in the port turret model.; TUR_FLAG_SUPPORT/AMMOSOURCE are not stored as flags; support behavior is hardcoded in the C# class instead of derived from spawnflags. (Verified TUR_FLAG_AMMOSOURCE is declared but NEVER read anywhere in Base qcsrc — purely declarative — so its omission is functionally harmless; TUR_FLAG_SUPPORT only selects turret_targetscore_support, which the reactor never invokes since it has no targetscore-driven path.) |
| 478 | 0.6 | `turret-hellion` | `turret-hellion.identity.def` | presentation:partial | Head model (hellion.md3) not instantiated as a separate spinning-launcher head entity; no tag attachment. |
| 479 | 0.6 | `turret-hellion` | `turret-hellion.weapon.launch` | presentation:partial | No two-cannon tag_fire/tag_fire2 muzzle alternation (single computed ShotOrg origin). The head-spin frame state that Base uses to SELECT the tag is now cycled (turret-hellion.anim.headspin) but there is no head-model bone, so the muzzle-tag alternation stays a client-render gap.; No PROJECTILE_ROCKET CSQC trail / MIF_SPLASH\|MIF_PROXY\|MIF_GUIDED_HEAT flags (no visible smoke trail). |
| 480 | 0.6 | `turret-hk` | `turret-hk.identity.def` | presentation:partial | Head model (hk.md3) not instantiated as a separate head entity; no tag_head attachment (presentation only). |
| 481 | 0.6 | `turret-hk` | `turret-hk.anim.head` | presentation:partial | Frame cycler runs on the turret edict's own .Frame (the port has no separate tur_head bone entity), and there is no hk.md3 head model instantiated, so the 0..5 load/cycle animation is computed but never visibly displayed. |
| 482 | 0.6 | `turret-machinegun` | `turret-machinegun.spawn.identity` | presentation:partial | No separate tur_head sub-entity / head model attached at tag_head (Base turret_initialize:1344 spawns a turret_head entity and _setmodel(head_model)); the port's head is angle-state only (TurretState.HeadAngles), so the head model never renders or animates. |
| 483 | 0.6 | `turret-machinegun` | `turret-machinegun.lifecycle.damage` | presentation:partial | Headshake jitter is applied to the angle-state head (TurretState.HeadAngles, EventDamage:893-897) but there is no separate tur_head edict and no CSQC TNSF_ANG send, so the recoil flinch is server-state only — it will not be visible until a head model is attached and networked. Behaviour is faithful; visual is presentation-deferred. |
| 484 | 0.6 | `turret-mlrs` | `turret-mlrs.identity.def` | presentation:partial | Head model (mlrs.md3) not instantiated as a separate tur_head entity; no tag_head attachment. The ammo-gauge frame now rides the turret's own .Frame (see turret-mlrs.anim.ammogauge) but the head model is not separately rendered/animated. |
| 485 | 0.6 | `turret-mlrs` | `turret-mlrs.anim.ammogauge` | presentation:partial | Frame formula now ported every think onto the turret edict's OWN .Frame (Bound(0, 6 - floor(0.1 + Ammo/ShotDamage), 6)) — logic/values/timing faithful. But Base writes it to the separate tur_head entity's .frame and nets that head; the port has no distinct head bone entity, so the gauge is not rendered on a separately-attached mlrs.md3 head. Presentation partial. |
| 486 | 0.6 | `turret-mlrs` | `turret-mlrs.damage.lifecycle` | presentation:partial | Death gib toss still missing: Base cl_turrets.qc turret_die tosses base-gibN.md3 + head_model gibs (turret_gibtoss). The port has no CSQC turret edict to spawn client gib entities, so no gibs fly on death.; No client turret_draw (cl_turrets.qc): the head does not visibly spin (angles += avelocity*dt) and a damaged MLRS emits no low-health sparks (<127 hp -> te_spark) or smoke (<85 EFFECT_SMOKE_LARGE / <32 EFFECT_SMOKE_SMALL). |
| 487 | 0.6 | `turret-phaser` | `turret-phaser.identity.model_hitbox` | presentation:partial | Head model (phaser.md3) is not attached as a separate tracking head bone; head frame animation is also absent (see tr_think feature). |
| 488 | 0.6 | `turret-plasma` | `turret-plasma.identity.model_hitbox` | presentation:partial | head_model (plasma.md3/plasmad.md3) is never loaded or rendered — there is no tur_head sub-entity, only the base body. |
| 489 | 0.6 | `turret-plasma` | `turret-plasma.death.die_respawn` | presentation:partial | Death client gibs (base-gib1..4 + head gib toss/turret_gibboom) are still not spawned — needs a CSQC turret edict. The explosion + impact sound ARE now emitted. |
| 490 | 0.6 | `turret-tesla` | `turret-tesla.identity.def` | presentation:partial | Head model (tesla_head.md3) not instantiated as a separate head/coil entity; no tag_head attachment. TrThink computes head spin as state (HeadAngles) but grep confirms no head-bone node consumes HeadAngles, so the coil never visibly spins (see anim feature). |
| 491 | 0.6 | `turret-tesla` | `turret-tesla.anim.tr_think` | presentation:partial | Head spin not rendered: TrThink computes HeadAVelocity ('0 45 0' low-ammo / '0 180 0' charged, scaled by ammo/shot_dmg, zeroed when inactive) and integrates HeadAngles, but grep confirms no head-bone/coil entity (tesla_head.md3) consumes HeadAngles — the coil never visibly spins. Server-side state only.; Idle crackle arc reach distribution: Prandom.Vec() returns each component in [-1,1] (a cube) whereas QC randomvec() (DP builtin #91) rejection-samples a uniform direction inside the unit sphere; the *350 idle crackle endpoint is thus a slightly different shape. Cosmetic-only (idle muzzle crackle). |
| 492 | 0.6 | `turret-walker` | `turret-walker.identity.def` | presentation:partial | Head model (walker_head_minigun.md3) not instantiated as a separate tur_head entity; no tag attachment, so the minigun head never visibly tracks. |
| 493 | 0.6 | `vehicle-bumblebee` | `vehicle-bumblebee.pilot.raygun` | presentation:partial | The visible BRG_* beam is a placeholder EffectEmitter.TeHealBeam/TeBeam stub (Bumblebee.cs:698-700), not the networked CSQC bumble_raygun beam; the per-tick BRG_START/BRG_END send + `wait = time + 1` beam-hold + the bumble_raygun_draw multi-segment cylinder are not modeled. Beam COLOUR is bit-faithful (pure green heal / pure red damage). |
| 494 | 0.6 | `vehicle-bumblebee` | `vehicle-bumblebee.gunner.frame` | presentation:partial | Gunner aux crosshairs + cockpit HUD are now LIVE (Wave-7): GunnerFrame writes VehGunnerLeadPoint/Valid + VehGunnerHitPoint/Valid (Bumblebee.cs:605/648) and mirrors gunner.VehicleHealth/Shield/Ammo1/Energy (632-636); NetGame.UpdateVehicleHud selects the BumblebeeGun art for a seated gunner and projects the magenta '1 0 1' LEAD (aux slot 1) + reload-color READY (aux slot 0) crosshairs, AND mirrors the gunner READY markers onto the PILOT (slots 1/2 via FeedPilotGunnerAux). RESIDUAL presentation: cosmetic gun cockpit models + viewport offsets are still not built (HUD bars/crosshairs render, but the 3D gun cockpit art does not). |
| 495 | 0.6 | `vehicle-bumblebee` | `vehicle-bumblebee.gunner.cannon` | presentation:partial | Wave-8e FIX: EFFECT_BIGPLASMA_MUZZLEFLASH is now emitted at the muzzle (FireCannon: EffectEmitter.Emit('BIGPLASMA_MUZZLEFLASH', org, vel, 1); BIGPLASMA_MUZZLEFLASH registered EffectsList.cs:163). RESIDUAL: the networked CSQCProjectile (PROJECTILE_BUMBLE_GUN) bolt trail/model visual is still NOT IMPLEMENTED. |
| 496 | 0.6 | `vehicle-bumblebee` | `vehicle-bumblebee.death` | presentation:partial | Gib toss (gun1/gun2/gun3 from their tags + body via vehicle_tossgib, scale 1.5) NOT IMPLEMENTED — the vehicle_gib physics-entity subsystem is unported. |
| 497 | 0.6 | `vehicle-framework` | `vehicle-framework.spawn.respawn` | presentation:partial | EFFECT_TELEPORT spawn flash, CSQCMODEL_AUTOINIT, hook-removal sweep, and vehicles_reset_colors are deferred (client/presentation). |
| 498 | 0.6 | `vehicle-framework` | `vehicle-framework.think.tick` | presentation:partial | The painframe smoke particle (EFFECT_SMOKE_SMALL) is deferred — note: EffectEmitter now EXISTS in the Common layer (used by the descriptor death/muzzle FX) so the old 'no headless emitter' rationale is stale, but PainFrame still does not emit the smoke. The authoritative jitter IS produced (see combat.painframe). |
| 499 | 0.6 | `vehicle-framework` | `vehicle-framework.enter.handshake` | presentation:partial | MOVETYPE_NOCLIP + disableclientprediction=1 modeled as MoveType.None (player origin rewritten by the vehicle); behaviorally close but not the QC movetype.; SVC_SETVIEWPORT/SETVIEWANGLES + CSQCVehicleSetup, the temp_wepent weaponslot swap, CENTER_VEHICLE_ENTER notification, and RemoveGrapplingHooks are deferred (cross-boundary). The VehicleHud panel (the client view of CSQCVehicleSetup) IS fed live — see hud_camera_xhair. antilag_clear is now wired (pl.AntilagNeedsClear set on enter). |
| 500 | 0.6 | `vehicle-framework` | `vehicle-framework.exit.handshake` | presentation:partial | SVC_SETVIEWPORT/SETVIEWANGLES + CSQCVehicleSetup, temp_wepent switchweapon restore, Kill_Notification(CPID_VEHICLES), and last_vehiclecheck are deferred. (The HUD exits cleanly: UpdateVehicleHud calls hud.Exit() once p.Vehicle is null.) antilag_clear is now wired (pl.AntilagNeedsClear set on exit).; The WP_Vehicle return waypoint (vehicles_showwp) itself is still CSQC-deferred (the return SCHEDULE is now set — see respawn.return_waypoint). |
| 501 | 0.6 | `vehicle-framework` | `vehicle-framework.combat.damage` | presentation:partial | vehicle_shieldent hit flash (colormod/alpha angled to hitloc) is a CSQC visual — not drawn.; antilag_clear on death is deferred (cross-boundary). |
| 502 | 0.6 | `vehicle-framework` | `vehicle-framework.combat.painframe` | presentation:partial | Smoke particle (EFFECT_SMOKE_SMALL at origin + randomvec()*80) is deferred — the only unported piece. NOTE: EffectEmitter now EXISTS in the Common layer (Wave 8 wired it for the descriptor death/muzzle FX), so the old 'no headless emitter' rationale is stale; PainFrame simply does not call it yet. The authoritative velocity/angle jitter IS produced. |
| 503 | 0.6 | `vehicle-framework` | `vehicle-framework.aim.locktarget` | presentation:partial | The cockpit aux-crosshair lock-color update is LIVE for the building/locked target (UpdateVehicleHud -> SetAuxiliaryXhairLock projects VehLockTarget tinted by VehLockStrength); the per-target-class lock SEMANTICS are now also complete. |
| 504 | 0.6 | `vehicle-framework` | `vehicle-framework.projectile.shared` | presentation:partial | CSQCProjectile networking + the in-flight projectile trail/model visual are deferred (client/net). The muzzle EFFECT_* IS now emitted by the descriptors (Send_Effect via EffectEmitter).; g_bot_dodge list (bot AI) deferred. |
| 505 | 0.6 | `vehicle-framework` | `vehicle-framework.presentation.hud_camera_xhair` | presentation:partial | Cockpit/chase camera STILL dead: no client reads p.Vehicle to apply SVC_SETVIEWPORT (vehicle_viewport) — the pilot keeps the normal player view. (This is the only remaining DEAD piece of this row; it is its own viewport concern, not the HUD panel/crosshair.); Host-only feeder: UpdateVehicleHud reads LocalServerPlayer (host path). The cross-client VEHICLESTAT_* networking for a pure remote pilot is still unwired.; Vehicles_drawCrosshair is ported for the Raptor only (bomb/flare reticle + dropmark); the other vehicles' per-mode reticles are not yet wired (FeedRaptorReticle clears the centered reticle for non-Raptor). |
| 506 | 0.6 | `vehicle-racer` | `vehicle-racer.identity.def` | presentation:partial | hud cockpit model + tag_viewport attachment and the scale-0.5 model hack are cosmetic-only and not instantiated (physics uses the real hitbox).; view_ofs '0 0 50' + height 200 (camera view offset/dist) not set; affects the cockpit camera, not the sim. |
| 507 | 0.6 | `vehicle-racer` | `vehicle-racer.spawn.lifecycle` | presentation:partial | scale 0.5 (cosmetic model-shrink hack) still not applied; physics uses the real hitbox so this is presentation-only. |
| 508 | 0.6 | `vehicle-racer` | `vehicle-racer.weapon.cannon` | presentation:partial | The EFFECT_RACER_MUZZLEFLASH muzzle effect is now emitted; the CSQCProjectile (PROJECTILE_WAKICANNON) bolt trail remains a client-render concern and is unported. |
| 509 | 0.6 | `vehicle-racer` | `vehicle-racer.weapon.rocket` | presentation:partial | The EFFECT_RACER_ROCKETLAUNCH launch effect is now emitted; the CSQCProjectile (PROJECTILE_WAKIROCKET) rocket trail remains a client-render concern and is unported. |
| 510 | 0.6 | `vehicle-racer` | `vehicle-racer.weapon.rockethud` | presentation:partial | Authority-side ammo2/reload2 % stats are written each tick onto the pilot; the client HUD progressbar that READS them (Vehicles_drawHUD vehicle_icon_ammo2) is presentation/CSQC and not yet wired — a client-render concern, not an authority gap. |
| 511 | 0.6 | `vehicle-raptor` | `raptor.bombs` | presentation:partial | No CSQCProjectile visual (PROJECTILE_RAPTORBOMB / RAPTORBOMBLET) for the falling bomb/bomblet (client-render trail only).; The burst FX is partial: RAPTOR_BOMB_SPREAD particle puff IS now emitted (Send_Effect equivalent), but the bomb-casing shell-fragment gibs (RaptorCBShellfragToss, the CSQC half of Damage_DamageInfo DEATH_VH_RAPT_FRAGMENT) are still client-only — see raptor.cb_shellfrag_gib. |
| 512 | 0.6 | `vehicle-raptor` | `raptor.impact` | audio:missing | CONFIRMED LIVE (fixup commit 4be8357, re-verified 2026-06-22): Raptor.Spawn no longer nulls vehicle.Touch, so the VehicleCommon.Touch dispatcher SpawnVehicle installs stands and calls vehic.VehicleDef.Impact on a hard touch (matches Racer.cs:217-219 / QC vr_spawn which does NOT reset .touch). Raptor.Think:231 snapshots vehicle.OldVelocity BEFORE the tick's physics (matches Racer.Think:296), and Spawn:141 seeds PlayTime = Time to clear the impact debounce on respawn — so vehicles_impact computes the true Velocity-OldVelocity delta. The raptor takes DEATH_FALL crash damage on hard impacts.; Dispatch chain VERIFIED: a BounceMissile-movetype raptor is integrated by MoveTypePhysics.cs:160 which calls PushEntity(doTouch:true) -> FlyMove.cs:107-110 invokes e.Touch for non-Solid.Not solids -> VehicleCommon.Touch:766 -> VehicleDef.Impact when PlayTime<Time. (Earlier baseline cited TriggerTouch.cs for this; the actual moving-solid touch dispatch is FlyMove/MoveTypePhysics — corrected, behavior unchanged.); VehicleCommon.Impact:783-796 is line-for-line faithful to vehicles_impact (delta=Velocity-OldVelocity, min(speedfac*len,maxpain), DEATH_FALL, play_time=time+0.25). Only the trace_dphitq3surfaceflags & Q3SURFACEFLAG_NOIMPACT skip is deferred (no per-touch surface-flag in the headless collision result — noted at VehicleCommon.cs:785). |
| 513 | 0.6 | `vehicle-raptor` | `raptor.death` | presentation:partial | EXPLOSION_MEDIUM (death) + the per-think 5% EXPLOSION_SMALL ARE now emitted via EffectEmitter, but the findbetterlocation surface-nudge for the medium blast is not reproduced (FX at origin directly) — cosmetic offset only, matching every other vehicle vr_death port. No cosmetic wreck model / EF_NODRAW handling (presentation). |
| 514 | 0.6 | `vehicle-spiderbot` | `vehicle-spiderbot.identity.registration` | presentation:partial | view_ofs '0 0 70' / height 170 (the seated camera offset + chase distance) are client-view concerns not modeled on the headless server descriptor. |
| 515 | 0.6 | `vehicle-spiderbot` | `vehicle-spiderbot.weapon.minigun` | presentation:partial | Cosmetic barrel spin (QC gun1.angles.z += 45 / gun2.angles.z -= 45 with 360 wrap) is done client-side (Firing flag in VehicleVisuals); the server-state field is not advanced — functionally fine, non-1:1 split.; FIXED-SINCE-BASELINE: EFFECT_SPIDERBOT_MINIGUN_MUZZLEFLASH is now emitted server-side via EffectEmitter.Emit (Spiderbot.cs:493; effect registered src/XonoticGodot.Common/Gameplay/Effects/EffectsList.cs:161), matching QC spiderbot.qc:276 Send_Effect(EFFECT_SPIDERBOT_MINIGUN_MUZZLEFLASH, v, v_forward*2500, 1). (The client VehicleVisuals Firing flag also pops a flash; the server emit is the QC-faithful path.) Residual presentation gap = barrel-spin server-state only. |
| 516 | 0.6 | `vehicle-spiderbot` | `vehicle-spiderbot.weapon.rockets` | presentation:partial | ARTILLERY clearance-height traces use a headless WorldOnly trace; the h1/h2 (0.75 of the up/diagonal world clearance) are reproduced but the resulting arc was not in-game verified.; FIXED-SINCE-BASELINE: the launch effect EFFECT_SPIDERBOT_ROCKETLAUNCH is now emitted server-side via EffectEmitter.Emit (Spiderbot.cs:629; effect registered src/XonoticGodot.Common/Gameplay/Effects/EffectsList.cs:157), matching QC vehicles_projectile Send_Effect. Residual: the rocket-in-flight CSQCProjectile visual (PROJECTILE_SPIDERROCKET) stays client/net deferred to the shared projectile catalog. |
| 517 | 0.6 | `vehicle-spiderbot` | `vehicle-spiderbot.death.blowup` | presentation:partial | Presentation still simplified: no 4 physics gib entities (head MF_ROCKET+EF_FLAME bounce+fade via spiderbot_headfade, 2 tossed guns body-vel v_forward*700, body frame 11). FIXED-SINCE-BASELINE: the per-burn EFFECT_EXPLOSION_SMALL (Spiderbot.cs:459) and the spiderbot_headfade EFFECT_EXPLOSION_BIG (Spiderbot.cs:440) are now emitted server-side via EffectEmitter.Emit (the Send_Effect equivalent; effect names registered EffectsList.cs:29/31). The head EF_FLAME flag + the physics gib bodies stay a TODO (client VehicleVisuals does an approximate scripted gib arc). |
| 518 | 0.5 | `mutator-waypoints` | `waypoints.itemstime.respawn_countdown` | audio:partial | On the first countdown tick spawns WP_Weapon (weapon pickups) / WP_Item (else) attached to the item, UpdateBuildFinished drives the build-progress bar to the scheduled respawn, and every tick Pings the waypoint (radar ring) + plays ITEMRESPAWNCOUNTDOWN. SpectatorOnly items get SPRITERULE_SPECTATOR (the sv_itemstime 1/2 gate).; audio: partial — QC soundto(MSG_ONE) plays ITEMRESPAWNCOUNTDOWN per visible client; the port's Common layer has no per-client soundto seam, so it emits a single positional cue on CH_TRIGGER (ATTEN_NORM). SpectatorOnly items over-broadcast slightly to nearby live players — the only divergence.; wp_extra-driven per-item icon/color (m_waypointblink etc.) not networked — WP_Item renders generic magenta, WP_Weapon generic black/client-resolved. |
| 519 | 0.48 | `sv-impulse` | `sv-impulse.action.reload` | audio:unknown | weaponLocked is modeled only for the FREEZE half (FrozenStat + STATUSEFFECT_Frozen). QC weaponLocked also covers player_blocked and the LockWeapon mutator hook — both still unmodeled (low impact; not reachable in stock modes the same way). The game-start/game-stopped/timeout halves are gated upstream by the dispatcher. |
| 520 | 0.45 | `mapobject-func` | `mapobject-func.bobbing` | audio:partial | the spawn-time soundto(MSG_INIT) looping ambient is not resent per-player; played through the facade where present. |
| 521 | 0.45 | `mapobject-func` | `mapobject-func.pendulum` | audio:partial | ambient .noise not resent per-player (same reduction as bobbing). |
| 522 | 0.45 | `mapobject-func` | `mapobject-func.fourier` | audio:partial | looping ambient .noise resent per-player (soundto MSG_INIT) reduced to a single facade Play. |
| 523 | 0.45 | `mapobject-func` | `mapobject-func.vectormamamam` | audio:partial | looping ambient .noise per-player resend reduced to facade Play in setactive. |
| 524 | 0.45 | `mapobject-movers-platforms` | `mapobject-movers-platforms.plat.q3compat_defaults` | audio:partial | CPMA sound_start/sound_end + the pt1_strt/pt1_end default-file probe are not ported (require GetField_fullspawndata / FindFileInMapPack, unavailable headless). The medplat default still applies, so a Q3 plat is audible and correct — only the CPMA per-mapper sound OVERRIDE keys are skipped. Sole residual gap on this row; audio marked partial for it. |
| 525 | 0.3 | `monster-golem` | `monster-golem.audio.voice_cues` | audio:partial | Full data-driven .sounds-file parse + skin-keyed Monster_Sounds_Update is still hardcoded per-descriptor (SoundCues set + SoundCueCount switch mirror golem.dpm_0.sounds by hand) rather than parsed from the model's .sounds at load; behaviorally faithful for golem but the general GlobalSound/Monster_Sounds_Update plumbing is a named no-op.; QC spamsound(SND_BODYIMPACT1) pain placeholder (when a monster has no pain sample) not played — n/a for golem (golem has pain samples). |
| 526 | 0.24 | `turret-ewheel` | `turret-ewheel.damage.lifecycle` | audio:unknown | Death blast is applied unconditionally in Die (Base turret_die has the RadiusDamage variant commented out) — minor over-fidelity, scaled by leftover ammo; not ewheel-specific (framework-level). |