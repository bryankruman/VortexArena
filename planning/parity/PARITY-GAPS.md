# Parity Gaps — ranked by gameplay impact

_Generated 2026-06-27. 465 features with gaps, highest-impact first._

Score weights logic & liveness highest, then values/timing, then presentation/audio, scaled by category (core gameplay > niche entities). Intended divergences excluded.

| # | score | unit | feature | worst | gaps |
|---|---|---|---|---|---|
| 1 | 19.5 | `cts` | `cts.checkpoints.intermediate` | logic:missing | target_checkpoint and trigger_race_checkpoint are consumed as no-ops for CTS (GameWorld:3207-3208 documents them as Race-only); defrag-style maps lose ordered checkpoint enforcement.; No 'went backwards' detection, no spawnflag-4 backward-damage, no per-checkpoint split times/records, no .defragcp order file. Wave-5 added per-checkpoint split records to the Race port (Race.cs RecordCheckpointSplit / _checkpointRecords), but Cts.cs got no equivalent — CTS remains a single start->stop course with no intermediate checkpoints. |
| 2 | 19.5 | `physics-movetypes` | `physics-movetypes.matchticrate.interp` | logic:missing | STILL OPEN: No Movetype_Physics_MatchTicrate equivalent and no tic_* snapshot fields on Entity (grep: zero hits in src/ for MatchTicrate/tic_origin/tic_velocity/tic_flags/sloppy). Entities advance only on the fixed server tick, with no between-tick interpolation in this layer. In Base this smooths CSQC gibs/casings/projectiles/trains at render rate; whether visible in the port depends on its separate render-side interpolation (PredictionBuffer/ClientNet — out of this unit's scope). |
| 3 | 19.5 | `weapon-hook` | `weapon-hook.presentation.rope_line` | logic:missing | No hook ROPE line is drawn. There is no GrapplingHookSend/ENT_CLIENT_HOOK stream (with hook_start/hook_end), no Draw_CylindricLine/Draw_GrapplingHook, and no team-coloured rope texture/WarpZone-traced segment in game/ (all Draw_CylindricLine hits in game/ are unrelated beam/laser/porto renderers). The chain HEAD is networked + rendered as a Hookbomb spike while in flight (and disappears once latched), but the connecting rope between the firer's gun and the latch point is entirely absent. |
| 4 | 19.5 | `weapon-porto` | `weapon-porto.presentation.trajectory_preview` | logic:missing | While holding porto (non-secondary mode), Base draws a reflecting red/blue cylindric-line polyline (with the same slick-continue / noimpact-stop / CheckWireframeBox size checks the placement uses) predicting where the in/out portals will land. The port STILL renders nothing — the signature porto view feature is absent. |
| 5 | 18.0 | `clanarena` | `clanarena.spectate.force_spectate_cmd` | logic:missing | Wave-16/17 added the logic (OnSpectateCommand force-spectates + sends INFO_CA_LEAVE when sv_spectate is on) and a server adapter (ClientManager.GametypeSpectateCommand at ClientManager.cs:515). BUT GametypeSpectateCommand has ZERO callers anywhere in src/ or game/ — the `spectate` client-command handler never routes through it. So the CA force-spectate + CA_LEAVE notice is unreachable dead code. The CA_LEAVE notification string (NotificationsList.cs:703) is still never sent on the live path. |
| 6 | 18.0 | `freezetag` | `freezetag.frags.center_message` | logic:missing | STILL OPEN. The infrastructure now ALL exists and is live: the MutatorHooks.FragCenterMessage chain (MutatorHooks.cs:616), the FRAG_FREEZE/FRAGGED_FREEZE choices (NotificationsList.cs:1292), and FragCenterMessage.Call firing on every frag (Scores.cs:809, whose comment explicitly anticipates FreezeTag swapping in the freeze lines + suppressing when the victim was already frozen). The ONLY missing piece is the producer: FreezeTag.Activate subscribes no FragCenterMessage handler (grep across GameTypes/ → none), so fcm.AttackerChoice/TargetChoice stay the generic FRAG/FRAGGED and the personal 'You froze X' / 'You were frozen by Y' centerprint never fires. The kill-feed INFO lines (INFO_FREEZETAG_FREEZE/SELF) DO fire (covered by freezetag.presentation.notifications). |
| 7 | 17.25 | `freezetag` | `freezetag.presentation.ice_model` | logic:missing | No distinct ice block entity (random 1-of-20 frame, team-colored, following the player at origin-16) — frozen players only get an icy-blue colormod on their normal model. |
| 8 | 17.25 | `onslaught` | `onslaught.presentation.csqc` | timing:missing | LANDED (W16/W17, server-authoritative + live-rendered): generator/control-point/icon WORLD MODELS now assigned (Entity.Model + EF_LOWPRECISION) and networked+rendered via the entity feed (ClientEntityView.ApplyIdentity, the CTF-flag pattern); progressive DAMAGE-MODEL swaps (GeneratorModelForBucket 0..10, IconModelForFraction 75/50/25%) + the pad PAD1->PAD2 capture swap; the generator damage FRAME (Frame=10*(1-hp/max)); the RADAR LINK lines round-trip (CollectRadarLinks -> SendRadarLinks -> ClientNet.RadarLinks -> RadarPanel.DrawLinkQuad, rendered); the generator/CP WAYPOINT SPRITES (CollectWaypoints -> SendWaypoints -> WaypointSpriteLayer/RadarPanel). Models resolved server-side approximate what CSQC generator_damage/cpicon_damage would derive from the networked hp fraction.; STILL MISSING (untouched by W16/W17): icon BOB/SPIN/PUNCH-on-hit animation (timing missing); the objective DEATH-CAM (ENT_ONSCAMERA/WantEventchase/ons_camSetup) on round loss; the generator EXPLOSION sequence (rays/shockwave/gibs/electricity); all particle EFFECTS (EFFECT_SPARKS/CAP/RAGE/ROCKET_EXPLODE — audio cues emit elsewhere but particles do not); the build-bar/health-bar alpha overlay. liveness PARTIAL because the model/sprite/radar producers are live but the animation/death-cam/explosion/particle half has no code at all. |
| 9 | 16.5 | `freezetag` | `freezetag.bots` | values:missing | Bots have no FT-specific freeing/offense roles — they won't deliberately route to and revive frozen teammates (havocbot_role_ft_freeing/offense + havocbot_goalrating_ft_freeplayers absent).; The frozen-target ATTACK gate (Bot_ForbidAttack) IS effectively covered: BotBrain.ShouldAttack (BotBrain.cs:793) skips a target carrying the shared Frozen status effect, so bots don't waste fire on frozen enemies. The dedicated FT Bot_ForbidAttack hook is not registered, but the generic frozen-skip yields the same outcome. |
| 10 | 16.5 | `mutator-nades` | `mutator-nades.presentation.client` | logic:missing | No nade projectile model/trail (nade_customize / nade_spawn CSQCProjectile + Nade_TrailEffect), no orb 3D model (orb_draw), no orb 2D full-screen color flash (orb_draw2d / hud_colorflash_alpha), no darkness blind overlay (HUD_DarkBlinking) + SND_BLIND, no spawn-loc EF_STARDUST render.; No DrawAmmoNades (cl_nades.qc:86): the ammo-panel bonus-nade count + progress bar + (rainbow) type icon is not drawn.; The held-nade charge ring (CrosshairPanel.NadeTimer) is wired + networked AND now functional: a nade can finally be primed (input wired in Wave-5), so NadeTimer climbs 0->1 and the ring appears during a charge. |
| 11 | 16.2 | `sv-mapvoting` | `sv-mapvoting.vote_scoreboard_hide` | logic:missing | The scoreboard-hide EFFECT is achieved a DIFFERENT way: UpdateScoreboard models Base's intermission==2 by hiding the scoreboard whenever the MapVote panel is Visible (mapVoteShowing → intermissionShow = MatchIntermission && !mapVoteShowing), so the player does not see the scoreboard during the vote (the visible outcome of Base's SVC_FINALE). Presentation goal met.; But Base's actual MECHANISM is absent: it forces RES_HEALTH=2342 on every client (incl. bots) and sends SVC_FINALE "" once. The port models neither (logic/values/timing missing). The -2342 refs in GameWorld/NetGame are the separate INTERMISSION sentinel (FixIntermissionClient), explicitly NOT reproduced.; Base also zeroes CS(it).impulse each tick after reading it; the port reads the impulse synchronously in DispatchImpulse (no stored per-tick impulse field), so this specific clear is moot. |
| 12 | 15.75 | `tdm` | `tdm.rules.map_support` | liveness:dead | m_isAlwaysSupported (auto-offer TDM on maps with >=8 spawnpoints AND diameter>3250) is still entirely unimplemented (no BSP world-bounds/spawn-count probe in the menu's file-only mapinfo layer).; m_isForcedSupported (g_tdm_on_dm_maps -> DM maps also support TDM) now has CODE — MapInfoBackend.ApplyForcedGametypes adds 'tdm' to a DM map's supported set when forceTdmOnDmMaps — but it is DEAD: (a) no caller constructs MapInfoBackend anywhere in src, and (b) the ForceTdmOnDmMaps flag defaults false and is never set from the g_tdm_on_dm_maps cvar. The Duel.cs:39 comment claiming a live 'MapInfoCache.Get' caller is stale (no MapInfoCache.cs exists). So at runtime g_tdm_on_dm_maps still changes nothing. |
| 13 | 15.6 | `sv-cheats` | `sv-cheats.impulse.clone` | logic:missing | No CopyBody / corpse-clone mechanic; the clone impulses do nothing and sv_clones is unread. Unchanged by Wave-5. |
| 14 | 15.6 | `sv-cheats` | `sv-cheats.frame.drag` | logic:missing | No CheatFrame per-frame hook and no Drag subsystem: cannot pick up/carry/throw entities (cheat or normal-range), no te_lightning1 drag beam. Unchanged by Wave-5.; No dragbox_spawn/dragpoint_spawn/drag_remove/drag_setcnt/drag_save/drag_saveraceent/drag_clear race-checkpoint map-editor commands.; g_grab_range cvar exists (Cvars.cs:395) but is never read (no consumer). |
| 15 | 15.6 | `sv-clientkill` | `sv-clientkill.indicator.entity` | logic:missing | No floating numeric countdown model is attached above the player; nothing is networked for it (KillCountdown drives only the announcer + center-print, explicitly NOT the digit entity — KillCountdown.cs:12-14).; Clones on g_clones ghosts (the IL_EACH(g_clones) killindicator copy on each non-respawnghost clone) are also absent.; The digit-entity colormod tint (black / grey / Team_ColorRGB) is unmodeled. |
| 16 | 14.4 | `sv-client-lifecycle` | `sv-client-lifecycle.connect.fix_client_cvars_welcome` | values:missing | More of the welcome channel exists now: the campaign welcome level-num (SendCampaignWelcome), the non-campaign 'Active modifications:' line (SendMutatorsWelcome → BuildMutatorsPrettyString), the g_mutatormsg server mutator message, the sv_motd MOTD (\n-escapes expanded), and the grappling-hook + offhand-blaster gameplay tips are all surfaced to the connecting client. Still unmodeled: FixClientCvars (cl_jumpspeedcap_min/max, cl_shootfromfixedorigin, prediction/gentle), clientstuff, set _teams_available, Send_NextMap_To_Player, and the ClientInit_misc per-client init entity (armor-blockpercent, damagepush speedfactor, fog, serverflags, g_trueaim_minrange, hook/arc shot origins).; The port pushes a few physics-relevant cvars via per-client replicated-cvar tables; there is no FixClientCvars cvar-push stuffcmd and no per-client ClientInit_misc init-entity carrying the server's damage/fog/trueaim constants. (The `cmd clientversion` request IS now handled — Commands.cs:3248 — and its mismatch nag fires via frame.name_and_version.) |
| 17 | 13.5 | `nexball` | `nexball.hud.modicon` | logic:partial | Power-meter charge bar NOT drawn: STAT(NB_METERSTART) is not networked (GametypeStatusBlock NexBall case writes ONLY the carrier net id), so HUD_Mod_NexBall's triangle-wave progress bar over nb_pb_period has no data and no drawer.; No nexball event-chase (cl_eventchase_nexball — third-person when NOT carrying); FirstPersonView.WantEventChase only handles cl_eventchase_death + Freeze-Tag cl_eventchase_frozen.; Accuracy / item-stats scoreboard panels not hidden for nexball (QC DrawScoreboardAccuracy/DrawScoreboardItemStats return false). |
| 18 | 13.0 | `mutator-buffs` | `buffs.carrier_model` | logic:missing | A player holding a buff still has no colored MDL_BUFF glow model attached: no buffs_BuffModel_Spawn/Update/Think/Customize, no removal on observer/disconnect. EF_NOSHADOW IS set on the player while held (ApplyBuff:461), but the glow model itself is absent. |
| 19 | 13.0 | `mutator-buffs` | `buffs.buff_effect` | logic:missing | No per-carrier Send_Effect particle emission throttled by buff_effect_delay; g_buffs_effects (default ON) is never read. EntityMutatorState declares a buff_effect_delay field but nothing emits the effect. |
| 20 | 13.0 | `mutator-buffs` | `buffs.waypoint` | logic:missing | No WP_Buff radar/HUD waypoint on buff items (no buff_Waypoint_Spawn, no RADARICON_Buff, no build-finished timer); the colored buff icon never appears on radar/screen edge, and there is no per-viewer hide-if-already-held logic. |
| 21 | 13.0 | `mutator-sandbox` | `mutator-sandbox.drag.grab` | logic:missing | STILL MISSING: no Drag/Drag_Begin/Drag_Finish system. ObjectThink computes .Grab (e.Grab, written at SandboxMutator.cs:343-347) but nothing consumes it; +button8 does nothing; objects cannot be physically carried. (grep: no Drag_Begin/Drag_Finish/button8 grab consumer anywhere; e.Grab is written but never read.) |
| 22 | 12.75 | `clanarena` | `clanarena.join.late_join_observer` | logic:partial | FORCE-OBSERVE half is now LIVE+faithful in EFFECT: CanJoin returns !roundStarted, wired to Clients.GametypeJoinGate (GameWorld.cs:2846). While a round is live JoinAllowed (ClientManager.cs:356) refuses the join, so a mid-round joiner stays an observer until the round resets (the QC net outcome of ForbidSpawn + PutClientInServer force-observe). This is the practical observer-until-next-round behavior the prior baseline lacked.; INGAME_JOINING TRACKING half is DEAD: OnJoinLate (which adds the player to _joiningMidRound and sends INFO_CA_JOIN_LATE) is wired to Clients.GametypeOnJoin, but GametypeOnJoin?.Invoke (ClientManager.cs:298) runs only INSIDE the successful-Join path — AFTER JoinAllowed already PERMITTED the join (round not started). And OnJoinLate's host guard (GameWorld.cs:2847) only calls it `if (Rounds.IsRoundStarted)`. Those two conditions are mutually exclusive, so OnJoinLate NEVER executes on the live path. Therefore _joiningMidRound is never populated, INFO_CA_JOIN_LATE is never sent (the notification is registered but has no reachable Send), and the greyout/IsEliminatedPlayer _joiningMidRound branch is dead (see eliminated_greyout). The CA_JOIN_LATE string remains effectively un-sent.; QC Bot_FixCount counts in-game-joined clients (so bots fill mid-round) — not ported. |
| 23 | 12.0 | `clanarena` | `clanarena.mapinfo.always_supported` | logic:missing | Base CLASS(ClanArena) overrides m_isAlwaysSupported to auto-enable CA on any map with >=8 spawnpoints and a world diameter >3250 (so big arena maps offer CA even without an explicit mapinfo gametype tag). The port has no map-support / auto-gametype heuristic for CA (the same gap noted for Duel.cs:37 and Mayhem.cs:296 — a map-pool concern needing BSP world bounds), so CA availability per map is not derived this way. No in-match gameplay impact. |
| 24 | 12.0 | `cts` | `cts.bot.role` | logic:missing | No CTS bot role: bots are not routed to their next-expected race-checkpoint waypoint (with the raw-touch redirect for unreachable checkpoints), so bots do not actually run the CTS course. (CTS does, however, force _independent_players=1, which makes bots hold fire via the BotBrain fire-gate — see cts.mode.register_latch.) |
| 25 | 12.0 | `overkill-weapons` | `overkill-weapons.mode.weapons_activation` | logic:missing | Base has a SECOND activation path: the ok_weapons mutator un-blocks (clears WEP_FLAG_MUTATORBLOCKED on) the five OK weapons whenever g_overkill_weapons evaluates true, EVEN WITHOUT full Overkill mode (g_overkill). This lets a server add the OK weapons to an otherwise-normal game. The port couples OK-weapon availability to OverkillMutator (g_overkill) only (OverkillMutator.IsEnabled = g_overkill!=0); there is no g_overkill_weapons reader anywhere in src/. |
| 26 | 12.0 | `race` | `race.qualifying.mode` | logic:partial | Retract uses Clients.Spawn (a normal spawn), not the Base checkpoint/grid-aware respawn (race_RetractPlayer + spawn_evalfunc by race_place).; independent_players / MAKE_INDEPENDENT_PLAYER (solo scoring isolation) not modeled.; Qualifying checkpoint records + RACE_NET_CHECKPOINT_*_QUALIFYING packets absent. |
| 27 | 12.0 | `tdm` | `tdm.rules.map_team_config` | logic:missing | Custom per-map team declarations are not ported: spawnfunc_tdm_team (map-defined team netname + scoreboard color cnt, team=cnt+1) is ignored, and the mapinfo `teams=` override path (m_parse_mapinfo / m_setTeams writing g_tdm_teams) is not implemented. The port reads team COUNT only from g_tdm_teams / g_tdm_teams_override cvars and always uses the default team set. Maps that rely on tdm_team for non-default team names/colors render with the stock teams. NOT addressed by Wave-2. |
| 28 | 11.7 | `mapobject-misc` | `mapobject-misc.bgmscript.adsr` | logic:missing | No bgmscript ADSR envelope: a func_clientwall / func_pointparticles carrying a .bgmscript key does not animate its alpha / origin / angles / emission to the map's .bgs music script — the prop is static instead of pulsing to the soundtrack. doBGMScript / BGMScript_InitEntity / the .bgs loader are absent. |
| 29 | 11.7 | `warpzones` | `warpzones.client.render` | logic:missing | No Godot SubViewport+Camera3D renders the linked OUT side onto the portal surface; HeroMaterials.BuildPortal (HeroMaterials.cs:168) is a static dark near-black StandardMaterial3D mirror. The warpzone reads as an opaque/wavy wall — you cannot see through it. PVS-fade and the r_water enable HACK are also absent. |
| 30 | 11.7 | `warpzones` | `warpzones.client.fixview` | logic:missing | No client view rotate when the camera straddles a zone, no near-clip push-out (geometry clips at the seam), no roll-kill on a roll-changing crossing, no exterior-model hide. Crossing a warpzone is visually jarring. |
| 31 | 11.7 | `warpzones` | `warpzones.camera` | logic:missing | No func_camera spawnfunc; security-camera dpcamera surfaces render as a dark mirror placeholder (HeroMaterials) rather than a fixed camera view of the target position. |
| 32 | 11.25 | `clanarena` | `clanarena.lifecycle.reset_map` | liveness:dead | Wave-16/17 added ClanArena.ResetMapPlayers (clears the INGAME-joining set, zeroes killcount, snapshots prev_team for the MatchEnd restore). BUT GameWorld.ResetMap's per-gametype switch (GameWorld.cs:4608-4622) has cases for Mayhem/TeamMayhem/Cts/LMS and NO `case ClanArena` — ResetMapPlayers is never called. So none of the QC reset_map_players bookkeeping runs: no killcount reset, no prev_team snapshot (leaving MatchEnd restore a no-op), no INGAME->JOINED flip. The effective one-life-per-round respawn-everyone behavior still holds via the generic round-gated DeadPlayerThink/ResetMap machinery (so logic stays partial, not fully missing), but the CA-specific re-transmute/state is dead.; QC reset_map_global: allowed_to_spawn = true — the port gates spawn on round phase instead (effective), so this half is N/A. |
| 33 | 10.8 | `sv-world-rules` | `sv-world-rules.entity.pingplreport` | logic:partial | Only the packet-loss (PL) half is fed: the port has NO ping feed — the scoreboard ping column is hard -1 (ScoreboardPanel.cs:228/249 'Ping isn't networked per row yet'), and there is no playerstats latency record (the LATENCY_THINKRATE=10s avg-latency accumulation for the xonstat report).; Mechanism diverges: Base uses the TE_CSQC_PINGPLREPORT round-robin entity (one client per think, 3/maxclients s); the port quantizes ENet PacketLoss to a 0..255 byte and folds it into the per-tick scoreboard wire (ScoreRowWire.PacketLossByte), de-quantized client-side for the PL column. |
| 34 | 10.5 | `clanarena` | `clanarena.hud.spectator_info_messages` | logic:missing | Base cl_ca DrawInfoMessages returns true when ISGAMETYPE(CA) && the local player's entcs spec-state is ENTCS_SPEC_IN_SCOREBOARD, which keeps the HUD info-message panel drawing for a CA spectator who is looking at the scoreboard. The port's InfoMessagesPanel (game/hud/InfoMessagesPanel.cs) ports the cl_lms DrawInfoMessages variant (locally-eliminated LMS player) but has NO CA branch and no ENTCS_SPEC_IN_SCOREBOARD spec-state gate, so this CA-specific info-message visibility is not modeled. Minor client-presentation only. |
| 35 | 10.5 | `ctf` | `ctf.leaderboard` | logic:missing | QC the g_ctf_leaderboard subsystem (ClientConnect race_SendAll, GetPressedKeys race_checkAndWriteName, GetRecords) sends + records the persistent cross-session fastest-capture leaderboard via the race record store + ServerProgsDB. The port ports neither the race record store nor any of these CTF hooks, so the persistent leaderboard does not exist (the in-match fastest-cap broadcast in ctf.flag.capture is unaffected). |
| 36 | 10.5 | `freezetag` | `freezetag.presentation.frozen_anim` | logic:missing | Base ft PlayerAnim ORs ANIMSTATE_FROZEN into the player's animation state so a frozen player holds the static frozen pose. The port conveys frozen-ness only via the FrozenColormod tint (freezetag.presentation.ice_model); the frozen animation-state freeze is not applied, so a frozen player's normal animation may keep playing under the tint. |
| 37 | 10.5 | `overkill-weapons` | `overkill-weapons.oknex.zoom` | logic:missing | oknex wr_zoom/wr_zoomdir not ported as an OK-Nex-specific override. At stock balance (secondary==2 = blaster), the QC gate `!WEP_CVAR(secondary)` is false, so ATCK2 does NOT trigger zoom — the OK Nex only zooms via the dedicated zoom button (button_zoom/zoomscript), i.e. the generic per-weapon zoom path. So at stock balance the observable behavior is the generic zoom button only; no OK-Nex-specific divergence is visible.; If a server sets g_balance_oknex_secondary 0, QC would zoom on ATCK2 (and forgo the blaster secondary) — that non-default branch is unmodeled. |
| 38 | 10.4 | `cl-scoreboard` | `cl-scoreboard.block.itemstats` | logic:missing | No item-stats block at all: Base shows a per-item icon grid of pickup counts (g_inventory.inv_items), filtered by itemstats_filter_mask=12. The port has no SetItemStats / DrawItemStats and inventory is not networked for it. |
| 39 | 10.4 | `cl-scoreboard` | `cl-scoreboard.ui.interactive` | logic:missing | No interactive UI: the port scoreboard is a passive overlay. Missing TAB panel cycling (SCOREBOARD↔RANKINGS), arrow-key player/team selection, ENTER/SPACE team-join + spectate, Ctrl+C column-layout cycle, Ctrl+R scores_per_round toggle, Ctrl+T tell, Ctrl+K vote-kick, ESC exit, and the team-selection screen (scoreboard_ui_enabled==2) with its join prompts and world dimming (0.7). |
| 40 | 10.4 | `cl-teamradar` | `cl-teamradar.maximized.map` | logic:missing | Maximized fullscreen radar entirely absent: no m-bind (+hud_panel_radar_maximized → cl_cmd hud radar 1), no fullscreen tactical overview, no separate maximized rotation/zoom/scale runtime, no default-border swap. |
| 41 | 10.4 | `cl-teamradar` | `cl-teamradar.maximized.click` | logic:missing | No clickable ONS spawn-point selection (cmd ons_spawn), no teleport-click, no mouse/key input handling (MOUSE1/MOUSE2/ESC/console-key passthrough), no dead+showscores temp-hide, no 'Click to select…' prompt, no icon hover-glow. |
| 42 | 10.0 | `mutator-invincibleproj` | `mutator-invincibleproj.client.active_mut_flag` | logic:missing | The Base GetActiveMutators-style scan (util.qc:280-330) walks the active_mutators cvar (or, when empty, the live cvars) and calls mut_set_active(MUT_INVINCIBLE_PROJECTILES) + appends the translated name when g_invincible_projectiles is set, so the menu / server-browser 'active mutators' list and any MUT_ bitmask consumer can light up this mutator. The port still has NO active_mutators / mut_set_active / MUT_ flag implementation (only TouchExplodeMutator.cs comments reference the gap; zero impl hits across src+game). |
| 43 | 9.75 | `cts` | `cts.lifecycle.prepare_player` | logic:partial | OnPlayerSpawn now does the run-clock reset (AbandonRun, incl. the .race_movetime accumulator) AND race_checkpoint=-1 (Cts.cs:715-719) — the meaningful half of race_PreparePlayer for a single start->stop course. OnMakePlayerObserver is ported+live (FRAGS_PLAYER_OUT_OF_GAME when the observer holds a ranked time, else FRAGS_SPECTATOR; AbandonRun + race_checkpoint=-1, Cts.cs:625-638). FIXED in Wave-16/17: the AbortSpeedrun hook is now ported (Cts.AbortSpeedrun, Cts.cs:448) and LIVE — dispatched from the impulse-141 (CHIMPULSE_SPEEDRUN) cheat handler (Commands.cs:2145-2151) after a successful Cheats.Speedrun() restore, dropping the in-progress run clock (QC MUTATOR_CALLHOOK(AbortSpeedrun)). Still MISSING: ClientConnect / PutClientInServer hooks (race_SendAll on connect, the PutClientInServer spawn==qualifying re-prepare-vs-race_RetractPlayer branch), the g_allow_checkpoints practice-mode re-prepare, and respawn-checkpoint/spotref recording — so the feature stays partial.; Minor AbortSpeedrun fidelity residual: the port fires cts.AbortSpeedrun whenever Cheats.Speedrun() returns true, which includes the start-solid case (RestorePersonal returns true even when the teleport was skipped). QC fires AbortSpeedrun only inside the teleport-SUCCESS else-branch (not when 'Cannot move there, cheater'). For CTS the hook only drops the run clock and the start-solid restore already invalidates the run, so the divergence is negligible. |
| 44 | 9.75 | `race` | `race.spawn.grid_respawn` | logic:partial | race_place START-GRID ordering still missing: racers do not get ordered start positions in a multi-racer race (no race_place read / info_player_race grid).; trigger_race_checkpoint_verify map-load validation (missing-spawn error) not performed. |
| 45 | 9.75 | `weapon-arc` | `weapon-arc.beam.visual` | logic:partial | FIX since prior audit (Wave 16 b3a9c06): the beam now ALSO emits a CYLINDRIC line. ARC_BEAM_LINE / ARC_BEAM_LINE_HEAL (EffectsList.cs:73-74, isTrail only so the count-0 emission survives the point guard) carry NO effectinfo block and classify as EffectClass.Beam (EffectSystem.cs:727-729), short-circuiting at :492-501 to Beams.Beam(origin, endpos, tint) — a real straight glow cylinder (BeamRenderer.cs:56), networked server->client via EffectNetSink (ServerNet.cs:303) -> RenderSink (ClientWorld.cs:393). The Arc m_color blue (damage) / 0.125,1,0.125 green (heal) tint is passed. The prior 'spark trail only, never reaches BeamRenderer.Beam' gap is CLOSED for the cylinder. The arc_beam SPARK TRAIL (ARC_BEAM/ARC_BEAM_HEAL) is still emitted alongside (Arc.cs:558) — which matches Base, where Draw_ArcBeam draws the cylinder AND trailparticles together.; Residual VALUES gap (now partial, was missing): the cylinder width is a fixed 6f (BeamRenderer.Beam default), NOT Base's per-beam_type thickness (8 normal / 14 burst). No burst-thickness selection.; Residual presentation gaps (UNCHANGED): no spinning MDL_ARC_MUZZLEFLASH flash entity on the beam, no hit/muzzle dynamic lights (ARC_BT_HIT hitlight 20 / muzzlelight 50), no team-color tint (heal vs damage tint IS selected, but not per-team). The beam is drawn STRAIGHT origin->hit-point (BeamRenderer 2-point path), not the curving bezier body — though the gameplay TRACE is now the multi-segment bezier (see beam.dps_core); only the rendered LINE is straight. |
| 46 | 9.6 | `cl-viewmodel` | `cl-viewmodel.anim.networked_frames` | values:missing | No networked weapon fire/idle/reload animation (the QC wframe temp entity is not networked); the gun never visibly cycles or reloads. |
| 47 | 9.6 | `sv-antilag` | `sv-antilag.mode.client_verified_and_cursor` | logic:missing | g_antilag modes 1 (verified client-side) and 3 (prydon cursor hitscan) are unimplemented: a server set to either gets NO lag compensation at all (port treats anything != 2 as no-rewind). |
| 48 | 9.6 | `sv-commands-votes` | `sv-commands-votes.debug_tools.missing` | logic:missing | radarmap is not registered: no in-game radar-image (XPM) generation for the minimap.; gettaginfo / animbench / bbox / trace / stuffto / database / make_mapinfo console verbs are unimplemented (no Register() calls). |
| 49 | 9.45 | `mapobject-teleporters-portals` | `portals.entity.lifecycle` | logic:partial | Porto portals are realised as a seamless two-way warpzone, so there is no portal health (g_balance_portal_health=200) — Base portals can be shot down (Portal_Damage); the cvar is unused.; Portal expiry is now modeled (15s g_balance_portal_lifetime, recharged on each crossing, ExpirePortals sweep at GameWorld.cs:1811) BUT the SUB_SetFade(time,0.5) 0.5s visual fade-out is absent — a timed-out pair is hard-removed, not faded; and the recharge-to-200-health half is moot (no portal health).; No portal model (MDL_PORTAL) / skins (0/1/2) or EF_RED / EF_BLUE\|EF_STARDUST / EF_ADDITIVE glow; a warpzone is a transparent window, not a Quake portal disc.; RE-VERIFIED 2026-06-27 (second pass): SND_PORTO_EXPIRE on natural timeout NOW plays — ExpirePortals -> EmitPortalExpire (Warpzone.cs:691-696) emits porto/expire.wav on CH_SHOTS (-4) at the portal origin for BOTH halves of the pair. Still ABSENT (the shoot-down path, which has no warpzone equivalent without a portal-HEALTH entity): SND_PORTO_EXPLODE + EFFECT_ROCKET_EXPLODE on Portal_Remove(this,1) at health<0. So audio is partial (expire cue live, explode cue missing), not fully missing.; Portal_Customize visibility gating (hiding the portal model from players who can't use it, IS_INDEPENDENT_PLAYER) is absent.; Portal_FindSafeOrigin (move_out_of_solid 16u in front, portal_safe_origin) and CheckWireframeBox 96^3 clearance are not modeled. |
| 50 | 9.1 | `bot-waypoints` | `bot-waypoints.editor` | logic:missing | No in-game waypoint editor: cannot place/remove/hardwire/mirror waypoints (g_waypointeditor*); no editor visualization (waypoint model, link lightning beams, colormod by flag).; No runtime auto-waypointing toward players (botframe_autowaypoints) or useless-waypoint pruning (botframe_deleteuselesswaypoints): the loaded graph is static. |
| 51 | 9.0 | `sv-client-lifecycle` | `sv-client-lifecycle.frame.prethink_misc` | logic:partial | UpdateChatBubble (the chatbubble.spr / minigame_busy bubble over a typing or minigame-busy player, ChatBubbleThink + ShowChatBubble hook) is not modeled — no visible 'is typing' bubble.; The PlayerPreThink +use edge dispatch (PlayerUseKey) is not a single ported function: individual +use consumers (buttons/vehicles/ctf) read the button directly, but the usekeypressed edge tracker + PlayerUseKey routing (and its spectate/minigame branches) are scattered/absent.; SetZoomState (server-side weapon-zoom state for the zoom HUD/FOV net stat) + the taunt_soundtime taunt voice + target_voicescript_next are not driven from a lifecycle PreThink. (The deferred teamkill_soundtime friendly-fire complaint voice IS now fired — PlayerFrameLogic.Regen:130-149.) |
| 52 | 9.0 | `warpzones` | `warpzones.client.teleported_view` | logic:missing | Server never sends a warpzone_teleported entity and never re-rotates v_angle (WarpZone_PlayerPhysics_FixVAngle); the client never rotates VF_CL_VIEWANGLES / CL_RotateMoves on a crossing. The view does not carry through the seam. |
| 53 | 9.0 | `weapon-vortex` | `weapon-vortex.fx.charge_glow` | logic:partial | Glow is applied to the FIRST-PERSON VIEW model (the local player's own gun, via ViewModel.SetTeamGlow), NOT the exterior player/carrier model that QC's wr_glow→weaponentity_glowmod brightens for OTHER players. So the local player's gun now brightens with charge, but a remote observer still sees the carrier identical regardless of charge.; Host-only: VortexCharge is read off LocalServerPlayer's slot state (NetGame.UpdateViewModelTeamGlow), so on a pure remote client there is no charge value and the glow stays at the base team color. Same cross-client-charge-STAT root cause as the charge ring and beam tint. |
| 54 | 8.8 | `cl-teamradar` | `cl-teamradar.cvar.dynamichud` | liveness:dead | hud_panel_radar_dynamichud is registered with the correct default (1) but nothing consumes it: the port has no HUD_Scale_Enable/Disable (the dynamic-HUD shrink applied when the player is alive) so the radar never participates in dynamic-HUD scaling.; HUD_Radar_Export (writing the radar's 8 aesthetic cvars into a saved hud skin file) has no port analogue; the port's skin/HUD-config export path does not round-trip these radar cvars. |
| 55 | 8.0 | `cl-crosshair` | `cl-crosshair.core.origin_chase` | logic:missing | Crosshair is always drawn at the geometric panel centre (DrawPanel: center = Size2*0.5f). The chase-camera branch (trace from player org along view_forward MOVE_WORLDONLY, project the impact, fade the player body alpha toward crosshair_chase_playeralpha while it obstructs the view, restore on chase-off via the crosshair_chase_state latch) is not implemented.; crosshair_chase / crosshair_chase_playeralpha / crosshair_2d are now REGISTERED in RegisterDefaults (CrosshairPanel.cs:323-325) with the Base default values, but have NO reader — they are inert settable cvars (the chase trace + body-alpha fade + side-scroller selection are still unported). |
| 56 | 7.8 | `turret-framework` | `turret-framework.net.sync` | logic:missing | No turret networking: none of the TNSF_* send/parse / CSQC turret edict exists. |
| 57 | 7.7 | `bot-ai` | `bot-ai.scripting.commands` | logic:missing | Bot scripting entirely missing: the bot_cmd command queue does nothing, so map/console bot scripts, cutscene-driven bots, and g_waypointeditor bot driving are inert. (UNCHANGED.) |
| 58 | 7.65 | `mapobject-misc` | `mapobject-misc.models.clientwall_fade` | logic:partial | func_clientwall distance-fade is not rendered: fade_start/fade_end/alpha_max/alpha_min/fade_vertical_offset are parsed onto the Entity (MapObjectFieldsExtra.cs) but NO renderer applies Ent_Wall_PreDraw's distance-fade alpha formula or its PVS alpha-cull — a clientwall meant to fade out with distance stays fully opaque. (The ClientWorld.cs:1234 ItemDistanceAlpha fade is the unrelated item-despawn fade; FadeEndDist is otherwise only read by WeatherSystem for rain/snow draw distance.); The antiwall .inactive flag toggles SOLID server-side but does NOT also force the wall's render alpha to 0 (Ent_Wall_PreDraw sets alph=0 when inactive); a deactivated clientwall goes non-solid but stays VISIBLE until a wall renderer consumes .inactive. |
| 59 | 7.65 | `mapobject-teleporters-portals` | `portals.weapon.hook_and_predict` | values:missing | Base Portal_Think predicts a player/hook crossing the portal plane THIS frame (Portal_WillHitPlane, PHYS_GRAVITY-aware tracebox over velocity*2*frametime, portals.qc:237/317) and teleports pre-emptively so fast movers don't tunnel; the warpzone realisation relies on a fixed trigger overlap + a same-frame finishtime guard + plane-side test, with no velocity*2*frametime predict-ahead.; A player's active grappling hook is still not teleported through the portal (Portal_Think iterates weaponentity.hook); the warpzone path does not relocate the hook entity. |
| 60 | 7.5 | `clanarena` | `clanarena.matchend.restore_status` | logic:partial | Hook is now SUBSCRIBED + FIRED LIVE: ClanArena.Activate adds OnMatchEndBeforeScores to MutatorHooks.MatchEndBeforeScores (ClanArena.cs:299-300); the host fires it via MutatorHooks.FireMatchEndBeforeScores (GameWorld.cs:3954). BUT the handler only clears _joiningMidRound + _prevTeam, and BOTH of those are never populated on the live path (_joiningMidRound is dead per late_join_observer; _prevTeam is written only by the DEAD ResetMapPlayers, see lifecycle.reset_map). So the actual QC MatchEnd_RestoreSpectatorAndTeamStatus(prev_team) — restoring each player's pre-CA observer/team from the snapshot — is a NO-OP: there is nothing to restore. |
| 61 | 7.5 | `ctf` | `ctf.abort_speedrun` | logic:missing | QC the ctf AbortSpeedrun hook: when a player aborts a speedrun (CTS/race-style speedrunning) while carrying a flag, the flag is force-respawned (ctf_RespawnFlag) and INFO_CTF_FLAGRETURN_ABORTRUN is broadcast. The port has no AbortSpeedrun dispatch (the speedrun/CTS abort path is not wired into CTF), so a carried flag is not auto-returned on a speedrun abort. |
| 62 | 7.5 | `ctf` | `ctf.drop_special_items` | logic:partial | QC the ctf DropSpecialItems hook throws a carried flag (ctf_Handle_Throw DROP_THROW) when the generic DropSpecialItems event fires (e.g. nade-drop / forced item-drop). The port drops a carried flag on death (OnDeath -> DropFlag) but has no DropSpecialItems hook, so any non-death special-item-drop path that exists for other items does NOT also eject the flag. |
| 63 | 7.5 | `ctf` | `ctf.followfc_superspec` | logic:missing | QC the ctf SV_ParseClientCommand hook handles the spectator 'followfc [team]' command (gated by g_superspectate): a superspectator jumps to spectating the/an active flag carrier (optionally of a given team), or gets a 'No active flag carrier' message. The port has no followfc command and no superspectate subsystem. |
| 64 | 7.5 | `freezetag` | `freezetag.damage.softkill_void` | logic:partial | STILL OPEN — no soft-kill relocate path (g_frozen_damage_trigger 0): the NEEDKILL soft-kill (restore HP=1+armor, teleport to a spawn point, no weapon reset) in PlayerDies / the Damage_Calculate relocate branch is absent — FT.OnDamageCalculate leaves NEEDKILL deathtypes alone (frozen player dies in void/lava), and the relocate needs the server spawn selector (intentionally NOT done in the gametype layer). Base default cvar is 1 = die, so this branch is dormant at stock cvars regardless.; Fall-damage revive (g_frozen_revive_falldamage) is implemented and live (FreezeTag.cs:507-515): a hard enough DEATH_FALL hit thaws the player to falldamage_health (40) and sends FREEZETAG_REVIVED_FALL/_REVIVE_SELF. Default off, so dormant at stock cvars but correct when enabled.; Nade self-revive is now FULLY implemented (FIX since baseline): FreezeTag.OnDamageCalculate (FreezeTag.cs:485-499) Unfreezes the player, restores g_freezetag_revive_nade_health (40), suppresses damage/force, and sends INFO_FREEZETAG_REVIVED_NADE + CENTER_FREEZETAG_REVIVE_SELF. g_freezetag_revive_nade ships 1 so it is live. (NadesMutator still also zeroes the same hit, but the actual unfreeze now lives in FT, hook-order-independent.) |
| 65 | 7.5 | `keyhunt` | `keyhunt.capture.instant_path_dead` | liveness:dead | AssignKey (KeyHunt.cs:415) routes to CheckCaptureGeometry() — the maxdist-gated path — so it would score CORRECTLY if a future caller picked it. It remains dead (zero source callers; the live collect path is KeyTouchEntity->CollectKey->CheckCaptureGeometry). Harmless dead code, no mis-score hazard. |
| 66 | 7.5 | `mutator-buffs` | `buffs.item.auto_spawn` | logic:partial | Default count is 0 so this never runs unless explicitly configured (matches Base default) — this stays liveness:partial. (The map item_buff_* spawnfuncs are now the primary obtain path; see buffs.item.spawnfunc.); QC defers via InitializeEntity(buffs_DelayedInit, INITPRIO_FINDTARGET) and sets spawnflags\|=64 (always randomize/relocate) on each seed; port sets BuffAlwaysRelocate=true on each auto-seed (SpawnInitialBuffs), then routes through ConfigureBuffEntity (which now sets the relic model + box + cooldown + think + touch, same as the map spawn). Still spawns immediately at Hook() (not via the INITPRIO_FINDTARGET defer). Residual presentation gap: per-buff skin/color/glowmod still not applied, and no waypoint. |
| 67 | 7.5 | `mutator-buffs` | `buffs.item.random_location` | logic:partial | MoveToRandomMapLocation relocation IS done (BallEntity.RandomMapLocation, which folds in the SelectSpawnPoint fallback), with the '0 0 200' launch + a mins*1.5/maxs*1.5 tracebox unstick + g_buffs_random_lifetime arming + SND_KA_RESPAWN on relocation. The g_buffs_randomize teamplay gate (& (teamplay?2:1)) and g_buffs_random_location/spawnflag-64 relocate gate are honored.; Still missing: g_buffs_random_location_attempts is not read (RandomMapLocation uses its own fixed sampling rather than the attempts/256/1024 box-content rejection of MoveToRandomMapLocation), so the relocation's content-validity check differs from QC. No EFFECT_ELECTRO_COMBO x2 visual at the relocation, no WaypointSprite_Ping. |
| 68 | 7.5 | `weapon-crylink` | `crylink.reload` | liveness:dead | Clip-load reload semantics: WrReload (the base WeaponFireGate machine) reloads the generic clip; QC's wr_reload also passes SND_RELOAD. The reload SOUND cue (SND_RELOAD) is whatever the base reload machine plays — not crylink-specific, faithful by inheritance. No crylink-specific reload gap remains. |
| 69 | 7.5 | `weapon-electro` | `electro.primary.midaircombo` | liveness:dead | DEAD on the live path: midaircombo_radius defaults 0 (bal-wep-xonotic.cfg:232), so the BoltThink midair branch (Electro.cs:323-334 -> MidairTriggerCombo) never runs under stock xonotic balance. The branch IS reachable (and now faithful) under samual/xdf balance where the cvar is 100.; IS_INDEPENDENT_PLAYER orb-owner skip is ported (Electro.cs:356) but Entity.IsIndependentPlayer is hardwired false, so the guard is dead-OFF-equivalent (no independent-player mode is ported). Harmless: default modes have no independent players. |
| 70 | 7.5 | `weapon-machinegun` | `machinegun.fire.single_mode0` | liveness:dead | mode 0 is not the bundled default (mode 1), so AttackSingle/AttackFrame/the snipe are dead on the live path — matches Base, where the xonotic balance also defaults mode 1 (only bal-wep-nexuiz25.cfg sets mode 0). |
| 71 | 7.5 | `weapon-machinegun` | `machinegun.reload.checkammo` | liveness:dead | Inert at stock balance only: reload_ammo defaults 0 in bal-wep-xonotic.cfg, so the forced-reload branch (WrThink:120 gated on ReloadingAmmo()!=0) and the clip cycle never run in stock play — identical to Base, where reload is likewise off. The code is now fully present and faithful, so liveness is 'dead' purely because no stock cvar reaches it; a server setting reload_ammo>0 exercises the same clip/reload behavior as Base. |
| 72 | 7.5 | `weapon-porto` | `weapon-porto.fire.aim_hold` | liveness:dead | Only reachable when g_balance_porto_secondary=0 (non-default); dormant in stock play (liveness:dead). |
| 73 | 7.2 | `cl-scoreboard` | `cl-scoreboard.field.name_icons` | logic:partial | Handicap icon is PORTED + LIVE (DrawRow:1075-1083 draws a square at the EXACT QC tint '1 0 0' + '0 1 1' * ((16-lvl)/15), white@1 → red@16, fed by the networked handicap_level).; Still missing: player-color shirt/pants swatch (gfx/scoreboard/playercolor_*), pulsing ready check, ignored icon, the pulsing (Q) wants-join team marker, and the playerid '#N' prefix (Scoreboard_AddPlayerId).; Handicap icon is drawn as a flat colored square (no gfx/scoreboard/player_handicap texture); presentation faithful in tint but not in the actual icon art. |
| 74 | 7.2 | `cl-view` | `cl-view.demo.free_camera_lockview_ortho` | logic:partial | Demo free/chase camera (CSQC_Demo_Camera) not ported (tracked separately on the demo-cinematics branch).; Orthographic radar-render view (cl_orthoview / View_Ortho) not ported (dev/radar tool).; viewloc 2.5D side-scroller camera (GetViewLocationFOV) not ported (niche 2.5D-map feature).; FPS report to the server (fpscounter_update STAT(SHOWFPS)) not ported.; cl_lockview: only the bare cvar lock_type is honored; the UI-forced lock_type=1 conditions (hud_configure / clickable radar / minigame menu / quickmenu) are not on the net play path so they are not modelled (documented in ApplyViewLock). |
| 75 | 7.2 | `mutator-multijump` | `mutator-multijump.counter.networking` | logic:unknown | Whether the predicted client sees a correctly server-synced MultijumpCount (vs a server-only tally) was not traced; a desync could cause a predicted re-jump to be granted/denied differently from the server near the count limit. |
| 76 | 7.2 | `sv-client-lifecycle` | `sv-client-lifecycle.join.join_allowed` | logic:partial | MIN_SPEC_TIME + (teamplay && lockteams) + Player_GetForcedTeamIndex==TEAM_FORCE_SPECTATOR + g_playban (ForcedSpectate host hook→Bans.IsPlayBanned) + a gametype ForbidSpawn/late-join gate (LMS lockout, GametypeJoinGate) ARE enforced. Still missing: version_mismatch, g_maxping (ping<=0 too-soon + ping>maxping), and the whole QueueNeeded/queuePlayer/ShowTeamSelection + nJoinAllowed/GetPlayerLimit/g_maxplayers/bots_would_leave capacity gate.; No CENTER_JOIN_PREVENT / CENTER_JOIN_PREVENT_VERSIONMISMATCH / CENTER_JOIN_PLAYBAN / CENTER_JOIN_PREVENT_PING / CENTER_TEAMCHANGE_LOCKED centerprints for blocked joins. |
| 77 | 7.0 | `items-pickups` | `items-pickups.itemgroup.networking` | logic:missing | Small-item clustering is not ported: Base groups small health/armor/ammo within 120qu into one item_group (networked via item_group/item_group_count) so the itemstime HUD can show a single 'xN' badge for a tightly-packed cluster instead of overlapping timers. The port has no item_group field, no setItemGroup/setItemGroupCount pass, and the itemstime producer (ItemstimeMutator) doesn't aggregate clustered smalls. Presentation-only effect on the timed-item HUD. |
| 78 | 7.0 | `mutator-bloodloss` | `mutator-bloodloss.presentation.active_mutator_signalling` | logic:partial | The util.qc:309 X("Blood loss", MUT_BLOODLOSS, g_bloodloss>0) GameType active-mutator row and the CSQC mut_set_active(MUT_BLOODLOSS) HUD active-mutator icon are STILL entirely unported — the port has no MUT_BLOODLOSS / mut_set_active / active_mutators flag surface at all, so the menu/server-browser HUD active-mutator ICON can never light up (the textual 'modifications' string IS now surfaced, but not the bitmask/icon).; BuildMutatorsString (':bloodloss') is LIVE but only via the gamelog/eventlog feed (sv_eventlog-gated) and the 'modifications' serverinfo; it is not consumed by an in-game scoreboard. |
| 79 | 7.0 | `mutator-buffs` | `buffs.observer_cleanup` | logic:missing | Still no MakePlayerObserver / ClientDisconnect handler to run buffs_RemovePlayer (delete the carrier buff_model). Moot only because the buff_model glow itself is unimplemented (buffs.carrier_model). |
| 80 | 7.0 | `mutator-touchexplode` | `touchexplode.list.activemutators` | logic:missing | Base appends 'Touch explode' to the active_mutators() X-table string (util.qc:315, MUT_TOUCHEXPLODE bitfield) used for the server-browser mutator field and the scoreboard/info mutator list. The port has no active_mutators()/MUT_-bitfield string builder. NOTE: the port's BuildMutatorsString/BuildMutatorsPrettyString hook infra (MutatorActivation.cs) is a DIFFERENT mechanism (the per-mutator server-info/scoreboard token chain), and Base touchexplode registers NO BuildMutatorsString hook, so that infra does not advertise touchexplode and does not close this gap. Systemic gap shared by all mutators; only the create-game menu checkbox (DialogMutators.cs:132) exists. |
| 81 | 6.75 | `keepaway` | `keepaway.ball.drop` | logic:partial | The drop velocity includes the crandom xy scatter (DropOffsetVelocity == Base '0 0 200' + '0 100 0'*crandom + '100 0 0'*crandom; Prandom.Signed == crandom); the 0.5s previous_owner/wait self-recapture lockout is armed (GtNextTakeTime + Owner=carrier).; SND_KA_DROPPED plays globally (PlayGlobal KA_DROPPED, ATTEN_NONE); INFO_KEEPAWAY_DROPPED (all) + CENTER_KEEPAWAY_DROPPED (all) notifications sent and registered.; ALL FOUR drop triggers are wired live (RE-VERIFIED Wave 16/17). DEATH (OnDeath → Drop, Combat.Death bus); USE-KEY (OnUseKey on MutatorHooks.PlayerUseKey, fired live via VehicleBoarding.cs:106 FirePlayerUseKey — the +use impulse tail that runs only when the press neither boarded nor exited a vehicle); DISCONNECT + OBSERVE (OnPlayerRemoved, fired live at ClientManager.cs:549 PutObserverInServer AND :837 ClientDisconnect path). QC's ClientDisconnect/MakePlayerObserver both ka_DropEvent.; STILL OPEN (logic): no nudgeoutofsolid_OrFallback after placing the dropped ball; no multi-ball chain advance / ka_PlayerReset chain semantics (single ball only).; PRESENTATION note: QC ka_DropEvent emits NO drop particle (spec drop algorithm lists only the WP spawn) — so 'missing' here means only the WP_KaBall sprite, which IS now re-spawned via CollectWaypoints when the ball is loose (see keepaway.waypoints.tracking). There is no missing drop-particle emit in Base to port.; NOTE: QC also drops via DropSpecialItems (the generic special-item drop hook); the port has no DropSpecialItems subscription, but death/disconnect/observe/use-key already cover every drop trigger DropSpecialItems would, so this is redundant rather than a gap. |
| 82 | 6.75 | `overkill-weapons` | `overkill-weapons.shared.secondary_blaster_jump` | logic:partial | refire_type==0 secondary path (blaster on the shared ATTACK_FINISHED timer) is not handled; only refire_type==1 is. Default is 1 for all five OK weapons, so no observable gap at stock balance.; FIRE2 weapon animation / animdecide_setaction not driven (no third-person fire-2 pose). |
| 83 | 6.6 | `sv-teamplay` | `sv-teamplay.balance.autobalance_bots` | logic:partial | Steady-state poll runs on a 3s cadence (BalanceTeamsTick, GameWorld.cs:3319-3321) where Base autobalances per-event; the cvar gate is now GONE (the poll is unconditional, stopped only by GameStopped/intermission, matching QC's commented-out gate). A synchronous compensation also fires on every human team-change (KillCountdown.PerformTeamChange, GameWorld.cs:959-980), so the only timing divergence left is the 3s polling granularity.; Uses frag score (ScoreFrags), not SP_SCORE, to pick the bot to move; ordering can differ when frags != composite score. |
| 84 | 6.6 | `sv-world-rules` | `sv-world-rules.misc.redirection` | logic:partial | Only the core quit_and_redirect path is ported (read at match-end map flow, RedirectHandler -> MenuCommand.Connect reconnects the local/listen client). MISSING: the 'self' restart variant (redirection_target == "self" -> disconnect; defer reconnect), the cvar_set sv_public -2 advertise-suppression, the per-second retry loop with redirection_timeout/redirection_nextthink (Base tries twice to catch slow clients), the quit_and_redirect_timer defer delay, and the localcmd quit once all clients are gone. |
| 85 | 6.6 | `turret-hk` | `turret-hk.menu.describe` | logic:missing | The MENUQC HunterKiller.describe PAGE_TEXT body is stored as a plain HkTurret.Description string (with the %s name slots resolved literally to 'Hunter-Killer Turret' / 'Devastator'), but the port has no live turret-info/describe page system that renders it, so it is never displayed. |
| 86 | 6.6 | `weapon-tuba` | `weapon-tuba.audio.note_sound` | values:partial | Audible result NOT runtime-verified: this is a headless code read. The server-side emission carries the loop+pitch (SoundService.Play -> SoundEvent(Loop,Pitch), EngineServices.cs:615), but whether the CLIENT renderer actually applies the pitch shift on a looping (entity,channel) REPLACEMENT — vs treating a same-(entity,channel) re-emit as idempotent and ignoring the new pitch — is unobserved. Needs a behavioral/listen check before audio can be called faithful.; No two-sample cos/sin crossfade: Base's in-range branch plays the floor sample at cos(pi/2 m/step) AND the upper neighbour at sin(...) for a smooth timbre blend; the port plays only the floor neighbour at full volume (residual PITCH is shifted via 2^(m/12), but the upper-neighbour timbre blend needs a second emitter and is unported).; No cl_tuba_fadetime release ramp: NoteOff Stops the loop cleanly rather than ramping volume down over 0.25s (needs a client-side per-frame volume think).; No ENT_CLIENT_TUBANOTE networking: remote players' tuba notes are not driven by a dedicated note entity (no per-listener re-origin / SendFlags broadcast). |
| 87 | 6.5 | `mutator-buffs` | `buffs.disability` | logic:partial | Movement-speed slow (Stunned x0.7 via SpeedMultiplier, OnPlayerPhysics:678-680) is LIVE.; Weapon fire-time slow (WeaponRateFactor x1.5) is LIVE: OnWeaponRateFactor (691-697) multiplies args.Factor by 1.5 for a stunned player; WeaponRateFactor.Call has a live site (WeaponFireGate).; STILL MISSING: weapon projectile-speed slow (WeaponSpeedFactor x0.7) and monster run/walk slow (MonsterMove x0.7) — BuffsMutator subscribes neither hook (no WeaponSpeedFactor mutator-hook chain exists; MonsterMove is unsubscribed). |
| 88 | 6.3 | `warpzones` | `warpzones.combat.traceextras` | logic:missing | Grenade/toss trajectory prediction (WarpZone_TraceToss) and projectile particle trails (WarpZone_TrailParticles[_WithMultiplier]) do not bend through portals — bot grenade aim and trail visuals are wrong across a warpzone. Minor. |
| 89 | 6.0 | `cl-crosshair` | `cl-crosshair.per_weapon_2d_vehicle` | logic:partial | Per-weapon crosshair: PerWeaponNumber dict is never populated by any caller (grep finds zero assignments outside the property def CrosshairPanel.cs:97), so every weapon shows the same `crosshair` number (Base maps each weapon to its own w_crosshair pic + w_crosshair_size multiplier).; crosshair_2d (side-scroller, default 54) is now REGISTERED (CrosshairPanel.cs:323) but has no reader — the FREEAIM/viewloc side-scroller crosshair selection is unported. HUD_Crosshair_Vehicle (info.vr_crosshair) has no port. |
| 90 | 6.0 | `cl-csqcmodel` | `cl-csqcmodel.effects.ef_mf_trail_lights` | logic:partial | MF_ROCKET is LIVE (ClientWorld.DriveCsqcModelHooks composes ForcedAppearance(jetpackActive:true) from e.UsingJetpack -> CsqcModelEffects.Apply -> modelFlags\|=MF_ROCKET drives both the rocket trail return and the jetpack loop). The OTHER MF trails (grenade/gib/tracer/zomgib/tracer2/tracer3) are mapped in CsqcModelEffectFlags but never supplied — base modelFlags is still 0 — so non-jetpack MF-driven trails never fire on this path.; Strength/Shield role-glow is LIVE via PowerupsMutator (EF_BLUE/EF_RED ORed into player.Effects server-side, networked normally, rendered as the EF_BLUE/EF_RED dynamic lights). The dead-and-redundant code is the unused CsqcModelEffectFlags.RoleGlowFlags helper (colors inverted) + the ComposeForcedAppearance strength/shield args.; STILL OPEN (forced glowmod DEAD): the live composition passes forcedGlowmod (-1,-1,-1) = unset; ApplyForcedGlowmod has zero callers (re-verified 2026-06-27 Wave16/17: still only the definition at CsqcModelEffects.cs:228, sole references are planning docs).; FIXED (drift 2026-06-27 Wave16/17): EF_BRIGHTFIELD on a player model now trails — CsqcModelEffects.Apply returns TR_NEXUIZPLASMA and ClientWorld.DriveModelTrail (ClientWorld.cs:1162) spawns the per-segment trail on the live DriveCsqcModelHooks pass; the trail return is no longer discarded.; Render-flags (additive/fullbright/nodepthtest) only apply to per-instance BaseMaterial3D overrides, not shared/cached shader materials.; MF_ROTATE spin is not applied (no caller supplies the bit).; boxparticles volume emission (absmin..absmax) is reduced to a point-burst at the entity origin for EF_FLAME/EF_SHOCK/EF_STARDUST. |
| 91 | 6.0 | `cl-csqcmodel` | `cl-csqcmodel.networking.csqcmodel_contract` | logic:partial | Now WIRED end-to-end: alpha (EntityField.Alpha 1<<17), UsingJetpack (NetEntityFlags 1<<8 -> client MF_ROCKET), the upper-body anim ACTION block (EntityField.AnimAction 1<<18 -> UpperAction/AnimActionTime), and the exterior-weapon (wepent) block (EntityField.Wepent -> SwitchWeapon/SwitchingWeapon/WepPhase/ViewmodelSkin/GunAlign/WepAlpha).; STILL NOT networked (unchanged-missing): colormap proper (only a team id is sent), the rest of modelflags (non-jetpack MF_* still 0), traileffect, non-player glowmod, the per-entity colormod (sent for EVERY entity in Base), scale, v_angle, and the anim_state/anim_lower_action LOCOMOTION block (the lower body is inferred client-side, as Base does).; Consequence chain (each also tracked in its feature row): no colormod => no server-driven per-model tint; no scale => no resized models; no traileffect/non-jetpack modelflags => no non-rocket MF trails; no v_angle => aim pitch uses body angles; no networked colormap => force-color path leans on a reconstructed team id. |
| 92 | 6.0 | `deathmatch` | `deathmatch.scoring.teamkill_punishing` | logic:partial | DM is FFA so this never fires in practice; the escalating teamkill penalty is unmodeled, but unreachable in deathmatch (relevant only to team modes). |
| 93 | 6.0 | `duel` | `duel.mapsupport.gating` | logic:partial | FIXED (m_isForcedSupported half): MapInfoBackend.ApplyForcedGametypes (MapInfoBackend.cs:134-141) promotes any DM map to also support duel unless g_duel_not_dm_maps, called live from MapInfoBackend.Get (MapInfoBackend.cs:118) and wired from the menu cvar at CreateGameScreen.cs + DialogCreateGameMapInfo.cs. Unit-tested in MenuDataSourceTests.cs (both g_duel_not_dm_maps=0 forces-in and =1 excludes). Matches QC MapInfo_Get_ByName FOREACH(Gametypes, it.m_isForcedSupported...).; STILL OPEN (m_isAlwaysSupported half): no port logic gates duel auto-support by map size (diameter<3250). grep 3250 / m_isAlwaysSupported / diameter over src -> only doc-comments (Duel.cs, Mayhem.cs). A too-large map could still be offered for duel. Requires BSP world bounds not available in the menu's file-only mapinfo parsing layer (per Duel.cs:36-38 note). UNCHANGED by Waves 16-17. |
| 94 | 6.0 | `lms` | `lms.lives.starting_count` | logic:partial | Base derives lives from the per-map mapinfo string `lives=N` (default 5) with `m_legacydefaults "9 20 0"` selecting legacy=9 when Nexuiz-era maps are loaded (lms.qh:11/22). The port reads g_lms_lives_override (>0 wins), else the live fraglimit cvar (the faithful stand-in for `lives=N` since Base sets fraglimit via GameRules_limit_score from mapinfo), else DefaultLives=5. The g_lms_lives NON-EXISTENT cvar claim in the prior note was stale — the port does NOT read it (CvarLivesDefault = g_lms_lives_override). Remaining gap: (1) the per-map `lives=N` mapinfo key is not parsed by MapInfoBackend.cs (which only parses title/author/gametype-support, not game-parameter keys); (2) the m_legacydefaults 9-lives switch has no legacy-mode detection in the port. STRUCTURAL: both require mapinfo game-parameter plumbing not yet in scope. Default 5 lives and explicit override (g_lms_lives_override) land correctly. |
| 95 | 6.0 | `mutator-physical_items` | `mutator-physical_items.item_spawn.ghost_entity` | liveness:dead | setSendEntity(item, func_null) is not ported — the real item's net-send is not explicitly suppressed; the port relies on EF_NODRAW for client invisibility. Minor net-presentation divergence (item stays in the entity feed but draws nothing).; DropToFloor_QC_DelayedInit is not called for map-item ghosts; the port assumes the Physics/TOSS integrator settles the ghost on the next step. With no ODE integrator wired this never matters today (mutator is dead), but it is a settle-behavior divergence if the gate were ever opened.; Ghost colormap/glowmod are NOT copied from item.owner at spawn (QC sv_physical_items.qc:115-116 wep.colormap = item.owner.colormap; wep.glowmod = item.owner.glowmod) — cosmetic ghost-tint divergence, same class as the per-think colormap/colormod/glowmod omission in the think feature.; DEAD: the whole hook is unreachable because IsEnabled is hard-false, so OnItemSpawn is never subscribed and never fires in a normal match. |
| 96 | 6.0 | `mutator-physical_items` | `mutator-physical_items.item_callbacks.think_touch_damage` | liveness:dead | Ghost colormap/colormod/glowmod are NOT copied from the owner each think (QC sv_physical_items.qc:43-46) — a cosmetic per-frame ghost-tint divergence; alpha IS followed.; DEAD: the think/touch/damage callbacks are only wired onto a ghost created by OnItemSpawn, which never runs because the mutator never adds (gate hard-false). No ghost exists, so nothing thinks/touches/damages in a normal match. |
| 97 | 6.0 | `race` | `race.mode.setup_limits` | logic:partial | Unset g_race_laps_limit defaults to 7 in the port; Base (-1) consults the mapinfo laplimit.; The remaining legacy gametype_init defaults (teamlaplimit=15, the 20-min timelimit / 5-min qualifying defaults) are not applied as defaults — only laplimit=7 has a port fallback. |
| 98 | 6.0 | `sv-teamplay` | `sv-teamplay.queue.join_queue` | logic:partial | DRIFT (Wave 16, b3a9c06): the queue ENQUEUE half is now ported AND live — QueueNeeded + EnqueueJoin are called from CmdJoin (Commands.cs:1539-1546): on a live-match join that would unbalance teams the joiner is held as an observer. But the TAG-IN half is DEAD: QueuedPlayersTagIn has ZERO callers anywhere in src (BalanceTeamsTick does NOT call it, despite the Commands.cs:1545 comment claiming 'tag-in happens from BalanceTeamsTick'). So a queued player is enqueued but NEVER tagged back in — they are stuck as an observer indefinitely. This is a one-way trap when g_balance_teams_queue is enabled.; QueueNeeded's specific-team preference is inert: WantsJoin is still only ever ASSIGNED 0 (ClientManager.cs:235/286, GameWorld.cs:2016, KickTeamkillerMutator.cs:152) — never set to a preferred team color — so EnqueueJoin(p, p.WantsJoin) always enqueues with prefTeam=0 (any) and QueueNeeded always takes the any-team SizeDifference>=1 branch. |
| 99 | 6.0 | `sv-teamplay` | `sv-teamplay.global.team_entities` | logic:partial | No unified Team_* global entity API: per-team score lives in Scores, alive/owned-item counts and winner queries are reimplemented ad hoc per gametype (CA/Dom/Onslaught) rather than via Team_GetWinnerAliveTeam / Team_GetWinnerTeam_WithOwnedItems. |
| 100 | 6.0 | `weapon-arc` | `weapon-arc.bolt.secondary_burst` | logic:partial | Whole bolt_count burst fires in ONE WrThink call (one PrepareAttack, a for-loop over count at Arc.cs:493); Base fires one bolt per bolt_refire via misc_bulletcounter counting up from -to_shoot, enforcing bolt_refire2 only after the last bolt. Dormant at stock bolt_count 1. UNCHANGED.; Ammo-fraction scaling is ported (Arc.cs:473-481: to_shoot = floor(count*min(1,ammo/bolt_ammo)), TakeResource(min(bolt_ammo,ammo))) matching arc.qc:610-622 — but applied once to the whole one-shot burst, not per-bolt, so the per-bolt refire2 tail is still absent. UNCHANGED. |
| 101 | 5.7 | `mutator-overkill` | `overkill.weapons.unblock` | logic:partial | No ok_weapons sub-mutator gating the OK weapons behind (g_overkill_weapons \|\| ok). Whether the OK weapons are WEP_FLAG_MUTATORBLOCKED outside Overkill — and unblocked under it — is not enforced via this hook; the OK weapons exist as direct registrations. UNCHANGED by Wave-2. |
| 102 | 5.7 | `turret-framework` | `turret-framework.client.presentation` | logic:partial | No tur_head sub-entity: the steady-state head MODEL rotation (turret_draw tur_head.angles += avelocity*dt) is never rendered, and the per-shot head-frame anim rides the turret body's own Entity.Frame instead of a separate head bone.; No turret_draw2d radar layer: no waypoint sprite and no over-turret healthbar.; No turret_changeteam team recolor (colormap/glowmod).; No client gib toss (turret_gibtoss/turret_gibboom) — needs a CSQC turret edict. |
| 103 | 5.6 | `bot-ai` | `bot-ai.special.jetpack_rocketjump` | logic:partial | Jetpack NAVIGATION (take off / fly to a jetpack point / brake-land to cross a jetpack-only gap) is still missing: bots can't cross jetpack-only gaps even with bot_ai_navigation_jetpack on. (UNCHANGED.) |
| 104 | 5.6 | `bot-waypoints` | `bot-waypoints.steer.steerlib` | logic:missing | No steerlib library: flocking/swarm/traceavoid/beamsteer primitives absent (group flocking, the trident/funnel traceavoid obstacle steering, and beamsteer). Bots do not flock and use the simpler single-probe avoidance in Steer + the BotDanger fall probe. |
| 105 | 5.6 | `cl-crosshair` | `cl-crosshair.trueaim.showimpact` | logic:missing | Base (crosshair.qc:306-321): the hit-test block reprojects the box-trace endpoint into screen space (wcross_origin) and, unless crosshair_hittest_showimpact is set, restores wcross_origin to the geometric centre. With showimpact=1 the crosshair is left drawn AT the projected impact point (a debug/aim-assist cue). The port always draws at the geometric panel centre (DrawPanel: center = Size2*0.5f) and never reprojects — the cvar is registered with the Base default (0) but has no reader.; Tied to the same missing screen-space reprojection as the trueaim HITOBSTRUCTION test; default 0 so the visible default behaviour matches Base. |
| 106 | 5.6 | `cl-scoreboard` | `cl-scoreboard.draw.export` | logic:missing | No equivalent of Scoreboard_Draw_Export (writes the scoreboard table to a file handle for the dump-scoreboard console command); the port has no scoreboard-export command. |
| 107 | 5.6 | `cl-teamradar` | `cl-teamradar.mutator.hook` | logic:missing | No TeamRadar_Draw client mutator hook: mutators cannot add custom blips/icons to the radar, and the hook's return value (which Base ORs into the per-icon team-match gate for hover-glow visibility) is unmodeled. |
| 108 | 5.4 | `sv-teamplay` | `sv-teamplay.nagger.team_nagger` | liveness:partial | The unbalanced-teams center-print is still NOT shown on the live path: SendTeamNag (Teamplay.cs:1081) was ADDED in Wave 16 and would broadcast the nag via NotificationSystem.SendCenterRaw, but it has ZERO callers anywhere in src — BalanceTeamsTick / the server frame never invoke it. So the visible nag is DEAD-CODED, not networked. (The warmup HOLD half is live; only the player-facing warning is unwired.); Warmup-hold uses SizeDifference over the full roster; Base TeamBalance_SizeDifference(NULL) is the same NULL-ignore form, so the hold trigger is faithful. |
| 109 | 5.4 | `vehicle-bumblebee` | `vehicle-bumblebee.client.presentation` | logic:partial | The networked BRG_* healray/damage beam (the signature green/red ray; ENT_CLIENT_BUMBLE_RAYGUN, bumble_raygun_send/NET_HANDLE) + bumble_raygun_draw multi-segment Draw_CylindricLine cylinder NOT IMPLEMENTED (placeholder EffectEmitter.TeHealBeam/TeBeam stub only; colour bit-faithful).; Wave-7 LIVE NOW (in addition to the Wave-5 pilot HUD): the GUNNER cockpit HUD (VehicleHudKind.BumblebeeGun selected for a seated gunner, NetGame.cs:3001) + the two GUNNER aux crosshairs (magenta '1 0 1' lead + reload-color READY, NetGame.cs:3025-3034) + the PILOT mirror of both gunners' READY markers (FeedPilotGunnerAux, NetGame.cs:3050-3051) all render each frame. STILL missing: the 'No right/left gunner!' blinking prompts (vr_hud text drawn when AuxiliaryXhair[1]/[2] are absent), and the vr_crosshair vCROSS_HEAL pilot pointer (the aux LOCK crosshair is drawn but not the dedicated vCROSS_HEAL center pointer).; Wave-15 FIX: the cosmetic gun models (CANNON_RIGHT/LEFT/CENTER) are now created + tag-attached at spawn (Bumblebee.cs:115-117, see vr_spawn row) — render code-verified only. STILL NOT IMPLEMENTED: the gun COCKPIT models (GUNCOCKPIT wakizashi_cockpit.dpm) + cockpit/viewport offsets + scale 1.5.; MENUQC describe text (the three-passenger role explanation) NOT IMPLEMENTED. |
| 110 | 5.25 | `keepaway` | `keepaway.ball.respawn` | logic:partial | The relocate MOVES the ball — BallEntity.Relocate calls RandomMapLocation (a random spawnpoint, falling back to a jitter around home) + the '0 0 200' kick + re-arms NextThink; the loose-ball think (RespawnThink) is live via SpawnForGametype.; SND_KA_RESPAWN plays at relocate (Keepaway.RespawnBall → SoundSystem.PlayOn KA_RESPAWN, ATTEN_NONE).; FIXED post Wave-16/17 (presentation missing→partial): EFFECT_KA_BALL_RESPAWN IS now emitted at BOTH the old AND new origin — RespawnBall captures oldBallOrigin, Relocates, then calls EffectEmitter.Emit("KA_BALL_RESPAWN", oldOrigin) + Emit("KA_BALL_RESPAWN", e.Origin) (Keepaway.cs:455-456) == QC ka_RespawnBall's two Send_Effect calls. The effect is registered (EffectsList.cs:232). The server-side emit-API call is wired; the CSQC particle RENDER fidelity itself is unverified (→ partial, not faithful).; NEEDKILL ka_DamageEvent respawn path is LIVE — AdoptBall installs OnBallDamage on the ball's Entity.GtEventDamage, the live DamageSystem.EventDamage (DamageSystem.cs:369-371) routes a non-player edict's damage to it, and a NEEDKILL hit (DeathTypes.ItemDamageNeedKill) force-respawns the loose ball.; STILL OPEN (logic, why partial): RandomMapLocation uses the QC FALLBACK path (random info_player_* spawnpoint, then a ±256 jitter around home) only — it does NOT do the full MoveToRandomMapLocation world-bounds sampling with the SOLID/SLIME/LAVA/SKY content rejection (BallEntity.cs:395-421). Functionally close (ball relocates to a reachable spawn) but not bit-identical placement.; STILL OPEN (logic): no NOIMPACT fall-off respawn branch (ka_TouchEvent trace_dphitq3surfaceflags & Q3SURFACEFLAG_NOIMPACT — a ball that touches a NOIMPACT surface, i.e. off the map, is not force-respawned; the NEEDKILL damage branch IS covered). |
| 111 | 5.25 | `keepaway` | `keepaway.waypoints.tracking` | logic:partial | FIXED (was missing/na→partial/live): CollectWaypoints (overriding GameType.CollectWaypoints, pulled live each tick by ServerNet.cs:927 SendWaypoints) now EMITS the sprites — a KaBallCarrier sprite on the carrier (offset '0 0 64', RADARICON_FLAGCARRIER) when held, else a KaBall sprite at the loose ball's origin. The sprite TYPES were already registered in the catalog (Waypoints.cs:137-138 KaBall/KaBallCarrier with radar icons).; FIXED (was 'cvar never read'): g_keepawayball_tracking is now read (BallTracking) and honored: 0 = no waypoint at all, 1 = always, 2 = only when the ball is dropped/loose (the carrier sprite is suppressed when tracking != 1).; STILL OPEN (logic, why partial): the spectator/invisibility visibility rules (ka_ballcarrier_waypointsprite_visible_for_player: hide the carrier sprite from spectators-of-the-carrier and when the owner IS_INVISIBLE) are not modeled; QC also forces the sprite ON during warmup, but the host pull path has no warmup flag here so the cvar governs unconditionally.; STILL OPEN (presentation): waypoint sprite RENDER fidelity is the client's; this row asserts the server-side spawn/attach + cvar gating, not the CSQC draw. |
| 112 | 5.25 | `tka` | `tka.ball.pickup` | presentation:missing | GiveBall is LIVE: BallTouchEntity is the ball's touch handler (registered in the live SpawnBall), so a player touching the loose ball becomes the carrier, with SND_KA_PICKEDUP, INFO/CENTER_KEEPAWAY_PICKUP[_SELF] notifications, the 0.5s self-recapture lockout, AttachToCarrier, and TKA_PICKUPS+1.; The carried-ball orbit anim (Tick -> BallEntity.CarryOrbit, BALL_XYSPEED 100 / XYDIST 24) runs — GameWorld.cs:2583 calls tka.Tick(Simulation.FrameTime) every frame. The ball orbits the carrier.; No scale 0.75 while carried, no GameRules_scoring_vip equivalent, no alpha-sync invisibility, no multi-ball chaining (this.ballcarried insert + cnt offset).; World-touch non-player branch plays SND_KA_TOUCH but omits the EFFECT_BALL_SPARKS particle (presentation). |
| 113 | 5.25 | `weapon-devastator` | `weapon-devastator.presentation.flight_fx` | liveness:partial | Muzzle-flash MODEL (models/flash.md3) is not attached at the muzzle — only the ROCKET_MUZZLEFLASH particle effect is emitted (Attack: EffectEmitter.Emit ROCKET_MUZZLEFLASH, no flash-model attach).; Crosshair image/size (gfx/crosshairrocketlauncher, 0.7) not audited in this pass. |
| 114 | 5.25 | `weapon-electro` | `electro.describe_guide` | logic:partial | GuideDescription (Electro.cs:104-113) ports the four describe() prose paragraphs but NOT the trailing W_Guide_Keybinds line nor the computed W_Guide_DPS_secondaryMultishotWithCombo metric line (electro.qc:764-765 — those helpers aren't ported). |
| 115 | 5.25 | `weapon-hook` | `weapon-hook.primary.fire_grapplinghook` | logic:partial | No dedicated hook entity stream (GrapplingHookSend / ENT_CLIENT_HOOK with hook_start/hook_end). The chain HEAD IS networked via the generic projectile feed (Item + Owner + MoveType.Fly classifies as a Projectile) and renders as a red Hookbomb spike — but with no rope-endpoint stream there is no rope line (see weapon-hook.presentation.rope_line).; No WarpZone refsys tracking (WarpZone_RefSys_SpawnSameRefSys, server/hook.qc:323).; weaponLocked(actor)/actor.vehicle early-return guard at the top of FireGrapplingHook IS now ported (Hook.cs:278, IsWeaponLocked \|\| Vehicle is not null) — the prior 'weaponLocked guard absent' note is resolved; only the broader weaponLocked subset (game-start/player_blocked/LockWeapon) remains unmodeled, gated upstream. |
| 116 | 5.25 | `weapon-minelayer` | `minelayer.mine.render` | liveness:partial | No CSQCProjectile(PROJECTILE_MINE) in-flight networking analogue: Base networks the in-flight mine as a CSQC projectile and only the STUCK mine is a plain server entity. The port renders the mine as a plain server model for its whole life, so in-flight prediction/trail networking differs (functional but not the CSQC path). |
| 117 | 5.2 | `cl-scoreboard` | `cl-scoreboard.block.accuracy` | logic:partial | Drawn as plain text '%' cells, not the per-weapon ICON grid (drawpic weapon model2) with column/row highlighting.; No accuracy_showdelay gating, no warmup suppression, no doublerows, no Accuracy_GetColor weapon ramp or nocolors option; the port's average color ramp differs from QC's per-weapon ramp. |
| 118 | 5.0 | `mutator-buffs` | `buffs.item.respawn_cooldown` | logic:partial | Port respawn is still a single Think after cooldown_respawn (clears Owner and runs MaybeRandomize/MaybeRelocate); no buff_activetime countdown, no activate-cooldown arming on round start (Base buff_Reset uses g_buffs_cooldown_activate; port only models cooldown_respawn), no buff_active/buff_activetime state machine, no buff_Customize light pulse.; SND_STRENGTH_RESPAWN + EFFECT_ITEM_RESPAWN ARE now emitted on (re)activation (ActivateBuff:336-337, ScheduleRespawn:462-463). Still missing: the waypoint build-finished timer and g_buffs_pickup_anyway instant-respawn.; Owner-gone / dead / frozen / wrong-buff respawn conditions (QC buff_Think) are still collapsed: the port unconditionally clears the owner and re-arms after the respawn cooldown. |
| 119 | 5.0 | `mutator-dodging` | `mutator-dodging.anim.jump_action` | logic:missing | The explicit server-side animdecide_setaction(ANIMACTION_JUMP) write is not ported (logic: missing) — the port has NO animdecide/anim-action server->client one-shot channel. HOWEVER this seam has no distinct rendered effect even in Base: animdecide.qc:283 states verbatim 'currently, in CSQC this is the only jump detection, as the explicit jump action is never called!' — Base's client renderer drives the jump clip off the IMPLICIT ground->air (ANIMIMPLICITSTATE_INAIR) transition, identical to the port's path. So the missing logic produces no observable presentation gap. |
| 120 | 5.0 | `mutator-overkill` | `overkill.randomitems.inject_ok_items` | logic:partial | Minor: the OK item pool is a hard-coded NetName list (OverkillItemNetNames = health_mega/armor_small/armor_medium/armor_big/armor_mega + weapon_okhmg/okrpc), NOT the Base g_overkill_items IntrusiveList built in MUTATOR_ONADD (which the port never builds — see overkill.onadd row). The set/weights match Base, but it is not driven by the real ITEM_FLAG_MUTATORBLOCKED-filtered list; Item_IsDefinitionAllowed is approximated by def.ItemDef.IsAllowed.; liveness=partial: only fires when the random_items mutator is also active (the chain is reached via RandomItemsMutator.GetRandomItemClassName). |
| 121 | 4.8 | `cl-csqcmodel` | `cl-csqcmodel.tagindex.weapon_attach` | logic:partial | The QC h_/v_ model tag resolution cascade (weapon bone -> gettagindex weapon/tag_weapon -> shot/tag_shot, with recursive parent predraw and drawmask=0 when the tag can't resolve) is simplified to a tag_weapon marker attach; weapon-on-weapon and the shot-muzzle tag chain are not faithfully reproduced.; No recursive predraw of the parent entity to fix forcemodel/LOD bone-index mismatch (forcemodel is live, so a forced-model bone-index mismatch on the held weapon attach is a possible edge case; LOD swap is still absent so no LOD-driven mismatch). |
| 122 | 4.8 | `monster-spider` | `monster-spider.attack.player_wielded` | logic:missing | STILL OPEN. The SpiderAttack weapon's IS_PLAYER(actor) path (a player wielding the hidden spider attack via impulse 9 / monster-riding or dev tooling) is not ported: no SpiderAttack weapon in the port (the port's Spider re-derives melee/ranged inside Monster.Attack and never goes through a Weapon). The player path uses weapon_prepareattack timing, attack_range=60, and Monster_FindTarget self-targeting — all absent. |
| 123 | 4.8 | `sv-cheats` | `sv-cheats.impulse.teleport` | logic:partial | Random-location path uses BallEntity.RandomMapLocation (a spawnpoint-sampling fallback), NOT the true MoveToRandomMapLocation world-bounds sampler; the 100 / 100000 attempt-count constants (the sv_cheats<2 vs >=2 wider search) and the DPCONTENTS/Q3SURFACEFLAG_SKY content masks are not honored.; The noclip-preferred info_autoscreenshot path (Cheats.cs:300-318) is now REACHABLE: as of the info_autoscreenshot spawnfunc fix (sv-cheats.mapent.info_autoscreenshot) the entity spawns from the BSP lump, so on a map with autoscreenshot points a noclipping cheater is teleported to (and consumes) one — matching Base. On maps without them it still falls through to the random path, as in Base. |
| 124 | 4.72 | `cl-hud` | `cl-hud.panel.minigame` | logic:partial | Board + status are merged into one MinigameRenderer (board square + team pieces + win-line + status line) rather than the QC BOARD/STATUS split; only the implemented minigame rule-sets are renderable. Per-game _hud_board/_hud_status art coverage not all runtime-verified. |
| 125 | 4.55 | `bot-waypoints` | `bot-waypoints.auto.spawnforteleporter` | logic:partial | No warpzone-angle math (Base makevectors(warpzone_angles) src/dest projection) and no oblique-warpzone JUMP flag; jumppad push-time cost is approximated (Base uses trigger_push_get_push_time / the auto-link cost), the port uses the plain travel cost.; Generated-graph path only; shipped maps use authored teleport waypoints from the file. |
| 126 | 4.5 | `mutator-nades` | `mutator-nades.boom.translocate` | logic:partial | move_out_of_solid is a minimal upward-nudge approximation (no builtin), so embedded-in-solid recovery is less robust than Base's full search.; MUTATOR_CALLHOOK(PortalTeleport) is not fired (no subscribers in port). |
| 127 | 4.5 | `mutator-overkill` | `overkill.onadd.precache_and_itemblock` | logic:partial | ok_player models are never precached / forced as the default player model (precache_all_playermodels equivalent absent).; g_overkill_items list (the random-items injection source) is never built.; Mechanism differs: Base ONADD sets ITEM_FLAG_MUTATORBLOCKED on the four items at add-time; the port instead reads the four filter cvars in Hook() and enforces the block per-spawn in the FilterItem hook (overkill.filter row). End behavior (which items are blocked, with which defaults) now matches — but the ONADD spawnflag-blocking and ONREMOVE clear are not modeled. |
| 128 | 4.5 | `turret-phaser` | `turret-phaser.presentation.beam_visual_and_head_anim` | logic:partial | No PhaserTurret_beam model spawned/scaled to the impact distance (MDL_TUR_PHASER_BEAM + .scale = vlen/256 + setattachment tag_fire); acknowledged TODO comment in Attack (PhaserTurret.cs:161-165). The server-side beam logic/damage IS done; only the visible beam model is absent.; phaser.md3 head model is not attached as a separate tracking head bone — the head-frame animation drives the turret edict's own networked Entity.Frame, so the frame VALUE animates faithfully but there is no visible phaser.md3 head to show it (presentation gap shared with identity.model_hitbox). |
| 129 | 4.5 | `weapon-tuba` | `weapon-tuba.note.sustain_lifetime` | values:partial | W_WeaponRateFactor scaling on the keep-alive — the port DOES multiply refire*2 by WeaponRateFactor(actor) (Tuba.cs:163), so this matches Base; only relevant under haste/rate modifiers.; No W_Tuba_NoteThink server-side per-listener volume/angle re-origin + SendFlags (moot until tuba-note sound networking exists). The port's NoteThink only models the teleport_time self-expiry, not the re-origin broadcast. |
| 130 | 4.2 | `monster-golem` | `monster-golem.menu.describe` | liveness:dead | CORRECTION (was stale): the 3-paragraph describe() text DID land — Golem overrides Monster.Description (Golem.cs:43-49) with the verbatim golem.qc:308-316 entry. But NOTHING consumes Monster.Description (declared virtual in EntityClasses.cs:25; zero readers across src) — the port has no monster info-page UI, so the text is dead data and never surfaced to the player. Pure menu/UI flavor, no gameplay effect. |
| 131 | 4.2 | `sv-client-lifecycle` | `sv-client-lifecycle.join.join_player` | logic:partial | Join queue + team-selection ordering not modeled: the queued_join TeamBalance_AreEqual two-pass (queued-team dibs first pass + strongest-autoselect-by-skill second pass, player_with_dibs conflict) is absent; the port autoselects via a single connect-time AssignBestTeam.; Campaign Join branch (campaign_bots_may_start / ReadyRestart) only partially modeled (CampaignHostAutojoin opts the host in); the bot-start ReadyRestart trigger is not in ClientManager.Join.; Queue-conflict + queued-player branches still deferred: CENTER_JOIN_PLAY_TEAM_QUEUECONFLICT (depends on the unmodeled wants_join/player_with_dibs queue) and ANNCE_BEGIN for a queued player are not fired; Kill_Notification(CPID_PREVENT_JOIN) clear not modeled. |
| 132 | 4.2 | `turret-fusionreactor` | `turret-fusionreactor.menu.describe` | logic:missing | The MENUQC describe() help string (shown in the menu turret codex) is not ported — the port has no menu turret-description surface for the Fusion Reactor. |
| 133 | 4.05 | `mapobject-triggers` | `mapobject-triggers.viewloc.trigger_viewlocation` | liveness:partial | no client 2.5D side-scroller camera consumer: the server stamps player.ViewLoc each frame but the view is never locked to the start->end line, so the camera region has no visible effect (unchanged this wave — no ViewLoc consumer found under game/client/) |
| 134 | 4.0 | `mutator-nades` | `mutator-nades.bonus.economy` | logic:partial | GameRules_scoring_is_vip / flagcarrier multiplier: the VIP-kill medium bonus + flagcarrier time-score multiplier (g_nades_bonus_score_time_flagcarrier=2, scaled by KH key count) ARE now wired in OnPlayerDies / OnPlayerPreThink (GametypeEntities.ScoringIsVip + KeyHuntKeyCount). Whether those two helpers return Base-faithful values for every gametype (CTF/KA/TKA VIP, KH key carrier) is the residual values:partial risk — the per-second accrual path now reads CvarRaw(g_nades_bonus_score_time_flagcarrier) and the VIP/key branches, so the previously-MISSING flagcarrier cvar is now consumed. |
| 135 | 3.92 | `cl-hud` | `cl-hud.panel.infomessages` | logic:partial | Cycling group hints (spectate controls), team-unbalanced nag, spectator list and the full message set are partially covered (the eliminated-LMS line is now present). |
| 136 | 3.85 | `bot-ai` | `bot-ai.combat.dodge` | logic:partial | 15 of Base's 20 bot_dodge producer files are ported (all weapon projectiles + OkRpc + the PhaserTurret beam); the ones still missing are monsters (golem/spider), the napalm nade, the grappling hook, and the generic sv_turrets producer, so dodge does not trigger for those hazards. (NARROWED 2026-06-27 third pass: +OkRpc.); Non-SUPERBOT bots still only do the generic CombatMovement strafe-flip + HP-bias (an invented behaviour), not a per-projectile dodge — Base also restricts havocbot_dodge to SUPERBOT, so this matches stock, but the generic strafe is not a Base behaviour. (UNCHANGED.); The SUPERBOT random-direction combat jitter (havocbot_movetogoal:1280-1307) is not reproduced. (UNCHANGED.) |
| 137 | 3.76 | `cl-hud` | `cl-hud.panel.quickmenu` | logic:partial | Nested quick-chat tree from a quickmenu file + translatecommands/server_is_default parity not verified. |
| 138 | 3.76 | `fx-effectinfo` | `fx-effectinfo.emit.te_builtins` | liveness:partial | The te_* helper -> registry-name mapping (e.g. TeSmallflash -> BLASTER_MUZZLEFLASH, TeGunshot -> MACHINEGUN_IMPACT) is a port convention, not a 1:1 DP te_* semantic; not verified that every live caller hits these wrappers vs emitting the registry effect directly. Beam/arc temp-effects route to BeamRenderer, not the particle catalog. |
| 139 | 3.75 | `clanarena` | `clanarena.hud.eliminated_greyout` | logic:partial | IsEliminatedPlayer now reads `p.IsDead \|\| _joiningMidRound.Contains(p)` (Wave-16/17), so it CAN grey a mid-round joiner — but _joiningMidRound is never populated on the live path (OnJoinLate is unreachable, see clanarena.join.late_join_observer), so the _joiningMidRound branch is dead and the effective behavior is STILL `IsDead` only. QC ca_isEliminated also greys FRAGS_PLAYER_OUT_OF_GAME (N/A to the port — no per-life elimination column). So a late joiner waiting out the round still isn't greyed until they die. |
| 140 | 3.75 | `clanarena` | `clanarena.spawn.ingame_status_decimal_reset` | logic:partial | QC ca PlayerSpawn: INGAME_STATUS_SET(player, JOINED). Wave-16/17 added a `_joiningMidRound.Remove(player)` to OnPlayerSpawn (ClanArena.cs:576) intending to model this flip, but _joiningMidRound is never populated on the live path (see clanarena.join.late_join_observer), so the flip is a no-op and no real INGAME state is maintained. logic stays partial because the INGAME_STATUS half is still not actually modeled.; QC ca PlayerSpawn: `if (!warmup_stage) eliminatedPlayers.SendFlags = 0xFFFFFF` (force eliminated-list resend on spawn) — the port's elimination-list capture runs per frame anyway, so the explicit dirty-flag is N/A. |
| 141 | 3.75 | `ctf` | `ctf.flag.remove_player` | liveness:partial | PORTAL-teleport carry-leave drop (g_ctf_portalteleport) is still NOT wired — the ctf PortalTeleport hook has no port dispatch (the PortalTeleport event itself is never fired in the port; BuffsMutator/NadeTranslocateBoom note it omitted). Disconnect + MakePlayerObserver (OnPlayerRemoved) and vehicle board/leave (OnVehicleEnter/Exit) DO drop the flag now. |
| 142 | 3.75 | `ctf` | `ctf.hud.waypoints` | values:partial | Base/dropped/carrier-own (health bar) + carrier-enemy (stalemate/self-carry reveal) sprites + the carrier helpme FLASH are emitted; no dedicated flag-return sprite, no on-pickup enemy-FC spotting reveal (g_ctf_flagcarrier_waypointforenemy_spotting). |
| 143 | 3.75 | `ctf` | `ctf.oneflag` | liveness:partial | Liveness depends on a map shipping item_flag_neutral; the neutral-pickup + team-base-capture + reverse logic is present but exercised only on a one-flag map. g_ctf_reverse (two-flag reverse) is NOT modeled (only OneFlagReverse). |
| 144 | 3.75 | `cts` | `cts.map_reset` | logic:partial | ResetMapGlobal resets every runner's in-progress run (AbandonRun: RunStartTime + .race_movetime accumulator zeroed), zeroes the round-best speed award, and cancels pending finish-retracts on map reset — live (GameWorld:4442). The persistent top-99 CTS records are intentionally kept (QC keeps them too).; Still not modeled: the cts_EventLog place echo per client (Score_NicePrint + the ftos(race_place) gamelog line) and the qualifying==2 -> race collapse (moot: CTS pins g_race_qualifying=1). The PlayerScore_Sort/race_place bookkeeping is also absent (CTS has no intermediate-checkpoint race_place tracking). |
| 145 | 3.75 | `cts` | `cts.records.getrecords` | logic:partial | The port models the single-map ranking listing (QC getrankings: ranks 1..RANKINGS_CNT of the current map) and wires it live to the rankings/records console commands. The QC GetRecords hook's multi-map paged form (200 maps/page across MapInfo, the cross-map 'records [<page>]' listing) is NOT modeled — only the current map's records are listed. |
| 146 | 3.75 | `deathmatch` | `deathmatch.menu.describe` | liveness:partial | MenuDescription only renders during a GAMETYPE vote (sv_vote_gametype, opt-in default 0); a normal map vote and the static menu gametype-picker do not surface it. This matches Base (describe() is shown only in the gametype-vote ballot, GameTypeVote_DrawGameTypeItem), but means the prose is unseen at stock settings.; Listen-server only: the whole mapvote/gametype-vote ballot is fed from the in-process GameWorld.MapVote (NetGame.UpdateMapVotePanel); a dedicated-server / remote client has no ENT_CLIENT_MAPVOTE net entity, so it never sees the description. This is the broader mapvote-net gap tracked in sv-mapvoting / sv-intermission, not a DM-specific defect. |
| 147 | 3.75 | `freezetag` | `freezetag.frozen.no_pickup` | liveness:partial | BuffTouch has no separate hook in the port — OnItemTouch (the world-item gate) is the only frozen-pickup gate; buff-pickups route through a different path that has no frozen gate. Minor: frozen players can't initiate movement, and buffs are a mutator. World-item pickups ARE correctly gated. |
| 148 | 3.75 | `keepaway` | `keepaway.ball.pickup` | values:partial | Pickup is reachable — the live engine trigger-touch pass dispatches Solid.Trigger entities' Touch=BallTouchEntity, which calls PickUp; AttachToCarrier makes the ball SOLID_NOT + MOVETYPE_NONE + sets GtCarrier.; SND_KA_PICKEDUP plays (PlayOn KA_PICKEDUP, ATTEN_NONE); INFO_KEEPAWAY_PICKUP (all) + CENTER_KEEPAWAY_PICKUP (all-except) + CENTER_KEEPAWAY_PICKUP_SELF (one) notifications all sent and registered.; KEEPAWAY_PICKUPS column increment runs (AddCol on PickUp); the 0.5s self-recapture lockout is honored (BallTouchEntity → IsRecaptureLocked).; The non-player world-touch branch plays SND_KA_TOUCH (ATTEN_NORM) AND emits EFFECT_BALL_SPARKS — BallTouchEntity calls EffectEmitter.Emit("BALL_SPARKS", self.Origin) (Keepaway.cs:479) before the touch sfx == QC ka_TouchEvent Send_Effect(EFFECT_BALL_SPARKS, ...). The effect is registered (EffectsList.cs:239).; FIXED post Wave-16/17 (gap was stale): the EFFECT_BALL_SPARKS emit-API call IS now present on the non-player touch (the baseline row claimed 'only the sound is' emitted). CSQC particle render fidelity itself stays the client's, so presentation remains partial.; STILL OPEN (values/presentation, why partial): carried scale 0.75 (QC scale = 12/16) is not applied — the entity layer has no scale field. Note GtCarrier IS marked VIP: AttachToCarrier calls GametypeEntities.ScoringVip(carrier,true) (BallEntity.cs:310), so the prior 'VIP flag not modeled' gap is partly closed (the nades flagcarrier-bonus VIP flag is set; the scoreboard VIP marker is not separately modeled).; STILL OPEN (logic): multi-ball chaining (g_keepaway_ballcarrier_maxballs, cnt chain) is collapsed to a single ball (cnt=1). |
| 149 | 3.75 | `keepaway` | `keepaway.bot.role` | logic:partial | RoleKeepaway is live (set on the brain at bot creation via BotRoles.ChooseRole; 'ka'/'keepaway'/'tka' → RoleKeepaway) and engages the ball: FindBall() queries classname 'keepawayball' (BotObjectiveRoles.cs:691-696) == the live spawned ball; carrier read off ball.GtCarrier (the field AttachToCarrier sets). A loose ball is chased and a carried ball's carrier is contested (RatingBall 8000, routerating 2000f, BotObjectiveRoles.cs:633-636); carrying switches to carrier behaviour.; Bot_ForbidAttack equivalent is wired LIVE (RE-VERIFIED Wave 16/17). Keepaway.ForbidBotAttack(bot,targ) = `!targCarries && !botCarries && haveHeldBall` (Keepaway.cs:265-271 == QC: neither carries + a held ball exists), installed at GameWorld.cs:2996-2997 onto BotBrain.ForbidAttackHook (in the case Keepaway ka block of ActivateGameType) and consulted in bot_shouldattack at BotBrain.cs:1087 on the live attack-eligibility path — so bots cluster the carrier instead of fragging bystanders.; STILL OPEN (logic, why status stays partial): the collector/carrier split is collapsed into one adaptive RoleKeepaway (Base has two distinct havocbot_role_ka_carrier / havocbot_role_ka_collector functions with a per-bot role-timeout state machine). The port branches on carry-state inline with the same ratingscales rather than swapping the dedicated role function — a structural simplification, behaviourally equivalent at the rating level. |
| 150 | 3.75 | `keepaway` | `keepaway.score.resetmapglobal` | logic:partial | QC's reset_map_global hook clears the shared `.float2int_decimal_fld` (the fractional-score carry) on EVERY client at a global map reset. The port keeps a SINGLE gametype-level remainder (_timePointsRemainder) rather than a per-player field, and zeroes it in Activate (Keepaway.cs:157) at match (re)start — functionally equivalent for the single-ball case (only the lone carrier ever accrues fractional points), but it is not a per-client FOREACH reset and is not wired to a dedicated reset_map_global seam.; Low impact: the only observable effect would be a sub-1-point fractional-score carry surviving a map reset, which Activate already clears; the per-client breadth matters only if multiple players simultaneously held fractional remainders, which the single-ball model precludes. |
| 151 | 3.75 | `nexball` | `nexball.weapon_arena.player_setup` | logic:partial | SpectateCopy hook (copy STAT(NB_METERSTART) from the spectatee to the spectator) is not ported — a spectator watching a charging carrier won't see the meter (also blocked on the unnetworked NB_METERSTART).; WantWeapon (force the start weapon to 0) is not explicitly ported as a hook; the port instead grants BallStealer on spawn (basketball mode) and clears all weapons (football mode). Behaviorally close but the QC WantWeapon force-0 path is folded into OnPlayerSpawn.; PlayerSpawn basketball-mode grant uses \|= (keeps the player's normal start weapons + adds BallStealer), matching QC's STAT(WEAPONS, player) \|= WEPSET(NEXBALL). The active weapon swap to BallStealer happens on pickup (GiveBall), not on spawn — matches QC. |
| 152 | 3.75 | `nexball` | `nexball.item.filter_block` | logic:partial | ForbidDropCurrentWeapon (QC returns true for WEP_MORTAR.m_id — a long-standing odd special-case) is NOT ported as a distinct hook; the port only wires ForbidThrowCurrentWeapon (returns true for the BallStealer). The QC WEP_MORTAR drop-block is dropped (its purpose is unclear in Base, marked 'TODO: what is this for?'). |
| 153 | 3.75 | `overkill-weapons` | `overkill-weapons.mode.enablement` | liveness:partial | Mode reachable only by exec'ing ruleset-overkill.cfg / setting g_overkill via console — no in-menu arena radio. VERIFIED this MATCHES Base (Base dialog_multiplayer_create_mutators.qc also exposes only g_instagib + g_nix radios). So this is faithful to Base, NOT a port-side regression. |
| 154 | 3.75 | `race` | `race.hud.mod_icon_rankings` | liveness:partial | The mod-icon + split panels are fed host-side (LocalServerPlayer / _serverWorld.GameType), so on a PURE remote client the floating PB/server-best icon + medal flash do not render — there is no TE_CSQC_RACE / RACE_NET_SERVER_RECORD net stream feeding ModIconsPanel; only the RANKINGS scoreboard column is genuinely networked. On a listen server the mod-icon renders live.; ReadName/ReadTime are read straight from the persistent RaceRecords DB (no separate networked record-name stream); the local PB is resolved by PersistentId rather than the QC ClientProgsDB cache. |
| 155 | 3.75 | `tka` | `tka.ball.respawn` | presentation:missing | BallEntity.RandomMapLocation uses the QC SelectSpawnPoint FALLBACK path (a random spawnpoint), not the engine MoveToRandomMapLocation world-bounds sampler (rejecting solid/lava/sky). Faithful to the fallback QC itself drops to, but the primary random-world-point sampler is approximated.; Respawn sound (SND_KA_RESPAWN, ATTEN_NONE) IS played in RespawnBall, but the EFFECT_KA_BALL_RESPAWN particles at the old+new origin remain deferred (the WP_KaBall waypoint IS now produced — see tka.waypoints.tracking).; No NOIMPACT/fall-off respawn branch (tka_TouchEvent off-map Q3SURFACEFLAG_NOIMPACT handling not ported — the world-touch goes to the spark/sfx branch instead). |
| 156 | 3.75 | `weapon-arc` | `weapon-arc.beam.heat_overheat` | logic:partial | Structural divergence (UNCHANGED): Base stores beam_heat on the beam ENTITY (arc_beam) and DELETES that entity when the beam ends — heat is gone, and arc_overheat/arc_cooldown are pure timestamps. The port keeps heat as a persistent slot-state value (st.BeamHeat) drained over time by BeamCooldown. Observably close at stock values; not bit-structurally identical.; cooldown_release still ignored at stock (Arc.cs:230 CooldownSpeed branch only sets ArcOverheat when CooldownRelease != 0); Base sets arc_overheat when (cooldown_release \|\| overheat). Dormant at stock cooldown_release 0.; Minor: the NetGame heat ring while-firing branch keys on st.BeamHeat > 0 rather than st.ArcBeam != null (arc.qc:62 keys on player.arc_beam); functionally equivalent at stock (the beam is the only thing that raises heat). |
| 157 | 3.75 | `weapon-arc` | `weapon-arc.heat_persistence` | logic:partial | DROP/PICKUP migration (wr_drop/wr_pickup) still unported: arc_overheat/arc_cooldown are not migrated to a dropped Arc and restored on pickup. The port has no weapon-throw/heat-carry path yet (WeaponThrowing.cs:51 notes no weapon implements wr_drop). Mostly dormant since primary beam_heat is 0. UNCHANGED.; Reset is per-slot in WrSetup, not a respawn-wide hook: WrSetup runs on every weapon switch-in/raise, so a switch away+back also clears heat (benign — the beam is gone while holstered). Base wr_resetplayer/wr_playerdeath are dedicated respawn/death hooks. UNCHANGED. |
| 158 | 3.75 | `weapon-blaster` | `weapon-blaster.offhand.fire` | liveness:partial | Live only when g_offhand_blaster is set (default 0 in mutators.cfg:435), so liveness stays partial — but when enabled the behaviour is now faithful. |
| 159 | 3.75 | `weapon-electro` | `electro.orb.csqc_netlink_draw` | values:partial | The dedicated REGISTER_NET_LINKED(Electro_Orb) link/draw is not reproduced 1:1: Base streams the orb as its own CSQC entity (electro_orb_send writes origin+owner+lifetime; electro_orb_draw applies a (ltime-time)-based scale shrink as the orb nears expiry plus an avelocity spin '7 0 11', alpha 0.7). The port renders the orb via the generic ProjectileCatalog/ProjectileRenderer Electro body (ebomb model + TR_NEXUIZPLASMA trail + dynamic light + electro_fly loop). The in-flight identity matches; the time-based shrink pulse and explicit spin axis are cosmetic and unreproduced. |
| 160 | 3.75 | `weapon-machinegun` | `machinegun.notification.kill_suicide` | liveness:partial | The message-SELECTION code is faithful and wired live (Scores.cs obituary path calls SelectKillMessage/SelectSuicideMessage; DeathMessages keys SNIPE/SPRAY on HasHitType(deathType, Secondary)). The SNIPE branch is reachable ONLY from the mode-0 secondary (now correctly carrying HITTYPE_SECONDARY in the port) — but mode 0 is non-default and dead, so in default mode-1 play BOTH Base and the port emit MURDER_SPRAY (faithful). |
| 161 | 3.75 | `weapon-minelayer` | `minelayer.fire.lay_mine` | liveness:partial | WEP_FLAG_MUTATORBLOCKED (Minelayer.cs:63): the weapon is excluded from the default loadout/allweapons (GiveItems.cs:340 = SpawnFlags & (MutatorBlocked\|Hidden) != 0), so this fire path is NOT reached in a normal match. Live only when granted via weaponarena/NewToys/impulse-give. WrThink dispatch (WeaponFireDriver) is correct on that conditional path. Liveness stays partial. |
| 162 | 3.75 | `weapon-minelayer` | `minelayer.fire.mine_limit` | liveness:partial | Liveness partial: MUTATORBLOCKED weapon (see minelayer.fire.lay_mine). |
| 163 | 3.75 | `weapon-minelayer` | `minelayer.mine.stick` | liveness:partial | Liveness partial: MUTATORBLOCKED weapon (see minelayer.fire.lay_mine). |
| 164 | 3.75 | `weapon-minelayer` | `minelayer.mine.proximity` | liveness:partial | Base proximity loop also skips IS_INDEPENDENT_PLAYER(head); the port does not model independent-player mode, so that exclusion is absent (no functional effect outside that unported mode).; Liveness partial: MUTATORBLOCKED weapon (see minelayer.fire.lay_mine). |
| 165 | 3.75 | `weapon-minelayer` | `minelayer.mine.lifetime_countdown` | liveness:partial | Port lifetime-arm branch adds an extra `self.Frame == 0f` term to the QC `!mine_time && cnt > 0` guard. Redundant (Frame is only set to 1 after MaxHealth is set, so MaxHealth==0f already excludes re-entry; deathTime is always >0 since lifetime-countdown=29.5) — no behavioral difference. Model note only.; Liveness partial: MUTATORBLOCKED weapon (see minelayer.fire.lay_mine). |
| 166 | 3.75 | `weapon-minelayer` | `minelayer.mine.owner_death` | liveness:partial | HITTYPE_BOUNCE deathtype tag on the auto-detonate is carried only as a bool param (the port's int weapon-id deathtype has no bounce flag); the obituary picks the same splash murder string regardless, so this is documentation-only (minor).; Liveness partial: MUTATORBLOCKED weapon (see minelayer.fire.lay_mine). |
| 167 | 3.75 | `weapon-minelayer` | `minelayer.fire.remote_detonate` | liveness:partial | Port detonates inline in RemoteDetonate rather than flagging minelayer_detonate=true and letting each mine's own W_MineLayer_Think call W_MineLayer_RemoteExplode the next tick (one-tick timing difference; behaviorally equivalent in practice).; deathtype HITTYPE_BOUNCE tag on remote blasts not set (obituary picks the splash murder string regardless; inert).; Liveness partial: MUTATORBLOCKED weapon (see minelayer.fire.lay_mine). |
| 168 | 3.75 | `weapon-minelayer` | `minelayer.mine.explode` | liveness:partial | Airshot-achievement branch is ported (Explode:391-395 -> NotificationSystem.Announce('ACHIEVEMENT_AIRSHOT')) but UNREACHABLE for the mine in Base too: every W_MineLayer_Explode caller (Explode_think, ProximityExplode) passes directhitentity=NULL, so the QC `if (directhitentity.takedamage == DAMAGE_AIM ...)` guard never fires for a mine. Carried for structural fidelity. Not a gap (faithful to the unreachable Base branch).; Liveness partial: MUTATORBLOCKED weapon (see minelayer.fire.lay_mine). |
| 169 | 3.75 | `weapon-minelayer` | `minelayer.ammo.checks` | liveness:partial | Port WrThink omits the forced-reload branch, but reload_ammo defaults to 0 so Base never enters it either — faithful at default values (would diverge if reload_ammo were set).; Base wr_checkammo1 also counts clip_load (weapon_load) as a second ammo source; port checks only the rocket resource. Inert at reload_ammo=0 (clip is never loaded), so no behavioral difference at defaults.; Liveness partial: MUTATORBLOCKED weapon (see minelayer.fire.lay_mine). |
| 170 | 3.75 | `weapon-porto` | `weapon-porto.touch.placement_tree` | logic:partial | Surface-flag gating logic differs from Base in WHERE the flags come from: Base reads trace_dphit* off the ORIGINAL bouncemissile collision trace; the port re-probes with a short worldonly traceline (ProbeImpact, Porto.cs:384-402) because the engine touch carries no per-contact flags. The TraceService fills DpHitQ3SurfaceFlags/DpHitContents/PlaneNormal from the BSP surface, so the slick/clip + noimpact branches are reachable and play the right cues — but the re-trace can disagree with the true contact face on a glancing bounce, so branch selection is approximate rather than exact. |
| 171 | 3.75 | `weapon-vaporizer` | `weapon-vaporizer.primary.beam_particle` | values:partial | Beam is a generic networked trail effect (TE_TEI_G3 / _HIT) tinted with the team-color/colorboost RGB, NOT the Base bespoke cylindric gauntletbeam/lgbeam draw; the 0.8 cl_vaporizerbeam_lifetime cvar is unmodeled (engine trail default lifetime). |
| 172 | 3.75 | `weapon-vaporizer` | `weapon-vaporizer.secondary.blaster_laser` | liveness:partial | Optional Blaster-ammo decrement (W_DecreaseAmmo if WEP_CVAR_PRI(BLASTER,ammo) set) is omitted — harmless at the default Blaster ammo 0. |
| 173 | 3.75 | `weapon-vortex` | `weapon-vortex.fx.beam` | values:partial | Beam is emitted at a constant intensity: alpha/fade/trail-spacing are NOT scaled by sqrt(charge), so a barely-charged shot and a full shot look identical. EffectRequest carries colorMin/colorMax but no per-emission alpha, so the sqrt(charge) brightness modulation has no port-side hook.; Team tint is computed server-side from the FIRER's charge+team and broadcast as a single colour (EmitBeam), so the per-recipient cl_tracers_teamcolor / cl_particles_oldvortexbeam client-cvar nuance is collapsed to one broadcast emit (the broadcast reads the host's cvars). |
| 174 | 3.75 | `weapon-vortex` | `weapon-vortex.hud.charge_ring` | liveness:partial | Host-only delivery: the charge ring is fed ONLY from LocalServerPlayer's slot state (NetGame.UpdateCrosshairWeaponRings, NetGame.cs:3303-3306). So the ring fills on a listen-server / single-player host, but a PURE remote client still sees no ring. NetGame's pure-client branch just hides every ring (NetGame.cs:3290-3295 sentinels); it does NOT consume any networked charge value.; CROSS-CLIENT PRODUCER IS DEAD-ON-ARRIVAL (W16 regression risk): ServerNet now WRITES vortex_charge/chargepool into the snapshot owner block (ServerNet.WriteOwnerState/ResolveOwnerWeaponRings, ServerNet.cs:2773-2780, the [W-wepent-charges] block) — but ClientNet.HandleSnapshot has NO matching reader (the 9 ring floats are never read; verified ClientNet.cs:986-1037 reads owner state -> accuracy -> straight to movevars). So the produced charge value is discarded, the pure-client ring still never draws, AND the unread 9 ring floats (+ the also-unread PressedKeys short, ServerNet.cs:2728) desync the owner-block wire offset. This is a net-protocol concern owned by net-entity-state, but it means the cross-client charge ring is still NOT delivered. |
| 175 | 3.6 | `mapobject-triggers` | `mapobject-triggers.music.target_music` | values:partial | fade curve approximated: the client uses a two-AudioStreamPlayer A/B crossfade rather than QC's per-source state easing by frametime/fade_time (in) and frametime/fade_rate (out) |
| 176 | 3.6 | `monster-mage` | `monster-mage.menu.describe` | liveness:dead | Mage.Describe() (Mage.cs:86-93) is the verbatim bestiary prose but has NO caller: the only references to Describe() are the base virtual (EntityClasses.cs:106) and unrelated DPM/DeferredCommands.Describe overloads. No bestiary/monster-tooltip UI surface in the port renders descriptor Describe() text, so the prose never reaches the screen. |
| 177 | 3.6 | `sv-world-rules` | `sv-world-rules.checkrules.cascade` | logic:partial | leadlimit / leadlimit_and_fraglimit WIN semantics are still not evaluated in the world layer (the win comes from the gametype MatchEnded latch + tie report). The leadlimit math exists in GameScores.CountFragsRemaining but only feeds the frags-remaining announcer, not the end-of-match decision.; The CheckRules_World mutator hook (M_ARGV status override) is still not invoked. |
| 178 | 3.6 | `turret-flac` | `turret-flac.weapon.player_form` | liveness:dead | FlacWeapon is implemented but its [Weapon] registration is intentionally OFF (FlacTurret.cs:178): impulse 5 collides with the Raptor bomb-mode impulse and registering shifts WeaponById/MenuWeaponOrder, which would break those test contracts. So the hidden player FLAC weapon has NO live caller (liveness dead).; presentation partial: BLASTER_MUZZLEFLASH is emitted (FlacTurret.cs:279) and — FIXED since the baseline — now WITHOUT except, i.e. Emit(...,1) with except defaulting null, networking to ALL clients exactly like Base flac_weapon.qc:30's plain Send_Effect (the firer sees their own flash). The remaining presentation gap is only the client-side hagar trail RENDER (Godot project, outside qcsrc parity), so the partial here is driven by the same unverified-client-render reason as the turret form, not the prior except divergence. |
| 179 | 3.5 | `bot-waypoints` | `bot-waypoints.file.save` | liveness:dead | DEAD scaffolding: the three writers (SaveToText/SaveLinksToText/SaveHardwiredLinksToText, Waypoint.cs:880-945) are a faithful inverse of the loaders — version/symmetry/time header, generated-node skip, vtos 1-decimal formatting, the JUMP/SUPPORT/CUSTOM_JP source filter for cache vs hardwired/special sections — BUT have ZERO live callers (grep 2026-06-27: only referenced inside Waypoint.cs). No editor, save command, or test invokes them, so authored graphs still cannot be re-saved on the live path.; Hardwired-vs-special partitioning uses the CustomJp source flag as the hardwired marker (no dedicated wphwXX mirror), so a CUSTOM_JP source's links appear in both the HARDWIRED and SPECIAL sections, matching the QC overlap but losing the precise per-link hardwired identity on round-trip. |
| 180 | 3.5 | `mutator-buffs` | `buffs.swapper` | logic:partial | Core swap is implemented and LIVE: nearest enemy within range (DIFF_TEAM/dead/frozen/vehicle filtered), origin/velocity/angles + oldorigin exchanged, SND_KA_RESPAWN at both ends, buff_RemoveAll consumes the buff. The ForbidThrowCurrentWeapon hook has live Call sites (WeaponThrowing.cs:149/185).; Divergences from QC: uses center-distance + Api.Entities.FindInRadius instead of WarpZone_FindRadius (no warpzone traversal); omits Drop_Special_Items(closest) (flag/key carriers keep their objective on swap); omits MUTATOR_CALLHOOK(PortalTeleport, player); omits the EFFECT_ELECTRO_COMBO x2 visual at both endpoints. |
| 181 | 3.5 | `scoring` | `scoring.winningcondition.remaining_frags_announce` | liveness:partial | PARTIAL MODE COVERAGE (re-verified 2026-06-27 against current src): 8 of Base's 11 Scores_CountFragsRemaining modes call CountFragsRemaining live — Deathmatch (cs:314), TDM (cs:389), Keepaway (cs:687, gated g_keepaway_score_timepoints==0), ClanArena (cs:905), FreezeTag (cs:1116), Duel (cs:265), Race (cs:1273, gated !g_race_qualifying), TeamKeepaway (cs:731, gated !g_tka_score_timepoints), all driven by the GameWorld per-frame leader recompute. Mayhem (Mayhem.cs:706) and TeamMayhem (TeamMayhem.cs:416) deliberately do NOT call it — Base comments-out their hook (intended divergence, not a gap). Survival is the ONE genuinely-missing enabled mode: Base registers MUTATOR_HOOKFUNCTION(surv, Scores_CountFragsRemaining) but the port's Survival.cs has zero CountFragsRemaining caller (grep 2026-06-27 = no match). So 8/11 wired, 2/11 intended-off, 1/11 (surv) missing. (Prior audit's '9 of 11' double-counted the GameWorld driver as a mode.); suddenDeathEnding is always passed false (all callers note checkrules_suddendeathend is framework-owned and not surfaced), so the QC 'force fragsleft=1 during sudden-death overtime end' branch never fires. |
| 182 | 3.2 | `mutator-buffs` | `buffs.networking` | liveness:unknown | Write/Read round-trip mirrors the QC major/minor bitmap + float time + byte flags, but whether the host net loop actually Flush()es buff state to clients each tick was not traced here (status_effects unit owns that). |
| 183 | 3.0 | `mutator-nades` | `mutator-nades.boom.entrap_veil_darkness` | presentation:missing | Veil tint of enemies (render colormod) omitted; only the teammate alpha-hide is ported.; Darkness blind overlay (HUD_DarkBlinking full-screen fill) + SND_BLIND are CSQC-only and missing; only the NadeDarknessTime deadline is set server-side. |
| 184 | 3.0 | `mutator-waypoints` | `waypoints.weapon.whereis` | values:partial | FIX vs baseline (was missing/dead): Weapon_whereis is now ported in Inventory.cs and live — on the complain path of client_hasweapon (reached from the weapon-switch/cycle impulse via GetCycleWeapon:503 + SwitchWeaponWithComplain:518), when a non-bot player presses a weapon key for a weapon they don't own and g_showweaponspawns != 0, it scans the live item set and spawns a personal WP_Weapon sprite over every matching shown weapon-spawn (wp_extra = weapon id, radarIcon 0 = in-world only). All three g_showweaponspawns modes (1/2/3, loot skip, impulse-group expansion) are modeled.; values/timing: the marker lifetime diverges — Base uses -2 ('instant, no fade', re-spawned every press), the port uses a flat WhereisLifetime=2.5s deployed-style fade. Functionally similar (a brief on-demand marker) but not the bit-exact one-frame-then-re-spawn cadence. Minor.; wp_extra-driven client weapon-icon resolution: WpExtra is now SET on the sprite, but it is still not networked (waypoints.net.serialize gap), so the client renders the generic Weapon def rather than the per-weapon icon. Cosmetic. |
| 185 | 3.0 | `nexball` | `nexball.goal.touch_scoring` | presentation:missing | No bprint / Send_Notification goal announcement (Goaaaaal! / own-goal / fault / out text).; GOAL_TOUCHPLAYER spawnflag is approximated: GoalTouchEntity always treats a touching ball-carrier as a hit (the port has no per-goal spawnflag bit), and the early BallTeam/BallPusher clear differs from QC (which keeps them through the glide) but is behaviorally equivalent under the ball.cnt!=0 guard. |
| 186 | 3.0 | `nexball` | `nexball.carry.perframe` | presentation:missing | STAT(OBJECTIVE_STATUS) NB_CARRYING bit is not networked (no NB_CARRYING anywhere in the port; no nexball case in any GametypeStatusBlock). NOTE: the held-ball forteam-return notification IS now sent (CarryFrame -> NotificationSystem.Info NEXBALL_RETURN_HELD_<team>) and the drop+reset effect IS applied.; No per-frame viewmodel scale (0.25) / EF_FLAME safe-pass lock indicator (client; QC ball_customize is CSQC setcefc). |
| 187 | 3.0 | `nexball` | `nexball.weapon.ballstealer` | presentation:missing | Primary launch routes velocity through WeaponFiring.ProjectileVelocity (newton-style OFF, no owner-velocity inheritance) where QC uses W_CalculateProjectileVelocity(actor, actor.velocity, ...); intended port-wide convention, but means the carrier's velocity is not added to the launched ball.; TackleTouch shove uses the secondary_force cvar directly (QC multiplies by toucher.damageforcescale, untracked in the headless player model — equals Base when damageforcescale==1).; No viewmodel / muzzle-flash / projectile model presentation (CSQC); QC spoofs WEP_PORTO.m_id in W_SetupShot to keep duplicates off the scoreboard — the port uses BallStealer identity instead (cosmetic, no scoreboard column for it). |
| 188 | 3.0 | `nexball` | `nexball.weapon.power_meter` | presentation:missing | No HUD power-meter progress bar (the HUD_Mod_NexBall charge bar is client-deferred — see nexball.hud.modicon). |
| 189 | 3.0 | `onslaught` | `onslaught.cp.touch_build` | presentation:missing | Presentation still missing: no EFFECT_CAP/EFFECT_RAGE particles, no pad-model swap (MDL_ONS_CP_PAD1/2), no build-bar waypoint sprite.; Audio emitted: SpawnIcon→ONS_CONTROLPOINT_BUILD on the CP; build completion→ONS_CONTROLPOINT_BUILT. |
| 190 | 3.0 | `onslaught` | `onslaught.cp.icon_damage` | presentation:missing | Audio emitted: shielded→ONS_DAMAGEBLOCKEDBYSHIELD to attacker (debounced); per-hit ONS_HIT1/2; destroy→ONS_GENERATOR_EXPLODE + ONSLAUGHT_CPDESTROYED_NONAME info. Presentation still missing: no EFFECT_SPARKS/ROCKET_EXPLODE particles, no progressive icon model swap.; Proximity self-destruct nearby-enemy reward MODELED (IconDamage attacker==self branch: ONS_TAKES+1/SCORE+5 to every nearby enemy within proximitydecap_distance), closed alongside onslaught.cp.proximity_decap. |
| 191 | 3.0 | `onslaught` | `onslaught.cp.icon_think_regen` | presentation:missing | Damaged-FX EFFECT_ELECTRIC_SPARKS particle still not spawned (presentation/client). The ONS_SPARK1/2 ambient spark crackle IS now emitted: IconThink rolls random()<0.6-hp/max, then SPARK1 at random()>0.8 else SPARK2 at random()>0.5 (faithful branch structure). |
| 192 | 3.0 | `onslaught` | `onslaught.gen.damage_destroy` | presentation:missing | Audio emitted: blocked-by-shield hint; GENERATOR_UNDERATTACK center + ONS_GENERATOR_UNDERATTACK (10s debounce); per-hit ONS_GENERATOR_EXPLODE at chance damage/220 else ONS_HIT1/2; destroy→ONSLAUGHT_GENDESTROYED(_OVERTIME) info + ONS_GENERATOR_EXPLODE. Presentation still missing: progressive damage `frame=10*(1-hp/max)`, flaming-gib/EFFECT_SPARKS particles, ons_camSetup death-cam.; NOTE: the per-hit 'gib impact' branch maps QC SND_ROCKET_IMPACT to ONS_GENERATOR_EXPLODE; under-attack alarm uses PlayGlobal not play2team. |
| 193 | 3.0 | `onslaught` | `onslaught.cp.proximity_decap` | presentation:missing | Proximity-decap IMPLEMENTED in IconThink: (friendly - enemy) live-player count within g_onslaught_cp_proximitydecap_distance changes icon HP by ±dps*THINKRATE each tick; at <=0 the icon is destroyed via IconDamage (lone enemy, or `self` to spread the multi-attacker +5 reward to all nearby enemies); a pure-friendly crowd repairs it. Master toggle defaults OFF in Base. Remaining: no waypoint/HUD presentation of the decap state. |
| 194 | 3.0 | `physics-player` | `physics-player.water.swim` | liveness:unknown | Liveness of the swim path on a live water map not behaviorally confirmed this audit (depends on CheckWater correctly setting WaterLevel from map liquid contents — the SUPERCONTENTS bitmasks are mirrored as literals and untested against a real BSP water volume here). |
| 195 | 3.0 | `physics-player` | `physics-player.ladder` | liveness:unknown | Not behaviorally tested on a live ladder map this audit; code-verified only (liveness:unknown). The func_water + viewloc ladder sub-cases are additionally inert on stock 3D maps (ViewLoc null; func_water ladder is a rare map feature). |
| 196 | 3.0 | `physics-player` | `physics-player.jetpack` | liveness:unknown | PM_jetpack maxspd_mod factor (player.qc:742 maxairspd *= max(1, maxspd_mod)) is now threaded through PMJetpack as maxspeedMod (PlayerPhysics.cs:886 `mp.MaxAirSpeed * MathF.Max(1f, maxspeedMod)`), so it is NO LONGER hardcoded to 1 — a spectator/high-speed-mod jetpack scales correctly. (Was a gap in the baseline; closed.) Residual: still unreachable in normal play (a real player has maxspeed_mod==1).; cl_jetpack_jump is pinned ON / mode-1 semantics (JetpackJumpEnabled const); QC mode 2 (latched thrust between frames) is not modelled. |
| 197 | 3.0 | `sv-chat` | `sv-chat.ignore.crud` | logic:partial | Permanent (db-persisted) ignore tier is dropped (the QC return-2 path / ServerProgsDB by UID); ignores are keyed by PersistentId in-memory only (acceptable within a match, lost across server restarts). This is the sole remaining logic-partial reason. |
| 198 | 3.0 | `sv-chat` | `sv-chat.helpers.printtochat` | liveness:partial | All six helpers (PrintToChat / DebugPrintToChat / PrintToChatAll / DebugPrintToChatAll / PrintToChatTeam / DebugPrintToChatTeam) are ported faithfully (Chat.cs:770-823): the \{1}^7 (\x01^7) chat marker + trailing \n, the autocvar_developer>0 gate on the Debug* variants (Cvars.Float("developer") > 0), and the bprint server-console echo (modeled by the sv_dedicated-gated DedicatedPrint tail). LIVENESS is now PARTIAL: PrintToChatAll has ONE live caller (the Tuba recognized-melody broadcast — Tuba.cs:274 invokes Tuba.TubaMelodyBprint, wired in GameWorld.cs:663-668 to Commands.Chat.PrintToChatAll, the QC W_Tuba_NoteOff bprint). The other five (PrintToChat / PrintToChatTeam / DebugPrintToChat / DebugPrintToChatAll / DebugPrintToChatTeam) remain DEAD — no port caller, matching Base (every teamplay.qc / sv_random_items.qc use of those is commented-out debug). |
| 199 | 3.0 | `sv-cheats` | `sv-cheats.cmd.particles_make` | logic:partial | make: mode-1 surface-align is an APPROXIMATION — Base does fixedvectoangles2(normal, v_forward) then AnglesTransform_ApplyToAngles(angles,'-90 0 0'); the port (Cheats.cs:463-472) does FixedVecToAngles2 + a flat pitch -=90 (comment-acknowledged). The e.mdl='rocket_explode' wreck/explosion model is presentation-only and intentionally skipped.; penalty: only acts when the live gametype is Race (Cheats.cs:212); in non-race modes Base's race_ImposePenaltyTime is also effectively inert, so faithful, but the <reason> string's CSQC HUD notification is dropped (presentation-only). |
| 200 | 3.0 | `sv-cheats` | `sv-cheats.mapent.info_autoscreenshot` | logic:partial | g_observepoints membership is unwired: Base does tracebox(MOVE_WORLDONLY) and IL_PUSH(g_observepoints) only when NOT start-solid (the observer camera cycles these); the port has no observer-camera g_observepoints consumer (client concern) and skips the start-solid test entirely, keeping every point findable. For the TELEPORT cheat path (the only port consumer) this is faithful — it only needs the entity to exist with the right origin/angles. |
| 201 | 3.0 | `sv-client-lifecycle` | `sv-client-lifecycle.connect.client_connect` | logic:partial | Connect-side server->client channel partly deferred (see connect.fix_client_cvars_welcome row): no clientstuff/FixClientCvars/_teams_available stuffcmds, no Handicap_Initialize-here/model_randomizer, no Send_NextMap. (The version_nagtime arming, the `clientversion` request handler, the MOTD/g_mutatormsg/mutators welcome lines, and playban/chatban ARE now wired.); Player_DetermineForcedTeam / forced-team-otherwise spectator routing IS now wired (ClientManager.cs:220 DetermineForcedTeam), with team pre-assigned via AssignBestTeam for team games. |
| 202 | 3.0 | `sv-client-lifecycle` | `sv-client-lifecycle.spawn.put_player_in_server` | logic:partial | In-PutPlayerInServer team assignment (bot_forced_team / wants_join>0 SetPlayerTeam / TeamBalance_JoinBestTeam) is not run here — the port pre-assigns the team once at connect, so a mid-match team change or wants_join preference isn't honored on respawn.; wr_resetplayer + reload-all-reloadable-weapons-on-spawn (weapon_load = reloading_ammo) and CL_SpawnWeaponentity per slot are flagged as client/networking concerns and not done server-side.; FixPlayermodel resolves every player+bot to sv_defaultplayermodel (client playermodel cvar not plumbed); faithful stand-in but not the real per-client model. GiveRandomWeapons IS wired (randomCount>0 branch); the ForbidRandomStartWeapons hook is not dispatched. |
| 203 | 3.0 | `sv-client-lifecycle` | `sv-client-lifecycle.observer.put_observer_in_server` | logic:partial | Still omits: SelectObservePoint / observe-spawn placement (the port keeps the current origin instead), VoteCount/ReadyCount recount, the SPECTATE_WARNING / sv_maxplayers_spectator_blocktime block, CHAT_NOSPECTATORS notice, RemoveGrapplingHooks/Portal_ClearAll, alivetime PlayerStats report, and the is_forced/use_spawnpoint signature (the port has no is_forced/use_spawnpoint params). |
| 204 | 3.0 | `sv-client-lifecycle` | `sv-client-lifecycle.observer.observer_think` | logic:partial | SpectatePrev (impulse 12/16/19/220-229), the IMP_weapon_drop CAMERA_SPECTATOR cycle, MinigameImpulse handling, the +use clippedspectating MOVETYPE_FLY toggle, and the sv_spectate 2 'spectating not allowed' block are not modeled. |
| 205 | 3.0 | `sv-client-lifecycle` | `sv-client-lifecycle.observer.autojoin_delayed` | logic:partial | g_maxping / forced-team (TEAM_FORCE_SPECTATOR) / AutoJoinOnConnection mutator hook / (!teamplay \|\| g_balance_teams) gates deferred. The QC `ping > 0` precondition (delay until centreprints work) is not modeled. sv_spectate and g_campaign holds ARE honored. |
| 206 | 3.0 | `sv-client-lifecycle` | `sv-client-lifecycle.disconnect.client_disconnect` | logic:partial | Still not modeled: ignore-list cleanup, Portal_ClearAll/RemoveGrapplingHooks, chatbubble/killindicator/personal/g_counters delete, minigame part, WaypointSprite_PlayerGone, ReadyCount/VoteCount-on-leave, sv_hook_lastleave, and TeamBalance_RemoveExcessPlayers / QueuedPlayersTagIn. (Vehicle exit, player_powerups_remove_all + poweroff sound, the status-effect clear, the gametype objective-drop, and the generic MUTATOR_CALLHOOK(ClientDisconnect) ARE now modeled.) |
| 207 | 3.0 | `sv-impulse` | `sv-impulse.weapon.priority_cycle` | liveness:partial | BROKEN PER-CLIENT WIRING (re-audit 2026-06-27): Inventory.PriorityProviderForGroup is declared (Inventory.cs:191) but NEVER ASSIGNED anywhere (grep: no `PriorityProviderForGroup =` in the tree; only the MAIN Inventory.PriorityProvider is wired at Commands.cs:511). So WeaponPriorityForGroup's per-client branch (provider invoke) is always null/empty and it falls through to `Api.Cvars.GetString("cl_weaponpriority"+number)` — the SERVER's own global cvar, not the requesting player's value. The client's cl_weaponpriority0..9 ARE replicated (SentCvarAllowlist, Commands.cs:222-224), fixed-up (CmdSentCvar:3330-3335, FixWeaponOrder allow-incomplete), and stored per-client (SetClientCvar:3337) — but NOTHING READS that store for the group lists. Result on the live remote-client path: a player's custom per-group priority category lists are ignored; the bind cycles whatever cl_weaponpriority<N> the server host happens to have (typically the stock default / empty -> main-list fallback). The handler/dispatch/group-index threading is faithful; only the data source is mis-wired. (A listen-server host whose client cvars ARE the server cvars would coincidentally see the right list.); Minor (secondary): when the group's priority cvar is unset/out-of-range, WeaponPriorityForGroup falls back to the MAIN cl_weaponpriority rather than QC's empty-category no-op. |
| 208 | 3.0 | `sv-impulse` | `sv-impulse.weapon.getcycle` | logic:partial | The impulse-group complain path is simplified: QC computes have_other and a custom-group bitmask for imp<0 (selection.qc:148-171); the port omits both since it drives the imp=-1 cycle/best path. The weaponsInMap (map-spawned-but-unowned weapon) branch (selection.qc:166,187) is unmodeled, so unowned-but-on-map weapons are skipped (cannot be cycle-selected before pickup). |
| 209 | 3.0 | `sv-impulse` | `sv-impulse.cheat.command_impulse` | liveness:partial | CLONE_MOVING (140) and CLONE_STANDING (142) are still unported — they need a CopyBody engine service (clone the player entity into a static decoy, ++this.lip, with the v_forward*300 momentary nudge for the moving variant) the port does not expose. Neither is routed in DispatchImpulse. (This is why liveness stays PARTIAL for the row: 30/141 are live, 140/142 are unreachable.); SPEEDRUN_INIT (30) snapshot is faithful (origin/v_angle/velocity/RES_*/WEAPONS/items/statuseffects/pause timers/teleport_time into Player.PersonalCheckpoint, health floored at 1). The .personal is modeled as a value record on the Player, not a simulated edict (QC's personal_wp edict only ever stores fields, so this is behaviorally equivalent).; NOTE: there is no separate 'drag' or 'fixed-clone' CHIMPULSE — Drag is mouse-grab via CheatFrame/CheatCommand, not an impulse. The actual unported impulse set is exactly CLONE_MOVING(140) and CLONE_STANDING(142). |
| 210 | 3.0 | `sv-intermission` | `sv-intermission.mapvote.server_core` | liveness:partial | Wave 17 ADDED a NetControl.MapVote=22 opcode (NetProtocol.cs:240, ProtocolVersion 12→13) and a full server-side per-peer ballot push (ServerNet.SendMapVote, ServerNet.cs:1011, on the live SendAll loop) — but the CLIENT DECODE was never written, so remote parity is still NOT achieved. ClientNet.OnPacket's dispatch switch (ClientNet.cs:530-547) has no `case NetControl.MapVote` → the frame is dropped at `default: break;`; there is no ClientNet.HandleMapVote (the ServerNet.cs:1010 comment 'mirrors ClientNet.HandleMapVote' points at a nonexistent method). Net: the server now serializes the ballot to every peer, but no remote client decodes it. On a LISTEN server the cast path is live (number-key 1..9 → C2S impulse → Commands.DispatchImpulse → MapVoting.CastVote while Running). On a dedicated/remote-client server the vote still closes on timeout/PlayerCount with zero real votes (deterministic RNG tiebreak). |
| 211 | 3.0 | `sv-intermission` | `sv-intermission.mapvote.client_ui` | liveness:partial | The panel is STILL fed only from the in-process GameWorld.MapVote (NetGame.UpdateMapVotePanel, NetGame.cs:3729, reads _serverWorld?.MapVote), so it lights up ONLY on a listen server. Wave 17 added the server-side NetControl.MapVote=22 push (ServerNet.SendMapVote), but ClientNet never decodes it (no HandleMapVote, no switch case — the frame is dropped at OnPacket's default) and UpdateMapVotePanel has no networked-source branch, so a pure --connect client's ballot still stays blank and it cannot vote. Full remote parity now needs only the client DECODE + a networked feed for this panel (see sv-intermission.mapvote.server_core); the wire opcode and server send already exist.; The panel has no mouse/cursor click handler (Base MapVote_InputEvent lets you click a map tile); voting is by number-key 1..9 only. Faithful enough to vote, but the click-to-vote affordance is absent. |
| 212 | 3.0 | `sv-ipban` | `sv-ipban.insert` | logic:partial | The dosync leg (OnlineBanList_SendBan, with the reason!="" and not-~-prefixed guard) is dropped — no online ban propagation on insert (see sv-ipban.sync.online). |
| 213 | 3.0 | `sv-mapvoting` | `sv-mapvoting.gametype_vote` | logic:partial | FIXED: the sv_vote_gametype_hook_all/_<name> localcmd hooks NOW FIRE. Commands.GameTypeVoteHookHandler is assigned at NetGame.cs:826-827 to `name => _serverWorld?.LoadedConfig?.ExecuteLine($"sv_vote_gametype_hook_{name}")`, and ApplyGametypeSwitch invokes it for 'all' + the winning gametype (GameWorld.cs:4164-4165). Stock hooks are empty aliases (no visible effect), but a server.cfg that redefines an alias now runs against the cvar store. (Was the primary baseline logic-partial reason — now resolved.); STILL PARTIAL: GameTypeVote_AvailabilityStatus' next-map availability filter is absent — StartGametype marks every resolvable type GTV_AVAILABLE (MapVoting.cs:307-309, the comment documents the missing MapInfo filter) without the MapInfo_Get_ByName(get_nextmap()) + supported-gametypes check, so an option the queued nextmap can't support is shown available instead of dimmed.; STILL PARTIAL: Maplist-reset enumerate fallback absent — ApplyGametypeSwitch sets g_maplist only from a per-gametype sv_vote_gametype_<name>_maplist cvar (GameWorld.cs:4169-4173); with maplist_reset=1 but no per-gametype list Base regenerates g_maplist via MapInfo_ListAllowedMaps, the port keeps the current list (GameWorld.cs:4174-4175, the comment documents the gap).; The match_gametype temp-switch / restart-reapply dance (MapVote_Init's `if (match_gametype) GameTypeVote_SetGametype(...)`) is not modeled; the port applies the resolved gametype to the `gametype` cvar for the next boot (GameWorld.cs:4160) rather than live-switching MapInfo mid-process. |
| 214 | 3.0 | `sv-mapvoting` | `sv-mapvoting.client.draw` | liveness:partial | Panel is FED on a listen server: NetGame.UpdateMapVotePanel calls SetVote/SetVotes/SetAvailability/OwnVote/SetWinner and owns Visible, so the player sees the ballot grid, level-shots, live counts, own-vote highlight, the selection pulse, the top-2 reduce-fade, and the winner reveal during a real listen-server match.; The GametypeVote DRAW path is ALSO driven: UpdateMapVotePanel passes gametypeVote: isGametypeVote and feeds gametype icons (gfx/menu/<skin>/gametype_<name>), pretty DisplayNames + descriptions + sv_vote_gametype_detail, so GameTypeVote_DrawGameTypeItem's counterpart renders during a real gametype vote.; The mv_top2_alpha reduce-fade is driven: SetAvailability stamps _top2Time on first reduce.; STILL DEAD for remote/dedicated-server clients: no net entity feeds the panel when _serverWorld is null (see sv-mapvoting.net.sync), so a non-listen client never sees the vote screen (liveness partial). |
| 215 | 3.0 | `sv-spawnpoints` | `sv-spawnpoints.select.select_spawn_point` | logic:partial | Emergency fallback differs: Base, when Spawn_FilterOutBadSpots returns empty, RE-FILTERS the spot list once with targetcheck=false (server/spawnpoints.qc:421-435) before giving up. The port runs the targetcheck=false re-filter (SpawnSystem.cs:435-436), then returns null if even that comes back empty (SpawnSystem.cs:445-446) so the ClientManager caller can retry/log — rather than seeding all spots at prio-0. Faithful in shape; the only divergence is the post-re-filter NULL handling vs Base's error()/GotoNextMap branch. Low severity. UNCHANGED.; anypoint / g_spawn_useallspawns path: Base skips Spawn_FilterOutBadSpots and does Spawn_WeightedPoint(1,1,1) over ALL spots (spawnpoints.qc:413-416). The port mirrors this (SpawnSystem.cs:415-421: seeds all spots at prio0/weight0, NO startsolid/team/active/restriction filtering) — matches Base. UNCHANGED (faithful).; testplayerstart debug edict (spawnpoints.qc:363-370) — Base returns a 'testplayerstart' classname spot before any other logic; not ported (debug-only). UNCHANGED. |
| 216 | 3.0 | `sv-spawnpoints` | `sv-spawnpoints.mutator.spawn_near_teammate` | logic:partial | Residual relocation-branch defers: tracebox_hits_trigger_hurt (hurt-volume reject), checkpvs (PVS visibility), and the g_nades nade-in-range reject are still skipped (a faithful superset that may keep a spot QC would reject); weaponLocked and PHYS_INPUT_BUTTON_CHAT gates are always false (no plumbed input/weapon-lock); closetodeath uses the player's current origin not .death_origin. The sky-surface (Q3SURFACEFLAG_SKY) and lava/slime/water pointcontents rejects ARE applied. STILL OPEN (the residual defers), but no change since baseline. |
| 217 | 3.0 | `sv-teamplay` | `sv-teamplay.admin.lock_shuffle_move` | logic:partial | moveplayer/team don't enforce lockteams backup/restore semantics of MoveToTeam (which temporarily disables the lock for the move then restores it); behavior overlaps but isn't the exact MoveToTeam path. |
| 218 | 3.0 | `sv-world-rules` | `sv-world-rules.warmup.reset_map` | logic:partial | No per-client accuracy_reset pass on reset_map (the per-player hit/fire accuracy counters are not zeroed). Inventory_clear is subsumed by the Clients.Spawn -> PutClientInServer start-loadout re-give. |
| 219 | 3.0 | `sv-world-rules` | `sv-world-rules.misc.shutdown` | logic:partial | ServerProgsDB persistence on shutdown (db_save/db_dump of server.db<sessionid>) has no port analogue.; bot_endgame, CheatShutdown, WeaponStats_Shutdown and MapInfo_Shutdown are not performed on world teardown. |
| 220 | 3.0 | `tka` | `tka.ball.drop` | presentation:missing | DropBall is invoked from OnDeath (LIVE death hook) when the victim carried. It detaches, drops at the feet with the crandom xy scatter, arms the 0.5s self-recapture lockout, re-arms the relocate think, plays SND_KA_DROPPED, sends INFO/CENTER_KEEPAWAY_DROPPED, and (via CollectWaypoints next tick) shows the loose-ball waypoint.; FIXED (liveness partial -> live): the use-key, observer, and disconnect drop triggers are now wired. OnUseKey subscribes MutatorHooks.PlayerUseKey (Activate, TeamKeepaway.cs:387) and is fired live via VehicleBoarding.cs:106 FirePlayerUseKey on the +use impulse path (QC tka PlayerUseKey -> tka_DropEvent, consumes the press). OnPlayerRemoved (TeamKeepaway.cs:406) drops the ball when the carrier is demoted to observer or disconnects, dispatched from ClientManager.cs:513 (PutObserverInServer/MakePlayerObserver) + :761 (ClientDisconnect) — QC MakePlayerObserver/ClientDisconnect both tka_DropEvent.; RESIDUAL: DropSpecialItems (the special-item-strip drop edge) is still not wired — 3 of Base's 4 drop hooks are now live. Also: no nudgeoutofsolid_OrFallback, no multi-ball chain advance / tka_PlayerReset VIP clear. |
| 221 | 3.0 | `turret-fusionreactor` | `turret-fusionreactor.fx.attack` | liveness:partial | No client turret render (ENT_CLIENT_TURRET): the reactor renders no model and no head, so the ammo-scaled head spin (Base '0 250 0'*ammo/ammo_max, up to 250 deg/s) is computed onto TurretState.HeadAVelocity but never integrated/displayed. No waypoint sprite/healthbar/death FX either. |
| 222 | 3.0 | `weapon-arc` | `weapon-arc.beam.burst_variant` | presentation:missing | ARC_BT_BURST visual thickness (14) and burst beam color/visuals absent (depends on the beam.visual feature). UNCHANGED. |
| 223 | 3.0 | `weapon-blaster` | `weapon-blaster.bolt.think_lifetime` | presentation:missing | CSQCProjectile(PROJECTILE_BLASTER) client trail/sprite STILL not networked: the flying bolt has no DP-style render trail (Wave-1/2 added no client-render seam for the bolt). |
| 224 | 3.0 | `weapon-crylink` | `crylink.menu.describe` | presentation:missing | The MENUQC describe() weapon-guide prose (burst of laser-like projectiles, converge-on-release, double-damage-on-bounce, secondary tight-group, cells ammo, 'crylink running' pull blurb + W_Guide_Keybinds/DPS_bothMultishot) has no port. Menu-only documentation; no gameplay impact. Consistent with the weapon-shotgun.menu.describe gap. |
| 225 | 3.0 | `weapon-minelayer` | `minelayer.describe` | presentation:missing | The MENUQC describe() weapon-guide prose (minelayer.qc:549-563 — the multi-paragraph in-menu weapon description shown in the Weapons guide page) has no port analogue. Several other weapons reference describe() only in comments (Electro.cs:98, Tuba.cs:63, Crylink.cs:160) but the port has no weapon-guide menu page, so this is a menu-only presentation gap (no gameplay effect). |
| 226 | 2.8 | `bot-ai` | `bot-ai.aim.lead` | logic:partial | Ballistic aim is a closed-form gravity-drop approximation, not QC's 10-trace tracetoss search; lobbed-weapon aim (mortar/nade) is less accurate, especially with obstacles in the arc, and never traces the world to confirm the toss lands. (UNCHANGED.); Shot speed is read from g_balance_<wep>_primary_speed/gravity for the single ChosenWeapon (via the BotAimShotSpeed override hook, now used by Devastator/Crylink/Fireball), not per-weaponentity with W_WeaponSpeedFactor applied; weapons without those cvars are treated as hitscan (no lead). (PARTIALLY NARROWED.) |
| 227 | 2.8 | `bot-ai` | `bot-ai.roles.goalrating` | logic:partial | DEAD CODE: a full QC navigation_routerating Dijkstra route-cost field WAS coded in Wave 16 (GoalRater.SeedRoute → WaypointNetwork.ComputeRouteCosts/RouteCostTo, plus a multi-seed NearestSeeds + FindPath(seeds) A*) but NOTHING CALLS SeedRoute (grep-verified across all of src/). GoalRater._routeSeeded therefore stays false, so Rate ALWAYS uses straight-line vlen distance and the Dijkstra branch never executes. The headline 'straight-line not Dijkstra' divergence is NOT closed; a future pass must wire SeedRoute into BotBrain before Role() and switch SetGoal to FindPath(NearestSeeds(...), goalWp). (UNWIRED 2026-06-27 third pass.); rangebias is still hardcoded 2000 for every rate call (items/enemies/roam) instead of the per-rating-call bias QC uses. (UNCHANGED.); No separate _movingtarget (5.5s) strategy interval for goals that can move. (UNCHANGED.); enemyplayers aggression refinement: QC skill>3 nudges the t factor by Strength/Shield powerup timers (StatusEffects_gettime); the port has no status-effect-expiry accessor yet, so that refinement is deferred. |
| 228 | 2.8 | `cl-crosshair` | `cl-crosshair.color.special_modes` | logic:partial | crosshair_color_special 3 (rainbow: randomvec()*brightness re-rolled every rainbow_delay, crosshair.qc:189-196) is not ported — ColorModeCvar (CrosshairPanel.cs:537-542) maps only 0/1/2, so mode 3 silently falls back to fixed color. The rainbow_brightness/delay cvars are registered but dead. |
| 229 | 2.8 | `cl-crosshair` | `cl-crosshair.objring.nade_capture_revive` | liveness:partial | IMPROVED (W17): NADE_TIMER + REVIVE_PROGRESS are now fed on BOTH paths via the consolidated UpdateCrosshairFeedback (NetGame:3163, called unconditionally at 2681). Host: reads the live LocalServerPlayer (NadeTimer 3173 / ReviveProgress 3174). Pure remote client: reads the networked own-entity slice ClientNet.LocalState (NadeTimer 3188 / ReviveProgress 3190) — these ARE produced server-side (NadesMutator.cs:187 -> p.NadeTimer; FreezeTag.cs:796 -> p.ReviveProgress) and networked (ServerNet.cs:2102-2104 -> NetEntityState, NetEntity.cs Feedback codec). So the nade + thaw rings now render for a remote client too (the prior baseline's 'remote read-back missing' gap is CLOSED for nade/revive).; STILL DEAD: CAPTURE_PROGRESS has no production producer — nothing assigns Player.CaptureProgress, ServerNet.cs:2100 deliberately leaves NetEntityState.CaptureProgress at 0 (commented 'no gametype sets it'), and on the host path UpdateCrosshairFeedback hardcodes x.CaptureProgress = 0f (NetGame:3175). The remote path reads ls.CaptureProgress (3189) but it is always 0. The 2nd-priority capture ring never draws in production (grep finds only test-side ClientFeedbackTests.cs:275).; Rings drawn centred on the crosshair panel (radius 64px) rather than at Base's `0.5*conwidth, 0.6*conheight` screen anchor with `0.1*conheight` radius; nade-timer text label (cl_nade_timer 2) not drawn. UNCHANGED. |
| 230 | 2.8 | `cl-csqcmodel` | `cl-csqcmodel.predraw.dispatch_order` | logic:partial | Order is appearance->LOD->effects (skeletal anim + torso-action overlay run in a separate PlayerModel.Pose pass, not interleaved here); the appearance-before-LOD ordering is moot because LOD is a no-op swap.; AutoTagIndex recursion + drawmask=0 on null-model are not part of this pass (held weapons attach via ViewEntityRenderer). |
| 231 | 2.8 | `cl-hud` | `cl-hud.panel.radar` | liveness:partial | Maximized clickable mode (ons spawn / teleport select) is still unported; the discovered radar panel is force-hidden (StartHiddenIds) to avoid a double radar and the live radar is the standalone _radar. |
| 232 | 2.8 | `cl-scoreboard` | `cl-scoreboard.field.formatting` | logic:partial | SP_FPS and SP_SKILL columns never render (FieldByLabel returns null for fps/skill — client-only display fields with no port plumbing); the GetField FPS/SKILL branches exist (ScoreboardPanel.cs:684-697) but are unreachable because the columns are never fed.; scores_per_round averaging is now plumbed (ScoresPerRound + per-field roundsPlayed division), but no interactive scores_per_round toggle (Ctrl+R) — only a console cvar drives it. |
| 233 | 2.8 | `fx-effectinfo` | `fx-effectinfo.emit.by_name_fallback` | liveness:partial | The Effect=null engine-fallback request (unregistered effectinfo name) records the name but there is no DP-engine __pointparticles equivalent client-side; only names that also resolve in the effectinfo.txt catalog render. Used by te_csqc_lightningarc/te_beam (arc_lightning/heal_beam) which are handled by the beam renderer rather than the registry. |
| 234 | 2.72 | `cl-hud` | `cl-hud.panel.itemstime` | values:unknown | Progressbar / dynamicsize / ratio-columns fidelity not runtime-verified. |
| 235 | 2.72 | `cl-hud` | `cl-hud.panel.strafehud` | values:unknown | Bar zones / best-angle marker / wturn-switch indicator + mode/style/range fidelity not runtime-verified. |
| 236 | 2.7 | `turret-walker` | `turret-walker.drive.gaits_extra` | logic:partial | No standalone STRAFE_L/R or PAIN gaits (Base never drives these from the SVQC brain either — they are leftover model anim states — so this is parity-by-omission, not a behavioral gap).; JUMP/LAND/TURN gait cases exist in ApplyGait but are never assigned (same as Base — the brain never sets them). |
| 237 | 2.5 | `mutator-dodging` | `mutator-dodging.frozen.dodge` | logic:partial | DodgingMutator.PhysFrozen (DodgingMutator.cs:273) tests `player.FrozenStat != 0` ONLY — it does NOT include the STATUSEFFECT_Frozen path. The port's own canonical PHYS_FROZEN (PlayerPhysics.IsFrozen, PlayerPhysics.cs:1859-1865) is `FrozenStat != 0 \|\| StatusEffectsCatalog.Has(player, Frozen)`. So a player frozen by the STATUSEFFECT_Frozen status effect (e.g. a freeze/cold nade) — as opposed to the Freeze-Tag gametype stat — is NOT seen as frozen by the three dodging frozen branches: DetermineForce won't return HorizForceFrozen (200), and the frozenDodging / frozenNoDoubletap carve-outs in CheckPressedKeys + PMDodging won't engage. The frozen-wishmove bound(-2,2) at PlayerPhysics.cs:219 DOES use the cvar gate (sv_dodging_frozen) but the mutator's own frozen detection is narrower than Base PHYS_FROZEN. Low practical impact (sv_dodging_frozen defaults 0, and Freeze Tag uses FrozenStat), but a real logic divergence wrongly closed to gaps:[] in the prior audit. |
| 238 | 2.5 | `mutator-nades` | `mutator-nades.boom.dispatch` | presentation:missing | The colored explosion particle (Send_Effect_Except with per-type EFFECT + player-color min/max via findbetterlocation) is omitted (render-only).; Base plays SND_Null on CH_SHOTS_SINGLE + SND_ROCKET_IMPACT on CH_SHOTS; the port plays only weapons/rocket_impact.wav. |
| 239 | 2.5 | `mutator-nades` | `mutator-nades.boom.napalm` | presentation:missing | EFFECT_FIREBALL_LASER trail + EF_FLAME ball effect omitted (render). SND_NADE_NAPALM_FIRE spamsound on ball-eject is not emitted (audio partial). |
| 240 | 2.5 | `mutator-nades` | `mutator-nades.damage.freezetag_revive` | logic:partial | The port suppresses the self-nade damage+force but does NOT call freezetag_Unfreeze / set freezetag_revive_nade_health / send EFFECT_ICEORGLASS + INFO_FREEZETAG_REVIVED_NADE/CENTER_FREEZETAG_REVIVE_SELF notifications (documented cross-task seam — FreezeTag owns the unfreeze). So the revive-nade does not actually revive. |
| 241 | 2.5 | `mutator-overkill` | `overkill.respawn.remember_restore_weapon` | logic:partial | Port records/restores only slot 0: OnPlayerDies stores Inventory.CurrentWeapon(target) into OkLastWeapon[0] and nulls slots 1..MAX_WEAPONSLOTS; Base captures every slot's m_switchweapon (ok_lastwep[MAX_WEAPONSLOTS]). Low practical impact — Overkill loadouts occupy slot 0 only — but the per-slot capture is not faithfully reproduced. UNCHANGED by Wave-2. |
| 242 | 2.5 | `mutator-random_gravity` | `random_gravity.roll.round_gate` | liveness:partial | Liveness is partial-by-design: RoundNotStartedProvider is wired only inside GameWorld.EnableRounds (GameWorld.cs:1645), i.e. only for round-based gametypes (CA/Freezetag/round Domination/…). In non-round modes the provider is null and RoundGateBlocks() returns false — which exactly matches QC where round_handler_IsActive() is false outside round modes. So the gate is correctly inert where Base is inert, and active where Base is active. Not a defect, just mode-scoped reachability. |
| 243 | 2.5 | `mutator-sandbox` | `mutator-sandbox.object.duplicate` | liveness:partial | WIRED: copy/paste reachable via CmdSandbox → HandleCommand; PASTE is fully live (flood/cap/ObjectPortLoad).; CORRECTED (re-audit 2026-06-27): COPY clipboard is NO LONGER an empty no-op — SandboxObjectStore.SetClipboard now calls SetSandboxClipboard (NetGame.cs:948/971-983), which, FOR THE LOCAL-HOST player (ReferenceEquals(player, LocalServerPlayer)), un-escapes the serialized value and sets cl_sandbox_clipboard directly in the shared cvar store (+MarkArchived). So on a listen server, `object_duplicate copy` genuinely fills the local console's clipboard cvar, and a following paste reads it. Residual (now the only gap): a REMOTE client on a dedicated server is silently skipped — the port has no svc_stufftext channel to set a remote client's cvar. Hence liveness:partial (local-host live, remote dead).; Paste shares the flood + maxobjects gates with object_spawn (faithful). |
| 244 | 2.5 | `mutator-waypoints` | `waypoints.registry.defs` | liveness:partial | Residual: the wp_extra-DRIVEN spritelookupblinkvalue overrides (superweapon Weapon=2, Item=item.m_waypointblink) are not modeled — Weapon's def blink is a flat 1 and there is no per-instance wp_extra blink lookup. Harmless while those producers (superweapon pickups) are absent. The Seeker/Item/Weapon DEFS are now exercised by live producers; only the wp_extra per-instance blink override is unmodeled.; liveness: partial — defs are reachable only once their gametype emits waypoints; CTF + Domination + FreezeTag + Keepaway + TeamKeepaway + Assault + Key Hunt + LMS + itemstime (Item/Weapon) + Seeker + Weapon_whereis (Weapon def) + Monster (g_monsters_healthbars) + player-deployed pings now exercise their defs. Still NOT on the live path: Ons*/Nb*/Race*/Vehicle*/Buff. |
| 245 | 2.5 | `mutator-waypoints` | `waypoints.server.manager_api` | liveness:partial | DEPLOY + CLEAR + HELPME slice is live (DeployFixed/DeployPersonal/Attach/ClearPersonal/ClearOwned/HelpMePing from Commands.cs; Reset() + Think() in GameWorld; SendWaypoints merges WaypointSprites.Active per peer).; OBJECTIVE/UPDATE slice is BROADLY live now: SpawnFixed/Spawn/UpdateMaxHealth/UpdateHealth/Disown/Kill via Assault (SpawnObjectiveSprite); Spawn/UpdateBuildFinished/Ping/Kill via itemstime (ItemPickupRules.RespawnCountdown, ItemPickupRules.cs:794/809/824); Spawn/UpdateRule/Kill via Seeker (Seeker.cs:496/514/519); Spawn/UpdateMaxHealth/UpdateHealth/Kill via powerup drops (PowerupsMutator.cs:541-575); Spawn (personal, radarIcon 0) via Weapon_whereis (Inventory.cs:403); Spawn/UpdateMaxHealth/UpdateHealth/UpdateTeamRadar/Kill via MONSTER healthbars (MonsterAI.cs:452/460/461/433/691).; FIX this audit: UpdateTeamRadar is now LIVE — MonsterAI.ChangeTeam (monster_changeteam) re-colors the monster healthbar radar icon to the new team (MonsterAI.cs:433). Still NO live caller: AttachCarrier, DetachCarrier — the CTF/Dom/FreezeTag/Keepaway/KeyHunt/LMS carrier producers build WaypointSprite records directly in CollectWaypoints (not via the manager carrier helper). |
| 246 | 2.43 | `mapobject-teleporters-portals` | `portals.teleport.transform_and_amazing` | values:partial | The exact +/-48 plane-shift (Portal_ApplyTransformToPlayerAngle s/t bound onto the exit plane), Portal_FindSafeOrigin tracebox safe-origin validation, and the precise angle transform are replaced by the generic warpzone transform; angle/velocity/seam correctness is not behaviorally verified (values: partial). |
| 247 | 2.4 | `monster-framework` | `monster-framework.move.brain` | logic:partial | movelib_move_simple / movelib_brake_simple are approximated with a fixed 0.4 lerp / (1 - stopspeed*0.01) decay 'so behavior is stable regardless of frametime' rather than the QC frametime-scaled accel — timing/feel diverges slightly. (UNCHANGED.); steerlib_attract2 (the QC steering blend toward moveto with leadahead 500/0.95) is replaced by a direct (moveto-origin) yaw turn; the steer-prediction smoothing is not modeled. (UNCHANGED.); The MatchHalted brake-and-idle halt is LIVE (RunThink->Move on every monster Think); MatchHalted reads the live VehicleCommon.GameStopped (per-tick server write), so monsters brake-and-idle for prematch AND intermission/timeout. DRIFT FIX (2026-06-25): the campaign bots-may-start movement hold (QC sv_monsters.qc:846 autocvar_g_campaign && !campaign_bots_may_start) is now LIVE too — CampaignMovementHeld() reads MonsterAI.CampaignBotHold, wired at GameWorld.cs:686 to (g_campaign && !Campaign.BotsMayStart). MonsterMove mutator hook + draggedby remain deferred host concerns; the movelib/steerlib approximations remain (the residual partial). |
| 248 | 2.25 | `assault` | `assault.mapobject.assault_wall` | timing:partial | Timing: QC re-evaluates each func_assault_wall on a 0.2s nextthink; the port re-evaluates every tick (DriveWalls from the per-frame DriveFrame) but only re-stamps the edict on a state transition. Functionally equivalent (toggle happens at most ~0.2s sooner); transition-gated and cheap. Not a behavioral divergence. |
| 249 | 2.25 | `freezetag` | `freezetag.mode.registration` | values:partial | Match limit semantics differ: Base reads mapinfo pointlimit (10) / leadlimit (6) via g_freezetag_point_limit=-1 passthrough; port keys off fraglimit/leadlimit. Live default round target (10) coincidentally matches, leadlimit 6 likely not honored. |
| 250 | 2.25 | `freezetag` | `freezetag.revive.score` | values:partial | No nade revival bonus (g_nades_bonus_score_low) on a completed revive — minor, requires the nades mutator. |
| 251 | 2.25 | `freezetag` | `freezetag.round.flow` | values:partial | C# fallback timings (warmup 5/timelimit 180/enddelay 5) still differ from Base (10/360/0); masked live because gametypes-server.cfg ships. The round-timelimit-expiry tie path (CheckWinner ROUND_OVER) IS implemented (FreezeTag.cs:797-811: thaw all + ROUND_OVER notif + return -1) but depends on Handler.RoundEndTime being mirrored from the live RoundHandler. |
| 252 | 2.25 | `freezetag` | `freezetag.win.roundlimit` | values:partial | Lead-limit default (Base 6 via mapinfo) likely not honored (port reads leadlimit cvar, default 0 = off). |
| 253 | 2.25 | `keyhunt` | `keyhunt.unreachable.damage_destroy` | values:partial | Minor: g_balance_keyhunt_damageforcescale (default 1) is not read — the port's KeyDamage gates the re-team branch on force != 0 (raw force) rather than scaling the key's knockback by damageforcescale. At stock 1 this is a no-op; a tuned damageforcescale wouldn't affect the key's physical kick. The destroy/return behavior (NEEDKILL -> delay_damage_return) and the force-reteam logic are faithful. |
| 254 | 2.25 | `mapobject-movers-platforms` | `mapobject-movers-platforms.plat.outside_target_use` | liveness:partial | PlatOutsideTouch is implemented and faithful but DEAD — and that is itself faithful to Base: Base platforms.qc only DEFINES plat_outside_touch (line 111) and never installs/spawns an outside trigger anywhere, so stock func_plat never calls it. The port matches (no outside trigger generated). Reachable only via custom-mapper wiring neither Base nor the port produces. |
| 255 | 2.25 | `mapobject-target` | `mapobject-target.items.use` | logic:partial | The spawnfunc's normalized re-serialization of item tokens into a max/min/minus-prefixed give string (driven by spawnflags 0/1/2/4, then re-tokenized) is NOT reproduced; the port keeps the raw netname and parses it directly via GiveItems.Apply — equivalent for the common (give-string) case, but spawnflag-prefixed (max/min/minus) target_items semantics and the GetResource-driven resource serialization are not honored.; The loot-toucher delete branch (ITEM_IS_LOOT actor -> EXACTTRIGGER_TOUCH + delete, and IL_EACH delete of loot enemies) is not modelled (target_items is .use-only here; the port early-returns on an Item-flagged actor instead of deleting it). |
| 256 | 2.25 | `mapobject-target` | `mapobject-target.speaker.ambient_triggered` | logic:partial | QC objerrors a LOOPED_OFF untargeted speaker; the port silently leaves it inert (SpeakerSetup falls through with no error).; The *-prefixed per-player voice-message sample requires a real-client activator WITH a model; an ambient/null/no-model invocation falls back to silence (matching QC's SND(Null) not-found). For the ACTIVATOR path this is correct (it gates on IsRealClient first); for the looped/one-shot *-name paths a model is still required. |
| 257 | 2.25 | `mapobject-teleporters-portals` | `teleporters.spawn.entities` | logic:partial | teleport_findtarget bot-waypoint spawning (waypoint_spawnforteleporter) is omitted (bot navigation).; noise precache (FOREACH_WORD precache_sound) is a no-op; harmless headless. |
| 258 | 2.25 | `onslaught` | `onslaught.bot.roles` | timing:partial | Residual (logic simplifications, not partial-correctness): no waypoint-near-objective routing (Base navigation_routerating onto g_waypoints within 400 qu of the node + ++bestwp.cnt spread), no havocbot_attack_time (5s) throttle, no havocbot_goalrating_enemyplayers(20000,650) pass, and no havocbot_ons_target teammate-interest cost balancing. The decision FLOW (generator-attack first, skip CPs if a gen is rated, shield+neighbor-filter CPs, free-point ×2) is faithful.; timing partial: Base throttles re-rating via navigation_goalrating_timeout + havocbot_attack_time; the port re-rates every brain tick with no objective-attack cooldown. |
| 259 | 2.25 | `weapon-fireball` | `weapon-fireball.primary.shootable` | timing:partial | Depletion explode is SYNCHRONOUS (fired inside the damage call) rather than Base's deferred next-frame W_PrepareExplosionByDamage (nextthink=time; setthink), and the port does NOT call the MUTATOR_CALLHOOK(PrepareExplosionByDamage) hook. Shared Projectiles.MakeShootable simplification (Devastator/Electro/Hagar/Arc/Minelayer/Mortar/Seeker/OkRpc all share it). Only observable when a server raises g_balance_fireball_primary_health > 0 — at the stock health 0 the whole path is a faithful no-op. NOTE: the owner-reassign-to-attacker IS now ported (ShootDown gates it on g_projectiles_keep_owner==0, faithful to W_PrepareExplosionByDamage:94-98).; find/LaserPlay are still plain radius (warpzone-unaware) — unrelated to this row but the projectile world is non-warpzone like all port finds. |
| 260 | 2.0 | `cl-crosshair` | `cl-crosshair.core.master_gating` | logic:partial | NARROWED: a dedicated UpdateCrosshairGate (NetGame:3980, called live at 2996) now reproduces the REACHABLE subset of Base's per-frame suppress-set (crosshair.qc:226-235): HEALTH<=0/dead (LocalDeadNow), intermission==2/GAME_STOPPED (MatchIntermission), scoreboard_active, spectatee_status==-1 (IsObserving) — the crosshair shows only when havePlayer AND none of these hold. STILL MISSING from the suppress-set: cl_lockview, CAMERA_SPECTATOR==2 dead-spectator mode, the viewloc/non-FREEAIM gate, the DrawCrosshair mutator hook, the minigame-menu gate, and hud!=HUD_NORMAL (vehicle handoff). Gate runs only where the skinned panel draws (host path); a pure remote client's reticle is owned by NetHud and gated separately. |
| 261 | 2.0 | `cl-crosshair` | `cl-crosshair.anim.pickup_pulse` | liveness:partial | Host path only: UpdatePickupFeed calls CrosshairPanel.PulsePickup() (NetGame:3260) when a new weapon or a resource jump is detected on the LocalServerPlayer (re-baselined on respawn so the spawn loadout isn't counted). The crosshair bumps on item pickup on the host/listen-server. STILL host-only: the detection reads only LocalServerPlayer (NetGame:3218), so a pure remote client (LocalServerPlayer==null) gets no pulse — there is no networked STAT(LAST_PICKUP) equivalent in the LocalState slice (unlike the nade/hit feedback fields, which W17 wired for remote). UNCHANGED this pass.; Pickup trigger is derived (new-weapon-owned or per-frame resource jump > threshold) rather than the Base STAT(LAST_PICKUP) tick; approximate but matches the visible cue for the host player. |
| 262 | 2.0 | `cl-crosshair` | `cl-crosshair.anim.switch_crossfade` | liveness:partial | Keyed on the resolved Texture2D changing rather than the wcross_name/resolution string pair; with the per-weapon dict unpopulated the trigger only fires on a `crosshair` cvar change, NOT on every weapon switch — so in normal play (one crosshair number for all weapons) the cross-fade effectively never runs. |
| 263 | 2.0 | `cl-crosshair` | `cl-crosshair.ring.vortex_charge` | liveness:partial | Host path only: UpdateCrosshairWeaponRings feeds CrosshairPanel.ChargeFraction = st.VortexCharge and .ChargePool = st.VortexChargePoolAmmo (NetGame:3305-3306) while a vortex/vaporizer/*nex* weapon is active off the host-side slot-0 WeaponSlotState (the C# home of QC wepent.*). The Vortex charge ring + inner chargepool/moving-average ring render for the host player. VortexCharge is genuinely produced server-side. STILL host-only: UpdateCrosshairWeaponRings reads only LocalServerPlayer (NetGame:3286); a pure remote client (LocalServerPlayer==null) gets the -1 sentinel and no ring — the wepent ring fields are NOT in the networked LocalState slice (unlike the W17 nade/hit feedback fields). UNCHANGED this pass (line refs re-pinned).; Minor fidelity: ChargePool is fed unconditionally for vortex-like weapons (default VortexChargePoolAmmo=1f), so the inner chargepool ring shows even when secondary chargepool isn't enabled; Base gates the inner-pool branch on use_vortex_chargepool. Overkill-Nex weapon itself is otherwise absent from this port. |
| 264 | 2.0 | `cl-crosshair` | `cl-crosshair.ring.reload_ammo` | liveness:partial | Host path only: UpdateCrosshairWeaponRings feeds CrosshairPanel.ClipLoad = st.ClipLoad and .ClipSize = st.ClipSize when the active slot has a clip (NetGame:3317-3318). The ammo ring renders for every reloadable weapon on the host. ClipLoad/ClipSize are genuinely maintained on the slot by WeaponFireGate/WeaponFireDriver. STILL host-only: feeder reads only LocalServerPlayer (NetGame:3286); remote clients get the -1/0 sentinel and no ring (no networked clip stat in LocalState). UNCHANGED this pass (line refs re-pinned). |
| 265 | 2.0 | `cl-crosshair` | `cl-crosshair.ring.minelayer_hagar_arc` | liveness:partial | Host path only: UpdateCrosshairWeaponRings feeds CrosshairPanel.HagarLoad (st.HagarLoad, NetGame:3329), .MineCount (live g_mines scan of mines owned by the player, 3341-3344), and .ArcHeat (st.BeamHeat/OverheatMax with the overheat-jam latch, 3360-3363) when the matching weapon is active. Mine-count, Hagar burst-load, and Arc overheat rings render for the host player. HagarLoad/BeamHeat/ArcOverheat are genuinely produced server-side (Hagar.cs/Arc.cs). STILL host-only: feeder reads only LocalServerPlayer (NetGame:3286); remote clients get the -1 sentinel and no ring (no networked wepent ring state in LocalState). UNCHANGED this pass (line refs re-pinned). |
| 266 | 2.0 | `cl-crosshair` | `cl-crosshair.ring.switch_fade` | liveness:partial | Now reachable on the host path: with the weapon-stat rings fed (UpdateCrosshairWeaponRings), DrawStatRing produces rings and calls SwitchRingFade (DrawStatRing:943), so the switch fade runs when the host player swaps between ring-bearing weapons. Still dead for a pure remote client (no ring ever draws there). |
| 267 | 2.0 | `cl-csqcmodel` | `cl-csqcmodel.fallbackframe.remap` | liveness:partial | The runtime frame-id Remap table is live only on the MD3 ModelAnimator player path; the skeletal-IQM player path drives frames by clip NAME (BuildClipTable Pick keyword chains), which BAKE the same fallback discipline (melee->shoot, duckwalk-variant->duckwalk) into the clip resolution rather than remapping a numeric frame id at runtime. So the runtime remap rarely fires for skeletal players, but the substitution behavior is preserved by the keyword cascade. |
| 268 | 2.0 | `cl-scoreboard` | `cl-scoreboard.teams.grouping_totals` | values:partial | Only the primary team slot total is shown (SetWireRows stores wire.Teams primary slot); the secondary teamscore (e.g. keyhunt) is not networked/rendered (the wire's Teams list carries a single (team, score) pair).; team_size_position N/M side display is now PORTED (DrawTeamTotals:934-962, 0/1/2 placement), but totals are still drawn as colored banner boxes rather than the QC bold hud_fontsize*1.5 free-standing side numbers; size string uses 12px text on the box. |
| 269 | 2.0 | `cl-scoreboard` | `cl-scoreboard.block.mapstats` | liveness:partial | Map-stats are read from server-side static state (MonsterAI / MapObjectsState) directly on the client feed path — live for a listen-server host, but neither monsters nor secrets are networked as a STAT, so a remote (non-host) client would not see the rows. |
| 270 | 2.0 | `cl-teamradar` | `cl-teamradar.cvar.defaults` | liveness:partial | Discovered HUD-registry radar is force-hidden (HudManager StartHiddenIds 'radar'), so only the standalone NetGame instance (256x256 at 24,24) is live — the enable/team gating runs against the standalone draw, which is correct, but the discovered panel never participates.; No hud_panel_radar_size cvar analogue (port keys off live control size + the 2500u range fallback instead of the 128px default). |
| 271 | 2.0 | `cl-view` | `cl-view.chase.classic_thirdperson` | logic:partial | The server-side spectator-camera mode toggle is not ported: View_SpectatorCamera's STAT(CAMERA_SPECTATOR) 1/2 state + IMP_weapon_drop cycling (the chase_active<=0 auto-switch that flips chase_active to -2 and Impulse_Sends weapon-drop to cycle the server spectator camera) has no port analogue. The port drives the spectator third-person purely from the user chase_active cvar. |
| 272 | 2.0 | `cl-viewmodel` | `cl-viewmodel.model.skin_colormap_glow_effects` | values:partial | Team colormap is an approximate whole-model albedo tint (0.6 + 0.4*teamColor), not Base's true colormap palette region swap (256 + c); reads as your team color but is not pixel-faithful.; Per-child m_skin (wepskin) not applied; the gun renders with its base material. |
| 273 | 2.0 | `cl-viewmodel` | `cl-viewmodel.muzzle.flash_particle` | values:partial | Local muzzle position is a heuristic at the tag_shot socket rather than Base's view-projected movedir_aligned (not verified to read identically on-screen).; Secondary fire does not flash (only the primary _attackHeld path calls Fire; UpdateLocalFireFeedback comments 'secondary FX stay networked for now').; ClientWorld.OnMuzzleFlash wrapper has no caller (NetGame calls _viewModel.Fire() directly) — dead wrapper, but the feature is live by another path. |
| 274 | 2.0 | `fx-deathtypes` | `fx-deathtypes.message.mapper_custom` | liveness:partial | DEATH_CUSTOM emitter is DEAD: the selector + emit path are now fully wired (DeathTypes.Custom const, Scores.EmitObituary CUSTOM branch at :698-709 with the 'You were %s' formatting matching damage.qc:457), but nothing in the port tags a death with DeathTypes.Custom (re-grep 2026-06-27: only the consumer at Scores.cs:698; no mapper trigger_hurt/deathmessage emitter), so the path never fires. |
| 275 | 2.0 | `fx-sounds` | `fx-sounds.emit.play2_family` | liveness:partial | play2 per-recipient targeting is a broadcast approximation: QC play2 sends MSG_ONE (msg_entity=e) so EXACTLY one client hears it; the port's SoundWire has no per-client send, so Play2/Play2Team broadcast to all connected clients — exact only on a single-human listen server. This is the SOLE residual reason liveness stays partial. |
| 276 | 2.0 | `fx-sounds` | `fx-sounds.voice.globalsound_dispatch` | logic:partial | AUTOTAUNT per-recipient probability roll (tauntrand < CS_CVAR(it).cl_autotaunt) still omitted: per-client cl_autotaunt is not networked, so autotaunts that clear sv_autotaunt always play for all recipients (documented deferral; a single global cl_autotaunt default 0 would wrongly silence ALL autotaunts). This is the SOLE remaining reason logic stays partial. |
| 277 | 2.0 | `mutator-campcheck` | `campcheck.check.vehicle` | liveness:unknown | Vehicle branch liveness unverified: whether a player is ever actually in a driveable vehicle in the port is unconfirmed, so this branch may never execute in a real match. |
| 278 | 2.0 | `mutator-nades` | `mutator-nades.boom.normal` | presentation:missing | DamageInfo (Damage_DamageInfo) network record omitted (render-only). |
| 279 | 2.0 | `mutator-nades` | `mutator-nades.boom.ice` | presentation:missing | The muzzleflash/icefield particle gate is omitted (render). |
| 280 | 2.0 | `mutator-nades` | `mutator-nades.boom.spawn` | presentation:missing | EF_STARDUST marker render + drawonlytoclient omitted (render). |
| 281 | 2.0 | `mutator-nades` | `mutator-nades.boom.heal` | presentation:missing | EFFECT_HEALING particle omitted (render). |
| 282 | 2.0 | `mutator-nades` | `mutator-nades.boom.ammo` | presentation:missing | Per-weapon weapon_load[] persistent store collapsed onto each slot's live ClipLoad (port note). EFFECT_AMMO_REGEN particle omitted (render). |
| 283 | 2.0 | `mutator-sandbox` | `mutator-sandbox.object.spawn` | presentation:missing | WIRED + LIVE: `g_sandbox` client-callable (ClientCommandRegistry:105) → CmdSandbox → HandleCommand; NetGame.WireSandbox assigns Trace (SandboxTraceProvider — real crosshair forward TraceService), so object_spawn lands at the trace endpoint with a real edict (Api.Entities.Spawn + SetModel + SetOrigin).; ObjectSpawn now DOES register the engine touch callback (e.Edict.Touch = ObjectTouch, SandboxMutator.cs:204) — the port equivalent of Base settouch (sv_sandbox.qc:180); see touch_material_fx row (now live). Think (setthink, sv_sandbox.qc:181) is driven externally by OnStartFrame over all objects rather than the per-edict engine think, which is faithful enough.; FIXED (re-audit 2026-06-27): ModelExists is NO LONGER unconditionally permissive — NetGame.WireSandbox:952-953 wires sandbox.ModelExistsProvider = path => _vfs.Exists(path) when the asset VFS is present (it is, on the live setup path), so object_spawn now rejects a non-existent model path like Base fexists(argv(2)). Headless/tests (no VFS) stay permissive by design.; STILL OPEN — presentation: CSQCMODEL_AUTOINIT (object→client networking) has no port seam (TODO SandboxMutator.cs:236) — client-side render of a spawned object is unverified (the object carries a real edict+model, so the generic entity feed MIGHT show it, but this was not confirmed in-game). |
| 284 | 2.0 | `mutator-sandbox` | `mutator-sandbox.object.think_grab` | presentation:missing | WIRED + LIVE: OnStartFrame is subscribed to MutatorHooks.SvStartFrame (fired in the server tick) and ticks ObjectThink for every object over the live RealClientsProvider roster (NetGame.WireSandbox — non-bot players); CryptoIdfpProvider(=PersistentId) resolves realowner from UID and clears it on disconnect.; Sandbox_DragAllowed veto hook ported and fired (MutatorHooks.FireSandboxDragAllowed, SandboxMutator.cs:346) — the grab-class branch is bit-faithful to Base (readonly OR DragAllowed→0). No subscriber exists, identical to Base's empty MUTATOR_CALLHOOK.; presentation: CSQCMODEL_AUTOUPDATE has no port seam (TODO SandboxMutator.cs:364); .Grab is computed but never consumed (no drag system). |
| 285 | 2.0 | `mutator-sandbox` | `mutator-sandbox.object.edit_properties` | presentation:missing | WIRED + LIVE: object_edit reachable via CmdSandbox → HandleCommand on the live path.; physics-2 maps to MoveType.Physics (EntityEnums.cs:21, value 32) at SandboxMutator.cs:811, matching Base MOVETYPE_PHYSICS=32 (dpextensions.qc:1707) — the serialized movetype byte is Base-identical. (Behaviorally still think-only since the port has no ODE integrator, same as Base without ODE.); The faithful Base solidity→physics fall-through (missing break;) IS preserved (re-verified Base sv_sandbox.qc:685-697 has no break on solidity; port reproduces via goto case "physics" at SandboxMutator.cs:797). |
| 286 | 2.0 | `mutator-sandbox` | `mutator-sandbox.object.scale` | presentation:missing | WIRED + LIVE: reached from object_edit (live via CmdSandbox) and ObjectPortLoad (live via Store load/paste).; Model re-set + mins/maxs resize depend on the entity service (Edict); now that objects carry a real edict the SetModel/SetSize path executes. |
| 287 | 2.0 | `mutator-walljump` | `mutator-walljump.fx.anim_and_oldvel` | presentation:missing | animdecide_setaction(player, ANIMACTION_JUMP, true) not triggered on a wall jump (no anim-action seam in the port; WalljumpMutator.cs:135 is a comment only). UNCHANGED — same missing server->client one-shot action channel noted on Dodging. Purely cosmetic (jump pose). |
| 288 | 1.92 | `cl-hud` | `cl-hud.panel.modicons` | values:unknown | Per-gametype icon-art fidelity (dom point-ownership icons, nexball_carrying art) is approximate; not all runtime-verified. The NexBall icon has a vector stand-in (ball glyph + 'BALL' tag) when nexball_carrying art is missing from the skin. |
| 289 | 1.8 | `mapobject-teleporters-portals` | `teleporters.trigger.team_claim` | liveness:unknown | SendFlags networking (SF_TRIGGER_UPDATE) still not replicated (client-side only; harmless headless). |
| 290 | 1.8 | `sv-chat` | `sv-chat.flood.per_type_throttle` | values:partial | Line-wrap width is an integer 82 visible chars vs Base's 82.4289758859709 px-equiv (perl averagewidth on gfx/vera-sans.width); proportional per-char widths are not modeled, so long-message line-count / flood==2 'too long' boundary can differ by one char on edge-length messages. |
| 291 | 1.8 | `sv-teamplay` | `sv-teamplay.join.skill_weighting` | values:partial | No TrueSkill ratings for humans: SkillProvider returns the bot's real BotSkill (0..10) for bots but a flat 5 for every human (IsRated treats only bots as rated; humans fall through the unranked server-average path). So skill weighting biases bot-involved ties but human-vs-human equal-size ties still reduce to a plain count. This is a VALUES/data gap, not a logic gap — the algorithm now matches Base.; No warmup skill-aware join branch (Base TeamBalance_FindBestTeams warmup+sv_teamnagger+skill block picking the team whose weighted-mean skill is furthest from the joiner). The port always uses the size->strength ladder even in warmup.; The weighted path is only reached from AssignBestTeam/IsTeamAllowedForSwitch (connect/moveplayer/team/selectteam); the per-gametype TeamBalance.JoinSmallestTeam callers (CTF/CA/FreezeTag/Dom/KeyHunt/TDM/TeamMayhem) ignore skill entirely. |
| 292 | 1.8 | `turret-plasma_dual` | `turret-plasma_dual.damage.gating_die_respawn` | presentation:missing | Respawn EF_NODRAW hide-frame split not modeled: Base hides (EF_NODRAW) at time+0.2 then turret_respawn at respawntime-0.2; port schedules Respawn directly at now+RespawnTime. NET respawn timing is identical (0.2 + (60-0.2) = 60 == now+60) — only the intermediate EF_NODRAW hide-frame (a presentation detail) is unmodeled.; Death FX/sound (SND_ROCKET_IMPACT + EFFECT_ROCKET_EXPLODE) only fire in Base on TFL_DMG_DEATH_NORESPAWN; plasma_dual always respawns so this branch never applies (port has it coded in Die but unreachable for this unit). tr_death is a no-op for plasma_dual. |
| 293 | 1.8 | `warpzones` | `warpzones.porto` | presentation:missing | Porto portals share the missing client portal render — they teleport correctly server-side but show as dark placeholders, same as map warpzones.; The Amazing-on-portal-telefrag announcer (portals.qc:192-194, ANNCE_ACHIEVEMENT_AMAZING within 1s of portal creation) needs the tdeath path the warpzone crossing does not run — see the portals.teleport unresolved gap noted inline (Warpzone.cs:262).; The +/-48 PortoMins/Maxs trigger box is a port choice; Base Porto portal bounds not re-verified. |
| 294 | 1.8 | `warpzones` | `warpzones.client.prediction` | presentation:missing | Render/view side of the crossing still missing: a predicted warp relocates the carrier in lockstep with authority (no rubber-band) and stamps .fixangle, but the seamless view-rotate/portal render (warpzones.client.render / .fixview / .teleported_view) is unported — the camera still snaps visually across the seam even though the origin no longer corrects. |
| 295 | 1.75 | `bot-ai` | `bot-ai.target.chooseenemy` | logic:partial | No second see-through-transparent (DPCONTENTS_OPAQUE) scan pass and no misc_breakablemodel secondary targeting; bots won't shoot breakables or acquire targets through transparent surfaces. (UNCHANGED.) |
| 296 | 1.75 | `bot-ai` | `bot-ai.move.steer` | logic:partial | QC's full movetogoal handling of ladder/box-volume waypoints, water swim-up, and the per-goal stop/turn logic is partially collapsed; complex waypoint traversal is less robust than stock. (NARROWED 2026-06-27 third pass: teleporter/jumppad goal-forcing now ported — see notes.); BotTracewalk (reachability test, separate from the brain danger check) still uses a hardcoded JumpStepHeight const = 48 rather than the live derivation; non-default physics doesn't adjust that one path. (Minor.) |
| 297 | 1.75 | `bot-waypoints` | `bot-waypoints.graph.autolink` | liveness:partial | The it.SUPPORT_WP incoming-direction forbid (waypoints.qc:1213/1227) is not ported — the port has no SUPPORT_WP back-pointer field, so a SUPPORT waypoint forbids only its OWN outgoing/incoming links, not links INTO an arbitrary node it supports. Documented at Waypoint.cs:335. Edge case (SUPPORT waypoints are rare); affects only those specific pairs. |
| 298 | 1.75 | `bot-waypoints` | `bot-waypoints.auto.spawnforitem` | liveness:partial | Only runs in the no-.waypoints-file auto-generate path; on maps that ship a .waypoints file the port does NOT inject runtime item/teleporter waypoints (Base always does waypoint_spawnforitem at item spawn, snapping to the item). This is the liveness-partial: the fileless auto-generate fallback is a port-only playability helper, not a Base feature, but it cannot replace Base's always-on runtime injection on shipped maps. |
| 299 | 1.75 | `bot-waypoints` | `bot-waypoints.path.routetogoal` | logic:partial | Dynamic/moving-target shortcut (bot_ai_strategyinterval_movingtarget 5.5) and the item incoming-link walkability cache are still absent. Teleport-goal forcing IS now ported (DRIFT FIX Wave 16): when the route ends at a Teleport box, SetGoal pushes the box's wp00 destination ahead of it so the bot commits to the trigger (BotNavigation.cs:262-275, matching navigation.qc:1318-1334). The path-optimization shortcut (drop the nearest node when the second is a shorter straight shot, BotNavigation.cs:251-260) was already ported. |
| 300 | 1.75 | `bot-waypoints` | `bot-waypoints.steer.movetogoal` | logic:partial | Obstacle avoidance is a single forward step/jump-height tracebox probe rather than Base's steerlib trident/funnel sweep (see bot-waypoints.steer.steerlib); lateral evade is folded into a straight -velocity brake (do_break), a documented simplification of Base's AI_STATUS_DANGER_AHEAD sidestep. |
| 301 | 1.75 | `bot-waypoints` | `bot-waypoints.steer.bunnyhop` | logic:partial | Base forbids bunnyhop when evadedanger \|\| do_break is set this frame (havocbot.qc:1315). The port reports bunnyhop intent via a SEPARATE WantBunnyhop flag (BotNavigation.cs:112/378) gated by the Steer-internal ledge brake, then the BotBrain ANDs it with 'no danger brake this frame' before pressing jump — so the brain owns the final danger-suppression. The two-stage gate is close to Base but not a single in-place check. |
| 302 | 1.65 | `net-entity-state` | `net-entity-state.replicate.client_cvars` | values:partial | ONE replicated cvar is stored server-side but not consumed off the replicated value: cl_autoscreenshot is in SentCvarAllowlist (Commands.cs:233) so it is accepted+stored per-client, but nothing on the server reads the REPLICATED value — autoscreenshot is driven CLIENT-locally off the networked intermission flag reading the client's own Api.Cvars cl_autoscreenshot (NetGame.cs), not the per-client replicated store. FIX 2026-06-27: g_xonoticversion left this list — it now has a live server consumer (Commands.CheckClientVersion reads the per-client replicated g_xonoticversion to send the version-mismatch notification, armed at ClientManager.cs:229, fired on the live PlayerPreThink path src/XonoticGodot.Server/GameWorld.cs:2255). FIX 2026-06-25: cl_clippedspectating left this list — live free-fly MOVETYPE consumer (ClientManager.ClippedSpectatingProvider). |
| 303 | 1.5 | `assault` | `assault.objective.destructible_heal` | presentation:partial | Presentation (downgraded but not closed): the heal now updates the wall's waypoint-sprite health bar AND that bar IS surfaced to the client (Wave 17 added the full net+render path — see assault.objective.waypoint_sprite), so the healed-bar-rising is visible. The ONLY residual is QC's func_breakable_colormod (the visible green->red color tint on the wall MESH itself as it heals), which the port does not model. The heal LOGIC + health restoration + sprite bar are faithful + live; only the mesh colormod tint is missing (minor cosmetic). |
| 304 | 1.5 | `ctf` | `ctf.flag.spawn_setup` | presentation:partial | Base flag is static at the spawn origin (no DropToFloor settle on non-fixed spawns); dropped flags now DO settle (MoveType.Toss integrator, see ctf.flag.drop_on_death).; No glow trail (g_ctf_flag_glowtrails), no dynamic team light (g_ctf_dynamiclights); EF_LOWPRECISION is set, EF_FULLBRIGHT only when g_ctf_fullbrightflags (matches Base default off). |
| 305 | 1.5 | `ctf` | `ctf.flag.capture` | presentation:partial | Only the PERSISTENT capture-time record (ctf_CaptureRecord ServerProgsDB write + cross-session g_ctf_leaderboard) remains unported (needs a persistent DB). The in-match fastest-cap broadcast (CTF_CAPTURE_TIME/_BROKEN/_UNBROKEN, picked via MSG_CHOICE) IS now fired by CaptureRecord.; Per-flag score_capture override + cnt-group guard not modeled (own flag home requirement IS faithful). |
| 306 | 1.5 | `ctf` | `ctf.flag.fckill_score` | presentation:partial | Auto-helpme is modeled as a carrier-sprite helpme FLASH (GtHelpMeUntil networked via ServerNet, consumed in CollectWaypoints), not the full Base WaypointSprite_HelpMePing sprite; effective-HP test approximates healtharmor_maxdamage with health+armor. |
| 307 | 1.5 | `cts` | `cts.hud.race_timer` | presentation:partial | CTS has no intermediate checkpoints, so the panel's per-checkpoint split / anticipation delta / next-best lines are intentionally inert for CTS (RaceCheckpoint=254, no split feed) — this matches a single start->stop course but means the panel shows only the running clock + medal flash, not the full Race split UI. |
| 308 | 1.5 | `cts` | `cts.hud.cl_panel_gating` | presentation:partial | HUD_Score_show (hide the score panel while observing in CTS) and DrawScoreboardItemStats / DrawDeathScoreboard hiding are not specifically modeled per-CTS; the 'Rankings' scoreboard header is a hard-coded literal, not a CTS-conditional relabel.; Scoreboard accuracy is still not networked, so the CTS-gated accuracy block draws nothing (low impact: CTS has no accuracy display by intent). |
| 309 | 1.5 | `freezetag` | `freezetag.unfreeze` | presentation:partial | Does not delete an ice entity (not spawned in the first place); the waypoint sprite IS cleared (it stops being collected once IsFrozen flips false). |
| 310 | 1.5 | `freezetag` | `freezetag.presentation.overlay_eventchase` | presentation:partial | Overlay + crosshair ring + eventchase cam are all driven on the host/listen-server path (LocalServerPlayer / its Frozen status effect); a pure remote client with no LocalServerPlayer shows none of them (the cross-client wepent/stat follow-up). |
| 311 | 1.5 | `keepaway` | `keepaway.ball.spawn` | presentation:partial | SpawnBall is CALLED on the live match path — GameWorld.Boot (the NetGame match-start path) procedurally spawns the single ball at a RandomMapLocation right after the entity lump, replacing the missing QC ka_Handler_CheckBall driver.; PRESENTATION residual: the EF_DIMLIGHT bit + glow_color/glow_trail are set as data on the edict, but the actual ball MODEL render + glow trail are client concerns not yet realized (model path models/orbs/orbblue.md3 is configured but the CSQC render of it is deferred). |
| 312 | 1.5 | `monster-framework` | `monster-framework.presentation.team_colors` | liveness:partial | monster_setupcolors is PORTED + LIVE — SetupColors (MonsterAI.cs:385) picks .colormap by team (1024+(team-1)*17) / owner / skill tier (1126/1075/1228/1092/1160) bit-faithful to QC, and is called from SpawnFromMap (MonsterAI.cs:563) for every natural (!RESPAWNED) monster (gated exactly as QC at sv_monsters.qc:1525). It writes Entity.ColorMapOverride with RENDER_COLORMAPPED set; the csqcmodel derives the glowmod from the colormap nibble client-side, so the QC .glowmod write is covered by the established colormap render seam (presentation: the actual client tint depends on that csqcmodel path being wired — verified for the same field g_model_setcolormaptoactivator uses).; DRIFT FIX (2026-06-27): monster_changeteam is PORTED (ChangeTeam, MonsterAI.cs:416): re-teams + re-tints (SetupColors) in teamplay, AND the team radar-icon update is now LIVE — ChangeTeam (:430) calls WaypointSprites.UpdateTeamRadar on st.Sprite + sets sprite.Team, exactly as QC monster_changeteam:211-216, because the WP_Monster sprite now exists (healthbar fix). RESIDUAL: ChangeTeam still has NO live external caller today (no dispatch site outside the monster module), so the teamplay monster re-team itself is dead-wired (liveness: partial). When healthbars are off the sprite is null and the radar update is a harmless no-op (matching QC's `if (this.sprite)` guard). |
| 313 | 1.5 | `monster-golem` | `monster-golem.anim_frames` | timing:partial | AnimFrame stamps only the group START index onto Entity.Frame (server-side, per think via DriveAnimFrame). The animfixfps framecount/fps per-frame playback advance WITHIN a group is not driven — there is no true .dpm frame timeline; the client renders a single stamped frame per phase/group (timing/presentation partial). |
| 314 | 1.5 | `mutator-nades` | `mutator-nades.toss.projectile` | values:partial | Newton-style 0 is implemented as pure absolute _velocity rather than W_CalculateProjectileVelocity(e, e.velocity, _velocity, true); for non-default g_projectiles_newton_style this diverges from Base's velocity blending (style 0 is the shipped default, so identical at defaults). |
| 315 | 1.5 | `nexball` | `nexball.ball.spawn` | presentation:partial | Scale 1.3 + glow_trail rendering are still client-deferred (model path + trail color are configured; SpawnForGametype never sets e.Scale; bbox/effects/playerclip/relocate are exact). |
| 316 | 1.5 | `nexball` | `nexball.ball.giveball` | presentation:partial | The carrier glow effect bit is cleared on the ball but not rendered on the carrier (client EF, deferred). The carrier waypoint sprite (WP_NbBall AttachCarrier) IS now drawn via CollectWaypoints (see nexball.waypoint.sprites). |
| 317 | 1.5 | `onslaught` | `onslaught.win.check_winner` | presentation:partial | BankRoundWin sends ROUND_TEAM_WIN_<team> center+info to all + play2all CTF_CAPTURE_<team> win jingle (winner>0), ROUND_TIED (winner==-1). Remaining: scoreboard column rendering + winner-banner HUD is a client concern (tracked under onslaught.presentation.csqc). |
| 318 | 1.5 | `onslaught` | `onslaught.captureshield` | presentation:partial | Server-side faithful + live: SpawnGenerator/SpawnControlPoint spawn a co-located SOLID_TRIGGER/MOVETYPE_NOCLIP ons_captureshield with the back-link (GtShieldFlag), EF_ADDITIVE, avelocity '7 0 11', hitbox 1.20x. ShieldTouchEntity reproduces ons_CaptureShield_Touch: pass-through for same-team or attackable/un-shielded node, else Combat.Damage(0, force=normalize(dir)*ShieldForce) push + play2 ONS_DAMAGEBLOCKEDBYSHIELD + GENERATOR/CONTROLPOINT_SHIELDED center.; presentation partial (client-deferred): the shield MODEL + EF_ADDITIVE additive blend + avelocity SPIN are assigned on the entity, but actually RENDERING the spinning additive shield is a CSQC/client concern not done here. ons_CaptureShield_Customize (per-viewer visibility) + ons_CaptureShield_Reset (colormap on map reset) not ported. |
| 319 | 1.5 | `onslaught` | `onslaught.audio.cues` | presentation:partial | Core cues EMITTED live: SpawnIcon→ONS_CONTROLPOINT_BUILD; build completion→ONS_CONTROLPOINT_BUILT + ONSLAUGHT_CAPTURE_NONAME + ONS_CAPTURE(_TEAM)_NONAME; IconDamage→ONS_DAMAGEBLOCKEDBYSHIELD / ONS_HIT1/2 / ONS_GENERATOR_EXPLODE + ONSLAUGHT_CPDESTROYED_NONAME; GeneratorDamage→blocked-by-shield / GENERATOR_UNDERATTACK + ONS_GENERATOR_UNDERATTACK / ONS_HIT1-2 (or explode at chance damage/220) / ONSLAUGHT_GENDESTROYED(_OVERTIME) + explode; GeneratorThink→ONS_NOTSHIELDED_TEAM/_<team> + ONS_GENERATOR_ALARM (every 5s while un-shielded); OvertimeDecayTick first tick→OVERTIME_CONTROLPOINT + ONS_GENERATOR_DECAY; BankRoundWin→ROUND_TEAM_WIN / ROUND_TIED + play2all CTF_CAPTURE jingle. Sounds (7) + notifications (40) registered AND now all emitting.; Newly EMITTED this pass: ShieldTouchEntity→play2 ONS_DAMAGEBLOCKEDBYSHIELD + GENERATOR/CONTROLPOINT_SHIELDED center on a shield bump; IconThink damaged-icon spark crackle ONS_SPARK1/2.; Remaining (presentation/cross-boundary): particle effects, waypoint-sprite build/health bars, the CP map .message name (port emits NONAME variants), and play2team→global emission (NotifBroadcast.All + SoundSystem.PlayGlobal) as the headless approximation (no per-team routing). |
| 320 | 1.5 | `onslaught` | `onslaught.gen.unshielded_alarm` | presentation:partial | Presentation: the alarm is server-authoritative (center prints via NotificationSystem.Send + siren via SoundSystem); no client-side HUD/visual of the un-shielded state beyond the center-print. The siren is emitted once globally (SoundSystem.PlayGlobal) as the headless approximation of QC's per-player ATTEN_NONE soundto — no per-listener spatial routing. |
| 321 | 1.5 | `overkill-weapons` | `overkill-weapons.okrpc.primary_chainsaw_missile` | presentation:partial | FIXED (Wave-8b): W_MuzzleFlash (Attack:184 EFFECT_ROCKET_MUZZLEFLASH — matches okrpc.qh m_muzzleeffect) and wr_impacteffect (Explode:243-244 EFFECT_ROCKET_EXPLODE puff + SND_ROCKET_IMPACT at the blast point) are now wired LIVE — closing the baseline 'no wr_impacteffect / no W_MuzzleFlash' gap.; Shoot-down LIVE+faithful (unchanged): OkRpc.Attack:179 Projectiles.MakeShootable(missile, exception:-1) installs the GtEventDamage shim DamageSystem dispatches; ShootDown runs W_CheckProjectileDamage(-1) + RES_HEALTH subtract → Explode at hp<=0. Correctly INERT under stock g_projectiles_damage=-2, exactly like Base.; FIXED (Wave-16, commit b3a9c06): missile now registered for bot dodge — OkRpc.Attack:147-148 sets missile.BotDodge=true + BotDodgeRating=damage*2 (QC bot_dodge / bot_dodgerating). Consumed LIVE by the SUPERBOT-gated dodge scan BotBrain.HavocbotDodge:698-735 (iterates Api.Entities.All for the BotDodge flag, skips own shots). SUPERBOT gate matches QC (only high-skill bots specifically dodge bolts). Closes the baseline 'not registered for bot dodge' gap.; FIXED (Wave-16, commit b3a9c06): missile now sets missile.DamagedByContents=true (QC IL_PUSH g_damagedbycontents). Consumed LIVE by the per-frame contents sweep GameWorld.CreatureFrameAll:2194-2204 (scans Api.Entities.All for the flag, skips noclip) -> PlayerFrameLogic.ProjectileContentsDamage, so a chainsaw resting in lava/slime now takes contents damage and detonates via its shoot-down shim. Correctly NOT gated by g_projectiles_damage (contents use g_balance_contents_projectiledamage), matching Base. Closes the baseline 'damagedbycontents missing' gap.; STILL OPEN: missile not networked as a CSQC projectile (no visible flying chainsaw on clients) — CSQCProjectile(PROJECTILE_RPC) omitted. The muzzle flash + explosion FX network, but the in-flight chainsaw body does not.; Accuracy bookkeeping (first-hit fired/hit add, explode 'add fired back') omitted (stat-only).; Pass-through victim filter is `(Flags & Client) && TakeDamage!=No` vs QC IS_PLAYER(trace_ent): could catch a spectator client edict carrying the Client flag. Minor; corpses filtered by TakeDamage. |
| 322 | 1.5 | `tka` | `tka.ball.spawn` | presentation:partial | Single ball only: SpawnBall makes one ball; g_tkaball_count multi-ball chaining (toucher.ballcarried.cnt vs g_tka_ballcarrier_maxballs) not modeled. Default count is 1, so values match the stock server.; Model/EF_DIMLIGHT/trail_color/damageforcescale are set on the sim edict, but the actual glow_trail particle trail + EF_DIMLIGHT light are client/render concerns — not yet visible.; Spawn is procedural at map-load (GameWorld.cs:718 switch on GameType; TKA case), replacing QC tka_Handler_CheckBall's per-frame INITPRIO_GAMETYPE think. There is no game_starttime gate / remove-and-respawn-during-warmup loop, but the net effect (one ball once the match is live) matches. |
| 323 | 1.5 | `tka` | `tka.waypoints.tracking` | presentation:partial | FIXED (missing -> faithful logic/values + live): CollectWaypoints (TeamKeepaway.cs:713) produces a TEAM-COLORED carrier sprite (TkaBallCarrier<team> + per-team radar tint, offset '0 0 64', RADARICON_FLAGCARRIER, Rule=SPRITERULE_TEAMPLAY) while held, and the neutral KaBall sprite on the loose ball. Honors g_tkaball_tracking (0 = no waypoint). Pulled LIVE by ServerNet.SendWaypoints (ServerNet.cs:927 _world.GameType.CollectWaypoints), filtered per peer, networked, rendered by WaypointSpriteLayer + RadarPanel.; FIXED (values partial -> faithful): g_tkaball_tracking's THREE Base modes (0 none / 1 always / 2 only-when-dropped) are now all distinguished — CarrierWaypointVisibleFor returns BallTracking==1 for the enemy case, so ==2 hides the carrier sprite from enemies (matching the QC `return autocvar_g_tkaball_tracking == 1`).; FIXED (presentation): the carrier sprite is now networked with SPRITERULE_TEAMPLAY + a per-viewer VisibleForPlayer predicate (CarrierWaypointVisibleFor), honored per-peer at ServerNet.cs:996. It models same-team-always, spectator-always, warmup-always, and enemy-tracking==1 faithfully.; FIXED (Wave 16/17, presentation): the QC IS_INVISIBLE(owner) hide is now ported — CarrierWaypointVisibleFor (TeamKeepaway.cs:817-822) returns false (hiding the carrier sprite from ENEMIES) when the carrier holds the invisibility powerup, placed AFTER the spec/warmup/same-team early-return so teammates+spectators still see it. Mirrors the already-faithful Keepaway.CarrierWaypointVisibleFor (Keepaway.cs:707-710). Reads the live invisibility StatusEffectDef (StatusEffects.cs:235) via StatusEffectsCatalog.ByName/Has; reachable on the live CollectWaypoints->VisibleForPlayer per-peer path.; RESIDUAL (presentation:partial): now ONLY the single narrowest QC nuance of tka_ballcarrier_waypointsprite_visible_for_player remains approximated — hide-from-spectators-of-the-carrier (`view.ballcarried && IS_SPEC(player)`). The predicate has no spectatee field, so this still resolves as visible. |
| 324 | 1.5 | `turret-framework` | `turret-framework.registry.descriptor` | logic:partial | Per-unit cvar table unported (load_unit_settings / TR_PROPS_COMMON / turret_scale_* / turret_initparams clamping / dumpturrets) — per-unit balance is hardcoded C# const, mappers can't retune. (Framework-level g_turrets_* delays/nofire ARE cvar-backed; per-unit fields are not.) |
| 325 | 1.5 | `turret-walker` | `turret-walker.client.draw` | values:partial | Head avelocity spin is the ONLY remaining unported walker_draw piece: Base's CSQC copy spins the head model by its avelocity; the port has no tur_head sub-entity so the minigun head never visibly spins (tracked on the identity row's head-model gap). Everything else in walker_draw now has a server-side counterpart.; Client render-copy MOVETYPE_BOUNCE + gravity 1 has no direct counterpart (values:partial): the port drives the walker authoritatively on the server (MoveType.Step) and nets the origin through the shared entity feed instead of a CSQC bounce sim — gameplay-equivalent, but the client-side bounce/gravity render nicety is absent. |
| 326 | 1.5 | `weapon-arc` | `weapon-arc.identity.attributes` | presentation:partial | EFFECT_ARC_MUZZLEFLASH is now emitted on the bolt secondary (Arc.cs:489 ARC_MUZZLEFLASH -> electro_muzzleflash), but it is not carried as the weapon's m_muzzleeffect/m_muzzlemodel attribute and there is no spinning flash.md3 muzzle entity on the beam (that lives in Draw_ArcBeam — tracked under weapon-arc.beam.visual). crosshair gfx/crosshairhlac size 0.7 is a CSQC attribute not carried headless. Presentation stays partial. |
| 327 | 1.5 | `weapon-blaster` | `weapon-blaster.identity.registration` | presentation:partial | model/anim/crosshair fidelity (view/world/item models, gfx/crosshairlaser, weaponlaser image) not audited here — names declared only. |
| 328 | 1.5 | `weapon-fireball` | `weapon-fireball.primary.launch` | presentation:partial | No projectile flight sound (fireball_fly) / FIREBALL trail attached to the live plasma_prim entity (deferred CSQC render concern, same as other weapons). |
| 329 | 1.5 | `weapon-fireball` | `weapon-fireball.primary.explode` | presentation:partial | modeleffect_spawn(sphere.md3) BFG visual ball not produced (CSQC render layer); FIREBALL_EXPLODE + impact sound at ATTEN_NORM*0.25 are emitted. |
| 330 | 1.5 | `weapon-fireball` | `weapon-fireball.primary.bfg` | presentation:partial | IS_INDEPENDENT_PLAYER(e) is checked but Entity.IsIndependentPlayer is a hardcoded stub (always false) — faithful for all non-independent (default) modes; the filter is a no-op until that mode is wired.; EFFECT_FIREBALL_BFGDAMAGE per-victim Emit is now wired, but its render fidelity (and the sphere.md3 ball) is the deferred CSQC layer. |
| 331 | 1.5 | `weapon-fireball` | `weapon-fireball.primary.laserscorch` | presentation:partial | EFFECT_FIREBALL_LASER scorch beam is Emit-wired; its render fidelity (the actual beam visual) is the deferred CSQC layer, same as every weapon. |
| 332 | 1.5 | `weapon-fireball` | `weapon-fireball.secondary.firemine` | presentation:partial | No firemine flight sound / FIREMINE trail attached (deferred CSQC render), same as every weapon. |
| 333 | 1.5 | `weapon-hlac` | `weapon-hlac.identity.metadata` | presentation:partial | Bot-aim (wr_aim) is now IMPLEMENTED + LIVE but DIVERGES: QC HLAC wr_aim (hlac.qc:156-159) only ever sets PHYS_INPUT_BUTTON_ATCK (primary) via bot_aim at primary speed — it never presses secondary. The port's Hlac.BotWantsSecondary (Hlac.cs:184-185) returns secondary on ~10% of bot decisions (ctx.Random01 <= 0.1), so port HLAC bots occasionally alt-fire where Base bots never do. The lead itself (BotAimShotSpeed => Primary.Speed, Hlac.cs:188) is faithful (primary==secondary speed 6000).; HLAC bolt now has a DEDICATED client render type (ProjectileType.Hlac, ProjectileCatalog.cs:165/297) — no longer the generic fallback — and its NO-trail matches Base (PROJECTILE_HLAC traileffect=EFFECT_Null, projectile.qc:356). But the body is a generic GlowSprite (no ModelPath); Base sets MDL_PROJECTILE_HLAC=models/hlac_bullet.md3 (all.inc:65). So the bolt renders as a glow sprite, not the dedicated MD3 model (contrast Blaster, which wires models/laser.mdl). |
| 334 | 1.5 | `weapon-hook` | `weapon-hook.primary.latch` | presentation:partial | Latched hook becomes INVISIBLE: GrapplingHook_Stop sets MoveType.None (Hook.cs:340), which is NOT a projectile movetype, so the server entity classifier (IsProjectileMoveType requires Fly/Toss/Bounce) no longer classifies it as Projectile; with no model it falls to Generic and is dropped from the render. The rendered spike disappears on latch. Base keeps the latched hook drawn via CSQC. |
| 335 | 1.5 | `weapon-rifle` | `rifle.fx.impact` | presentation:partial | Emit PATH still diverges from Base: the port emits RIFLE_IMPACT server-side per bullet inside FireBullets off a world-only re-trace; Base emits it CSQC in wr_impacteffect off the Damage_DamageInfo feed. The effect NAME + the org2/backoff offsets now match QC, but the per-bullet server emit is a different fan-out than the CSQC damageinfo path — remote-client render not visually verified. |
| 336 | 1.5 | `weapon-tuba` | `weapon-tuba.reload.instrument_cycle` | presentation:partial | weaponname (akordeon/kleinbottle) is not set — in Base it feeds only the CSQC wr_viewmodel (per-instrument view model h_akordeon/h_kleinbottle). The instrument index (TubaInstrument) IS tracked, so the obituary/sound branch correctly; only the per-instrument VIEW MODEL swap is unwired (render-only gap). |
| 337 | 1.5 | `weapon-tuba` | `weapon-tuba.fx.smoke_ring` | presentation:partial | Ring origin uses the player eye (Origin + ViewOfs) rather than the weapon muzzle tag (gettaginfo), so the puff spawns slightly off from Base — but Base itself FIXMEs that gettaginfo doesn't return the real tag origin (tuba.qc:309), so this is a near-equivalent approximation, not a faithful tag read. Per-instrument vertical offsets and the 0.25s throttle are bit-exact. |
| 338 | 1.5 | `weapon-tuba` | `weapon-tuba.identity.metadata` | presentation:partial | Per-instrument view models (h_akordeon/h_kleinbottle, driven by CSQC wr_viewmodel/weaponname) still unwired; render-only (see weapon-tuba.reload.instrument_cycle).; Crosshair gfx/crosshairtuba and weapon-image weapontuba assets exist in data but live-render wiring not verified (headless audit). |
| 339 | 1.4 | `items-pickups` | `items-pickups.show.visibility` | presentation:partial | ITS_ALLOWSI (sv_simple_items, Base default 1) not honored — no simple-item model swap (no client resolver for the *_simple variants, no bob suppression). The bit is left unset rather than faked. |
| 340 | 1.35 | `mapobject-func` | `mapobject-func.train` | presentation:partial | no func_train reset (QC explicitly has `// TODO make a reset function for this one` — so this is faithful to Base's own omission, unlike door/plat/button).; train moving-sound stop (stopsoundto MSG_BROADCAST on arrival) is not done; the moving noise plays but is not explicitly stopped at each dwell (cosmetic). Port uses SoundChannel.Voice vs QC ATTEN_IDLE (cosmetic).; no CSQC ENT_CLIENT_TRAIN draw/networking. |
| 341 | 1.2 | `cl-teamradar` | `cl-teamradar.links.onslaught` | values:partial | Per-end color uses Team_ColorRGB (the radar team palette) instead of Base's colormapPaletteColor(colors & 0x0F / 0xF0) console-palette lookup; the team intent matches but the exact RGBs can differ slightly. Neutral/uncaptured ends map to white (0), close to Base's neutral. |
| 342 | 1.2 | `cl-teamradar` | `cl-teamradar.cvar.zoomscale` | values:partial | scale2d is hardcoded to 1 (assumes mi_picmin=(0,0)/mi_picmax=(1,1), i.e. the minimap image covers the full [mi_min,mi_max] rect); maps with a hand-authored _radar texcoord sub-rect would get a slightly wrong fit. Inherits the same BSP-bounds-vs-authored-rect caveat as background.minimap.; RangeUnits=2500 + scale/ScaleReferenceDivisor heuristic still exists but only on the degenerate no-map-bounds fallback (HasMapBounds false); the live standalone + discovered paths always have bounds and use the real Base bigsize/normalsize math. |
| 343 | 1.2 | `cl-viewmodel` | `cl-viewmodel.align.gunalign_gunoffset` | values:partial | cl_gunalign side magnitude is a fixed representative AuthoredShotSideY (-3.5u) rather than each v_ model's own shot-tag vecs.y; left/center now move the gun but by an approximate amount, not the per-model Base value. |
| 344 | 1.2 | `sv-campaign` | `sv-campaign.welcome.levelnum` | presentation:partial | Presentation surface differs from Base: Base's CSQC net_handle_ServerWelcome (client/main.qc:1379) builds a full Welcome MENU DIALOG; the port surfaces the same text as a single per-client CHAT line via Commands.ChatToPlayer (Campaign.Log fallback on a bare host). The level number, the per-level description (shortdesc + longdesc, sourced from the all-levels catalogue keyed by absolute level index), and the +jump keybind (resolved exactly like Base getcommandkey) are ALL reproduced — the only residual divergence is dialog-vs-chat. The 'no per-level description string' gap from the 2026-06-22 baseline is CLOSED (Wave 8e, commit 412ca19): CampaignLevelDescription (GameWorld.cs) now reconstructs the description the SVQC slice drops. |
| 345 | 1.2 | `sv-client-lifecycle` | `sv-client-lifecycle.frame.pressed_keys` | presentation:partial | Net delivery to the spectated HUD is still missing: Player.PressedKeys is computed server-side but is NOT in the owner-state net protocol, and PressedKeysPanel.PressedKeysOverride is never assigned — so a SPECTATED player's held keys don't reach the client HUD. The local-player panel works because PressedKeysPanel computes the bitmask client-side from bind state.; GetPressedKeys mutator hook is not fired as a discrete hook here (the race/cts speed-award + Vortex/OkNex velocity-charge consumers are driven from their own per-frame ticks instead). |
| 346 | 1.2 | `sv-commands-votes` | `sv-commands-votes.vote.call` | presentation:partial | The networked Nagger vote HUD entity (the on-screen vote prompt) is not emitted by this core — a client-side networked-entity presentation gap. The ANNCE_VOTE_CALL announcer cue IS now played (audio fixed). |
| 347 | 1.2 | `sv-impulse` | `sv-impulse.action.drop` | presentation:partial | Dual-wield sideways offset (dv = v_right * -movedir.y) collapses to zero — the port drives a single weapon slot (documented). The FL_TOSSED flag + colormap/glowmod render nits on the thrown loot are not modeled (presentation partial). |
| 348 | 1.2 | `sv-spawnpoints` | `sv-spawnpoints.put.put_player_in_server` | presentation:partial | EF_TELEPORT_BIT \| EF_RESTARTANIM_BIT are SET on the (re)spawned player server-side (SpawnSystem.cs:868, OR'd into p.Effects, networked via EntityField.Effects). The NO-INTERP half of EF_TELEPORT_BIT is now CONSUMED client-side (ServerNet.cs:1921 raises NetEntityFlags.Teleported on the spawn/teleport clear signal so the model snaps to the spawn spot instead of lerping). EF_RESTARTANIM_BIT is an intended no-op (the port's procedural LocomotionBlend re-derives the pose every frame; no frame-anim to restart). The remaining gap is the visible teleport SPARKLE: there is no dedicated csqcmodel flash from the EF bit — the spawn flash is delivered by the (team-colored) EFFECT_SPAWN particle burst (fx.spawn_event_particle) instead. Residual presentation gap, narrowed from baseline. STILL OPEN (presentation only). |
| 349 | 1.2 | `sv-world-rules` | `sv-world-rules.warmup.serverflags` | presentation:partial | Only the FORBID_PICKUPTIMER consumer is ported: the HUD pickup panel reads the bit to gate the leading match timer (PickupPanel.cs:169-173, QC client/hud/panel/pickup.qc:91). The ALLOW_FULLBRIGHT consumer is still unported — nothing reads SERVERFLAG_ALLOW_FULLBRIGHT to gate fullbright player rendering (Base client/view.qc:1115 cl_fullbright gate), so that bit has no on-screen effect. |
| 350 | 1.2 | `sv-world-rules` | `sv-world-rules.nextlevel.intermission` | presentation:partial | target_music_kill (stop any playing trigger-music entity at match end) is not invoked here — no MusicKill analogue in the port.; MatchEnd_RestoreSpectatorStatus (move match-joiners back to spectator at match end) is not called here (see new_unmapped).; The bprint team/player 'wins the match' broadcast lines are handled by the notification layer, not this path (verify coverage in sv-notification). |
| 351 | 1.2 | `sv-world-rules` | `sv-world-rules.boot.lightstyles` | presentation:partial | Styled lights animate by modulating only the dynlight's own RADIUS (DynamicLightRenderer samples LightStyles.Sample at 10fps and scales OmniRange). DarkPlaces additionally animates WORLD-BRUSH lightmaps for named styles; the port has no lightmap-modulation subsystem, so styled world geometry renders steady (only the dynlight node flickers). (Tracked also under mapobject-misc.yaml.) |
| 352 | 1.2 | `tmayhem` | `tmayhem.teams.count` | presentation:unknown | QC tmayhem_DelayedInit spawns/labels tmayhem_team map entities via Team_MapEnts_FindOrSpawn; the port derives only the team COUNT and relies on generic team registration. Whether tmayhem_team colours/entities are honoured was not verified (unchanged from baseline). |
| 353 | 1.2 | `turret-fusionreactor` | `turret-fusionreactor.setup.tr_setup` | presentation:missing | Initial head spin (50 deg/s) and head scale 0.75 are not applied — no head entity / no client render. |
| 354 | 1.2 | `turret-hellion` | `turret-hellion.anim.headspin` | presentation:missing | Server-authoritative frame STATE is now cycled (idle 0 -> spinning 1..6 kicked per shot, wrap at >=7), but there is no separate head MODEL entity (hellion.md3) attached to a bone, so the cycling frame drives no VISIBLE spinning launcher and is not used to select the firing muzzle tag (tag_fire vs tag_fire2 / two-cannon alternation) — both remain client-render gaps tracked here + on turret-hellion.weapon.launch. |
| 355 | 1.2 | `turret-plasma` | `turret-plasma.track.fluid_inertia` | presentation:missing | Head angles live only in server TurretState and are never networked/rendered — the head never visibly aims (presentation). |
| 356 | 1.2 | `turret-plasma` | `turret-plasma.headspin.frame_anim` | presentation:missing | PRESENTATION only: the frame-cycle logic runs server-side on the turret edict's e.Frame, but the body is a static base.md3 with no head-spin frames and there is no rendered tur_head sub-entity / CSQC turret net layer — so the spin is computed but never visibly shown. |
| 357 | 1.2 | `turret-plasma_dual` | `turret-plasma_dual.damage.headshake` | presentation:missing | Server-side head-angle jolt is now applied, but the CLIENT-rendered head model (plasmad.md3) is not attached, so the visible flinch is not drawn (the head-angle state nudges the next shot's aim, which IS the gameplay effect). Base also nets TNSF_ANG to CSQC; the port has no turret CSQC net layer. |
| 358 | 1.2 | `turret-plasma_dual` | `turret-plasma_dual.presentation.head_frame_anim` | presentation:missing | Server-authoritative head-frame state (Entity.Frame) is now ported and live, but the CLIENT render of the spinning plasmad.md3 head bone is absent — the static base.md3 body has no spin frames and there is no tur_head sub-entity / CSQC turret net layer, so the wheel is not visibly drawn.; Two-barrel tag_fire alternation (tag_fire on plasmad.md3) not modeled — single computed ShotOrg muzzle. |
| 359 | 1.2 | `vehicle-bumblebee` | `vehicle-bumblebee.spawn.vr_spawn` | presentation:missing | Wave-15 FIX: the three cosmetic cannon models are NOW created + tag-attached at spawn (Bumblebee.cs:115-117 SetGunModel: gun1=bumblebee_plasma_right.dpm @cannon_right, gun2=bumblebee_plasma_left.dpm @cannon_left, gun3=bumblebee_ray.dpm @raygun) via the real Api.Entities.SetModel + Api.Models.SetAttachment engine ops (previously the model strings were only assigned at death). The attached-model RENDER is code-verified only, not runtime-confirmed.; STILL NOT CREATED: the shield hit-flash entity (vehicle_shieldent, MDL_VEH_BUMBLEBEE_SHIELD, scale=512/vlen(maxs-mins)), the cockpit/viewport model offsets (body hudmodel '50 0 -5'/viewport '5 0 2'; gun1 '90 -27 -23'/'-85 0 50'; gun2 '90 27 -23'/'-85 0 50'), scale 1.5, and the networked BRG_* raygun entity (Net_LinkEntity + bumble_raygun_send). |
| 360 | 1.2 | `vehicle-framework` | `vehicle-framework.enter.steal` | presentation:missing | The CSQC presentation half is deferred: the WP_VehicleIntruder radar waypoint (g_vehicles_steal_show_waypoint) and the CENTER_VEHICLE_STEAL / _SELF centerprint notifications. The authoritative steal STATE (shield zero + flag backup/strip, restored on exit) is fully applied. |
| 361 | 1.2 | `vehicle-framework` | `vehicle-framework.respawn.return_waypoint` | presentation:missing | No WP_Vehicle return waypoint / radar icon is shown while a vehicle returns (still deferred, CSQC). Base spawns a separate vehicle_return entity with vehicles_showwp as think; the port re-arms the vehicle's own think directly (functionally equivalent for the return SCHEDULE, but no radar icon). |
| 362 | 1.2 | `vehicle-raptor` | `raptor.enter` | presentation:missing | tur_head.exteriormodeltoclient owner-binding and CSQCVehicleSetup (client viewport/HUD) not done here (cross-boundary client/net).; Carried-flag reparent ('-20 0 96') not modeled (CTF-on-raptor edge): a flag carried while boarding the raptor will not be repositioned to the cockpit mount. |
| 363 | 1.2 | `vehicle-raptor` | `raptor.flares` | presentation:missing | No EF_FLAME flare glow / MDL_VEH_RAPTOR_FLARE model / scale 0.5 (decoy is invisible but functional).; Flare damage uses Health<=0 -> remove inline rather than the dedicated raptor_flare_damage event_damage hook; behaviorally equivalent (a shot flare with health 20 dies).; Seduction scan filters proj.VehGuideMode>=0; QC's raptor_flare_think scans g_projectiles by it.enemy==owner WITHOUT a guide-mode filter (the MIF_GUIDED_TRACKING gate is only on the incoming-alarm path, not the flare retarget). The port's extra filter could under-match guided rockets the flare should seduce. |
| 364 | 1.1 | `net-entity-state` | `net-entity-state.csqcmodel.interpolation` | presentation:partial | Animation frame-group cross-fade (cl_lerpanim_maxdelta_framegroups/cl_nolerp) not networked; replaced by client-side LocomotionBlend — remote animation FRAME blending diverges from Base's networked 2-frame lerp. (The separate networked anim ACTION state is tracked in net-entity-state.csqcmodel.anim_state.) |
| 365 | 1.08 | `mapobject-teleporters-portals` | `teleporters.dest.random_selection` | timing:unknown | Port still forces telefrag-avoid always-on for clients rather than reading the now-registered g_telefrags_avoid / STAT(TELEPORT_TELEFRAG_AVOID) — equivalent at the Base default of 1 but not cvar-driven (see teleporters.core.telefrag_cvars). |
| 366 | 1.05 | `bot-ai` | `bot-ai.think.throttle` | values:partial | Simulated bot ping/aim-latency (CS(this).ping, bot_aimlatency) is not modeled; bots have zero network latency, so their lead/reaction is slightly sharper than stock at low skill. |
| 367 | 1.0 | `mutator-bloodloss` | `mutator-bloodloss.menu.dialog` | presentation:partial | The full MENUQC describe paragraph (bloodloss.qc:72-79, two sentences on stun/impaired movement/rapid health drain) is reduced to a one-line tooltip on the checkbox/slider. |
| 368 | 1.0 | `mutator-buffs` | `buffs.item.spawnfunc` | presentation:partial | Per-buff visual (skin/color/glowmod) is NOT applied to the spawned item: ConfigureBuffEntity sets the relic model + size but never assigns buff.m_skin / buff.m_color / glowmod, so the pickup renders as the uncolored relic regardless of which buff it carries (QC buff_Think colors it). Presentation partial.; Minor lifecycle omissions vs buff_Init: PFLAGS_FULLDYNAMIC dynamic light + buff_Customize light pulse, bot_pickupbasevalue 1000 (bots don't weight buff items), and the buff_Waypoint_Spawn build-finished waypoint (see buffs.waypoint) are not ported. |
| 369 | 1.0 | `mutator-buffs` | `buffs.item.touch_pickup` | presentation:partial | EFFECT_ITEM_PICKUP particle is still not spawned (no effect-spawn service); only the pickup sound + notifications play.; cl_buffs_autoreplace is read as the server-side value (no per-client REPLICATE store), so it can't differ per client; functionally equivalent on a server-wide setting. |
| 370 | 1.0 | `mutator-buffs` | `buffs.inferno` | presentation:partial | Carrier-as-target fire-immunity (DEATH_FIRE -> 0) + half-lava (DEATH_LAVA x0.5) present and match Base. The 0.5 lava factor is QC's hardcoded `0.5` (QC has a `TODO cvarize` so this is faithful).; Burn-on-hit via MonsterFramework.AddFireDamage applies the shared Burning status effect rather than QC's .fire_* accumulator; the integrated total over the burn window approximates QC. Minor numeric divergence in the per-tick distribution, not the total or the curve.; Presentation: no EF_FLAME flame visual is networked specifically for inferno's burn. |
| 371 | 1.0 | `mutator-buffs` | `buffs.replace_powerups` | presentation:partial | Replacement buff item inherits the spawnfunc presentation gap (buffs.item.spawnfunc): per-buff skin/color/glowmod is not applied, so the replaced powerup renders as the uncolored relic. No carrier glow/waypoint either. |
| 372 | 1.0 | `mutator-new_toys` | `mutator-new_toys.menu.describe_page` | presentation:partial | The describe() content (gimmicky-weapons intro, InstaGib/Overkill mutual-exclusivity note, and the current new-toy weapon list) IS ported, but as the New Toys checkbox tooltip rather than a dedicated PAGE_TEXT describe-page widget (the port has no describe-page surface — DialogMutators.cs:220-221 comment). Presentation SURFACE differs from Base's PAGE_TEXT; the text content (including the weapon list) is faithful. |
| 373 | 1.0 | `mutator-powerups` | `powerups.glow.effects` | presentation:partial | Presentation fidelity: Base's EF_ADDITIVE\|EF_FULLBRIGHT recolors the WHOLE model additively; the port renders the glow primarily as a blue/red OmniLight3D (EF_BLUE->blue light, EF_RED->red light, CsqcModelEffects.cs:119-120), and the EF_ADDITIVE/EF_FULLBRIGHT bits only affect per-instance-material meshes (shared/shader-material player models skip them — a documented CsqcModelEffects render gap). |
| 374 | 1.0 | `mutator-waypoints` | `waypoints.ctf.flag_markers` | presentation:partial | No flag-carrier HEALTH BAR (QC WaypointSprite_AttachCarrier sets one from 2*healtharmor_maxdamage; the port's CollectWaypoints sets Health=-1 and AttachCarrier has no live caller). UNCHANGED.; No auto-helpme on carrier hit (g_ctf_flagcarrier_auto_helpme_damage 100) — depends on the uncalled AttachCarrier/HelpMePing-on-carrier path. UNCHANGED.; Enemy-flagcarrier waypoint reveal on stalemate not wired: the QC ctf_stalemate enemy-FC waypoint reveal is not produced from the port's stalemate state. UNCHANGED. |
| 375 | 1.0 | `mutator-waypoints` | `waypoints.client.draw` | presentation:partial | g_waypointsprite_text / _uppercase / _iconsize / _fontsize / _scale / _alpha / _edgeoffset_* are read live and take effect; cl_hidewaypoints 0/1/2 + hideflag&1 (Hideable) gating honored. UNCHANGED.; g_waypointsprite_iconcolor still hardcoded white (QC iconcolor 0 default → white, so faithful at default; the non-zero tint option is not modeled). Minor. UNCHANGED.; The off-screen arrow is a simpler filled triangle than QC's bordered glyph (cosmetic). UNCHANGED. |
| 376 | 1.0 | `mutator-waypoints` | `waypoints.client.fades` | presentation:partial | Lifetime fade is server-computed (FadeAlpha) and networked rather than client-computed from fadetime/lifetime**timealphaexponent; timealphaexponent (1) not applied at the client (value 1 → no shape difference, but the computation site differs). Minor. UNCHANGED.; Off-screen arrow glyph still a simplified triangle (tracked under waypoints.client.draw). |
| 377 | 1.0 | `mutator-waypoints` | `waypoints.monster.healthbar` | presentation:partial | Live producer of the floating WP_Monster healthbar: SpawnHealthbar (MonsterAI.cs:446, called from Monster spawn at :568 under the g_monsters_healthbars gate) spawns WP_Monster at maxs.z+15 following the monster (maxdist 1024, hideable, RADARICON_DANGER), primes the bar unless INVINCIBLE, refreshes on hit (UpdateHealthbar / UpdateHealth), re-teams via ChangeTeam→UpdateTeamRadar, and is Killed on death (MonsterAI.cs:691/1909). Merged per peer via WaypointSprites.Active → ServerNet.SendWaypoints.; presentation: wp_extra (= monsterid, used client-side to resolve the monster's name/icon via get_monsterinfo) is not set on the sprite (and not networked), so the client renders the generic WP_Monster def text ('Monster') rather than the per-monster name. Cosmetic — same class as the other un-networked wp_extra gaps (waypoints.net.serialize). |
| 378 | 0.96 | `cl-hud` | `cl-hud.panel.weapons` | timing:unknown | Selection-slide animation + accuracy-tint overlay fidelity not runtime-verified. |
| 379 | 0.96 | `cl-hud` | `cl-hud.panel.powerups` | timing:unknown | Countdown expanding-flash (<=5s) timing not runtime-verified. |
| 380 | 0.96 | `cl-hud` | `cl-hud.panel.timer` | values:unknown | Secondary round-timer + overtime/sudden-death/warmup subtext coverage not all runtime-verified. |
| 381 | 0.96 | `cl-hud` | `cl-hud.panel.physics` | timing:unknown | topspeed peak fidelity not runtime-verified. |
| 382 | 0.9 | `mapobject-func` | `mapobject-func.button` | presentation:partial | the pressed-state alternate texture (Frame=1) is set on the entity but whether the brush model renders the alt frame in-engine is unverified (presentation).; button_reset round-restart re-arm is LIVE — ButtonReset is assigned at Buttons.cs:42 and dispatched by GameWorld.ResetMapObjects (e.Reset?.Invoke at GameWorld.cs:4704, body :4683; line shifted post-Wave-16). The Buttons.cs:15 comment is correct. (See mapobject-func.reset.) |
| 383 | 0.9 | `mapobject-func` | `mapobject-func.plat` | presentation:partial | plat_reset initial placement is live + faithful (targeted->raised, untargeted->bottom); the round-restart .reset is ALSO live now — PlatReset is assigned at Platforms.cs:82 and dispatched by GameWorld.ResetMapObjects. (CORRECTED 2026-06-25; the prior 'STILL NOT wired' note was stale — see mapobject-func.reset.); plat_outside_touch (the CSQC-side outside trigger that sends a top plat down when a player leaves) is not ported — it is gated to CSQC in Base; minor.; no CSQC ENT_CLIENT_PLAT draw/networking. |
| 384 | 0.9 | `mapobject-teleporters-portals` | `portals.weapon.projectile` | presentation:partial | OnTouch re-probes the impact face with a worldonly traceline (ProbeImpact) to recover Q3 surface flags + the true plane normal, so the slick/clip + noimpact branches are reachable; but on a miss (no nearby solid) it falls back to the negated-velocity normal rather than the real wall normal.; porto_v_angle held-angle capture for the non-secondary combined shot is dormant in stock play (g_balance_porto_secondary defaults to 1, so that branch is never reached); the hold state machine is modeled but only exercised with secondary disabled. |
| 385 | 0.9 | `mapobject-triggers` | `mapobject-triggers.push.trigger_push` | presentation:partial | no animdecide_setaction(ANIMACTION_JUMP) third-person jump pose on launch (jumppads.qc) — needs the cross-file networked anim-action seam (same gap as PlayerPhysics jump / walljump grunt); the locomotion blend still shows the airborne pose |
| 386 | 0.9 | `monster-spider` | `monster-spider.identity.registration` | values:partial | Spider.qh spawnflags (MON_FLAG_MELEE\|RANGED\|RIDE) are not represented on the port descriptor; RIDE is moot (no monster-riding mode), MELEE/RANGED are advisory (the Attack picks by distance regardless). |
| 387 | 0.9 | `monster-spider` | `monster-spider.think.brain` | timing:partial | Spider.mr_think in QC is a trivial return-true (no per-type logic); the port Think -> RunThink matches. The shared move steering uses a FIXED 0.4 velocity blend (movelib_move_simple approximation) rather than QC's frametime-scaled blend — a documented determinism choice, shared by all monsters; flagged timing:partial. |
| 388 | 0.9 | `monster-zombie` | `monster-zombie.setup.mr_setup` | timing:partial | Spawn-anim/no-think window uses AnimDurSpawn=61/30s (~2.033s), a baked constant read from the zombie.dpm group-30 framegroup at compile time, rather than a live animstate_endtime/frameduration() call (close approximation, not a runtime model query). The spawn POSE (frame 30) IS played; only the per-frame fps source is baked-vs-live (timing). No longer the old hardcoded 1/3s. |
| 389 | 0.9 | `monster-zombie` | `monster-zombie.presentation.animation` | timing:partial | Per-frame fps/duration (the second/third animfixfps components, e.g. die1 '9 1 0.5' = 2s) is not modeled by the phase enum: frame VALUES are byte-exact but the per-group playback RATE is approximate (timing). No frame-group/value losses remain — all 9 distinct groups (spawn 30, idle 19, walk/run 27, melee 4, pain1/2 20/22, die1/2 9/12, blockstart 8, blockend 7, shoot 0) are mapped, and both random variants (pain1/pain2, die1/die2) now alternate. |
| 390 | 0.9 | `turret-framework` | `turret-framework.spawn.init` | values:partial | Per-unit cvar table unported (load_unit_settings / TR_PROPS_COMMON / turret_scale_* / turret_initparams clamping / dumpturrets) — per-unit balance is hardcoded C# const.; No tur_head child entity / tag_head attachment (head bone is a TurretState.HeadAngles vector, not an edict).; turret_initparams clamping/derivation absent: per-turret consts are used raw with no bound/default fill. |
| 391 | 0.9 | `vehicle-framework` | `vehicle-framework.init.initialize` | values:partial | targetname controller / ACTIVE_NOT gating / vehicle_use not ported (map-scripted activation absent, sv_vehicles.qc:1178-1201/1276-1277). STILL OPEN. The targetname branch that ASSIGNS .team is unported, but the team-CLEAR branch (below) is independent of it.; dphitcontentsmask PLAYERCLIP gate (g_playerclip_collisions) unported (descriptor-set mask only). Keeps values partial. |
| 392 | 0.9 | `vehicle-racer` | `vehicle-racer.board.usekey` | values:partial | Flag offset diverges: the port anchors a boarding carrier's flag at (0,0,96) (the shared CTF VEHICLE_FLAG_OFFSET applied every tick by Ctf.Tick when carrier.Vehicle != null), whereas the racer's vr_enter does a one-shot setorigin to '-190 0 96' (190u BEHIND the cockpit). Logic is live and equivalent-or-stronger (the flag follows the moving craft each tick), but the carried-flag world position is off by ~190u forward vs Base on the racer. Cosmetic/position only. |
| 393 | 0.8 | `cl-crosshair` | `cl-crosshair.anim.smooth_ease` | presentation:partial | Color is NOT eased: Base eases scale, alpha AND color toward goal over effect_time (crosshair.qc:444 `wcross_color = f*wcross_color + (1-f)*wcross_color_prev`); the port's SmoothGoal eases only scale+alpha and sets color directly each frame. Visually equivalent only for slow color changes within the effect_time window. |
| 394 | 0.8 | `cl-crosshair` | `cl-crosshair.trueaim.teammate_blur_signal` | presentation:partial | Blur is a 5x5 spread of 0.04-alpha texture copies (matches Base's CROSSHAIR_DO_BLUR for textured crosshairs, crosshair.qc:585-596); only applies to the textured path (DrawCrosshairTexture), not the vector fallback (DrawVectorCrosshair). |
| 395 | 0.8 | `cl-hud` | `cl-hud.engine.registry_and_order` | presentation:partial | Draw order follows Godot child order, not the QC _hud_panelorder reverse-walk; relative panel layering may differ but panels don't overlap in luma. |
| 396 | 0.8 | `cl-hud` | `cl-hud.panel.pressedkeys` | presentation:partial | key_*/key_*_inv per-key art coverage not all runtime-verified (the inverted/pressed art set). |
| 397 | 0.8 | `cl-scoreboard` | `cl-scoreboard.header.gameinfo` | presentation:partial | Lead-limit header term IS now built (BuildLimitsHeader handles ll > 0 && (ll < fl \|\| fl <= 0) with the '^2+N' term and the '& '/'/'  delimiter, LeadLimit/LeadAndFragLimit fed from cvars NetGame.cs:4175-4176).; m_hidelimits and campaign player-count suppression not handled. |
| 398 | 0.8 | `cl-scoreboard` | `cl-scoreboard.block.spectators` | presentation:partial | spectators_position (0/1/2/3 placement around the blocks) and spectators_aligned column alignment not implemented (drawn after the table only). |
| 399 | 0.8 | `cl-scoreboard` | `cl-scoreboard.block.rankings` | presentation:partial | Speed-award / all-time-fastest line is now PORTED + LIVE (DrawSpeedAward, ScoreboardPanel.cs:1257, fed by SetSpeedAward NetGame.cs:3837 from networked SpeedAward/Best + holders; QC unit conversion + alltimebest gate).; No multi-column wrap / horizontal scroll (single-column rankings list). |
| 400 | 0.8 | `fx-notifications` | `fx-notifications.info.killfeed` | presentation:partial | MULTIICON_INFO (icon derived from args via iconargs) has no port path; the only Base user is MINIGAME_INVITE (minigames not a port priority).; notification_allow_chatboxprint (MSG_INFO -> chatbox routing) not modelled; benign — Base disables it in code (default 0). |
| 401 | 0.8 | `mutator-instagib` | `mutator-instagib.spawn.fullbright` | presentation:unknown | Whether EffectFlags.FullBright actually renders the player model fullbright in the Godot renderer is still unverified (behavioral check needed). |
| 402 | 0.72 | `monster-mage` | `monster-mage.anim.frames` | timing:unknown | timing (animfixfps fps normalization — the '<start> 1 <fps>' frame-rate component) is not modeled; only the start frame index is stamped onto Entity.Frame, so per-group playback rate is not normalized to Base. |
| 403 | 0.6 | `monster-golem` | `monster-golem.attack.lightning` | presentation:partial | Projectile renders the correct model (models/ebomb.mdl) but at 1x — QC gren.scale = 2.5 (golem.qc:145) is not networked (Entity has no render-scale field; the CSQCMODEL scale-bit contract is unported). Visible bolt is smaller than Base but no longer headless. |
| 404 | 0.6 | `monster-mage` | `monster-mage.attack.spike` | presentation:partial | In-flight spike CSQC visual is now WIRED but MISCLASSIFIED (regression introduced post-baseline). FireSpike (Mage.cs:242) pins spike.NetName="mage_spike"; the spike networks as a Projectile (ServerNet Classify: FL_ITEM+owner+FLYMISSILE) so it DOES reach ProjectileRenderer.OnSpawn -> ProjectileCatalog.Classify. But the combined key string contains the substring "spike" (from "mage_spike"), and Classify hits `Has(s,"spike","crylink")` (ProjectileCatalog.cs:272) BEFORE the `Has(s,"mage")` MageSpike branch (cs:299) -> the spike renders as a CRYLINK bolt (purple shards) instead of PROJECTILE_MAGE_SPIKE (TR_VORESPIKE violet trail). Ironically the pre-edit default NetName "mage" would have classified correctly; the "mage_spike" override broke it. Fix: rename to a token without "spike" (e.g. just "mage") OR move the `mage` check above the `spike` check in Classify. |
| 405 | 0.6 | `sv-world-rules` | `sv-world-rules.checkrules.overtime` | audio:partial | race/qualifying race_StartCompleting branch deliberately omitted (those modes latch their own winner). |
| 406 | 0.6 | `turret-ewheel` | `turret-ewheel.identity.def` | presentation:partial | Head model (ewheel-gun1.md3) string is now CARRIED as identity data (HeadModel field, Wave-17) but is still not instantiated as a separate head bone entity; no tag_head attachment and no client render of the head. Its only consumer (TurretAI.TossDeathGibs head-gib) explicitly skips turret_ewheel (ClassName guard at TurretAI.cs:1204, because Base tosses the whole ewheel model in its own CSQC draw), so for ewheel the field is presently inert identity data — presentation stays partial, NOT closed.; ROAM spawnflag not modeled as an explicit flag (the path-roaming behavior it gates is now implemented and live — see turret-ewheel.drive.path). |
| 407 | 0.6 | `turret-ewheel` | `turret-ewheel.weapon.fire` | presentation:partial | No two-cannon head-frame alternation (Base ewheel_weapon.qc:32-35: tur_head.frame += 2, wrap > 3 -> 0) — needs the unported head sub-entity (the turret's own Entity.Frame is consumed by the locomotion drive animation); tracked under turret-ewheel.identity.def.; EFFECT_BLASTER_MUZZLEFLASH is emitted with except: turret, but Base Send_Effect -> Send_Effect_Except(..., NULL) does NOT exclude the firer — minor over-exclusion (cosmetic for a turret, which is not a first-person view source). |
| 408 | 0.6 | `turret-ewheel` | `turret-ewheel.client.draw` | presentation:partial | CSQC ewheel_draw rolling integration not ported: the model does not client-side roll (origin += velocity*dt, head angles += avelocity*dt, MOVETYPE_BOUNCE+gravity). The server already networks origin/velocity, so the visible motion follows the netted transform rather than the client integration. |
| 409 | 0.6 | `turret-flac` | `turret-flac.identity.spawn` | presentation:partial | Head-BONE render attachment for the live in-combat head is still absent: no client turret draw attaches/renders a separate head bone, so the 4-frame head cycle (FlacTurret.cs:159, runs on the turret's own Entity.Frame) + the headshake jolt drive head-local angle/frame STATE server-side but nothing visibly articulates a head sub-model in combat. This is a whole-turret-family render gap, not flac-specific. |
| 410 | 0.6 | `turret-flac` | `turret-flac.fire.flak_shell` | presentation:partial | Presentation partial (Wave 8e landed the server-emit side, re-verified 2026-06-27): BLASTER_MUZZLEFLASH is emitted via EffectEmitter (FlacTurret.cs:154), the shell is stamped NetName='hagar' so the client ProjectileCatalog resolves the HAGAR trail (FlacTurret.cs:119), and the 4-frame head cycle runs on turret.Frame (FlacTurret.cs:159). REMAINING: the actual HAGAR trail RENDER + muzzleflash draw live in the Godot client project (not qcsrc) and are not yet verified end-to-end; the head MODEL flac.md3 is now set but is only consumed for the death-gib toss — no in-combat head-bone render attaches it, so the frame index articulates nothing visible during play yet. |
| 411 | 0.6 | `turret-fusionreactor` | `turret-fusionreactor.identity.def` | presentation:partial | head_model (reactor.md3) is not carried — no head entity exists in the port turret model.; TUR_FLAG_SUPPORT/AMMOSOURCE are not stored as flags; support behavior is hardcoded in the C# class instead of derived from spawnflags. (Verified TUR_FLAG_AMMOSOURCE is declared but NEVER read anywhere in Base qcsrc — purely declarative — so its omission is functionally harmless; TUR_FLAG_SUPPORT only selects turret_targetscore_support, which the reactor never invokes since it has no targetscore-driven path.) |
| 412 | 0.6 | `turret-hellion` | `turret-hellion.identity.def` | presentation:partial | Head model (hellion.md3) not instantiated as a separate spinning-launcher head entity; no tag attachment. |
| 413 | 0.6 | `turret-hellion` | `turret-hellion.weapon.launch` | presentation:partial | No two-cannon tag_fire/tag_fire2 muzzle alternation (single computed ShotOrg origin). The head-spin frame state that Base uses to SELECT the tag is now cycled (turret-hellion.anim.headspin) but there is no head-model bone, so the muzzle-tag alternation stays a client-render gap.; No PROJECTILE_ROCKET CSQC trail / MIF_SPLASH\|MIF_PROXY\|MIF_GUIDED_HEAT flags (no visible smoke trail). |
| 414 | 0.6 | `turret-hk` | `turret-hk.identity.def` | presentation:partial | Head model (hk.md3) not instantiated as a separate head entity; no tag_head attachment (presentation only). |
| 415 | 0.6 | `turret-hk` | `turret-hk.anim.head` | presentation:partial | Frame cycler runs on the turret edict's own .Frame (the port has no separate tur_head bone entity), and there is no hk.md3 head model instantiated, so the 0..5 load/cycle animation is computed but never visibly displayed. |
| 416 | 0.6 | `turret-machinegun` | `turret-machinegun.spawn.identity` | presentation:partial | No separate tur_head sub-entity / machinegun.md3 head model attached at tag_head (Base turret_initialize:1344 spawns a turret_head entity and _setmodel(head_model)); the port's head is angle-state only (TurretState.HeadAngles), so the head model never renders or frame-animates. The descriptor still CARRIES HeadModel (MachinegunTurret.cs:56) so the death-gib toss can throw the head model, but nothing attaches it as a live render bone. This is now the unit's primary presentation residual (sparks/smoke/gibs/muzzleflash/tracer all landed). |
| 417 | 0.6 | `turret-machinegun` | `turret-machinegun.lifecycle.damage` | presentation:partial | Headshake jitter is applied to the angle-state head (TurretState.HeadAngles, EventDamage:893-897) but there is no separate tur_head edict and no CSQC TNSF_ANG send, so the recoil flinch is server-state only — it will not be visible until a head model is attached and networked. Behaviour is faithful; visual is presentation-deferred. |
| 418 | 0.6 | `turret-mlrs` | `turret-mlrs.identity.def` | presentation:partial | Head model (mlrs.md3) is now set on the descriptor (MlrsTurret.cs:62) but is NOT instantiated as a separate in-world tur_head entity; no tag_head attachment. The ammo-gauge frame rides the turret's own .Frame (see turret-mlrs.anim.ammogauge); the head_model string is consumed only by the death head-gib (TurretAI.TossDeathGibs), not rendered/animated as a live head bone. |
| 419 | 0.6 | `turret-mlrs` | `turret-mlrs.anim.ammogauge` | presentation:partial | Frame formula now ported every think onto the turret edict's OWN .Frame (Bound(0, 6 - floor(0.1 + Ammo/ShotDamage), 6)) — logic/values/timing faithful. But Base writes it to the separate tur_head entity's .frame and nets that head; the port has no distinct head bone entity, so the gauge is not rendered on a separately-attached mlrs.md3 head. Presentation partial. |
| 420 | 0.6 | `turret-mlrs` | `turret-mlrs.damage.lifecycle` | presentation:partial | Death gib toss is now PORTED + LIVE but visually unverified: TurretAI.Die -> TossDeathGibs (TurretAI.cs:1199-1235) runs the generic non-ewheel/walker/tesla branch for turret_mlrs, spawning base-gibN.md3 chunks + a head_model head-gib (with the head's last angles/spin, halved gravity, exploding via GibBoom -> head-gibN.md3 sub-chunks) as SERVER-SIDE physics entities through the entity feed (the vehicle-wreckage pattern). Bit-faithful to cl_turrets.qc turret_die/turret_gibtoss/turret_gibboom by code read, but the actual gib RENDER has not been runtime-verified, so presentation is partial not faithful.; Low-hp sparks/smoke now emitted server-side (TurretAI.DrawFx, TurretAI.cs:1319-1337, called from RunCombat AND the MlrsTurret.Think mid-burst branch): <127 hp te_spark / <85 SMOKE_LARGE / <32 SMOKE_SMALL with the single shared random roll. Code-read faithful; render unverified.; Still genuinely missing: the in-world head SPIN (cl_turrets.qc turret_draw angles += avelocity*dt) — there is no client turret edict, so the mlrs.md3 head is never rendered/spun in-world (FusionReactorTurret.cs:152-154 confirms 'no client turret render yet' for the whole family). |
| 421 | 0.6 | `turret-phaser` | `turret-phaser.identity.model_hitbox` | presentation:partial | Head model (phaser.md3) is not attached as a separate tracking head bone; head frame animation is also absent (see tr_think feature). |
| 422 | 0.6 | `turret-plasma` | `turret-plasma.identity.model_hitbox` | presentation:partial | head_model (plasma.md3/plasmad.md3) is now SET (ctors) and consumed server-side by TossDeathGibs, but there is still no rendered tur_head sub-entity — the live turret shows only the static base.md3 body, never the aiming head. |
| 423 | 0.6 | `turret-tesla` | `turret-tesla.identity.def` | presentation:partial | Head model (tesla_head.md3) not instantiated as a separate head/coil entity; no tag_head attachment. TrThink computes head spin as state (HeadAngles) but grep confirms no head-bone node consumes HeadAngles, so the coil never visibly spins (see anim feature). |
| 424 | 0.6 | `turret-tesla` | `turret-tesla.anim.tr_think` | presentation:partial | Head spin not rendered: TrThink computes HeadAVelocity ('0 45 0' low-ammo / '0 180 0' charged, scaled by ammo/shot_dmg, zeroed when inactive) and integrates HeadAngles, but grep (2026-06-27) confirms no head-bone/coil entity (tesla_head.md3) consumes HeadAngles — the only readers are TurretAI track-motor/death-gib logic + TeslaTurret.TrThink itself. The coil never visibly spins; server-side state only. This is the last real gap on this unit. |
| 425 | 0.6 | `turret-walker` | `turret-walker.identity.def` | presentation:partial | Head model (walker_head_minigun.md3) not instantiated as a separate tur_head entity; no tag attachment, so the minigun head never visibly tracks. |
| 426 | 0.6 | `vehicle-bumblebee` | `vehicle-bumblebee.pilot.raygun` | presentation:partial | The visible BRG_* beam is a placeholder EffectEmitter.TeHealBeam/TeBeam stub (Bumblebee.cs:698-700), not the networked CSQC bumble_raygun beam; the per-tick BRG_START/BRG_END send + `wait = time + 1` beam-hold + the bumble_raygun_draw multi-segment cylinder are not modeled. Beam COLOUR is bit-faithful (pure green heal / pure red damage). |
| 427 | 0.6 | `vehicle-bumblebee` | `vehicle-bumblebee.gunner.frame` | presentation:partial | Gunner aux crosshairs + cockpit HUD are now LIVE (Wave-7): GunnerFrame writes VehGunnerLeadPoint/Valid + VehGunnerHitPoint/Valid (Bumblebee.cs:605/648) and mirrors gunner.VehicleHealth/Shield/Ammo1/Energy (632-636); NetGame.UpdateVehicleHud selects the BumblebeeGun art for a seated gunner and projects the magenta '1 0 1' LEAD (aux slot 1) + reload-color READY (aux slot 0) crosshairs, AND mirrors the gunner READY markers onto the PILOT (slots 1/2 via FeedPilotGunnerAux). RESIDUAL presentation: cosmetic gun cockpit models + viewport offsets are still not built (HUD bars/crosshairs render, but the 3D gun cockpit art does not). |
| 428 | 0.6 | `vehicle-bumblebee` | `vehicle-bumblebee.gunner.cannon` | presentation:partial | Wave-8e FIX: EFFECT_BIGPLASMA_MUZZLEFLASH is now emitted at the muzzle (FireCannon: EffectEmitter.Emit('BIGPLASMA_MUZZLEFLASH', org, vel, 1); BIGPLASMA_MUZZLEFLASH registered EffectsList.cs:163). RESIDUAL: the networked CSQCProjectile (PROJECTILE_BUMBLE_GUN) bolt trail/model visual is still NOT IMPLEMENTED. |
| 429 | 0.6 | `vehicle-bumblebee` | `vehicle-bumblebee.death` | presentation:partial | Wave-15 FIX: the gun1/gun2/gun3 wreckage gibs ARE now tossed (Bumblebee.cs:469-488 -> VehicleCommon.TossGib, the now-ported vehicle_tossgib physics-entity subsystem) — each cannon flung along the body axes (+/-300*right, +300*fwd) with jitter, 50/50 burn (EF_FLAME) + 50/50 explode-on-contact, carrying its real plasma/ray model string, over a 6s fade window. RESIDUAL (minor, intended): Base also replaces the BODY with a separate tossgib that runs diethink; the port keeps the diethink/blowup+respawn on the original body in place (functionally equivalent, one fewer throwaway entity). The tossed-gib RENDER appearance is not runtime-verified (code-kind only). |
| 430 | 0.6 | `vehicle-framework` | `vehicle-framework.spawn.respawn` | presentation:partial | EFFECT_TELEPORT spawn flash is NOW EMITTED (VehicleCommon.cs:401-402 via the live EffectEmitter seam, the same one Teleporters/Tuba use). Minor: Base spawns it at origin+'0 0 64' (sv_vehicles.qc:1144); the port emits at origin (no +64 z-offset) — a tiny positional nuance, not a missing cue.; CSQCMODEL_AUTOINIT, the hook-removal sweep, and vehicles_reset_colors are still deferred (client/net) — keeps presentation partial. |
| 431 | 0.6 | `vehicle-framework` | `vehicle-framework.enter.handshake` | presentation:partial | MOVETYPE_NOCLIP + disableclientprediction=1 modeled as MoveType.None (player origin rewritten by the vehicle); behaviorally close but not the QC movetype.; SVC_SETVIEWPORT/SETVIEWANGLES + CSQCVehicleSetup, the temp_wepent weaponslot swap, CENTER_VEHICLE_ENTER notification, and RemoveGrapplingHooks are deferred (cross-boundary). The VehicleHud panel (the client view of CSQCVehicleSetup) IS fed live — see hud_camera_xhair. antilag_clear is now wired (pl.AntilagNeedsClear set on enter). |
| 432 | 0.6 | `vehicle-framework` | `vehicle-framework.exit.handshake` | presentation:partial | SVC_SETVIEWPORT/SETVIEWANGLES + CSQCVehicleSetup, temp_wepent switchweapon restore, Kill_Notification(CPID_VEHICLES), and last_vehiclecheck are deferred. (The HUD exits cleanly: UpdateVehicleHud calls hud.Exit() once p.Vehicle is null.) antilag_clear is now wired (pl.AntilagNeedsClear set on exit).; The WP_Vehicle return waypoint (vehicles_showwp) itself is still CSQC-deferred (the return SCHEDULE is now set — see respawn.return_waypoint). |
| 433 | 0.6 | `vehicle-framework` | `vehicle-framework.combat.damage` | presentation:partial | vehicle_shieldent hit flash (colormod/alpha angled to hitloc) is a CSQC visual — not drawn.; antilag_clear on death is deferred (cross-boundary). |
| 434 | 0.6 | `vehicle-framework` | `vehicle-framework.aim.locktarget` | presentation:partial | The cockpit aux-crosshair lock-color update is LIVE for the building/locked target (UpdateVehicleHud -> SetAuxiliaryXhairLock projects VehLockTarget tinted by VehLockStrength); the per-target-class lock SEMANTICS are now also complete. |
| 435 | 0.6 | `vehicle-framework` | `vehicle-framework.projectile.shared` | presentation:partial | CSQCProjectile networking + the in-flight projectile trail/model visual are deferred (client/net). The muzzle EFFECT_* IS now emitted by the descriptors (Send_Effect via EffectEmitter).; g_bot_dodge list (bot AI) deferred. |
| 436 | 0.6 | `vehicle-framework` | `vehicle-framework.presentation.hud_camera_xhair` | presentation:partial | Cockpit/chase camera STILL dead: no client reads p.Vehicle to apply SVC_SETVIEWPORT (vehicle_viewport) — the pilot keeps the normal player view. (This is the only remaining DEAD piece of this row; it is its own viewport concern, not the HUD panel/crosshair.); Host-only feeder: UpdateVehicleHud reads LocalServerPlayer (host path). The cross-client VEHICLESTAT_* networking for a pure remote pilot is still unwired.; Vehicles_drawCrosshair per-mode reticles are now wired for Raptor (bomb/flare + dropmark), Spiderbot (FeedSpiderbotReticle: vCROSS_BURST/_RAIN/_GUIDE by rocket mode, Wave 17, NetGame.cs:3477) AND Bumblebee (crosshair_heal, Wave 17, NetGame.cs:3511) — all fed live from UpdateVehicleHud. Racer has no per-mode reticle in Base (correct absence). |
| 437 | 0.6 | `vehicle-racer` | `vehicle-racer.identity.def` | presentation:partial | hud cockpit model + tag_viewport attachment and the scale-0.5 model hack are cosmetic-only and not instantiated (physics uses the real hitbox).; view_ofs '0 0 50' + height 200 (camera view offset/dist) not set; affects the cockpit camera, not the sim. |
| 438 | 0.6 | `vehicle-racer` | `vehicle-racer.spawn.lifecycle` | presentation:partial | scale 0.5 (cosmetic model-shrink hack) still not applied; physics uses the real hitbox so this is presentation-only. |
| 439 | 0.6 | `vehicle-racer` | `vehicle-racer.weapon.cannon` | presentation:partial | The EFFECT_RACER_MUZZLEFLASH muzzle effect is now emitted; the CSQCProjectile (PROJECTILE_WAKICANNON) bolt trail remains a client-render concern and is unported. |
| 440 | 0.6 | `vehicle-racer` | `vehicle-racer.weapon.rocket` | presentation:partial | The EFFECT_RACER_ROCKETLAUNCH launch effect is now emitted; the CSQCProjectile (PROJECTILE_WAKIROCKET) rocket trail remains a client-render concern and is unported. |
| 441 | 0.6 | `vehicle-racer` | `vehicle-racer.weapon.rockethud` | presentation:partial | Authority-side ammo2/reload2 % stats are written each tick onto the pilot; the client HUD progressbar that READS them (Vehicles_drawHUD vehicle_icon_ammo2) is presentation/CSQC and not yet wired — a client-render concern, not an authority gap. |
| 442 | 0.6 | `vehicle-raptor` | `raptor.spawn.setup` | presentation:partial | DRIFT FIX (2026-06-27b): the client now renders a vehicle BODY model + two SPINNING rotors (engine_left +900°/s, engine_right -900°/s) + the gun mounts, via game/client/VehicleVisuals.cs fed live by ClientWorld.DriveVehicles. So presentation is no longer fully missing — but the rotors are PLACEHOLDER primitives (Placeholder('Prop',...) at VehicleVisuals.cs:177), NOT the real raptor rotor model, there is no raptor_rotor_anglefix yaw-overflow fix, and no bomb1/bomb2/tur_head cosmetic models. Hence partial, not faithful. (gun1/gun2 server aim-entities ARE created in Raptor.Spawn.); mass=1 is set on the entity but has NO behavioral effect — the port's MoveTypePhysics solver never reads .Mass, so the QC bounce-missile mass tuning is inert (value set for fidelity only).; FIXED (Wave-2 fixup): Spawn() no longer nulls vehicle.Touch, so the VehicleCommon.Touch dispatcher SpawnVehicle installed stands (crush + vr_impact path now live, see raptor.impact). Matches QC vr_spawn (does NOT clear .touch) and Racer (Racer.cs:189-190). |
| 443 | 0.6 | `vehicle-raptor` | `raptor.takeoff` | presentation:partial | Rotors now spin on the client (VehicleVisuals SpinRotors), BUT the takeoff animation-frame ramp (VehAnimFrame 0->25) still drives NO visible model frame — the raptor body is NOT FrameDrivenBody (only the spiderbot legs are; VehicleVisuals.DriveBodyFrame is a no-op for the raptor), so the gear/canopy/rotor-tilt takeoff pose is not shown, and the QC rotor avelocity RAMP (90 + frame/25*25000) is approximated by a constant 900°/s spin rather than the takeoff spool-up. Hence partial.; Wave-2 added a button-clear (ATCK/ATCK2/CROUCH) at the end of Takeoff, but it writes vehicle.VehInput which GameWorld.cs:1104-1105 overwrites with fresh player input every tick BEFORE the next Think — so the clear is effectively a no-op on the live path. QC's per-tick button zero is faithful but the port's input-refresh architecture makes it inert (minor — input only reaches Frame, not Takeoff, so no fire/dive actually leaks). |
| 444 | 0.6 | `vehicle-raptor` | `raptor.flight_controller` | presentation:partial | The client now renders the raptor body model following the entity origin + YAW (ClientWorld.DriveVehicles:1503-1504 sets vis.Position + vis.Rotation = yaw-only), so the airframe is visible and turns. BUT DriveVehicles applies ONLY yaw — the pitch and roll the flight controller computes into vehicle.Angles (the nose-down/up + bank-on-strafe pose) are NOT applied to the visual node, so the body doesn't visibly pitch/bank. Hence partial. (Player glue origin+'0 0 32' is server-side and correct.) |
| 445 | 0.6 | `vehicle-raptor` | `raptor.cannon_fire` | presentation:partial | DRIFT FIX (2026-06-27 re-audit): the EFFECT_RAPTOR_MUZZLEFLASH IS now emitted server-side — Raptor.cs:573 EffectEmitter.Emit('RAPTOR_MUZZLEFLASH', org, vel, 1), matching QC vehicles_projectile's Send_Effect(_mzlfx, proj.origin, proj.velocity, 1). This is LIVE: RAPTOR_MUZZLEFLASH registered EffectsList.cs:156 -> effectinfo raptor_cannon_muzzleflash (effectinfo.txt:5085) -> EffectEmitter.Sink is the live EffectNetSink (ServerNet.cs:303) -> networked -> client RenderSink (ClientWorld.cs:393) renders the particle. (Wave-17 added the emit; the registration/effectinfo always existed but had no caller.); Cannon spread RNG FIXED (Wave-15, Raptor.cs:561): the port now does QC's normalize(fwd + randomvec()*spread)*speed exactly (was the uniform-in-disc Prandom.Spread cone). Distribution shape now bit-faithful.; Remaining: no CSQCProjectile in-flight bolt trail (PROJECTILE_RAPTORCANNON) — a client-render trail on the moving bolt (server hit logic + muzzle FX are live). |
| 446 | 0.6 | `vehicle-raptor` | `raptor.bombs` | presentation:partial | Remaining presentation: no CSQCProjectile visual (PROJECTILE_RAPTORBOMB / RAPTORBOMBLET) for the FALLING bomb/bomblet in-flight trail.; DRIFT FIX (2026-06-27b): the burst FX is now MORE complete — the RAPTOR_BOMB_SPREAD particle puff AND the bomb-casing shell-fragment gibs (RaptorCBShellfragToss) BOTH render on burst now (see raptor.cb_shellfrag_gib, which went missing->faithful/live this re-audit). The only burst-FX piece left is the in-flight projectile trail above. |
| 447 | 0.6 | `vehicle-raptor` | `raptor.exit_land` | presentation:partial | Rotors now spin on the client during the land descent (VehicleVisuals self-drives them while alive), but the Land animation frame from altitude (Raptor.cs:472-473 VehAnimFrame = (hgt/128)*25 — added Wave-15) still drives NO visible model frame (raptor is not FrameDrivenBody). So the gear-down/settle pose is not shown — hence partial.; antilag_clear on exit + CSQCVehicleSetup viewport restore are cross-boundary (net/client). |
| 448 | 0.6 | `vehicle-raptor` | `raptor.impact` | audio:missing | CONFIRMED LIVE (fixup commit 4be8357, re-verified 2026-06-22): Raptor.Spawn no longer nulls vehicle.Touch, so the VehicleCommon.Touch dispatcher SpawnVehicle installs stands and calls vehic.VehicleDef.Impact on a hard touch (matches Racer.cs:217-219 / QC vr_spawn which does NOT reset .touch). Raptor.Think:231 snapshots vehicle.OldVelocity BEFORE the tick's physics (matches Racer.Think:296), and Spawn:141 seeds PlayTime = Time to clear the impact debounce on respawn — so vehicles_impact computes the true Velocity-OldVelocity delta. The raptor takes DEATH_FALL crash damage on hard impacts.; Dispatch chain VERIFIED (2026-06-27): a BounceMissile-movetype raptor is integrated by MoveTypePhysics (src/XonoticGodot.Engine/Simulation/MoveTypePhysics.cs:190) which calls PushEntity(doTouch:true) -> FlyMove invokes e.Touch for non-Solid.Not solids -> VehicleCommon.Touch:793 -> VehicleDef.Impact:819 when PlayTime<Time. (Earlier baseline cited TriggerTouch.cs for this; the actual moving-solid touch dispatch is FlyMove/MoveTypePhysics — corrected, behavior unchanged.); VehicleCommon.Impact:835 is line-for-line faithful to vehicles_impact (delta=Velocity-OldVelocity, min(speedfac*len,maxpain), DEATH_FALL, play_time=time+0.25). Only the trace_dphitq3surfaceflags & Q3SURFACEFLAG_NOIMPACT skip is deferred (no per-touch surface-flag in the headless collision result). |
| 449 | 0.6 | `vehicle-raptor` | `raptor.death` | presentation:partial | EXPLOSION_MEDIUM (death) + the per-think 5% EXPLOSION_SMALL ARE emitted via EffectEmitter (server), and the client VehicleVisuals.Die (Wave-17) ALSO bursts generic gib debris chunks (GibCount 7) + an explosion + the death sound on the alive->dead edge and hides the intact body/guns/rotors. The findbetterlocation surface-nudge for the medium blast is not reproduced (FX at origin directly — cosmetic offset only), and the gibs are generic debris primitives, not the real raptor wreck/gib models. So presentation stays partial. |
| 450 | 0.6 | `vehicle-spiderbot` | `vehicle-spiderbot.identity.registration` | presentation:partial | view_ofs '0 0 70' / height 170 (the seated camera offset + chase distance) are client-view concerns not modeled on the headless server descriptor. |
| 451 | 0.6 | `vehicle-spiderbot` | `vehicle-spiderbot.weapon.minigun` | presentation:partial | Cosmetic barrel spin (QC gun1.angles.z += 45 / gun2.angles.z -= 45 with 360 wrap) is done client-side (Firing flag in VehicleVisuals); the server-state field is not advanced — functionally fine, non-1:1 split.; FIXED-SINCE-BASELINE: EFFECT_SPIDERBOT_MINIGUN_MUZZLEFLASH is now emitted server-side via EffectEmitter.Emit (Spiderbot.cs:493; effect registered src/XonoticGodot.Common/Gameplay/Effects/EffectsList.cs:161), matching QC spiderbot.qc:276 Send_Effect(EFFECT_SPIDERBOT_MINIGUN_MUZZLEFLASH, v, v_forward*2500, 1). (The client VehicleVisuals Firing flag also pops a flash; the server emit is the QC-faithful path.) Residual presentation gap = barrel-spin server-state only. |
| 452 | 0.6 | `vehicle-spiderbot` | `vehicle-spiderbot.weapon.rockets` | presentation:partial | ARTILLERY clearance-height traces use a headless WorldOnly trace; the h1/h2 (0.75 of the up/diagonal world clearance) are reproduced but the resulting arc was not in-game verified.; FIXED-SINCE-BASELINE: the launch effect EFFECT_SPIDERBOT_ROCKETLAUNCH is now emitted server-side via EffectEmitter.Emit (Spiderbot.cs:629; effect registered src/XonoticGodot.Common/Gameplay/Effects/EffectsList.cs:157), matching QC vehicles_projectile Send_Effect. Residual: the rocket-in-flight CSQCProjectile visual (PROJECTILE_SPIDERROCKET) stays client/net deferred to the shared projectile catalog. |
| 453 | 0.5 | `mutator-waypoints` | `waypoints.itemstime.respawn_countdown` | audio:partial | On the first countdown tick spawns WP_Weapon (weapon pickups) / WP_Item (else) attached to the item, UpdateBuildFinished drives the build-progress bar to the scheduled respawn, and every tick Pings the waypoint (radar ring) + plays ITEMRESPAWNCOUNTDOWN. SpectatorOnly items get SPRITERULE_SPECTATOR (the sv_itemstime 1/2 gate).; audio: partial — QC soundto(MSG_ONE) plays ITEMRESPAWNCOUNTDOWN per visible client; the port's Common layer has no per-client soundto seam, so it emits a single positional cue on CH_TRIGGER (ATTEN_NORM). SpectatorOnly items over-broadcast slightly to nearby live players — the only divergence.; wp_extra-driven per-item icon/color (m_waypointblink etc.) not networked — WP_Item renders generic magenta, WP_Weapon generic black/client-resolved. |
| 454 | 0.48 | `monster-spider` | `monster-spider.attack.bite` | presentation:unknown | QC bite picks a random melee anim (anim_melee '0' vs anim_shoot '3' via setanim). The port NOW reproduces that coin-flip: Spider.Attack sets st.Anim = random > 0.5 ? Shoot : Attack (Spider.cs:134), and the chosen frame is networked AND consumed client-side by DpmFrameDriver (the prior 'client DPM doesn't render it' gap is closed in code — see monster-spider.anim.mr_anim). presentation:unknown only because the bite anim is code-traced, not rendered/observed.; QC NPC bite uses actor.attack_range (= g_monsters_attack_range 120); the player-wielded path forces attack_range 60. Port uses st.AttackRange for the NPC path (faithful); the 60 player path is part of the unported player-wielded SpiderAttack (see monster-spider.attack.player_wielded). |
| 455 | 0.48 | `monster-spider` | `monster-spider.anim.mr_anim` | presentation:unknown | CLIENT DPM PLAYBACK NOW WIRED (Wave 17) — not runtime-verified. The server maps the logical MonsterAnim phase to the spider's DPM frame-group ordinal (Spider.AnimFrame) and stamps it on the networked Entity.Frame each think (DriveAnimFrame, called in RunThink/Move/MarkPain/MarkDead), AND the client now consumes it: monsters route NetEntityKind.Generic -> OnEntityUpdate(frameDriven:true) -> ClientWorld._frameDriven; a DPM resolved via EntityModelFactory then gets DpmFrameDriver.TryAttach (ClientWorld.cs:1707), and DpmFrameDriver._Process each frame reads Entity.Frame, clamps to the framegroup count, and plays the matching baked clip. DpmBuilder bakes one AnimationPlayer clip per .framegroups range in group order and records FrameGroupClipsMeta. The prior 'client never consumes Entity.Frame / only the MD3 path honours it' gap is CLOSED in code. Remaining: presentation:unknown (not faithful) because the on-screen animation is code-traced, not behaviorally rendered/observed in a live match. |
| 456 | 0.48 | `monster-spider` | `monster-spider.pain.mr_pain` | presentation:unknown | Pain twitch is fully wired: MarkPain sets st.Anim=Pain + st.AnimVariant random 0/1 (-> AnimFrame Pain 7/8), the pain-window matches the QC anim length (0.44s), and the chosen frame is now BOTH networked (Entity.Frame) AND consumed client-side by DpmFrameDriver (see monster-spider.anim.mr_anim — the prior 'client DPM doesn't render it' gap is closed in code). presentation:unknown only because the on-screen pain anim is code-traced, not rendered/observed. |
| 457 | 0.48 | `monster-spider` | `monster-spider.death.mr_death` | presentation:unknown | Death anim fully wired: the random die1/die2 pick is rolled server-side (RollDeathVariant -> DeathLanded), networked on Entity.Frame (die2 ? 2 : 1), and now CONSUMED client-side by DpmFrameDriver (the prior 'client DPM does not render it' gap is closed in code — see monster-spider.anim.mr_anim). presentation:unknown only because the death pose is code-traced, not rendered/observed. |
| 458 | 0.48 | `sv-impulse` | `sv-impulse.action.reload` | audio:unknown | weaponLocked is modeled only for the FREEZE half (FrozenStat + STATUSEFFECT_Frozen). QC weaponLocked also covers player_blocked and the LockWeapon mutator hook — both still unmodeled (low impact; not reachable in stock modes the same way). The game-start/game-stopped/timeout halves are gated upstream by the dispatcher. |
| 459 | 0.45 | `mapobject-func` | `mapobject-func.bobbing` | audio:partial | the spawn-time soundto(MSG_INIT) looping ambient is not resent per-player; played through the facade where present. |
| 460 | 0.45 | `mapobject-func` | `mapobject-func.pendulum` | audio:partial | ambient .noise not resent per-player (same reduction as bobbing). |
| 461 | 0.45 | `mapobject-func` | `mapobject-func.fourier` | audio:partial | looping ambient .noise resent per-player (soundto MSG_INIT) reduced to a single facade Play. |
| 462 | 0.45 | `mapobject-func` | `mapobject-func.vectormamamam` | audio:partial | looping ambient .noise per-player resend reduced to facade Play in setactive. |
| 463 | 0.45 | `mapobject-movers-platforms` | `mapobject-movers-platforms.plat.q3compat_defaults` | audio:partial | CPMA sound_start/sound_end + the pt1_strt/pt1_end default-file probe are not ported (require GetField_fullspawndata / FindFileInMapPack, unavailable headless). The medplat default still applies, so a Q3 plat is audible and correct — only the CPMA per-mapper sound OVERRIDE keys are skipped. Sole residual gap on this row; audio marked partial for it. |
| 464 | 0.3 | `monster-golem` | `monster-golem.audio.voice_cues` | audio:partial | Full data-driven .sounds-file parse + skin-keyed Monster_Sounds_Update is still hardcoded per-descriptor (SoundCues set + SoundCueCount switch mirror golem.dpm_0.sounds by hand) rather than parsed from the model's .sounds at load; behaviorally faithful for golem but the general GlobalSound/Monster_Sounds_Update plumbing is a named no-op.; QC spamsound(SND_BODYIMPACT1) pain placeholder (when a monster has no pain sample) not played — n/a for golem (golem has pain samples). |
| 465 | 0.24 | `turret-ewheel` | `turret-ewheel.damage.lifecycle` | audio:unknown | Death blast is applied unconditionally in Die (Base turret_die has the RadiusDamage variant commented out) — minor over-fidelity, scaled by leftover ammo; not ewheel-specific (framework-level). |