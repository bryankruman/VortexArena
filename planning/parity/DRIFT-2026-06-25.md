# Parity drift — 2026-06-25

**0 regressions · 2 new gaps · 14 registry-coverage gaps · 41 fixes · 2 reclassified** across 10 units (ctf, deathmatch, domination, freezetag, keyhunt, nexball, onslaught, race). No parity got worse — the only alarm-worthy item is one confirmed round-timing gap in Domination.

## Regressions

No drift detected. (No parity got worse since the baseline.)

## New gaps

Newly-introduced or newly-confirmed parity gaps. The Domination item is a real timing defect surfaced by re-tracing the host wiring; the FreezeTag item is a newly-added registry row that is itself faithful (it closes a previously-unmapped Base behavior).

| unit | feature | dimension | was -> now | reason |
| --- | --- | --- | --- | --- |
| domination | domination.roundbased.win | timing | unknown -> partial | Re-traced the round-timing wiring: `GameWorld`'s Domination `EnableRounds` call (GameWorld.cs:2264) omits the roundTimeLimit/countdown/endDelay args, so `RoundHandler.Init` is never called and the generic default `RoundTimeLimit=180s` stands instead of Base's `autocvar_g_domination_round_timelimit=120` (`round_handler_Init`). Port round-based dom rounds run 180s vs Base 120s. Baseline had assumed the host supplied 120 (timing:unknown); it does not. |
| freezetag | freezetag.spawn.late | logic | missing -> faithful | Newly-added registry row for the mid-round spawn-as-frozen behavior (`MUTATOR_HOOKFUNCTION(ft, PlayerSpawn)`). It is implemented and live in the port (`OnPlayerSpawn` on `MutatorHooks.PlayerSpawn`) but the baseline registry had no row for it; recorded here as a new coverage row (faithful, live). |

## Registry coverage gaps

Base features with no dedicated registry row (the registry is missing coverage for these). These are not regressions — they are uncovered Base behavior surfaced during the re-audit. Most are unported; a few are functionally subsumed by existing port structures but lack a dedicated row.

| unit | Base feature | status / note |
| --- | --- | --- |
| freezetag | `MUTATOR_HOOKFUNCTION(ft, PlayerSpawn)` — mid-round spawn-as-frozen + `CENTER_FREEZETAG_SPAWN_LATE` | ADDED this pass as `freezetag.spawn.late` (was previously unmapped); now has a row. |
| freezetag | `MUTATOR_HOOKFUNCTION(ft, ItemTouch)` / `(ft, BuffTouch)` — a frozen player cannot collect items or buffs | No dedicated registry row; port-side frozen item/buff block not confirmed. |
| freezetag | `MUTATOR_HOOKFUNCTION(ft, PlayerAnim)` — frozen players get `ANIMSTATE_FROZEN` | No row; no port `ANIMSTATE_FROZEN` found. |
| freezetag | `MUTATOR_HOOKFUNCTION(ft, SpectateCopy)` — copies `STAT(FROZEN)`/`REVIVE_PROGRESS` to a spectator's view | No row. |
| freezetag | `MUTATOR_HOOKFUNCTION(ft, AccuracyTargetValid)` — hits on already-frozen players don't count toward accuracy | No row. |
| freezetag | `MUTATOR_HOOKFUNCTION(ft, PlayerRegen)` — frozen players don't health/armor regen | No dedicated row; implied by Frozen status effect. |
| freezetag | `MUTATOR_HOOKFUNCTION(ft, GiveFragsForKill)` — kills give 0 frags in Freeze Tag | No dedicated row; partially covered by freeze score matrix. |
| freezetag | `MUTATOR_HOOKFUNCTION(ft, ClientKill)` — frozen players can't `/kill` out | No row. |
| freezetag | `freezetag_LastPlayerForTeam_Notify` — `CENTER_ALONE` to the last unfrozen player on a team | No row. |
| keyhunt | `LogDeath_AppendItemCodes` hook (sv_keyhunt.qc:1340) — a key carrier's death-log entry appends item code `K` | Cosmetic death-log detail; no registry row. Not ported (no death-log item-code pipeline in the port). |
| keyhunt | `MatchEnd -> kh_finalize` (sv_keyhunt.qc:1302) and `reset_map_global -> kh_WaitForPlayers` (sv_keyhunt.qc:1354) — match-end cleanup + map-reset re-enter-waitforplayers lifecycle hooks | Functionally subsumed by the port's `Deactivate` + Phase state machine but no dedicated registry row. |
| keyhunt | `PreferPlayerScore_Clear` (sv_keyhunt.qc:1359, returns true) — scoreboard-ordering quirk telling the engine not to prefer player score for KH team ranking | Trivial; unmapped. |
| nexball | `MUTATOR_HOOKFUNCTION(nb, SendWaypoint)` (sv_nexball.qc:930) — clears the waypoint bit `0x80` for nexball | No registry row (client/waypoint concern; currently subsumed under "waypoint sprites are client-deferred" notes). |
| nexball | `nb_Goal_Customize` (sv_nexball.qc:614) — the goal waypoint sprite same-team customize/hide predicate | No registry row (client waypoint concern). |
| nexball | Ball jumppad/bounce physics tuning cvars — `g_nexball_basketball_jumppad`/`g_nexball_football_jumppad` (pushable), `..._bouncefactor`/`..._bouncestop` in `spawnfunc(nexball_basketball/football)` | No dedicated registry row; `BallConfig` carries `BounceFactor`/`BounceStop` but the cvar reads and pushable flag are not mapped as a feature. |

## Fixes

Parity that improved since the baseline. Grouped by unit. Core gameplay categories (gametype/scoring/damage) first.

### ctf

| feature | dimension | was -> now | reason |
| --- | --- | --- | --- |
| ctf.flag.throw | audio | missing -> faithful | `ThrowDropCommon`'s `DROP_THROW` branch now falls through to the `Handle_Drop` body and fires `FlagAnnounceSound(DROPPED)` = the global `snd_flag_dropped` voice (`CTF_DROPPED_<team>`, `ATTEN_NONE`), matching Base sv_ctf.qc:388. Sound is registered (`SoundsList` `CTF_DROPPED_*`) and the path is live via `+use -> HandleUseKey -> ThrowFlag`. |
| ctf.flag.pass | audio | missing -> faithful | `DROP_PASS` now plays `snd_flag_touch` (`CTF_TOUCH`, `ATTEN_NORM`) on the passer in `ThrowDropCommon` (Base sv_ctf.qc:545) and `ctf_Handle_Retrieve`/`RetrieveFlag` plays `snd_flag_pass` (`CTF_PASS`) on the receiver (Base sv_ctf.qc:456). Both sounds registered in `SoundsList` and on the live `+use` pass path. |
| ctf.flag.capture | presentation | partial -> partial | The in-match fastest-cap broadcast (`CTF_CAPTURE_TIME`/`_BROKEN`/`_UNBROKEN`, in-process `ctf_captimerecord` + holder name) is now ported in `CaptureRecord` and fired from `Capture` and `CaptureDropped` (one-flag uses `INFO_CTF_CAPTURE_NEUTRAL`). Dimension stays partial because only the PERSISTENT cross-session `ServerProgsDB` record + `g_ctf_leaderboard` remain unported (needs a DB) — the worst-gap is narrowed, not eliminated. |
| ctf.stalemate | presentation | missing -> faithful | `PackCtfStatus` (GametypeStatusBlock.cs:208) now ORs the `CTF_STALEMATE` bit (8192) from `ctf.Stalemate` (was reserved-but-never-set); `CheckStalemate` center-prints `CTF_STALEMATE_CARRIER`/`_OTHER` once per stalemate with the live roster; `CollectWaypoints` emits the enemy-FC reveal sprites during a stalemate (gated by `ctf_Stalemate_Customize`). The three presentation halves that were the worst-gap are all now wired. |
| ctf.stalemate | liveness | partial -> live | The stalemate state is now fully consumed: status bit fed to the HUD, center-prints fired (`Tick` passes `Clients.Players` as the roster), and the waypoint reveal emitted. No remaining dead presentation half. |
| ctf.hud.modicons | values | faithful -> faithful | Neutral status-flip in the constant table: the `CTF_FLAG_NEUTRAL`/`CTF_SHIELDED`/`CTF_STALEMATE` constant row was match:false (STALEMATE bit not fed). `PackCtfStatus:208` now feeds the stalemate bit, so the constant is match:true and the modicons stalemate-overlay gap is closed. (Dimension status unchanged but the recorded constant/gap defect is resolved.) |
| ctf.hud.waypoints | presentation | partial -> partial | `FLAG_WAYPOINT_OFFSET` (0 0 64) is now applied (was n/a/match:false in the baseline constant), the carrier own-team health-bar sprite is emitted, and the enemy-FC stalemate/self-carry reveal sprites are now wired in `CollectWaypoints`. Stays partial because the dedicated flag-return sprite and on-pickup enemy-FC spotting reveal remain missing. |

### deathmatch

| feature | dimension | was -> now | reason |
| --- | --- | --- | --- |
| deathmatch.respawn.timing | presentation | missing -> faithful | `GameWorld.ShowRespawnCountdown` (GameWorld.cs:2986-3006) is now a line-faithful port of Base `ShowRespawnCountdown` (client.qc:2043-2060), called from `DeadPlayerThink` (line 2968) on every dead-player tick. It reads `Player.RespawnCountdown` (which `RespawnTiming.Calculate` seeds to 10 for waits>=5s) and presents the 10-9-8 countdown. The baseline's failing "no reader/announcer" verification is resolved. |
| deathmatch.respawn.timing | audio | missing -> faithful | The countdown plays the `NUM_RESPAWN_n` announcer via `AnnceIfEnabledTo -> NotificationSystem.Send(OneOnly)`. `NUM_RESPAWN_{n}` is registered in `NotificationsList` with `enabled:false`, matching Base's opt-in (disabled-by-default) announcer, so it is silent at defaults exactly as in Base — faithful, not a gap. |

### freezetag

| feature | dimension | was -> now | reason |
| --- | --- | --- | --- |
| freezetag.damage.softkill_void | logic | partial -> partial | Nade self-revive went from a `NadesMutator` suppression stub to a real revive in `FreezeTag.OnDamageCalculate` (FreezeTag.cs:485-499): Unfreeze + restore `g_freezetag_revive_nade_health` (40) + suppress hit. Logic stays "partial" overall only because the void/lava NEEDKILL relocate (`g_frozen_damage_trigger 0`, default-dormant) is still absent; the nade branch itself is now faithful. |
| freezetag.damage.softkill_void | values | partial -> faithful | `g_freezetag_revive_nade` / `_nade_health` (1 / 40) are now honored by the real nade self-revive (restores 40 HP); previously the cvar only gated suppression with no health restore. |
| freezetag.damage.softkill_void | presentation | partial -> faithful | Nade self-revive now sends `CENTER_FREEZETAG_REVIVE_SELF` (and `INFO_FREEZETAG_REVIVED_NADE`) and the visible unfreeze actually occurs; baseline had no unfreeze and no notifications. |
| freezetag.damage.softkill_void | audio | partial -> faithful | The notification producers for the nade/fall revive paths are all wired now; the dependent events the baseline flagged as absent (nade unfreeze) now exist. |
| freezetag.presentation.notifications | audio | partial -> faithful | `SPAWN_LATE` and `REVIVED_NADE` now have live producers (`OnPlayerSpawn` FreezeTag.cs:321 and the nade self-revive branch FreezeTag.cs:496); baseline recorded both as "declared but never sent", the only remaining notification gap. |

### keyhunt

| feature | dimension | was -> now | reason |
| --- | --- | --- | --- |
| keyhunt.waypoints.sprites | logic | missing -> faithful | `CollectWaypoints` (KeyHunt.cs:1324-1376) now rebuilds `WP_KeyDropped` on loose keys + team-colored `WP_KeyCarrier` markers on carriers, flipping to `WP_KeyCarrierFinish` on the all-owned transition. Jun-26 edit (commit c59134e). |
| keyhunt.waypoints.sprites | values | missing -> faithful | `KH_KEY_WP_ZSHIFT=20` (`KeyWpZShift`) and the carrier head offset (0,0,64) now match Base; all 7 def names registered in `WaypointRegistry` (Waypoints.cs:128-134). |
| keyhunt.waypoints.sprites | presentation | missing -> faithful | Dropped + carrier + Finish sprites render with team radar tint and faithful per-viewer visibility predicates (`DroppedKeyWaypointVisibleFor` / `CarrierWaypointVisibleFor` mirror the QC `visible_for_player` gates). |
| keyhunt.waypoints.sprites | liveness | na -> live | Consumed on the live path at ServerNet.cs:1022 (`_world.GameType?.CollectWaypoints` in `SendWaypoints`, the per-frame waypoint network feed). |
| keyhunt.controller.round_loop | presentation | partial -> faithful | The `delay_tracking` / `TrackingEnabled` flag was held at partial in the baseline ONLY because it gated not-yet-implemented sprites. Those sprites now exist and `DroppedKeyWaypointVisibleFor`/`CarrierWaypointVisibleFor` return `TrackingEnabled`, so the tracking-device reveal is now a real player-visible behavior. |

### nexball

| feature | dimension | was -> now | reason |
| --- | --- | --- | --- |
| nexball.goal.touch_scoring | audio | partial -> faithful | The goal SCORE sound is now the QC-faithful `ctf/respawn.wav`. `GoalTouch` plays `Sounds.ByName("NB_GOAL")` for a score (and `TYPEHIT` for fault/out), and SoundsList.cs:113 maps `NB_GOAL -> "ctf/respawn"`. The baseline recorded it as kh/capture (`KH_CAPTURE`). |
| nexball.ball.spawn | logic | faithful -> faithful | The baseline recorded a NEW liveness/logic gap: `WireObjectiveSpawns` spawned a (carryable) basketball for BOTH `nexball_basketball` and `nexball_football`. GameWorld.cs:2657-2670 now switches on `e.ClassName -> SpawnBall(basketball:true)` for `nexball_basketball` and `SpawnBall(basketball:false)` for `nexball_football`, so a football map now spawns a kick-only football. The logic dimension was already "faithful" in the baseline but carried this concrete gap, which is now removed. |
| nexball.ball.dropball | liveness | partial -> live | Drop-on-disconnect and drop-on-observe are now wired. `Nexball.OnPlayerRemoved` overrides `DropBall(player)` and the server dispatches `OnPlayerRemoved` from both `ClientManager` paths: line 517 (`PutObserverInServer` / `MakePlayerObserver`) and line 770 (`ClientDisconnect`), matching QC `nb_DropBall`. The baseline recorded these hooks as NOT wired. |
| nexball.sound.cues | audio | partial -> faithful | The SCORE goal sound is now `ctf/respawn.wav` (`NB_GOAL`) and the `g_nexball_sound_bounce` gate is now honored (`SpawnBall` sets `_bounceSoundEnabled` from the cvar and both touch handlers respect it before playing `NB_BOUNCE`). Baseline recorded the wrong score sample and an ungated bounce. SHOOT/STEAL remain dead pending the missing weapon (liveness stays partial). |

### onslaught

| feature | dimension | was -> now | reason |
| --- | --- | --- | --- |
| onslaught.cp.touch_build | values | partial -> faithful | OnslaughtControlPoint.cs:54 `DefCpHealth` was corrected 200f->1000f. `SpawnIcon` uses `CpHealth` (=`Cvar(g_onslaught_cp_health, DefCpHealth)`) and now falls back to the Base 1000 (gametypes-server.cfg:578) on a missing-cfg boot instead of 200. The baseline's worst-gap (icons built to 1/5 health) is closed. |
| onslaught.gen.damage_destroy | values | partial -> faithful | OnslaughtControlPoint.cs:58 `DefGenHealth` was corrected 2000f->2500f. `SpawnGenerator` uses `GenHealth` (=`Cvar(g_onslaught_gen_health, DefGenHealth)`) and writes the correct 2500 onto both the entity and the graph node, matching Base (gametypes-server.cfg:576). The baseline's worst-gap (generators at 4/5 health, overwriting the node's 2500) is closed. |
| onslaught.gen.unshielded_alarm | logic | missing -> faithful | `ons_GeneratorThink` is now ported as OnslaughtControlPoint.cs:153 `GeneratorThink`: gate on `!RoundStarted`/shielded/`Now<GtAlarmWait`, `GtAlarmWait=Now+5`, team->`ONS_NOTSHIELDED_TEAM` center+siren, enemy->`ONS_NOTSHIELDED_<team>` center, skips bots via `IsBot` — faithful to sv_onslaught.qc:1021-1040. |
| onslaught.gen.unshielded_alarm | values | missing -> faithful | `GenThinkRate=1s` matches `GEN_THINKRATE`; `GtAlarmWait=Now+5` matches `this.wait=time+5`. |
| onslaught.gen.unshielded_alarm | timing | missing -> faithful | Generator Think re-arms every 1s (`NextThink=Now+GenThinkRate`) and the alarm repeats every 5s, matching Base think cadence. |
| onslaught.gen.unshielded_alarm | audio | missing -> faithful | `GeneratorThink` now emits `ONS_GENERATOR_ALARM` siren (`PlayGlobal` once) + `ONS_NOTSHIELDED_TEAM`/`_<team>` center prints — the registered-but-never-emitted cues are now live. |
| onslaught.gen.unshielded_alarm | presentation | missing -> partial | Server-authoritative center prints + siren now emit; only the per-listener spatial siren routing and any extra client HUD remain (headless `PlayGlobal` approximation). |
| onslaught.gen.unshielded_alarm | liveness | na -> live | `SpawnGenerator:138-140` wires `e.Think=GeneratorThink`, `NextThink=Now+GenThinkRate`; driven by `SimulationLoop.RunThink` on the live entity tick (same mechanism as the verified-live `IconBuildThink`). |
| onslaught.audio.cues | audio | partial -> faithful | The last un-emitted cue (`ons_GeneratorThink` un-shielded alarm) is now emitted live via `GeneratorThink`; all core onslaught cues now fire on the live path. |

### race

| feature | dimension | was -> now | reason |
| --- | --- | --- | --- |
| race.speed_awards | logic | missing -> faithful | `race_SpeedAwardFrame` is now fully ported: `Race.SpeedAwardFrame` tracks round-best planar speed + holder/uid and persists the all-time best into `RaceRecords` (`WriteSpeedAwardBest`). Was port_refs:[] (missing) in baseline. |
| race.speed_awards | values | missing -> faithful | Planar speed = `sqrt(vx^2+vy^2)` (z-stripped, matches Base `vlen(vec2)`); wire rounding `floor(s+0.5)` matches Base `floor(speed+0.5)`. |
| race.speed_awards | timing | missing -> faithful | Driven once per server frame at GameWorld.cs:2849 (`race.SpeedAwardFrame(Clients.Players)`), matching Base's per-frame `GetPressedKeys` hook. |
| race.speed_awards | presentation | missing -> faithful | Networked via `ScoreboardBlock.SpeedAward` (ServerNet.cs:2237) and rendered by `ScoreboardPanel.DrawSpeedAward`; `RaceTimerPanel` speed-at-CP readout fed at NetGame.cs:3540-3543 (the baseline noted these fields existed but were never fed). |
| race.speed_awards | liveness | na -> live | Now has a live caller (GameWorld.cs:2849) and a networked scoreboard path that works for a pure remote client. |
| race.hud.mod_icon_rankings | logic | partial -> faithful | The `HUD_Mod_Race` floating mod-icon is now ported (`ModIconsPanel` `ModIconsMode.Race` + `DrawRace`, fed via NetGame.cs:4075-4104 with PB/server-record/medal-flash status). Baseline marked the mod-icon entirely un-ported. |
| race.hud.mod_icon_rankings | values | partial -> faithful | Rankings display cap now reads `g_cts_send_rankings_cnt` (Base default 15) clamped to `RANKINGS_CNT`(99) — matches Base `min(RANKINGS_CNT, autocvar_g_cts_send_rankings_cnt)`; was a hardcoded 15 (match:false) in baseline. Medal fade is 5s (`ModIconsPanel _raceStatusTime = time+5`), matching Base. |
| race.hud.mod_icon_rankings | presentation | partial -> faithful | `DrawRace` renders the PB + server-best rows and the new-record/new-rank/fail medal flash with rank ordinal; the prior "no Race case in ModIconsPanel" gap is closed. |

## Reclassified

Neutral re-categorizations — the dimension status did not get better or worse, but the underlying classification was corrected.

| unit | feature | dimension | was -> now | reason |
| --- | --- | --- | --- | --- |
| domination | domination.roundbased.win | values | partial -> partial | The `round_timelimit_s` constant moved from match:unknown to match:false (port 180 default vs Base 120) and `warmup_s` from match:unknown to match:true (both 5 by default coincidence). Dimension status stays partial but the underlying gap is now confirmed concrete rather than "unverified host value". |
| race | race.hud.mod_icon_rankings | timing | na -> faithful | Medal flash now has a real 5s fade window (`ModIconsPanel _raceStatusTime = time + 5`) matching Base `RACE_NET_SERVER_STATUS`; previously na because no mod-icon code existed. |
