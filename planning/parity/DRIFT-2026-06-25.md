# Parity drift — 2026-06-25

**0 regressions · 1 new gap · 5 registry-coverage gaps · 78 fixes · 2 reclassified** across 10 units (cl-centerprint, cl-hud, cl-scoreboard, cl-shownames, cl-teamradar, cl-view, cl-viewmodel, physics-movetypes, physics-player).

## Regressions

No regressions detected. No parity dimension moved to a worse status in this update.

## New gaps

| unit | feature | dimension | was -> now | reason |
| --- | --- | --- | --- | --- |
| cl-hud | cl-hud.panel.centerprint | values | faithful -> partial | Port registers `hud_panel_centerprint_fade_in 0.15` (the QC code default), but Base's shipped `_hud_common.cfg:133` `setas` it to 0, so port messages fade in over 0.15s where the stock HUD pops instantly. Minor, user-overridable. |

## Registry coverage gaps

Base features with no registry row (registry is missing coverage for them).

| unit | Base feature | status / note |
| --- | --- | --- |
| cl-centerprint | `hud_panel_centerprint_dynamichud` (centerprint.qh:18 default true; centerprint.qc:244 HUD_Scale_Enable/Disable gate) | The dynamic-HUD scaling toggle for the panel is not rowed and appears unimplemented in the port. |
| cl-centerprint | Slide-below-scoreboard / below-radar panel repositioning (HUD_CenterPrint:221-242) | When the scoreboard or clickable radar is shown the panel lerps below it (sqrt(scoreboard_fade_alpha) ease, clamps panel height); not rowed and not reproduced in the port (noted only as a minor omission in layout.draw notes). |
| cl-teamradar | `cl-teamradar.cvar.dynamichud` — `hud_panel_radar_dynamichud` (HUD_Scale_Enable/Disable dynamic-HUD scaling) + HUD_Radar_Export (writing the radar's 8 aesthetic cvars into a saved hud skin file) | Neither was tracked by the baseline; added as a new row. |
| cl-viewmodel | `cl-viewmodel.model.wr_viewmodel_override` — Base viewmodel_draw calls wep.wr_viewmodel(wep,this) for a per-weapon view-model name override | Only Tuba::wr_viewmodel (tuba.qc:423) uses it (swaps the v_ model by played note). No baseline row; added as cl-viewmodel.model.wr_viewmodel_override (missing/na in the port). |
| physics-player | `physics-player.punch_decay` — PM_check_punch (common/physics/player.qc:625) view-punch decay (punchangle 10 deg/s, punchvector 30 u/s) | Ported as CheckPunch (PlayerPhysics.cs:1823) and live, but had no registry row. ADDED. (Two further Base seams noted but intentionally NOT added, out of scope for the player-physics unit: `sys_phys_pregame_hold` (sv_physics.qc:48, warmup movement freeze — server-physics seam, covered by gametype warmup handling); `viewloc_PlayerPhysics` (physics.qc:45, 1D-rail side-scroller view-lock — separate viewloc concern, inert on stock 3D maps).) |

## Fixes

Parity that improved since the baseline. Core gameplay (physics) sorted first.

### Core gameplay — physics

| unit | feature | dimension | was -> now | reason |
| --- | --- | --- | --- | --- |
| physics-movetypes | physics-movetypes.toss.bounce | values | partial -> faithful | Three per-entity bounce value gaps closed (Wave-6c/6e): MonsterAI.SpawnProjectile now assigns proj.BounceFactor/BounceStop (spider web 0.3/0.05 via Spider.cs:109, golem zap 0.5/0.075 via Golem.cs:153, MonsterAI.cs:1332-1333); BallEntity grew the fields (nexball 0.6/0.075, BallEntity.cs:69-74,130-131) and wires them at spawn (:200-201); Racer grew them (0.25/0, Racer.cs:53-54) and wires them at spawn (:194-195). All five live producers (mortar, electro-secondary, raptor, spider/golem, nexball, racer) now match Base bounce constants. |
| physics-movetypes | physics-movetypes.toss.bounce | liveness | live -> live | Liveness unchanged (still live), but verification flipped fail->pass: every live bounce producer now wires the correct .bouncefactor/.bouncestop, zero remaining match:false constants, empty gaps list. |
| physics-player | physics-player.specialcommand | logic | missing -> faithful | The xwxwxsxsxaxdxaxdx1x button cheat-sequence is now fully ported in PlayerPhysics.CheckSpecialCommand/SpecialCommand (PlayerPhysics.cs:530-577); button-mask decode faithful to player.qh:191, per-player pointer Entity.SpecialCommandPos. |
| physics-player | physics-player.specialcommand | values | missing -> faithful | Sequence literal xwxwxsxsxaxdxaxdx1x (SpecialCommandSequence) matches Base player.qc:580 exactly. |
| physics-player | physics-player.specialcommand | liveness | na -> live | Called from Move (PlayerPhysics.cs:163) on the non-predicted server-authority tick; give-all seam wired live at GameWorld.cs:1003 (SpecialCommandGiveAll = Cheats.GiveAll), running the sv_cheats/maycheat gate like the QC SpecialCommand body. |
| physics-player | physics-player.jetpack | values | partial -> faithful | g_jetpack_* tunables now read live as cvars via JetpackCvar (PlayerPhysics.cs:874-880) and the CheckPlayerJump fuel gate (:982); baked constants are now only fallbacks. JetpackMaxSpeedSide corrected 1500->1200 (balance-xonotic.cfg:248), maxspd_mod factor (player.qc:742) now threaded through as maxspeedMod instead of hardcoded max(1,1). |

### Client — view / viewmodel

| unit | feature | dimension | was -> now | reason |
| --- | --- | --- | --- | --- |
| cl-view | cl-view.zoom.velocity_zoom | logic | missing -> faithful | FirstPersonView.ComputeVelocityZoom (434) ports GetCurrentFov's velocityzoom block (curspeed type 1/2/3 fwd-dot, low-pass _avgSpeed, float2range11 + exp), folded into rendered fov (UpdateCamera 422-423). |
| cl-view | cl-view.zoom.velocity_zoom | values | missing -> faithful | All cvars read live with faithful fallbacks (enabled 0, factor 0, type 3, speed 1000, time 0.2). |
| cl-view | cl-view.zoom.velocity_zoom | timing | missing -> faithful | Frametime-scaled low-pass averaging over cl_velocityzoom_time ported. |
| cl-view | cl-view.zoom.velocity_zoom | presentation | missing -> faithful | FOV now widens/narrows with player speed; the cl_velocityzoom_* menu controls take effect. |
| cl-view | cl-view.zoom.velocity_zoom | liveness | na -> live | Wired on the live net path via UpdateView (NetGame:4460). |
| cl-view | cl-view.zoom.zoom_scroll | logic | missing -> faithful | FirstPersonView.NotifyZoomScroll (197) ports ZoomScroll/View_InputEvent; UpdateZoom (264-289) ports the GetCurrentFov zoomscroll integration. |
| cl-view | cl-view.zoom.zoom_scroll | values | missing -> faithful | cl_zoomscroll 1, _scale 0.2, _speed 16 read live and match Base. |
| cl-view | cl-view.zoom.zoom_scroll | timing | missing -> faithful | Frametime-independent averaging toward the wheel target ported. |
| cl-view | cl-view.zoom.zoom_scroll | presentation | missing -> faithful | Scrolling while +zoom held now changes the zoom factor. |
| cl-view | cl-view.zoom.zoom_scroll | liveness | na -> live | Mousewheel events wired to NotifyZoomScroll gated on ZoomHeld (NetGame:3873-3876). |
| cl-view | cl-view.zoom.zoomscript_buttonstatus | logic | partial -> faithful | Zoomscript auto-zoom (autocvar_fov <= 59.5 -> zoomscript_caught) ported in UpdateZoom (233-239); folds into the zoom like +button9, exposes ZoomScriptCaught. |
| cl-view | cl-view.zoom.zoomscript_buttonstatus | values | partial -> faithful | The 59.5 fov threshold and unpress-zoom-on-death now present; only weapon-switch unpress glue cvars remain unmodelled (niche). |
| cl-view | cl-view.zoom.zoomscript_buttonstatus | liveness | partial -> live | zoomscript_caught computed each frame in the live UpdateZoom, feeds both the zoom integration and the reticle (NetGame:2672). |
| cl-view | cl-view.bob.view_bob | logic | missing -> faithful | FirstPersonView.ApplyBobbing (532) faithfully transcribes CSQCPlayer_ApplyBobbing (cl_player.qc:321) — vertical bob, horizontal bob2 with bob2_smooth, fall-bob. Baseline wrongly claimed Base only TODO-stubs view-bob. |
| cl-view | cl-view.bob.view_bob | values | missing -> faithful | All bob cvars read live with Base defaults (cl_bob 0, bobcycle 0.5, bob_limit 7, bobup 0.5, bob2 0, bob2cycle 1, bob2smooth 0.05, bobfall 0.05, bobfallcycle 3, bobfallminspeed 200, bob_velocity_limit 400). |
| cl-view | cl-view.bob.view_bob | timing | missing -> faithful | Cycle clock advances from frametime; fall-bob uses frametime decay matching Base. |
| cl-view | cl-view.bob.view_bob | presentation | missing -> faithful | View now bobs (when opted in); OnGround/JumpHeld fed via ViewState (NetGame:4457-4458). |
| cl-view | cl-view.bob.view_bob | liveness | na -> live | Wired on the live net path via UpdateView (NetGame:4460). |
| cl-view | cl-view.roll.view_roll | logic | missing -> faithful | FirstPersonView.CalcRoll (509) ports CSQCPlayer_CalcRoll (cl_player.qc:432); result added to view_angles.z in UpdateCamera (377). |
| cl-view | cl-view.roll.view_roll | values | missing -> faithful | cl_rollangle 0 read live, cl_rollspeed 200 DP fallback (QC autocvar default 0) — matches Base. |
| cl-view | cl-view.roll.view_roll | timing | missing -> faithful | Per-frame velocity-driven roll, no timer dependence — matches Base. |
| cl-view | cl-view.roll.view_roll | presentation | missing -> faithful | Camera now banks when strafing (when opted in). |
| cl-view | cl-view.roll.view_roll | liveness | na -> live | Wired on the live net path via UpdateView. |
| cl-view | cl-view.tilt.death_idle | logic | missing -> faithful | ApplyDeathTilt (470) ports CSQCPlayer_ApplyDeathTilt (incl. the cl_eventchase_death 2 incompatibility guard); ApplyIdleScaling (493) ports CSQCPlayer_ApplyIdleScaling. |
| cl-view | cl-view.tilt.death_idle | values | missing -> faithful | v_deathtilt 0, deathtiltangle 80, idlescale 0, i*_cycle 1/2/0.5, i*_level 0.3/0.3/0.1 all present with Base values. |
| cl-view | cl-view.tilt.death_idle | timing | missing -> faithful | Death edge tracked via _deathTime/_wasDead; (time-death_time)*8 ramp and sin-wave clocks from _viewTime match Base. |
| cl-view | cl-view.tilt.death_idle | presentation | missing -> faithful | Death roll and idle sin-wave now render (when opted in). |
| cl-view | cl-view.tilt.death_idle | liveness | na -> live | Wired on the live net path via UpdateView (ApplyIdleScaling runs after the chase split, FirstPersonView:398). |
| cl-view | cl-view.reticle.zoom_reticle | logic | faithful -> faithful (gap closed) | The previously-noted zoomscript_caught gap is closed: ReticleOverlay takes a zoomScriptCaught arg (NetGame:2672) and uses the full-alpha (1.0) branch when caught (line 107/118), matching DrawReticle:704. Lingering gap entry removed. |
| cl-viewmodel | cl-viewmodel.alpha.viewmodel_alpha | liveness | partial -> live | ViewModel.RefreshCvars now runs every frame from _Process, reads cl_viewmodel_alpha/_alpha_min from the shared store, applies via ApplyModelAlpha; previously the opacity path was inert. |
| cl-viewmodel | cl-viewmodel.alpha.viewmodel_alpha | values | missing -> faithful | RefreshCvars reads cl_viewmodel_alpha (1) and _alpha_min (0) live and computes amax/amin/bound exactly per Base viewmodel_draw. |
| cl-viewmodel | cl-viewmodel.alpha.viewmodel_alpha | logic | partial -> faithful | Continuous opacity now applied to the model materials with the Base amax/amin/bound formula, plus dead/holster/chase/intermission hide via Visible. |
| cl-viewmodel | cl-viewmodel.alpha.viewmodel_alpha | presentation | partial -> faithful | The opacity slider now visibly affects the gun (was inert). |
| cl-viewmodel | cl-viewmodel.align.gunalign_gunoffset | liveness | partial -> live | RefreshCvars now reads cl_gunalign and cl_gunoffset live each frame and rebuilds the rest placement on change; previously both were baked Export defaults. |
| cl-viewmodel | cl-viewmodel.align.gunalign_gunoffset | logic | partial -> faithful | cl_gunalign 1/2/3/4 and cl_gunoffset are now driven by cvars; left/right are no longer visually identical (GunAlignOffset uses AuthoredShotSideY so the mirror/center deltas are real). |
| cl-viewmodel | cl-viewmodel.align.gunalign_gunoffset | presentation | partial -> faithful | The gunalign radio buttons and gunoffset now move the gun distinctly; the prior 'menu barely changes the gun' defect is resolved. |
| cl-viewmodel | cl-viewmodel.model.skin_colormap_glow_effects | presentation | missing -> partial | EF_NODEPTHTEST now implemented and live: ApplyMaterialFx sets NoDepthTest=true on every viewmodel surface override (run each frame from _Process->RefreshCvars), so the gun draws on top of the world. colormap/glowmod/skin remain missing. |
| cl-viewmodel | cl-viewmodel.model.skin_colormap_glow_effects | logic | missing -> partial | The per-child effects loop is partially ported (csqcmodel_effects EF_NODEPTHTEST applied); colormap/glowmod/skin assignment still absent. |
| cl-viewmodel | cl-viewmodel.model.skin_colormap_glow_effects | values | missing -> partial | EF_NODEPTHTEST constant now matches; colormap (256+c), glowmod, and m_skin values still not applied. |
| cl-viewmodel | cl-viewmodel.model.skin_colormap_glow_effects | liveness | na -> live | port_refs now non-empty (ViewModel.ApplyMaterialFx) and the NoDepthTest application runs on the live _Process path. |
| cl-viewmodel | cl-viewmodel.casings.brass_eject | audio | missing -> faithful | Casing_Touch bounce sound is now implemented (brass1-3/casings1-3 on vel>50, 0.2s throttle) AND wired live: ShellCasings.SoundHook = ClientWorld.OnSound on CH_SHOTS (ClientWorld.cs:334). |
| cl-viewmodel | cl-viewmodel.casings.brass_eject | values | partial -> faithful | Casing lifetimes are now BulletTime=10 / ShellTime=30 and MaxCasings=100 (match Base 10/30/100; were 1.5/2.0/64); avelocity tumble is now the QC '0 10 0'+100*prandomvec magnitude (was a generic RandRange(-12,12)). |

### Client — centerprint / HUD / scoreboard / shownames / radar

| unit | feature | dimension | was -> now | reason |
| --- | --- | --- | --- | --- |
| cl-centerprint | cl-centerprint.countdown.count_token | logic | partial -> faithful | HudNotifications now models QC durcnt arg-selection via ResolveDurcnt/ResolveDurcntToken (HudNotifications.cs:208-244): token0=duration, token1=^COUNT-count resolved as literal/fN/item_centime. The hardcoded count=flts[0] is gone. |
| cl-centerprint | cl-centerprint.countdown.count_token | values | partial -> faithful | NotificationsList.CenterDurcnt stamps Base durcnt specs onto Notification.Durcnt (COUNTDOWN_ROUNDSTART="1 f2", GAMESTART="1 f1"); ROUNDSTART ^COUNT now reads f2=flts[1]=secondsLeft, not flts[0]=round#. |
| cl-centerprint | cl-centerprint.countdown.count_token | timing | partial -> faithful | Per-step duration is now 1s (ResolveDurcnt token0="1" -> Push duration=1) instead of the old DefaultDuration=3 cadence. |
| cl-centerprint | cl-centerprint.fade.in_out | values | partial -> faithful | hud_panel_centerprint_fade_in default now 0.15 (RegisterDefaults at CenterPrintPanel.cs:283; live read CvarF("fade_in",0.15f) at :304), matching Base centerprint.qh:6. |
| cl-centerprint | cl-centerprint.fade.in_out | timing | partial -> faithful | fade_in ramp now occurs (0.15s) instead of an instant pop; the fade-in timing matches Base. |
| cl-centerprint | cl-centerprint.fade.in_out | presentation | partial -> faithful | Messages now ramp in over 0.15s instead of popping in instantly. |
| cl-centerprint | cl-centerprint.layout.draw | values | partial -> faithful | fontscale_bold=1.2 / fontscale_title=1.3 (the shipped hud_luma/luminos skin overrides) now baked as registered + live-read defaults (CenterPrintPanel.cs:280-281,302-303), matching hud_luma.cfg:290-291 instead of header defaults 1.4/1.8. |
| cl-centerprint | cl-centerprint.title.gametype | values | partial -> faithful | Title fontscale skin gap closed: FontScaleTitle default now 1.3 (the shipped-skin value) instead of header default 1.8. |
| cl-centerprint | cl-centerprint.config.preview | logic | missing -> faithful | UpdateHudConfigurePreview (CenterPrintPanel.cs:210-263) now implements the QC autocvar__hud_configure branch: enter clear + immediate gen, rotating countdown/multiline/standard samples + 'Title' when highlighted, 'Generic message' otherwise, leave clear, with matching r-thresholds, ids, durations and regen cadences. |
| cl-centerprint | cl-centerprint.config.preview | presentation | missing -> faithful | The panel now previews live sample centerprint lines + a Title in the HUD editor instead of rendering empty. |
| cl-centerprint | cl-centerprint.config.preview | liveness | na -> live | _Process calls UpdateHudConfigurePreview every frame, self-gating on the _hud_configure cvar (registered HudConfig.cs:76, toggled by the HUD setup dialog); IsHighlighted driven by HudConfigEditor.cs:548. |
| cl-hud | cl-hud.engine.progressbar | logic | partial -> faithful | Wave 6e (faa4e89) added the shared HudPanel.DrawProgressBar primitive porting all 4 baralign modes (incl. signed center accel bar) + vertical + the only-baralign-3-may-go-negative clamp; used live by HealthArmor/Physics/Powerups. |
| cl-hud | cl-hud.engine.progressbar | values | partial -> faithful | Bar geometry/alignment math is now faithful and resolves the real skin progressbar/progressbar_vertical/accelbar art via DrawSkinPic (skin->default), flat-rect only as a miss fallback. |
| cl-hud | cl-hud.engine.num_color | logic | partial -> faithful | NumColor rewritten (Wave 6e) as a line-for-line port of HUD_Get_Num_Color: 5-stop CASE_COLOR_BETWEEN ramp replacing the 2-stop lerp. |
| cl-hud | cl-hud.engine.num_color | values | partial -> faithful | Exact COLOR100..COLOR10 vectors and percent thresholds (100/75/50/25/10) now match Base. |
| cl-hud | cl-hud.engine.num_color | timing | partial -> faithful | >=100% sine flash + <25% low-health pulse now driven by HudPanel.HudClock (== QC time), fed each frame by HudManager._Process. |
| cl-hud | cl-hud.engine.num_color | presentation | partial -> faithful | Animated blink/pulse emphasis is now present, matching Base's visible number tint behavior. |
| cl-hud | cl-hud.engine.dynamic_follow | logic | missing -> faithful | Wave 6e ported the Hud_Dynamic_Frame follow block (HudManager.ComputeDynamicFollow): -cl_followmodel_ofs*scale, axis remap z->x/x->y/y->z, 0.001 snap, ±0.1 bound, shift/scale math line-for-line. |
| cl-hud | cl-hud.engine.dynamic_follow | values | missing -> faithful | Defaults match: hud_dynamic_follow 0, _scale 0.01, scale_xyz remap and ±0.1 bound identical to Base. |
| cl-hud | cl-hud.engine.dynamic_follow | timing | missing -> faithful | Computed and applied to the HUD CanvasLayer Offset/Scale every frame in _Process. |
| cl-hud | cl-hud.engine.dynamic_follow | presentation | missing -> faithful | The whole HUD now sways/scales with the viewmodel followmodel offset when the cvar is enabled. |
| cl-hud | cl-hud.engine.dynamic_follow | liveness | na -> live | Wired live via NetGame.UpdateHudDynamicFollow (each frame) feeding ViewModel.LastFollowOffset; previously port_refs was empty (unported). |
| cl-hud | cl-hud.panel.vote | values | partial -> faithful | Wave 6e corrected hud_panel_vote_alreadyvoted_alpha default (property + registered cvar) from 0.75 to 0.7, matching Base hud_luma.cfg:201. |
| cl-hud | cl-hud.panel.radar | logic | partial -> faithful | Wave 6e added zoommode (current_zoomfraction-driven), rotation modes (player-aligned + fixed N/E/S/W), big-zoom map-center recenter, v_flipped X-mirror, and corrected teamradar_loadcvars code defaults (scale 4096, foreground_alpha 0.8). |
| cl-scoreboard | cl-scoreboard.field.name_icons | liveness | na -> partial | The name-cell handicap icon is now networked end-to-end and rendered on the live UpdateScoreboard path (Handicap.UpdateHandicapLevel -> Entity.HandicapLevel -> ScoreboardBlock.CaptureRows:84 -> ScoreRowWire WriteByte:107/ReadByte:151 -> SetWireRows:251 -> DrawRow:977-985). Baseline had the whole row missing/na. |
| cl-scoreboard | cl-scoreboard.field.name_icons | logic | missing -> partial | Handicap-level logic ported (level read from the wire, icon shown only when nonzero); other 4 icons (player-color/ready/ignored/wants-join) still absent. |
| cl-scoreboard | cl-scoreboard.field.name_icons | values | missing -> partial | Handicap tint uses the exact QC formula '1 0 0' + '0 1 1' * ((16-lvl)/15) (white@1 -> red@16); remaining icons' values still absent. |
| cl-scoreboard | cl-scoreboard.field.name_icons | timing | missing -> partial | Handicap icon updates every frame on the live feed; the pulsing ready/wants-join markers (which have timing animation) are still unported. |
| cl-scoreboard | cl-scoreboard.field.name_icons | presentation | missing -> partial | Handicap icon now drawn (flat colored square at the exact tint, no gfx/scoreboard/player_handicap art); player-color swatch, ready, ignored, (Q) wants-join still not drawn. |
| cl-shownames | cl-shownames.gate.team_enemy | logic | partial -> faithful | sameteam now reproduces QC's m_entcs_private authority: ShowNamesLayer.cs:204-208 entcsPrivate = !isSelf && (sameTeamColor \|\| radar_showenemies \|\| LocalIsSpectating), matching the server _entcs_send gate, instead of the old bare (Colormap&0xFF)==LocalTeam compare. radar_showenemies and observe/spectate arms now honoured; LocalIsSpectating fed from NetGame.cs:2736. |
| cl-shownames | cl-shownames.name.decolorize | logic | partial -> faithful | Width truncation is now implemented: DrawName calls ShortenToWidth / ShortenRunsToWidth (ShowNamesLayer.cs:470,481,499-556) reproducing QC textShortenToWidth(name, namewidth=mySize.x) with stringwidth_colors. Baseline recorded NO width clamp as the worst gap. |
| cl-shownames | cl-shownames.name.decolorize | presentation | partial -> partial | Both baseline presentation defects resolved: (1) the 1px black drop-shadow REMOVED, (2) width-overflow no longer happens. Stays partial only for the minor residual (decolorized names render flat white not the playername team tint), so net presentation fidelity improved markedly. |
| cl-shownames | cl-shownames.entcs.private_healtharmor | logic | partial -> faithful | Enemy health/armor info-parity divergence closed both ends: server-side ServerNet.RelevantEntitiesFor:1930-1940 zeroes Health/Armor for enemy recipients of a player entity; client-side ShowNamesLayer.cs:399-400 reads s.Health/s.Armor only when entcsPrivate — mirroring QC Draw_ShowNames_All:237-248 m_entcs_private gate. Baseline read the always-present remote slice unconditionally. |
| cl-shownames | cl-shownames.entcs.private_healtharmor | values | na -> faithful | Baseline had values na (it read the slice unconditionally so there was nothing to gate); now the enemy-zeroing produces correct zeroed values both server and client side, so values is faithful. |
| cl-teamradar | cl-teamradar.icon.ping | logic | missing -> faithful | Ping pulse path fully ported end-to-end: ServerNet encodes bit 7 of the radar-icon byte via WaypointSprites.IsPinging (ServerNet.cs:968-970), ClientNet decodes bit 0x80 and stamps a per-id timestamp (ClientNet.cs:625-632), RadarPanel.DrawPingPulse draws the expanding ring. |
| cl-teamradar | cl-teamradar.icon.ping | values | missing -> faithful | Pulse size (2*iconPx*dt), alpha ((1-dt)*a), and the 0.3s anti-spam window all match Base; the ping bit (RadarIcon&0x7F \| 0x80) is now sent instead of the old RadarIcon&0xFF that masked it off. |
| cl-teamradar | cl-teamradar.icon.ping | timing | missing -> faithful | dt window (0,1)s and 0.3s ping de-dupe ported faithfully on both server and client. |
| cl-teamradar | cl-teamradar.icon.ping | presentation | missing -> faithful | gfx/teamradar_ping additive expanding ring now drawn (DrawPingPulse). Residual: one timestamp per id vs Base's 32-slot ring array (minor, downgraded to a gap note). |
| cl-teamradar | cl-teamradar.icon.ping | liveness | na -> live | The ping path is invoked on the live waypoint-net path: WaypointSprites.Ping marks the sprite, the per-frame serializer stamps bit 7, the client radar consumes it. |
| cl-teamradar | cl-teamradar.icon.objective | values | partial -> faithful | Icon size is now a fixed 8px (const iconPx = 8f, RadarPanel.cs:315) matching Base's fixed '8 8 0' px; the prior radius*0.07 (7..12px) clamp is gone. |
| cl-teamradar | cl-teamradar.icon.objective | presentation | partial -> faithful | Fixed 8px sizing + single gfx/teamradar_icon_1 sprite; the spurious dead icon_2 selector keyed on RadarIcon==2 has been removed. |
| cl-teamradar | cl-teamradar.transform.coords | values | partial -> faithful | v_flipped now applied: flipX = v_flipped ? -1 : 1 (RadarPanel.cs:205) threaded through WorldToScreen and DrawPlayerArrow, mirroring the map/blips/arrows like Base teamradar_texcoord_to_2dcoord and draw_teamradar_player. |
| cl-teamradar | cl-teamradar.cvar.defaults | logic | partial -> faithful | hud_panel_radar 0/1/2 enable + team gating now honored at runtime in DrawPanel (RadarPanel.cs:158-162): enable 0 returns, enable!=2 && !Teamplay returns. |
| cl-teamradar | cl-teamradar.cvar.defaults | values | partial -> faithful | Registered cvar defaults now the QC code defaults (scale 4096, foreground_alpha 0.8, maximized_rotation 1), not the luma-skin values (8192/1/0) the baseline flagged. |
| cl-teamradar | cl-teamradar.cvar.defaults | presentation | partial -> faithful | Correct defaults + enable gating mean the radar presents per Base when the cvar is at code-default; the dialog Disable/Team/Always now has a runtime consumer. |
| cl-teamradar | cl-teamradar.cvar.zoomscale | logic | partial -> partial | zoommode 0/1 now follow the live ZoomFraction (current_zoomfraction / 1-current_zoomfraction) instead of collapsing to static 0/1; big-zoom recentering ported (RadarPanel.cs:235-241). Still partial: the bigsize/normalsize scale endpoints remain a 2500u + scale/4 heuristic, not the mi_scale/scale2d fit. |
| cl-teamradar | cl-teamradar.cvar.rotation | presentation | faithful -> faithful (gap closed) | Status unchanged (already faithful) but the 'v_flipped not applied' gap is now closed by flipX; recorded as a gap-clearing fix, not a status change. |

## Reclassified

Neutral re-labellings (no fidelity change).

| unit | feature | dimension | was -> now | reason |
| --- | --- | --- | --- | --- |
| cl-hud | cl-hud.panel.radar | values | unknown -> faithful | teamradar code-default constants (scale 4096, maximized_scale 8192, foreground_alpha 0.8) verified registered correctly; the registry's prior '8192/5120' were the luma skin overrides, now clarified. |
| cl-shownames | cl-shownames.gate.team_enemy | timing | na -> na | Baseline marked timing na and presentation na; unchanged (no-op record, included for transparency of the row's dimension set). |
