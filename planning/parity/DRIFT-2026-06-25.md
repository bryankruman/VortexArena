# Parity drift — 2026-06-25

1 regression, 52 fixes, 0 new gaps, 5 reclassified, 12 registry coverage gaps (newly-unmapped Base features).

## Regressions

| unit | feature | dimension | was -> now | reason |
| --- | --- | --- | --- | --- |
| mayhem | mayhem.score.damage_accrual | logic | faithful -> partial | The DEATH_CAMP omission from Mayhem's `IsEnvironmentalSuicide` set is no longer latent. Since baseline the port gained a `DeathTypes.Camp` constant (DeathTypes.cs:107-109) and a live `CampcheckMutator` dealing 'camp'-deathtype damage (CampcheckMutator.cs:130,139). When `g_campcheck` is enabled, a camp-killed player's death damage is NOT subtracted from `total_damage_dealt` (Mayhem.cs:241-250 omits `DeathTypes.Camp`), where Base subtracts it. The divergence is now reachable, so logic moves faithful->partial. Conditional on the non-default `g_campcheck` mutator; one-line fix (add `DeathTypes.Camp` to `IsEnvironmentalSuicide`). |

## New gaps

No new gaps.

## Registry coverage gaps

Base features observed during this pass that have no registry row (or only an implicit one). The registry under-counts the gap surface here.

### lms
- **lms ClientConnect hook** (sv_lms.qc:438-442): sets `player.frags = FRAGS_SPECTATOR` on connect (the LMS connect-as-observer default). No dedicated registry row — partially implicit in the join-gate flow but the explicit Base hook isn't mapped.
- **lms Bot_FixCount hook** (sv_lms.qc:693-702, CBC_ORDER_EXCLUSIVE): LMS counts only INGAME real clients as `activerealplayers` (so bot fill ignores eliminated/observer players). Affects bot-balance fill; no registry row.
- **lms AddPlayerScore hook** (sv_lms.qc:725-730): allows writing `SP_LMS_RANK` during intermission/game_stopped so a newly-joining player's rank can still be set. No registry row.
- **lms PlayerPreThink stat export** (sv_lms.qc:444-454): exports `STAT(REDALIVE)=lms_leaders` and `STAT(BLUEALIVE)=lms_leaders_lives_diff` per player, plus the `DEAD_DYING -> DEAD_RESPAWNING` deadflag advance. The stat-export half overlaps the leader-stat gap already tracked under lms.leader.waypoint/lms.hud.mod_icon, but is not its own row; the deadflag advance is unmapped.
- **lms ReadLevelCvars hook** (sv_lms.qc:247-251): forces `sv_ready_restart_after_countdown=0` (incompatible with LMS mid-match reset). No registry row; now noted inline as a minor gap under lms.respawn.continuous since the port has no ReadLevelCvars-equivalent.

### invasion
- **AccuracyTargetValid** (sv_invasion.qc): monster targets return `MUT_ACCADD_INVALID` so monster hits do not count toward weapon-accuracy stats — no port hook chain; server-side accuracy mis-tally is masked only by the client scoreboard hide. Added as invasion.rules.accuracy_target_valid.
- **AllowMobSpawning + AllowMobButcher** (sv_invasion.qc): block the `spawnmob` and `butcher` console commands while an invasion is active — not ported (Commands.cs:2363 explicitly notes no AllowMobSpawning analog). Added as invasion.rules.mob_command_guards.
- **m_configuremenu** (invasion.qh): the create-game 'Point limit:' menu slider (50..500 step 10) — not present in the port's create-game menu. Added as invasion.client.point_limit_menu.

### clanarena
- **clanarena.notify.alone_on_leave**: Base sends CENTER_ALONE from ClientDisconnect (sv_clanarena.qc:393) and MakePlayerObserver (sv_clanarena.qc:406), not just PlayerDies; the port wires only the death path.
- **clanarena.matchend.restore_status**: `MUTATOR_HOOKFUNCTION(ca, MatchEnd_BeforeScores)` restores pre-CA spectator/team status via `MatchEnd_RestoreSpectatorAndTeamStatus(prev_team)`; no port impl, no prev_team snapshot.
- **clanarena.spectate.force_spectate_cmd**: `MUTATOR_HOOKFUNCTION(ca, ClientCommand_Spectate)` force-spectates an INGAME player (MUT_SPECCMD_FORCE) and sends INFO_CA_LEAVE; no port hook or CA_LEAVE notification.
- **clanarena.cmd.shuffleteams_reset**: `MUTATOR_HOOKFUNCTION(ca, SV_ParseServerCommand)` defers a mid-round shuffleteams to the next ResetMap (`shuffleteams_on_reset_map = !allowed_to_spawn`); no port hook.

## Fixes

Parity that improved since the baseline.

### ctf
| feature | dimension | was -> now | reason |
| --- | --- | --- | --- |
| ctf.flag.pickup | values | partial -> faithful | Score-cvar fallback defaults now match stock ctfscoring-samual.cfg (pickup_base=1, dropped_early=1, dropped_late=1) instead of the old 0 fallbacks. Confirmed samual is the cfg exec'd by gametypes-server.cfg:357. The three pickup constants flipped match:false->true. |
| ctf.flag.capture | values | partial -> faithful | capture (20) and capture_assist (10) fallbacks now match samual; constants flipped match:false->true. |
| ctf.flag.capture | presentation | partial -> partial | Capture EFFECT (`<team>_cap` via EffectEmitter.Emit, Ctf.cs:552) is now emitted at the flag base — was an explicit gap. Dimension stays partial because the persistent leaderboard/ServerProgsDB record + fastest-cap CTF_CAPTURE_TIME_<team> broadcast remain unported. |
| ctf.flag.return | logic | partial -> faithful | ReturnFlag now docks the dropper's SCORE (penalty_returned) AND arms the dropper's re-take collect-delay (GtNextTakeTime), Ctf.cs:584-590 — previously the dropper penalty + re-take shield were not applied on return. |
| ctf.flag.return | values | partial -> faithful | return (10) and penalty_returned (1) fallbacks now match samual; constants flipped match:false->true. |
| ctf.flag.drop_on_death | values | partial -> faithful | penalty_drop fallback now matches samual (1) instead of 0; constant flipped match:false->true. |
| ctf.flag.fckill_score | logic | partial -> faithful | The flagcarrier damage/force factors (Damage_Calculate hook) are now ported in OnDamageCalculate (Ctf.cs:1029) and registered+dispatched LIVE via MutatorHooks.DamageCalculate.Call from DamageSystem.cs:256. |
| ctf.flag.fckill_score | values | partial -> faithful | kill (5), flagcarrier factors (1), and auto_helpme_damage/_time (100/2) fallbacks now match Base; constants flipped match:false->true. |
| ctf.flag.fckill_score | presentation | missing -> partial | Auto-helpme on a low-health carrier is now wired: OnDamageCalculate sets GtHelpMeUntil, CollectWaypoints flags the carrier sprite, networked via ServerNet.cs:964. Stays partial because it is a sprite helpme FLASH, not the full Base WaypointSprite_HelpMePing. |
| ctf.flag.think_dropped | logic | partial -> faithful | Dropped-flag auto-capture within g_ctf_dropped_capture_radius (CaptureDropped, Ctf.cs:1375), float-in-water buoyancy (Api.Trace.PointContents water test), and return-when-dropped-near-base radius are now all implemented and live on the per-entity FlagThinkEntity — all three were explicit gaps. |
| ctf.flag.think_dropped | values | partial -> faithful | dropped_capture_radius/delay (100/1), floatinwater (200), return_dropped (100) now read live with Base-matching values; constants flipped match:false->true. |
| ctf.capture_shield | logic | partial -> faithful | CaptureShieldStatus now scores by the QC composite CtfShieldScore = (CAPS-PICKUPS)+(RETURNS+FCKILLS) (Ctf.cs:1572), not net SCORE — the shield-score-basis constant flipped match:false->true. |
| ctf.capture_shield | values | partial -> faithful | shield_max_ratio fallback now 0 (matches Base default = disabled) and shield_force fallback 100 (matches Base); both constants flipped match:false->true. |
| ctf.capture_shield | confidence | medium -> high | Composite-score + Base-matching fallbacks verified in code; raised confidence to high. |

### lms
| feature | dimension | was -> now | reason |
| --- | --- | --- | --- |
| lms.respawn.continuous | logic | partial -> faithful | EnableRounds() was removed from ActivateGameType (GameWorld.cs:1916 is now `case LastManStanding lms: lms.Activate(); break;`). LMS now runs as Base's single continuous per-lives match — the round-based inversion that was the headline divergence is gone. |
| lms.respawn.continuous | timing | unknown -> faithful | With no rounds, respawn timing is owned by ApplyRespawnTiming (per-lives) on every mid-match death; the round-gate masking is gone. |
| lms.respawn.continuous | liveness | partial -> live | Rounds is null for LMS, so DeadPlayerThink's round gate (line 2401) no longer short-circuits ApplyRespawnTiming (line 2417); the continuous per-lives path is fully live. |
| lms.respawn.continuous | values | partial -> faithful | The degenerate DefaultCanRoundEnd team-scan no longer applies; per-lives values now govern. |
| lms.respawn.dynamic_delay | liveness | partial -> live | The round-gate that previously masked the dynamic delay (only firing between rounds) is removed; ApplyRespawnTiming->RespawnDelayFor now drives respawn timing on every mid-match death (GameWorld.cs:2417). |
| lms.lives.eliminate_at_zero | liveness | live -> live | Already live, but the stale round-gate co-decision caveat is resolved: the RESPAWN_SILENT 0-lives denial now fires unconditionally on the live path (no EnableRounds gate). Notes/verification corrected; no status value change. |
| lms.leader.glow | logic | missing -> faithful | OnPlayerPowerups (LastManStanding.cs:853) now applies EF_ADDITIVE|EF_FULLBRIGHT to IsLeader players / clears them otherwise — exact match to Base PlayerPowerups (sv_lms.qc:515-522). Subscribed in Activate(). |
| lms.leader.glow | presentation | missing -> faithful | Leaders now visibly glow via the live PlayerPowerups seam. |
| lms.leader.glow | timing | na -> faithful | The glow is recomputed/applied per frame (leader set recomputed each frame), matching Base's per-frame PlayerPowerups. |
| lms.leader.glow | liveness | na -> live | Fired live each frame from PlayerFrameLogic.cs:325 (MutatorHooks.PlayerPowerups.Call). |
| lms.reset.map_players | logic | missing -> faithful | ResetMapPlayers (LastManStanding.cs:479) now re-livens eliminated players, zeroes+re-seeds LMS_RANK/LMS_LIVES, clears leader/forfeiter/win latches, and resets _lowestLives=999 (QC reset_map_players + reset_map_global). |
| lms.reset.map_players | liveness | na -> live | Wired into the host ResetMap path (GameWorld.cs:3143) on a real match restart. |
| lms.win.last_standing | logic | partial -> faithful | CheckWinningCondition now models the warmup/pre-start guard (PreMatch), the top-two-equal tie-cancel (TopTwoLivesEqual => TimeLimitCancelled), the forfeit-win path (totalPlayed + GameStartTime gated), the no-more-joiners survivor win, and the zero-living draw end — previously a reduced single-survivor core only. |
| lms.win.last_standing | values | partial -> faithful | g_lms_forfeit_min_match_time is now read (Cvar, default 0, match true) — the previously-absent forfeit-time constant is wired; tie-cancel is modeled. |

### invasion
| feature | dimension | was -> now | reason |
| --- | --- | --- | --- |
| invasion.win.stage | logic | partial -> faithful | Wave-6a (commit e596bf0) replaced the unconditional first-touch RoundEndReached set with TriggerRoundEnd's real-player threshold: the touch volume (GameWorld.cs:2199) now passes the toucher, TriggerRoundEnd (Invasion.cs:154-183) adds real non-bot players to _endReached and only sets RoundEndReached once reached >= ceil(realCount * frac), matching Base target_invasion_roundend_use. Headless/no-roster falls back to single-touch for solo test maps. |
| invasion.win.stage | values | partial -> faithful | The 0.7 (count) fraction is now honored: needed = ceil(realplayers * min(1, fraction)), default 0.7, bots excluded — matching Base. Baseline recorded 'no threshold / 1 player touch'. |
| invasion.win.point_limit | logic | partial -> faithful | CheckPointLimit (Invasion.cs:937-949) now latches MatchEnded on the LEADING player's banked KILLS via TopKillsScore() (FindByClass(player) max GameScores.Kills), matching GameRules_limit_score / WinningConditionHelper_topscore; the cumulative MonstersKilled is now only the headless fallback. Baseline recorded the cumulative-across-all-players total as the live path. |
| invasion.client.hide_item_stats | logic | missing -> faithful | ScoreboardPanel.cs:1097 DrawAccuracy now short-circuits (return y) when GameScores.Gametype == 'inv', implementing cl_invasion.qc DrawScoreboardItemStats (ISGAMETYPE(INVASION)). Baseline had empty port_refs and logic:missing. |
| invasion.client.hide_item_stats | presentation | missing -> faithful | The weapon-accuracy item-stats panel is now visibly suppressed in Invasion via the live DrawAccuracy gate; baseline recorded the panel as not suppressed (presentation:missing). |
| invasion.client.hide_item_stats | liveness | na -> live | The gate lives in DrawAccuracy on the live scoreboard render path; baseline had liveness:na because no port code existed. |

### survival
| feature | dimension | was -> now | reason |
| --- | --- | --- | --- |
| survival.round.handler | logic | partial -> faithful | The dual-handler desync is GONE. The Survival arm now drives the ONE live GameWorld.Rounds handler off Survival's own predicates: EnableRounds(surv.HandlerCanRoundStart, ()=>{surv.CheckWinningCondition(); return surv.RoundOver;}, onRoundStart: surv.AssignRoles, ...) at GameWorld.cs:1935-1941. No longer the degenerate team-based DefaultCanRoundEnd. |
| survival.round.handler | timing | unknown -> faithful | _roundSync=MirrorRoundTiming(survRounds, surv.Handler) now mirrors RoundEndTime/RoundsPlayed/GameStartTime/IsRoundStarted into surv.Handler every frame (GameWorld.cs:1946, 3336-3348), like CA/FreezeTag. surv.Handler is no longer separately ticked. |
| survival.round.handler | liveness | partial -> live | Single coherent live drive: the live handler fires AssignRoles at round start, uses CheckWinningCondition for round-end, and feeds the live roster via _roundPrep. |
| survival.round.assign_roles | timing | unknown -> faithful | AssignRoles is now the actual live round-start callback (EnableRounds onRoundStart, GameWorld.cs:1938), not a second independently-timed handler; surv.Handler's clock is mirrored from the live handler. Secret-role moment now coincides with the live round-start/respawn-unlock. |
| survival.win.side_wipe | timing | partial -> faithful | The g_survival_round_enddelay>0 deferral path (round_handler_SetEndDelayTime + re-check + both-sides-alive reset) is now fully modeled in CheckWinningCondition (Survival.cs:468, 474-491), reading the live-mirrored RoundEndTime — matching QC sv_survival.qc:114-128. |
| survival.win.timeout | timing | faithful -> faithful | Clock-provenance concern resolved (not a status change but a gap closure): surv.Handler.RoundEndTime is now mirrored from the live GameWorld.Rounds handler via MirrorRoundTiming, so the 120s timer reads the live map-reset clock rather than an unmirrored owned clock. A baseline gap entry is removed. |
| survival.death.eliminate | timing | partial -> faithful | The silent-forced-respawn camera nuance is now ported: OnDeath sets RESPAWN_SILENT + RespawnTime=time+2 while a round is live (Survival.cs:363-367) and RESPAWN_FORCE always (cs:368), matching QC PlayerDies sv_survival.qc:377-383. Confidence also raised medium->high. |
| survival.notify.last_survivor_alone | timing | partial -> faithful | The disconnect + forced-observe call sites are now wired via Survival.OnPlayerRemoved (Survival.cs:434-441), dispatched from ClientManager.cs:473 (PutObserverInServer) and :682 (ClientDisconnect). All three QC surv_LastPlayerForTeam_Notify call sites are now ported + live; the prior death-only gap is closed. |
| survival.gametype.registration | values | partial -> faithful | The timelimit=20 pointlimit=12 mapinfo defaults are now asserted at activation when unconfigured (GameWorld.cs:1923-1926: timelimit=20, fraglimit=12), matching Base gametype_init default args. Only m_isForcedSupported LMS-map auto-support remains unmodeled (a coverage gap, not a value mismatch). |

### clanarena
| feature | dimension | was -> now | reason |
| --- | --- | --- | --- |
| clanarena.notify.round_outcome | liveness | partial -> live | NotifyLastPlayerForTeam (CENTER_ALONE 'You are now alone!') was DEAD at the prior baseline (zero callers). It now has a live caller: OnDeath (ClanArena.cs:301) is subscribed to Combat.Death in Activate (ClanArena.cs:193-194) and calls NotifyLastPlayerForTeam(victim,_roster). Combat.Death.Call fires on the live player-death path (DamageSystem.cs:576), and the IsRoundStarted gate reads the mirrored owned handler. |
| clanarena.notify.round_outcome | logic | partial -> faithful | With the 'alone' notify now wired on the death path, the round-outcome notification logic (team win / tied / over + last-survivor 'alone') matches Base on the common path. The only remaining defect is the disconnect/make-observer notify path, now split out into its own row clanarena.notify.alone_on_leave. |
| clanarena.notify.round_outcome | presentation | partial -> faithful | CENTER_ALONE is registered (NotificationsList.cs:847) and is now actually sent on the live death path, so the on-screen presentation of the last-survivor print is faithful for the teammate-dies case. |

### deathmatch
| feature | dimension | was -> now | reason |
| --- | --- | --- | --- |
| deathmatch.respawn.timing | constants/values (GAMETYPE_DEFAULTED_SETTING dm-prefix) | match:false (RespawnTiming.Calculate reads only generic g_respawn_delay_*; gametype-prefixed g_dm_respawn_delay_* and g_respawn_delay_forced ignored) -> match:true (RespawnTiming.GametypeDefaulted('dm',...) reads g_dm_respawn_delay_* first then generic, honors g_respawn_delay_forced and negative->0; live caller GameWorld.cs:2419 passes GameType?.NetName='dm') | RespawnTiming.cs was reworked since the 2026-06-22 baseline to add the GametypeDefaulted helper (a faithful port of GAMETYPE_DEFAULTED_SETTING, client.qh:347-351), and the DeadPlayerThink death-edge caller now passes the gametype NetName. The baseline's gametype-prefixed-cvar gap is closed. logic/values/timing stay faithful; the only remaining gap is the unspoken 10-9-8 announcer (presentation/audio missing, owned by sv-client-lifecycle). |

### mayhem
| feature | dimension | was -> now | reason |
| --- | --- | --- | --- |
| mayhem.identity.registration | values | partial -> faithful | The timelimit=15 gametype_init default is now applied on the live path. Mayhem.OnInit (Mayhem.cs:351, called from GameWorld.cs:503 GameType?.OnInit() during boot) calls SeedTimeLimit(15), which sets the timelimit cvar when it still equals the generic default 20 (Cvars.cs:108). Baseline recorded DefaultTimeLimitMinutes as a dead const with zero live consumers; it now has a live consumer, so the timelimit_default constant moves match:false->true and the timelimit gap is closed. |

### tmayhem
| feature | dimension | was -> now | reason |
| --- | --- | --- | --- |
| tmayhem.identity.registration | values | partial -> faithful | The SeedTimeLimit self-cancelling guard (`if timelimit==20`) was REMOVED. SeedTimeLimit (TeamMayhem.cs:107-112) now UNCONDITIONALLY calls Api.Cvars.Set("timelimit","20") on every gametype select, called live from GameWorld.OnInit@503 before ActivateGameType@545. This faithfully reproduces QC _MapInfo_Map_ApplyGametypeEx (mapinfo.qc:551 reset-to-defstring + :572 unconditional gametype-default apply), so a prior mode/vote/round's non-20 timelimit leftover is now reset to tmayhem's 20. Closes the baseline's only identity gap; timelimit_default constant match false->true; gaps now empty. |

### race
| feature | dimension | was -> now | reason |
| --- | --- | --- | --- |
| race.checkpoint.entities | logic | partial -> faithful | WireObjectiveSpawns (GameWorld.cs:2053-2055) now passes timed: (e.SpawnFlags & 8) != 0 to SpawnCheckpoint, so the race_timed_checkpoint follows the highest spawnflag-8 CP on start!=finish tracks instead of hardcoding finish=CP0. The baseline's logic gap (finish assumed index 0) is resolved. |
| race.checkpoint.ordered_crossing | logic | partial -> faithful | The spawnflag-4 wrong-order kill branch (CheckpointTouch -> Combat.Damage 10000) is now reachable: WireObjectiveSpawns (GameWorld.cs:2054) populates punishWrongWay from e.SpawnFlags & 4. Only the cosmetic Void-vs-HURTTRIGGER deathtype and SUB_UseTargets side branch remain. |
| race.checkpoint.ordered_crossing | liveness | partial -> live | The wrong-way kill is no longer dead — a wrong-order crossing of a spawnflag-4 checkpoint now deals 10000 damage on the live BSP-spawned path (baseline recorded zero punishWrongWay:true callers). |
| race.mode.setup_limits | timing | partial -> faithful | The g_race_qualifying_timelimit(_override) source is now wired at Race activation (GameWorld.cs:1986-1997): a positive qualifying timelimit derives g_race_qualifying=2, stashes the real limits via SaveRaceLimits, and replaces the active timelimit with the qualifying clock. Baseline marked SaveRaceLimits as having no caller. |
| race.mode.setup_limits | liveness | partial -> live | The qualifying-timelimit-derived limit stashing now runs on the live activation path (the SaveRaceLimits caller the baseline flagged as missing now exists in GameWorld.WireGametype). |
| race.qualifying.transition | values | partial -> faithful | The qualifying-timelimit SOURCE (g_race_qualifying_timelimit -> the qualifying-phase timelimit) is now wired (GameWorld.cs:1986-1997), closing the baseline's last open gap. The qualifying phase now runs on the qualifying clock, not the plain timelimit. |
| race.qualifying.transition | timing | partial -> faithful | The qualifying clock now correctly times the qualifying phase (active timelimit replaced by the qualifying timelimit at activation); when it elapses TransitionQualifyingToRace restores the stashed limits. Full qualifying==2 chain is live. |

## Reclassified

Neutral status changes (no parity movement).

| unit | feature | dimension | was -> now | reason |
| --- | --- | --- | --- | --- |
| lms | lms.win.last_standing | timing | unknown -> partial | Win is latched from a per-frame poll (DriveGametypeFrame line 2329); core works on the live path, but tick ordering vs Base's CheckRules_World remains unverified — downgraded from unknown to partial. |
| tmayhem | tmayhem.score.damage_accrual | logic | faithful -> faithful | Neutral: status dimensions unchanged (logic/values stay faithful, DEATH_CAMP still non-default). But the underlying fact behind the env_suicide_set gap changed — DeathTypes.Camp NOW EXISTS in the port (Mayhem.cs:109), so the gap is reclassified from 'port models no campcheck death' to 'camp tag exists but is not added to MayhemScoring.IsEnvironmentalSuicide (Mayhem.cs:243-249)'. The in-code comments at Mayhem.cs:225-226/238-239 are now stale. |
| race | race.checkpoint.entities | liveness | live -> live | Already live; re-verified. The spawnflag-4 punishWrongWay and spawnflag-8 timed flags are now both armed from the BSP lump (the prior audit's one-branch-away wiring is done). |
| survival | survival.win.timeout | timing | faithful -> faithful | Neutral: status unchanged. Clock-provenance concern resolved via MirrorRoundTiming (the 120s timer now reads the live map-reset clock); a baseline gap entry is removed. |
