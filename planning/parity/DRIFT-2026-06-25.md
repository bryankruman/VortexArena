# Parity drift — 2026-06-25

**3 regressions · 1 new gap · 6 registry-coverage gaps · 38 fixes · 1 reclassified** (12 units changed: fx-deathtypes, fx-notifications, fx-sounds, items-pickups, mutator-bloodloss, mutator-buffs, mutator-bugrigs, turret-phaser, turret-walker, vehicle-framework, vehicle-raptor, vehicle-spiderbot).

## Regressions

Parity that got worse since the baseline. Core gameplay categories (gametype/weapon/damage/physics/item/scoring) first.

| unit | feature | dimension | was -> now | reason |
| --- | --- | --- | --- | --- |
| fx-deathtypes | fx-deathtypes.message.teamchange | liveness | live -> partial | DEATH_AUTOTEAMCHANGE no longer has a live emitter. The prior audit cited Teamplay.cs:698 emitting DeathTypes.AutoTeamChange, but the bot auto-balance path (AutoBalanceBots -> KillPlayerForTeamChange) now emits DeathTypes.TeamChange. grep DeathTypes.AutoTeamChange finds only the const/predicates (DeathTypes.cs) and the consumer (Scores.cs). Its self-line is selectable but dead. |
| fx-deathtypes | fx-deathtypes.message.teamchange | logic | faithful -> partial | Auto-balance kills are mis-classified. Per Base (damage.qc:272-273,304) an auto-balance death is DEATH_AUTOTEAMCHANGE, which SKIPS the GiveFrags(-1) frag-negation. The port routes auto-balance through the same KillPlayerForTeamChange that emits DeathTypes.TeamChange, so the -1 frag is NOT skipped for auto-balanced bots. |
| fx-deathtypes | fx-deathtypes.message.teamchange | presentation | faithful -> partial | Auto-balanced bots now render the manual DEATH_SELF_TEAMCHANGE wording instead of DEATH_SELF_AUTOTEAMCHANGE, because the auto path emits DeathTypes.TeamChange (selector picks the manual line at Scores.cs:601-602). |

## New gaps

| unit | feature | dimension | was -> now | reason |
| --- | --- | --- | --- | --- |
| mutator-buffs | buffs.replace_powerups | presentation | na -> partial | Now that the replacement path is implemented, its spawned buff item inherits the spawnfunc presentation gap (per-buff skin/color/glowmod not applied -> uncolored relic). A real, now-relevant presentation defect on this row. |

## Registry coverage gaps

Base features observed during this pass that have no registry row yet.

| unit | Base feature / note |
| --- | --- |
| turret-walker | MENUQC WalkerTurret.describe — the turret encyclopedia help text (walker.qc:645-653); not represented by any registry row (low-value, menu-only). |
| vehicle-framework | vehicle-framework.lifecycle.reset — the QC `.reset = vehicles_reset` round/match-restart hook (sv_vehicles.qc:1095 install at :1118): eject pilot (vehicles_exit VHEF_RELEASE) + vehicles_clearreturn + respawn if active. NOT registered on any port vehicle (no GtReset/.Reset assignment in src/.../Vehicles). Added as a new row (status missing/na). |
| vehicle-framework | vehicle-framework.death.gibs — vehicle_tossgib + vehicles_gib_explode/touch/think (sv_vehicles.qc:274-326): flying debris gibs on vehicle death (MOVETYPE_TOSS gib at a tag, velocity+avelocity, optional burn/explode, alpha fade, EXPLOSION_SMALL FX + rocket-impact sound). UNPORTED (only a NOTE at Bumblebee.cs:502); previously only mentioned as a deferred gap. Added as a new row (missing on all dimensions). |
| vehicle-framework | vehicle_addplayerslot (sv_vehicles.qc:328) gunner-slot/MULTISLOT framework fn — NOTED as potentially-unmapped at the framework level, but intentionally NOT given a framework row because its only consumer is the Bumblebee and it is already covered by registry vehicle-bumblebee.yaml. |
| items-pickups | server/items/items.qc:setItemGroup + setItemGroupCount — clusters small health/armor/ammo within a 120qu radius into a shared item_group / item_group_count (networked) so the itemstime HUD shows one 'xN' badge per stacked-smalls cluster. No port code at all (no item_group field, no clustering pass in ItemstimeMutator). ADDED as new row items-pickups.itemgroup.networking (status: missing). |
| items-pickups | server/items/items.qc:generic_pickupevalfunc / weapon_pickupevalfunc / ammo_pickupevalfunc / healtharmor_pickupevalfunc — the per-item-class bot pickup-rating functions (assigned to .bot_pickupevalfunc, called from havocbot/roles.qc:170). The port re-implements the need-based rating heuristic in BotRoles.cs:266+ rather than porting these functions verbatim; arguably bot-unit scope, so NOT added as an items-pickups row, flagged here for registry coverage decision. |

## Fixes

| unit | feature | dimension | was -> now | reason |
| --- | --- | --- | --- | --- |
| turret-phaser | turret-phaser.firecheck.fireflag_guard | logic | partial -> faithful | Baseline recorded the fireflag state as tracked but NOT consulted by the firecheck gate (fire-block was purely the shot_refire AttackFinished hold, missing Base's fireflag==2 discharge window). Current TurretAI.RunCombat:719 reads `if (st.FireFlag != 0) return enemy;` in the fire gate, blocking fire across both the beam-active (FireFlag==1) and discharge (FireFlag==2) windows exactly as turret_phaser_firecheck. Gap resolved, verification flipped fail->pass. |
| turret-phaser | turret-phaser.fire.target_select_bias | values | partial -> faithful | Baseline recorded the phaser-specific scoring biases as omitted (defaulting to 1.0). PhaserTurret.Think (PhaserTurret.cs:88) now passes rangeBias:0.85, sameBias:0, angleBias:0.25, missileBias:0, playerBias:1 matching turrets.cfg:302-306 exactly; values consumed live in ScoreTarget/select (TurretAI.cs:435-443,468). |
| turret-phaser | turret-phaser.aim.head_track_tuning | values | partial -> faithful | Baseline recorded track_accel_rot/track_blendrate as omitted (falling back to defaults 0.5/0.35). PhaserTurret.Think (PhaserTurret.cs:89-90) now passes trackAccelPitch:0.5, trackAccelRot:0.65, trackBlendRate:0.2 matching turrets.cfg:317-320 exactly; consumed live in the TrackFluidInertia motor (TurretAI.cs:614-615). |
| turret-phaser | turret-phaser.aim.head_track_tuning | timing | partial -> faithful | Same fix: track_accel_rot (0.65) and track_blendrate (0.2) are now passed and drive the head-slew cadence/settle rate faithfully, where the baseline had them at default values producing a divergent tracking feel. |
| turret-walker | turret-walker.weapon.minigun | presentation | missing -> faithful | Attack (WalkerTurret.cs:582-603) now emits the EFFECT_BULLET tracer (FireBullet tracerEffect:"BULLET") AND EffectEmitter.Emit("BLASTER_MUZZLEFLASH", st.ShotOrg, dir*1000f, 1), both server-side networked effects matching walker_weapon.qc:22-23. Only divergence: emitted at the shot org, not a head muzzle tag (no tur_head sub-entity). |
| turret-walker | turret-walker.weapon.rocket | presentation | partial -> faithful | FireRocket (WalkerTurret.cs:551-578) now selects tag_rocket01/02 by post-decrement volly parity and resolves the muzzle via Api.Models.TryGetTag, with graceful st.ShotOrg fallback — the tag_rocket01/02 alternation (walker.qc:447) is now ported. (Base emits no EFFECT_BLASTER_MUZZLEFLASH for rockets, so the prior "no muzzleflash" gap was never real.) |
| turret-walker | turret-walker.drive.idle_roam | logic | partial -> faithful | The walker brain now consumes the turret_checkpoint waypoint chain: WalkerFindTarget (resolve entry checkpoint by targetname, wired from Spawn:167 + OnRespawn), WalkerMovePath (advance PathCurrent via CloseToTarget/NextCheckpoint), and the TSF_NO_PATHBREAK flag (TsfNoPathbreak, DoMovement:228). The TSL_ROAM roam-walk branch is also now plumbed (TslRoam-gated, IdleSelect:424). Both baseline gaps resolved. |
| vehicle-framework | vehicle-framework.presentation.hud_camera_xhair | presentation | partial -> partial | Wave 12a (commit 8563014) ported Vehicles_drawCrosshair (cl_vehicles.qc:293) — the centered per-mode reticle (VehicleHud.DrawMainReticle, vCROSS_BURST/vCROSS_RAIN by VehW2Mode) + the Raptor bomb dropmark (DrawBombDropmark, green-live tracetoss prediction via VehiclePhysics.BombDropPredict when reload2==1 / red 5s linger after a drop), fed live every frame by NetGame.FeedRaptorReticle (NetGame.cs:3306-3357). Status stays partial only because the cockpit/chase camera (SVC_SETVIEWPORT) is still dead and non-Raptor per-mode reticles are unwired, but the gap is materially narrowed (drawCrosshair was previously implicitly unported). |
| vehicle-raptor | raptor.takeoff | audio | missing -> faithful | The SND_VEH_RAPTOR_SPEED engine fly sound IS played in Takeoff (Raptor.cs:286-290) on the VehSoundNextTime gate (Time+7.955812s, SoundChannel.Item), matching QC raptor_takeoff CH_TRIGGER_SINGLE. Present since commit ff8a699 but the prior baseline still recorded audio:missing. |
| vehicle-raptor | raptor.flight_controller | audio | missing -> faithful | The SND_VEH_RAPTOR_SPEED engine loop IS played in Frame (Raptor.cs:417-421) on the same VehSoundNextTime gate, matching raptor_frame:158-164. Baseline recorded audio:missing. |
| vehicle-raptor | raptor.death | audio | missing -> faithful | The per-think 5% SND_ROCKET_IMPACT (weapons/rocket_impact.wav on CH_SHOTS) is now played during the death tumble (Raptor.cs:505-509), matching raptor_diethink. Baseline recorded audio:missing + the diethink-chance constant as NOT IMPLEMENTED. |
| vehicle-raptor | raptor.death | presentation | missing -> partial | EFFECT_EXPLOSION_MEDIUM (death blast, Raptor.cs:495) and the per-think 5% EFFECT_EXPLOSION_SMALL (Raptor.cs:508) are now emitted server-side via EffectEmitter (Send_Effect equivalent). Only the findbetterlocation surface-nudge + wreck model remain. Matches the sibling racer.death convention (missing->partial/faithful). |
| vehicle-raptor | raptor.bombs | presentation | missing -> partial | Wave-12a (commit 8563014) added EffectEmitter.Emit('RAPTOR_BOMB_SPREAD', origin, velocity) on cluster burst (Raptor.cs:617) — the particle half of DEATH_VH_RAPT_FRAGMENT. Only the CSQCProjectile bomb/bomblet trails + shell-fragment gibs remain client-only. |
| vehicle-spiderbot | vehicle-spiderbot.spawn.setup | values | partial -> faithful | mass=5000 is now modeled: Spiderbot.cs:119 sets vehicle.Mass=5000f, binding the Entity.Mass field (DamageEntityState.cs:128) that the blast-push pipeline reads at DamageSystem.cs:791. This was the last value-mismatch constant in the row (baseline had it match:false, status values:partial). All other setup constants already matched, so values rolls up to faithful. |
| fx-deathtypes | fx-deathtypes.message.special_nade_misc | liveness | partial -> live | DEATH_SHOOTING_STAR gained a live emitter: PlayerFrameLogic.FallDamage:464 emits DeathTypes.ShootingStar via Combat.Damage on the g_maxspeed over-speed guard, and FallDamage runs per-frame for every live player (GameWorld.cs:2107). It was the last selector-only/dead member of this family; the family is now fully live. |
| fx-deathtypes | fx-deathtypes.message.teamchange | liveness | partial -> partial | Manual DEATH_TEAMCHANGE went dead->live: a new Teamplay.KillPlayerForTeamChange (Teamplay.cs:809) emits DeathTypes.TeamChange and is called from GameWorld.cs:949/956/3253 + Teamplay.cs:425. (Liveness label stays partial because the dead member flipped to AUTOTEAMCHANGE — see the paired regression.) |
| fx-notifications | fx-notifications.net.shouldsend | logic | partial -> faithful | ServerNet.NotificationReaches (game/net/ServerNet.cs:2738) now implements the QC spectator-follow clause `IS_SPEC(to) && to.enemy == other` for ALL four spec-aware broadcast modes (One :2750, AllExcept :2746, Team :2759, TeamExcept :2767) via `p.IsObserver && p.Spectatee == d.Target`, including the TeamExcept `to.enemy != other` guard (`p.Spectatee != d.Target`). Added in Wave 12b commit 6da9e80 (2026-06-26), after the prior 2026-06-25 audit, so the baseline 'STILL OPEN, omits spec clause, result: fail' is now stale. Only residual is the benign non-Player-target edge case in TEAM/TEAM_EXCEPT. |
| fx-notifications | fx-notifications.net.kill | liveness | live -> live | Call-site coverage broadened from the baseline's 3 sites to 7 distinct live SendCenterKill callers: GameWorld.cs:864 (CPID_ROUND), :1914 (CPID_IDLING — NEW, baseline said idle not wired), :3810/:4491 (KillAll), Teamplay.cs:1080 (CPID_REMOVE — NEW), CampcheckMutator.cs:200 (CPID_CAMPCHECK), InstagibMutator.cs:392 (CPID_INSTAGIB_FINDAMMO — NEW, baseline said instagib find-ammo not wired). Status dimensions were already faithful/live; this narrows the documented breadth gap. Gap text + verification updated. |
| fx-sounds | fx-sounds.emit.sound_allowed | liveness | partial -> live | IsAllowed now calls WalkOwner internally (SoundSystem.cs:62) so the owner-walk re-home is live on every gated emit (was DEAD with zero callers), and the bot_sound_monopoly gate is now applied by the SoundSystem.PlayOn/PlayRaw/SpamSound family used at ~50 live call sites (was only the 3 vehicle paths). Both baseline 'dead/vehicle-only' gaps resolved. |
| fx-sounds | fx-sounds.emit.play2_family | liveness | partial -> partial | Play2Team went DEAD->LIVE: it is now wired to BOTH ONS underattack cues (OnslaughtControlPoint.cs:271 ONS_GENERATOR_UNDERATTACK, :613 ONS_CONTROLPOINT_UNDERATTACK) with the QC per-team roster filter, replacing the prior PlayGlobal per-team->global divergence. Many other play2 sites (UNAVAILABLE, doors, voicescript, target_print TALK, mapmover) are now wired. Dimension stays 'partial' overall only because MSG_ONE per-recipient targeting is still a broadcast approximation and vehicle lock cues remain unwired, but the underlying defect was fixed. |
| fx-sounds | fx-sounds.emit.play2_family | audio | faithful -> faithful | play2all port (PlayGlobal/PlayGlobalRaw) now honors QC play2all's `if(autocvar_bot_sound_monopoly) return;` gate (SoundSystem.cs:314/324/337); the baseline's NEW gap (global cues still played when bot_sound_monopoly=1) is closed and the play2all constant match goes false->true. Audio dimension was already 'faithful' so status value is unchanged, but the recorded gap/constant is corrected. |
| fx-sounds | fx-sounds.presentation.hitsound_sample | values | partial -> faithful | The mode 2/3 hitsound pitch curve was a linear damage->pitch ramp; ComputePitch (HitSound.cs:96-128) now implements QC's exact asymptotic gradient function (b*d*(a-1)+a*c*(1-b))/(d*(a-1)+c*(1-b)) with the mode-3 mirror (a-b)/2+b, bit-faithful to view.qc:937-949. pitch_formula constant match false->true. |
| items-pickups | items-pickups.loot.item_damage | logic | missing -> faithful | ItemDamage (ItemPickupRules.cs:889-895) now wired on loot via item.GtEventDamage = ItemPickupRules.ItemDamage (StartItem.cs:218) and dispatched live by DamageSystem.EventDamage for non-player edicts. DeathTypes.ItemDamageNeedKill matches the items.qh:123 NEEDKILL set. Added in commit 6da9e80 (Wave 12b), after the baseline. |
| items-pickups | items-pickups.loot.item_damage | liveness | na -> live | GtEventDamage seam is dispatched by DamageSystem.EventDamage (DamageSystem.cs:358-360) for any non-player edict — same live mechanism monsters/turrets/CTF-flag use. Loot in a lava/slime/swamp/hurt volume is now removed by damage. |
| items-pickups | items-pickups.target.use_setactive | logic | missing -> faithful | ItemUse + ItemSetActive (ItemPickupRules.cs:912/933) now wired: StartItem.SetupPermanent (l.298-300) sets item.Use=ItemUse on targetname'd items and item.SetActive=ItemSetActive + Active=ACTIVE_ACTIVE on all permanent items. Logic matches items.qc:987-1006 (spawnflags&16 immediate-touch / else Respawn; toggle/set + Show). Added in commit 6da9e80 (Wave 12b). |
| items-pickups | items-pickups.target.use_setactive | liveness | na -> live | Reachable via the live dispatch: MapObjectsCommon.UseTargets (l.447-465) invokes t.Use, and the relay path (LogicGates.cs:392) invokes trg.SetActive — so a trigger/relay firing an item's targetname spawns/toggles it. |
| items-pickups | items-pickups.team.finditem | logic | missing -> faithful | FindTeam (StartItem.cs:432-496) now wired at the spawn tail (StartItem.cs:168-172: if team!=0 FindTeam else Item_Reset) AND the Item_Touch team-respawn re-pick (ItemPickupRules.cs:436-467). Random-weighted (.cnt) member selection over Api.Entities.All, hide-the-rest, re-roll on respawn — matches items.qc:813-849 + 762-780. Added in commit 6da9e80 (Wave 12b). |
| items-pickups | items-pickups.team.finditem | liveness | na -> live | FindTeam is invoked directly from the live BSP-lump spawn driver (StartItem.SpawnInternal) and the touch-respawn re-pick runs on the live Item_Touch path over Api.Entities.All. |
| mutator-bloodloss | mutator-bloodloss.presentation.active_mutator_signalling | liveness | partial -> partial | BuildMutatorsPrettyString gained a THIRD live consumer since the baseline (which recorded two): SendMutatorsWelcome at GameWorld.cs:1387, called per-join at GameWorld.cs:1291, surfaces 'Active modifications: ^3<list>' to the connecting client (Base networks this in the CSQC Welcome dialog, client/main.qc:1438). Together with the 'modifications' serverinfo cvar (GameWorld.cs:1128) and the server-browser infostring (ServerNet.cs:608), all server-side textual outputs are now live. Cell value stays 'partial' only because the MUT_BLOODLOSS bitmask / mut_set_active HUD active-mutator ICON surface is still entirely unported. |
| mutator-buffs | buffs.replace_powerups | logic | missing -> faithful | BuffsMutator now subscribes MutatorHooks.FilterItem (Hook():107) and OnFilterItem (912-931) implements buff_SpawnReplacement: returns false in g_buffs<0 mode, gated on g_buffs_replace_powerups, replaces an item.Pickup.IsPowerup with a random buff. Baseline recorded port_refs:[] / missing. |
| mutator-buffs | buffs.replace_powerups | values | missing -> faithful | g_buffs_replace_powerups, the g_buffs<0 disable gate, and g_buffs_replace_available (re-roll unavailable type in ConfigureBuffEntity) are all now read and match Base. |
| mutator-buffs | buffs.replace_powerups | liveness | na -> live | StartItem.cs:119 calls MutatorHooks.FireFilterItem(item) on the real item-spawn path; OnFilterItem is subscribed. The g_buffs>0 'replace' obtain path is now reachable. |
| mutator-buffs | buffs.medic.regen | logic | partial -> faithful | The ad-hoc per-frame MedicTick is deleted; OnPlayerRegen (774-781) now scales the shared regen/limit/rot factors exactly like QC PlayerRegen (M_ARGV 1..4), consumed live by PlayerFrameLogic.Regen (cs:56-89). |
| mutator-buffs | buffs.medic.regen | values | partial -> faithful | g_buffs_medic_rot (0.2) is now applied via RotMod (was dropped/match:false); medic_max (1.5) -> LimitMod=MaxMod and medic_regen (1.7) -> RegenMod all match Base. |
| mutator-buffs | buffs.medic.regen | timing | partial -> faithful | Regen pacing/ceiling now follows QC's folded multipliers through the shared RotRegen loop instead of an ad-hoc heal toward a recomputed max; rot_mod gates rotFrameTime, max_mod scales stable set-points, limit_mod scales the cap. |
| mutator-bugrigs | mutator-bugrigs.serverinfo.mutator_strings | liveness | partial -> live | BuildMutatorsPrettyString chain now has a live consumer: SendMutatorsWelcome (GameWorld.cs:1387, dispatched at :1291 on the non-campaign client-connect tail) sends 'Active modifications: ^3<list>' to the joining client, plus the 'modifications' serverinfo cvar set at match start (:1128). Baseline gap 'pretty token dead' resolved. |
| mutator-bugrigs | mutator-bugrigs.player_physics.prevangles | logic | partial -> faithful | The missing disableclientprediction=2 write is now a VERIFIED intended no-op: the client predictor (EntityMovementStep) runs the same RaceCarPhysics via the shared MutatorHooks.PMPhysics chain (PlayerPhysics.cs:364,446-449), so prediction is already in lockstep with authority. Reclassified to intended_divergence:true; gap removed. |
| mutator-bugrigs | mutator-bugrigs.player_physics.prevangles | values | partial -> faithful | disableclientprediction constant mismatch is now classified as an intended, verified-inert divergence rather than an unintended value gap (the shared PMPhysics chain gives client prediction the same lockstep the DP flag would protect). |

## Reclassified

Neutral changes (no parity-direction change).

| unit | feature | dimension | was -> now | reason |
| --- | --- | --- | --- | --- |
| fx-sounds | fx-sounds (summary) | feature_count | 16 -> 15 | Registry self-consistency fix: the file has 15 feature rows but the summary asserted feature_count: 16 (a pre-existing mismatch carried in the baseline). Corrected to 15 to match the actual row count. |
