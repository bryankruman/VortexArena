# Parity drift report — 2026-06-25

**0 regressions · 233 fixes · 3 new gaps · 3 newly-unmapped Base features · 17 reclassified** across 38 mutator units (all units re-wrote their registry files). The whole delta is Wave-6b (commit `f86d947`) landing mutator gameplay-logic ports live. No core-gameplay parity went backwards; the only new gaps are dead pretty-string chains and one inert chat-typing carrier.

## Regressions

No drift detected. (No `kind=regression` change in this diff — nothing got worse.)

## New gaps

| unit | feature | dimension | was → now | reason |
| --- | --- | --- | --- | --- |
| mutator-campcheck | campcheck.realclient.gate | liveness | live → partial | Adversarial re-trace found the chat-typing exemption is INERT end-to-end: `player.ButtonChat` is fed from `input.Typing` (PlayerPhysics.cs:213), but `ServerNet.ToMovementInput` (game/net/ServerNet.cs:1439) never sets `Typing` and `InputButtons` (src/XonoticGodot.Net/InputCommand.cs:11-22) has no Chat bit, so the wire never carries the typing flag and `ButtonChat` is permanently false for real network clients. The gate term runs but its carrier signal is not live, so a typing player is still not actually exempted in multiplayer. |
| mutator-invincibleproj | mutator-invincibleproj.string.pretty | liveness | na → dead | `MutatorActivation.BuildMutatorsPrettyString` (MutatorActivation.cs:121) has ZERO callers anywhere in src+game. The override + chain exist but nothing invokes the chain, so the scoreboard/server-browser pretty active-mutators label is still never assembled. The QC consumer (server/client.qc:1107) is unported. Code-present but dead. |
| mutator-melee_only | melee_only.ui.mutator_string_pretty | liveness | na → dead | The `BuildMutatorsPrettyString` override now exists but `MutatorActivation.BuildMutatorsPrettyString` has no external caller (grep confirms zero callers across src/), so the contribution is dead code. Baseline had liveness=na (no code); now there is code with no live caller. |

Note: the two `na → dead` rows are net-new code that is wired but unconsumed (an honest downgrade from "feature absent" to "feature present-but-dead"), not a behavioral regression. The campcheck row is a genuine liveness downgrade surfaced by adversarial re-trace — the gate logic was ported faithful, but the typing carrier signal it depends on was found to be unwired on the network path. This same dead `BuildMutatorsPrettyString` chain affects many other units' pretty tokens (recorded as `partial` liveness there); these two are the only rows whose pretty path is the entire feature and so flip to `dead`.

## Registry coverage gaps

These Base features had no registry row; rows were added (status all-missing / liveness na) during this pass.

| unit | newly-mapped Base feature |
| --- | --- |
| mutator-nades | `mutator-nades.display.mutators_string` — `BuildMutatorsString` (`":Nades"`, sv_nades.qc:954) + `BuildMutatorsPrettyString` (`", Nades"`, sv_nades.qc:949). `NadesMutator` does not override `MutatorBase.BuildMutatorsString/BuildMutatorsPrettyString` (GameplayBases.cs:213/221), so the nades mutator is omitted from the machine mutators string and the human-readable active-mutators list (server browser / scoreboard / `g_mutatorblacklist` token). Other mutators (BloodlossMutator.cs:104/107) DO override these. Trivial display-only gap, no gameplay effect. |
| mutator-pinata | `mutator-pinata.report.mutators_pretty_string` — split from the former combined `report.mutators_string` row. `BuildMutatorsPrettyString` (`", Piñata"`) is now ported (override + `MutatorActivation.BuildMutatorsPrettyString` chain runner) with faithful logic/values, but the chain runner has NO external live caller (no GameWorld/scoreboard/map-info consumer), so liveness is DEAD. Added as a new feature row. |
| mutator-walljump | `walljump.qc` MENUQC `METHOD(MutatorWallJump, describe)` — the in-menu mutator description text ("enables jumping off walls for added mobility…"). Pure MENUQC tutorial/UI string with no gameplay effect; consistent with other mutator units the registry does not carry a row for it. Listed only for registry-coverage completeness. |

## Fixes

Parity that improved since the baseline (233 dimension transitions). Grouped by unit.

### mutator-bloodloss
| feature | dimension | was → now | reason |
| --- | --- | --- | --- |
| presentation.active_mutator_signalling | logic | missing → partial | Wave-6b added `BuildMutatorsString` (`:bloodloss`) and `BuildMutatorsPrettyString` (`, Blood loss`) overrides (BloodlossMutator.cs:104-107) plus a MutatorActivation aggregator. Baseline recorded the whole row missing with empty port_refs. |
| presentation.active_mutator_signalling | values | missing → faithful | The `:bloodloss` machine token and `, Blood loss` pretty token exactly match Base. Only the MUT_BLOODLOSS active-mutator id remains unported. |
| presentation.active_mutator_signalling | liveness | na → partial | `BuildMutatorsString` is LIVE via GameWorld.cs:876 (gamelog init, sv_eventlog-gated). `BuildMutatorsPrettyString` aggregator (MutatorActivation.cs:121) has no caller and is dead; CSQC HUD-icon row still missing. |

### mutator-breakablehook
| feature | dimension | was → now | reason |
| --- | --- | --- | --- |
| breakablehook.owner.punish_splash | logic | partial → faithful | The Wave-2 teamplay regression is fixed (f86d947). `DiffTeam` no longer keys off the dead static `GameScores.Teamplay`; it is now `!ReferenceEquals(a,b) && !Teams.SameTeam(a,b)`, reading live authoritative `Entity.Team`. Exhaustive truth-table verification confirms all four QC DIFF_TEAM rows now match Base (teamplay same-team no longer wrongly punishes). |

### mutator-buffs
| feature | dimension | was → now | reason |
| --- | --- | --- | --- |
| buffs.drop | logic | missing → faithful | `OnPlayerUseKey` handler now exists, gated on `g_buffs_drop`, mirrors Base PlayerUseKey (ITEM_BUFF_DROP center + INFO_ITEM_BUFF_LOST + RemoveAllBuffs + pickup-shield + return true). |
| buffs.drop | liveness | na → live | PlayerUseKey hook subscribed and fired live via `MutatorHooks.FirePlayerUseKey` at VehicleBoarding.cs:106 (the real +use seam). |
| buffs.drop | audio | missing → faithful | SND_BUFF_LOST now played on drop. |
| buffs.drop | values | partial → faithful | `g_buffs_drop` read and `g_buffs_pickup_delay` (0.7) applied to post-drop BuffShield; both match Base. |
| buffs.mutator_string | logic | missing → faithful | `BuildMutatorsString/PrettyString` overrides added, appending `:Buffs`/`, Buffs` only when `g_buffs>0`. |
| buffs.mutator_string | values | missing → faithful | Token `:Buffs`/`, Buffs` and the `g_buffs>0` gate match Base exactly. |
| buffs.mutator_string | liveness | na → live | Live via `MutatorActivation.BuildMutatorsString` at GameWorld.cs:876. |
| buffs.notifications | logic | partial → faithful | Pickup-to-others (INFO_ITEM_BUFF), replace-loss, and drop notifications now emitted by `BuffTouch`/`OnPlayerUseKey`. |
| buffs.notifications | presentation | partial → faithful | All four notification tokens defined and now emitted on the correct events. |
| buffs.item.touch_pickup | logic | partial → faithful | `cl_buffs_autoreplace` now honored; team_forced + vehicle + lifetime-zero-on-pickup checks added. |
| buffs.item.touch_pickup | audio | partial → faithful | Pickup sound (misc/shield_respawn) + notifications cover the Base audio/notify set. |
| buffs.ammo | logic | partial → faithful | clip_load handling added (FillSlotClip on apply+tick, RestoreAmmoClips on remove). Reload-weapon gap closed. |
| buffs.item.available | logic | partial → faithful | start_items IT_UNLIMITED_AMMO exclusion now modelled, so the ammo buff is excluded when ammo is already unlimited. |
| buffs.magnet | logic | partial → faithful | Reach test now uses expanded box-overlap matching QC `boxesoverlap`, with origin-distance fallback only when bounds degenerate. |
| buffs.flight | logic | partial → faithful | Gravity-trigger restore on removal now modelled (`GravityCheck.Enemy.Gravity` else saved old gravity). |

### mutator-bugrigs
| feature | dimension | was → now | reason |
| --- | --- | --- | --- |
| clientconnect.chase_camera | logic | missing → faithful | Implements `OnClientConnect` (BugrigsMutator.cs:120), dispatched live from GameWorld.cs:1011 gated on `!p.IsBot && Added`. |
| clientconnect.chase_camera | values | missing → faithful | `chase_active` set to 1 for the connecting human, matching Base stuffcmd. |
| clientconnect.chase_camera | presentation | missing → faithful | Client view reads `chase_active` each frame, so setting it to 1 produces the forced 3rd-person view. |
| clientconnect.chase_camera | liveness | na → live | `OnClientConnect` invoked on the live connect path. Marked intended_divergence (direct cvar set vs networked stuffcmd). |
| serverinfo.mutator_strings | logic | missing → faithful | Overrides `BuildMutatorsString`/`BuildMutatorsPrettyString` (BugrigsMutator.cs:105/108). |
| serverinfo.mutator_strings | values | missing → faithful | Tokens match Base: `:bugrigs` and `, Bug rigs`. |
| serverinfo.mutator_strings | presentation | missing → faithful | Machine token rendered in the gamelog mutator list (live at GameWorld.cs:876). |
| serverinfo.mutator_strings | liveness | na → partial | `BuildMutatorsString` live; `BuildMutatorsPrettyString` has NO live caller — `, Bug rigs` value-correct but never displayed. |

### mutator-campcheck
| feature | dimension | was → now | reason |
| --- | --- | --- | --- |
| realclient.gate | logic | partial → faithful | The typecheck/chat term `(TypeCheck \|\| !player.ButtonChat)` and `!WeaponLocked(player)` are now both PRESENT (CampcheckMutator.cs:118-119), matching the QC guard chain. `g_campcheck_typecheck` now genuinely consulted. |
| realclient.gate | values | partial → faithful | With both terms wired, the gate constants are consistent with Base; two previously match:false constants now structurally present. |
| lifecycle.misc | logic | partial → faithful | `BuildMutatorsString` `:CampCheck` advertise now implemented + dispatched. Remaining unported CopyBody clone-origin reclassified as intended_divergence (port reuses the player edict; no corpse clone). |
| lifecycle.misc | presentation | partial → faithful | Both presentation-facing sub-features (PlayerDies centerprint-clear + `:CampCheck` advertise) now live. |
| lifecycle.misc | liveness | partial → live | `BuildMutatorsString` now live (GameWorld.cs:876) joining the already-live PlayerDies centerprint-clear. |

### mutator-cloaked
| feature | dimension | was → now | reason |
| --- | --- | --- | --- |
| report.mutators_pretty_string | logic | missing → faithful | Overrides `BuildMutatorsPrettyString` (CloakedMutator.cs:73-78), returning `s + ', Cloaked'` with the `!g_cts` suppression gate. |
| report.mutators_pretty_string | values | missing → faithful | The append literal `, Cloaked` and the `g_cts` gate match Base exactly. |
| report.mutators_pretty_string | presentation | missing → faithful | The pretty-string contribution is now produced; token matches Base. |
| menu.metadata | logic | missing → faithful | Create-game checkbox wired (DialogMutators.cs:131-132) matching Base dialog. |
| menu.metadata | values | missing → faithful | Checkbox cvar `g_cloaked` and label "Cloaked" match Base. |
| menu.metadata | presentation | missing → partial | Checkbox + label + tooltip present and live; only the in-game describe-page text remains unported (general menu-describe gap). |
| menu.metadata | liveness | na → live | The checkbox is live in the multiplayer create-game mutators dialog. |

### mutator-damagetext
| feature | dimension | was → now | reason |
| --- | --- | --- | --- |
| sv.first_hit_bitset | logic | partial → faithful | The ClientDisconnect `dent_attackers` clear is now implemented and live (new `MutatorHooks.ClientDisconnect`, dispatched ClientManager.cs:707; `OnClientDisconnect` removes the leaving client from every live player's DentAttackers set). |
| cl.draw_fade_shrink_move | values | partial → faithful | World drift now placed along the full view basis (fwd/right/up) from velocity_world + offset_world with QU→m conversion. Prior Z-only hardcoded-rise gap gone. |
| cl.draw_fade_shrink_move | presentation | partial → partial | Color codes + font now faithful (HudText.Parse in Xolonium, centered by parsed-run width). Stays partial only for the QC drawfontscale crisp-cell residual. |
| ui.settings_tab | logic | missing → faithful | Damage Text settings sub-tab ported as `DialogSettingsGameDamageText`, faithfully building every widget in QC order. |
| ui.settings_tab | values | missing → faithful | All slider ranges/steps + the mixed Never(0)/range/Always(-1) endpoints match ui_damagetext.qc exactly. |
| ui.settings_tab | presentation | missing → faithful | Tab renders live with color picker + per-weapon override gate. |
| ui.settings_tab | liveness | na → live | Tab instantiated and added to the live Game-settings tab list (DialogSettingsGame.cs:42). |

### mutator-dodging
| feature | dimension | was → now | reason |
| --- | --- | --- | --- |
| lifecycle.reset | logic | partial → faithful | `MutatorHooks.MakePlayerObserver` HookChain now exists and Dodging subscribes; both Base reset triggers (PlayerSpawn + MakePlayerObserver) now ported. |
| lifecycle.reset | liveness | partial → live | MakePlayerObserver now dispatched live from ClientManager.cs:480. Both reset paths reachable. |
| anim.jump_action | presentation | missing → partial | The dodge does UNSET_ONGROUND + up-impulse and the client reconstructs a jump pose for any `!onGround` player. Baseline "a dodge shows no jump pose" no longer accurate; only the explicit `animdecide_setaction` one-shot remains absent. |

### mutator-doublejump
| feature | dimension | was → now | reason |
| --- | --- | --- | --- |
| stat.per_player_doublejump | logic | partial → faithful | Now consumes the per-player resolved DOUBLEJUMP stat (`mp.DoubleJump` → `PlayerJumpArgs`) instead of gating on the raw `sv_doublejump` cvar, matching Base PHYS_DOUBLEJUMP(player). |
| stat.per_player_doublejump | liveness | partial → live | The per-player stat is wired all the way into the live mutator hook; the g_physics_clientselect override path is honored on the real jump path. |

### mutator-dynamic_handicap
| feature | dimension | was → now | reason |
| --- | --- | --- | --- |
| triggers.recompute | logic | partial → faithful | Wave-6b ported all 4 Base recompute hooks (ClientDisconnect, MakePlayerObserver, and GameScores post-write AddedPlayerScoreHook filtered to SP_SCORE). Baseline had only PlayerSpawn/PlayerDies. |
| triggers.recompute | timing | partial → faithful | Score deltas now re-handicap instantly via the live post-write hook + roster shrink; the mean is no longer stale until next spawn/death. |
| apply.invalid_value_guard | logic | partial → faithful | `SetForcedHandicap` now exists with the exact value<=0 Log.Fatal guard (== QC error()) + HANDICAP_DISABLED early-return. |
| present.handicap_level | logic | missing → faithful | `UpdateHandicapLevel` computes `floor(map_bound_ranges((take+give)/2,1,2.0,0,16))` with HANDICAP_DISABLED→0 branch, matching handicap.qc:104-115. |
| present.handicap_level | values | missing → faithful | HANDICAP_MAX_LEVEL_EQUIVALENT=2.0 and the 0..16 range match Base; MapBoundRanges matches map_bound_ranges. |
| present.handicap_level | liveness | na → live | HandicapLevel computed on the live SetForcedHandicap path. |
| present.mutator_string | logic | missing → faithful | Both `BuildMutatorsString` (`:handicap`) and `BuildMutatorsPrettyString` (`, Dynamic handicap`) overrides now exist. |
| present.mutator_string | values | missing → faithful | Token strings match Base exactly. |
| present.mutator_string | presentation | missing → partial | Machine `:handicap` live (GameWorld.cs:876); `, Dynamic handicap` pretty has no live caller. |
| present.mutator_string | liveness | na → partial | BuildMutatorsString chain live; BuildMutatorsPrettyString has zero call sites. |

### mutator-globalforces
| feature | dimension | was → now | reason |
| --- | --- | --- | --- |
| advertise.mutatorstring | logic | missing → faithful | `BuildMutatorsString/PrettyString` now implemented port-wide; MutatorActivation runs the chain over active mutators. Baseline claimed NO chain existed. |
| advertise.mutatorstring | values | missing → faithful | Emits bit-exact Base tokens `:GlobalForces` and `, Global forces`. |
| advertise.mutatorstring | liveness | na → partial | Machine token LIVE via GameWorld.cs:876; pretty-string chain wired but no live Server caller. |

### mutator-hook
| feature | dimension | was → now | reason |
| --- | --- | --- | --- |
| info.mutator_string | logic | missing → faithful | `BuildMutatorsString` appends `:grappling_hook`; aggregator called live from gamelog init. |
| info.mutator_string | values | missing → faithful | Both tokens match Base: `:grappling_hook` and `, Hook`. |
| info.mutator_string | presentation | missing → partial | Info token live; `, Hook` pretty/votescreen token aggregator has no live caller (QC votescreen path unwired). |
| startitems.fuel | values | partial → faithful | StartLoadout gained `WarmupAmmoFuel`; `OnSetStartItems` sets it and warmup spawn consumes it. warmup_start_ammo_fuel omission resolved. |

### mutator-instagib
| feature | dimension | was → now | reason |
| --- | --- | --- | --- |
| enable.predicate | logic | partial → faithful | IsEnabled now reads `g_instagib != 0 && g_weaponarena == 0`, closing the console double-activation hole. |
| enable.predicate | values | missing → faithful | enable_cvar now matches Base's `g_instagib && !m_weaponarena` (was `g_instagib != 0` only). |
| countdown.announce | presentation | partial → faithful | StopCountdown now issues `SendCenterKill(CPID_INSTAGIB_FINDAMMO)`, retracting the lingering FINDAMMO/DOWNGRADE centerprint. |
| lifecycle.stop_countdown | logic | missing → faithful | MakePlayerObserver ported; MatchEnd covered by the per-tick game_stopped stop condition. |
| lifecycle.stop_countdown | liveness | na → live | `OnMakePlayerObserver` subscribed + dispatched live at ClientManager.cs:480. |
| misc.strings_monsters | logic | missing → partial | `BuildMutatorsString` overridden + LIVE (`:instagib`); pretty has no live caller and SetModname/MonsterDrop/MonsterSpawn/RandomItems remain missing. |
| misc.strings_monsters | presentation | missing → partial | `:instagib` machine token reaches output live; `, InstaGib` pretty dead; Mage skin / modname still missing. |
| misc.strings_monsters | values | missing → partial | Token constants match Base; monster-drop/random-items picks unported. |
| misc.strings_monsters | liveness | na → partial | BuildMutatorsString live; BuildMutatorsPrettyString dead. Mixed = partial. |

### mutator-invincibleproj
| feature | dimension | was → now | reason |
| --- | --- | --- | --- |
| string.machine | logic | missing → faithful | `BuildMutatorsString` override emits `:InvincibleProjectiles`, wired through the chain. |
| string.machine | values | missing → faithful | Token matches Base byte-for-byte. |
| string.machine | liveness | na → live | The chain now has a live caller at GameWorld.cs:876; chain includes only Added mutators, mirroring QC. |
| string.pretty | logic | missing → faithful | `BuildMutatorsPrettyString` override + chain method exist; code is correct. |
| string.pretty | values | missing → faithful | Label `, Invincible Projectiles` matches Base byte-for-byte. |
| string.pretty | presentation | missing → faithful | The pretty display-string contribution is now ported with the correct token. |

### mutator-kick_teamkiller
| feature | dimension | was → now | reason |
| --- | --- | --- | --- |
| detect.playtime | logic | partial → faithful | Ports per-client `Player.StartPlayTime`, stamped on the spectator→player edge; mutator computes `Time - StartPlayTime` (was absolute Time). |
| detect.playtime | values | partial → faithful | playtime source matches Base: seconds since the spectator→player switch. Constants flipped false→true. |
| detect.playtime | timing | partial → faithful | Rate denominator now per-client; threshold trips at the same cadence as Base for mid-match joiners. |
| detect.threshold | timing | partial → faithful | The only timing-dependent input (playtime) is now faithful, so the threshold result is no longer skewed. |

### mutator-melee_only
| feature | dimension | was → now | reason |
| --- | --- | --- | --- |
| ui.mutator_string_machine | logic | missing → faithful | Overrides `BuildMutatorsString` returning `s + ":MeleeOnly"`. |
| ui.mutator_string_machine | values | missing → faithful | Machine tag `:MeleeOnly` contributed, exact match. |
| ui.mutator_string_machine | presentation | missing → faithful | `:MeleeOnly` reaches the server mutators field via the live chain. |
| ui.mutator_string_machine | liveness | na → live | Chain invoked at GameWorld.cs:876 (GameLogInit, sv_eventlog-gated). |
| ui.mutator_string_pretty | logic | missing → faithful | Overrides `BuildMutatorsPrettyString` returning `s + ", Melee only Arena"`. |
| ui.mutator_string_pretty | values | missing → faithful | Pretty label `, Melee only Arena` contributed, exact match. |

### mutator-midair
| feature | dimension | was → now | reason |
| --- | --- | --- | --- |
| bot.disable_bunnyhop | logic | partial → faithful | Separate `BotMoveSkill` field (Player.cs:92) mirrors QC `.bot_moveskill`; OnPlayerSpawn zeroes ONLY BotMoveSkill (not BotSkill), and the gate is now `Skill + MoveSkill < skillOffset`. Resolves both prior logic gaps (whole-skill nuke + always-false gate). |
| bot.disable_bunnyhop | values | partial → faithful | bot_moveskill + the bunnyhop gate term both flipped match:false → match:true. |

### mutator-multijump
| feature | dimension | was → now | reason |
| --- | --- | --- | --- |
| jump.client_gate | logic | partial → faithful | `cl_multijump` added to SentCvarAllowlist (Commands.cs:198) so CmdSentCvar stores the replicated value instead of dropping it; opt-out(0)/cap(>1)/default(-1) gate now operates on real values. |
| jump.client_gate | values | partial → faithful | `cl_multijump` -1/0/2+ now round-trip through the per-client store. |
| jump.client_gate | presentation | missing → faithful | The per-client opt-in/out/cap path is now end-to-end wired. |
| jump.client_gate | liveness | partial → live | `ClientMultijumpProvider` assigned in the live Commands ctor + now reads a store that actually receives the value. |
| presentation.mutator_list_string | logic | missing → faithful | `BuildMutatorsString/PrettyString` overrides + central aggregators added. |
| presentation.mutator_list_string | values | missing → faithful | Exact tokens `:multijump` and `, Multi jump` produced. |
| presentation.mutator_list_string | presentation | missing → faithful | Both tags produced; machine tag consumed live by gamelog init. |
| presentation.mutator_list_string | liveness | na → partial | `:multijump` consumed live; `, Multi jump` pretty aggregator has no live consumer. |

### mutator-nades
| feature | dimension | was → now | reason |
| --- | --- | --- | --- |
| lifecycle.cleanup | liveness | partial → partial | Wave-6b added MakePlayerObserver + ClientDisconnect subscriptions wired to RemovePlayer (faithful port of nades_RemovePlayer). Both hook chains have live callers (ClientManager.cs:480/707). Held-nade/bonus/spawn-marker leaks on observer + disconnect now closed; value stays partial but two previously-dead paths are now live. |
| lifecycle.cleanup | logic | partial → partial | RemovePlayer now implements nades_RemovePlayer on the observer/disconnect paths. Dimension stays partial because four hooks (reset_map_global, PutClientInServer, SpectateCopy, DropSpecialItems) still have no port HookChain, but the observer/disconnect logic is now present and faithful. |

### mutator-new_toys
| feature | dimension | was → now | reason |
| --- | --- | --- | --- |
| mapreplace.set_weaponreplace | logic | missing → faithful | Wave-6b ported the SetWeaponreplace hook (map-key-or-auto pick, random_items early-out, W_Apply_Weaponreplace post-pass). |
| mapreplace.set_weaponreplace | values | missing → faithful | Map `new_toys` BSP key parsed onto Entity.NewToys + auto-mapping wired; replacement tokens resolve. |
| mapreplace.set_weaponreplace | timing | missing → faithful | Replacement runs at map weapon spawn time before regular weaponreplace. |
| mapreplace.set_weaponreplace | presentation | missing → faithful | The actually-spawned replacement weapon now appears. |
| mapreplace.set_weaponreplace | liveness | na → live | FireSetWeaponreplace called from WeaponSpawn on the live map-load path. |
| pickup.roflsound | logic | missing → faithful | Ported the FilterItem pickup-sound override (gated on IsWeaponPickup + IsNewToy + g_new_toys_use_pickupsound). |
| pickup.roflsound | values | missing → faithful | `g_new_toys_use_pickupsound` read; WEAPONPICKUP_NEW_TOYS resolves to the roflsound. |
| pickup.roflsound | presentation | missing → faithful | The override is now selected and applied via Entity.ItemPickupSoundOverride. |
| pickup.roflsound | audio | missing → faithful | PlayPickupSound reads ItemPickupSoundOverride first, so the roflsound plays. |
| pickup.roflsound | liveness | na → live | FilterItemDefinition fires for every spawned item; PlayPickupSound runs on the real pickup path. |
| replace.mapping | liveness | partial → live | The map-replace consumer now exists, backing BOTH Base call sites (start-items rearrange + map weapon_* replacement). |

### mutator-nix
| feature | dimension | was → now | reason |
| --- | --- | --- | --- |
| enable.gate | logic | partial → faithful | IsEnabled now adds the instagib/overkill/weaponarena exclusion via cvar stand-in. Marked intended_divergence. |
| remove.restore | logic | partial → faithful | Unhook restores the real start_ammo_*/start_weapons loadout via ComputeStartItems, not a blaster-only best-effort. |
| remove.restore | values | partial → faithful | Restored resources sourced from the same ComputeStartItems seam, so numeric start_ammo_* now match Base. |
| rotation.engine | logic | partial → partial | Reloadable full-clip prefill ported, closing half the omission. Still partial: `wpn.wr_resetplayer` not called on resync. |
| ammo.refill_trickle | logic | partial → faithful | IT_UNLIMITED_AMMO branch ported: fills to max and skips the trickle. |
| weaponset.force | logic | partial → faithful | Per-frame switch now uses QC's switch-only-when-target-not-owned guard, making a manual mid-round switch sticky. |
| spawn.loadout | logic | partial → faithful | OnPlayerSpawn sets `Items |= IT_UNLIMITED_SUPERWEAPONS`; the live SuperweaponTimeout pass honors it. |
| entity.target_items | logic | missing → faithful | OnEntityPreSpawn hook chain + a live caller (GameWorld.cs:2915) now exist; NixMutator deletes target_items. |
| entity.target_items | liveness | na → live | FireOnEntityPreSpawn called live in GameWorld map spawn and removes the edict on a true return. |
| modname.strings | logic | missing → partial | Overrides `BuildMutatorsString` (`:NIX`, live) + `BuildMutatorsPrettyString` (`, NIX`). Still partial: pretty has no consumer and SetModname absent. |
| modname.strings | liveness | na → partial | BuildMutatorsString half live; pretty override has no caller + SetModname unported. |

### mutator-offhand_blaster
| feature | dimension | was → now | reason |
| --- | --- | --- | --- |
| fire.offhand_think | timing | partial → faithful | The residual `WeaponUseForbidden` stub is wired live: delegates to RoundFireForbidden set to `Rounds is { IsRoundStarted: false }`, zeroing the offhand button during the inter-round grace. Only the unused ForbidWeaponUse mutator-hook half remains. |
| report.mutators_string | logic | missing → faithful | `BuildMutatorsString` ported + run via the chain. |
| report.mutators_string | values | missing → faithful | Machine `:offhand_blaster` + pretty `, Offhand blaster` match Base exactly. |
| report.mutators_string | presentation | missing → faithful | Mutator-list contribution produced; pretty override exists. |
| report.mutators_string | liveness | na → partial | Machine string LIVE (GameWorld.cs:876); pretty chain has no live caller. |

### mutator-overkill
| feature | dimension | was → now | reason |
| --- | --- | --- | --- |
| enable.predicate | logic | partial → faithful | IsEnabled now applies all three Base guards (`!OtherEnabled("instagib")`, `g_weaponarena==0`, `g_mod_balance=="Overkill"`). Double-activation gap closed. |
| randomitems.inject_ok_items | logic | missing → partial | OnRandomItemsGetClassName implements the OK-pool weighted reservoir. Partial only because the pool is a hard-coded NetName list, not the Base IntrusiveList. |
| randomitems.inject_ok_items | values | missing → faithful | Item set + probability cvars now read + weighted; matches Base. |
| randomitems.inject_ok_items | liveness | na → partial | The mod-injection hook chain exists + is fired on the live spawn/replace paths. Partial: reachable only when random_items also active. |
| naming.mod_strings | logic | missing → partial | Overrides `BuildMutatorsString` (`:OK`) + `BuildMutatorsPrettyString` (`, Overkill`). SetModname + cl_overkill sync still absent. |
| naming.mod_strings | values | missing → partial | `:OK` + `, Overkill` match Base; modname "Overkill" still absent (no SetModname chain). |
| naming.mod_strings | presentation | missing → partial | `:OK` machine token emitted on the LIVE chain; `, Overkill` pretty has no live consumer. |
| naming.mod_strings | liveness | na → partial | BuildMutatorsString live; pretty chain no caller; SetModname/cl_overkill absent. |

### mutator-pinata
| feature | dimension | was → now | reason |
| --- | --- | --- | --- |
| death.throw_impulse | values | partial → faithful | `Prandom.Vec()` rewritten to rejection-sample the unit ball, matching DarkPlaces `randomvec()` #91 distribution + the QC prandomvec fallback. Cube-distribution gap resolved. |
| report.mutators_string | logic | missing → faithful | Port now has the `BuildMutatorsString` virtual + chain runner + pinata override returning `s + ':Pinata'`. Row narrowed to the machine-string hook only. |
| report.mutators_string | values | missing → faithful | Appends the exact `:Pinata` token. |
| report.mutators_string | liveness | na → live | GameWorld.cs:876 builds the gamelog `:gameinfo:mutators:` line; Pinata's token contributes on the live path. |
| menu.metadata | logic | missing → faithful | DialogMutators.cs:223 adds a `g_pinata` checkbox with a Dependent.Bind InstaGib compatibility gate. |
| menu.metadata | values | missing → faithful | Label "Piñata" + Base describe text verbatim, bound to `g_pinata`. |
| menu.metadata | presentation | missing → faithful | The Piñata toggle renders; describe via tooltip. Residual: no PAGE_TEXT panel + simplified InstaGib-only binding (minor). |

### mutator-powerups
| feature | dimension | was → now | reason |
| --- | --- | --- | --- |
| speed.attack_rate | timing | partial → faithful | The 6 weapons that bypassed the Speed hook (Electro, Machinegun, OkHmg, OkMachinegun, Rifle, Shotgun) now ALL call the hooked `WeaponRateFactor(actor)` overload. Worst gap closed; every weapon now fires/reloads faster under Speed. |
| speed.attack_rate | liveness | partial → live | The WeaponRateFactor hook now applies to every weapon's animtime/refire path. |
| strength.fire_sound | logic | missing → faithful | `W_PlayStrengthSound` ported + subscribed; the anti-spam branch is bit-faithful (play if now>prevSound+antispam OR now>prevAttempt+refire_threshold, then stamp both). |
| strength.fire_sound | values | missing → faithful | `sv_strengthsound_antispam_time` (0.1) + `..refire_threshold` (0.04) read + applied; match Base. |
| strength.fire_sound | timing | missing → faithful | Anti-spam timer model modeled per player; per-pellet shotgun calls collapse to one cue. |
| strength.fire_sound | audio | missing → faithful | SND_STRENGTH_FIRE now actually played when firing while Strength is active. |
| strength.fire_sound | liveness | na → live | FireWPlayStrengthSound called per bullet from WeaponFiring.FireBullet — the live weapon-fire path. |
| invisibility.stealth | logic | missing → partial | Bot stealth (Bot_ForbidAttack) + monster stealth (MonsterValidTarget) ported + subscribed. Only the radar piece (CustomizeWaypoint) remains unported. |
| invisibility.stealth | liveness | na → live | FireBotForbidAttack consulted at BotBrain.cs:800 + FireMonsterValidTarget at MonsterAI.cs:586. Bots no longer shoot, monsters no longer acquire an invisible player. |
| drop.ondeath_onuse | logic | partial → partial | The +use drop (PlayerUseKey hook, g_powerups_drop) is now wired + live (VehicleBoarding.cs:106). Stays partial only because the WP_Item waypoint sprite + ItemTouched waypoint-kill remain unported. |
| obituary_mutator_strings | logic | missing → partial | Overrides `BuildMutatorsString` (`:no_powerups`/`:powerups`) + `BuildMutatorsPrettyString` (`, No powerups`/`, Powerups`), run live. The death-log S/I item codes remain unported. |
| obituary_mutator_strings | presentation | missing → partial | Server-browser/scoreboard tokens emitted; death-log item codes remain unported. |
| obituary_mutator_strings | liveness | na → live | BuildMutatorsString/PrettyString run live via the chain + votescreen pretty-string chain. |
| invisibility.alpha | presentation | partial → partial | One of three gaps closed: on-lapse restore now uses `DefaultPlayerAlpha` (composing with Cloaked/Running-Guns) instead of hardcoded 1f. Stays partial: exterior-weapon-entity alpha + the `if(!actor.vehicle)` guard remain unported. |

### mutator-random_gravity
| feature | dimension | was → now | reason |
| --- | --- | --- | --- |
| enable.cvar_gate | logic | partial → faithful | MUTATOR_ONADD `cvar_settemp("sv_gravity", …)` now modelled + wired to the server's settemp restore stack. Only enable-side gap closed. |
| roll.round_gate | logic | missing → faithful | The round-handler gate is now implemented (`RoundGateBlocks` → `RoundNotStartedProvider`), matching sv_random_gravity.qc:30. |
| roll.round_gate | timing | missing → faithful | Gate ordered after game_starttime exactly as QC; pre-round window suppressed in round modes. |
| roll.round_gate | liveness | na → partial | The seam was added + wired live (GameWorld.cs:1134, nulled on teardown). Live in round-based gametypes only, matching QC — correctly scoped partial. |
| advertise.mutator_string | logic | missing → faithful | `BuildMutatorsString` + `BuildMutatorsPrettyString` implemented with exact Base tokens. |
| advertise.mutator_string | values | missing → faithful | `:RandomGravity` / `, Random gravity` match Base byte-for-byte. |
| advertise.mutator_string | presentation | missing → faithful | The mutator contributes its tokens; machine token reaches gamelog/server-info live. |
| advertise.mutator_string | liveness | na → partial | `:RandomGravity` live; BuildMutatorsPrettyString chain has NO live caller port-wide. |

### mutator-random_items
| feature | dimension | was → now | reason |
| --- | --- | --- | --- |
| engine.mod_injection_hook | logic | missing → faithful | GetRandomItemClassName fires `FireRandomItemsGetClassName` before the vanilla fallback; HookChain exists. |
| engine.mod_injection_hook | values | missing → faithful | OverkillMutator override substitutes `g_overkill_items` + okhmg/okrpc weighted by probability cvars, so randomitems-overkill.cfg probabilities are reachable. |
| engine.mod_injection_hook | liveness | na → partial | Overkill override live (subscribed when enabled); Instagib override intentionally deferred. |
| map.filter_replace | logic | missing → faithful | Entity-level FilterItem seam exists (StartItem.SpawnInternal:116); replaces a spawning map item with a random classname. |
| map.filter_replace | values | missing → faithful | `g_random_items_replace_<classname>` machinery ported (literal 'random' weighted draw, else tokenized uniform, chosen==current no-op, placement copied). |
| map.filter_replace | timing | missing → faithful | Replacement spawns at spawn time on the live StartItem path; permanent lifetime. |
| map.filter_replace | liveness | na → live | FireFilterItem called live from StartItem.cs:116. |
| map.item_touched_rerandom | logic | missing → faithful | ItemTouched hook chain exists; replaces the picked-up item, schedules respawn, deletes original. |
| map.item_touched_rerandom | values | missing → faithful | ScheduleRespawn + RemoveItem + the ITEM_IS_LOOT skip match QC. |
| map.item_touched_rerandom | timing | missing → faithful | FireItemTouched fired after give+pickup sound, matching QC items.qc:746 ordering. |
| map.item_touched_rerandom | liveness | na → live | FireItemTouched called live from ItemPickupRules.cs:417. |
| loot.spawn_item | values | partial → faithful | LootTime default now 10f, matching Base/cfg; the latent 20-vs-10 divergence resolved. |
| spawn.recursion_guard_ok_flag | logic | missing → faithful | static `_isSpawning` recursion guard + ITEM_IS_LOOT skips ported. |
| spawn.recursion_guard_ok_flag | values | missing → faithful | `ok_item` flag ported (Entity.OkItem); OverkillMutator reads OkItem\|\|ItemIsLoot. |
| spawn.recursion_guard_ok_flag | liveness | na → live | Guard + ok_item flagging run on the now-live FilterItem/loot paths. |
| string.mutator_labels | logic | missing → faithful | Overrides `BuildMutatorsString`/`BuildMutatorsPrettyString`; chain iterates active mutators. |
| string.mutator_labels | values | missing → faithful | Tokens match Base: `:random_items` and `, Random items`. |
| string.mutator_labels | presentation | missing → faithful | The scoreboard/server-browser pretty label `, Random items` is now produced. |
| string.mutator_labels | liveness | na → live | BuildMutatorsString run live from GameLogInit. |

### mutator-rocketflying
| feature | dimension | was → now | reason |
| --- | --- | --- | --- |
| allowrocketjumping.force_on | logic | missing → faithful | Devastator rewritten to add the full remote_jump_* system AND the AllowRocketJumping hook chain. Handler forces `Allow=true`; DoRemoteExplode runs the dedicated rocket-jump branch. |
| allowrocketjumping.force_on | values | missing → faithful | All remote_jump_* defaults match Base: damage 70, force 450, radius 100, velocity_z 0/400/1500. |
| allowrocketjumping.force_on | liveness | na → live | Handler subscribed + the hook fired/consumed on the live remote-detonate path. |
| advertise.mutator_string | logic | missing → faithful | Overrides `BuildMutatorsString` (`:RocketFlying`) + `BuildMutatorsPrettyString` (`, Rocket Flying`); both chains exist. |
| advertise.mutator_string | presentation | missing → partial | Machine token emitted live; pretty chain runner has no live Server caller (same dead pretty surface as cloaked/globalforces/multijump). |
| advertise.mutator_string | liveness | na → partial | BuildMutatorsString chain live; pretty chain runner has zero Server callers. |

### mutator-rocketminsta
| feature | dimension | was → now | reason |
| --- | --- | --- | --- |
| secondary.laser_barrage | logic | partial → faithful | The hold-to-stream rapid-fire ramp ported (hagar_load/JumpInterval2 state); g_rm_laser==2 standalone mode + g_rm_laser_spread_random multiplier modeled. |
| secondary.laser_barrage | timing | partial → faithful | Rapid-ramp timers ported with Base-matching defaults: g_rm_laser_rapid 1, rapid_delay 0.6, rapid_refire 0.35. |
| laser.electrobitch | logic | missing → faithful | The Electrobitch achievement guard implemented (DAMAGE_AIM/IS_PLAYER/DIFF_TEAM/!IS_DEAD/IsFlying), with the touch-vs-timeout split matching Base. |
| laser.electrobitch | audio | missing → faithful | `Announce(ACHIEVEMENT_ELECTROBITCH)` called; notification registered + announcer sound exists. |
| laser.electrobitch | liveness | na → live | The achievement path is wired live: WrThink → barrage → Think → explode. |

### mutator-sandbox
| feature | dimension | was → now | reason |
| --- | --- | --- | --- |
| hooks.gating | logic | partial → faithful | The three Sandbox_* veto hooks (DragAllowed/SaveAllowed/EditAllowed) fully ported as hook chains, fired at all three Base-equivalent live call sites. No subscriber exists, exactly like Base's empty MUTATOR_CALLHOOK — bit-faithful. |
| object.think_grab | logic | partial → faithful | The DragAllowed veto hook (the sole baseline reason for partial) is ported + fired; the grab-class branch is now Base-faithful. |
| object.edit_properties | values | partial → faithful | physics-2 now maps to MoveType.Physics (value 32) matching Base MOVETYPE_PHYSICS=32 (was MoveType.Push=7). physics_map flips false→true. |

### mutator-spawn_near_teammate
| feature | dimension | was → now | reason |
| --- | --- | --- | --- |
| bias.spawn_score | logic | partial → faithful | The checkpvs visibility gate is ported + live (Api.Trace.CheckPvs, backed by the compiled BSP PVS). Biased/look-at set now matches Base instead of being a superset. |
| relocate.spot_search | logic | partial → faithful | The two baseline-missing rejects ported + live: HitsTriggerHurt (swept-AABB slab test) + NadeInRange (g_nades-gated radius scan). All 5 QC rejects present. g_nades_nade_radius=300 flips false→true. |
| relocate.commit_closetodeath | logic | partial → faithful | closetodeath now measures from the real death origin via a new `Player.DeathOrigin` field latched on the live death path (DamageSystem.Killed). Baseline recorded NO .death_origin field existed. |

### mutator-stale_move_negation
| feature | dimension | was → now | reason |
| --- | --- | --- | --- |
| smneg.mutator_string | logic | missing → faithful | BuildMutatorsString subsystem now exists; overrides match both QC hookfunctions byte-for-byte, with chain runners iterating active mutators. |
| smneg.mutator_string | values | missing → faithful | `:StaleMoveNegation` + `, Stale-move negation` present + exact. |
| smneg.mutator_string | presentation | missing → faithful | Both string contributions produced faithfully. |
| smneg.mutator_string | liveness | na → partial | BuildMutatorsString chain LIVE via GameWorld.cs:876; BuildMutatorsPrettyString chain has NO server caller (votescreen consumer unported). |

### mutator-status_effects
| feature | dimension | was → now | reason |
| --- | --- | --- | --- |
| framework.tick | presentation | partial → faithful | `Entity.Effects` is now a real networked field; StunnedTick/SpawnShieldTick set the EF_* bits + CsqcModelEffects renders EF_ADDITIVE/FULLBRIGHT/SHOCK. Per-effect tick presentation now produced + rendered. |
| spawnshield_stunned_superweapon | presentation | missing → partial | StunnedTick sets EF_SHOCK + SpawnShieldTick sets EF_ADDITIVE\|FULLBRIGHT (networked + rendered) with OnRemove clearing them. Stunned aura fully live; spawnshield shimmer live for monsters only — partial. |
| spawnshield_stunned_superweapon | logic | partial → faithful | The per-effect m_tick bodies + m_remove overrides are now Base-faithful. Only the player-spawnshield-via-bespoke-timer routing remains (a liveness/wiring gap). |

### mutator-superspec
| feature | dimension | was → now | reason |
| --- | --- | --- | --- |
| ui.mutator_strings | logic | missing → faithful | `BuildMutatorsString`/`BuildMutatorsPrettyString` overrides now exist (Wave-6b). |
| ui.mutator_strings | values | missing → faithful | Tokens match Base: `:SS` + `, Super Spectators`. |
| ui.mutator_strings | presentation | missing → partial | Machine `:SS` reaches gamelog/serverinfo live; pretty label never displayed. |
| ui.mutator_strings | liveness | na → partial | BuildMutatorsString chain LIVE; BuildMutatorsPrettyString has zero external callers. |

### mutator-touchexplode
| feature | dimension | was → now | reason |
| --- | --- | --- | --- |
| blast.deathtype | logic | partial → faithful | `DeathTypes.TouchExplode` is now a registered constant + SelectSpecial has a TouchExplode branch, so the blast tag resolves to MURDER_TOUCHEXPLODE instead of falling through to GENERIC/FRAG. |
| blast.deathtype | presentation | missing → faithful | The DEATH_SELF/MURDER_TOUCHEXPLODE notification rows ("died in an accident") are now reachable; obituary text matches Base. |
| blast.deathtype | liveness | partial → live | Full obituary chain now live: blast tags the kill → WeaponSplash routes → SelectSpecial resolves → registered notification rows. No longer dead-ends on an unregistered tag. |

### mutator-vampirehook
| feature | dimension | was → now | reason |
| --- | --- | --- | --- |
| drain.engine | logic | partial → faithful | The pre-match start gate now uses `time < game_starttime` faithfully via the host-wired provider, instead of the old `GameStopped` mistranslation. Sole residual is the minor DMG_NOWEP attribution flag. |
| drain.engine | values | partial → faithful | DamageRate default now 0.2f matching Base; the old 0.1f fallback that mismatched when the cvar was 0/unset is gone. |
| drain.heal_indirection | logic | partial → faithful | HealPlayer reproduces Heal()'s game_stopped/spectator/FROZEN/IS_DEAD guards AND PlayerHeal()'s dead-target/cap floor before the capped give. |
| drain.heal_indirection | values | partial → faithful | Heal-cap matches + the dead-target-floor is now value-correct. |

### mutator-walljump
| feature | dimension | was → now | reason |
| --- | --- | --- | --- |
| fx.anim_and_oldvel | logic | partial → faithful | The shared Entity now carries an `OldVelocity` field, and WalljumpMutator writes `OldVelocity = Velocity` — the faithful POST-impulse value matching Base. The OldOrigin-clobber that cancelled render interpolation is gone. animdecide (presentation) remains missing. |

### mutator-waypoints
| feature | dimension | was → now | reason |
| --- | --- | --- | --- |
| server.manager_api | logic | partial → faithful | The objective-marker manager half (SpawnFixed/UpdateHealth/UpdateMaxHealth/Disown/Kill) is no longer dead: Assault drives it live; the QC-faithful semantics now have a real caller. |
| server.manager_api | liveness | partial → partial | Label unchanged but substance improved: baseline said the entire objective path had ZERO live callers; Assault now exercises SpawnFixed/UpdateHealth/etc and they are networked. Only AttachCarrier/UpdateRule/UpdateTeamRadar/UpdateBuildFinished/Ping/DetachCarrier remain uncalled. |
| gametype.collect | liveness | partial → partial | Two new live CollectWaypoints producers: Keepaway (KaBall, g_keepawayball_tracking-gated) + TeamKeepaway (team-colored, g_tkaball_tracking-gated), plus Assault objective markers. Baseline recorded only CTF+Dom+FreezeTag. |
| visibility.rules | logic | partial → faithful | WaypointVisible implements the full SPRITERULE_SPECTATOR sv_itemstime gating AND the SPRITERULE_DEFAULT team-restriction + live-player gate, matching Base line-for-line. |
| client.fades | logic | partial → faithful | Distance-fade, edge-fade + crosshair-fade alpha+scale ramps implemented; helpme blink now bright-half (*2); per-def blink applied at draw; blink_time = health*10/time matches QC. |
| client.fades | values | partial → faithful | distancefade 1/0.7/0.5, edgefade 0.5/50/1, crosshairfade 0.25/150/1 + SPRITE_HELPME_BLINK=2 all match Base defaults. |
| client.fades | timing | partial → faithful | The fade ramps + blink half-cycle now faithful to QC's Draw_WaypointSprite cadence; only the lifetime-fade computation site still differs cosmetically. |
| client.fades | presentation | partial → partial | Presentation substantially improved (all three fades + correct blink render) but still partial because the off-screen arrow glyph remains a simplified triangle. |
| client.cvars_inert | values | faithful → faithful | The previously-inert fade cvars are now READ LIVE by WaypointSpriteLayer._Draw; only g_waypointsprite_iconcolor remains unread. |

## Reclassified

Neutral re-labels — status didn't move up or down (corrected notes, misclassification fixes, line-number drift).

| unit | feature | dimension | was → now | reason |
| --- | --- | --- | --- | --- |
| mutator-buffs | buffs.notifications | audio | partial → partial | SND_BUFF_LOST now emitted on drop + KA_RESPAWN on relocation; only the SND_STRENGTH_RESPAWN reactivation cue remains (replace-path silence is faithful). Still partial but narrower. |
| mutator-buffs | buffs.item.touch_pickup | presentation | partial → partial | Notifications now fire; the only residual presentation gap is the missing EFFECT_ITEM_PICKUP particle. |
| mutator-campcheck | campcheck.mutator.registration | logic | faithful → faithful | Notes corrected: `g_campcheck_typecheck` is now READ AND USED in the gate, no longer read-but-dead. |
| mutator-damagetext | sv.first_hit_bitset | values | faithful → faithful | Constants row "ClientDisconnect clears attacker bit on every victim" flipped match:false → true now that the disconnect clear is ported; dimension unchanged, gaps list now empty. |
| mutator-damagetext | sv.player_damaged_producer | logic | faithful → faithful | No behavior change; port_ref line numbers shifted (OnPlayerDamaged now :139). |
| mutator-damagetext | cl.format_and_size | logic | faithful → faithful | No behavior change; DamageTextFormat.cs relocated under Gameplay/Mutators/; port_ref path corrected. |
| mutator-dodging | anim.jump_action | liveness | na → live | The up-impulse path that produces the airborne jump pose now executes on the live dodge path; with a presentation contribution present, liveness is no longer na. |
| mutator-dynamic_handicap | present.handicap_level | presentation | missing → missing | Still missing but narrower: the value is computed server-side but never networked (no ent_cs handicap stat) and no scoreboard player_handicap-icon consumer. |
| mutator-globalforces | advertise.mutatorstring | presentation | na → faithful | Server-info/scoreboard mutator-list advertisement now produced; reclassified from na to faithful as the presentation contribution now exists. |
| mutator-hook | info.mutator_string | liveness | na → partial | Code now exists, so liveness applies: BuildMutatorsString info-token path live but BuildMutatorsPrettyString votescreen path has no caller. |
| mutator-invincibleproj | string.machine | presentation | missing → na | This is the colon-delimited MACHINE token (gamelog/server-info), not user-facing; the display label belongs to string.pretty. |
| mutator-powerups | strength.fire_sound | audio | na → faithful | Audio dimension reclassification note — covered by the audio fix above (kept for completeness of the audio transition). |
| mutator-rocketflying | advertise.mutator_string | values | na → faithful | Token strings now bit-faithful (`:RocketFlying` / `, Rocket Flying`); promoting values from na to faithful to reflect the now-present matching constants. |
| mutator-rocketminsta | secondary.laser_barrage | logic | partial → faithful | The baseline "owner velocity not added to laser bolts" gap was a MISCLASSIFICATION: Base passes forceAbsolute=true (forces newton_style=0), so owner velocity is NOT added. The port's speedFactor-only application matches Base exactly — never a real divergence. |
| mutator-stale_move_negation | smneg.registration.enable | logic | GameWorld.cs:535 → GameWorld.cs:563 | MutatorActivation.Apply() call site shifted by surrounding Wave-6 edits; activation path + faithful/live status unchanged. port_ref line updated. |
| mutator-status_effects | framework.remove | logic | faithful → faithful | Stale note corrected: Remove now invokes def.OnRemove before the sound on every removal_type, wired for spawnshield + stunned. New constant row for the m_remove override added (match:true). |
| mutator-status_effects | framework.registry | values | partial → partial | Still partial (m_color/m_icon/m_name dropped) but a new constant row records the def now carries the m_remove override (OnRemove) used by spawnshield/stunned. |

---

_Report generated 2026-06-25 from per-unit Base↔port re-audit deltas (Wave-6b, commit `f86d947`). Registry mode: update — all 38 units re-wrote their `registry/<unit>.yaml`._
