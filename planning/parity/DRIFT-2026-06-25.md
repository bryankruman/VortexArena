# Parity drift report — 2026-06-25

**0 regressions · 187 fixes · 0 new gaps · 10 newly-unmapped Base features · 10 reclassified** across 20 server/damage/scoring/net units (every unit re-wrote its registry file). The whole delta is Waves 5/6 landing server gameplay-logic ports live (teamplay/anticheat/antilag, clientkill, handicap, intermission, mapvoting, spawnpoints, world-rules, etc.). No parity went backwards — this is a large net-improvement pass.

## Regressions

No drift detected. (No `kind=regression` change in this diff — nothing got worse.)

## New gaps

No drift detected. (No `kind=new-gap` change in this diff — no previously-faithful feature regressed into a gap.)

## Registry coverage gaps

Base features discovered during this audit that the registry had no row for (or whose row scope changed). Rows were added during this pass.

| unit | newly-mapped Base feature | status |
| --- | --- | --- |
| resources | `common/resources/sv_resources.qc:231` GetAmmoConsumption — ammo-per-shot helper scaling a Q3 ammo box `.count` into a Xonotic resource amount. Ported faithfully (CompatRemaps.cs:98) and LIVE via the q3compat ammo spawnfunc (ItemSpawnFuncs.cs:137). Added as a 9th feature row. | ported / live |
| scoring | `server/scores.qc:Score_NicePrint` (+ `_Team`/`_Player`/`_Spectators`/`_Spectator`/`_ItemColor`, ~829-947) — the server-side `scores`/`teamscores` console command that prints a formatted text standings table to the requesting player. No port equivalent (grep finds nothing). Added as row `scoring.display.niceprint`. | missing |
| sv-antilag | `server/weapons/tracing.qc:crosshair_trace` / `WarpZone_crosshair_trace` — server-side crosshair/prydon-cursor aim trace routed through traceline_antilag at ANTILAG_LATENCY. Port cursor/aim traces (Spiderbot/Raptor/Racer, SandboxMutator) use plain traces with no takeback. Coupled to the unimplemented g_antilag modes 1/3 but a distinct Base symbol. | missing |
| sv-antilag | `server/antilag.qc:antilag_debug` (te_spark history visualization in antilag_record) — a debug-only spark trace of the rewound history. No port equivalent. Debug-only, low value, listed for completeness. | missing |
| sv-chat | `server/chat.qc:593-645` `PrintToChat` / `PrintToChatAll` / `PrintToChatTeam` (+ `Debug*` developer-gated variants) — `\{1}^7`-prefixed chat-line emitters used by teamplay.qc and the random_items mutator. No named port helper. Added as row `sv-chat.helpers.printtochat`. | missing |
| sv-commands-votes | `server/command/sv_cmd.qc:GameCommand_defer_clear*` — `defer_clear` / `defer_clear_all` console deferred-command-list management verbs. No registry row and not registered in Commands.cs. Niche admin tooling. | missing |
| sv-impulse | `cheats.qc:184` CheatImpulse `clone`(140)/`speedrun`(141)/`fixed-clone`(142)/`teleport`(143)/`drag`(147) (all.qh CHIMPULSE_* 140-147) — split out of `sv-impulse.cheat.impulses` now that give-all/r00t are live; the 140-147 set has a distinct still-unported liveness story. Added as row `sv-impulse.cheat.command_impulse`. | missing / unported |
| sv-spawnpoints | `server/client.qc:814-822` PutPlayerInServer fires the spawn spot's `.target` (SUB_UseTargets, with the assault/race `spot.target = string_null` suppression hack). Implemented faithfully and live at ClientManager.cs:709-716 (suppressed for Assault/Race/Cts). Had no baseline row. Added as `sv-spawnpoints.put.spawn_target_fire` (logic/timing faithful, live). | ported / live |
| sv-world-rules | `server/world.qc:1264` DumpStats — final/periodic `:scores:`/`:status:` per-player score dump to console (sv_logscores_console), eventlog (sv_eventlog) and file (sv_logscores_file); called from NextLevel(final) and the printstats/status console command. No port analogue. Added as row `sv-world-rules.eventlog.dumpstats`. | missing |
| sv-world-rules | `server/world.qc:1370/1395` MatchEnd_RestoreSpectatorStatus / MatchEnd_RestoreSpectatorAndTeamStatus — at match end moves join-during-match players back to spectator and restores forced teams (bprint "moved back to spectator"); called from survival mode. No port match; noted under `sv-world-rules.nextlevel.intermission` gaps. | missing |

## Fixes

Parity that improved since the baseline (187 dimension transitions). Grouped by unit.

### damage-pipeline
| feature | dimension | was → now | reason |
| --- | --- | --- | --- |
| dispatch.teamplay_shaping | audio | na → faithful | Teamkill complaint SOUND wired end-to-end: DamageSystem.Apply:282-294 sets the teamkill timers/source on the attacker; PlayerFrameLogic.Regen:127-149 plays the deferred 'teamshoot' voice on the victim (live via GameWorld.cs:1699). Baseline discarded complainTeamDamage. |
| dispatch.teamplay_shaping | values | partial → faithful | Stock fallbacks corrected: DefTeamplayMode=4, DefFriendlyFireVirtualForce=1 (DamageSystem.cs:73-74). g_teamdamage_resetspeed decay of dmg_team now applied per frame (PlayerFrameLogic.Regen:121-125). |
| dispatch.teamplay_shaping | timing | na → faithful | Per-frame dmg_team decay + the +0.4s deferred teamshoot voice introduce a real, frametime-faithful timing dimension to this feature. |
| fire.burning | logic | partial → faithful | Fire TRANSFER implemented (StatusEffects.FireApplyDamage:397-432): re-ignites adjacent damageable entities via FindInRadius+boxesoverlap at firetransfer 0.8/0.9. Napalm now routes through FireAddDamage (NadeNapalmBoom.cs:218) gaining the overlap merge + napalm FireDeathType. |
| heal.dispatcher | logic | missing → faithful | Central Heal() dispatcher now exists (Combat.Heal/DamageContracts.Heal:79-103): rejects freed/spectator/frozen/dead targets, routes through GtEventHeal. Baseline had port_refs:[]. |
| heal.dispatcher | liveness | na → live | Heal invoked live: Arc healgun (Arc.cs:333), Onslaught generator/icon (OnslaughtControlPoint.cs:135/330 via GameWorld.cs:2396), vehicle repair (VehicleCommon.cs:379). |

### net-entity-state
| feature | dimension | was → now | reason |
| --- | --- | --- | --- |
| entcs.privacy_mask | logic | missing → faithful | Wave-6c ba3ae47 added ServerNet.RelevantEntitiesFor enemy-privacy gating mirroring `_entcs_send`'s break-condition (radar_showenemies/SAME_TEAM/non-active-player → send private; else strip). |
| entcs.privacy_mask | values | missing → partial | Enemy HEALTH/ARMOR resource slice zeroed per-recipient before EncodeSnapshot. Partial: origin/angles privacy governed by whole-entity PVS culling (intended divergence). |
| entcs.privacy_mask | liveness | na → live | RelevantEntitiesFor called per-recipient in BroadcastSnapshots with radar_showenemies + TeamGame read each broadcast tick; Race.cs:419 drives radar_showenemies=1 live. Anti-cheat HP/armor leak closed on the real broadcast path. |

### resources
| feature | dimension | was → now | reason |
| --- | --- | --- | --- |
| api.give | logic | partial → faithful | GiveResourceWithLimit now fires its QC mutator hook (ResourceHooks.cs:30-31,79-87) before the trim (Resources.cs:150). Sole baseline gap closed. |
| api.take | logic | partial → partial (gap reduced) | Plain TakeResource mutator hook added (Resources.cs:163), closing one of two baseline gaps. Row stays partial: TakeResourceWithLimit still lacks its own WithLimit-specific hook. |
| regen.loop | logic | partial → faithful | PlayerRegenArgs (MutatorHooks.cs:199-232) carries all 10 QC tuning slots; max_mod/regen_mod/rot_mod/limit_mod applied + clamped (PlayerFrameLogic.cs:82-185). Last baseline gap closed. |
| regen.pause_timers | logic | partial → faithful | Warmup-countdown offset (game_starttime - time) added to primed spawn pause timers + shield (SpawnSystem.cs:767-782), fuel-rot excluded — bit-faithful to client.qc:665-671. |

### scoring
| feature | dimension | was → now | reason |
| --- | --- | --- | --- |
| lifecycle.game_stopped | logic | partial → faithful | Central game_stopped clamp armed live: GameWorld.cs:1762 publishes `GameScores.GameStopped = GameStopped` every OnEndFrame tick. Baseline recorded ZERO live assignments. |
| lifecycle.game_stopped | timing | partial → faithful | Clamp now toggles in lockstep with match state each tick rather than relying on per-call-site guards firing at different times. |
| lifecycle.game_stopped | liveness | partial → live | GameScores.GameStopped assigned from the live per-tick path; the central clamp in PlayerScore_Add/TeamScore_AddToTeam is now reachable and fires. |

### sv-anticheat
| feature | dimension | was → now | reason |
| --- | --- | --- | --- |
| detect.div0_evade | liveness | partial → live | OnClientMove calls AntiCheat.PreThink(p) for non-bots (GameWorld.cs:1537-1538), the QC per-frame re-arm. Previously zero callers; the evade reseed re-zeros EvadeOffset every frame again. |
| detect.div0_evade | logic | partial → faithful | With the PreThink re-arm live, the reseed branch re-samples the evasion phase walk once per frame like Base, not against a stale sample. |
| detect.div0_evade | timing | partial → faithful | Per-frame re-arm cadence matches Base (anticheat_prethink every PlayerPreThink). |
| frame.fixangle_window | logic | partial → faithful | Teleport fixangle wired (Teleporters.cs:396 OnPlayerFixAngle → GameWorld.cs:1004 → AntiCheat.FixAngle); EndFrame FOREACH_CLIENT propagation reclassified as intended divergence. |
| frame.fixangle_window | timing | partial → faithful | Window opens at the correct moment on both forced-view sources (spawn + teleport). |
| frame.fixangle_window | liveness | partial → live | Both .fixangle sources (spawn GameWorld.cs:1214, teleport :1004) reach AntiCheat.FixAngle live; no dead branch remains. |
| frame.fixangle_window | values | partial → partial (isolated) | Still partial but isolated: AntiCheat.PingProvider hardcoded 0f (GameWorld.cs:997) → window always 0.2s vs min(0.4,ping*0.001)+0.2. Teleport + endframe sub-gaps resolved. |
| report.sv_cmd | logic | missing → faithful | The `anticheat` admin command exists (Commands.cs:691 → CmdAnticheat:912-933), resolving target by entity-number/name and running ReportToEventLog gated on sv_eventlog. |
| report.sv_cmd | presentation | missing → faithful | Usage/no-match/sv_eventlog-disabled messages and the `:anticheat:` report output emit via the registered command. |
| report.sv_cmd | liveness | na → live | Registered in the server command table (Commands.cs:691), reachable from the admin/console path. |
| report.register_playerstats | logic | missing → faithful | anticheat_register_to_playerstats ported: PlayerStats.Init (PlayerStats.cs:128) calls AntiCheat.RegisterPlayerStats, registering `anticheat-_time` + one slot per detector; register-then-emit contract honored. |
| report.register_playerstats | values | na → faithful | Registered event ids (`anticheat-`+name) match the PLAYERSTATS_ANTICHEAT prefix and the emit-side ids exactly. |
| report.register_playerstats | liveness | na → live | RegisterPlayerStats invoked on the live PlayerStats.Init path. |

### sv-antilag
| feature | dimension | was → now | reason |
| --- | --- | --- | --- |
| clear.on_spawn | logic | partial → faithful | Explicit antilag_clear lifecycle wired via a one-shot Entity.AntilagNeedsClear set on respawn (SpawnSystem.cs:638), teleport (Teleporters.cs:390), vehicle enter+exit (VehicleBoarding.cs:230/263); BuildEntitySet clears the ring on that flag OR the >150u jump backstop. |
| melee.secondary | logic | partial → faithful | Shotgun.Melee now brackets the swing in LagComp.Begin/End (Shotgun.cs:244/281, finally) so the slap rewinds players/monsters/nades to the shooter's view-time. Baseline traced present-position. |
| melee.secondary | liveness | na → live | Real antilag code on the melee path, reached live from the secondary/out-of-ammo fire path (Shotgun.cs:166 → Melee), gated on g_antilag==2 + cl_noantilag. |

### sv-campaign
| feature | dimension | was → now | reason |
| --- | --- | --- | --- |
| preinit.gametype_switch | logic | partial → faithful | GameWorld.Boot wires both host probes (LoadedMapName/GametypeMismatch, lines 506-515) BEFORE PreInit() (517); the Campaign_Invalid gametype/map-mismatch → Bailout checks are genuinely performed on the live boot path. |
| preinit.gametype_switch | liveness | partial → live | Validate()'s two host probes assigned every Boot, reached live via PreInit. Previously both null and the guard was inert. |
| bots.may_start | logic | partial → faithful | All five Base consumers of campaign_bots_may_start wired: bot movement, round countdown (RoundHandler.cs:237), monster move (MonsterAI.cs:778), campcheck (CampcheckMutator.cs:141), LMS (LastManStanding.cs:604). |
| bots.may_start | liveness | partial → live | All four host-layer gate probes assigned every Boot; each consumed on a live gameplay path; setter fires on the human's spawn (GameWorld.cs:1221). |

### sv-chat
| feature | dimension | was → now | reason |
| --- | --- | --- | --- |
| ignore.crud | values | partial → faithful | IGNORE_MAXPLAYERS corrected 50 → 16 (Chat.cs:444), matching Base. logic stays partial (intentionally-dropped permanent db ignore tier). |
| routing.recipients | logic | partial → partial (gap reduced) | active_minigame team-say branch ported + LIVE (Chat.cs:363-378, wired to MinigameSessionManager). One of two logic gaps closed; INGAME() round-elimination still approximated by !IsObserver. |
| format.crosshair_tokens | logic | partial → faithful | Crosshair tokens un-stubbed: CrosshairTrace (Chat.cs:628-642) does a real view-angle trace; %y resolves NearestLocation, %x the hit NetName, %d uses DeathOrigin. |
| format.crosshair_tokens | values | partial → faithful | %d/%y read correct positions (DeathOrigin / trace endpos) instead of self-origin / 'nothing'. |
| format.crosshair_tokens | presentation | partial → faithful | Expanded %y/%x/%d strings reflect the aimed location/entity/death location rather than placeholders. |
| format.crosshair_tokens | liveness | partial → live | %y/%x/%d now perform a real trace and resolve live values (only %l was live before). |
| delivery.dedicated_print_gating | logic | partial → faithful | DedicatedPrint (Chat.cs:736-740) gates the server-console echo on sv_dedicated, matching Base; no longer unconditionally echoes on a listen server. |
| delivery.dedicated_print_gating | presentation | partial → faithful | On a listen server, host stdout no longer surfaces routed chat / tells where Base prints nothing. |
| format.macros | values | partial → faithful | PlayerHealth models the (health==2342 && mapvote_initialized) 'observing' case via MapVote.Running (Chat.cs:615), closing the only narrowed macro edge case. |

### sv-cheats
| feature | dimension | was → now | reason |
| --- | --- | --- | --- |
| gating.cheats_allowed | logic | partial → faithful | sv_clones clone-allowance branch present (Cheats.cs:78) via the cloneImpulse param. Sole baseline defect closed. |
| impulse.r00t | logic | missing → faithful | Cheats.R00t (Cheats.cs:186-216) reservoir-samples a uniform live enemy (IS_PLAYER && !IS_DEAD && DIFF_TEAM, fallback self), matching cheats.qc:264-283. |
| impulse.r00t | values | missing → faithful | RadiusDamage constants match Base (1000/0/128/500); deathtype DeathTypes.Cheat (standalone DEATH_CHEAT). |
| impulse.r00t | presentation | missing → faithful | EFFECT_ROCKET_EXPLODE emitted at the victim. |
| impulse.r00t | audio | missing → faithful | SND_ROCKET_IMPACT played at the victim. |
| impulse.r00t | liveness | na → live | DispatchImpulse (Commands.cs:1799) routes imp==148 to R00t; 'impulse' allowlisted, so `impulse 148` reaches it live. |
| cmd.teleporttotarget | logic | missing → faithful | `teleporttotarget` → Teleporters.TeleportToTarget (spawns cheattriggerteleport, resolves target, runs SimpleTeleportPlayer, DID_CHEAT on success), matching cheats.qc:687-699. |
| cmd.teleporttotarget | liveness | na → live | Registered (Commands.cs:701 → CmdCheat) + allowlisted; routes CmdCheat → Cheats.Command live. |
| mapent.info_autoscreenshot | values | partial → faithful | g_max_info_autoscreenshot default corrected 60 → 3 (Cvars.cs:368). (spawnfunc logic still missing.) |

### sv-client-lifecycle
| feature | dimension | was → now | reason |
| --- | --- | --- | --- |
| spawn.spawn_shield | logic | partial → faithful | The countdown extension (`!sv_ready_restart_after_countdown && time < game_starttime`) applied (SpawnSystem.cs:761-782): shield + pauserot/health + pauseregen rolled forward, fuel rot excluded. TODO closed. |
| spawn.spawn_shield | timing | partial → faithful | Same extension fixes shield/rot-pause duration for a player spawned during the pre-match countdown. |
| dead.respawn_countdown_announce | logic | missing → faithful | ShowRespawnCountdown ported (GameWorld.cs:2751), called from DeadPlayerThink (:2733) with the 0.5s overlap guard. Baseline had empty port_refs. |
| dead.respawn_countdown_announce | values | missing → faithful | RespawnCountdown start (10 when sdelay+waves≥5 && wait>1.75s) seeded (RespawnTiming.cs:70) and consumed. |
| dead.respawn_countdown_announce | timing | missing → faithful | Per-second decrement + 0.5s overlap guard ported. |
| dead.respawn_countdown_announce | presentation | missing → faithful | The NUM_RESPAWN_n announcer sent to the dead player. |
| dead.respawn_countdown_announce | audio | missing → faithful | Announcer_PickNumber(CNT_RESPAWN,n) equivalent fires (gated on the default-disabled opt-in NUM_RESPAWN announcer). |
| dead.respawn_countdown_announce | liveness | na → live | DeadPlayerThink calls ShowRespawnCountdown every tick on the live dead-player path. |
| dead.respawn_state_machine | presentation | partial → faithful | The respawn countdown announcer is now wired into DeadPlayerThink, removing the only presentation deficiency. |
| frame.player_powerups | logic | partial → faithful | Faithful port: EF_NODEPTHTEST clear+set, g_fullbrightplayers, remove-all-on-death, gibbed/dead bare-return, full 4-case superweapon state machine (PlayerFrameLogic.cs:366-551). Only cosmetic MF_ROCKET dropped. |
| frame.player_powerups | presentation | missing → faithful | CENTER_SUPERWEAPON_LOST/BROKEN/PICKUP + INFO broadcast (CTS-suppressed) + EF_NODEPTHTEST/EF_FULLBRIGHT toggles emitted. |
| frame.player_powerups | audio | missing → faithful | play_countdown(SND_POWEROFF) + the poweroff sound on remove-all played. |
| frame.player_regen | logic | faithful → faithful (gap closed) | Residual gap closed: the PlayerRegen hook now passes ALL tuning slots (max/regen/rot/limit_mod + 6 health overrides) and reads them back; a mutator that retunes regen is honored. gaps[] empty. |
| spawn.put_player_in_server | logic | partial → partial (gap reduced) | SUB_UseTargets(spot) fires on spawn with the Assault/Race/Cts suppression (ClientManager.cs:709-716) + Handicap_Initialize wired (:699). Remaining: in-PutPlayerInServer team assignment. |
| join.join_allowed | logic | partial → partial (gap reduced) | JoinAllowed enforces TEAM_FORCE_SPECTATOR, the play-ban ForcedSpectate hook, and a gametype late-join gate (LMS). Remaining: version_mismatch, g_maxping, capacity gate. |
| disconnect.client_disconnect | logic | partial → partial (gap reduced) | ClientDisconnect runs vehicle exit (VHEF_RELEASE), powerups_remove_all (poweroff sound), status-effect clear, MUTATOR_CALLHOOK(ClientDisconnect). Remaining: ignore-list/chatbubble/waypoint/ready-vote cleanups. |
| frame.prethink_misc | audio | missing → partial | The deferred teamkill_soundtime friendly-fire voice (teamplay_mode 4) fired from the per-frame regen slice (PlayerFrameLogic.cs:127-149). Taunt voice + SetZoomState still absent. |

### sv-clientkill
| feature | dimension | was → now | reason |
| --- | --- | --- | --- |
| antispam.penalty | logic | missing → faithful | g_balance_kill_antispam registered (Cvars.cs:162); clientkill_nexttime carry-forward implemented (KillCountdown.cs:116-123). |
| antispam.penalty | values | missing → faithful | Default 5 registered matching Base; round-unstarted min(antispam,2) clamp present. |
| antispam.penalty | timing | missing → faithful | The next-allowed-kill window defers/extends a mashed `kill`; KillCntdownNextTime persists per player. |
| antispam.penalty | liveness | na → live | KillAntispam wired (GameWorld.cs:854); carry-forward runs on every CmdKill → Begin. |
| teamchange.deferred | logic | partial → faithful | CmdSelectTeam/CmdSpectate route through Begin; auto-select defer-to-Join, ALREADYBEST early-out, silent-indicator instant-kill shortcut all modeled. |
| teamchange.deferred | timing | missing → faithful | Team move/spectate defers through g_balance_kill_delay with a cancel window; PerformTeamChange resolves on expiry. |
| teamchange.deferred | presentation | missing → faithful | The team-colored/grey CENTER_TEAMCHANGE_* countdown center-print fires for team change and spectate. |
| teamchange.deferred | audio | missing → faithful | The per-second NUM_KILL announcer drives the deferred team-change/spectate countdown via Tick. |
| mutator.hooks | logic | missing → faithful | ClientKillHook wired (GameWorld.cs:866-875): a frozen freezetag/CA player is forbidden from self-killing; cts/race force killtime=0. Closes the frozen-self-kill exploit. |
| mutator.hooks | timing | na → faithful | cts/race killtime=0 override makes those self-kills instant via the hook. |
| mutator.hooks | liveness | na → live | ClientKillHook invoked from Begin (KillCountdown.cs:88-89) on the live `kill`/team-change path. |
| silent.cts_finish | logic | partial → faithful | OnFinishRetract changed from a bare teleport-respawn to a real silent DEATH_KILL (Combat.Damage 100000, GameWorld.cs:2269); ScheduleRetract honors 0 = never. |
| silent.cts_finish | values | partial → faithful | g_cts_finish_kill_delay 0=never and -1=instant now both correct (previously Max(0,delay) collapsed both to retract-now). |
| impulse.waypoint_kill | logic | missing → faithful | CmdWaypointClear routes the checkpoint-reset self-kill through Begin gated on (cts\|\|race)&&g_allow_checkpoints (Commands.cs:1564-1573). Previously sprite-removal only. |
| impulse.waypoint_kill | liveness | na → live | Wired live from waypoint_clear[_personal] + impulses 47/48; CTS forces killtime=0. |
| centerprint.teamchange | presentation | faithful → faithful (gap closed) | Notif-set constant moved partial → match: RED/BLUE/YELLOW/PINK/SPECTATE reachable, ALREADYBEST fires. Only colormod tint remains unmodeled. |

### sv-commands-votes
| feature | dimension | was → now | reason |
| --- | --- | --- | --- |
| vote.call | logic | partial → faithful | Voting.TimeoutActive + GameStartTime wired live (GameWorld.cs:931/934); the timeout-active and pre-match guards now fire. Only !IS_CLIENT remains unwired. |
| vote.parse_whitelist | logic | partial → faithful | ValidateMap (recent-map + gametype-support gate) wired live (GameWorld.cs:938) for map/nextmap; VoteCommand_checkargs ported as CheckArgs (VoteController.cs:538) and applied at Parse:613. |
| vote.count | logic | partial → faithful | Voting.IsPlayer wired live (GameWorld.cs:928); the spectator-exclusion branch now drops spectator ballots under sv_vote_nospectators 2. |
| vote.count | liveness | partial → live | The spectator-exclusion path is reachable because its IsPlayer predicate is wired live. |
| vote.master | logic | partial → faithful | The master-vote timeout_status guard (VoteController.cs:248) resolves through the live-wired TimeoutActive. Residual: sv_vote_master_ids auto-login. |
| timeout.state_machine | values | partial → faithful | TIMEOUT_SLOWMO_VALUE (0.0001) takes effect: ApplySlowmo drives the slowmo cvar (GameWorld.cs:1055) which ServerNet.cs:439 reads into Simulation.TimeScale. |
| timeout.state_machine | timing | partial → faithful | The pause is realized as engine slowmo (GameStopped no longer includes Timeout.IsPaused), so players inch at the time-scaled rate, matching QC. |
| timeout.state_machine | presentation | missing → partial | Per-second CENTER_TIMEOUT_BEGINNING/ENDING countdown sinks wired (GameWorld.cs:1062-1067, carried as chat-broadcast). |
| timeout.state_machine | audio | missing → partial | ANNCE_PREPARE cue sink wired (GameWorld.cs:1068, surfaced as chat-broadcast). |
| timeout.state_machine | liveness | partial → live | All previously-dead timeout sinks (ApplySlowmo, CenterPrint*, AnnouncePrepare) wired live; slowmo flows to Simulation.TimeScale. |
| common.info_commands | logic | partial → faithful | CmdWho (Commands.cs:2056-2093) prints the full QC ent/nickname/ping/pl/time/ip/crypto_id table with sv_status_privacy hiding, separator handling, bot rows, slots footer. |
| common.info_commands | values | partial → faithful | sv_status_privacy honored (privacy = !IsServerConsole && sv_status_privacy, Commands.cs:2060). |
| common.info_commands | presentation | partial → faithful | The who output matches the QC column layout (only ping/pl carry 0, a documented headless-core limitation). |
| admin.teamplay_match | values | partial → faithful | CmdShuffleTeams uses a FOREACH_CLIENT_RANDOM reservoir shuffle via Prandom.Float() instead of deterministic round-robin. |

### sv-handicap
| feature | dimension | was → now | reason |
| --- | --- | --- | --- |
| init.defaults_to_one | logic | partial → faithful | A dedicated Handicap.Initialize (Handicap.cs:44-50) resets forced give/take to 1 and tails into UpdateHandicapLevel, mirroring handicap.qc:16-23. |
| init.defaults_to_one | timing | unknown → faithful | Initialize called live on every (re)spawn (ClientManager.cs:699), ordered ahead of the PlayerSpawn hook so dynamic_handicap recompute overwrites it. |
| init.defaults_to_one | liveness | partial → live | Invoked on the real spawn path, not just field defaults. |
| level.compute_and_network | logic | missing → faithful | UpdateHandicapLevel (Handicap.cs:84-96) computes floor(map_bound_ranges((take+give)/2,1,2,0,16)) with the disabled→0 branch. |
| level.compute_and_network | values | missing → faithful | MaxLevelEquivalent=2.0 + 0..16 range match handicap.qh:66; ENTCS net width WriteByte/ReadByte. |
| level.compute_and_network | presentation | missing → faithful | handicap_level networked server→client (ScoreboardBlock.cs:84/107/151). |
| level.compute_and_network | liveness | na → live | Computed live on Initialize + every SetForcedHandicap, serialized on every scoreboard block. |
| scoreboard.handicap_icon | logic | missing → faithful | ScoreboardPanel.cs:977-985 draws the marker when HandicapLevel != 0, matching scoreboard.qc:1003-1009. |
| scoreboard.handicap_icon | values | missing → faithful | Tint Color(1,(16-lvl)/15,(16-lvl)/15) = exact white@lvl1 → red@lvl16. |
| scoreboard.handicap_icon | presentation | missing → faithful | The marker rendered (colored square with the exact Base tint; no gfx texture path = intended divergence). |
| scoreboard.handicap_icon | liveness | na → live | Rendered live on the scoreboard from the networked HandicapLevel row field. |
| dynamic.mutator | logic | partial → faithful | All four Base recompute triggers ported live (ClientDisconnect, PlayerSpawn, MakePlayerObserver, AddedPlayerScore filtered to SP_SCORE), routing through SetForcedHandicap; iteration scope matches. |
| dynamic.mutator | timing | partial → faithful | Recompute fires on the same event set as Base (roster shrink + every SP_SCORE delta); dispatch sites confirmed live. |

### sv-impulse
| feature | dimension | was → now | reason |
| --- | --- | --- | --- |
| dispatch.commands_gate | logic | partial → faithful | Minigame impulses routed before the Timeout.IsPaused gate (Commands.cs:1772-1773), matching impulse.qc:379. Both baseline dispatch gaps resolved. |
| weapon.priority_cycle | logic | partial → faithful | PriorityHandle takes the group number and routes through CycleWeaponGroup → WeaponPriorityForGroup (Inventory.cs:489/200), reading per-group cl_weaponpriority0..9. |
| weapon.priority_cycle | values | partial → faithful | List SOURCE matches: per-group cl_weaponpriorityN consulted; priority_group_list constant flipped match:true. |
| weapon.nextprev_singletons | logic | partial → faithful | weapon_next/prev_bygroup (18/19) cycle Inventory.WeaponOrderByImpulse for list==1; by-id/group/priority all faithful. |
| use.usekey | logic | partial → faithful | PlayerUseKey mutator-hook half wired (VehicleBoarding.cs:106 FirePlayerUseKey); CTF flag throw/pass via Ctf.HandleUseKey from DispatchImpulse imp==21. |
| use.usekey | liveness | partial → live | Ctf.HandleUseKey (Ctf.cs:1543) now has a live caller (DispatchImpulse:1831); Keepaway/TeamKeepaway/KeyHunt/Buffs/Powerups subscribe PlayerUseKey live. |
| waypoints.sprites | logic | missing → partial | Impulses 30-39/47/48 implemented + routed to the live WaypointSprites API (deploy + clear w/ CTS/race self-kill). Residual: crosshair/death variants reuse origin, .personal entity + HelpMePing unmodeled. |
| waypoints.sprites | values | missing → faithful | Impulse-number mapping (30-39 deploy, 47/48 clear) + checkpoint self-kill gate match Base. |
| waypoints.sprites | presentation | missing → faithful | Deployed sprites ride the live per-tick WaypointSprites net (markers render on radar/HUD). |
| waypoints.sprites | liveness | na → live | The impulse path reaches live deploy/clear handlers. |
| cheat.impulses | liveness | partial → partial | R00T (148) also routed (DispatchImpulse:1799). Two cheat impulses (99, 148) now live; partial only because 140-147 remain unported. |

### sv-intermission
| feature | dimension | was → now | reason |
| --- | --- | --- | --- |
| matchend.nextlevel | logic | partial → faithful | NextLevel (GameWorld.cs:2950-2991) ports VoteReset, MatchEnd_BeforeScores + MatchEnd hooks, Kill_Notification(NOTIF_ALL) live. Only stats-export/admin-script seams (WeaponStats_Shutdown, DumpStats, sv_hook_gameend) omitted. |
| matchend.nextmap_broadcast | logic | missing → faithful | NextMapBroadcast serialized into the per-second MatchState packet (ServerNet.SendMatchState:899/914) — the port's stand-in for Base's per-client settemp _nextmap stuffcmd. |
| matchend.nextmap_broadcast | presentation | missing → faithful | ClientNet.NextMap decoded and fed to the scoreboard 'Next map:' line on listen and remote clients. |
| matchend.nextmap_broadcast | liveness | na → live | The broadcast→decode→scoreboard-feed chain is on the live match-state path. |
| client.scoreboard_autoshow | logic | missing → faithful | UpdateScoreboard adds the intermission branch (NetGame.cs:3413-3428): force-show at ==1 (no +showscores), hide at ==2 (mapvote owns the screen). |
| client.scoreboard_autoshow | presentation | missing → faithful | The scoreboard auto-draws at intermission without a key and yields to the mapvote panel. |
| client.scoreboard_autoshow | liveness | partial → live | MatchIntermission networked, so the auto-show works on listen and remote clients. |
| client.view_freeze | logic | missing → partial | Half of FixIntermissionClient: the viewmodel is hidden at intermission (NetGame.cs:1575). Camera lock to a FindIntermission camera still missing. |
| client.view_freeze | presentation | missing → partial | The viewmodel EF_NODRAW hide at intermission is live; camera lock + -2342 sentinel pieces still absent. |
| client.view_freeze | liveness | partial → live | MatchIntermission is now an active consumer for the viewmodel-hide path. |
| audio.cdtrack | audio | missing → partial | target_music_kill ported: MusicPlayer.Intermission blanks the map track at match end. The cdtrack 'cd loop' random switch still not consumed. |
| audio.cdtrack | logic | missing → partial | The match-end music-kill logic present; the cdtrack random-selection/loop logic still missing. |
| audio.cdtrack | values | missing → faithful | sv_intermission_cdtrack registered default empty (Cvars.cs:138), matching Base. |
| audio.cdtrack | liveness | na → partial | target_music_kill live; the cdtrack-loop half still missing. |
| client.autoscreenshot | values | missing → partial | sv_autoscreenshot registered default 0 (Cvars.cs:139), matching Base. logic/timing/presentation stay fully missing (no consumer/stuffcmd/trigger). |

### sv-ipban
| feature | dimension | was → now | reason |
| --- | --- | --- | --- |
| softban.chatban | liveness | partial → live | The connect-time re-mute seam (Bans.IsChatBanned) has a live caller: InfraClientConnect (GameWorld.cs:1158) re-sets Player.Muted, so a muted player who reconnects/survives a map change stays muted. |
| softban.chatban | logic | partial → faithful | The connect-time re-application of the mute (Base client.qc:1246) implemented at GameWorld.cs:1158. |
| softban.playban | values | partial → faithful | g_playban_minigames default corrected "1" → "0" (Cvars.cs:382), matching Base. |
| softban.playban | liveness | partial → live | Connect-time play-ban re-spectate live: InfraClientConnect (GameWorld.cs:1156) runs PutObserverInServer for a reconnecting play-banned player. |
| persist | logic | partial → faithful | Shutdown Ban_SaveBans wired in GameWorld.Shutdown (cs:1383); the remaining bansyncer gap is moot/intended (online sync out of scope). |
| persist | timing | partial → faithful | With the shutdown Bans.Save() wired, a long session's persisted seconds-remaining is refreshed against the final clock at session end. |

### sv-mapvoting
| feature | dimension | was → now | reason |
| --- | --- | --- | --- |
| client.input | logic | missing → faithful | Full client vote-input UI (HandleMapVoteInput): arrows skipping unavailable cells, enter/space cast, digit + Ctrl+digit two-digit cast, mouse click/hover — faithful to MapVote_InputEvent + Move* + SendChoice. Baseline had zero input handler. |
| client.input | presentation | missing → faithful | Input writes the panel Selection (highlight/pulse) and casts via CastChoice → CastMapVote → impulse N+1. |
| client.input | liveness | na → live | NetGame._UnhandledInput routes events to HandleMapVoteInput while the panel is Visible (NetGame.cs:3792-3797); the cast reaches the in-process server. |
| cast.vote | liveness | partial → live | The cast path now has a real point-and-click/keyboard client UI instead of only console `impulse N`. |
| cast.abstain | liveness | partial → live | The abstain ballot slot routes through the live client input → CastVote → Abstain. |
| decide.tally_rank | liveness | partial → live | Real votes flow into RecountFromBallots/RankCandidates during a live match instead of only the zero-vote case. |
| decide.early_finish | liveness | partial → live | The unbeatable-leader / all-voted early-finish branches are reachable in a normal match because the client cast UI produces real votes. |
| decide.reduce | liveness | partial → live | The mid-vote reduce can fire and prune options live, dimming them via SetVotes(-1). Presentation stays partial (top-2 fade unfed). |
| suggestions | logic | partial → faithful | CmdSuggestMap records the suggester's netname AND applies the recency gate + gametype-support gate — all three baseline gaps closed. |
| ballot.build | logic | partial → faithful | MapSuggestions is now List<(Map,Suggester)>; GameWorld.cs:3085-3088 passes real suggesters into MapVote.Start. Only the panel-only MAPVOTE_COUNT (30 vs 20) residual remains. |
| gametype_vote | values | partial → faithful | All 8 sv_vote_gametype_* cvars registered with Base-faithful values (added missing 'ca' + reduce_time/count/detail/default_current/maplist_reset, Cvars.cs:437-445). logic/timing/presentation still missing. |
| vote_scoreboard_hide | presentation | missing → faithful | UpdateScoreboard hides the scoreboard whenever the MapVote panel is Visible (models Base's intermission==2 scoreboard-hide). Base's 2342/SVC_FINALE mechanism still unmodeled. |
| vote_scoreboard_hide | liveness | na → partial | The scoreboard-hide effect is live on a listen server; the underlying sentinel/SVC_FINALE logic still has no code. |

### sv-spawnpoints
| feature | dimension | was → now | reason |
| --- | --- | --- | --- |
| put.put_player_in_server | audio | missing → faithful | Wave-6c added the SND_SPAWN cue (Sounds.ByName('SPAWN') → 'misc/spawn') on TriggerAuto, gated on g_spawn_alloweffects bit1 (SpawnSystem.cs:735-736). |
| put.put_player_in_server | timing | partial → faithful | The rot/regen pause primers + the time<game_starttime countdown extension of the spawn shield + pauses implemented (SpawnSystem.cs:756-782). Both were NOTE-only at baseline. |
| put.put_player_in_server | presentation | partial → partial | EF_TELEPORT_BIT \| EF_RESTARTANIM_BIT now SET server-side (SpawnSystem.cs:743, networked via EntityField.Effects). Authority half faithful; stays partial because no client csqcmodel consumer reads the bits. |
| fx.spawn_event_particle | audio | missing → faithful | The same Wave-6c SND_SPAWN addition closes the spawn-event audio half; particle burst + sound now both emitted. |
| fx.spawn_event_particle | values | partial → faithful | g_spawn_alloweffects bit1 honored, so alloweffects=3 (particles\|sound) is fully represented rather than particle-path-only. |
| select.spawn_evalfunc | logic | missing → partial | SpotEvalReject consulted in ScoreSpot on the targetCheck path (SpawnSystem.cs:447); Assault wires it live (Assault.cs:597/631), mirroring target_objective_spawn_evalfunc. race_spawns + SPAWN_PRIO_RACE_PREVIOUS_SPAWN still missing. |
| select.spawn_evalfunc | values | missing → partial | The ASSAULT_VALUE_INACTIVE reject gate matched; SPAWN_PRIO_RACE_PREVIOUS_SPAWN=50 still unimplemented. |
| select.spawn_evalfunc | liveness | na → partial | Code exists + live in Assault; the race half remains unimplemented. |
| select.weighted_point | values | partial → faithful | The spot.cnt multiplier applied in WeightedPick (SpawnSystem.cs:529-530), defaulting a Cnt-0 spot to 1 like QC relocate_spawnpoint. |

### sv-teamplay
| feature | dimension | was → now | reason |
| --- | --- | --- | --- |
| change.team_change_hooks | logic | missing → faithful | Player_ChangeTeam/ChangedTeam hook chain exists (MutatorHooks.cs:68-91), fired by a new SetTeam seam (Teamplay.cs:350): pre-hook can veto, post-hook reacts, 'already on this team' early-out in SetTeam. |
| change.team_change_hooks | liveness | na → live | SetTeam is the single team-set seam on every live team-change path (selectteam GameWorld.cs:889, MovePlayer Commands.cs:1295, ShuffleTeams :1270, AutoBalanceBots Teamplay.cs:597). |
| forced.determine_team | logic | partial → faithful | All three Base forced-team sources act end-to-end: campaign, id/IP-list (cvars registered), otherwise→spectate (force-spectate block ClientManager.cs:326). |
| forced.determine_team | values | partial → faithful | g_forced_team_{red,blue,yellow,pink} ("") + g_forced_team_otherwise ("default") registered with Base defaults (Cvars.cs:171-175). |
| forced.determine_team | liveness | partial → live | The non-campaign id/IP-list path no longer dead (cvars registered, PlayerInList has data) + force-spectate routing wired. |
| join.find_best_team | logic | partial → faithful | AssignBestTeam mirrors TeamBalance_FindBestTeam: size → CompareTeams strength → RANDOM among equal-best via Prandom reservoir + the joinerOnBest early-out. Deterministic lowest-index tie-break gone. |
| join.skill_weighting | logic | partial → faithful | g_balance_teams_skill_unranked_factor (0.666) now consumed; CompareTeams applies the full Base time-scaled score ramp (gated on !IsWarmup). (values stays partial: humans flat 5.) |
| balance.autobalance_bots | timing | partial → partial | AutoBalanceBots also invoked synchronously when a human finishes a team change (GameWorld.cs:892-899). Stays partial: steady-state poll still 3s-cadence + tiebreak uses frag score not SP_SCORE. |
| admin.lock_shuffle_move | logic | partial → partial | shuffleteams does a Prandom reservoir shuffle (Commands.cs:1255-1274) + all admin moves route through SetTeam (firing hooks). Stays partial: MoveToTeam lock backup/restore unported. |

### sv-world-rules
| feature | dimension | was → now | reason |
| --- | --- | --- | --- |
| boot.worldspawn | values | partial → faithful | matchid uses the QC `<counter>.<unixtime>.<rand6>` format (GameWorld.cs:1018), not the bare map name. |
| boot.worldspawn | logic | partial → faithful | The only remaining logic divergence (map-name matchid) resolved; boot bookkeeping now matches Base for the tracked dimensions. |
| misc.max_shot_distance | logic | partial → faithful | max_shot_distance recomputed per-map from world bounds (GameWorld.cs:734); all hitscan/turret/vehicle sites read CurrentMaxShotDistance. |
| misc.max_shot_distance | values | partial → faithful | The clamp value is min(230000f, VLen(maxs-mins)) per map (WeaponFiring.cs:60), matching world.qc:731; 32768 survives only as the fallback. |
| misc.shutdown | liveness | partial → live | GameWorld.Shutdown (cs:1377-1398) now performs slowmo-reset-on-active-timeout, Bans.Save, and PlayerStats.GameReport(false) on the live teardown path. |
| warmup.reset_map | logic | partial → partial | ResetMapObjects fires every non-client map entity's .reset (GameWorld.cs:3576) + LMS reset_map_players, closing the projectile-only-delete gap. Stays partial: per-client accuracy/inventory reset + the .reset2 second pass still absent. |

## Reclassified

Neutral re-labels — status didn't move up or down (corrected notes, dimension reclassification, intended_divergence flips, confidence bumps, gap-text/line-number drift). 10 entries.

| unit | feature | dimension | was → now | reason |
| --- | --- | --- | --- | --- |
| damage-pipeline | heal.dispatcher | values | missing → na | Heal() carries no numeric constants of its own (the per-target cap is owned by the GtEventHeal sink, matching QC); values is na, not missing, now that the dispatcher exists. |
| net-entity-state | entcs.privacy_mask | presentation | intended_divergence false → true | origin/angles privacy deliberately handled by PVS culling (model+entcs folded into one channel) with documented rationale; no longer a plain unported gap. |
| scoring | player.add_set | logic | faithful → faithful | No dimension change; the stale gap describing the inert game_stopped clamp was removed (gaps:[]). The clamp inside AddToPlayer is now armed via GameWorld.cs:1762. |
| scoring | team.two_slot_model | logic | faithful → faithful | No dimension change; added a constant row confirming the AddToTeam game_stopped clamp is armed live (was implicitly inert). |
| sv-anticheat | frame.fixangle_window | values | partial → partial | Still partial but now isolated to a single cause: AntiCheat.PingProvider hardcoded 0f (window always 0.2s); the teleport + endframe sub-gaps are resolved. |
| sv-antilag | clear.on_spawn | liveness | live → live | Still live; confidence raised medium→high after verifying the four explicit set-sites are on live paths and the AntilagTests pass. Gap list emptied. |
| sv-commands-votes | debug_tools.missing | logic | missing → missing | The `anticheat` report command is now implemented and removed from this row's scope; 12 verbs (radarmap/gettaginfo/animbench/bbox/trace/stuffto/adminmsg/delrec/database/effectindexdump/make_mapinfo/printstats) remain missing. Membership shrank. |
| sv-mapvoting | client.draw | presentation | faithful → faithful | Status unchanged but the MV_FADETIME selection-pulse is now exercised because client input writes Selection (a noted baseline gap resolved). Top-2 reduce-fade remains unexercised. |
| sv-mapvoting | override_chain | logic | faithful → faithful | Status unchanged but the override chain is broader (quit_when_empty / quit_and_redirect / lastlevel handled); confidence raised medium→high. No defect change. |
| sv-teamplay | join.skill_weighting | values | partial → partial | Constant g_balance_teams_skill_unranked_factor moved match:false→true (registered AND consumed); but dimension stays partial because the human skill mu/var data is still flat 5 (no TrueSkill). |

---

_Report generated 2026-06-25 from per-unit Base↔port re-audit deltas (Waves 5/6). Registry mode: update — all 20 units re-wrote their `registry/<unit>.yaml`._
