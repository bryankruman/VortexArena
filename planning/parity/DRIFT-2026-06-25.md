# Parity drift — 2026-06-25

**0 regressions · 0 new gaps · 2 registry-coverage gaps · 5 fixes · 0 reclassified** across 2 units (domination, tka). No parity got worse.

## Regressions

No drift detected. (No parity got worse since the baseline.)

## New gaps

No drift detected. (No newly-introduced parity gaps.)

## Registry coverage gaps

Base features with no dedicated registry row before this diff (the registry was missing coverage for these). These are not regressions — they are uncovered Base behavior surfaced during the re-audit and added as rows.

| unit | Base feature | status / note |
| --- | --- | --- |
| domination | `domination.capture.eventlog` | `dom_EventLog :dom:taken:` GameLogEcho on capture (sv_domination.qc `dompoint_captured`, sv_eventlog-gated). Not ported, had no registry row. Added as `domination.capture.eventlog`. |
| domination | `domination.roundbased.player_blocked` | Round-based `player_blocked` set/clear (`reset_map_players` + `PlayerSpawn` + `Domination_RoundStart`). Not ported, had no registry row. Added as `domination.roundbased.player_blocked`. |

## Fixes

Parity that improved since the baseline. Grouped by unit.

### domination

| feature | dimension | was -> now | reason |
| --- | --- | --- | --- |
| domination.controlpoint.spawn | presentation | partial -> faithful | Both prior presentation gaps are now closed in Domination.cs: `e.ScaleFactor=0.6f` is set (line 374) and `AnimateDomPoint` (TWidth 0.02 / TLength 239 frame loop) is ported and called live from `PointThinkEntity` (lines 580-605). Row now has zero gaps. |
| domination.controlpoint.spawn | values | scale constant match:false -> match:true | QC `spawnfunc(dom_controlpoint)` scale 0.6 is now applied via `e.ScaleFactor = 0.6f` (line 374); baseline recorded it as not applied (match:false). |
| domination.controlpoint.spawn | values | frame_anim constant match:false (none) -> match:true (0.02/239) | QC `AnimateDomPoint` frame loop (t_width 0.02 / t_length 239, ++frame) is now ported (`ControlPoint.TWidth/TLength` + `AnimateDomPoint`) and invoked live each think from `PointThinkEntity`; baseline recorded it as not ported. |

### tka

| feature | dimension | was -> now | reason |
| --- | --- | --- | --- |
| tka.ball.drop | liveness | partial -> live | Use-key (`OnUseKey` on `MutatorHooks.PlayerUseKey`, fired via VehicleBoarding.cs:106 `FirePlayerUseKey` on the +use impulse path) plus observer+disconnect (`OnPlayerRemoved`, dispatched from ClientManager.cs:513/:761) drop triggers are now wired. 3 of Base's 4 drop hooks are live; only `DropSpecialItems` remains unwired. |
| tka.waypoints.tracking | values | partial -> faithful | `g_tkaball_tracking`'s three modes (0=none / 1=always / 2=dropped-only) are now fully distinguished: `CarrierWaypointVisibleFor` returns `BallTracking==1` for enemies, so `==2` hides the carrier sprite from enemies (QC `return autocvar_g_tkaball_tracking == 1`). Constant match flipped false->true. The carrier sprite also now uses SPRITERULE_TEAMPLAY + a per-viewer `VisibleForPlayer` predicate honored at ServerNet.cs:996, modeling same-team/spectator/warmup/tracking==1; presentation stays partial only for the spectatee-of-carrier + IS_INVISIBLE nuances. |

## Reclassified

No reclassifications.
