# Parity drift — 2026-06-25

**1 regression, 0 new gaps, 4 registry-coverage gaps, 39 fixes, 16 reclassified** across 14 units.

## Regressions

| unit | feature | dimension | was -> now | reason |
|---|---|---|---|---|
| fx-deathtypes | message.teamchange | liveness | live -> partial | AUTOTEAMCHANGE is live (Teamplay.cs:698) but manual DEATH_TEAMCHANGE now has no live emitter (all force-kill team changes route through AUTOTEAMCHANGE), so its newly-selectable self-line is latent. Neutral text-wise; recorded honestly as partial liveness. |

## New gaps

No new gaps.

## Registry coverage gaps

Base features with no registry row (rows were added during this pass where noted):

- **invasion** — `invasion.spawn.monster_hitcontents`: Base `MUTATOR_HOOKFUNCTION(inv, MonsterSpawn)` stamps `mon.dphitcontentsmask = DPCONTENTS_SOLID|DPCONTENTS_BODY|DPCONTENTS_BOTCLIP|DPCONTENTS_MONSTERCLIP` on every invasion monster so it collides with bot-clip/monster-clip brushes; the port's invasion spawn path (Invasion.cs:SpawnMonsterDef / MonsterAI.SpawnMonster) never sets this mask (only the golem/spider PROJECTILE_MAKETRIGGER case at MonsterAI.cs:1470 sets a hit-contents mask). Added as a new registry row (status logic:missing). Minor; only matters on maps that fence monster movement with botclip/monsterclip volumes.
- **weapon-devastator** — `weapon-devastator.presentation.impact_fx`: CSQC `wr_impacteffect` (EFFECT_ROCKET_EXPLODE + SND_ROCKET_IMPACT impact explosion FX/sound); implemented in port via Explode/DoRemoteExplode (faithful cues, intended server-emit vs CSQC-impact-event divergence).
- **weapon-devastator** — `weapon-devastator.menu.describe`: MENUQC describe (weapon-guide description text); not ported (missing), low gameplay impact.
- **items-pickups** — three Base item features with no rows (added as rows during this pass):
  - `items-pickups.loot.item_damage` (logic: missing): `Item_Damage` (server/items/items.qc:981) — loot with `takedamage=YES` is destroyed by `ITEM_DAMAGE_NEEDKILL` damage (lava/sky jets); port sets `TakeDamage=Yes` but wires no `event_damage` handler.
  - `items-pickups.target.use_setactive` (logic: missing): `item_use` / `item_setactive` (server/items/items.qc:987,995) — targeted-item trigger-to-spawn (`spawnflags&16` -> touch, else respawn) + active/toggle relay; port wires no `.use` handler on items.
  - `items-pickups.team.finditem` (logic: missing): `Item_FindTeam` / `Item_CopyFields` (server/items/items.qc:813,851) — team-grouped items show one random member at a time, re-rolled on respawn; `StartItem` always calls `Item_Reset` (never the `if(team)` branch).

## Fixes

| unit | feature | dimension | was -> now | reason |
|---|---|---|---|---|
| damage-pipeline | resource.player_damage | presentation | partial -> faithful | Both recorded presentation gaps closed in Wave 8e (commit 412ca19). IsTypeFrag now reads the live victim ButtonChat (DamageSystem.cs:436), matching player.qc:304; producer now live, Scores.cs consumer already plumbed. Bot-aim shake now ported (DamageSystem.cs:509-527), exact port of player.qc:388-395. Only the host-side ANIMACTION_PAIN keyframe picker remains out of seam (by design). |
| damage-pipeline | fire.burning | logic | faithful -> faithful | Recorded lava-contents ignition gap now closed (Wave 8e, 412ca19): PlayerFrameLogic.HotLiquids (PlayerFrameLogic.cs:375-379) reads g_balance_contents_playerdamage_lava_burn and feeds StatusEffectsCatalog.FireAddDamage(..., DeathTypes.Lava) after the instant lava Damage — exact port of main.qc:104-105. Gaps array reduced to empty. |
| deathmatch | winconditions.timelimit_overtime | presentation | na -> faithful | Overtime/sudden-death CENTER notifications now wired live: GameWorld.OverTime supplies OnOvertimeStarted -> NotificationSystem.Send(Center, OVERTIME_TIME, seconds) and OnSuddenDeathStarted -> Center OVERTIME_FRAG (GameWorld.cs:144-147), both registered in NotificationsList.cs:479-480. |
| fx-deathtypes | message.special_nade_misc | logic | partial -> faithful | SelectSpecial now maps the full NADE family (Nade/Napalm/Ice/Heal/Darkness) plus cheat/rot/shooting_star/touchexplode; baseline recorded these branches as absent (only CAMP present). |
| fx-deathtypes | message.special_nade_misc | values | partial -> faithful | nade self/murder lines now resolve to DEATH_SELF_NADE/DEATH_MURDER_NADE (and variants) instead of falling to GENERIC/FRAG; matches all.inc cols 2/3. |
| fx-deathtypes | message.special_nade_misc | presentation | partial -> faithful | correct nade/cheat/rot/touchexplode obituary text is now selected; rows were already present and are now used. |
| fx-deathtypes | message.special_nade_misc | liveness | partial -> partial | NADE family now live (5 nade booms emit via Combat.Damage when g_nades), plus cheat/rot/touchexplode live emitters; only SHOOTING_STAR remains selector-only (dead). Net improvement within partial. |
| fx-deathtypes | message.betrayal_suicide_text | logic | partial -> faithful | KILL->DEATH_SELF_SUICIDE, MIRRORDAMAGE->DEATH_SELF_BETRAYAL now mapped in SelectSpecial, and CTS /kill obituary suppression added (Scores.cs:586-588). Baseline had all three open. |
| fx-deathtypes | message.betrayal_suicide_text | values | partial -> faithful | kill self line = DEATH_SELF_SUICIDE and mirrordamage self line = DEATH_SELF_BETRAYAL now match Base all.inc:12/14. |
| fx-deathtypes | message.betrayal_suicide_text | presentation | partial -> faithful | correct suicide/betrayal obituary text + CTS suppression now rendered. |
| fx-deathtypes | message.teamchange | logic | partial -> faithful | EmitObituary (Scores.cs:594-604) now routes TEAMCHANGE/AUTOTEAMCHANGE to their own self-line per damage.qc:272-273; baseline had no teamchange case. |
| fx-deathtypes | message.teamchange | values | partial -> faithful | death_team f1 arg (victim.Team) now passed to DEATH_SELF_(AUTO)TEAMCHANGE rows, matching the death_team float arg in all.inc rows. |
| fx-deathtypes | message.teamchange | presentation | partial -> faithful | AUTOTEAMCHANGE self obituary now renders correct line + destination team instead of DEATH_SELF_GENERIC. |
| fx-deathtypes | message.mapper_custom | logic | partial -> faithful | DEATH_CUSTOM selection+emission now wired (DeathTypes.Custom const + Scores.cs:610-621 'You were %s' formatting matching damage.qc:457). VOID_ENT already faithful in baseline. |
| fx-deathtypes | message.mapper_custom | values | partial -> faithful | DEATH_CUSTOM strstrofs('%') format guard ported; both VOID_ENT and CUSTOM constant gates match Base. |
| fx-deathtypes | message.mapper_custom | presentation | partial -> faithful | DEATH_SELF_CUSTOM message formatting now correct when invoked; rows present. |
| fx-deathtypes | special.suicide_special_routing | logic | partial -> faithful | EmitObituary now carries the full per-deathtype suicide/accident sub-routing (CTS-kill return, TEAMCHANGE f1=team, DEATH_CUSTOM, VOID_ENT msg_from_ent); baseline residual TEAMCHANGE/MIRRORDAMAGE routing now present. |
| fx-deathtypes | special.suicide_special_routing | presentation | partial -> faithful | the per-deathtype obituary self-lines (teamchange/custom/void_ent) now render correctly. |
| fx-deathtypes | special.suicide_special_routing | audio | partial -> faithful | ANNCE_ACHIEVEMENT_BOTLIKE now emitted (EmitBotlikeAchievement, Scores.cs:773-779) on an accident driving score to -5, correctly gated to the no-player-attacker branch per damage.qc:471-476. |
| fx-effectinfo | model.baseline_defaults | values | partial -> faithful | Port baseline defaults corrected: StainColor0/1 now 0xFFFFFFFF (was 0xFFFFFF) and StainSizeMin/Max now 2f (was 1f), matching DP baselineparticleeffectinfo (cl_particles.c:271-274). All baseline defaults now match Base exactly. |
| fx-effectinfo | model.baseline_defaults | presentation | partial -> faithful | staincolor is now consumed as a MODDING FACTOR (StainMidColor()/ModFactor: byte/128, -1 => neutral 1.0), reproducing DP's stain = staincolor*particlecolor/0x8000 (cl_particles.c:740-768); corrected stainsize=2 baseline means the ~22 stainsize-omitting staintex blocks no longer render at half size. |
| fx-sounds | presentation.hitsound_sample | audio | missing -> faithful | HitSound.cs:118 now loads 'misc/hit' (with res://sound/misc/hit.wav fallback), the shipped file, instead of the nonexistent 'misc/hitconfirm'. The long-standing hitsound-silent bug is resolved. |
| fx-sounds | presentation.hitsound_sample | presentation | partial -> faithful | Correct sample path now resolves and plays; the feature is no longer silent. |
| fx-sounds | emit.sound_allowed | logic | missing -> faithful | SoundAllowedGate now exists (SoundSystem.cs) and faithfully ports the owner-walk re-home (body->enemy/realowner/owner) and the bot_sound_monopoly deny branch. |
| fx-sounds | emit.sound_allowed | values | missing -> faithful | bot_sound_monopoly cvar (default 0) read + honored; constants match Base. |
| fx-sounds | emit.sound_allowed | audio | missing -> faithful | Gate decision (allow/deny emit) now implemented and applied on the vehicle emit paths. |
| fx-sounds | emit.sound_allowed | liveness | na -> partial | IsAllowed is live on VehicleCommon hit/shield paths (502/511/522) and inside SpamSound; but WalkOwner (owner-walk) has zero callers and the gate is not on the general SoundService.Play path, so liveness is partial not live. |
| fx-sounds | emit.play2_family | timing | partial -> faithful | spamsound is now ported (SoundSystem.SpamSound/SpamSoundRaw + Entity.SpamTime) with the QC time>spamtime rate-limit and sound_allowed gate; per-entity over-emit risk (nade bounce / vehicle hit) is closed. |
| fx-sounds | voice.globalsound_dispatch | presentation | partial -> faithful | _GlobalSound now reads cl_voice_directional (==1 -> ATTEN_MIN else ATTEN_NONE) and applies the taunt-attenuation bound(ATTEN_MIN, cl_voice_directional_taunt_attenuation, ATTEN_MAX); two of three baseline gaps closed. |
| items-pickups | respawn.scheduling | presentation | partial -> faithful | RespawnCountdown (ItemPickupRules.cs:734-769) now spawns the WP_Item/WP_Weapon respawn-countdown waypoint sprite, pings it each tick, and drives the build-progress bar — matching Item_RespawnCountdown (items.qc:275-294). Added in Wave 8e (412ca19). |
| items-pickups | respawn.scheduling | audio | missing -> faithful | RespawnCountdown (ItemPickupRules.cs:774-784) now plays the per-second ITEMRESPAWNCOUNTDOWN cue (Api.Sound.Play, TriggerAuto) every countdown tick, matching soundto(... ITEMRESPAWNCOUNTDOWN ...) at items.qc:305. The prior baseline recorded this as the worst gap. |
| items-pickups | touch.gate | logic | faithful -> faithful | Baseline gap 'ItemTouch / ItemTouched mutator hook chain not present' resolved: MutatorHooks.FireItemTouch (ItemPickupRules.cs:372) and FireItemTouched (l.417, with IsFreed/wasfreed re-check) now fired with live subscribers (Instagib/LMS/SuperSpec on ItemTouch; Powerups/RandomItems on ItemTouched). |
| items-pickups | show.visibility | presentation | partial -> partial | Two of three baseline gaps fixed (Wave 8e): Item_Show now emits EF_NODEPTHTEST (ll.517-518) and EF_FULLBRIGHT (ll.522-523), and the powerup ITS_GLOW additive (EF_ADDITIVE\|EF_FULLBRIGHT, ll.512-513). Stays 'partial' only because simple-items (ITS_ALLOWSI) remains unported. |
| mapobject-func | door.keys | presentation | missing -> faithful | DoorCheckKeys now sends CENTER_DOOR_UNLOCKED / DOOR_LOCKED_NEED / DOOR_LOCKED_ALSONEED via NotificationSystem.Send(Center) with item_keys_keylist (Doors.cs:489,501-502); names defined NotificationsList.cs:937-939; reaches client via live NotificationNetSink (ServerNet.cs:300). |
| mapobject-func | door.keys | audio | partial -> faithful | QC NEED-vs-ALSONEED two-branch split (valid==0 ? NEED : ALSONEED) now reproduced (Doors.cs:497-504); the prior single-noise3-throttle collapse is gone. Both unlock (noise) and locked (noise3) sounds play with the matching notification. |
| mapobject-misc | dynlight.render | presentation | missing -> faithful | Wave-8e added game/client/DynamicLightRenderer.cs: persistent OmniLight3D per dynlight driven each frame from the server entity, plus .style animation via the new LightStyles.Sample table. Baseline worst-gap (authored map lights invisible) closed. Residual: animated world-brush LIGHTMAP modulation (separate subsystem). |
| mapobject-misc | dynlight.render | liveness | na -> live | DynamicLightRenderer is instantiated and AddChild'd in ClientWorld.cs:367 (property at :51) on the live client world path, scanning the ambient entity facade like LaserRenderer/MapParticleEmitters. |
| mapobject-misc | laser.beam_render | timing | partial -> faithful | Wave-8e removed the EndFxInterval ~10Hz throttle; LaserRenderer.cs:296-301 now emits the end-effect EVERY frame at count=FrameTime*1000, exactly matching Draw_Laser's __pointparticles(cnt,...,drawframetime*1000). |
| mapobject-misc | laser.beam_render | values | partial -> faithful | The per-second end-fx emission rate and the additive-vs-normal blend split are now Base-exact: both 'end fx rate' and 'additive when no alpha' constants are now match:true. |
| mapobject-misc | laser.beam_render | presentation | partial -> faithful | Beam blend is now a real Mix-at-alpha vs Add-at-0.5 split (LaserRenderer.cs:262-272) matching Draw_Laser:325-330. Only residual is the static-laser no-InterpolateOrigin approximation and the crossed-quad ribbon stand-in. |
| mapobject-misc | keys.item_key | presentation | partial -> partial | MF_ROTATE key spin now wired: SpawnItemKey sets Entity.ModelSpinRotate=true and EntityNode.cs:84-88 adds yawDeg += 100*fmod(time,3.6), byte-faithful to csqcmodel_hooks.qc:617. Stays partial because the centerprint sink remains unassigned (no-op). |
| mapobject-movers-platforms | plat.state_machine | audio | partial -> faithful | Plat go/hit sounds now pass SoundChannel.Item (=3) in all four state functions (Platforms.cs:207/216/225/235), which equals Base CH_TRIGGER_SINGLE (=3). Baseline recorded SoundChannel.Voice (=2) mismatch; divergence gone. |
| mapobject-movers-platforms | plat.state_machine | verification | fail (sound channel) -> pass | The verification entry that asserted plat Sound calls pass SoundChannel.Voice (result: fail) is now pass: Services.cs:119 Item=3 and Platforms.cs:207/216/225/235 all pass SoundChannel.Item, matching Base CH_TRIGGER_SINGLE=3. |
| mapobject-target | spawnpoint.use | liveness | partial -> live | Baseline said actor.SpawnPointTarg is written but read by NOTHING. spawnsystem unit has since wired SpawnSystem.SelectSpawnPoint (SpawnSystem.cs:344) to short-circuit-return forPlayer.SpawnPointTarg as the forced next spawn (cleared one-shot in PutPlayerInServer at SpawnSystem.cs:678). Race.cs:1021 also writes it. |
| mapobject-target | speaker.ambient_triggered | logic | partial -> partial | Gap shrink: baseline recorded 'speaker installs NO Reset hook'. TargetSpeaker.cs now installs this_.Reset = SpeakerReset on both LOOPED_ON (110) and LOOPED_OFF (118), driven by the live ResetMapObjects e.Reset pump. LOOPED_OFF-untargeted objerror remains the only logic gap. |
| mapobject-target | speaker.ambient_triggered | audio | partial -> partial | Gap shrink: baseline said the *-prefixed per-player voice-sample 'resolves to silence (always)'. ResolveNoise(noise, activator) now routes a *-name through Sounds.PlayerSoundSample + the host-wired ModelSoundResolver (PlayerSoundResolver.cs:47). MSG_ONE per-client targeting gap remains. |
| teleporters | core.teleport_player | presentation | partial -> faithful | EF_TELEPORT_BIT now XOR-toggled on teleport (Teleporters.cs:359 player.Effects ^= EffectFlags.Teleport) matching teleporters.qc:122, and istypefrag = ButtonChat now set in the kill-credit window (Teleporters.cs:383) matching teleporters.qc:166. Both baseline core gaps closed. |
| teleporters | fx.sound | audio | partial -> faithful | Sound channel is now SoundChannel.TriggerAuto (CH_TRIGGER = -3, Teleporters.cs:328 / Services.cs:112), matching Base's CH_TRIGGER. Baseline recorded mislabeled SoundChannel.Item(3) with match:false; the channel constant now matches. |
| portals | weapon.cleanup | liveness | partial -> live | PortalClearWithId and PortalClearAll are now ASSIGNED by the host (GameWorld.cs:425-429) to real Warpzone teardown methods (ClearPortoPortal/ClearAllPortoPortals). Baseline's declared-but-never-assigned no-op dead seam now wired; combined-shot soft-fail and player death/reset/race-checkpoint actually tear down portals. |
| portals | weapon.cleanup | logic | partial -> faithful | ClearPortoPortal/ClearAllPortoPortals (Warpzone.cs:442-483) remove the pending in-portal + any linked partner + trigger volumes; Porto.WrPlayerDeath (new) + Race.cs:372 drive PortalClearAll on death/checkpoint. Matches Portal_ClearWithID/Portal_ClearAll. |
| portals | weapon.cleanup | timing | missing -> partial | Death/reset/checkpoint teardown timing now implemented and live, but there is still no 15s g_balance_portal_lifetime expiry, re-shoot-after-complete does not replace the prior pair, and death dispatch is held-weapon-slot-only; upgraded from missing to partial. |
| portals | teleport.transform_and_amazing | values | missing -> partial | portal_activatetime (0.1s) owner self-skip now modeled (Warpzone.ActivateTime, Warpzone.cs:231-235, seeded at PlacePortoPortal:418) matching server/portals.qc:301-306,646. amazing_window_s and the +/-48 plane-shift remain absent, so values is partial not faithful. |
| portals | teleport.transform_and_amazing | liveness | partial -> live | The Porto warpzone teleport path is fully wired and live (PortalSpawner -> PlacePortoPortal, GameWorld.cs:421-422), and now additionally applies the portal-owner kill-credit (Warpzone.cs:263-272) and activatetime self-skip on the live crossing. |
| mapobject-triggers | common.sub_usetargets | presentation | partial -> faithful | MapObjectsCommon.Centerprint (MapObjectsCommon.cs:825) now routes the message TEXT through NotificationSystem.SendCenterRaw(OneOnly) directly (cs:834) — a live channel, no longer dependent on the unassigned CenterprintHandler delegate. |
| mapobject-triggers | secret.trigger_secret | presentation | missing -> faithful | The 'You found a secret!' centerprint is now surfaced — MapMover.Centerprint(toucher, message) at Triggers.cs:672 routes through SendCenterRaw (live channel) rather than the dead CenterprintHandler. |
| mapobject-triggers | keylock.trigger_keylock | presentation | missing -> faithful | Both the 'Unlocked!' centerprint (Triggers.cs:965 -> SendCenterRaw) AND the CENTER_DOOR_LOCKED_ALSONEED/_NEED keylist text (Triggers.cs:944/950 -> NotificationSystem.Send Center) are now surfaced on the live path. |
| mapobject-triggers | counter.trigger_counter | presentation | missing -> faithful | SEQUENCE_COMPLETED / SEQUENCE_COUNTER / SEQUENCE_COUNTER_FEWMORE center-print countdown TEXT now emitted via NotificationSystem.Send(One, Center, ...) (Triggers.cs:506/522/524) through the live notification channel. |
| mapobject-triggers | push.trigger_push | presentation | partial -> partial | The message-jumppad centerprint (Jumppads.cs:254) now delivered live via SendCenterRaw — that dead-presentation gap closed. Stays partial only because the animdecide JUMP pose + jumppadcount/jumppadsused bookkeeping remain unported. |
| mapobject-triggers | music.target_music | liveness | live -> live | The target_music_reset / trigger_music_reset round-restart gap closed: TargetMusicSetup/TriggerMusicSetup DO assign this_.Reset (TargetMusic.cs:44/131) and GameWorld.ResetMapObjects now dispatches e.Reset (GameWorld.cs:3905). Dormant sub-feature became live. |

## Reclassified

| unit | feature | dimension | was -> now | reason |
|---|---|---|---|---|
| deathmatch | winconditions.timelimit_overtime | audio | unknown -> na | Base OVERTIME_TIME/OVERTIME_FRAG are MSG_CENTER_NOTIF (center-print) with no announcer sound (all.inc:778-780). There is no overtime announcer cue in Base, so audio is na (not unknown). The baseline 'Overtime has begun announcer' framing was imprecise. |
| fx-sounds | presentation.hitsound_sample | values | partial -> partial | Sample path value corrected (now faithful), but the residual values gap is now the mode 2/3 pitch curve: the port uses a linear damage->pitch ramp vs QC's asymptotic gradient formula (a/b/c). Dimension stays partial for a different reason; inaudible at the default cl_hitsound 1. |
| mapobject-func | button | liveness | live -> live | Stale gap note corrected: button_reset round-restart re-arm is LIVE (ButtonReset assigned Buttons.cs:42, dispatched by GameWorld.ResetMapObjects:3887). The baseline gap claimed it was 'STILL dead' and the Buttons.cs:15 comment was wrong; both claims were stale. |
| mapobject-func | plat | liveness | live -> live | Stale gap note corrected: plat_reset round-restart .reset is LIVE (PlatReset assigned Platforms.cs:82, dispatched by ResetMapObjects). Baseline gap said 'STILL NOT wired'. |
| mapobject-func | rotating | liveness | live -> live | Stale gap note corrected: func_rotating_reset is LIVE (RotatingReset assigned MovingBrushes.cs:75, dispatched by ResetMapObjects). Baseline gap said 'STILL not ported/wired'. |
| mapobject-func | door_secret | liveness | live -> live | Stale gap note corrected: secret_reset is LIVE (SecretReset assigned TargetUtilities.cs:637, dispatched by ResetMapObjects). Baseline gap said 'STILL dead'. |
| mapobject-func | door.slide | liveness | live -> live | Stale gap note corrected: door_reset IS ported (DoorReset, Doors.cs:206; assigned :70) and LIVE (dispatched by ResetMapObjects). Baseline gap said 'STILL NOT ported and not wired'. |
| mapobject-func | door.rotating | liveness | live -> live | Stale gap wording corrected: the 'dead-reset' phrasing was stale — DoorReset is wired for door_rotating too (Doors.cs:130) and is live. Also inherits the now-live key-door cpid notifications. |
| mapobject-func | breakable | liveness | live -> live | Stale gap note corrected: func_breakable_reset IS ported (BreakableReset assigned Breakable.cs:82) and LIVE (dispatched by ResetMapObjects). Baseline gap said 'STILL not ported/wired'. Residual team_saved/colormod look_restore still noted. |
| mapobject-misc | dynlight.render | logic | na -> faithful | Now that a render consumer exists, the render decision flow (toggle/radius/color/style gating) is implemented and faithful; logic is no longer N/A for this presentation feature. |
| mapobject-misc | dynlight.render | values | na -> faithful | light_lev->radius (1qu=1godot), color hue/energy split, and defaults now have concrete, Base-matching values; reclassified from na. |
| mapobject-misc | dynlight.render | timing | na -> faithful | Per-frame light update + 10fps style sampling (wall-clock paced) now exist; timing reclassified from na to faithful. |
| mapobject-target | location.spawn | presentation | unknown -> faithful | The %l/%y/%d HUD/chat location reader was untraced in the baseline. Chat.NearestLocation (Chat.cs:651) walks MapObjectsState.Locations and returns the nearest label, reached from the chat formatmessage token expansion (Chat.cs:584-586). Confirmed faithful (only the item-fallback branch is out of scope). |
| mapobject-target | location.spawn | liveness | unknown -> live | The location volumes pushed to MapObjectsState.Locations are now confirmed consumed on the live chat token-expansion path via Chat.NearestLocation. |
| mapobject-triggers | monoflop.trigger_monoflop | liveness | live -> live | Reset gap REASON changed: GameWorld.ResetMapObjects now DOES dispatch e.Reset (GameWorld.cs:3905), but MonoflopSetup still never assigns this_.Reset (LogicGates.cs:99 'dormant' comment), so the reset handler stays dead for a different reason. Net effect unchanged. |
| mapobject-triggers | flipflop.trigger_flipflop | liveness | live -> live | Reset gap reason corrected: the ResetMapObjects pump is now live but FlipflopSetup (LogicGates.cs:65) never installs this_.Reset, so the latch reset stays dormant. Net effect unchanged. |
| mapobject-triggers | multivibrator.trigger_multivibrator | liveness | live -> live | Reset gap reason corrected: ResetMapObjects now dispatches e.Reset but MultivibratorSetup (LogicGates.cs:147-163) never assigns this_.Reset, so multivibrator_reset stays dormant. Net effect unchanged. |
| mapobject-triggers | disablerelay.trigger_disablerelay | liveness | live -> live | Reset gap reason corrected: pump now live but DisableRelaySetup (LogicGates.cs:228) installs no this_.Reset. Net effect unchanged. |
| mapobject-triggers | relay_if.trigger_relay_if | liveness | live -> live | Reset gap reason corrected: pump now live but RelayIfSetup (LogicGates.cs:277) installs no this_.Reset. Net effect unchanged. |
| mapobject-triggers | relay_teamcheck.trigger_relay_teamcheck | liveness | live -> live | Reset gap reason corrected: pump now live but RelayTeamCheckSetup (LogicGates.cs:313) installs no this_.Reset (and skips the team_saved push). Net effect unchanged. |
| mapobject-triggers | relay_activators.relay_activate | liveness | live -> live | Reset gap reason corrected: pump now live but RelayActivatorsInit (LogicGates.cs:353) installs no this_.Reset. Net effect unchanged. |
| mapobject-triggers | gamestart.trigger_gamestart | liveness | partial -> partial | Reset2 gap reason corrected: pump now live but GamestartSetup (LogicGates.cs:411) installs no this_.Reset (moot since gamestart self-deletes). GameStartTimeProvider still unwired (GameWorld.cs:627 wires only StartItem's), so wait>0 timing fidelity unchanged. |
