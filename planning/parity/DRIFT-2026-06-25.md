# Parity drift — 2026-06-25

**0 regressions, 22 fixes, 2 new gaps, 1 registry-coverage gap, 0 reclassified** across 7 re-audited units (turret-framework, turret-hk, vehicle-framework, vehicle-raptor, warpzones, weapon-rifle, weapon-tuba).

## Regressions

No drift detected — no parity got worse.

## New gaps

Newly-discovered gaps in mapped features:

| unit | feature | dimension | was -> now | reason |
| --- | --- | --- | --- | --- |
| turret-hk | turret-hk.weapon.player | liveness | na -> dead | HunterKillerWeapon carries NO `[Weapon]` attribute (registration deferred to avoid breaking WeaponByIdTests SpecialAttack contract), so the impulse-9 / give weapon_turret_hk path has no live caller through WeaponFireDriver; WrThink is unreachable in a normal match. Baseline had liveness na (feature missing); now code exists but is dead. |
| vehicle-raptor | raptor.missile_alarm | coverage | no row (unmapped) -> row added (logic/values/timing/audio faithful, liveness live) | The server-authoritative incoming-guided-missile alarm in raptor_frame (distinct from the decoy flares) was implemented live and faithfully in the port (Frame:414-432, separate VehAlarmNextTime once/sec gate matching QC's distinct `.bomb1.cnt` vs `.sound_nexttime`) but had no registry row. Added with one minor noted divergence (QC soundto(MSG_ONE) pilot-only routing broadened to a positional sound on the vehicle entity). |

## Registry coverage gaps

Base features with no registry row (registry was missing coverage):

| unit | Base feature (proposed row) | summary |
| --- | --- | --- |
| vehicle-raptor | `raptor.missile_alarm` | Incoming guided-missile warning sound (raptor.qc raptor_frame bomb1.cnt scan block, lines 405-425): scans g_projectiles for `.enemy==vehic` with MIF_GUIDED_TRACKING within 2*flare_range, once/sec gate, plays SND_VEH_MISSILE_ALARM. Implemented LIVE + faithfully in port Raptor.cs:Frame:414-432 but had no registry row. Added as a faithful/live row this audit. |

## Fixes

Parity that improved since the baseline:

| unit | feature | dimension | was -> now | reason |
| --- | --- | --- | --- | --- |
| turret-framework | turret-framework.projectile.generic | presentation | partial -> faithful | Wave-9-11 (commit ff8a699) added a projType parameter to TurretSpawn.Projectile and moved the NetName trail stamp INSIDE the helper (TurretSpawn.cs:122: `if (projType.Length != 0) proj.NetName = projType`), mirroring Base's turret_projectile which stamps `_proj_type` inline (sv_turrets.qc:487). The baseline gap (helper leaves NetName empty; only callers stamp it, so a turret that forgot would render trailless) is closed — the helper now owns the trail. All callers (plasma electro_bolt, mlrs devastator, flac hagar, ewheel blaster) pass projType. |
| turret-hk | turret-hk.weapon.player | logic | missing -> faithful | WEP_HK / HunterKillerAttack is now fully ported in src/XonoticGodot.Common/Gameplay/Turrets/HunterKillerWeapon.cs (WrThink ports the IS_PLAYER branch of hk_weapon.qc wr_think: PrepareAttack -> SetupShot -> GuidedProjectile.Launch -> te_explosion). Baseline recorded it as 'not ported at all'. |
| turret-hk | turret-hk.weapon.player | values | missing -> faithful | Constants now match Base: flags Hidden\|SpecialAttack, impulse 9, netname turret_hk, refire 1s, anim 0.5s, and the correct turret_initparams GENERIC fallback balance 50/25/37.5/2500 (NOT the HK turret 120/200/600) plus the shared guidance-band autocvars. |
| turret-hk | turret-hk.weapon.player | timing | missing -> faithful | RefireFor=1f (weapon_prepareattack attacktime 1) and AnimtimeFor=0.5f (weapon_thinkf WFRAME_FIRE1 0.5) ported faithfully. |
| turret-hk | turret-hk.weapon.player | presentation | missing -> faithful | Muzzle te_explosion (EffectEmitter.TeExplosion) and the PROJECTILE_ROCKET trail (shared GuidedProjectile.Launch NetName='rocket') are emitted; the IS_PLAYER head-frame kick is correctly skipped. |
| turret-hk | turret-hk.weapon.player | audio | missing -> faithful | weapons/rocket_fire.wav played on SoundChannel.Weapon, mirroring SND_HunterKillerAttack_FIRE on CH_WEAPON_B. |
| vehicle-framework | vehicle-framework.presentation.hud_camera_xhair | audio | missing -> faithful | Wave 11 (commit ff8a699, landed after the prior Wave-8 baseline) ported the framework low-health/shield HUD alarm into VehicleHud.UpdateAlarms (game/hud/VehicleHud.cs:167-247): SND_VEH_ALARM (vehicles/alarm, health<25%, +2s re-arm) / SND_VEH_ALARM_SHIELD (vehicles/alarm_shield, shield<25%, +1s re-arm), gated by cl_vehicles_alarm (default 0 -> silent, matching Base), with the SND_Null stop-on-recover/exit branch. Bit-faithful to vehicle_alarm + Vehicles_drawHUD (cl_vehicles.qc:3-9/228-267). AudioLoader fed at NetGame.cs:1402; driven live by UpdateVehicleHud's per-frame Health/Shield mirror (NetGame.cs:3158, called :2503). Sound samples exist in VFS. Cockpit/chase camera remains the only dead piece of this row. |
| warpzones | warpzones.reconnect | logic | missing -> faithful | WarpZoneManager.ThinkMovingZones (moving-warpzone re-derive, self-gated on warpzone_save_* snapshot, matching server.qc:714-731) + Reconnect/ReconnectByTarget/OnReconnectUse (runtime re-link, matching trigger_warpzone_reconnect_use server.qc:785-805) are now implemented in Warpzone.cs:607-690. |
| warpzones | warpzones.reconnect | timing | missing -> faithful | ThinkMovingZones is called every server frame (GameWorld.cs:1648) and only re-derives when the brush/partner moved (warpzone_save_* gate), exactly WarpZone_Think nextthink=time cadence. |
| warpzones | warpzones.reconnect | liveness | na -> live | trigger_warpzone_reconnect + target_warpzone_reconnect spawnfuncs registered (MapObjectsRegistry.cs:116-117); use-handler routed via WarpzoneSpawns.ReconnectSink -> OnReconnectUse (GameWorld.cs:437); ThinkMovingZones driven per server frame (GameWorld.cs:1648). |
| warpzones | warpzones.observer_warp | logic | missing -> faithful | WarpObservers/FindOverlapping/TeleportNoTargets (Warpzone.cs:699-758) warp observers/SOLID_NOT clients each frame with the IS_OBSERVER\|\|SOLID_NOT gate, plane-side gate, and a no-targets warp, matching WarpZone_StartFrame's observer block (server.qc:751-766) and WarpZone_Teleport(e,-1,0). |
| warpzones | warpzones.observer_warp | timing | missing -> faithful | WarpObservers runs once per server frame (GameWorld.cs:1649), matching the FOREACH_CLIENT per-frame cadence of WarpZone_StartFrame. |
| warpzones | warpzones.observer_warp | liveness | na -> live | Warpzones.WarpObservers(Clients.Players) is called every server frame at GameWorld.cs:1649. |
| warpzones | warpzones.teleport.player | logic | faithful -> faithful | EF_TELEPORT_BIT is now toggled on the crossing (e.Effects ^= EffectFlags.Teleport, Warpzone.cs:269), matching WarpZone_TeleportPlayer BITXOR_ASSIGN(player.effects, EF_TELEPORT_BIT) (server.qc:63); the baseline-recorded EF_TELEPORT gap is removed and the 'interp cancel' constant flips to match:true. Dimension status was already faithful; this closes one of its enumerated gaps. |
| warpzones | warpzones.spawn.trigger | values | partial -> partial | The 2026-06-25 misc_warpzone_position new-gap is closed: MiscWarpzonePositionSetup registered at MapObjectsRegistry.cs:114, re-classing the edict to trigger_warpzone_position through the same Sink. values stays partial (scale/modelscale + warpzone_isboxy still unmodeled) but the canonical-position-spawnfunc gap is resolved. |
| weapon-rifle | rifle.audio.ric | audio | partial -> faithful | SoundSystem.PlayRic (SoundSystem.cs:370-378) now explicitly plays SND_RIC_RANDOM on SoundChannel.ShotsAuto (== CH_SHOTS, -4) via PlayOn(emitter, ric, ShotsAuto, vol, atten), instead of the RIC sound's default channel hint (Weapon). Matches QC rifle.qc:218 byte-for-byte; channel constant now match:true. This was the prior baseline's last documented open gap, now closed. |
| weapon-tuba | weapon-tuba.audio.note_sound | logic | partial -> faithful | NoteOn now spawns a held, sustained note loop (Api.Sound.Play loop:true on CH_TUBA, replaced on pitch/instrument change, Stopped by NoteOff) instead of firing one static one-shot per ~0.05s blast tick — the sustained-note state machine the baseline marked as not-modeled is now reproduced (Tuba.cs:207-210,255). |
| weapon-tuba | weapon-tuba.audio.note_sound | audio | partial -> faithful | The correct sound API is now called with the right cue: a looping CH_TUBA emitter at VOL_BASE*cl_tuba_volume / cl_tuba_attenuation with the on-disk floor-neighbour sample AND the 2^(m/12) sound7 residual pitch shift passed as the engine pitch (Tuba.cs:207-210, PitchStepBaseNote). Baseline played a flat snapped sample with no pitch-shift/volume/attenuation. |
| weapon-tuba | weapon-tuba.audio.note_sound | timing | missing -> partial | A sustained held loop now exists (note keep-alive + NoteOff Stop). Only the cl_tuba_fadetime release fade ramp remains unported (NoteOff Stops cleanly rather than ramping volume to 0 over 0.25s), so timing is partial rather than missing. |
| weapon-tuba | weapon-tuba.audio.note_sound | values | partial -> partial | Still partial overall, but materially closer: cl_tuba_volume, cl_tuba_attenuation, cl_tuba_pitchstep rounding, and the 2^(m/12) sound7 residual pitch are NOW honored (baseline had volume/attenuation/fadetime unhonored). Only the cos/sin two-sample crossfade weights (vol1/vol2) and cl_tuba_fadetime remain unmatched. |
| weapon-tuba | weapon-tuba.describe.guide | presentation | missing -> faithful | Tuba.GuideDescription (Tuba.cs:67-74) now ports the 5 static MENUQC describe paragraphs verbatim, and DialogMediaGuide.cs:160 renders it on the live weapon-guide dialog. Baseline had empty port_refs / presentation missing. Only the dynamic Keybinds/DPS trailing PARs are omitted (by design, matching the Electro guide). |
| weapon-tuba | weapon-tuba.describe.guide | liveness | na -> live | The guide text is now read on the live menu path (DialogMediaGuide.cs:160 -> w.GuideDescription), so the feature is reachable in a normal session rather than absent (na). |

## Reclassified

No reclassified changes.
