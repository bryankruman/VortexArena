# Parity drift — 2026-06-25

0 regressions, 4 new gaps, 60 fixes, 5 reclassified, 13 registry coverage gaps (newly-unmapped Base features).

## Regressions

No regressions detected.

## New gaps

| unit | feature | dimension | was -> now | reason |
| --- | --- | --- | --- | --- |
| nexball | nexball.ball.spawn | logic | faithful -> faithful | WireObjectiveSpawns (GameWorld:2166) calls `nb.SpawnBall(e.Origin)` with the default `basketball=true` for BOTH nexball_basketball AND nexball_football, so a football-only map spawns a carryable basketball instead of a kick-only football. Logic stays faithful overall but a concrete spawn-routing defect was added to gaps. |
| onslaught | onslaught.cp.touch_build | values | faithful -> partial | The LIVE icon build path uses OnslaughtControlPoint.CpHealth -> DefCpHealth=200f (OnslaughtControlPoint.cs:54) as its bare-default fallback, NOT Onslaught.ControlPointHealth (1000, which SpawnIcon never calls). Base g_onslaught_cp_health=1000. The baseline recorded match:true citing the wrong/unused 1000 fallback. Masked only while the shipped cfg loads the cvar; a missing-cfg boot builds icons to a 200-health max and a wrong per-tick build rate. |
| onslaught | onslaught.gen.damage_destroy | values | faithful -> partial | The LIVE generator spawn path uses OnslaughtControlPoint.GenHealth -> DefGenHealth=2000f (OnslaughtControlPoint.cs:58) and writes it onto BOTH the entity and the graph node (SpawnGenerator:120-121 overwrites the node's correct 2500 from AddGenerator). Base g_onslaught_gen_health=2500. The baseline recorded match:true citing the unused/overwritten Onslaught.DefaultGenHealth (2500). Masked only while the cfg loads the cvar; a missing-cfg boot starts generators at 2000. |
| onslaught | onslaught.audio.cues | audio | faithful -> partial | The ons_GeneratorThink un-shielded ALARM (ONS_NOTSHIELDED_TEAM/_<team> center + ONS_GENERATOR_ALARM siren, every 5s while a generator is exposed) is registered as data but never emitted — the port wires no generator Think. Distinct from the under-attack alarm folded into GeneratorDamage. All other core cues still emit live. |
| assault | assault.objective.waypoint_sprite | presentation | missing -> missing | Clarified: no client render/networking path reads WaypointSprites.Active (net+engine grep empty), so the on-screen marker/health bar/radar icon is still not surfaced despite the server state existing. |

## Registry coverage gaps

Newly-discovered Base features with no registry row (the registry under-counts the gap surface here):

**freezetag**
- FT FragCenterMessage kill-feed: CHOICE_FRAG_FREEZE / FRAGGED_FREEZE center messages ('You froze X' / 'You were frozen by Y') when a frag freezes a not-already-frozen target (sv_freezetag.qc:984 FragCenterMessage hook). Notifications are declared (NotificationsList.cs:1267-1268) but FreezeTag.cs never sends them — no registry row.
- Frozen players cannot pick up items or buffs: sv_freezetag.qc ItemTouch (MUT_ITEMTOUCH_RETURN) + BuffTouch hooks block pickups while STAT(FROZEN). ItemPickupRules.cs has no frozen guard — no registry row.
- Last-player-alive notify: freezetag_LastPlayerForTeam + CENTER_ALONE sent to the sole surviving teammate (sv_freezetag.qc:136-160). Not ported / no registry row.
- g_freezetag_weaponarena: SetWeaponArena hook (sv_freezetag.qc:978) sets the FT weapon arena when none configured. Not ported / no registry row.
- AccuracyTargetValid frozen-frame gating: damage to a frozen player only counts toward accuracy in the frame they were frozen (sv_freezetag.qc:608, MUT_ACCADD_INDIFFERENT). Not ported / no registry row.

**assault**
- assault.notify.play_hitsound — MUTATOR_HOOKFUNCTION(as, PlayHitsound) returns true for a func_assault_destructible victim (forces the standard hitsound when shelling a wall); no port equivalent, no registry row.
- assault.config.entity_prespawn — MUTATOR_HOOKFUNCTION(as, OnEntityPreSpawn) deletes foreign team spawns (info_player_team1/2/3/4) so only info_player_attacker/defender apply; not ported, no registry row.
- assault.config.vehicle_init — MUTATOR_HOOKFUNCTION(as, VehicleInit) sets veh.nextthink = time + 0.5 on vehicle spawn in Assault; not ported, no registry row.

**nexball**
- MUTATOR_HOOKFUNCTION(nb,PlayerSpawn) + PlayerPreThink weapon-strip else-branch + WantWeapon + SpectateCopy (NB_METERSTART) — the weapon-arena player setup (grant WEP_NEXBALL, zero normal weapons). Added as nexball.weapon_arena.player_setup.
- MUTATOR_HOOKFUNCTION(nb,FilterItem) + ItemTouch + ForbidThrowCurrentWeapon + ForbidDropCurrentWeapon — the item/weapon-restriction hooks. Added as nexball.item.filter_block.
- MUTATOR_HOOKFUNCTION(nb,SendWaypoint) (strip the 0x80 waypoint bit) — minor CSQC waypoint hook, still unmapped (presentation/client-deferred; not added as a row).

**keepaway**
- keepaway.warn.noncarrier — Base ka PlayerDies sends CENTER_KEEPAWAY_WARN to a non-carrier who frags when g_keepaway_noncarrier_warn is set; the notification is registered (NotificationsList.cs:986) but the port's OnDeath has no such branch and never reads the cvar. Added as a new missing row.
- keepaway.score.countfragsremaining — Base MUTATOR_HOOKFUNCTION(ka, Scores_CountFragsRemaining) returns !autocvar_g_keepaway_score_timepoints (suppress the remaining-frags announce when time scoring is on); no port hook models this gating. Added as a new missing row.

**onslaught**
- onslaught.gen.unshielded_alarm — ons_GeneratorThink (sv_onslaught.qc:1021): the periodic (every 5s) un-shielded generator warning loop. While a generator is alive but un-shielded (an enemy controls a linked CP), Base re-warns the owning team with CENTER_ONS_NOTSHIELDED_TEAM + the ONS_GENERATOR_ALARM siren and the enemy team with APP_TEAM_NUM(team, CENTER_ONS_NOTSHIELDED). The port wires NO generator Think (SpawnGenerator sets only GtEventDamage/GtEventHeal), so this recurring alarm never fires. Distinct from the under-attack alarm folded into GeneratorDamage. The notification/sound assets (ONS_NOTSHIELDED_*, ONS_GENERATOR_ALARM) exist as data with no emitter. Added as a row this pass.

## Fixes

Parity that improved since the baseline.

| unit | feature | dimension | was -> now | reason |
| --- | --- | --- | --- | --- |
| cts | cts.physics.force_keyboard | logic | missing -> faithful | OnPlayerPhysics now implements the analog->keyboard wish-move quantization (same guard x!=0&&y!=0&&x!=y, same 2x pure-axis threshold, same M_SQRT1_2 diagonal as Base sv_cts.qc PlayerPhysics). |
| cts | cts.physics.force_keyboard | values | missing -> faithful | M_SQRT1_2 diagonal constant and pure-axis thresholds match Base exactly. |
| cts | cts.physics.force_keyboard | timing | missing -> faithful | Quantization runs per physics frame at the QC dispatch point; the per-frame snap cadence matches. |
| cts | cts.physics.force_keyboard | liveness | na -> live | Subscribed in Activate() and dispatched on the live physics path (PlayerPhysics.cs:221 MutatorHooks.PlayerPhysics.Call), with the rewritten MovementForward/Right folded back into the wish-move at PlayerPhysics.cs:232-235. |
| cts | cts.weapon.shotgun_only | logic | partial -> faithful | The per-frame OnPlayerPreThink/ForceShotgunOnly OwnedWeaponSet wipe was removed; the shotgun is now forced ONLY on spawn (start loadout), so mid-run map-weapon pickups survive exactly as Base WantWeapon governs only start_items. |
| cts | cts.items.loot_filter | logic | partial -> faithful | OnFilterItemDefinition now reads the real Entity.ItemIsLoot flag (EntityItemState.cs:44, set by StartItem.SpawnInternal) for the ITEM_IS_LOOT test instead of the old classname-tag approximation. Only the default-off monster-drop sub-case still uses a classname tag. |
| cts | cts.map_reset | logic | missing -> partial | ResetMapGlobal now resets every runner's in-progress run (RunStartTime=0, race_PreparePlayer) and cancels pending retracts on map reset. Residual: cts_EventLog place echo + qualifying==2 collapse (moot) still absent, hence partial not faithful. |
| cts | cts.map_reset | liveness | na -> live | Wired live at GameWorld.cs:3138 (case Cts c: c.ResetMapGlobal(Clients.Players)) on the map-reset dispatch. |
| cts | cts.records.getrecords | logic | missing -> partial | CommandReplies.GetRankings now reads the RaceRecords CTS table for the current map and returns the ranked 'Records for <map>:' listing. The multi-map paged GetRecords form (200 maps/page) is still not modeled, hence partial. |
| cts | cts.records.getrecords | values | na -> faithful | Per-rank line format and record_type segment (CtsRecord) match Base getrankings output. |
| cts | cts.records.getrecords | liveness | na -> live | Reachable via the 'rankings' and 'records' console commands (Commands.cs:712-713 -> CmdRankings/CmdRecords:2292 -> Replies.RankingsReply). |
| freezetag | freezetag.damage.frozen_invuln | logic | missing -> faithful | FreezeTag.OnDamageCalculate (FreezeTag.cs:439-459) now zeroes damage for a frozen, non-NEEDKILL/non-teamchange target; subscribed in Activate onto the live MutatorHooks.DamageCalculate (fired DamageSystem.cs:256). The baseline was written before this handler landed. |
| freezetag | freezetag.damage.frozen_invuln | values | missing -> faithful | args.Damage=0 and args.Force *= g_frozen_force (cvar, fallback 0.6) — both constants now match Base. |
| freezetag | freezetag.damage.frozen_invuln | liveness | na -> live | Handler subscribed by FreezeTag.Activate (ft.Activate called GameWorld.cs:1804) and the DamageCalculate hook fires on every hit; the subtract path (EventDamage) only runs when damage!=0, so the HP=1 frozen body now takes no health damage. |
| freezetag | freezetag.revive.auto_reducible | logic | missing -> faithful | OnDamageCalculate (FreezeTag.cs:410-437) implements the full revive_auto_reducible model: enemyOrAlways gate, force accumulation up to maxforce, forcefactor conversion, subtract from FrozenTimeout. The per-frame FrozenForce accumulator is reset across the roster in ReviveTick:553-554. |
| freezetag | freezetag.revive.auto_reducible | values | missing -> faithful | maxforce 400 / forcefactor 0.01 / reducible 1 all read from cvars with correct Base fallbacks. |
| freezetag | freezetag.revive.auto_reducible | timing | missing -> faithful | Per-hit force-to-time reduction is applied on the live damage path, frame-accurate to QC. |
| freezetag | freezetag.revive.auto_reducible | liveness | na -> live | Same live MutatorHooks.DamageCalculate subscription as frozen_invuln. |
| freezetag | freezetag.damage.softkill_void | logic | missing -> partial | Fall-damage revive (g_frozen_revive_falldamage) is now implemented and live in OnDamageCalculate (FreezeTag.cs:444-453); the void/lava NEEDKILL soft-kill relocate is still absent, so the dimension is partial rather than missing. |
| freezetag | freezetag.damage.softkill_void | values | missing -> partial | g_frozen_revive_falldamage / _falldamage_health (0/40) now read with correct fallbacks; g_frozen_damage_trigger relocate still unmodeled. |
| freezetag | freezetag.damage.softkill_void | presentation | missing -> partial | The fall-revive path sends FREEZETAG_REVIVED_FALL/REVIVE_SELF notifications; the void/lava teleport VFX + relocate remain absent. |
| freezetag | freezetag.damage.softkill_void | audio | missing -> partial | Fall-revive notification path is live; nade/spawn-late events still unimplemented. |
| freezetag | freezetag.presentation.overlay_eventchase | values | partial -> faithful | cl_eventchase_frozen is now implemented (FirstPersonView.WantEventChase frozenChase fed by NetGame ViewState.IsFrozen ~4293) — the baseline's match:false eventchase constant is now match:true. Presentation stays partial only for pure-remote-client coverage. |
| freezetag | freezetag.presentation.notifications | presentation | faithful -> faithful | CENTER_FREEZETAG_REVIVE_SELF + INFO_FREEZETAG_REVIVED_FALL are now sent from the live fall-damage-revive branch; baseline listed REVIVE_SELF/REVIVED_FALL as declared-but-never-sent. Status was already faithful; the gap set shrank. |
| freezetag | freezetag.unfreeze | values | partial -> faithful | Unfreeze now sets PauseRegenFinished = max(cur, now + g_balance_pause_health_regen) (FreezeTag.cs:366-368), closing the pauseregen divergence the baseline flagged. |
| freezetag | freezetag.bots | logic | missing -> partial | BotBrain.ShouldAttack (BotBrain.cs:792-793) now skips Frozen-status targets, satisfying the FT Bot_ForbidAttack frozen-target attack gate; the FT-specific freeing/offense ROLE logic remains missing. |
| freezetag | freezetag.bots | liveness | na -> partial | The frozen-target skip runs on the live bot targeting path; the freeing/offense roles have no code. |
| assault | assault.spawns.attacker_defender | logic | partial -> faithful | Wave 6a added AssaultSwapSpawnTeams (GameWorld.cs:3249) flipping each info_player_deathmatch edict's Red<->Blue team on round 2 (QC assault_new_round g_saved_team.team_saved swap); the new attackers now spawn at the geographic attacker location. |
| assault | assault.spawns.attacker_defender | liveness | partial -> live | AssaultSwapSpawnTeams is invoked live from OnSecondRoundRestart (GameWorld.cs:1881). |
| assault | assault.turret.roundstart_teamswap | logic | missing -> faithful | Wave 6a ported AssaultTurretRoundstart (GameWorld.cs:3215): unteamed-turret attacker seeding (TurretSpawn hook) + NUM_TEAM_1<->NUM_TEAM_2 swap + TurretAI.Respawn, campaign-guarded (assault_turrets_teamswap_forbidden). |
| assault | assault.turret.roundstart_teamswap | values | na -> faithful | Unteamed seed = attacker team, swap Red<->Blue match Base; campaign guard mirrors QC. |
| assault | assault.turret.roundstart_teamswap | liveness | na -> live | Wired at initial roundstart (GameWorld.cs:649) and on the round-2 restart (GameWorld.cs:1886). |
| assault | assault.objective.waypoint_sprite | logic | missing -> faithful | Wave 6a ported the full server-side waypoint-sprite state machine (SpawnObjectiveSprite, health-bar update on each decrease, DisownSprite on destroy) per QC target_objective_decrease_activate. |
| assault | assault.objective.waypoint_sprite | values | missing -> faithful | WP_AssaultDestroy + UpdateMaxHealth/UpdateHealth + RADARICON_OBJECTIVE + orange tint match Base. |
| assault | assault.objective.waypoint_sprite | timing | missing -> faithful | WaypointSprites.Think() is ticked live per-frame (GameWorld.cs:1557); sprite lifetime/disown timing modeled. |
| assault | assault.objective.waypoint_sprite | liveness | na -> live | Sprites spawn/update/tick on the live server path; presentation dimension remains the only gap. |
| assault | assault.objective.destructible_heal | logic | missing -> faithful | Wave 6a added HealDestructible (Assault.cs:482) with QC true_limit + already-destroyed/full guard + GiveResourceWithLimit clamp, wired via GtEventHeal (GameWorld.cs:2132). |
| assault | assault.objective.destructible_heal | values | missing -> faithful | true_limit selection and the heal guard match QC destructible_heal exactly. |
| assault | assault.objective.destructible_heal | presentation | missing -> partial | Sprite health-bar UpdateHealth on heal is modeled server-side (but not client-rendered); func_breakable_colormod tint still absent — so partial, not missing. |
| assault | assault.objective.destructible_heal | liveness | na -> live | Reachable live: Combat.Heal (DamageContracts.cs:77) -> GtEventHeal; Arc heal-beam (Arc.cs:332) invokes it for non-player targets. |
| assault | assault.spawns.objective_evalfunc | logic | missing -> faithful | Wave 6a added ShouldRejectSpawnSpot (Assault.cs:606) + the SpawnSystem.SpotEvalReject seam (SpawnSystem.cs:408): a spot targeting a destroyed (<0) or inactive (>=ObjectiveInactive) objective is rejected, per QC target_objective_spawn_evalfunc. |
| assault | assault.spawns.objective_evalfunc | values | missing -> faithful | Reject thresholds (<0 / >=ASSAULT_VALUE_INACTIVE) match Base. |
| assault | assault.spawns.objective_evalfunc | liveness | na -> live | SpotEvalReject installed in Activate (Assault.cs:597), consumed on the live target-checking spawn-score path; cleared on Deactivate. |
| assault | assault.config.warmup_incompatible | logic | missing -> faithful | Wave 6a forces g_warmup=0 + sv_ready_restart_after_countdown=0 for non-campaign Assault in Activate (GameWorld.cs:1851-1855), matching QC (as, ReadLevelCvars). |
| assault | assault.config.warmup_incompatible | values | missing -> faithful | Both forced cvar values (0/0) match Base; ReadyRestart_Deny effect achieved via sv_ready_restart_after_countdown=0. |
| assault | assault.config.warmup_incompatible | liveness | na -> live | The forcing runs in the live Assault Activate path. |
| assault | assault.win.defender_timelimit | values | partial -> faithful | Wave 6a asserts the Base mapinfo default timelimit=20 for Assault when none configured (GameWorld.cs:1860-1861), so the defender-timelimit win can fire even without a map/cvar timelimit. |
| assault | assault.bot.objective_role | logic | partial -> faithful | Wave 6a rewrote RoleAssault (BotObjectiveRoles.cs:453): rates EVERY destructible whose linked objective is Active (QC active-objective filter) rather than nearest-only, per havocbot_goalrating_ast_targets. |
| assault | assault.bot.objective_role | values | partial -> faithful | Radii corrected to Base 650 (offense) / 3000 (defense), ratingscale to 20000 for both, items 30000/10000 (baseline had 6000/7000 and a defender *0.7). |
| keyhunt | keyhunt.key.carried_orbit | logic | partial -> faithful | Wave-6a added OrbitCarriedKey: a carried key now circles the carrier (makevectors yaw = SpawnAngle + (now%360)*XYSPEED, origin = carrier + fwd*XYDIST + z*ZSHIFT) — the live Base #ifndef-KH_PLAYER_USE_ATTACHMENT orbit, replacing the prior fixed offset. |
| keyhunt | keyhunt.key.carried_orbit | values | partial -> faithful | Orbit constants now match Base exactly: KeyXyDist=24 (KH_KEY_XYDIST), KeyXySpeed=45 (KH_KEY_XYSPEED), KeyZShift=22 (KH_KEY_ZSHIFT). Baseline had z=20 and no XYDIST/XYSPEED. |
| keyhunt | keyhunt.key.carried_orbit | timing | missing -> faithful | Per-think (0.05s) origin update now advances the orbit yaw against (now%360)*XYSPEED, matching Base kh_Key_Think. Baseline had no per-frame origin update. |
| keyhunt | keyhunt.key.carried_orbit | presentation | partial -> faithful | The carried key visibly orbits the carrier now (radius-24 ring at height 22), the player-visible cosmetic the baseline noted as un-fixed. |
| keyhunt | keyhunt.combat.carrier_damage_force | logic | missing -> faithful | OnDamageCalculate now implements the full 9-way carrier/noncarrier damage+force matrix (attacker carry-state selects carrier_*/noncarrier_* cvar; component 0/1/2 = self/target-carrier/target-noncarrier). port_refs were empty in the baseline. |
| keyhunt | keyhunt.combat.carrier_damage_force | values | missing -> faithful | All four cvars (carrier_damage/force, noncarrier_damage/force) are read per-component via CvarVectorComponent with a default of 1, matching Base '1 1 1' stock. |
| keyhunt | keyhunt.combat.carrier_damage_force | liveness | na -> live | _damageCalcHandler is subscribed to MutatorHooks.DamageCalculate in Activate and dispatched at DamageSystem.cs:256 (MutatorHooks.DamageCalculate.Call) on every damage application. |
| keyhunt | keyhunt.unreachable.damage_destroy | logic | missing -> faithful | KeyDamage now implements kh_Key_Damage: a loose key in a NEEDKILL volume (lava/slime/swamp/void) fast-returns in delay_damage_return; else a forceful player hit outside the protect window re-teams the key. port_refs were empty in the baseline. |
| keyhunt | keyhunt.unreachable.damage_destroy | values | missing -> partial | delay_damage_return (default 5) and return_when_unreachable (default 1, gates handler install) now read and match Base; damageforcescale still unread (raw force used for the re-team gate) — values partial not faithful. |
| keyhunt | keyhunt.unreachable.damage_destroy | timing | missing -> faithful | The NEEDKILL fast-return bounds the return time to min(old, now+delay_damage_return), so a hazard-resting key returns in 5s instead of waiting the full 60s — matching Base bound() semantics. |
| keyhunt | keyhunt.unreachable.damage_destroy | liveness | na -> live | SpawnKey installs e.GtEventDamage = KeyDamage when return_when_unreachable != 0; DamageSystem.cs:331-333 routes a non-player edict's damage to that delegate. |
| keyhunt | keyhunt.drop.on_death | liveness | partial -> live | OnPlayerRemoved override now calls DropAllKeys(player, suicide:true), dispatched from ClientManager.cs:473 (MakePlayerObserver) and ClientManager.cs:682 (ClientDisconnect). 3 of 4 Base drop hooks now wired (was 1 — only PlayerDies). |
| keyhunt | keyhunt.drop.on_death | timing | partial -> faithful | The observe/disconnect drop edges are now covered, removing the timing partial that reflected the missing leave-play drop edges. Only the narrow DropSpecialItems edge remains unwired. |
| keyhunt | keyhunt.capture.scoring | logic | partial -> faithful | WinnerTeam now credits each carrier's individual SP_SCORE (c.ScoreFrags += DistributeEvenly chunk) AND calls Nades.NadeBonus.GiveBonus(c, g_nades_bonus_score_high) per carrier — the two residual gaps the baseline flagged are closed. |
| keyhunt | keyhunt.loss.loser_team_push_destroy | logic | partial -> faithful | LoserTeam destroy branch now performs the full Base DistributeEvenly across other-team players AND each non-loser key holder (destroyed_holdingkey ownfactor chunk), plus the cross-team fragsleft carry-forward, matching kh_LoserTeam structurally. |
| keyhunt | keyhunt.loss.loser_team_push_destroy | values | partial -> faithful | score_destroyed_ownfactor (default 1) is now read (DefaultScoreDestroyedOwnFactor) and used in the keys*of+players distribution denominator, matching Base; baseline had it unread with a flat per-team split. |
| keyhunt | keyhunt.round.spawn_keys | values | faithful -> faithful | The spawn_angle constant moved from non-matching (cosmetic random yaw) to matching: SpawnKey now sets key.SpawnAngle = 360f*keyIndex/teamCount (Base key.cnt = 360*i/AVAILABLE_TEAMS), consumed by the carried-key orbit. Row status was already faithful; the constant match flag flipped false->true. |
| keyhunt | keyhunt.controller.round_loop | values | partial -> faithful | delay_tracking (default 10) is now read (DefaultDelayTracking) and scheduled: StartRound sets _trackingEnableTime = now + delay_tracking and TickKeys flips TrackingEnabled when it elapses. Constant now matches Base; presentation stays partial only because it gates not-yet-implemented sprites. |
| nexball | nexball.init.initialize | values | partial -> faithful | radar_showenemies is now wired (Activate sets the replicated cvar from g_nexball_radar_showallplayers) and GoalLeadLimit now implements the QC cvar=-1 -> mapinfo/engine leadlimit fallback. Team derivation moved to SeedTeamsFromGoals/PresentTeams. Residual: PresentTeams always includes red+blue base. |
| nexball | nexball.goal.limit_win | values | partial -> faithful | GoalLeadLimit now reads the leadlimit cvar when the g_nexball_goalleadlimit cvar is -1 (QC GameRules_limit_lead mapinfo-fallback semantics); the previously-missing -1 nuance is closed. |
| nexball | nexball.ball.spawn | values | partial -> faithful | EF_LOWPRECISION is now set (Nexball.SpawnBall) and the g_nexball_playerclip_collisions DPCONTENTS_PLAYERCLIP mask is now applied; both were recorded as not-applied gaps in the baseline. |
| nexball | nexball.ball.lifecycle | timing | faithful -> faithful | The baseline timing gap (GameStartTime not synced -> first release at sim-time delay_start) is closed: GameWorld:2305 now syncs nb.GameStartTime = GameStartTime each Nexball frame, so the first release fires at game_starttime + delay_start exactly as QC. Dimension was already faithful; the gap entry is removed. |
| nexball | nexball.carry.perframe | logic | missing -> partial | CarryFrame (live at GameWorld:2307) now implements the QC PlayerPreThink view-ball follow (origin+view_ofs+viewmodel_offset) and the nexball_setstatus forteam auto-return. Residual: safe-pass crosshair lock + NB_CARRYING bit still absent. |
| nexball | nexball.carry.perframe | values | missing -> partial | carrier_highspeed 0.8 and viewmodel_offset 8 8 0 are now ported with faithful values; safepass + viewmodel_scale constants remain unimplemented. |
| nexball | nexball.carry.perframe | timing | missing -> faithful | The carry frame runs once per server frame (GameWorld Tick) and the highspeed multiplier is re-applied each physics frame via the live PlayerPhysics hook, matching QC's per-frame cadence. |
| nexball | nexball.carry.perframe | liveness | na -> live | CarryFrame is called live from GameWorld:2307 and OnPlayerPhysics is registered in Activate and fired live via MutatorHooks.PlayerPhysics.Call (PlayerPhysics.cs:221). The previously entirely-absent per-frame carry layer is now on the live path. |
| keepaway | keepaway.ball.respawn | logic | partial -> partial | NEEDKILL force-respawn (ka_DamageEvent) is now LIVE: AdoptBall installs OnBallDamage on Entity.GtEventDamage and DamageSystem.EventDamage (DamageSystem.cs:331-333) dispatches non-player edict damage to it; a NEEDKILL hit force-respawns the loose ball. Baseline recorded 'no NEEDKILL respawn path' as a fail. Stays partial overall (world-bounds sampling fidelity + NOIMPACT fall-off branch still open). |
| keepaway | keepaway.ball.drop | liveness | partial -> live | All four QC drop triggers are now wired (was death-only). Use-key via OnUseKey on MutatorHooks.PlayerUseKey (fired live at VehicleBoarding.cs:106 FirePlayerUseKey); disconnect+observe via OnPlayerRemoved (ClientManager.cs:473/:682). Baseline marked these unwired. |
| keepaway | keepaway.ball.drop | logic | partial -> partial | The use-key/disconnect/observe drop logic branches now exist and match QC ka_DropEvent; remains partial only for nudgeoutofsolid + multi-ball chain advance. |
| keepaway | keepaway.win.pointlimit | values | partial -> faithful | PointLimit getter now models QC -1->mapinfo(30): `if (pl != -1f) return pl;` skips the cfg -1, falls through to fraglimit then the literal 30 (== gametype_init pointlimit=30). Baseline failed this (the -1 leaked a no-limit match). Constant g_keepaway_point_limit_cfg now matches. |
| keepaway | keepaway.carrier.highspeed | logic | missing -> faithful | OnPlayerPhysics now registered on MutatorHooks.PlayerPhysics in Activate and fired live (PlayerPhysics.cs:221); for a carrier it does SpeedMultiplier *= BallCarrierHighspeed == QC STAT(MOVEVARS_HIGHSPEED) *= autocvar_g_keepaway_ballcarrier_highspeed. Baseline had this missing/deferred. |
| keepaway | keepaway.carrier.highspeed | values | missing -> faithful | BallCarrierHighspeed reads g_keepaway_ballcarrier_highspeed (default 1) == Base autocvar default. |
| keepaway | keepaway.carrier.highspeed | timing | missing -> faithful | Hook runs once per physics frame at the QC point (SpeedMultiplier reset to HighSpeed seed before the hook, folded via ApplyHighSpeed after), matching per-frame highspeed application. |
| keepaway | keepaway.carrier.highspeed | liveness | na -> live | Now reachable: the PlayerPhysics hook fires on the live movement step. Baseline na (no code). |
| keepaway | keepaway.waypoints.tracking | logic | missing -> partial | CollectWaypoints now EMITS KaBall (loose) / KaBallCarrier (held) sprites and reads+honors g_keepawayball_tracking (0/1/2) via BallTracking. Baseline: nothing spawned them and the cvar was never read. Stays partial: the spectator/invisibility visible_for_player predicate is not modeled. |
| keepaway | keepaway.waypoints.tracking | values | missing -> faithful | Offset (0,0,64), RADARICON_FLAGCARRIER, tracking default 1 all match Base. |
| keepaway | keepaway.waypoints.tracking | presentation | missing -> partial | Server-side sprite spawn/attach is now done; only the per-viewer visibility predicate + CSQC render fidelity remain. |
| keepaway | keepaway.waypoints.tracking | liveness | na -> live | CollectWaypoints is pulled live each tick by ServerNet.cs:927 SendWaypoints. Baseline na (nothing instantiated). |
| keepaway | keepaway.bot.role | logic | partial -> partial | Bot_ForbidAttack equivalent (Keepaway.ForbidBotAttack) is now wired LIVE: installed at GameWorld.cs:1896 onto BotBrain.ForbidAttackHook and consulted in bot_shouldattack (BotBrain.cs:797). Baseline had this as the residual STILL-OPEN item. Stays partial only for the structural carrier/collector role-function split. |
| onslaught | onslaught.cp.icon_think_regen | values | partial -> faithful | The cp_regen bare-default fallback was corrected from 10f to 20f (OnslaughtControlPoint.cs:57 DefCpRegen=20f), now matching Base g_onslaught_cp_regen 20 (gametypes-server.cfg:581). This was the single worst standing numeric gap in the baseline; it is closed. |
| onslaught | onslaught.cp.proximity_decap | logic | missing -> faithful | Proximity-decap is now implemented in IconThink (OnslaughtControlPoint.cs:408-451): (friendly-enemy) head count within proximitydecap_distance changes icon HP by ±dps*THINKRATE; at <=0 destroys via IconDamage (lone enemy, or self to spread the multi-attacker reward). Was entirely absent in the baseline. |
| onslaught | onslaught.cp.proximity_decap | values | missing -> faithful | Port now reads g_onslaught_cp_proximitydecap (default 0/OFF), _distance (512, DefProxDecapDist), _dps (100, DefProxDecapDps) — all matching Base sv_onslaught.qc:18-21. |
| onslaught | onslaught.cp.proximity_decap | timing | missing -> faithful | Runs at the ONS_CP_THINKRATE (0.2s) steady-state IconThink cadence with the hlt=±dps*THINKRATE step, matching QC ons_ControlPoint_Icon_Think. |
| onslaught | onslaught.cp.proximity_decap | liveness | na -> live | The decap branch lives in IconThink, the wired steady-state think of a built icon; reachable on the live path (gated by the default-OFF cvar, like Base). |
| onslaught | onslaught.bot.roles | logic | partial -> faithful | BotObjectiveRoles.RoleOnslaught was rewritten since the baseline. It now reads the live Onslaught power graph: rates enemy UN-shielded generators first (skip node.Shielded), skips the CP pass if any generator was rated, then rates CPs gated on !node.Shielded && NeighborsTeam(node,team), with free/touchable points at ×2 ratingscale — matching the QC generator_attack + controlpoints_attack + offense flow. The baseline described a coarse classname-only offense scan with no shield/neighbor filter. |
| onslaught | onslaught.bot.roles | values | missing -> faithful | The free-point ratingscale ×2 multiplier now matches Base (ratingscale*2), and the 120s role-timeout is behaviorally a no-op in Base (defense/assistant roles immediately reset to offense), so its absence is not a real numeric divergence. |
| onslaught | onslaught.bot.roles | timing | missing -> partial | Improved but not fully faithful: the port re-rates each brain tick with no havocbot_attack_time (5s) throttle and no navigation_goalrating_timeout-style objective-attack cooldown that Base uses. Reclassified from missing (baseline) to partial. |
| tka | tka.carrier.highspeed | logic | missing -> faithful | OnPlayerPhysics multiplies the carrier's SpeedMultiplier by g_tka_ballcarrier_highspeed (QC PlayerPhysics_UpdateStats); subscribed in Activate to MutatorHooks.PlayerPhysics. |
| tka | tka.carrier.highspeed | values | missing -> faithful | BallCarrierHighspeed reads g_tka_ballcarrier_highspeed with fallback 1 == Base autocvar default 1. |
| tka | tka.carrier.highspeed | timing | missing -> faithful | Applied per-frame in the live physics step (a pure multiply that composes after SpeedMultiplier is reset to 1 each frame). |
| tka | tka.carrier.highspeed | liveness | na -> live | MutatorHooks.PlayerPhysics.Call fires at PlayerPhysics.cs:221 (the live physics step); the hook is added in the TKA Activate case wired at GameWorld.cs:1832. |
| tka | tka.hud.modicon | logic | missing -> faithful | BallStatusFor bit-packs carrying/dropped/per-team-taken (QC PlayerPreThink) per recipient; ModIconsPanel.DrawTeamKeepaway selects icons exactly as HUD_Mod_TeamKeepaway. |
| tka | tka.hud.modicon | values | missing -> faithful | BIT(0..5) layout, blink(0.85,0.15,5), and the bound(0,elapsed*2,1) expand factor all match cl_tka.qc / tka.qh constants. |
| tka | tka.hud.modicon | timing | missing -> faithful | The status-change expand transition (elapsed*2 clamp) and per-frame blink are reproduced. |
| tka | tka.hud.modicon | presentation | missing -> faithful | DrawTeamKeepaway renders keepawayball_carrying / tka_taken_<color> icons; only the separate expanding-overlay sprite is folded into alpha (visually equivalent). |
| tka | tka.hud.modicon | liveness | na -> live | Full chain live: GametypeStatusBlock.cs:122 writes the byte per peer -> ServerNet Capture -> ClientNet decode -> NetGame.cs:3632 feeds ModIconsPanel.TeamKeepaway. |
| tka | tka.waypoints.tracking | logic | missing -> faithful | CollectWaypoints produces a team-colored carrier sprite while held and the neutral KaBall when loose, honoring g_tkaball_tracking (0=none). |
| tka | tka.waypoints.tracking | values | missing -> partial | Sprite names/offset/radar-icon/per-team tint faithful, but g_tkaball_tracking==2 (dropped-only) is collapsed to the always-show behavior. |
| tka | tka.waypoints.tracking | presentation | missing -> partial | Sprites are networked + rendered via the live WaypointSpriteLayer/RadarPanel, but the carrier uses SPRITERULE_DEFAULT not TEAMPLAY, so the tka_ballcarrier_waypointsprite_visible_for_player per-viewer visibility predicate is approximated as always-visible. |
| tka | tka.waypoints.tracking | liveness | na -> live | ServerNet.SendWaypoints (called at ServerNet.cs:474) pulls _world.GameType.CollectWaypoints at ServerNet.cs:927, filters per peer, networks, and the client renders via WaypointSpriteLayer + RadarPanel. |
| tka | tka.bot.role | values | partial -> faithful | RoleKeepaway now uses carrier enemyplayers 10000 (BotObjectiveRoles.cs:353) and collector enemyplayers 500 (BotObjectiveRoles.cs:360), exactly matching Base havocbot_role_tka_carrier(10000)/collector(500); the baseline's approximate 6000 is gone. Items 10000/10000 and ball 8000/4000 dist 2000 also faithful. |
| domination | domination.controlpoint.spawn | presentation | missing -> partial | Wave 6a (e596bf0) now sets the control-point body model (e.Model = DomPointModel: dom_<color>.md3 / dom_unclaimed.md3) on spawn AND capture, so the point is no longer invisible. Residual: AnimateDomPoint frame loop (t_width 0.02/t_length 239) and the 0.6 scale still unported. |
| domination | domination.controlpoint.spawn | values | partial -> faithful | The QC '+0 0 20 then DropToFloor' placement is now reproduced via DropPointToFloor (a downward box trace), so origin_z_offset matches Base. EF_LOWPRECISION/EF_FULLBRIGHT also set. |
| domination | domination.scoring.tick | timing | partial -> faithful | PointThink now reads VehicleCommon.GameStopped (not the never-assigned Scoring.GameScores.GameStopped). GameWorld pushes VehicleCommon.GameStopped = GameStopped every frame (GameWorld.cs:1517 = Intermission||MatchEnded||Timeout.IsPaused), so the game_stopped/timeout-pause gate is now live and a paused match stops ticking points. The prior dead-gate gap is closed. |
| domination | domination.roundbased.win | presentation | na -> faithful | CheckRoundWinner now sends the per-round CENTER/INFO notifications directly (ROUND_OVER Center+Info, and APP_TEAM_NUM ROUND_TEAM_WIN_<RED/BLUE/YELLOW/PINK> Center+Info) — all registered in NotificationsList.cs. Baseline recorded these as not sent from Domination.cs; that gap is closed. |
| domination | domination.hud.modicons_pps | values | partial -> faithful | WriteDominationPps (GametypeStatusBlock.cs:206) now sums EACH point's own amt/rate via the per-point resolvers PointAmountFor(cp)/PointRateFor(cp) (cvar override else .frags/.wait, default 1/5) — matching QC set_dom_state — instead of the global PointAmount/PointRate (1/2 fallback) that read ~2.5x high. The pps numerator magnitude now matches Base at default config. |
| duel | duel.rules.match_end_latch | timing | partial -> faithful | The baseline gap (Duel absent from the DriveGametypeFrame switch, so the leader/frag-limit re-check only fired incrementally on death) is now closed: a per-frame `case Duel duel: duel.RecomputeLeader(Clients.Players); break;` was added at GameWorld.cs:2279, alongside Mayhem/TeamMayhem/Tdm. The QC WinningConditionHelper-equivalent leader + frag-limit checkrules now runs every frame. RespawnDuePlayers also runs for duel via the shared call before the switch (GameWorld.cs:2275). MatchHasEnded Duel case verified at GameWorld.cs:2778-2780. |
| tdm | tdm.rules.lead_limit | logic | partial -> faithful | Baseline (2026-06-22) recorded that autocvar_leadlimit_and_fraglimit (require BOTH limits to end the match) was NOT modeled in the win check — only EITHER-limit independently. Current Tdm.cs:367-375 now models the conjunction exactly: fraglimitReached && leadlimitReached when both limits are set and LeadAndFrag (autocvar_leadlimit_and_fraglimit) is on, else fraglimitReached || leadlimitReached — a line-faithful mirror of Base server/world.qc:1625-1633. The CvarLeadAndFrag/LeadAndFrag wiring was added in Wave 6a (commit e596bf0, 2026-06-25), after the baseline audit. Gap removed, confidence raised medium->high. |

## Reclassified

Neutral status changes (no parity movement).

| unit | feature | dimension | was -> now | reason |
| --- | --- | --- | --- | --- |
| cts | cts.items.loot_filter | logic | partial -> partial | Confidence raised medium->high after verifying the ItemIsLoot flag is set before the hook fires. |
| cts | cts.mode.register_latch | logic | faithful -> faithful | Activate() now explicitly forces g_race_qualifying=1 via Api.Cvars.Set (Cts.cs:180), closer to the QC MUTATOR_ONADD assignment than the prior cvar-read approximation; status unchanged but the latch gap note narrowed. independent_players + timelimit=20 default still unmodeled (values stays partial). |
| assault | assault.bot.objective_role | timing | partial -> partial | Unchanged-status but reaffirmed: still omits the 120s havocbot_role_timeout and the havocbot_attack_time 2s commitment; confidence raised medium->high after re-read. |
| assault | assault.round.second_round | logic | faithful -> faithful | Still faithful, but two of its three listed residual gaps (spawn-point team swap, turret team-swap) are now closed in OnSecondRoundRestart; gap list reduced to the non-enforced freeze + missing bprint line. |
| assault | assault.objective.activate | presentation | partial -> partial | Still partial but for a different reason: ActivateObjective now spawns the objective sprite server-side; the residual is the absent client render, not the absent spawn. |
| keepaway | keepaway.win.pointlimit | logic | faithful -> faithful | Confidence raised medium->high; the -1 resolution defect that justified the medium confidence is now fixed. |
