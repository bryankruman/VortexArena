# Parity drift — 2026-06-25

**0 regressions · 0 new gaps · 5 registry-coverage gaps · 71 fixes · 3 reclassified** across 10 units (turrets + vehicles, Wave 7/8 porting). No parity got worse.

## Regressions

No drift detected. (No parity got worse since the baseline.)

## New gaps

No drift detected. (No newly-introduced parity gaps.)

## Registry coverage gaps

Base features with no dedicated registry row (the registry is missing coverage for these). These are not regressions — they are uncovered Base behavior surfaced during the re-audit.

| unit | Base feature | status / note |
| --- | --- | --- |
| turret-mlrs | `MLRSTurret.describe` (mlrs.qc:35, MENUQC) | Turret encyclopedia description text shown in the menu. Not modeled; cosmetic menu flavor text, consistent with other turret units having no `describe` row. No registry row. |
| turret-phaser | `turret-phaser.fire.target_select_bias` (turrets.cfg:302-306) | Per-unit target-selection scoring biases (rangebias 0.85, anglebias 0.25, samebias 0, missilebias 0, playerbias 1). `PhaserTurret.Think` omits these args so Range/Angle/Same/Missile bias default to 1.0 in `TurretParams`. Scoring machinery (`TurretAI.cs:381`) is live and faithful, but the phaser's target-priority weighting diverges from Base. **Added as a new row (values: partial).** |
| vehicle-framework | `vehicle_addplayerslot` (sv_vehicles.qc:328) | Multi-seat slot registration helper (VHF_PLAYERSLOT gun-slot setup for the Bumblebee gunner). Substantially covered implicitly by the enter.handshake gunner branch + `Bumblebee.GunnerEnter`, but has no dedicated registry row. |
| vehicle-framework | `vehicles_reset` (sv_vehicles.qc:1095) | Round-reset hook that returns a vehicle to its spawn state at round start. Overlaps `spawn.respawn` but is a distinct reset entry point; no dedicated row. |
| vehicle-framework | `vehicle_tossgib` / `vehicles_gib_explode` / `vehicles_gib_think` (sv_vehicles.qc:274-326) | Death-gib spawner + gib physics/explode. Pure presentation (deferred), no registry row. |

## Fixes

Parity that improved since the baseline. Grouped by unit.

### turret-mlrs

| feature | dimension | was -> now | reason |
| --- | --- | --- | --- |
| setup.tr_setup | logic | partial -> faithful | Baseline kept logic partial solely because TFL_DMG_HEADSHAKE was absent. Now implemented: Spawn passes `headShake:true`, `TurretSpawn.Init:79` sets `st.HeadShake`, `EventDamage:887-891` jolts HeadAngles.X/.Y by `(Prandom.Float()-0.5)*take`. With ZPREDICT removed and VOLLYALWAYS modeled, every tr_setup flag is now faithful. |
| setup.tr_setup | liveness | live -> live | Constant `damage_flags HEADSHAKE` flipped match:false -> match:true; the headshake gap was removed from the row (liveness unchanged but the row's defect is resolved). |
| combat.brain | logic | partial -> faithful | VOLLYALWAYS enemy-present residual resolved. `MlrsTurret.Think` mid-burst branch (`st.Active && VollyCounter != ShotVolly`) now fires for both enemy-present and enemy-null cases ahead of RunCombat, skipping range/LOS re-checks — matching turret_firecheck:888 early-return. A live-target burst no longer aborts mid-volley. |
| anim.ammogauge | logic | missing -> faithful | tr_think ammo-gauge frame formula ported: `MlrsTurret.Think:78` computes `e.Frame = Bound(0, 6 - floor(0.1 + Ammo/ShotDamage), 6)` every think, matching mlrs.qc:14 bit-for-bit. Was previously missing with empty port_refs. |
| anim.ammogauge | values | missing -> faithful | Frame formula constants (bound 0..6, ammo/shot_dmg = 300/50) now match Base; constant flipped match:false -> match:true. |
| anim.ammogauge | timing | missing -> faithful | Frame recomputed every Think tick, matching Base tr_think cadence. |
| anim.ammogauge | presentation | missing -> partial | Frame now drives the turret edict's own `.Frame`, but Base writes to a separate netted tur_head entity; the port has no distinct head bone, so the gauge is not rendered on a separately-attached mlrs.md3 head — partial rather than full. |
| anim.ammogauge | liveness | na -> live | Now has a live caller (`MlrsTurret.Think`) where it previously had no port code. |
| damage.lifecycle | logic | partial -> faithful | Server HEADSHAKE aim jolt now on the live damage path: `EventDamage:887-891` jolts HeadAngles.X/.Y by `(Prandom.Float()-0.5)*take`, bit-faithful to turret_damage:228-234. The sole reason logic was partial is resolved. |

### turret-phaser

| feature | dimension | was -> now | reason |
| --- | --- | --- | --- |
| setup.flags | logic | partial -> faithful | `PhaserTurret.Think` now passes only `lead:true`; shotTimeCompensate/zPredict left at false struct defaults, matching Base aim_flags=TFL_AIM_LEAD only. Baseline recorded the port enabling both extra flags. |
| setup.flags | values | partial -> faithful | aim_flags constant now matches Base (lead-only); prior shotTimeCompensate+zPredict divergence gone. |
| fire.imobeam_multihit | logic | partial -> faithful | `BeamThink` (PhaserTurret.cs:155-186) now implements the full FireImoBeam penetration loop: make each hit Solid.Not, re-trace until a world brush, restore solidity, then damage+slow every collected entity. Baseline had a single `Api.Trace` hitting only the front target. |
| lifecycle.respawn_time | values | partial -> faithful | `PhaserTurret.Spawn` now passes `respawnTime: RespawnTime` (=90f) to `TurretSpawn.Init`, matching turrets.cfg:287 g_turrets_unit_phaser_respawntime 90. Baseline omitted the arg, inheriting the 60s default. |
| lifecycle.respawn_time | timing | partial -> faithful | Respawn timer now 90s (was effectively 60s); turret no longer respawns 30s too early. |

### turret-plasma

| feature | dimension | was -> now | reason |
| --- | --- | --- | --- |
| headspin.frame_anim | logic | missing -> faithful | Per-shot head-spin frame cycle ported for both single (0..5 idle-at-0) and dual (0..6 idle-at-0-and-3) in `PlasmaTurret.Think/Attack` and `PlasmaDualTurret.Think/Attack`, running on the turret edict's `Entity.Frame`; matches plasma.qc/plasma_dual.qc tr_think+tr_attack. |
| headspin.frame_anim | values | missing -> faithful | Frame thresholds/wrap (>5 single, >6 dual), idle states (0 single; 0 and 3 dual), and the frame=1 / ++frame attack triggers match Base exactly. |
| headspin.frame_anim | timing | missing -> faithful | Frame advance runs each think (turret_think calls tr_think every frame); Think is the live per-frame driver. |
| headspin.frame_anim | liveness | na -> live | Now has live port code: Think wired via `TurretSpawnFuncs:47 e.Think -> def.Think`, driven by `SV_RunThink`. Baseline had empty port_refs / liveness na. |
| setup.flags | values | partial -> faithful | Both partial-causing gaps cleared: TFL_FIRECHECK_AFF now a live fire-gate branch (`TurretAI.RunCombat:689` withholds shot when ImpactEnt is same-team) and TFL_DMG_HEADSHAKE now applied in EventDamage (`TurretAI.cs:887-891`). MakeParams/Think pass `fireCheckFlags=FireCheckDefault|FireCheckAff`; Spawn passes `headShake:true`. |
| damage.gate_retaliate | logic | faithful -> faithful | Headshake sub-feature constant went match:false -> match:true: EventDamage now perturbs `TurretState.HeadAngles` X/Y by `(Prandom-0.5)*take`, matching turret_damage:228-233 (post-friendlyfire-scaled value). Dimension was already faithful; gap text/constant corrected, gap now presentation-only. |

### turret-plasma_dual

| feature | dimension | was -> now | reason |
| --- | --- | --- | --- |
| damage.headshake | logic | missing -> faithful | Headshake ported. `TurretAI.EventDamage` (886-891) applies `stHit.HeadAngles.X/Y += (Prandom.Float()-0.5f)*take` when HeadShake is set, bit-faithful to sv_turrets.qc:226-234. Baseline recorded port_refs:[] / all-missing. |
| damage.headshake | values | missing -> faithful | Jitter magnitude uses post-friendlyfire-scaled `take * (random-0.5)` on both axes, matching Base which uses the post-FF-scaled damage. |
| damage.headshake | liveness | na -> live | `TurretSpawn.Init` plumbs `headShake:true` into `TurretState.HeadShake` and `PlasmaDualTurret.Spawn` passes `headShake:true`; the jolt runs on the live GtEventDamage -> EventDamage path. Baseline had liveness:na. |
| presentation.head_frame_anim | logic | missing -> faithful | Head-frame wheel ported on `Entity.Frame`: Think runs `if (Frame!=0 && Frame!=3) Frame+=1; if (Frame>6) Frame=0` (== plasma_dual.qc tr_think) and Attack does the SUPER frame=1 start-kick (non-instagib only) + unconditional ++Frame (== tr_attack). Baseline had port_refs:[] / all-missing. |
| presentation.head_frame_anim | values | missing -> faithful | Frame cycle 0..6 idling at both 0 and 3 matches Base exactly (distinct from single plasma 0..5). |
| presentation.head_frame_anim | timing | missing -> faithful | tr_think advance+wrap runs every think and ++frame on each fire, matching Base cadence (server-authoritative `Entity.Frame`). |
| presentation.head_frame_anim | liveness | na -> live | The Frame wheel runs live in `PlasmaDualTurret.Think/Attack` each frame. Baseline had liveness:na. |

### turret-tesla

| feature | dimension | was -> now | reason |
| --- | --- | --- | --- |
| firecheck.custom | values | partial -> faithful | Scan-throttle constants targetscan_mindelay(0.1) / targetscan_maxdelay(1) — previously match:false (not applied to Tesla) — now ported via cvar-backed `TurretAI.Cvar("g_turrets_targetscan_mindelay/maxdelay")` and gate the rescan in `TeslaTurret.Think:83-110`, matching Base turret_tesla_firecheck exactly. |
| firecheck.custom | timing | partial -> faithful | Previously SelectTarget ran unconditionally every think (no scan cadence). Port now replicates Base do_target_scan timing: force scan every maxdelay, suppress under mindelay, re-select only on maxdelay-elapsed OR held-target-invalid, stamping TargetSelectTime. Cadence now matches Base. |

### turret-walker

| feature | dimension | was -> now | reason |
| --- | --- | --- | --- |
| setup.tr_setup | values | partial -> faithful | First-spawn ground-snap home pose ported: `WalkerTurret.Spawn` (111-121) tracebox-drops the map origin to the floor (origin+'0 0 128' down 10000 with mins/maxs, then origin = endpos+'0 0 4') before switching to MOVETYPE_STEP and stores it as home pose, mirroring Base tr_setup. The 'no ground-snap' gap is removed. |
| weapon.rocket | logic | partial -> faithful | walker_rocket_loop up-and-over maneuver ported in GuidedProjectile (WalkerRocketLoop/Loop2/Loop3, 366-415) with the 1% launch roll (Launch:142), the 1%/in-flight-think roll + InLoop/1337 sentinel (WalkerRocketThink:346), and the +300/back-to-start/-200 waypoint chain faithful to walker.qc:153-202. Previously only the straight steer-pull existed. |
| weapon.rocket | presentation | missing -> partial | te_explosion rocket-launch flash now emitted server-side in FireRocket (425, `EffectEmitter.TeExplosion(st.ShotOrg)`) as a networked temp-entity. Only the tag_rocket01/02 muzzle alternation and EFFECT_BLASTER_MUZZLEFLASH remain unported. |
| client.draw | presentation | missing -> partial | The low-hp te_spark (the networked, viewer-independent part of walker_draw: <127 hp at 15%/frame, te_spark(origin+'0 0 40', randomvec*256+'0 0 256', 16)) now emitted server-side in DoMovement (236-242), faithful to walker.qc:627-629. Ground-align/head-spin/anim-frames still absent. |
| client.draw | logic | missing -> partial | Spark decision logic (health threshold + per-frame chance roll) now present server-side; the gait animation-frame state machine (turrets_setframe) still missing. |
| client.draw | values | missing -> partial | Spark constants now match Base: threshold=127, chance=0.15, TE offset/velocity/count. groundalign4point/client-movetype/anim-frame constants still unported. |
| client.draw | timing | missing -> partial | Per-frame spark roll now runs on the live think cadence; TNSF_MOVE/anim-frame timing remains unported. |
| client.draw | liveness | na -> partial | Part of walker_draw (the te_spark) now live on the server think path; the rest (ground-align/head-spin/anim frames) has no code. Liveness upgraded na -> partial. |

### vehicle-bumblebee

| feature | dimension | was -> now | reason |
| --- | --- | --- | --- |
| gunner.frame | presentation | missing -> partial | Wave 7 (commit 8cb57e3, post-baseline) wired the gunner cockpit HUD and both gunner aux crosshairs live. GunnerFrame writes VehGunnerLeadPoint/Valid + VehGunnerHitPoint/Valid (Bumblebee.cs:605/648) and mirrors gunner.VehicleHealth/Shield/Ammo1/Energy (632-636); NetGame.UpdateVehicleHud selects BumblebeeGun for a seated gunner (3001) and projects the magenta '1 0 1' LEAD + reload-color READY crosshairs, plus FeedPilotGunnerAux mirrors them onto the pilot. Baseline: both NOT IMPLEMENTED. Only cosmetic gun cockpit models remain. |
| gunner.frame | logic | faithful -> faithful | Logic stays faithful but the recorded freezetag-only GAP is closed: the FROZEN cannon-lock guard (bumblebee.qc:113/119) ported at Bumblebee.cs:585 (drop locked-frozen enemy) and :589 (skip frozen candidate) via `IsFrozen()`. Cannon no longer locks onto a frozen enemy. |
| pilot.raygun | logic | faithful -> faithful | Logic stays faithful but the recorded freezetag-only GAP is closed: the FROZEN heal-lock guard (bumblebee.qc:520/526) ported at Bumblebee.cs:367/371. Heal beam no longer locks onto / heals a frozen teammate. |
| client.presentation | presentation | partial -> partial | Still partial overall, but the gunner-side scope advanced: GUNNER cockpit HUD (BumblebeeGun art, NetGame.cs:3001) + both gunner aux crosshairs now live on the host path, on top of the Wave-5 pilot HUD. Residual: networked BRG beam, cosmetic models, describe text, 'No right/left gunner!' prompts, vCROSS_HEAL pilot pointer. |
| client.presentation | liveness | partial -> partial | Liveness already partial; gunner-side HUD/crosshair paths that were dead in the baseline are now reached live (UpdateVehicleHud isGunner branch). Remaining dead/missing scope shrank; partial retained but the live surface grew. |

### vehicle-framework

| feature | dimension | was -> now | reason |
| --- | --- | --- | --- |
| enter.steal | logic | partial -> faithful | Wave 7 ported the teamplay enemy-board branch (VehicleBoarding.cs:212-225): a different-team player now boards a team vehicle when g_vehicles_steal is on, zeroing the shield, backing up old_vehicle_flags, stripping VHF_SHIELDREGEN (restored on exit, VehicleCommon.cs:297) — faithful to sv_vehicles.qc:960-979. |
| enter.steal | values | partial -> faithful | g_vehicles_steal default now 1 (Cvar fallback 1f) matching vehicles.cfg:5 / sv_vehicles.qh:11; shield=0 + flag backup/strip values match Base. |
| enter.steal | liveness | partial -> live | The steal enemy-board path now reaches the authoritative steal handling on the live boarding path instead of only the refuse fallback. |
| aim.locktarget | logic | partial -> faithful | Target-validity gate now includes the QC IS_VEHICLE\|\|IS_TURRET and alpha<=0.5&&!=0 rejections (VehiclePhysicsHelpers.cs:288-291 + new IsVehicleOrTurret helper :354), bit-faithful to sv_vehicles.qc:120-125 — the homing lock can no longer acquire a player or any other live entity. |
| respawn.return_waypoint | logic | partial -> faithful | Abandoned-living return now wired (VehicleBoarding.Exit:307 calls SetReturn), so both the death path and the pilot-quit path schedule the return, matching Base vehicles_exit's vehicles_setreturn tail. |
| respawn.return_waypoint | values | partial -> faithful | Return-delay constant now matches Base exactly: respawntime-5 (dead) / respawntime-1 (alive) at VehicleCommon.cs:829, vs the baseline's plain respawntime. |
| respawn.return_waypoint | timing | partial -> faithful | The return Think is re-armed with the correct Base delay on both the dead and alive paths. |
| respawn.return_waypoint | liveness | partial -> live | The living-vehicle return is now reached on the live exit path (was only the death path before). |
| combat.regen | liveness | live -> live | No liveness change, but the owner-mirror gap closed: Raptor (shield+energy, Raptor.cs:264-265) and Spiderbot (shield+ammo1, Spiderbot.cs:276-277) now mirror their full scalar-pool stats onto the pilot — all four vehicles mirror the full stat set (previously Raptor/Spiderbot mirrored only health). |
| presentation.hud_camera_xhair | logic | partial -> faithful | Raptor + Spiderbot now mirror shield/energy/ammo onto the pilot, so the in-vehicle HUD shows correct shield/energy/ammo for all four vehicles — the 'Raptor/Spiderbot show only health' gap is closed. |
| exit.handshake | logic | faithful -> faithful | No dimension flip (already faithful), but a recorded gap closed: VehicleBoarding.Exit:307 now calls VehicleCommon.SetReturn for the abandoned-living vehicle (baseline: only reached via the death path). |
| init.initialize | logic | partial -> partial | The drop-to-floor downward tracebox now ported (VehicleSpawnFuncs.cs:56-69), closing one of three init sub-gaps; stays partial because delayspawn jitter, the targetname/ACTIVE controller, and the g_vehicles_teams clear-branch remain missing. |

### vehicle-racer

| feature | dimension | was -> now | reason |
| --- | --- | --- | --- |
| spawn.lifecycle | values | partial -> faithful | bouncefactor 0.25 / bouncestop 0 / mass 900 now applied in Spawn (Racer.cs:201-203); baseline recorded them as 'not set'. Only cosmetic scale 0.5 remains. |
| board.usekey | logic | partial -> faithful | CTF flag-carrier reposition to -190 0 96 now LIVE via Ctf.Tick (FlagVehicleCarryOffset, Ctf.cs:1448-1454) when carrier.Vehicle != null; baseline had only a TODO. Exit oldvelocity + Think oldorigin also modeled (the old 'oldorigin' gap was a misattribution). |
| drive.hover4point | logic | partial -> faithful | Crouch-dive water up-push 30->200 now gated on `ButtonCrouch && Time < VehAirFinished` (Racer.cs:565), matching the Base racer_air_finished air-meter; baseline keyed it on crouch alone. |
| drive.controller | logic | partial -> faithful | racer_watertime 3s post-water downforce ramp (Time - VehWaterTime <= 3f, Racer.cs:485-487) and the 5s racer_air_finished air-meter (VehAirFinished armed 371-374) now both modeled; baseline had only TODO markers. |
| drive.controller | values | partial -> faithful | water_time 5 and the 3s watertime window now applied as real values via VehAirFinished / VehWaterTime; baseline recorded both as 'not modeled'. |
| drive.afterburn | audio | partial -> faithful | Engine-sound state machine (racer_move 10.922667s / racer_idle 11.888604s loop swap gated on VehSoundNextTime/VehSoundState) now ported, and the boost sound gated to its 10.92s loop via VehBoostSoundTime with Sound.Stop on release (Racer.cs:417-477); baseline had the boost replayed per-tick and the move/idle loop absent. |
| drive.afterburn | timing | partial -> faithful | Boost-sound cadence now gated to the 10.922667s loop length (VehBoostSoundTime) rather than re-triggered every afterburn tick. |
| weapon.cannon | logic | partial -> faithful | FireCannon now fires from a fixed tag_fire1 with no tag alternation (Racer.cs:694), matching the Base piloted W_SetupShot_Dir path (alternation is bot-only in Base); baseline flagged unconditional tag_fire1/tag_fire2 alternation. |
| weapon.rocket | logic | partial -> faithful | GuideRocket RacerHoming now traces ahead (origin -> origin + fwd*64 - '0 0 32') and lifts newDir.Z += 16f*frametime when the ahead-trace hits a non-target (VehiclePhysicsHelpers.cs:403-418); baseline recorded the obstacle-avoid traceline + z-lift as omitted. |
| weapon.rockethud | logic | missing -> faithful | player.VehAmmo2 now set 100/50/0 across the rocket pair (Racer.cs:510-536); baseline had it missing on all dimensions. |
| weapon.rockethud | values | missing -> faithful | vehicle_reload2 = Bound(0, 100*(Time-VehReloadStart)/(VehWeaponDelay-VehReloadStart), 100) now written (Racer.cs:539-542) with VehReloadStart (.lip) stamped on pair-complete; fields exist on EntityVehicleStateExtra. |
| weapon.rockethud | timing | missing -> faithful | The reload-bar progression from .lip (pair-complete) to .delay (next allowed shot) now produced each tick; baseline had timing missing. |
| weapon.rockethud | presentation | missing -> partial | Authority-side ammo2/reload2 stats now written onto the pilot; only the client HUD progressbar that reads them remains unwired, so this dimension moves missing -> partial rather than fully faithful. |
| weapon.rockethud | liveness | na -> live | The feature is now implemented and runs each tick in the piloted Frame, so liveness moves na -> live. |

### vehicle-spiderbot

| feature | dimension | was -> now | reason |
| --- | --- | --- | --- |
| impact.bouncepain | logic | missing -> faithful | Spiderbot.Impact override (Spiderbot.cs:155-165) now ports vr_impact: guards on bouncepain != Vector3.Zero with the stock '0 0 0' default hardcoded, so vehicles_impact is skipped — a faithful no-op matching QC's falsy-vector guard. Baseline recorded port_refs:[] / logic:missing. |
| impact.bouncepain | liveness | na -> live | VehicleCommon.Touch dispatches `vehic.VehicleDef?.Impact(vehic)` on the live touch path (VehicleCommon.cs:769, mirroring QC info.vr_impact when PlayTime<time), so the now-existing Impact override is reachable in a normal match. |
| spawn.setup | values | partial -> partial | pushable now modeled: Spawn sets vehicle.PushableFlag=true (Spiderbot.cs:136) and MapObjectsCommon.IsPushable checks it FIRST before the IS_VEHICLE exclusion (MapObjectsCommon.cs:190), matching QC vr_setup instance.pushable=true + isPushable (triggers.qc:5). Stays partial only because mass=5000 remains unmodeled. |

## Reclassified

Neutral re-classifications (no parity change; baseline status was corrected after verification).

| unit | feature | dimension | was -> now | reason |
| --- | --- | --- | --- | --- |
| turret-mlrs | damage.lifecycle | audio | unknown -> partial | Verified: Die() plays SND_ROCKET_IMPACT (weapons/rocket_impact.wav) only on the NORESPAWN branch; the normal respawning-death path plays no death sound. So death audio is partially present, not unknown. |
| turret-plasma_dual | damage.headshake | timing | na -> faithful | Now applies per-hit on the live damage path (each EventDamage), as in Base turret_damage; previously na because unimplemented. |
| turret-plasma_dual | damage.gating_die_respawn | timing | unknown -> faithful | Net respawn timing verified faithful: Base turret_hide schedules at time+0.2 then turret_respawn at respawntime-0.2 = total 60s, equal to the port's direct Respawn at now+60. The remaining -0.2 EF_NODRAW hide-frame is a presentation detail (already in gaps), not a timing divergence. |
